# Decisions Register

Product and architecture decisions with dates. Architectural decisions with lasting consequences also have ADRs (`docs/architecture/adr/`). All decisions below were confirmed on adoption of the revised plan unless otherwise dated.

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-001 | 2026-07-30 | Revised plan supersedes the earlier agent-drafted plan | User-issued revision adopted verbatim as execution baseline; recorded as CR-001 in the change-request register |
| D-002 | 2026-07-30 | Product name: **Healthy360** | Plan §2 |
| D-003 | 2026-07-30 | Identifiers: PHP namespace `Healthy360\`, npm scope `@healthy360/*`, bundle IDs `com.healthy360.*` | Plan §2 |
| D-004 | 2026-07-30 | Launch markets: Lebanon, UAE, Saudi Arabia, Qatar, Kuwait, Bahrain, Oman, Jordan, Egypt | All ISO countries seeded; only launch set marked active (Plan §2) |
| D-005 | 2026-07-30 | Initial languages: English and Arabic (full RTL) | French and others capable of later addition (Source §6; Plan §2) |
| D-006 | 2026-07-30 | Laravel modular monolith; microservices prohibited this phase | ADR-0001 |
| D-007 | 2026-07-30 | Shared PostgreSQL database with organisation isolation | ADR-0002 |
| D-008 | 2026-07-30 | Global user identity with organisation memberships | ADR-0003 |
| D-009 | 2026-07-30 | One universal Expo codebase (web, iOS, Android); build families; role areas | ADR-0004 |
| D-010 | 2026-07-30 | OpenAPI 3.1 spec-first; single generated TypeScript client via wrapped hey-api | ADR-0005 |
| D-011 | 2026-07-30 | Custom membership RBAC; spatie/laravel-permission rejected; allow-only in Phase 1 | ADR-0006 |
| D-012 | 2026-07-30 | Incremental RLS: app scoping first, six representative tables, three DB roles, no BYPASSRLS app role | ADR-0007 |
| D-013 | 2026-07-30 | PHP 8.4 + Pest 5 ("PHP 8.3 or later" mandate satisfied by 8.4) | ADR-0008 |
| D-014 | 2026-07-30 | Node 24 LTS + Expo SDK 57 + TypeScript 6; TS 7 explicitly not adopted | ADR-0009 |
| D-015 | 2026-07-30 | Laravel filesystem abstraction over any S3-compatible store; Garage v2 for local dev (MinIO community archived) | ADR-0010 |
| D-016 | 2026-07-30 | Frontend repository boundary; mock mode impossible in production (4 gates) | ADR-0011 |
| D-017 | 2026-07-30 | Offline persistence restricted to non-sensitive reference data | ADR-0012 |
| D-018 | 2026-07-30 | Adopt the generated Laravel 13.23 app into `apps/api/`; do not regenerate | Plan §5; Phase 0 complete |
| D-019 | 2026-07-30 | Retain Fortify, convert to JSON responses under `/api/v1/auth/*`; remove Inertia frontend and starter-kit web assets | Plan §5, §13 |
| D-020 | 2026-07-30 | Step-up authentication returns HTTP 403 with `auth.step_up_required` (not 423) | Plan §13; `docs/api/conventions.md` |
| D-021 | 2026-07-30 | Passkeys may remain disabled in the UI even where Fortify installs supporting dependencies | Plan §13; enablement is OQ-004 |
| D-022 | 2026-07-30 | InterNACHI Modular adopted only after the compatibility spike passes; fallback is `app/Modules/` namespaces | Plan §6 |
| D-023 | 2026-07-30 | UUIDv7 primary keys, application-generated via central identifier service; ISO codes for stable reference data | Plan §8 |
| D-024 | 2026-07-30 | Dependency versions governed by one compatibility matrix, lockfiles and controlled ranges — no per-patch ADRs | Plan §3; `docs/architecture/dependency-compatibility.md` |
| D-025 | 2026-07-30 | No audit-log partitioning until volume/retention confirmed | Plan §12; risk R-007 |
| D-026 | 2026-07-30 | Latin digits as configurable default for clinical/financial numerals in Arabic locales | Provisional product default pending management sign-off (A-002, OQ-001) |
| D-027 | 2026-07-30 | Prompt 2 prototype IA folds into the existing `public` and `customer` route areas; no new RouteArea values, no permission-kernel changes | Plan §1; new `(marketplace)` route group under `public`; B2B/professional surfaces reuse the existing `corporate`/`partner`/`dietitian` areas |
| D-028 | 2026-07-30 | One no-dead-controls mechanism: `usePrototypeAction` (honest "nothing was changed" notice + dev-only contract line) reserved for genuinely absent capabilities; everything the mock world supports mutates the `PrototypeStore` for real | Plan §5; `apps/universal/src/prototype/prototype-action.ts`; enforced by the prototype-action e2e sweep |
| D-029 | 2026-07-30 | Nutrition targets computed only from cited public formulae (Mifflin–St Jeor, Katch–McArdle when body fat is present, FAO/WHO/UNU activity multipliers, IoM AMDR bands); every engine result carries `prototype: true`; no competitor formula claimed or reverse-engineered | `packages/nutrition` MockNutritionTargetEngine; reference-research boundary (docs/reference-research) |
| D-030 | 2026-07-30 | Fixture currency policy: **USD** default across consumer fixtures and the Verdant demo tenant (supersedes the earlier AED default); exactly one SAR B2B price proves multi-currency handling; cross-currency summation is forbidden and test-pinned | `mock/prototype/constants.ts` `DEFAULT_CURRENCY`; `DemoTenantSeeder`; asserted in business feature tests |
| D-031 | 2026-07-31 | Visual-regression baselines are authoritative only from the pinned `mcr.microsoft.com/playwright:v1.62.0-noble` container; host-local visual runs skip with an explicit message | Wave 5B; `e2e:visual` root scripts; frontend.yml visual job |
| D-032 | 2026-07-30 | Query persistence limited to `reference` and `catalogue` roots; planner, nutrition, VD, commerce and business data stay memory-only in the prototype | `PERSISTABLE_QUERY_ROOTS`, pinned by test (plan §21) |
| D-033 | 2026-07-31 | Quotation submission is a real store mutation, not a prototype notice: `PrototypeStore.requestQuotation` validates minimums and files a real `submitted` quotation, so an honest notice would be a lie | Wave 5A applying D-028's own rule; draft persistence, accept/decline and document export remain notices (OQ-023) |
| D-034 | 2026-07-31 | Original brand palette — fresh leafy-green primary, warm-terracotta accent, warm-greige neutrals — designed for this product and not derived from any reference brand; both light and dark themes flow through the `@healthy360/design-tokens` pipeline only (no raw hex in components), and every foreground/background pair is contrast-gated at WCAG AA (body text ≥4.5:1; UI, focus rings and nutrition fills ≥3:1) in both themes | Visual-theme pass; `packages/design-tokens/src/colour.ts`, enforced by `colour.test.ts`; nutrition meters keep pattern + numeric-label redundancy so colour is never the sole carrier of meaning |
| D-035 | 2026-07-31 | Prototype imagery is bundled, same-origin, licence-safe stock photography (Unsplash Free License), never hotlinked: a matched photo per dish/kitchen/plan/dietitian/diet-hub and per landing/discover/how-it-works/for-business slot, resolved from a generated manifest (`build:images`, keyed by the fixture `imagePlaceholderId`) with the generated pattern placeholder as fallback; 40 meals reuse 20 dish photos. Fixtures stay labelled synthetic — the photos are illustrative, not photographs of real Healthy360 products — with per-file attribution in `apps/universal/assets/images/CREDITS.md`. The export byte budget was re-baselined (+15% headroom) for the added imagery | Visual-theme pass; enforced by the `no-external-requests` e2e gate and `apps/universal/scripts/check-export-budget.mjs` |

## Cross-phase architecture decisions (Order-Kitchen programme, phase P0)

D-036 to D-053 are the §4 decisions of the Order-Kitchen master plan v2, frozen before any domain migration exists and binding on every phase of that programme. They renumber the provisional D-036+ identifiers that the three design reports proposed independently. Decisions local to a single phase — guest token design, MSA click-wrap, tombstone policy and the like — are recorded by the phase that makes them, not here.

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-036 | 2026-08-02 | Module status vocabulary is `planned` / `foundation` / `active` / `deprecated` / `retired`; the eleven platform modules become `active` | master plan v2 §4.1; `docs/architecture/module-registry.yaml`, enforced by `apps/api/app-modules/support/tests/ModuleRegistryTest.php`. A status change and the architecture test that enforces it land in the same commit, always |
| D-037 | 2026-08-02 | Recipe outputs are modelled by `recipe_version_outputs` (version-specific yields, several outputs, one primary); `ingredients.produced_by_recipe_id` is dropped from the design | master plan v2 §4.2; an intermediate is simply an ingredient that appears in some version's outputs — this removes the circular FK migration and the fabricated stub-recipe special case |
| D-038 | 2026-08-02 | Plan durations use `duration_kind` (`one_off` or `fixed_days`) with a nullable `duration_days` and a CHECK, not a zero-day sentinel row | master plan v2 §4.3; widens the `PlanDurationOption` wire contract (supersedes design-report OD-3) |
| D-039 | 2026-08-02 | Two monetary representations: commercial prices are `amount_minor bigint` plus `currency_code`; calculated recipe costs are `decimal(18,6)` in **major** currency units plus `currency_code`. Every value carries exactly one currency; no cross-currency arithmetic and no summing cost columns into price columns | master plan v2 §4.4; extends D-030. "Decimal minor units" is never used as a description. Both prohibitions are architecture-tested from K1.3 |
| D-040 | 2026-08-02 | `measurement_units` gains a `dimension` (mass, volume, count, serving, package); automatic conversion happens only within a dimension, and pack equivalences (bottle or gallon content) are product data on catalogue-item pack variants | master plan v2 §4.5 |
| D-041 | 2026-08-02 | Allergen governance: canonical codes are immutable regulatory identities (code primary key, deactivate only — never rename or delete); the platform owns display names, Arabic, market applicability, thresholds and notes; kitchens own mappings, containment, verification source and review requests | master plan v2 §4.6; the fourteen EU-14 codes match the frontend `AllergenCode` union one-for-one |
| D-042 | 2026-08-02 | Publication lifecycle for sellable and publishable records is `draft`, `review_required`, `published`, `retired`; publication is an explicit action gated by a computed readiness evaluator; critical allergen conflicts quarantine the record as `review_required` and block publication structurally; published recipe versions are immutable and changes are new draft versions | master plan v2 §4.7; "publishable" is the evaluator's verdict, never a stored state. Operational records keep the separate `active`/`inactive`/`archived` family |
| D-043 | 2026-08-02 | Every anonymous or customer endpoint serialises through an explicit PublicProjection presenter, against a named denylist: recipe lines, raw-material quantities, unit costs, cost snapshots, waste coefficients, supplier identities, margins, data-quality notes, document paths, KYC, signature evidence, review commentary and internal recipe ids the consumer contract hides | master plan v2 §4.8; automated literal and key-denylist sweeps run across the whole anonymous surface in every phase that adds a public endpoint |
| D-044 | 2026-08-02 | Sensitive entities take explicit nullable owner foreign keys plus an exactly-one CHECK — never `owner_type`/`owner_id`. A phase that adds an owner kind widens the CHECK additively | master plan v2 §4.9; applies to `contact_points`, `otp_challenges` and uploaded documents |
| D-045 | 2026-08-02 | `users.email` stays the canonical authentication identity; `contact_points` own notification destinations and verification state. One write path mirrors the login email and a drift test pins the two together | master plan v2 §4.10; Fortify is unchanged. Login by phone would be an identity migration and is out of scope |
| D-046 | 2026-08-02 | Exactly three data-loading mechanisms: committed platform reference seeders, committed synthetic fixtures (no real formulations or costs), and a private Healthy360 kitchen workbook importer reading an out-of-repo source path. Confidential source data is never committed to the repository | master plan v2 §4.11; `php artisan kitchen:import-workbook --source=<private path>` — manifest and sha256 checksums, dry-run, counts, unresolved-alias, data-quality, allergen-review and quarantine reports, insert-if-absent so operator edits are never overwritten, idempotent, environment-allowlisted, audited. Supersedes the committed technical-sheet fixtures of plan v1 |
| D-047 | 2026-08-02 | Every table names exactly one isolation strategy from a fixed vocabulary of seven: `platform-public-ref`, `org-rls`, owner-RLS (`user-owner-rls` / `customer-owner-rls`), `join-rls-parent`, `platform-only`, `capability-token`, `append-only-ledger`. "Application scoped" is not an isolation strategy | master plan v2 §4.12; extends ADR-0007. RLS tests run under the non-owner role (`rls` group, `SET ROLE`) and extend in every phase that adds a policy |
| D-048 | 2026-08-02 | Optimistic locking: management single-resource GETs return `ETag: "<lock_version>"`, writes on lock-versioned resources require `If-Match`, a missing precondition answers HTTP 428 and a stale one answers the existing 409 `resource.conflict` | master plan v2 §4.13; `docs/api/conventions.md`. The 428 code is introduced with its first consumer (K1), not before |
| D-049 | 2026-08-02 | Idempotency: the `EnforceIdempotency` middleware over the existing `idempotency_keys` table replays the original envelope for a repeated key, and answers 409 when a key is reused with a different request fingerprint | master plan v2 §4.14; introduced with its first consumer (C1 `POST /orders`, then B1 provisioning, G1 conversion, K1 import commit). Until then no implemented endpoint accepts `Idempotency-Key` |
| D-050 | 2026-08-02 | Lifecycle transitions are POST sub-resource actions (`…/publish`, `…/retire`, `…/approve`) with their own permission and their own audit action — never `PATCH status` | master plan v2 §4.15; `docs/api/conventions.md` |
| D-051 | 2026-08-02 | The permission registry splits into organisation-scoped and platform-scoped sets; organisation template roles, `organisation_owner` included, are built only from the organisation set | master plan v2 §4.16; `PermissionRegistry::organisationPermissions()` and `platformPermissions()`, regression-tested by `apps/api/app-modules/access-control/tests/PermissionRegistryTest.php`. The platform set starts empty; permissions and template roles arrive with the phase that uses them. Closes plan-v1 finding R-017 |
| D-052 | 2026-08-02 | Error codes stay additive within `/api/v1`: each phase adds only the codes its own endpoints raise, with the `ErrorCode` enum, the OpenAPI document and `docs/api/conventions.md` changed in the same commit | master plan v2 §4.17; the existing enum-to-spec parity test enforces the pairing |
| D-053 | 2026-08-02 | i18n: English and Arabic always, with all six Arabic plural forms; French deferred (OQ-007); legal and regulatory text is authored in each language, never machine-translated; entity names are bilingual columns server-side, the consumer wire carries one server-localised `name`, and the admin wire carries both and ignores `Accept-Language` for them | master plan v2 §4.18 |

## Phase K1 — decisions local to the kitchen catalogue slices

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-054 | 2026-08-02 | The private Healthy360 kitchen workbook importer **reports and continues** on an unresolved raw-material designation, instead of failing loudly and abandoning the file | K1.8. This is a deliberate, single-scoped deviation from the programme's fail-loudly-abort default. §4.11 asks the importer for created / skipped / failed counts, and a count of failures only means something if a run survives one. So an unresolved designation costs its **own recipe line**, is reported by name with every sheet and row it appears on, and flags its sheet incomplete; the remaining lines and the other twenty-eight sheets import normally. It never fabricates an ingredient and it never silently skips a file. The alternative — one unknown spice discarding a whole correctly-read workbook — would have produced an operator fixing one dictionary entry per run for a fortnight |
| D-055 | 2026-08-02 | Designation resolution is **curated only**: a human-authored dictionary, then tenant ingredients, then the platform library. No fuzzy matching, edit distance, plural stripping or transliteration exists anywhere in the import path | K1.8; `app-modules/ingredients/database/data/kitchen-workbook-aliases.json`. No string metric separates "Cripsy Spice → Crispy Spice" (a typo) from "Sweet Paprika → Paprika" (a different product at a different price), and the failure mode of getting it wrong is a wrong allergen label rather than a wrong report. The dictionary also records the merges a curator **refused** (`never_merge`) and the product-to-recipe links a curator refused (`recipe_links_declined`), so a later reader sees a decision rather than an omission (risk R4) |
| D-056 | 2026-08-02 | A bare integer in a technical sheet's "Quantity Produced" cell is disambiguated by the sheet's **own cost-block wording**, and the ambiguity is reported every time | K1.8. Six sheets write a yield as a whole number with no unit. A fraction cannot be a piece count and `130 (7 kg)` states both, but `97` and `80` are genuinely ambiguous, and the sheets themselves disagree — sheet 11 claims a per-kilogram cost over a "yield" larger than its own input total. So the *transcription* consults the label once, emits `yield_shape_ambiguous` naming the sheet and the label that decided it, and everything downstream keeps the programme rule that a cost basis is derived from the version's yield fields and never from a label (appendix D findings #1/#2) |
| D-057 | 2026-08-02 | The sauce and dressing sheets import as **indicative recipes with no lines** — the "typical ingredients" prose is kept verbatim in `notes`, and only the declared allergen rows become data | K1.8. The source describes those columns as "typical Lebanese/Mediterranean formulations — validate against the kitchen's actual recipes before use". Splitting disclaimed prose on commas and calling the result a formulation would manufacture quantities nobody stated. The allergen columns are a real human statement — a class and the ingredient that triggers it — so they become `recipe_version_allergens` rows with `derivation = declared`, which a later recomputation may strengthen and may never weaken |
| D-058 | 2026-08-02 | The importer's **dry run executes the whole write path inside a transaction it always rolls back** | K1.8. Counting what would be created without doing it needs a second implementation of every insert-if-absent rule in the importer, and the day the two disagree is the day the dry run stops being worth running. Executing and rolling back makes "would create" mean exactly "did create, and then did not"; a test asserts row counts are identical either side of a dry run |
| D-059 | 2026-08-02 | The importer lives in the **Kitchens** module and that module's declared dependencies widen to the whole K1 family | K1.8. Catalogues could not host it: Pricing depends on Catalogues, so a Catalogues → Pricing edge would close a cycle the architecture test rejects. Nothing that may hold code depends on Kitchens, so widening it stays acyclic — and standing a kitchen up from its own workbook is what a kitchens module is for. `docs/architecture/module-registry.yaml` and `ModuleRegistryTest` change in the same commit as the code |

## Integration wave — decisions local to wiring the committed domains to HTTP

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-060 | 2026-08-02 | Domain exceptions declare their own wire shape through a `ProvidesApiError` interface in Support, rather than being mapped inside `ApiExceptionRenderer` | The domains (Verification, Customers, B2B) were built ahead of their HTTP surface and carry stable *reason strings* rather than wire codes, which was right — they did not own the vocabulary. Mapping those reasons in the renderer would have made Support import Verification, Customers and B2B, inverting the dependency every other module respects and making the one class every failure passes through depend on every domain that can fail. The interface keeps Support pointing only downwards, and a module that adds a refusal declares what it means on the wire in the same file. It is not a licence to build an error body: the return type is `ApiError`, so the code still comes from the shared enum and the correlation identifier is still added where it cannot be forgotten |
| D-061 | 2026-08-02 | The idempotency guarantee lives in **two** layers on purpose: `EnforceIdempotency` middleware and `OrderIdempotency` service | §4.14. Only the middleware can *replay* a response, because by the time a service returns the response is already gone; only the service holds for an order placed by a queued job or a console command, which never passes through a middleware at all. Neither subsumes the other, and the duplication is the smaller cost |
| D-062 | 2026-08-02 | `idempotency_keys.user_id` becomes nullable beside `customer_account_id`, with the unique key rebuilt over `COALESCE(user_id, customer_account_id)` | C1 shipped an honest hole — a guest had no user, so the service returned "unprotected" and let the order through with no guard. That was the wrong population to leave out: a guest checking out on a phone is exactly who double-taps. The coalesce is safe as a key expression only because a CHECK guarantees exactly one subject is set |
| D-063 | 2026-08-02 | `AgreementService::sign()` now **requires** a consumed `b2b_signatory` OTP challenge belonging to the signing user; `SigningEvidence::$otpVerified` can only be set by the service, never asserted by a caller | B1 recorded the challenge id unchecked and said so, because the verification module was being built in parallel. It is built. The column gains a real FK with `ON DELETE RESTRICT` — evidence a purge job can delete is not evidence — and the four failure modes (absent, wrong purpose, unconsumed, wrong subject) answer with one code and no detail about which, so a caller holding somebody else's challenge id learns only that it did not work. This is still **not** a qualified electronic signature (INT-007) |
| D-064 | 2026-08-02 | `OtpPurpose::isIssuable()` opens to all six purposes, and a new `grantsStepUp()` narrows which of them may stamp an `otp` step-up | The gate exists to stop a purpose being issued before the consumer that honours it exists. All six consumers now exist. The step-up replay risk the purpose column was built to prevent moves to `grantsStepUp()`, which admits only `closure_step_up` and `payment_details_step_up`: a contact-verification code must never unlock a payment change |
| D-065 | 2026-08-02 | `contact_points` gains `b2b_application_id` as a third owner kind rather than B2B growing its own contact table | §4.9, widening a named CHECK additively exactly as that clause anticipated. A B2B application names a signatory who may hold no account at all, and their destination still needs normalisation, hashing, verification state and a retire path. A separate table would have produced a second, subtly different implementation of all of it. The verified-value unique index is untouched and stays global across owner kinds, because "one proven holder of an address" is a statement about the address |
| D-066 | 2026-08-02 | `organisation_invitations` gets **no** row-level security policy, and the decision is recorded rather than deferred again | The management surface is organisation-scoped and could carry an `org-rls` policy, but the accept path resolves an invitation by *hashed token* before the acceptor is a member of anything — an org-match policy would break it, and a token-hash session variable would be a weaker mechanism than the token itself. Access is therefore service-mediated and token-gated. `RlsTest` pins the protected set, so this table's absence from it is a stated choice rather than an omission |
| D-067 | 2026-08-02 | `GET /reference/delivery-areas` makes `country_code` optional, reversing K1.7 | The original argument was right about *defaults* and wrong about *absence*. A client that has not asked for a country has not asked for the wrong one, and the consumer marketplace legitimately wants the whole gazetteer before it knows where the person is. `country_code` is embedded on every row, so an unfiltered page is unambiguous by construction. An unrecognised code is still a 400 |
| D-068 | 2026-08-02 | `allergens.severe_by_default` marks seven classes, and is documented as a **presentation default, never a diagnosis** | Closes the second M1 blocker. The set — crustaceans, egg, fish, peanut, tree nut, sesame, mollusc — matches the fixture the frontend was built against, which is the point: a default the two halves disagree about is worse than no default. The other seven are deliberately unmarked; coeliac disease and a milk allergy can both be extremely serious, what they are not is *reliably* so, and a default that cried wolf would train people to clear it without reading |
| D-069 | 2026-08-02 | The B2B signatory passcode goes to the **authenticated actor's own** contact point, and the application's named signatory address is recorded separately as an application-owned contact | The challenge must carry a subject `sign()` can match, and the only subject that means anything is the person in the session. So the named address is written down as evidence of who was nominated, and the person signing must independently hold it — if they do not, the refusal says the named signatory must sign in and sign for themselves, rather than letting an applicant step up on somebody else's behalf |
| D-070 | 2026-08-02 | `customer_accounts` RLS gains a third arm admitting **ownerless** rows, so the guest journey works at all | J1's policy admits a row belonging to the session's user or organisation. A guest account has neither — both NULL by CHECK — so it matched nothing, and the whole G1 surface would have answered `401 guest.session_invalid` in production while passing every Feature test, because those connect as the schema owner and bypass RLS by ownership. Found by reading the predicates. The rejected alternative was a fourth session variable: the account is discovered *by* validating the token and validating the token reads this table, so the variable cannot be set before the read it is meant to authorise — and the only way round that is publishing the token digest onto a shared database session, which is strictly worse than the token. The arm admits ownerless rows **only**; a registered customer's account still matches their session alone and a corporate account its organisation alone, and `RlsTest` pins both. What it concedes is that guests are isolated from each other in the application layer by their capability token rather than by a predicate — which is the strategy §4.12 names for them and exactly what `guest_sessions`, `carts` and `orders` already rely on, none of which carries a policy at all |

## Integration wave 2 — decisions local to subscriptions, closure and offboarding going live

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-071 | 2026-08-03 | The three tables the final backend wave gave read paths to — `subscriptions`, `account_closure_requests`, `b2b_offboardings`/`record_exports` — take **no** PostgreSQL policy, and the `RlsTest` pin stays at eleven | Decided per table rather than as one answer, and each reason is different. **`subscriptions`**: S1 declared `app-scope` on the C1 `carts`/`orders` precedent — a customer is a member of no organisation, so an org policy hides their own plan from them. A user-arm-via-account subquery would have served `/me/subscriptions`, and it would have made `GenerationService::tick()` read **zero rows** in every scheduler run: the hourly sweep is platform-wide by design, runs as `healthy360_app` with no context published, and would have failed *silently* — the tick would report `generated: 0` and nobody would notice until the food stopped. Making it work would mean rewriting a committed sweep to iterate organisations, to gain database isolation on a table whose sibling `orders` (which holds the delivery address) deliberately has none. **`account_closure_requests`**: a user-owner policy fits `/me/closure-requests/live` exactly and breaks `ProcessScheduledClosures` the same way — the safety net for a delayed job the queue lost is precisely the thing that must not depend on a session. **`b2b_offboardings`, `record_exports`**: B1 proposed `org-rls` and it is the wrong word. The only reader is a platform operator whose `X-Organisation-Id` is the *platform operator's* organisation while the row's `organisation_id` is the corporate customer's, so an org-match policy would hide every row from the one surface that reads it — the D-066 finding again. Their strategy is `platform-only`: no tenant read path exists, and both routes carry `platform.context` plus a platform permission. In all four cases isolation is the application layer, and every read names its scope — `SubscriptionLocator`, `ResolvesClosureRequest`, `ResolvesOffboarding`, and `ScheduleProjection::forOrganisation()` |
| D-072 | 2026-08-03 | `BlockerStatus` gains a fourth case, `advisory`, and `CreditMemoBlocker` is the first to return it | J2's `WalletBalanceBlocker::COVERS` explicitly declined to claim `credit_memos` — customer-held value S1 owns — and raised it for the integration wave rather than converting an open question into a false all-clear. The answer is a blocker over a new `CustomerCreditPresence` port, and it is **advisory rather than blocking**: a memo is a debt the *kitchen* owes the *customer*, so refusing the closure until it is settled would give the party that owes the money a veto over the erasure of the party owed it, and the slower a kitchen paid the longer it could keep somebody's data. It is also unnecessary — `credit_memos.customer_account_id` points at a row closure anonymises rather than deletes, so the obligation and its currency survive intact and stay settleable. `advisory` is a genuine fourth state: `clear` is the absence of a finding and carries no count, `not_applicable` means nothing was checked, and `blocking` would be wrong on the merits. `stopsClosure()` returns false for it; the count reaches the closure screen and the audit trail. Turning it into a refusal is one line and belongs with PAY1 |
| D-073 | 2026-08-03 | No `closure.blocked` error code; a blocked closure is a **200/202 with `blocked: true`** | `account.closure_blocked` was proposed in an earlier wave and never reached the `ErrorCode` enum, which turned out to be right. J2's design is explicit that a blocker is part of the journey — "you have two orders in flight" is information a customer acts on, it comes back inside a successful acknowledgement, and the request *survives* so they can return when the food has arrived. An error code there would turn a checklist into a failure and would force every client to render the blocked case through an exception path. `closure.refused` (409) is added for the things that genuinely are errors: a second request while one is in flight, proof offered for a request awaiting none, a cancellation after there is nothing to cancel, no verified contact to send a passcode to |

## DEC1 — the product owner's decisions written into the database

> **Renumbering note (recorded 2026-08-03, reconciliation sweep).** These three decisions were
> first written by commit `dd77895` as D-071–D-073, and its commit message still says so. Two
> later commits — `7f3bbd8` and `7bf7000` — branched from the same register revision and appended
> their own D-071–D-073, and the merge kept theirs. The DEC1 rows were lost from the register
> while the code and the data they describe stayed in the repository. They are restored here as
> **D-074–D-076**: the integration-wave numbering is load-bearing in code comments
> (`RlsTest`, the offboarding migrations, `ClosureJourneyApiSmokeTest` all cite D-071 and D-073),
> so the newer numbers keep their meaning and the older text takes new ones. The loss itself is
> risk R-028.

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-074 | 2026-08-03 | The 73 undetermined ingredients are resolved by **standard-knowledge determination, recorded as owing a supplier confirmation** — never by inference in code, and never by leaving them blank | Closes OQ-042 as *resolved-pending-confirmation*. D-055 refused to infer an allergen from a name inside the importer and that refusal stands: this is a person deciding, once, in a reviewable file (`app-modules/allergens/database/data/kitchen-workbook-allergen-determinations.json`) that a human can read and a kitchen can argue with. Every mapping lands `verification_status = requires_supplier_confirmation`; where an ingredient carries no class the statement is the *ingredient's* `verification_status = verified` with the reason appended to its notes, because there is no mapping row that can say "assessed and clear". Where the ordinary reading is genuinely uncertain the file says `may_contain` rather than nothing — over-declaring costs a customer a menu option, under-declaring costs them an ambulance. Applied by `kitchen:apply-allergen-determinations`, which is idempotent by comparison and re-derives the affected labels. The same decision resolves the Burghul (IG-025) and Pita bread (IG-143) quarantine in favour of the workbook's own allergen key: gluten `contains`, and the ingredient moves `requires_review → unverified` rather than to `verified`, because the contradiction is settled and the supplier confirmation is not (assumption A-014, risk R-019) |
| D-075 | 2026-08-03 | Plan prices are seeded as **owner-authorised approximations, stamped as such, landed as `confirmed`** so the plans can publish now | Closes OQ-043. The rule: a per-day base of breakfast 6 / lunch 9 / dinner 9 / snack 4 summed over what the combination includes, premium +15%, energy-band scaling from −10% at the lowest band to +10% at the highest, and duration discounts of 0 / 0 / 5 / 10 / 15 per cent. **The duration discount lives on `plan_variant_durations.discount_percent`, never inside the price row** — the per-day list price is the base for every duration, so changing the 60-day discount is one column rather than a re-derivation of 370 assignments. They are `confirmed` because a plan cannot publish over a placeholder and the owner judged publishing now to be worth more than waiting; they are honest about being approximate through `source_ref = owner-approx-2026-08/<variant>` and the explicit expectation that the owner edits them through the price-list administration. The placeholders are **superseded, not deleted**: 74 rows closed with an end date and a pointer to the confirmed row that replaced them, because what a plan was priced at is evidence. The band scaling is implemented but **inert** — the workbook matrix attaches no energy bands to its configurations (OQ-046) — and that is recorded rather than hidden (assumption A-013, risk R-031) |
| D-076 | 2026-08-03 | The three DEC1 commands perform their state transitions **below the HTTP permission stack**, with the environment allowlist as their only authority | They run with no authenticated user because there is none — the operator is at a terminal, which is the arrangement `kitchen:import-workbook` already makes (D-054's context). The consequence that must be stated rather than hidden: `kitchen:publish-ready` does not evaluate `plan.publish_organisation`, so it reproduces `PublishCatalogueItem`'s transition, compare-and-swap and audit event instead of calling it. What it does **not** reproduce is the gate — readiness is asked of the same `CatalogueItemReadiness` and `RecipeVersionReadiness` services the API asks, because two implementations of a food-safety gate would eventually disagree and the disagreement would be discovered by a diner |

## DEC2 — the product owner's decision-sheet answers, 2026-08-07

> **Read I-001 first.** DEC2 was pointed at the live development database and found the
> workbook-import state these decisions are written against **absent**: no recipes at all, no
> `green-life-kitchen` organisation, and none of the imported kitchen-owned ingredients. Only the
> answers whose subject genuinely exists were applied; the rest are recorded as answers, and the
> rows below say which is which rather than implying a write that did not happen.
>
> **Addendum, later the same day — I-001 resolved.** The owner named the workbook source and
> authorised a run. The import was replayed against `green-life-kitchen` (1 810 rows, 0 failed) and
> the three owner-decision commands re-ran on top of it, so D-087, D-090 and D-092 are now applied
> against real data rather than recorded against an absence. **Two things still did not land**, both
> for the same new reason (I-002): the imported organisation has no members, so nobody holds
> `recipe.view_costs_organisation`, and the two writes that need the service layer to accept a costed
> line or a kitchen's own allergen overlay could not be made. D-089 and the GreenLife half of D-091
> say so in their own rows. Nothing was forced past a permission gate to make a row read better.

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-087 | 2026-08-07 | The **approximate plan prices are confirmed as real** by the owner — they are no longer provisional and no longer awaiting replacement | Answers decision-sheet A1, retiring assumption A-013 and risk R-031 *conditionally*. The intended action was to clear any `approximate`/`provisional` data-quality marker from the stored rows; there is none to clear, because the marker D-075 chose was never a status but a `source_ref` stamp, and the environment holds no row carrying it (I-001). Every stored price entry that exists here is already `price_status = confirmed` — 132 of 135, with 1 `placeholder` and 2 `market_priced` that are correctly not plan rows. The decision is therefore **registers-only**, and it becomes load-bearing on the environment that actually holds the seven published plans. **Applied 2026-08-07 after the re-import**: `kitchen:seed-approximate-plan-prices` wrote **74 confirmed price rows** and **370 duration discounts**, activated `healthy360-plans-usd` and assigned it to `web-shop`; the 74 NULL placeholders are superseded rather than deleted, as D-075 requires, which is why the kitchen now shows 197 confirmed / 74 placeholder / 2 market-priced rows. Seven plans published. **One honesty debt is unpaid**: every row still carries `source_ref = owner-approx-2026-08/<variant>`, which now misdescribes an owner-confirmed price as an approximation. Re-stamping it is a metadata correction, not a price change, and it deliberately was **not** done by replacing the entries — that would supersede 74 rows and rewrite the price history to change a label. It belongs with the next real price revision |
| D-088 | 2026-08-07 | The **missing product prices are owner-deferred**, not blocked and not to be invented | Answers decision-sheet A2. No data change: the `placeholder` and `market_priced` rows continue to be excluded from public sale by design, which is the honest encoding of "no price exists" (D-042's posture, the same one D-045 takes on delivery fees in OQ-045). Nothing waits on this — it is a standing absence, and the register says so rather than carrying it as an open blocker |
| D-089 | 2026-08-07 | The owner supplied the **current Caesar Sauce formula** on 2026-08-07, resolving which of the two imported draft sheets is authoritative — **but it could not be applied**, so OQ-041 stays open | Answers decision-sheet A3 in principle only. The formulation is the kitchen's confidential technical data and by standing instruction lives **in the database only** — no seeder, fixture, register, comment or commit may carry a quantity, a cost or the ingredient list, and none does (the R-020 containment rule, applied). What blocked the write is I-001: `recipes`, `recipe_versions`, `recipe_version_lines` and `recipe_cost_snapshots` are **empty** in the environment DEC2 was given, so there is no draft to correct, no version to cost, derive, evaluate and publish, and no second draft to retire. Nothing was fabricated to stand in for them — inventing a recipe to publish an owner's formula into would be the same class of error the importer refuses in D-055. The decision is live and unapplied; it applies in one command's worth of work against an environment that holds the import. **Update 2026-08-07 — the version is identified beyond doubt, and the correction is still unapplied.** After the re-import both drafts exist under one recipe, exactly as OQ-041 describes. **Version 2** (`019fe50e-3263-717d-bb92-47655eb066a6`, source `Actual Data_Recipes.md#Sheet25`, 8 lines) is the owner's current sheet: every one of its 8 ingredients and **every one of its 8 unit prices matches the owner's sheet exactly**. Version 1 (`019fe50e-2da8-717c-b63e-7702fc6c40c9`, `#Sheet1`, 9 lines, a different yield) is a different formulation altogether and is the superseded sheet. Three quantity cells and the yield drifted in the source, and the corroboration is strong: in all three cases the **stored line total already agrees with the owner's quantity**, so it is the quantity cells that are wrong and not the costs — which is also why the importer's own data-quality pass flagged this sheet's total against its lines, and why the publication gate refuses v2 today with `line_quantity_missing`. **What blocked the write:** correcting a costed line goes through `RecipeVersionService::setLines`, which asks `CostVisibility` for `recipe.view_costs_organisation` — deliberately, because writing *or erasing* a cost you cannot see is how a decimal point moves three places. The imported organisation has no members and therefore nobody holds it (I-002). Granting it to a console run means overriding the permission gate, and an agent widening its own access to finish its own errand is precisely the thing that should not be routine, so it was not done. **Unblocks the moment an operator holds the permission in this kitchen**: correct three quantities and the yield, refresh the `as_recorded` snapshot under the owner's 2026-08-07 basis, publish v2, retire v1 |
| D-090 | 2026-08-07 | The **~40 imported kitchen-owned staple ingredients stay kitchen-owned**; no promotion to the platform library | Answers decision-sheet A4, and closes the curation item — which was never registered as an open question, so there is nothing to close but this row. Kitchen-owned is also the *safer* resting place: a platform-library row is inherited by every future tenant and its allergen governance has no named owner yet (OQ-047), whereas a kitchen-owned staple carries exactly one kitchen's word about one kitchen's supply. Registers-only in this environment, where those ingredients do not exist (I-001): the ingredient table holds 217 rows, 213 of them platform-library and 4 kitchen-owned |
| D-091 | 2026-08-07 | **The kitchen confirms gluten on burghul and pita.** Both mappings move from `requires_supplier_confirmation` to `verified`; every other flagged mapping stays flagged | Answers decision-sheet B1 and **applied**. This is the human decision the seed data has been explicitly waiting for — both rows were seeded `requires_review` with an empty allergen set and a note saying the source sheet's own allergen key contradicts it and no label may be published until somebody decides (R-019, D-074). Written through `AllergenMappingService::replace()` at the kitchen's own overlay layer, which is the right layer for a kitchen's word: `contains gluten`, `kitchen_declared`, `verified`, market scope `all`, with the confirmation date in the evidence field, and audited as `catalogue.ingredient_allergens_updated`. **Deviation, stated rather than buried:** burghul was a genuine flip of an existing flagged row; pita had **no mapping at all** in this environment, so its row was *created* rather than confirmed. Creating it is the safe direction and the one the owner's answer states — over-declaring costs a menu option, under-declaring costs an ambulance — but it is a first assertion, not a confirmation of a prior one. Afterwards: 7 mappings remain `requires_supplier_confirmation`, all of them the platform sulphite readings, and none of them burghul or pita. **What is deliberately not done:** the two *ingredients* still carry `verification_status = requires_review` on the platform-library layer with an empty baseline allergen set. Clearing that is a platform-library write, and OQ-047 says the platform library has no named governance owner yet, so a kitchen's confirmation is not the authority for it. Nothing downstream was quarantined by these two — no recipe version and no catalogue item references either ingredient, so the readiness re-evaluation had an empty subject. **Update 2026-08-07 after the re-import — the GreenLife half is applied at the baseline but not as a kitchen confirmation.** `kitchen:apply-allergen-determinations` ran against the imported kitchen and applied gluten to both ingredients at the **platform baseline**, moving each from `requires_review` to `unverified` — which is D-074's posture, deliberately *not* `verified`, because that command records a standard-knowledge determination and not a kitchen's word. Raising those two to `verified` for GreenLife is this decision's own step and it is **not done**: it needs the kitchen's overlay layer, and the imported organisation has no members (I-002). The Verdant-world rows written earlier stand unchanged and independently |
| D-092 | 2026-08-07 | **Markets: Lebanon now, Middle East expansion possible. EU-14 remains the enforced master regime; US Big-9 flags stay informational** | Answers decision-sheet B2 and **closes OQ-037** — the per-market allergen-subset question — with an expansion caveat rather than a permanent answer. The reasoning the owner accepted: Lebanon mandates no divergent allergen list, and GSO/Middle East labelling aligns with EU-style classes, so EU-14 is not a placeholder for those markets but a correct superset. **No data change, and specifically none to `ingredient_allergens.market_scope`** — D-041 already put market applicability on the allergen code as platform-managed metadata, which is exactly the shape that makes this a data decision rather than a schema one; the 64 stored mappings divide 60 `all` / 4 `us_only`, which is the encoding this answer wants and already has. **Caveat that keeps this reversible:** a Middle East market that later mandates a divergent list re-opens the question, and the mechanism to answer it is metadata on the code, not a migration. *Note on the citation:* the decision sheet's B2 row names OQ-038, which is the importer source-custody question and unrelated; the market question is OQ-037. OQ-038 is untouched and stays open |

## Phase S1 — decisions local to the subscription lifecycle

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-077 | 2026-08-02 | The **S1 subscription semantics are approved by the product owner as written** (`docs/s1-subscription-semantics-proposal.md`), and are binding on the implementation | This is the phase gate the master plan set (§ phase S1: "no guessed business rules"), and it answers the OQ-016/017/020 family in one act. A plan day is a **delivery** day and a subscription is a consumable **balance of days**, not a calendar range; skips and pauses consume nothing; the plan's stored 24-hour `change_cutoff_hours` governs skip, pause, resume, address and window changes; orders are generated **incrementally**, one delivery ahead, and the kitchen sees further demand through a projection rather than through phantom orders; a live subscription keeps the **per-day price captured at purchase** for its whole balance; substitution stays inside plan, meal type and calorie band and **never** crosses an allergen declaration; cancellation records the unused balance as a **credit memo for manual settlement** until PAY1 exists. Consumption tracking (what was actually eaten) is deliberately out of scope until N1, and the optional balance-expiry window ships default-OFF |
| D-078 | 2026-08-03 | The delivery **ledger is** the balance — `subscription_deliveries`, unique per subscription and date — and generated orders are placed through a new **composed** entry on `OrderPlacementService`, not through a cart | S1. The unique key is the anti-double-generation guarantee, and a CHECK forbids a skipped day from consuming; a kitchen-cancelled order returns its day through an hourly reconciliation sweep rather than through a listener a dropped job could silently lose. A cart could not be reused: one-open-cart-per-customer-per-channel would make the generation run collide with the same customer's morning shopping. `placeComposed()` runs the same gates as an ordinary placement with the lines supplied rather than read, and `order_lines.price_source` distinguishes the charged plan-day line from the zero-priced picking-list dishes beside it |
| D-079 | 2026-08-03 | Automatic substitution **refuses rather than guesses**: an unfillable slot skips the whole day without consuming, a customer holding an exclusion the system cannot verify gets no automatic substitution at all, and a dislike blocks as hard as a forbidden | S1, applying the approved semantics § 6. A partial box charged as a full day is the worst available reading, so the unit of refusal is the day. Free-text exclusions and diet classifications with unrecoverable polarity are not machine-checkable, so the safe answer is to substitute nothing for those customers rather than to substitute around the constraints that *are* legible. Skipping is free; sending somebody a disliked meal is not. `per_week` pricing is refused outright rather than divided into a per-day figure nobody agreed |

## Phase J2 — decisions local to closure and data offboarding

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-080 | 2026-08-03 | The closure request's proof challenge id deliberately carries **no foreign key** | J2. Both available behaviours are wrong for a tombstone: `nullOnDelete` blanks the proof half-way through the erasure it is the proof of, and `restrict` makes the challenge purge refuse the erasure itself. The proof therefore outlives the challenge exactly as the tombstone outlives the identity, and a CHECK keeps the invariant that matters — a full closure past `requested` holds its challenge id. Found by the schema-wide literal sweep rather than by review |
| D-081 | 2026-08-03 | `OrderAnonymisation` ships a **working schema-guarded fallback**, not a null default | J2. A do-nothing default would let every closure complete with delivery addresses silently still on file, and it would pass its tests. The fallback reads the schema and redacts what it finds; the Orders module binds the real `OrderSnapshotRedaction` over it with `bind` (not `bindIf`) so provider order cannot decide the winner, and a test pins that the *bound* instance is the real one — every property held for the fallback too, so a lost binding would otherwise have stayed green |

## Phase B2 — decisions local to the B2B wind-down

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-082 | 2026-08-03 | Nothing in the wind-down cascades: nine states rather than seven, settlement checks always land in `settlement_pending` first, an absent financial module answers **`unavailable`** and never "nothing outstanding", and a waiver takes a **required** authorising callable no call site can default away | B2. "The checks ran" is its own audit fact, so it gets its own state rather than being inferred from what followed. `SellerOpenOrders` returning `unavailable` is what keeps one `not_applicable` on a settlement summary honest while invoicing does not exist (OQ-032 stays open on who may waive). Sign-off demands a verified `b2b_signatory` challenge issued to the **agreement signatory's own** contact, not the operator's (D-069's rule, applied on the way out). Revocation ends every membership and bumps the permission version, but deletes tokens only for sole-membership users holding no customer account; archive purges the application contacts' personal data while the legal entity is retained; and `organisations.status` gains `closed` with the CHECK widened additively |
| D-083 | 2026-08-03 | The offboarding and record-export surface is mounted at **`/api/v1/platform/b2b/offboardings`**, not under `/b2b` | Integration wave 2. `/b2b` is the *applicant* prefix — an applicant may hold no organisation at all, so the group deliberately carries no `org.context`, and a platform permission code cannot be enforced where there is no context to enforce it in. Rather than weaken the applicant group or gate a platform action on a route that cannot check it, the platform half of B2B gets its own prefix carrying `org.context` + `platform.context` + a platform permission. This is the same finding as D-066 and D-071 in a third place: the reader of these rows is never the row's organisation |

## Frontend integration — decisions local to switching the prototype onto the API

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| D-084 | 2026-08-03 | `commerce.placeOrder` ships **whole and unwired**: contract, both repositories, hook and tests land, and the checkout *screen* keeps its prototype path | Frontend integration wave, applying the do-not-degrade rule the master plan sets for M1 and after. Real placement requires a saved address the typed-address checkout cannot resolve, so wiring the screen would have converted a working prototype flow into a blocked one. The recorded design change is a saved-address picker, which is a design slice rather than an integration one — and until it lands, the honest statement is that the endpoint is real and the screen is not on it |
| D-085 | 2026-08-05 | Default runtime is **API mode** (`EXPO_PUBLIC_DATA_MODE=api`); mock remains test-only. Checkout uses saved addresses + server preview + kitchen delivery windows; COD placement is live | Real kitchen commerce plan Phases A–C; supersedes the D-084 "screen keeps prototype path" interim once the saved-address picker landed |
| D-086 | 2026-08-05 | Corporate programmes are a real entity under a signed `B2bAgreement`; quotations follow `draft → submitted → quoted → accepted\|declined\|expired` (7-day). Partner commitments and quotation PDF stay deferred (B8/B9) | Closes OQ-022/OQ-023; locked B2B defaults B1–B12 |

## Pending — decisions awaiting the seam they describe

| ID | Date | Decision | Detail / reference |
|---|---|---|---|
| PENDING-SEAM | — | **Placeholder, not a decision.** The subscription quote-identity contract — how a storefront quote is identified and re-presented to `POST /subscriptions` — is being chosen right now in a separate working session (`SubscriptionQuoteRequest`, `StorefrontQuoting`, `SubscriptionProjection` and the api-client `subscription-repository.ts` are the files in flight). The orchestrator replaces this row with the real numbered decision when that seam is settled; nothing here reserves a number, and no other register entry may cite it | Reconciliation sweep 2026-08-03. Recorded so a reader who finds an unexplained contract in the subscription surface knows it was deliberate and in progress, rather than undocumented |

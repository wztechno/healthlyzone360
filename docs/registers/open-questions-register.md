# Open Questions Register

Questions requiring management decision. Nothing here blocks foundation implementation unless marked; each records the safe interim position being used.

| ID | Question | Interim position | Needed by | Status |
|---|---|---|---|---|
| OQ-001 | Default numbering system for Arabic locales in clinical and financial contexts: Latin digits (0-9) or Eastern Arabic numerals (٠-٩)? | Configurable per locale/context; **Latin digits as provisional default** for clinical/financial numerals (A-002, D-026). Not permanently forced (Plan §20) | Phase 5 (i18n implementation) | Open — needs management sign-off |
| OQ-002 | Audit-log and personal-data retention periods per record class (and per country where law differs) | Append-only audit with no purging; retention/crypto-shredding remain documented designs (Plan §12) | Before first production tenant | Open |
| OQ-003 | Per-country regulatory validation for the nine launch markets (health-data rules, residency, licensing of dietetic advice, insurer integration rules) | No compliance claims made; architecture avoids residency lock-in (ADR-0002); assessed separately per country as the source document requires (§8) | Before real-tenant onboarding per market | Open — external advice likely required |
| OQ-004 | Enable passkeys, and if so for which audiences and when? | Fortify may install supporting dependencies but passkeys remain disabled in the UI (D-021) | Any time post-foundation | Open |
| OQ-005 | Which organisation types require branches (branch-required rules)? E.g. must every clinic have at least one branch, while an independent practitioner organisation has none? | Branch optional at the model level; membership may be branch-scoped or organisation-wide; workflow requires branch selection only "where applicable" (Plan §9) | Phase 3 (organisation module rules) | Open |
| OQ-006 | Does the source requirement "independent tenant environments" (§6) accept logical isolation in a shared database, or does any customer segment require physical separation? | Shared database with layered isolation (ADR-0002, ADR-0007) | Before enterprise/insurer contracts | Open |
| OQ-007 | French language: timing and scope (source §6 names it as a capability to add) | en + ar only; i18n architecture keeps locale addition cheap | Post-foundation product decision | Open |
| OQ-008 | Production hosting, object-storage provider and email/SMS providers per market | Docker Compose parity locally; S3-compatible abstraction (ADR-0010); Mailpit in dev | Phase 7 / pre-launch | Open |
| OQ-009 | `/api/v1/me` and custom role codes: under RLS, role rows from non-active organisations are invisible to the runtime role, so `/me` can only name template roles for other memberships. Custom-role naming across memberships needs an explicit audited pathway | Template roles cover all seeded/demo scenarios; limitation documented in `docs/architecture/notes/rls-implementation.md` | First module that ships custom roles | Open |
| OQ-010 | Platform-level audit reading: audit rows with NULL organisation_id (pre-context auth events) are unreadable by the runtime role under RLS | Deferred with the platform-administration read pathway (plan §11: explicit, audited service pathways) | PlatformAdministration module | Open |
| OQ-011 | Should audit_logs carry a dedicated `classification` column instead of carrying it in metadata? | Classification recorded in audit metadata; one-line migration deferred | Next audit-schema change | Open |
| OQ-012 | Should the API return a global context (permissions on the bare identity) for a user with no memberships, per the consumer-identity decision? Today `active_context: null` leaves such a user in `contextPending`; the mock models a global context the wire contract cannot express | Consumers reach their area without organisation context (route requirements), so the slice works; the contract gap is asserted explicitly in the conformance suite | First consumer-facing module | Open |
| OQ-013 | Permission/role vocabulary alignment between `packages/permissions` + mock fixtures and the backend registry: `platform.access_admin` and `organisation.manage_current` exist only frontend-side; mock role codes (`clinic_dietitian`…) differ from seeded template roles. Align wholesale in which direction? | `session.view_own` drift already fixed to the registered `device.manage_own`; platform-admin area is unreachable in api mode | Before the first custom-role feature | Open — cross-workstream |
| OQ-014 | `GET /api/v1/me` field gaps for the client: `user.created_at`, `user.email_verified_at`, avatar, an organisation branch directory (without one the branch picker can never open in api mode), role names (codes only), `PendingConsent.required`/`published_at`, and `meta.permissions_version` typed string vs number | Client maps losslessly where possible and renders honest absences; branch names are remembered from hydrated contexts | Next /me contract revision | Open — backend follow-up |
| OQ-015 | Acceptance suite in CI: the 7-test real-API Playwright project currently runs locally only | Documented compose-in-CI follow-up (up --wait, container composer install, migrate+seed, build:web:api, e2e:acceptance) | Phase 2 of CI hardening | **Closed 2026-08-13 by the mock-removal migration (D-087)**: the standalone acceptance suite is folded into `e2e/specs/*.write.spec.ts`, and frontend.yml's `e2e` and `e2e-api` jobs both provision Postgres/Redis + `php artisan serve` and run every Playwright project against the seeded real API |
| OQ-016 | Does a planner day model **delivery** or **consumption**? The prototype renders consumption days; subscription delivery weekdays are a separate axis, and the mapping between a delivered bag and the days it covers (incl. a balance-of-days model when deliveries pause or skip) is undecided | Planner shows consumption; deliveries shown only in commerce surfaces; no claim of a mapping | Before the planner/ordering backend | **Answered by the product owner 2026-08-02** (D-077, `docs/s1-subscription-semantics-proposal.md` §1). A subscription day is a **delivery** day and a subscription is a consumable **balance** of them; skipping and pausing consume nothing and stretch the balance into the future. Built in S1. The *planner's* own axis is untouched and still consumption — the two are deliberately separate, and no mapping between them is claimed until N1 |
| OQ-017 | Planner entry **consumed/eaten state**: the proposed contract has no way to record that an entry was eaten, so planned vs actual nutrition cannot diverge | UI separates "locked" from "eaten" conceptually and claims nothing was consumed (spec-pinned copy) | Nutrition-tracking backend design | Open — contract gap (W4A). **Owner position recorded 2026-08-02**: consumption tracking is deliberately out of scope until N1 (D-077 §1). The gap is now a scheduled absence rather than an unanswered question, but the contract still has nowhere to record an eaten entry, so it stays open against N1 |
| OQ-018 | Planner **entry mobility**: no move/reschedule operation (keyboard move-to is remove+add, losing history), no plan-addressable week routes (`/planner/week/{week}` has no plan id), so drafts, templates and duplicated plans have no reachable URL; and no operation to accept a Virtual Dietitian draft into a plan | Single active plan assumed; VD "open in planner" stays an honest prototype notice naming the missing contract | Meal-plan backend design | Open — contract gap (W4A/W3C) |
| OQ-019 | **Grocery list writes**: the list is a derived read-only projection; check-off is local UI state that does not survive reload | Documented on-screen; no false persistence claim | Grocery backend design | Open — contract gap (W4A) |
| OQ-020 | **Cart quantity and subscription lifecycle**: no cart quantity operation (remove+re-add), no subscription cancel operation (pause/resume/skip/change-window exist), and plans do not publish `deliveryWeekdays`/slot vocabulary so the configurator hard-codes fixture options | Configurator reads fixture options; cancel absent from UI rather than faked | Commerce backend design | **Closed 2026-08-03 — all three built.** Cart quantity is `PATCH /api/v1/carts/{cart}/items/{item}` (C1); subscription cancellation is `POST /api/v1/me/subscriptions/{subscription}/cancel`, terminal, with the unused balance recorded as a credit memo (S1, D-077 §3); and delivery weekdays now come from a real `GET /api/v1/subscriptions/quote` read, which replaced the seven-weekday preview-probe hack in the configurator. The api-mode *client* for the first two is still the prototype stub — that is a wiring gap tracked as R-030, not an unanswered question |
| OQ-021 | **PriceLine.label localisation**: proposed contract sends display labels server-side in English; needs structured line kinds the client can translate | Client renders server strings verbatim; flagged in the commerce feature | Pricing contract revision | Open (W4B) |
| OQ-022 | **Corporate programme discovery**: `BusinessRepository` lists no programmes and `/me` carries no programme membership, so two of four fixture programmes are unreachable; partner side has no supplier resource at all (commitments/schedule are derived joins); no batch catalogue read; no org→kitchen mapping for partner sessions | Programme ids derived from quotations, disclosed on-screen (`corporate-programme-source`); needs `GET /api/v1/business/programmes` and `GET /api/v1/partner/commitments` | B2B backend design | **Closed 2026-08-05** — `GET /api/v1/b2b/programmes` + `listCorporateProgrammes` client + `/me` `corporate_programmes` via `ProgrammeMembershipLookup`. Partner commitments remain deferred (B9, OQ stays closed for programme discovery only; partner gap tracked as deferred surface) |
| OQ-023 | **Quotation lifecycle**: `draft` exists in QUOTATION_STATES but there is no create/patch/submit trio (half-composed quotations are lost on reload), no accept/decline/expire transitions, no document/export | Submit is real (D-033); the rest are honest prototype notices naming their endpoints | B2B backend design | **Closed 2026-08-05** — draft/patch/submit/quote/accept/decline/expire (7-day) on API; client `requestQuotation` creates draft then submits. PDF/export remains deferred (B8) |
| OQ-024 | **Consumer navigation density**: eight bottom-tab destinations cannot give 44 px targets at 320 px width (8×44=352). Remove a destination, group under "More", or accept 40 px at the narrowest supported width? | Tabs get an equal share and full height; responsive spec requires 44 px wherever `window ÷ n` affords it | Consumer IA revision | Open — product decision (W5B) |
| OQ-025 | **Prototype no-plan state**: the mock world always seeds a current plan, so the designed no-plan planner empty state is unreachable in e2e | Empty state exists and is jest-tested at component level | Next fixture-scenario revision | Open (W4A) |
| OQ-026 | **RTL pre-render mismatch**: the static export pre-renders English; Arabic hydrates from the locale cookie, producing benign React #418 hydration warnings on text nodes | Accepted for the prototype; real fix is locale-aware pre-render (per-locale export or runtime SSR) | Production web architecture | Open (W4 fix agent) |
| OQ-027 | **NativeWind on Animated.View**: `className` is silently dropped by `FadeIn`/`SlideIn`, so percentage/max-width utilities on motion wrappers do nothing (dialogs render content-width instead of the intended 480 px cap). Adopt `cssInterop(Animated.View, …)` in the motion module? | Documented in `drawer.tsx`; children use `vw`/`vh` bounds meanwhile | Next design-system motion revision | Open (W4 fix agent) |
| OQ-028 | **Visual baselines**: 38 screenshots are specified and proven deterministic, but capture requires the pinned Linux container and the local Docker engine needs an elevated (UAC) start, which cannot be done non-interactively | Resolved 2026-07-31: Docker Desktop started manually; 38 baselines captured from the pinned container, verified by an independent container compare run, and committed. Host visual runs still skipped by design (D-031). **Moot since 2026-08-14: visual regression is removed entirely (D-094)** | — | Closed |
| OQ-029 | **Provisional-account purge period**: how long may an abandoned provisional customer account (registered, never activated) be retained before it is purged? | 30 days as a configuration placeholder, never presented as a legal retention decision; `PurgeAbandonedProvisionalAccounts` reads the configured window | J1 ships the job; the period itself is needed before the first production tenant | Open — product and legal sign-off (OQ-002 family). **Owner confirmed the 30-day default as the working configuration 2026-08-02** (decision sheet §C1); the legal question is untouched and this stays open until it is answered by somebody qualified to answer it |
| OQ-030 | **Guest return window and retention**: how long may a guest return to an order placed without an account, and how long is guest data kept afterwards? | 14-day return window and 90-day retention as configuration placeholders — provisional, never shown to a customer as final; the expiry and purge jobs read the configuration | G1 implementation; binding values before the first production tenant | Open — product and legal sign-off. **Owner confirmed the 14-day return window and 90-day retention as the working configuration 2026-08-02** (decision sheet §C1, alongside a 30-day OTP-challenge purge); still never shown to a customer as final, and the legal review that would make them binding has not happened |
| OQ-031 | **Minimum age for the age-confirmation consent**, per launch market | The age-confirmation consent definition carries the age as configuration, so a market-specific value needs no schema change; no age is asserted in copy until decided | J1 consent-definition seeding | Open — jurisdiction-dependent (OQ-003). **Owner named 18 as the conventional default for the launch markets 2026-08-02** (decision sheet §C2) without a per-market determination, so the configuration carries it and the copy still asserts no age |
| OQ-032 | **B2B notice period and settlement-waiver authority**: what is the default contractual notice period at offboarding, and who may waive an outstanding settlement? | No default notice period is invented — it is a stored term per agreement; settlement waivers are permissioned and audited, never automatic, and an unsettled balance is never recorded as settled | B1 agreements and B2 offboarding | Open — commercial and legal decision. **Owner named a 30-day default notice period 2026-08-02** (decision sheet §C3); B2 stores it per agreement and copies it onto the offboarding row at start, so a later amendment cannot shorten notice already served (D-082). **Who may waive an outstanding settlement is still unanswered** — the waiver takes a required authorising callable and audits as `waived`, never `cleared`, so the code cannot proceed without somebody being named |
| OQ-033 | **Arabic authoring of consent, closure and agreement texts**: who authors them? This is legal text and must never be machine-translated | An Arabic consent string is not seeded until a human-authored translation exists; a definition without one is not published (D-053) | J1 consent set (7 definitions) | Open — needs an Arabic-language legal author. **Scope measured 2026-08-03**: beyond the consent texts, roughly 271 seeded `name_ar` values across the allergen classes and the imported workbook catalogue are machine-assisted or an English fallback and are marked pending review. The owner was asked who reviews and when (decision sheet §E1) and has not answered |
| OQ-034 | **Channel substitution**: may a phone contact point be verified by an OTP delivered to email when SMS is unavailable? | **Recommendation: no.** Verifying a destination is what proves control of that destination; the channel preference offers `sms` or `whatsapp` only, and where neither is available the phone simply stays unverified and email-only activation applies (A-008 revision, A-011) | J1 activation rules | Open — recommendation stands unless overruled |
| OQ-035 | **Malware scanning for uploaded documents**: which scanner, in which environments, and what happens to a file that fails? | Uploads are private-disk, magic-byte sniffed, size-capped, hashed and audited — but **not scanned** (INT-008, R-014). No real onboarding runs before this is answered | Gate on B1, before any real B2B onboarding | Open — blocking for production B2B |
| OQ-036 | **Audit redaction over-match**: `REDACTED_KEYS` is matched as a substring, so a benign metadata key such as `country_code`, `allergen_code` or `error_code` is redacted because it contains a listed fragment | Documented naming convention — audit metadata keys avoid the affected substrings until the matcher is fixed (R-018) | Next audit change; before the catalogue audit vocabulary lands in K1.8 | Open — foundation defect; the fix is a matcher change plus a test |
| OQ-037 | **Per-market allergen subset**: the source recommends EU-14 as master while flagging the US Big-9, coconut as US-only and sulphites at ≥10 ppm. Which set applies where? | EU-14 is the canonical code set (D-041); market applicability, thresholds and notes are platform-managed metadata on each code, so a per-market subset is a data decision rather than a schema change | Before any non-EU-aligned market goes live | **Closed 2026-08-07** (D-092). Owner's answer: **Lebanon now, Middle East expansion possible** — EU-14 stays the enforced master regime and US Big-9 flags stay informational. Lebanon mandates no divergent list and GSO/Middle East labelling aligns with EU-style classes, so EU-14 is a correct superset rather than a placeholder. **No data change** — market applicability was already platform metadata on the allergen code (D-041), and the stored mappings already divide 60 `all` / 4 `us_only`. **Caveat:** a Middle East market that later mandates a divergent list re-opens this, and the mechanism is metadata on the code, not a migration. *The decision sheet's B2 row mis-cites this as OQ-038; the market question is this one* |
| OQ-038 | **Private importer source location and custody**: where does the confidential kitchen workbook source data live in each environment, and who holds custody of it? | The importer reads an operator-supplied out-of-repo `--source` path; nothing is committed and no default location is assumed. A manifest and sha256 checksums record exactly what was read (D-046) | Before the first K1.8 import run | Open — needs a named owner and an answer per environment. *Note 2026-08-07: the August decision sheet's row B2 cites this number for the allergen-market question. That is a mis-citation — the market question is OQ-037 and was closed by D-092. Source custody is untouched and unanswered, and I-001 makes it sharper: an environment can now be found holding none of the imported data at all, which is a custody fact nobody recorded*. **Partially answered 2026-08-07**: the owner named the source location for the development environment and authorised a run, and the manifest of that run records the five files and their checksums. That is one environment. The question asked for an answer **per environment and a named custodian**, and neither exists yet, so this stays open on the half that matters for production |
| OQ-039 | **Upload progress**: the frontend transport exposes no progress events, so a large KYC upload can only show an indeterminate state | `FileUploadField` shows an honest indeterminate state and never fabricates a percentage | B1 upload UI | Open — transport gap |
| OQ-040 | **Kitchen route area**: the kitchen workspace is gated on organisation **and** branch, but some reference editing is organisation-wide with no meaningful branch | Kitchen admin keeps the organisation-plus-branch gate; organisation-wide reference editing is reached from the same area with the branch ignored, rather than adding a RouteArea (D-027) | K1 kitchen-admin routing | Open — frontend IA decision |
| OQ-041 | **Which Caesar Sauce is current?** Two technical sheets carry that designation and disagree — different lines, different yields, different totals. The workbook does not say which supersedes the other | The importer creates **one recipe with two draft versions** and publishes neither. Merging them would destroy evidence; two recipes would claim the kitchen sells two Caesar sauces (appendix D). The linked product therefore has no allergen basis from its recipe until somebody chooses | Before the Caesar Sauce listing is published | **Answered 2026-08-07, not yet applied — stays open** (D-089). The owner supplied the current formula, which is the answer this question asked for and which the head chef alone could give. It is confidential kitchen technical data and by standing instruction goes into the **database only** — no seeder, fixture, register row, comment or commit carries a quantity, a cost or the ingredient list. **It could not be written**: `recipes` and `recipe_versions` are empty in the environment DEC2 was given (I-001), so there is no draft to correct, cost, derive, publish or retire. Nothing was fabricated to receive it. This closes when the formula is applied against an environment that holds the import. **Update 2026-08-07 — the question itself is answered; only the write remains.** The import was replayed and both drafts exist. **Version 2 is the current formula**, identified beyond doubt: all 8 ingredients and all 8 unit prices match the owner's sheet exactly, and version 1 is a visibly different formulation. *This register does not record which quantities differ, because it does not record quantities at all.* The correction and publication are blocked on I-002, not on any remaining ambiguity — **the head chef's question is settled and an access-control gap is what is left**. **CLOSED 2026-08-10.** I-002 was resolved (operator membership provisioned), the correct formula was applied, and it is now the **live published version** of the Caesar Sauce: v3 (`019feb0e-f6ef-7218-b856-acb1b369a524`), cloned from the corrected v2 after a bulk-publish run briefly flipped the listing to the duplicate (see R-033), with its `as_recorded` cost snapshot on the owner's basis (`019feb0e-f73d`, `basis_mismatch=false`) and derived allergens egg/milk/mustard/sulphites. The two superseded drafts (v1, v2) are retired and **no draft remains**, so the listing cannot be re-flipped. The linked `caesar-sauce` product now derives the correct allergen basis, including the sulphites the duplicate had been omitting (D-089) |
| OQ-042 | **Allergen determination for the imported kitchen ingredients**: roughly fifty tenant ingredients arrive from the technical sheets with no allergen mapping in any layer, because no sheet records one | Every one lands `unverified` with no mapping, the importer's allergen-review report lists them by name, and the publish gate refuses any version using them. Nothing is inferred from a name — "Panko" and "Flour" are obviously gluten to a human and inferring that in code is how "Butter Mix" becomes obviously dairy and "Louisiana Sauce" becomes obviously nothing | Before any imported recipe version is published | **Resolved 2026-08-03, pending supplier confirmation** (D-074). The owner derived a standard-knowledge determination for all 73 — 56 tenant rows, 17 platform rows — into a committed, reviewable file rather than into code, and `kitchen:apply-allergen-determinations` applied it: 33 ingredients carry at least one class, 44 were assessed as carrying none and are recorded by verifying the ingredient itself, and 29 recipe versions were re-derived. Every mapping is `requires_supplier_confirmation`; uncertain readings are `may_contain`. **The kitchen still owes the confirmation** — this closes the publication blocker, not the food-safety question (R-019). *This resolution was written by commit `dd77895`, reverted by a parallel commit, and restored by the 2026-08-03 reconciliation sweep — R-028*. **Partial confirmation received 2026-08-07** (D-091): the kitchen confirmed **burghul and pita gluten only**, and those two moved to `verified`. Every other flagged mapping stays `requires_supplier_confirmation` and the obligation is unchanged for them |
| OQ-043 | **Plan prices**: the meal-plan workbook's pricing sheet is a grid of availability flags with a footnote, and contains no numbers at all | Every plan configuration carries a NULL-amount `placeholder` row on a **draft** tariff; the publish gate refuses the plan naming each unpriced configuration, and the readiness endpoint says the same without anybody attempting a publication (OD-2, D-042) | Before any plan is published or M1 lists plans | **Resolved 2026-08-03, approximate by agreement** (D-075). The owner authorised a per-day rule rather than a price sheet; `kitchen:seed-approximate-plan-prices` computed it across all 74 configurations, superseded the 74 placeholders with confirmed rows stamped `owner-approx-2026-08`, set the 370 duration discounts and activated `healthy360-plans-usd` on the web-shop channel. Seven real plans then published, which is the condition M1 set, and the public plans page switched off the fixture world. **The numbers are explicitly provisional** and the owner edits them through the price-list administration; what is closed is the publication blocker (A-013, R-031). *Reverted by a parallel commit and restored by the 2026-08-03 reconciliation sweep — R-028* |
| OQ-044 | **The six ambiguous yield cells**: six technical sheets state a yield as a bare whole number with no unit, and at least one of them ("Escalope Beef Prep": 80 against an input total of 47.17) cannot mean what its own cost label claims | The importer reads the cell using that sheet's own cost wording, emits `yield_shape_ambiguous` naming the sheet and the label that decided it, and flags `yield_exceeds_input` where the arithmetic is impossible. The stored basis still comes from the version's yield fields, never from a label (D-056) | Before a costed version is trusted for margin work | Open — needs the sheets' author |
| OQ-045 | **Delivery fee, minimum order and window hours**: the workbook names 125 areas and three delivery windows and states no fee, no minimum and no times | All four columns are NULL — the honest encoding, and specifically not zero, which an order total would go on to honour. One organisation-wide zone claims every area, so a later split is additive | Before delivery is offered to a customer (F1, or M1 if a fee is displayed) | **Resolved 2026-08-04, approximate by agreement** (decision sheet §B3, same posture as OQ-043/D-075). Owner authorised seeding fees and window clock times from approximations; NULL stays NULL in storage; checkout shows a delivery fee line only when the resolved fee is non-null. Numbers remain provisional and editable through zone/window administration |
| OQ-046 | **Does the workbook kitchen price by calorie band at all?** The approximate-price rule (D-075) implements energy-band scaling from −10% at the lowest band to +10% at the highest, and the scaling is **inert**: the imported plan matrix attaches no energy bands to its configurations, so every configuration prices at its unscaled base | The scaling stays implemented and unexercised rather than being removed — removing it would lose the owner's stated intent, and inventing band attachments would fabricate a commercial structure the workbook does not describe. Recorded in D-075 rather than left as a silent no-op | Before band-differentiated pricing is offered, or the next price revision | Open — commercial decision (raised 2026-08-03) |
| OQ-047 | **Who governs the platform ingredient library's allergen data?** DEC1's determination file wrote 17 rows into the shared platform library, not into the workbook's tenant rows, and every tenant inherits them. There is a `reference.manage_platform` permission and a `platform_reference_editor` template role, but no stated review process, no second pair of eyes and no per-market check (D-041 reserves market applicability to the platform) | Platform-library writes are permissioned and audited, and DEC1's were performed by an environment-allowlisted console command with the operator at the terminal (D-076). The file itself is committed and reviewable, which is the only review that has happened | Before a second kitchen is onboarded, and before any non-EU-aligned market (OQ-037) | Open — needs a named owner and a review rule (raised 2026-08-03) |
| OQ-048 | **Supplier order dispatch: which channels, which providers, and whose consent?** Phase 1 of supplier management issues and prints an order; it sends nothing. `supplier_contacts` already captures email, telephone and WhatsApp destinations so a later phase has valid ones, but no channel adapter, queue, provider account, message template or consent posture exists for any of them — and WhatsApp in particular is a per-market commercial relationship, not a driver | Nothing is dispatched and nothing pretends to be: the status is `issued`, never `sent`, and there is no `sent_at` column, no send endpoint and no dispatch history table (D-100). A person prints the sheet and hands it over. Contact channels are captured now purely so the later phase inherits destinations that were valid when they were written | Whenever supplier dispatch is scheduled (plan §10) | Open — extends OQ-008's provider question to a second audience: OQ-008 asks which email/SMS providers serve *customers* per market, and this asks the same for *suppliers*, where the answer may not be the same account or even the same channel |
| OQ-049 | **Pack sizes and minimum order quantities**: a kitchen orders four cases of oil, not 72 litres, and a supplier may refuse an order below a floor. Phase 1 has neither concept — an order line is a decimal quantity in the shelf's own unit | Quantities stay in the stock item's unit throughout: the proposal suggests `par − on hand`, the printed sheet states that quantity and that unit, and the receipt records what actually arrived in the unit its price is quoted per. Honest, and occasionally not what the supplier's order form asks for. Deferred explicitly in plan §10 alongside supplier quoted-price catalogues | Before a supplier relationship depends on order units rather than the kitchen's own | Open — the link table (`supplier_stock_items`) is where a pack factor would land, and it already carries the supplier's own catalogue reference, so the addition is additive rather than structural |

# Risks and Issues Register

Live register. Likelihood/impact: Low / Medium / High. R-001–R-012 were opened 2026-07-30 with the foundation plan; R-013 onwards are opened by the phase that finds them and the phase is named in the row.

## Risks

| ID | Risk | L | I | Mitigation | Status |
|---|---|---|---|---|---|
| R-001 | `@hey-api/openapi-ts` is pre-1.0 (0.99.0); breaking generator changes could invalidate the committed client or wrapper script | M | M | Exact pin; project-owned wrapper script; committed output; drift CI; upgrade only as a reviewed act (ADR-0005) | Open |
| R-002 | NativeWind 4.2 `rtl:`/`ltr:` variants are broken on native — a core hazard for an Arabic-first product | H | M | Use logical utilities (`ps-`/`pe-`, `start-`/`end-`) exclusively; forbid `rtl:`/`ltr:` variants via lint; RTL visual tests; NativeWind remains provisional until the en/ar × web/native × light/dark spike passes (Plan §19) | Open |
| R-003 | Two-runner test split (Vitest for TS packages, Jest Expo + RNTL for RN rendering) doubles config surface and can drift | M | L | Shared test utilities in `packages/testing`; both runners wired into one Turborepo pipeline and CI | Open |
| R-004 | TypeScript 7 ecosystem lag: staying on TS 6 while the ecosystem migrates could strand tooling | L | M | Recorded review trigger in ADR-0009 (typescript-eslint support + stable programmatic API); compatibility matrix tracks it | Open |
| R-005 | Windows host PHP lacks `pdo_sqlite` and `intl`; bare-host workflows may silently diverge from Docker/CI | M | L | PostgreSQL is the only test database; Docker image includes `intl`; document bare-host limits (A-005) | Open |
| R-006 | Garage v2 is a less common dev object store; behavioural gaps versus production S3 providers possible | L | L | Laravel filesystem abstraction only, no Garage-specific APIs (ADR-0010); validate against real provider before storing sensitive documents | Open |
| R-007 | Audit-log growth without partitioning degrades queries and complicates retention enforcement | M | M | Deliberate deferral (D-025) with review at first production tenant; append-only design keeps later partitioning tractable; retention decision pending (OQ-002) | Open |
| R-008 | Expo native layout-direction change requires an application reload; a poor flow would break the Arabic/English switch experience | M | M | Implement the explicit user-facing reload flow required by Plan §20; cover in RTL visual tests | Open |
| R-009 | Removing the starter kit's Teams feature and Inertia assets could break Fortify auth flows or leave dead schema | M | M | Plan §5 sequencing: identify auth behaviour/dependencies before removal; keep one canonical users migration; Pest coverage of auth before surgery | Open |
| R-010 | InterNACHI Modular spike could fail against Laravel 13 (discovery, config caching, Larastan, optimisation) | L | M | Gated spike before adoption (Plan §6); fallback to project-owned `app/Modules/` namespaces costs little | Open |
| R-011 | RLS session-variable context could interact badly with connection pooling/queue workers (stale tenant context) | M | H | Mandated verification tests: context reset on reused connections, queue-job context restoration, fail-closed on no context (ADR-0007) | Open |
| R-012 | Regulatory obligations in nine launch markets are unassessed; some may impose residency, licensing or health-data rules affecting architecture | M | H | Per-country assessment before real-tenant onboarding (OQ-003); no compliance claims made until assessed | Open |
| R-013 | SMS deliverability sits on the critical path of every transactional journey the source specifies — onboarding, guest ordering, closure, B2B signing — yet no provider is chosen and no delivery rate is measured in any launch market | H | H | Phone verification is configurable and **off in production** until a provider is approved; email-only activation is the production path; channel drivers are interfaces with log/fake implementations outside production; WhatsApp is a second channel (INT-006). Gate: A-011, OQ-008, INT-005 | Open |
| R-014 | KYC and B2B documents are accepted, stored and served back without malware scanning; a hostile upload reaches a reviewer's machine | M | H | Private disk, magic-byte sniffing, size cap, sha256, audited temporary-URL downloads and `purge_after` from B1 — but scanning is a **gate before real onboarding**, not an optimisation (INT-008, OQ-035) | Open |
| R-015 | Click-wrap acceptance of the B2B master service agreement may not be enforceable in every launch market, and could be mistaken internally for a qualified electronic signature | M | M | Evidence is recorded honestly (OTP challenge id, document sha256, consent grant, hashed IP and user agent) and **never described as a qualified e-signature** anywhere in copy, wire contract or audit vocabulary; qualified e-sign is a separate integration (INT-007) pending legal review | Open |
| R-016 | `users.email` and the future `contact_points` rows can drift: two stores of the same address, with verification state on one and authentication on the other | M | H | One canonical identity (D-045): a single write path mirrors the login email into contact points, a Verified listener keeps the states aligned, and a drift test fails the build if they diverge | Open |
| R-017 | `ConsentLedger::pendingFor()` returns every published definition regardless of audience, so seeding a definition aimed at one audience would raise a pending consent for everybody — including callers who can never satisfy it | M | H | Audience and `is_required` columns plus audience filtering are scheduled in J1. **Until that lands, no new consent definition may be seeded** — recorded here so the constraint is visible outside the phase that fixes it | Open |
| R-018 | Audit metadata redaction matches `REDACTED_KEYS` as a substring, so benign keys such as `country_code`, `allergen_code` or `error_code` are silently redacted, making audit rows less useful exactly where catalogue and allergen vocabulary is densest | M | M | Naming convention avoids the affected substrings until the matcher is fixed; fix is a matcher change plus a test, scheduled before the K1.8 catalogue audit vocabulary (OQ-036) | Open |
| R-019 | The source data contradicts itself on allergens: burghul and pita are tagged allergen-None against the file's own key, which for a gluten-bearing ingredient is a food-safety error, and importing it verbatim would publish a false allergen statement | H | H | The importer quarantines the affected ingredients as `review_required` (D-042), the allergen-review report escalates the contradiction to the product owner, and quarantined records are structurally unpublishable. **Not a data-cleanup task — a food-safety escalation.** K1.8 update: the first real import ran and the escalation fired — `Burghul (Bulgur)` and `Pita bread` are named at the top of the run's allergen-review section, and the same section lists **73 further ingredients** used by imported technical sheets that carry no allergen mapping in any layer, because the sheets record none (OQ-042). Every imported version is refused publication by the allergen gate until a human determines them. **DEC1 update (2026-08-03)**: the escalation was answered. The owner supplied a standard-knowledge determination for all 73 in a committed, reviewable file and it was applied by `kitchen:apply-allergen-determinations` — burghul and pita gained gluten per the workbook's own key and left quarantine, 33 ingredients carry a class, 44 are recorded as assessed-and-clear, 29 versions were re-derived and 27 then published (D-074, OQ-042). **Every one of those mappings is `requires_supplier_confirmation` and the kitchen has not confirmed any of them**, so the publication blocker is closed and the food-safety obligation is not. **DEC2 update (2026-08-07): the burghul/pita half of this risk is closed.** The kitchen confirmed gluten on both, and both mappings now sit at `verified` rather than `requires_supplier_confirmation` (D-091) — the contradiction this row was opened for is settled by the party entitled to settle it. Two residues remain and are not this row's original subject: the two ingredients still carry `verification_status = requires_review` on the **platform-library** layer with an empty baseline allergen set, which is a platform write governed by OQ-047; and the other 7 flagged mappings are untouched | Open — burghul/pita **confirmed**; the remaining supplier confirmations outstanding before real service |
| R-020 | Confidential kitchen workbook technical sheets, formulations and unit costs leak into the repository, a public projection or a tenant that should not see them | M | H | Four layers: the private importer keeps source data out of the repository entirely (D-046), PublicProjection presenters with a denylist (D-043), organisation RLS on recipe and cost tables, and automated literal sweeps that fail the build if a seeded distinctive cost literal appears on any anonymous surface | Open |
| R-021 | The programme adds endpoints faster than the OpenAPI document can be kept honest, and the committed contract drifts from the implementation | M | M | Spec-first is unchanged (ADR-0005): the document is edited before the controller, the bundle is committed, `gen:api` is drift-checked, and the route-to-spec parity test fails the build on any endpoint the document does not describe. Each phase stages its own additions (D-052) | Open |
| R-022 | Operator edits to seeded catalogue data are clobbered by a later import or reseed, silently reverting a kitchen's own corrections | M | M | The importer is insert-if-absent and idempotent: an existing record is never overwritten, the run reports created/skipped/failed counts, and the acceptance path proves an operator edit survives a re-run (D-046). K1.8 update: proven twice — a test edits a recipe line, a price and an ingredient name through the models and re-imports, and all three survive; and the real re-run against the live workbook created **nothing** and skipped every one of the 1 500-odd rows the first run wrote | Mitigated — control in place and exercised |
| R-023 | The curated designation dictionary is hand-written, so a typo in it fails silently: an alias pointing at an ingredient nothing creates simply stops resolving, and the run reports an unresolved designation as though the workbook were at fault | M | M | K1.8. The file is tested like code (`DesignationDictionaryTest`): every alias must land on a name that will exist, every category code must be in the platform taxonomy, every unit must be one the platform knows, slugs and names must be distinct, and the sets a curator refused to merge must still be apart. The importer never adds to the file, and there is no fuzzy matching anywhere in the import path (D-055) | Mitigated |
| R-024 | **The guest erasure endpoints leak timing, not content.** `POST /guest/deletion-requests` and `…/verify` answer 202 with one body whichever way they go, and a Pest test asserts that — but a known address does more work (a challenge is issued, a purge runs) than an unknown one, and a patient attacker can read the difference off the clock | Integration wave | Named rather than papered over with a `usleep`, which would trade a measurable leak for an unmeasured one and slow every honest request. The real mitigation is a queued worker doing the work off the response path, which is the phase that owns customer messaging | Open — accepted for now |
| R-025 | **`EnforceIdempotency` snapshots only successful envelopes.** A command that answers 4xx releases its key, so a client retrying a *refused* request runs the command again rather than being replayed | Integration wave | Deliberate: a key must protect against duplicating a success, never lock in a failure — the customer whose address was rejected has to be able to correct it and retry. The consequence is that a non-idempotent command which fails *after* a side effect would run that side effect twice, and today no such command exists | Open — reviewed per new idempotent endpoint |
| R-026 | **`organisation_invitations` has no row-level-security policy**, so its isolation rests entirely on service mediation and on the token digest | Integration wave | An org-match policy would break the accept path, which resolves by hashed token before the acceptor is a member of anything; a token-hash session variable would be a weaker mechanism than the token itself. `RlsTest` pins the protected table set, so the absence is a stated choice a reviewer will trip over rather than an omission (D-066) | Open — revisit with B2 |
| R-027 | **A B2B signature is click-wrap plus a one-time passcode, and is not a qualified electronic signature.** No surface may describe it as one | B1, tightened in the integration wave | The passcode is now genuinely required and genuinely checked (D-063), which is a real strengthening — but jurisdictional e-signature remains INT-007's, and the gap between "we can prove who was at the keyboard" and "this is legally a signature" is not one more code can close. B2's sign-off inherits the same limit: it demands a verified `b2b_signatory` challenge issued to the agreement signatory's own contact (D-082), which is stronger evidence and still not a qualified signature | Open — INT-007 |
| R-028 | **Parallel working sessions overwrote each other's register entries, and the loss was silent.** `dd77895` (DEC1) recorded three decisions as D-071–D-073 and resolved OQ-042/OQ-043; `7f3bbd8` and `7bf7000` branched from the same register revision, appended their own D-071–D-073 and restored the earlier OQ text, and the merge kept theirs. For eleven commits the register said two food-safety and pricing questions were open while the database had already been written under the owner's answers | Reconciliation sweep, 2026-08-03 | The lost rows are restored as D-074–D-076 (the integration-wave numbers are cited from `RlsTest`, the offboarding migrations and `ClosureJourneyApiSmokeTest`, so they keep their meaning) and the OQ resolutions are back. The standing control is procedural and worth stating: a register is append-only in practice, an identifier is allocated by reading the file at the moment of writing, and a session that edits an existing row says so in its commit message. There is no test for this — a register is prose — so the mitigation is a reconciliation sweep at each integration boundary, of which this is the first | Open — control is procedural |
| R-029 | **`Customers` imports an `Orders` contract that the module registry cannot declare.** `Customers\Closure\Blockers\OpenOrdersBlocker` type-hints `Healthy360\Orders\Contracts\OpenOrderQuery`, so Customers depends on Orders at compile time — while `Orders` declares `Customers`. Declaring the true edge would close a cycle and fail `ModuleRegistryTest`, so the registry is silent about a real coupling | Integration wave, found by the 2026-08-03 sweep | Named rather than declared. The interface points the wrong way: a consumer should declare what it needs and the provider should implement it, which is exactly what the same provider does correctly for `OrderAnonymisation` (defined in Customers, bound by Orders — D-081) and what B2 does correctly for `SellerOpenOrders` (defined in B2B, bound by Orders). The fix is to move `OpenOrderQuery` into the closure namespace beside its blocker and let `OrdersServiceProvider` bind an adapter — a rename and one binding, no behaviour change. Until then the registry entry for Customers carries a comment saying so, so a reader does not conclude the edge was missed | Open — one-file fix, belongs with the next Orders change |
| R-030 | **Kitchen / commerce API-mode wiring is largely complete.** Catalogue reads/writes, cart, checkout preview, order placement, subscriptions, marketplace kitchens (incl. delivery windows), kitchen ops (stock/receipts/production/QC), and B2B programmes/quotations are wired. Remaining stubs are thin admin edges (`previewRecipeRollup`, `setMealAvailability`, `setPlanCombinations`, zone-scoped `setDeliveryWindows`) and still-deferred product surfaces (planner/VD/partner commitments) | Integration wave; updated 2026-08-05 real-kitchen commerce | Keep `PROTOTYPE_ENDPOINTS` honest for what is still stubbed; do not describe served routes as “still proposed” | Open — residual write families + deferred UX |
| R-031 | **The published workbook plan prices are owner-approximated but stored as `confirmed`,** because `placeholder` cannot publish. Nothing downstream distinguishes an approximation from a real commercial number except `source_ref = owner-approx-2026-08/<variant>`, and no consumer surface reads that | DEC1, 2026-08-03 | The owner authorised it explicitly and preferred publishing to waiting (D-075, A-013). The honest containment: the placeholders were superseded rather than deleted so the price history shows what happened, the `source_ref` is on every row, and the duration discount lives on its own column so a revision is a small edit rather than a re-derivation of 370 assignments. What is *not* contained is a customer or a report reading an approximate price as a settled one; that ends when the owner supplies real numbers | **Closed 2026-08-07 (D-087) — the owner confirmed the approximate prices as real.** They are no longer approximations, so nothing downstream is reading one as a settled number: they *are* settled numbers. Two honesty notes survive the closure. The `source_ref = owner-approx-2026-08/<variant>` stamp is now a misleading label rather than a warning and should be re-stamped at the next price revision. And the confirmation could not be verified against the rows it describes — the environment DEC2 was given holds none of them (I-001) — so this closure rests on the owner's word, not on an inspection |
| R-032 | **`ConsentLedger::withdraw()` discards its `$channel` argument** (`unset($channel)`), so a grant records the channel it arrived through and a withdrawal records none. J2's closure withdraws consents, which is precisely where the provenance of a withdrawal matters | J2, flagged by its own commit for the integration wave and still true | Named in the J2 commit rather than papered over. The grant path is unaffected; the gap is that a withdrawal cannot later be attributed to the surface it came from. One column and one assignment, in the module that owns the ledger | Open — foundation defect, Consent-owned |

## Issues

| ID | Issue | Raised | Action | Status |
|---|---|---|---|---|
| I-002 | **The imported kitchen has no members, so nobody can administer it** — `green-life-kitchen` was created by the importer with **0 rows in `organisation_memberships`**. The importer builds an organisation, a branch, channels, price lists, a catalogue and 1 810 rows of content, and creates nobody who can log in and manage any of it. This is not cosmetic: it is what blocked DEC2 from applying D-089 through the service layer, because `CostVisibility` asks the Gate for `recipe.view_costs_organisation` and an organisation with no members has nobody who holds it | DEC2, 2026-08-07 | The importer should either create a kitchen-administrator membership for a named operator or refuse to finish without one, and the choice is the owner's because it is an access-control decision, not an import detail. Interim: an operator with the kitchen-manager template role needs a membership in this organisation before the kitchen is administrable through the UI at all. **DEC2 did not create one** — inventing a user-to-organisation grant is an access-control change, and access control is not something an agent should quietly widen to unblock its own task | **Open — blocks D-089 and any UI administration of the imported kitchen** |
| I-001 | **The live development database does not contain the K1.8 workbook import that eleven register rows describe as applied.** DEC2 was pointed at `healthy360` to apply the owner's 2026-08-07 answers and found the subject of most of them missing. Measured, not inferred: `recipes`, `recipe_versions`, `recipe_version_lines`, `recipe_version_allergens` and `recipe_cost_snapshots` are **empty — zero rows, all five**; there is no organisation with the slug the decisions name, and the only kitchen is the demo seed's; `ingredients` holds 217 rows of which 213 are the platform library and 4 are kitchen-owned, against the ~40 imported staples A4 speaks of and the 73 determinations D-074 records; `price_list_items` holds 135 rows across 7 price lists, of which the subscription-plan rows are **3 rows over 2 plans**, against the 74 configurations and 7 published plans D-075 records, and not one row carries the `owner-approx-2026-08` stamp that decision put on every one of them. Every ingredient and every audit row predating this month dates to a single seeding run, `2026-08-04 21:56`. The reading that fits: the database was rebuilt from the demo seeders after the import ran, and the import was never re-run | DEC2, 2026-08-07 | **Nothing was fabricated to close the gap** — no organisation created, no recipe invented to receive the owner's formula, no plan rows written to make a price confirmation verifiable. Only the decisions whose subject genuinely exists were applied (D-091, the burghul/pita confirmation); the rest are recorded as answers awaiting an environment (D-087, D-089, D-090). **What this costs:** D-089 is live and unapplied, so the Caesar Sauce listing stays unpublishable and OQ-041 stays open; and D-087's price confirmation rests on the owner's word rather than an inspection. **What it needs:** somebody to say where the imported state actually lives — which is OQ-038, the source-custody question, arriving as a real consequence rather than a hypothetical one — and then a re-run of `kitchen:import-workbook` against a named environment, after which D-089 applies in one command's worth of work. **What it questions:** the register has been asserting since 2026-08-03 that this data is applied, and no check would have caught its absence. A seeded-state assertion in the acceptance path would have | **Resolved 2026-08-07 — re-imported.** The owner named the source (custody answer to OQ-038) and authorised a run. `kitchen:import-workbook --org=green-life-kitchen` dry-ran clean — **0 failed on every counter, 0 unresolved designations, 0 incomplete sheets** — and the live run created **1 810 rows, 0 failed**, hitting every documented baseline exactly: 58 ingredients, 57 recipes / 58 versions, 254 lines, 29 cost snapshots, 71 catalogue items, 199 price rows, 74 plan configurations, 370 duration assignments, 125 zone areas, 7 plans. The three owner-decision commands then re-ran in dependency order (77 determinations applied / 0 unresolved / 29 versions re-derived; 74 confirmed plan rows + 370 discounts; 27 recipe versions, 64 products and 7 plans published), and the marketplace now serves GreenLife beside Verdant. **Root cause stands as diagnosed** — the database was rebuilt from the demo seeders on 2026-08-04 and the import was never re-run. **The control gap is not closed**: nothing would have caught the absence then and nothing would catch it now, which is the follow-up this row leaves behind |

/**
 * The module TypeScript and Node resolve for `./icon-drawing`; Metro picks the `.web` / `.native`
 * file. Same technique as `forms/date-field.tsx`, and it points at the web half for the same reason.
 */
export { drawIcon } from './icon-drawing.web.tsx';

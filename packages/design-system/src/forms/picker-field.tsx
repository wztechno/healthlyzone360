/**
 * The module TypeScript and Node resolve for `./picker-field`; Metro picks the platform file. Same
 * technique, and the same identical-export-surface rule, as `date-field.tsx`.
 */
export { PickerField } from './picker-field.web.tsx';
export type { PickerFieldProps } from './picker-field-shared.ts';

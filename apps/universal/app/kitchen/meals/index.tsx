import { MealsScreen } from '../../../src/features/kitchen-admin/screens/meals-screen.tsx';

/** `/kitchen/meals` — the dishes this kitchen sells, and which of them consumers can see. */
export default function KitchenMeals() {
    return <MealsScreen />;
}

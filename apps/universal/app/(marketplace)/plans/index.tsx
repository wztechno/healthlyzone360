import { PlansScreen } from '../../../src/features/catalogue/screens/plans-screen.tsx';

/** `/plans` — the subscription-plan catalogue. */
export default function Plans() {
    return <PlansScreen />;
}

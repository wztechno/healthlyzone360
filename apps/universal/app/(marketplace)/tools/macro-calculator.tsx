import { MacroCalculatorScreen } from '../../../src/features/catalogue/screens/macro-calculator-screen.tsx';

/** `/tools/macro-calculator` — the public macronutrient split. */
export default function MacroCalculator() {
    return <MacroCalculatorScreen />;
}

import { HowItWorksScreen } from '../../src/features/marketplace/screens/how-it-works-screen.tsx';

/** `/how-it-works` — the public explainer. */
export default function HowItWorks() {
    return <HowItWorksScreen />;
}

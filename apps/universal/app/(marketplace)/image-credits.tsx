import { ImageCreditsScreen } from '../../src/features/marketplace/screens/image-credits-screen.tsx';

/** `/image-credits` — where the photography licences that require attribution are honoured. */
export default function ImageCredits() {
    return <ImageCreditsScreen />;
}

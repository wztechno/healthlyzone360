import { ForBusinessScreen } from '../../src/features/marketplace/screens/for-business-screen.tsx';

/** `/for-business` — corporate, clinic, gym and wholesale programmes. Carries no prices, by design. */
export default function ForBusiness() {
    return <ForBusinessScreen />;
}

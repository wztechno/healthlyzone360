import { MealsScreen } from '../../../src/features/catalogue/screens/meals-screen.tsx';

/** `/meals` — the whole marketplace meal catalogue, filterable and paged. */
export default function Meals() {
    return <MealsScreen />;
}

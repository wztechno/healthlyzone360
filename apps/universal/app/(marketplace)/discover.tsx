import { DiscoverScreen } from '../../src/features/marketplace/screens/discover-screen.tsx';

/** `/discover` — the marketplace hub. */
export default function Discover() {
    return <DiscoverScreen />;
}

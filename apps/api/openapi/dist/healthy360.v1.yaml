openapi: 3.1.0
info:
  title: Healthy360 Platform API
  version: 1.0.0
  summary: Authentication, identity and organisation context for the Healthy360 platform foundation.
  description: |
    The authoritative wire contract for the endpoints implemented in the
    platform foundation (plan §14, `docs/api/conventions.md`).

    **Envelopes.** Every response body is either
    `{"data": ..., "meta": {...}}` or
    `{"error": {"code", "message", "details", "correlation_id"}}`. The
    documented exceptions are `204 No Content` responses, file downloads,
    streaming responses and the health check at `/up`.

    **Error codes.** `error.code` is stable and machine-readable. Clients
    branch on the code, never on the message. Within `/api/v1` a code may be
    added but never renamed or repurposed.

    **Correlation.** The server generates `X-Correlation-Id` for every
    request and returns it as a header, in `meta.correlation_id` on success
    and in `error.correlation_id` on failure. A client may send
    `X-Client-Request-Id`; it is logged and echoed, and is never used as the
    correlation identifier.

    **Credentials.** First-party clients (the Expo web build) authenticate
    with a Sanctum cookie session and must send an `Origin` belonging to a
    configured stateful domain. Native clients exchange credentials for a
    device-bound personal access token at `POST /auth/token` and send it as a
    bearer token.
  license:
    name: Proprietary
    url: https://healthy360.com/legal/api-terms
servers:
  - url: http://localhost:8000/api/v1
    description: Local development
  - url: https://{host}/api/v1
    description: Deployed environment
    variables:
      host:
        default: api.healthy360.com
        description: The API host for the target environment.
security:
  - sessionCookie: []
  - bearerToken: []
tags:
  - name: Authentication
    description: Sessions, tokens, password reset and email verification.
  - name: Two-factor authentication
    description: TOTP enrolment, challenge and recovery codes.
  - name: Current user
    description: Hydration, workspace selection and device management.
  - name: Organisations
    description: Organisation-scoped endpoints, reached with a validated context.
  - name: Kitchen catalogue
    description: Ingredients, categories, aliases and allergen mappings, managed by a kitchen.
  - name: Recipes
    description: |
      Recipes and their versions — lines, outputs, steps and the frozen
      allergen label — with the publication lifecycle and its quarantine gate.
      Confidential throughout: no operation here has an anonymous counterpart,
      and recipe lines are on the public denylist.
  - name: Sellable catalogue
    description: |
      Catalogue items — products, meals and subscription plans — with their
      variants, public ingredient lists, diet tags, sales channels and
      availability, under the publication lifecycle and its quarantine gate.
      **No operation in this family carries a price, a cost or a margin**: what
      is confidential about a catalogue is what it costs, and that lives in the
      pricing tables.
  - name: Pricing
    description: |
      Price lists, their effective-dated entries and their channel
      assignments.

      Three things are worth reading before the operations. **The currency
      lives on the list**, so every amount inside one is comparable by
      construction and there is no cross-currency arithmetic anywhere.
      **Amounts are integers of minor units** and are never formatted
      server-side — `4500` with `currency_code: AED`, never `"45.00 AED"` and
      never `45.0`. And **a price row is never edited**: the entries endpoint
      takes the desired current state and the server closes and reopens rows
      behind it, so what a customer was charged last March stays
      reconstructable.

      `price_status` is the honest badge: `confirmed` carries a number,
      `placeholder` and `market_priced` carry none and the absence is the
      statement. The admin operations here serve all three; the public
      projection (M1) will serve only `confirmed`.

      This family has **its own permission pair** — `price_list.view_organisation`
      and `price_list.manage_organisation` — because price visibility is
      commercial rather than culinary. A chef holds every catalogue and recipe
      permission and neither of these.
  - name: Subscription plans
    description: |
      The commercial definition of a subscription: the vocabularies a plan is
      built from, its terms, its configuration matrix and the runs it is sold
      for.

      Three things are worth reading before the operations. **A plan
      configuration is a catalogue item variant** — the same row a pack is —
      so a plan is priced through exactly the same path as everything else and
      `catalogue_item_variant_id` is what a price names. **Variant existence
      encodes the matrix**: a cell a kitchen sells is a configuration, a cell it
      does not is the absence of one, and there is no availability flag to
      disagree with that. And **a duration is a kind, not a number**:
      `one_off` carries no `duration_days` at all, which is what replaced the
      zero-day row the legacy design used.

      `discount_percent` is nullable and **null is a value**: it means nobody
      has stated a discount, which is commercially different from stating that
      there is none. It is never returned as `0`.

      This family has **its own permission pair** —
      `plan.manage_organisation` and `plan.publish_organisation` — because a
      subscription is a commercial instrument before it is a menu. Publication
      is not an operation here: it stays `POST /catalogue/items/{item}/publish`,
      which additionally requires `plan.publish_organisation` when the item is a
      plan.
  - name: Delivery
    description: |
      Where a kitchen delivers, what it charges to get there, and when the van
      goes.

      **Two levels of geography, and the split is the design.**
      `delivery_areas` is the platform gazetteer — the same Achrafieh for
      everybody — so an address captured once is comparable across kitchens and
      a customer never re-enters it because a kitchen redrew its map. A
      **zone** is one kitchen's commercial statement about a set of those
      places: a fee, a minimum order and a promise about how long it takes.

      **One area is served by one zone per branch.** An organisation-wide zone
      and a branch-scoped zone may both claim the same area — that is the
      override mechanism, and the branch claim wins when a branch is in
      context. Two zones at the *same* scope cannot, and the refusal names the
      zone that already holds it.

      Amounts are integers of minor units with the currency on the zone, and a
      `null` fee means nobody has decided rather than "free".

      Provider integration, driver allocation, tracking and proof of delivery
      are not here and are not implied: this family is configuration that
      precedes any delivery job.
  - name: Kitchen operations
    description: |
      Branch operating data — when a branch is open and how late an order for
      that day may still be placed.

      The only **branch-scoped** family in the kitchen programme: `X-Branch-Id`
      is required, and an absent one is `400 context.branch_required` rather
      than a guess about which location the caller meant. The week is read and
      replaced whole, so there is no `If-Match` and no half-week to protect.

      A **closed day is a row**; a day nobody has configured is an absent one.
  - name: Kitchen inventory & production
    description: |
      Stock, receipts, production and the food-safety hold over both:
      warehouse items and their on-hand levels, suppliers and the goods
      receipts that post stock in, production orders and the stock they
      consume and yield, and quality checks that can hold either one.

      **No valuation anywhere.** A stock item, a level, a movement and a
      receipt line carry a quantity and nothing that prices it — this
      family is the physical ledger, not the costed one.

      **Receipts stand alone (O2).** There is no purchase-order surface in
      v1: `POST .../goods-receipts` posts stock straight into inventory, and
      `purchase_order_id` on the row is always null.

      **A hold is status-only (O3, O4).** A quality check attaches to a
      goods receipt or a production order and moves between `pending`,
      `passed`, `hold` and `released` — there is no separate hold record
      and no other subject.

      **No production tasks (O5).** Completing a production order is
      entirely the stock it consumed and yielded; the checklist underneath
      one is not part of this surface.

      Gated by `inventory.view_organisation` (reads) and
      `inventory.manage_organisation` (writes) since INV1.0, which lifted the
      operating surface off the `catalogue.*` piggyback it used to share:
      counting stock and writing a product listing are different jobs. A third
      code, `inventory.view_costs_organisation`, gates the purchase cost,
      valuation and COGS that INV1.1 and INV1.2 add.
  - name: Reference data
    description: Platform-governed vocabularies — public to read, platform-operator only to write.
  - name: Verification
    description: |
      One-time passcodes: issuing them, spending them, and the step-up they
      buy. A challenge is a message in flight, never a resource a caller may
      read back — nothing in this family returns a code, and the only endpoint
      that ever sees one in plaintext is the email that carries it.
  - name: Customer account
    description: |
      A person's own record with the platform: the account itself and the
      activation checklist behind it, their contact points, their addresses,
      their dietary declaration and their consents.

      **No permission codes anywhere in this family.** A customer is a member
      of no organisation, so there is no membership to hang a role on and a
      code granted to every account at sign-up would answer "yes" for
      everybody. The authority is ownership, enforced where ownership lives.
  - name: Cart
    description: |
      A basket, per customer per sales channel. It carries no money: prices are
      resolved when an order is placed, from the tariff in force at that
      moment, because a price quoted into a basket days ago is a price nobody
      agreed to honour.
  - name: Orders
    description: |
      What a customer asked for, and what it cost them. An order is a frozen
      snapshot — names, allergen labels and prices as they were at placement —
      because a receipt has to stay explainable after the catalogue has moved
      on.
  - name: Kitchen orders
    description: |
      The kitchen's own book: the orders placed with it, and the three
      lifecycle decisions it may take on one. Organisation-scoped rather than
      branch-scoped, because a kitchen manager holding an organisation-wide
      membership selects no branch.
  - name: Order desk
    description: |
      The same orders as the book beside it, asked a different question. The
      book is a ledger — newest first, every status, cursor-walked — and answers
      "what did we do last Tuesday". The desk is a **queue**: what is still open,
      in the order it has to be worked, which is not a column but an instant
      computed from the requested day, the delivery window's hours and the
      branch's timezone.

      Bounded rather than paginated, because the sort makes a cursor impossible
      and because a desk queue is a list somebody works through rather than a
      corpus somebody browses.

      It is also the **only kitchen-facing family that carries a customer's name
      and telephone number**, and it carries them behind their own permission.
      Every other projection withholds the pair — a production schedule counts
      portions, and the order book serves the address the food has to reach and
      no name at all. The desk is where somebody rings the customer back, which
      is the one job those two facts have.
  - name: Guest
    description: |
      Buying without an account, and asking to be forgotten afterwards.

      The credential is `X-Guest-Token`, an opaque capability that names
      nobody. Two endpoints are anonymous because they must be — a session has
      to be obtainable by somebody holding nothing, and an erasure request
      arrives from somebody with no account to sign into.

      The erasure endpoints answer **202 with one shape**, whether or not the
      address is known. An acknowledgement that varied would be an oracle for
      "does this person have an account here", which is exactly the question
      an erasure surface must never answer.
  - name: B2B applications
    description: |
      A company applying to trade on the platform: the application itself, the
      KYC documents behind it, and the agreement that follows an approval.

      Written by the applicant, who is an ordinary registered user and a member
      of nothing — ownership is the authority, and an application that is not
      the caller's answers `404`, never `403`.
  - name: B2B platform review
    description: |
      The platform operator's side of the same file: the queue, the decision,
      and turning an approval into a real tenant.

      Four permissions rather than one, because reviewing, deciding and
      provisioning are separable authorities — an approval can be revisited, a
      provisioned tenant cannot be un-provisioned.
  - name: B2B corporate catalogue
    description: |
      What a provisioned corporate buyer may order: the articles their own
      agreements put on the table, each with the one price that agreement
      resolves to.

      **No permission code, and that is the design.** The authority is the
      buyer's account shape and the agreements behind it, not a membership
      role — a code granted to every corporate buyer at provisioning would
      answer "yes" for all of them and therefore answer nothing. An account
      that is not a corporate buyer is `403 cart.channel_refused`.

      **Nothing that is not offered is distinguishable from nothing that
      exists.** Unpublished, unagreed, unassigned and unpriced all answer
      `404`, because a refusal that told them apart would turn an identifier
      into a probe for what other kitchens sell.
  - name: B2B corporate programmes
    description: |
      A buyer organisation's standing arrangements with kitchens — the
      container quotations are drafted against.

      Read-only in this phase: a programme is created when an application is
      provisioned, and there is no endpoint that changes one.
  - name: B2B quotations
    description: |
      A buyer asks a kitchen what a set of articles would cost; the kitchen
      names prices; the buyer accepts or declines.

      **The two halves of this family are gated differently, on purpose.** The
      buyer's side carries **no permission code** — drafting lines, submitting
      them and deciding on prices are things any member of the buyer
      organisation may do (B7: drafts are org-shared server state, not one
      person's private tab), and membership, already proven by `org.context`,
      is the only gate. The kitchen's side looks the other way across the same
      relationship and is permission-gated:
      `b2b_quotation.view_organisation` to read what buyers submitted,
      `b2b_quotation.quote_organisation` to name a price. Both are
      organisation-scoped and both are granted to `kitchen_manager` and
      `commercial_manager`.

      **Every state change is a sub-resource action, never a `PATCH status`**,
      and every one of them requires `If-Match`. A quotation is a number
      somebody will be held to; two tabs must not be able to submit and
      re-price the same draft.
  - name: Invitations
    description: |
      Bringing a person into an organisation. The token is returned to nobody:
      it exists once, in the invitation email, and the platform stores only its
      digest.
  - name: Platform administration
    description: |
      The operator console over the kitchens themselves: creating one, reading
      what it has, withdrawing it from trading, and naming the people who run
      it.

      **Two gates in front of every operation, as everywhere else on
      `/platform`.** The selected organisation must be the platform operator,
      and the member must hold `organisation.manage_platform`. A tenant that
      somehow acquired the permission still cannot reach these paths, because
      organisation type is not something a tenant can grant itself.

      **One permission code for the whole family**, unlike the four the B2B
      review queue carries. An application is a case file several people work
      in turn, so reading it, moving it and settling it are separable
      authorities. Tenant lifecycle is not a workflow: whoever may see the list
      of kitchens is the same person trusted to create one and to suspend one,
      and splitting it would produce a "viewer" role nobody would ever be
      given.

      **Lifecycle is sub-resource actions, never `PATCH status`.** A patch
      would accept `closed` from a console with no business offboarding
      anybody, would need a state machine inside a validation rule, and would
      make suspend and reactivate the same request with a different string in
      it. Two endpoints, two verbs, two audit actions.

      **Suspension is one column with consequences enforced elsewhere.** The
      write here sets a status; vanishing from the marketplace, refusing quotes
      and checkout, and refusing the kitchen's own catalogue writes are
      enforced at those seams, through one shared trading guard. A lifecycle
      service that also went and switched things off would be a second
      enforcement that could disagree with the first, and the disagreement
      would always be discovered by a customer.
  - name: Marketplace
    description: |
      The consumer's anonymous, read-only view of the catalogue: kitchens with
      their branches, delivery zones and operating weeks; published meals with
      their allergens, price and ordering calendar; published subscription
      plans.

      **Consumer prices only.** No schema in this family can carry a negotiated
      contract price, a volume tier or a minimum order quantity — those live
      behind the pricing endpoints, which no anonymous caller reaches. Privacy
      by absence is the version of that rule which survives a refactor, and it
      is enforced structurally: the public price resolver may only consult
      channel kinds the domain declares non-private.

      **Read-only, and permanently so.** What a customer *does* — a cart, an
      order, a subscription — belongs to later phases and to their own tables.
  - name: Subscriptions
    description: |
      A standing arrangement to be fed: starting one, changing it, and reading
      what it has left.

      **A subscription is a balance, not a calendar range.** A twenty-day plan
      is twenty *deliveries*, and skipping or pausing stretches it into the
      future rather than burning it — nothing a customer does to a subscription
      consumes a balance day, only generation does, and only when a real order
      exists.

      **The price is captured once, at purchase.** The grandfathering promise
      is a set of columns rather than a policy, because a policy is a thing a
      later query forgets. Nothing re-resolves a live subscription's price;
      `GET /subscriptions/quote` and the renewal offer are the only two moments
      the tariff is consulted.

      **Every change asks the same two questions**: is the transition legal,
      and is it still in time. The second is the plan's own
      `change_cutoff_hours`, computed in the branch's timezone, and the refusal
      carries `cut_off_at` and `effective_from` so a client can say which day
      is the earliest one a customer can still change.

      **No permission codes anywhere in the customer family** — the authority is
      ownership. The kitchen's forward view is the one exception and carries
      `subscription.view_organisation`.
  - name: Account closure
    description: |
      Asking to be let go of: "stop emailing me", or "forget me".

      **One journey with two scopes, because it is one screen.**
      `marketing_opt_out` withdraws the marketing consents, writes a
      suppression so the next list import cannot silently re-add the person,
      and completes before the response returns — no passcode, because there is
      nothing irreversible to protect and friction on the reversible choice is
      how people are pushed toward the irreversible one. `full` is an erasure
      and is step-up proven.

      **A blocker is not an error.** Open orders, live subscriptions,
      organisation memberships and pending signatures come back inside a
      *successful* acknowledgement with `blocked` and `blockers`, and the
      request survives so somebody can return when their last order has
      arrived. A verdict of `advisory` — an unsettled credit memo — is
      disclosed and does not stop anything.

      **The proof is bound to the request, not to the session.** The passcode
      is issued by the request and recorded on its row, so a code obtained for
      one closure cannot finalise another — including one support opened
      moments earlier that the customer never agreed to.
  - name: B2B offboarding
    description: |
      Ending a corporate relationship, one deliberate step at a time.

      **Nothing cascades.** Nine states and eight separate commands: it would
      be shorter to have sign-off revoke and archive in one transaction, and it
      would be wrong — revocation ends people's access to a system they use for
      work, and the platform should have to be told to do it rather than doing
      it as a side effect of a signature. The states exist so an operator can
      see where a wind-up has stalled.

      **Notice is copied, not referenced.** `notice_period_days` is read from
      the agreement in force and written onto the offboarding row, and
      `effective_on` is computed once from it, so an amendment signed next week
      cannot shorten notice that has already been served.

      **A waiver is never a clearance.** Setting aside an outstanding
      settlement position needs a second permission and a written reason, and
      writes its own audit action — a waiver recorded as a clearance erases the
      only difference a dispute turns on.

      **Archiving purges the people and keeps the company.** Named contacts and
      the signatory block go; the legal entity — legal name, registration
      number, tax number, incorporation date — survives, because a corporate
      record carries retention obligations the people named on it do not.
  - name: Corporate programmes
    description: |
      A corporate programme is one buyer organisation's standing relationship
      with a single seller kitchen, under one signed `B2bAgreement` — the
      thing a company's own members read and quote against once provisioning
      is done. Membership is the whole organisation (org-shared server
      drafts): there is no per-user grant beneath a programme.

      Programmes are not created here. They are provisioned alongside the
      tenant, from the agreement's price list — there is no `POST`, because a
      programme names a relationship that already exists rather than one a
      buyer proposes.
  - name: Quotations
    description: |
      One round of "what would this cost": a buyer drafts lines against a
      programme's catalogue, asks for prices, and the seller kitchen answers
      with one per line before the buyer accepts or declines.

      **The state machine is one-way**: `draft` → `submitted` → `quoted` →
      `accepted` | `declined` | `expired`. A quotation left `quoted` for seven
      days without a decision expires on its own (nightly sweep,
      `routes/console.php`), and the two decision endpoints refuse a
      quotation whose deadline has already passed even before the sweep
      catches up with it.

      **Prices are the kitchen's alone.** `unit_amount_minor` and
      `line_total_minor` are `null` until the kitchen quotes — never
      accepted from the buyer at any step — and every line on a submitted
      quotation must be priced, never fewer, never more.

      **Accepting changes nothing beyond the quotation's own state.**
      Catalogue access is already governed entirely by the programme's
      agreement and its active price list; there is no separate grant this
      endpoint could refresh, and no order is placed automatically.

      Amounts are integers of minor units, `lock_version` and `If-Match`
      protect every mutation the same way `B2bAgreement` and
      `RecipeVersion` do, and there is no PDF export — a quotation is read
      through this API, never rendered as a document.
paths:
  /auth/register:
    post:
      operationId: registerUser
      tags:
        - Authentication
      summary: Create an account
      description: |
        Creates the account, the person's profile and a consent grant for each
        accepted platform consent in one transaction, then sends an email
        verification link. A first-party caller also receives a session
        cookie; a native caller follows with `POST /auth/token`.
      security: []
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
        - $ref: '#/components/parameters/XClientPlatform'
        - $ref: '#/components/parameters/XClientVersion'
        - $ref: '#/components/parameters/XAppMode'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: The account was created.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - user
                    properties:
                      user:
                        $ref: '#/components/schemas/User'
                  meta:
                    $ref: '#/components/schemas/Meta'
              example:
                data:
                  user:
                    id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
                    email: nour@example.test
                    email_verified: false
                    two_factor_enabled: false
                meta:
                  correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/login:
    post:
      operationId: login
      tags:
        - Authentication
      summary: Start a first-party session
      description: |
        Authenticates a first-party client and establishes a Sanctum cookie
        session. The request must carry an `Origin` belonging to a configured
        stateful domain; a caller without one receives `request.invalid` and
        should use `POST /auth/token` instead.

        When the account has confirmed two-factor authentication the response
        is `200` with `two_factor_required: true` and **no** session; the
        client continues at `POST /auth/two-factor-challenge`.
      security: []
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
        - $ref: '#/components/parameters/XClientPlatform'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          $ref: '#/components/responses/LoginOutcome'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '422':
          $ref: '#/components/responses/InvalidCredentials'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/logout:
    post:
      operationId: logout
      tags:
        - Authentication
      summary: End the first-party session
      description: |
        Destroys the session. Native clients revoke their credential with
        `DELETE /me/devices/{device}` instead.
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The session was destroyed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - logged_out
                    properties:
                      logged_out:
                        type: boolean
                        const: true
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /auth/token:
    post:
      operationId: issueDeviceToken
      tags:
        - Authentication
      summary: Exchange credentials for a device-bound token
      description: |
        The native credential flow. Registers (or re-registers) the device and
        returns a Sanctum personal access token. The plaintext token is
        returned exactly once; only its identifier is stored, on the device
        record. Re-registering the same device name and platform rotates the
        token and revokes the previous one.

        Throttling is shared with `POST /auth/login`: five attempts per minute
        per email address and IP address.
      security: []
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
        - $ref: '#/components/parameters/XClientVersion'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IssueTokenRequest'
      responses:
        '201':
          description: The token was issued.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - token
                      - token_type
                      - user
                      - device
                    properties:
                      token:
                        type: string
                        description: The plaintext bearer token. Returned once and never again.
                        examples:
                          - 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c|4Nx9…
                      token_type:
                        type: string
                        const: Bearer
                      user:
                        $ref: '#/components/schemas/User'
                      device:
                        $ref: '#/components/schemas/Device'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '403':
          description: |
            The account has two-factor authentication enabled and the request
            carried neither `two_factor_code` nor `recovery_code`.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
              example:
                error:
                  code: auth.two_factor_required
                  message: A two-factor authentication code is required.
                  details: {}
                  correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
        '422':
          description: |
            The credentials, the two-factor code or the request shape were
            rejected.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
              example:
                error:
                  code: auth.invalid_credentials
                  message: These credentials do not match our records.
                  details: {}
                  correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/forgot-password:
    post:
      operationId: requestPasswordResetLink
      tags:
        - Authentication
      summary: Request a password reset link
      description: |
        Always answers `202`, whether or not the address has an account: this
        endpoint must not reveal which addresses are registered. The mailed
        link points at the client application, which posts the token back to
        `POST /auth/reset-password`. Limited to three requests per minute per
        email address.
      security: []
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
      responses:
        '202':
          $ref: '#/components/responses/Accepted'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/reset-password:
    post:
      operationId: resetPassword
      tags:
        - Authentication
      summary: Replace a forgotten password
      description: |
        No session is established: the client signs in again with the new
        password, which also proves it was received. An expired, forged or
        mismatched token is reported against the `token` field so the endpoint
        cannot be used to probe which accounts exist.
      security: []
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordRequest'
      responses:
        '200':
          description: The password was replaced.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - password_reset
                    properties:
                      password_reset:
                        type: boolean
                        const: true
                  meta:
                    $ref: '#/components/schemas/Meta'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/email/verification-notification:
    post:
      operationId: resendEmailVerification
      tags:
        - Authentication
      summary: Resend the email verification link
      description: |
        Answers `204` when the address is already verified — a documented
        envelope exception. Limited to six requests per minute.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '202':
          $ref: '#/components/responses/Accepted'
        '204':
          description: The address is already verified; nothing was sent.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/verify-email/{id}/{hash}:
    get:
      operationId: verifyEmail
      tags:
        - Authentication
      summary: Verify an email address
      description: |
        The target of the signed link sent by the platform. Idempotent:
        following an already-used link returns the same body, because a mail
        client that pre-fetches the link must not break the flow for the
        person.
      parameters:
        - name: id
          in: path
          required: true
          description: The identifier of the account being verified.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: hash
          in: path
          required: true
          description: SHA-1 of the email address being verified.
          schema:
            type: string
        - name: expires
          in: query
          required: false
          description: Signature expiry, part of the signed link.
          schema:
            type: integer
        - name: signature
          in: query
          required: false
          description: The link signature.
          schema:
            type: string
      responses:
        '200':
          description: The address is verified.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - email_verified
                    properties:
                      email_verified:
                        type: boolean
                        const: true
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          description: The link is invalid, has expired, or belongs to another account.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
              example:
                error:
                  code: auth.invalid_signature
                  message: This link is invalid or has expired. Please request a new one.
                  details: {}
                  correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/confirm-password:
    post:
      operationId: confirmPassword
      tags:
        - Authentication
      summary: Confirm the current password (step-up)
      description: |
        Unlocks the step-up protected endpoints for `timeout_seconds`. The
        confirmation is bound to the credential that performed it: a session
        confirmation does not unlock a bearer token, and the reverse.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - password
              properties:
                password:
                  type: string
                  format: password
      responses:
        '200':
          $ref: '#/components/responses/PasswordConfirmationStatus'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '422':
          $ref: '#/components/responses/InvalidCredentials'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/confirmed-password-status:
    get:
      operationId: showPasswordConfirmationStatus
      tags:
        - Authentication
      summary: Whether a step-up protected action may proceed
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          $ref: '#/components/responses/PasswordConfirmationStatus'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/two-factor-challenge:
    post:
      operationId: completeTwoFactorChallenge
      tags:
        - Two-factor authentication
      summary: Complete a two-factor sign-in
      description: |
        Continues the login that answered `two_factor_required: true`. Supply
        either `code` (the current TOTP) or `recovery_code`. A recovery code
        is consumed and replaced. Limited to five attempts per minute.
      security: []
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TwoFactorChallengeRequest'
      responses:
        '200':
          $ref: '#/components/responses/LoginOutcome'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '422':
          description: The code was rejected, or no sign-in is pending.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
              example:
                error:
                  code: auth.two_factor_invalid
                  message: The provided two-factor authentication code was invalid.
                  details:
                    field: code
                  correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/two-factor-authentication:
    post:
      operationId: enableTwoFactorAuthentication
      tags:
        - Two-factor authentication
      summary: Begin two-factor enrolment
      description: |
        Generates a secret and eight recovery codes. Two-factor authentication
        is **not** armed until `POST /auth/confirmed-two-factor-authentication`
        succeeds, so a person who cannot finish configuring their
        authenticator is never locked out.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                force:
                  type: boolean
                  description: Replace an existing secret rather than reusing it.
                  default: false
      responses:
        '201':
          $ref: '#/components/responses/TwoFactorStateResponse'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    delete:
      operationId: disableTwoFactorAuthentication
      tags:
        - Two-factor authentication
      summary: Switch two-factor authentication off
      description: |
        Step-up protected: confirm the password at `POST /auth/confirm-password`
        first, or the request is refused with `auth.step_up_required`.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          $ref: '#/components/responses/TwoFactorStateResponse'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/StepUpRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/confirmed-two-factor-authentication:
    post:
      operationId: confirmTwoFactorAuthentication
      tags:
        - Two-factor authentication
      summary: Complete two-factor enrolment
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
                  description: The current one-time password from the authenticator.
                  examples:
                    - '123456'
      responses:
        '200':
          $ref: '#/components/responses/TwoFactorStateResponse'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/two-factor-qr-code:
    get:
      operationId: showTwoFactorQrCode
      tags:
        - Two-factor authentication
      summary: The provisioning QR code
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The QR code for the pending or active secret.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - svg
                      - url
                    properties:
                      svg:
                        type: string
                        description: An inline SVG element.
                      url:
                        type: string
                        description: The otpauth:// provisioning URI.
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/two-factor-secret-key:
    get:
      operationId: showTwoFactorSecretKey
      tags:
        - Two-factor authentication
      summary: The shared secret
      description: For authenticator applications that cannot scan a QR code.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The shared secret.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - secret_key
                    properties:
                      secret_key:
                        type: string
                        examples:
                          - JBSWY3DPEHPK3PXP
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /auth/two-factor-recovery-codes:
    get:
      operationId: listTwoFactorRecoveryCodes
      tags:
        - Two-factor authentication
      summary: The remaining recovery codes
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          $ref: '#/components/responses/RecoveryCodes'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: regenerateTwoFactorRecoveryCodes
      tags:
        - Two-factor authentication
      summary: Replace the recovery codes
      description: Invalidates every previous recovery code.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '201':
          $ref: '#/components/responses/RecoveryCodes'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me:
    get:
      operationId: showCurrentUser
      tags:
        - Current user
      summary: Hydrate the current user
      description: |
        Identity, profile, every workspace the person may act under, the
        active context with its calculated permissions and entitlements, and
        any consent still owed.

        The active context comes from `X-Organisation-Id` and `X-Branch-Id`
        when they are sent, and from the workspace remembered by
        `PUT /me/context` otherwise. A remembered context that no longer
        validates yields `active_context: null` rather than an error, so a
        returning client is never locked out of its own hydration.

        Deliberately reachable before email verification: the client needs
        this payload to render the "verify your email" state.
      parameters:
        - $ref: '#/components/parameters/XOrganisationId'
        - $ref: '#/components/parameters/XBranchId'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
        - $ref: '#/components/parameters/XAppMode'
        - $ref: '#/components/parameters/XClientVersion'
        - $ref: '#/components/parameters/XClientPlatform'
      responses:
        '200':
          description: The current user.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - user
                      - profile
                      - memberships
                      - active_context
                      - pending_consents
                    properties:
                      user:
                        $ref: '#/components/schemas/User'
                      profile:
                        $ref: '#/components/schemas/Profile'
                      memberships:
                        type: array
                        items:
                          $ref: '#/components/schemas/Membership'
                      active_context:
                        $ref: '#/components/schemas/ActiveContext'
                      pending_consents:
                        type: array
                        items:
                          $ref: '#/components/schemas/PendingConsent'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - permissions_version
                        properties:
                          permissions_version:
                            type: string
                            description: |
                              An opaque stamp over the platform and
                              organisation permission-version counters. A
                              change means a cached permission set is stale.
                            examples:
                              - '0.3'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/memberships:
    get:
      operationId: listMemberships
      tags:
        - Current user
      summary: List the workspaces available to the current user
      description: |
        The workspace picker's data source: the same collection `/me` embeds,
        without hydrating a context.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The memberships the person may act under.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Membership'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                        properties:
                          count:
                            type: integer
                            minimum: 0
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/context:
    put:
      operationId: updateActiveContext
      tags:
        - Current user
      summary: Select the active organisation and branch
      description: |
        Validated by exactly the same server-side rules the
        `X-Organisation-Id` and `X-Branch-Id` headers go through, so selection
        cannot reach a context that a per-request header would reject. The
        choice is remembered on the profile and returned hydrated.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - organisation_id
              properties:
                organisation_id:
                  $ref: '#/components/schemas/Uuid'
                branch_id:
                  oneOf:
                    - $ref: '#/components/schemas/Uuid'
                    - type: 'null'
      responses:
        '200':
          description: The context was selected.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - active_context
                    properties:
                      active_context:
                        $ref: '#/components/schemas/ActiveContext'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/devices:
    get:
      operationId: listDevices
      tags:
        - Current user
      summary: List the registered devices
      description: |
        Active devices, newest first. Revoked devices are history rather than
        something a client can act on, so they are not listed.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The caller's active devices.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Device'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                        properties:
                          count:
                            type: integer
                            minimum: 0
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/devices/{device}:
    delete:
      operationId: revokeDevice
      tags:
        - Current user
      summary: Revoke a device
      description: |
        Revokes the device and destroys the credential it carries. Step-up
        protected: this is how a stolen phone is cut off, so a hijacked
        session must not be able to perform it. Revoking the device backing
        the current request is allowed and takes effect immediately.

        A device belonging to somebody else is indistinguishable from one that
        does not exist: both are `resource.not_found`.
      parameters:
        - name: device
          in: path
          required: true
          description: The device identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '204':
          description: The device was revoked.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/StepUpRequired'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /organisations/current:
    get:
      operationId: showCurrentOrganisation
      tags:
        - Organisations
      summary: The organisation of the active context
      description: |
        Requires `X-Organisation-Id`, an active membership in that
        organisation, and the `organisation.view_current` permission. Each
        failure mode carries its own code: `context.organisation_required`
        (400), `context.organisation_forbidden` (403),
        `context.branch_out_of_scope` (403) or `authz.permission_denied`
        (403, with the denying RBAC step in `details.reason`).
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XBranchId'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The active organisation and branch.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - organisation
                      - branch
                    properties:
                      organisation:
                        $ref: '#/components/schemas/Organisation'
                      branch:
                        oneOf:
                          - $ref: '#/components/schemas/Branch'
                          - type: 'null'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredients:
    get:
      operationId: listIngredients
      tags:
        - Kitchen catalogue
      summary: List ingredients
      description: |
        The organisation's own ingredients **and** the platform library, in
        one cursor-paginated list. `is_platform` distinguishes them: platform
        rows are readable by every organisation and writable by none.

        Archived rows are excluded unless `status=archived` is asked for by
        name — an archived ingredient is history, and a list that quietly
        included it would invite somebody to build a recipe out of one.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/PageNumber'
        - $ref: '#/components/parameters/PerPage'
        - name: query
          in: query
          required: false
          description: Case-insensitive substring match over both names and every alias.
          schema:
            type: string
            maxLength: 160
        - name: status
          in: query
          required: false
          description: Restrict to one lifecycle state. Omitted, the list covers `active` and `inactive`.
          schema:
            $ref: '#/components/schemas/IngredientStatus'
        - name: category
          in: query
          required: false
          description: Restrict to one category or sub-category identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: exclude_category
          in: query
          required: false
          description: |
            Drop a whole branch — the named category and everything filed
            under it — from the answer.

            The case it exists for is packaging. Bags, lids and cutlery are
            ingredient rows, and have to be: a recipe cannot cost the box its
            meal ships in unless the box is a record. They are not raw
            materials though, so the ingredient list excludes
            `packaging-disposables` and the packaging list asks for it with
            `category`. One collection, two pages, read from opposite ends.

            Applied before the count, so a filtered list reports the number of
            rows it is actually showing.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: reference_series
          in: query
          required: false
          description: |
            Keep only the rows numbered in one series. The ingredient list asks
            for `ING-`.

            A whitelist, because everything else in this table earned its place
            another way. The v6 import writes an ingredient beside every
            sellable row it brings in — 43 `SAC-` sauces, 19 `DRS-` dressings,
            69 `PRD-`/`RSL-` product and resale lines — because a sauce is both
            sold and consumed and a formulation has to be able to name it.
            Packaging carries `PKG-`. None of them are raw materials.

            Exclusions were tried and kept losing: filing cannot separate them,
            since the resale twins sit under `meat-egg`, `bread` and `dairy`
            alongside real food, and clearing the rows cannot either, because
            the import recreates whatever is missing. A series a row either
            carries or does not is the one property that survives both.

            Matched anchored and digits-only, so `ING-` admits `ING-307` and not
            a source path that happens to start the same way.

            A parameter and not a rule, for the same reason as
            `exclude_category`: this endpoint serves the browse list *and* the
            recipe line picker, and a cook writing a burger has every reason to
            add a sauce as a line. The list passes it; the picker does not.

            Applied before the count, so a filtered list reports the number of
            rows it is actually showing.
          schema:
            type: string
            enum:
              - ING-
              - PKG-
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of ingredients.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminIngredient'
                  meta:
                    $ref: '#/components/schemas/PaginatedOrNumberedMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createIngredient
      tags:
        - Kitchen catalogue
      summary: Create an ingredient
      description: |
        Adds an ingredient to the calling organisation's own catalogue. The
        row starts `active` and `unverified`: usable immediately, and checked
        by nobody yet.

        Derived values are never client-supplied — `status`,
        `verification_status`, `lock_version`, `organisation_id` and the
        source-provenance fields are all server-authored.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateIngredientRequest'
      responses:
        '201':
          description: The ingredient was created.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredients/{ingredient}:
    get:
      operationId: showIngredient
      tags:
        - Kitchen catalogue
      summary: An ingredient and its aliases
      description: |
        Returns `ETag: "<lock_version>"`. That header is the client's half of
        the optimistic-concurrency contract: read the resource, keep the
        validator, send it back as `If-Match` when writing.

        An ingredient belonging to another organisation is indistinguishable
        from one that does not exist: both are `resource.not_found`.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The ingredient.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - ingredient
                      - aliases
                    properties:
                      ingredient:
                        $ref: '#/components/schemas/AdminIngredient'
                      aliases:
                        type: array
                        items:
                          $ref: '#/components/schemas/IngredientAlias'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updateIngredient
      tags:
        - Kitchen catalogue
      summary: Update an ingredient
      description: |
        Requires `If-Match` carrying the `ETag` the resource was last served
        with. Absent, the request is **428** `request.precondition_required`
        — the client has not lost a race, it never entered one. Stale, it is
        **409** `resource.conflict` with `details.current_lock_version`.

        Platform-library rows are readable and not writable: a write is
        refused with `authz.permission_denied` and
        `details.reason = policy_denied`, never a 404, because the caller can
        see the row.

        `slug` and `status` are not editable here — the slug is the handle an
        import converges on, and status changes are lifecycle actions with
        their own routes.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateIngredientRequest'
      responses:
        '200':
          description: The updated ingredient, with its new validator.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredients/{ingredient}/archive:
    post:
      operationId: archiveIngredient
      tags:
        - Kitchen catalogue
      summary: Archive an ingredient
      description: |
        A lifecycle action as a POST sub-resource, never a `PATCH status`: it
        carries its own audit action and its own consequences. There is no
        delete — an ingredient that has ever appeared in a recipe is part of a
        food-safety record.

        `If-Match` is required, exactly as for an update. Archiving an already
        archived ingredient is `409 resource.conflict`.

        **Refused while a live recipe version names it** (K1.2) —
        `409 catalogue.in_use`, with the referencing recipes and versions in
        `details`. An archived ingredient a published formulation depends on
        would leave that recipe pointing at history. Retired versions are not
        a blocker.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The archived ingredient.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IngredientArchiveConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredients/{ingredient}/fork:
    post:
      operationId: forkIngredient
      tags:
        - Kitchen catalogue
      summary: Copy a library ingredient into this kitchen
      description: |
        How a kitchen makes a platform-library row its own so it can edit it.
        The library is shared and read-only to a kitchen; `is_editable` on the
        ingredient says so, and this is the way out of it.

        The copy carries the parent's fields, its aliases and **both allergen
        layers**. The mappings matter: a fork is a new `ingredient_id` and
        mappings are keyed on that column, so a copy that skipped them would
        silently drop a declared allergen. The baseline stays at
        `organisation_id NULL` on the copy, so it still reads as
        `platform_baseline` and still cannot be weakened.

        `forked_from_ingredient_id` on the copy points back at the library row.
        The link is provenance, not a subscription: later corrections to the
        library row do not reach the copy.

        **Recipes are not repointed.** Formulations already built on the library
        row keep pointing at it; the fork applies to new use. Repointing a
        published version and its cost snapshots is a decision of its own.

        **Idempotent.** A kitchen that has already forked this row gets the fork
        it already has — `200` with `meta.created: false` — rather than a second
        copy competing with the first in every search. The first call answers
        `201` with `meta.created: true`.

        No `If-Match`: this creates a row rather than changing one, so there is
        no version to be stale against.

        The library row is then **hidden from that kitchen's ingredient list**,
        so the fork replaces it rather than sitting beside it.

        `400 request.invalid` when the target is not a platform-library row —
        a kitchen's own ingredient is already editable and has nothing to fork.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The fork this kitchen already had.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientEnvelope'
        '201':
          description: The newly created fork.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredients/{ingredient}/allergens:
    get:
      operationId: listIngredientAllergens
      tags:
        - Kitchen catalogue
      summary: Allergen mappings for an ingredient
      description: |
        Both layers in one list, each labelled by `layer`: the platform
        baseline (`platform_baseline`) and the calling organisation's overlay
        (`organisation_overlay`). A mapping editor that showed only the
        overlay would let a kitchen believe an ingredient carries no allergens
        when the baseline says it carries milk.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The visible allergen mappings.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientAllergenCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: replaceIngredientAllergens
      tags:
        - Kitchen catalogue
      summary: Replace the allergen mappings of one layer
      description: |
        A PUT, not a PATCH: the body is the complete allergen statement for
        one market scope, so an omitted class means "not present" and can be
        acted on. A partial merge would make "removed" and "omitted" the same
        request, which on an allergen list is the difference between a
        correction and a silent regression. An empty `mappings` array is a
        legitimate statement, not a missing field.

        The layer written is derived from the caller's organisation and is
        never a request field: a platform operator replaces the baseline, any
        other organisation replaces only its own overlay.

        **Upgrade-only.** An organisation overlay may add a class the baseline
        does not mention, and may raise a `may_contain` to a `contains`. It
        may never drop a baseline class or weaken one — that is
        `422 validation.failed` with
        `details.weakened_allergen_classes`. Over-declaring an allergen costs
        a customer a menu option; under-declaring one costs them an ambulance.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceIngredientAllergensRequest'
      responses:
        '200':
          description: The mappings now visible for this ingredient.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientAllergenCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/AllergenBaselineWeakened'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredients/{ingredient}/aliases:
    get:
      operationId: listIngredientAliases
      tags:
        - Kitchen catalogue
      summary: The other names an ingredient answers to
      description: |
        Aliases are what designation resolution matches on first — a supplier
        wording or kitchen shorthand is an explicit human decision that "this
        text means this ingredient", where a name match is only a coincidence
        of spelling.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The aliases of this ingredient.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/IngredientAlias'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: addIngredientAlias
      tags:
        - Kitchen catalogue
      summary: Add an alias
      description: |
        `alias_normalised` is derived by the server — lower-cased, trimmed,
        internal whitespace collapsed — and is never accepted from a client,
        so two writers cannot normalise differently and break lookup. A name
        that normalises to one the ingredient already answers to is
        `409 resource.conflict`.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - alias
              properties:
                alias:
                  type: string
                  minLength: 1
                  maxLength: 160
                  description: Verbatim, as the operator wrote it.
                locale:
                  type:
                    - string
                    - 'null'
                  enum:
                    - en
                    - ar
                    - null
              additionalProperties: false
      responses:
        '201':
          description: The alias was added.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - alias
                    properties:
                      alias:
                        $ref: '#/components/schemas/IngredientAlias'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredients/{ingredient}/aliases/{alias}:
    delete:
      operationId: removeIngredientAlias
      tags:
        - Kitchen catalogue
      summary: Remove an alias
      description: |
        A real delete, unlike almost everything else in the catalogue: an
        alias is a lookup convenience, not a food-safety record, and a wrong
        one actively misroutes a designation. The removal is audited.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/IngredientPath'
        - name: alias
          in: path
          required: true
          description: The alias identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '204':
          description: The alias was removed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredient-categories:
    get:
      operationId: listIngredientCategories
      tags:
        - Kitchen catalogue
      summary: The ingredient taxonomy
      description: |
        The platform taxonomy plus the organisation's own, flat, with
        `parent_id` carrying the two-level tree. Deliberately not paginated
        and not nested: the taxonomy is small, the client renders it in one
        pass, and paginating a tree would hand it half a hierarchy.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every visible category.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/IngredientCategory'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createIngredientCategory
      tags:
        - Kitchen catalogue
      summary: Create a category
      description: |
        `code` must be unique across everything the caller can see, the
        platform taxonomy included: a tenant code that shadowed a platform
        code would make every later "which category is this?" ambiguous, in
        the interface and in an import alike. A collision is
        `409 resource.conflict`.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
                - name_en
              properties:
                code:
                  type: string
                  maxLength: 40
                  pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
                name_en:
                  type: string
                  maxLength: 255
                name_ar:
                  type:
                    - string
                    - 'null'
                  maxLength: 255
                  description: Falls back to the English name when absent.
                parent_id:
                  oneOf:
                    - $ref: '#/components/schemas/Uuid'
                    - type: 'null'
                display_order:
                  type: integer
                  minimum: 0
                  maximum: 100000
              additionalProperties: false
      responses:
        '201':
          description: The category was created.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientCategoryEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/ingredient-categories/{category}:
    patch:
      operationId: updateIngredientCategory
      tags:
        - Kitchen catalogue
      summary: Update a category
      description: |
        No `If-Match`: categories carry no `lock_version`, and the
        concurrency contract applies only to resources that do. Sending a
        validator a resource cannot honour would be worse than sending none.

        `code` is not editable — it is the handle an import maps onto.
        Platform categories are readable and not writable.

        Requires `catalogue.manage_organisation`.
      parameters:
        - name: category
          in: path
          required: true
          description: The category identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                name_en:
                  type: string
                  maxLength: 255
                name_ar:
                  type: string
                  maxLength: 255
                parent_id:
                  oneOf:
                    - $ref: '#/components/schemas/Uuid'
                    - type: 'null'
                display_order:
                  type: integer
                  minimum: 0
                  maximum: 100000
                is_active:
                  type: boolean
              additionalProperties: false
      responses:
        '200':
          description: The updated category.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientCategoryEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes:
    get:
      operationId: listRecipes
      tags:
        - Recipes
      summary: List recipes
      description: |
        The organisation's recipe book, cursor-paginated. There is no platform
        library of recipes: a recipe belongs to exactly one organisation,
        always.

        `published_version_number` answers "what is live" without a second
        request. It is computed, not stored — there is no
        `current_version_id` column to fall out of step with the versions.

        Archived recipes are excluded unless `status=archived` is asked for by
        name.

        Requires `recipe.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/PageNumber'
        - $ref: '#/components/parameters/PerPage'
        - name: query
          in: query
          required: false
          description: Case-insensitive substring match over both names and the slug.
          schema:
            type: string
            maxLength: 160
        - name: status
          in: query
          required: false
          description: Restrict to one lifecycle state. Omitted, only `active` recipes are listed.
          schema:
            $ref: '#/components/schemas/RecipeStatus'
        - name: category
          in: query
          required: false
          description: Exact match on `recipe_category`.
          schema:
            type: string
            maxLength: 40
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of recipes.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminRecipe'
                  meta:
                    $ref: '#/components/schemas/PaginatedOrNumberedMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createRecipe
      tags:
        - Recipes
      summary: Create a recipe
      description: |
        Creates the recipe **and** its draft version 1, in one transaction. A
        recipe with no versions is a name with nothing behind it, and the
        version is the unit of work, so the response carries both rather than
        making the client fetch what the server just created.

        Derived values are never client-supplied: `status`, `lock_version`,
        `organisation_id` and the source-provenance fields are all
        server-authored.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRecipeRequest'
      responses:
        '201':
          description: The recipe and its first draft version.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - recipe
                      - version
                    properties:
                      recipe:
                        $ref: '#/components/schemas/AdminRecipe'
                      version:
                        $ref: '#/components/schemas/AdminRecipeVersion'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/roll-up-preview:
    post:
      operationId: previewRecipeRollup
      tags:
        - Recipes
      summary: What a draft formulation would declare
      description: |
        The figures for a set of lines **without persisting anything**, so a
        chef composing a version can see what it would say before anybody
        publishes it.

        **Authorised in the request rather than by route middleware**, and the
        `or` is the point: either `recipe.view_organisation` or
        `recipe.manage_organisation` will do. A refusal is `403
        authz.permission_denied` with an empty `details` — it comes from the
        form request's own gate, not from the permission middleware, so there
        is no `details.reason` to branch on.

        **Cost is omitted unless the caller also holds
        `recipe.view_costs_organisation`.** `estimated_cost` is then `null`,
        and so it is when no line is fully costed, when the costed lines
        disagree about currency, or when a line is costed only halfway — each
        of which adds a `warnings` entry naming which.

        **`per_recipe`, `per_serving` and `per_100g` are always `null`.**
        Nutrition roll-up is not computed on the server yet; the keys are here
        because the shape is the one the published version will carry, and a
        client that had to grow three fields later would be a client that
        shipped a screen with nothing to put in them. Every response therefore
        carries at least the `nutrition_unavailable` warning — `warnings` is
        never empty.

        **An unknown or archived ingredient is a warning, not a refusal.** A
        preview exists to be run against a half-finished draft, so a line
        pointing at nothing yields `rollup.unknown_ingredient` and the rest of
        the figures still come back. What *is* refused (`422`) is a
        contradiction the caller stated: a cost amount with no currency, a
        currency with no amount, or a measurement unit that does not exist.

        `servings` is required and `recipe_id` is accepted, but neither
        currently changes the answer — the per-serving figures are the `null`s
        above. They are part of the request because they are part of the
        question, and the day the roll-up computes they will be read.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PreviewRecipeRollupRequest'
      responses:
        '200':
          description: The figures the draft would declare. Nothing was written.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/RecipeRollupPreview'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}:
    get:
      operationId: showRecipe
      tags:
        - Recipes
      summary: A recipe and its version history
      description: |
        Returns `ETag: "<lock_version>"` — the recipe's validator, which a
        write sends back as `If-Match`.

        The version summaries come with it: opening a recipe and asking which
        versions it has is one interaction, and the list is bounded by how
        many times a kitchen has revised one formulation.

        A recipe belonging to another organisation is indistinguishable from
        one that does not exist: both are `resource.not_found`.

        Requires `recipe.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The recipe and every version of it.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - recipe
                      - versions
                    properties:
                      recipe:
                        $ref: '#/components/schemas/AdminRecipe'
                      versions:
                        type: array
                        items:
                          $ref: '#/components/schemas/AdminRecipeVersion'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updateRecipe
      tags:
        - Recipes
      summary: Update a recipe
      description: |
        Requires `If-Match` carrying the `ETag` the resource was last served
        with: absent is **428** `request.precondition_required`, stale is
        **409** `resource.conflict` with `details.current_lock_version`.

        `slug` and `status` are not editable here — the slug is the handle an
        import converges on, and status changes are lifecycle actions with
        their own routes.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRecipeRequest'
      responses:
        '200':
          description: The updated recipe, with its new validator.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/archive:
    post:
      operationId: archiveRecipe
      tags:
        - Recipes
      summary: Archive a recipe
      description: |
        A lifecycle action as a POST sub-resource, never a `PATCH status`.

        **Refused while a published version exists** —
        `409 catalogue.in_use`, with `details.published_version_ids`.
        Withdrawing something from sale is retiring the version, which has its
        own route and its own permission; letting archive do it implicitly
        would pull a live recipe through a route that never mentions
        publication.

        There is no delete. A recipe that has ever been published is part of a
        food-safety record.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The archived recipe.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/RecipeArchiveBlocked'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions:
    get:
      operationId: listRecipeVersions
      tags:
        - Recipes
      summary: The version history of a recipe
      description: |
        Unpaginated: the collection is bounded by how many times one kitchen
        has revised one formulation, and a cursor over a dozen rows would add
        a walk to every client for no benefit.

        Requires `recipe.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every version, oldest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminRecipeVersion'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createRecipeVersion
      tags:
        - Recipes
      summary: Open a new draft version
      description: |
        This is how a published version is changed: it is not. A published
        version is frozen, and the supported change is a new draft — optionally
        copied from an existing one, which is the normal path because a new
        version is almost always "last one, with a change".

        Copying carries lines, outputs, steps and **declared** allergen rows.
        Derived rows are not copied: they are a conclusion about a published
        state, and this version has none yet.

        No `If-Match` — nothing existing is being written.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRecipeVersionRequest'
      responses:
        '201':
          description: The new draft version.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeVersionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}:
    get:
      operationId: showRecipeVersion
      tags:
        - Recipes
      summary: A version, with its lines, outputs, steps and allergen label
      description: |
        Everything in one response: a recipe editor needs all of it to render
        anything, and four requests to open one screen is four chances of a
        partial view of a formulation.

        `{version}` accepts either the version identifier or the version
        number — a client that walked the list holds identifiers, a human
        reading a technical sheet holds "version 3".

        **Lines carry no cost fields.** The cost projection is K1.3, behind
        `recipe.view_costs_organisation`; until that permission exists there is
        no way to serve a cost to the right people.

        `ETag: "<lock_version>"` is the version's validator — the one the
        line, output and step replacements require as `If-Match`.

        Requires `recipe.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The whole version.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - version
                      - lines
                      - outputs
                      - steps
                      - allergens
                    properties:
                      version:
                        $ref: '#/components/schemas/AdminRecipeVersion'
                      lines:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipeLine'
                      outputs:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipeOutput'
                      steps:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipeStep'
                      allergens:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipeVersionAllergen'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updateRecipeVersion
      tags:
        - Recipes
      summary: Update a draft version's header
      description: |
        Editable only while the version is `draft` or `review_required`. A
        published or retired version is **409**
        `catalogue.version_immutable` — not a conflict, because reloading will
        never make it writable and the answer is a new draft version.

        `status`, `derivation_state`, `derived_at`, `published_at` and
        `published_by` are absent from the body by design: each is a
        conclusion the server reached, and a client that could post
        `derivation_state: current` could declare an allergen label fresh
        without anything having been computed.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRecipeVersionRequest'
      responses:
        '200':
          description: The updated version.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeVersionEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionImmutableOrConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/lines:
    put:
      operationId: replaceRecipeLines
      tags:
        - Recipes
      summary: Replace the lines of a version
      description: |
        A PUT because the body is the complete formulation. A PATCH surface
        would make "I removed the sesame line" and "I forgot to send the
        sesame line" the same request, and on a formulation that difference is
        the whole point. An empty `lines` array is a legitimate statement, not
        a missing field.

        `line_number` is not a field: the array order is the sequence, so a
        client never keeps numbering in step with an insertion. Duplicate
        ingredients are **allowed** — a sheet that adds olive oil to the
        marinade and again to the finish is two lines.

        `quantity` may be null while the version is unpublished: the
        sauces-and-dressings source lists typical ingredients with no amounts,
        and refusing to store that would mean fabricating numbers. The publish
        gate is where nullability ends.

        `If-Match` carries the **version's** `lock_version`, not a line's: the
        set is the unit of change.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceRecipeLinesRequest'
      responses:
        '200':
          description: The version and its new lines.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - version
                      - lines
                    properties:
                      version:
                        $ref: '#/components/schemas/AdminRecipeVersion'
                      lines:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipeLine'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionImmutableOrConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/packaging:
    put:
      operationId: replaceRecipePackaging
      tags:
        - Recipes
      summary: Replace the packaging of a version
      description: |
        The formulation's other half, and a PUT for the same reason `lines` is
        one: the body is the complete list, so "I removed the sleeve" and "I
        forgot to send the sleeve" stay different requests. An empty
        `packaging` array is a legitimate statement.

        `line_number` is not a field — the array order is the sequence.

        **The numbers that come back are not the numbers that went in**, and a
        client has to render the response rather than its own request. Two of
        the three bases compute their own quantity from the version's yield:
        `fills_yield` works out how many containers the yield fills, and
        `per_container` multiplies by that count. Only `per_batch` keeps the
        quantity as sent, and it is the only basis where `quantity` is
        required — on the other two it is refused, because a figure the server
        derives is one a client can disagree with.

        `If-Match` carries the **version's** `lock_version`, not a line's: the
        set is the unit of change.

        No cost fields, in either direction. The costed view of packaging is
        the technical sheet, behind `recipe.view_costs_organisation`; the unit
        cost is read from the catalogue row rather than quoted by the client,
        so the two can never disagree.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceRecipePackagingRequest'
      responses:
        '200':
          description: The version and its new packaging, as stored.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - version
                      - packaging
                    properties:
                      version:
                        $ref: '#/components/schemas/AdminRecipeVersion'
                      packaging:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipePackagingLine'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionImmutableOrConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/outputs:
    put:
      operationId: replaceRecipeOutputs
      tags:
        - Recipes
      summary: Replace what a version produces
      description: |
        `recipe_version_outputs` is what replaced
        `ingredients.produced_by_recipe_id`: a version may produce several
        things, the yield belongs to the version rather than the recipe, and an
        "intermediate" ingredient is simply one that appears here.

        An empty set is legitimate — a component whose yield nobody has
        measured. A non-empty set must name **exactly one** primary; zero or
        two is `422 validation.failed`, because "the thing this recipe makes"
        has to be answerable without a tie-break.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceRecipeOutputsRequest'
      responses:
        '200':
          description: The version and its new outputs.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - version
                      - outputs
                    properties:
                      version:
                        $ref: '#/components/schemas/AdminRecipeVersion'
                      outputs:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipeOutput'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionImmutableOrConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/steps:
    put:
      operationId: replaceRecipeSteps
      tags:
        - Recipes
      summary: Replace the method of a version
      description: |
        `instruction_ar` is optional while `instruction_en` is not: a kitchen
        writes the method it works in first, and blocking a draft on a
        translation nobody has written would only produce machine translation,
        which is forbidden for text a person acts on.

        Requires `recipe.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceRecipeStepsRequest'
      responses:
        '200':
          description: The version and its new steps.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - version
                      - steps
                    properties:
                      version:
                        $ref: '#/components/schemas/AdminRecipeVersion'
                      steps:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipeStep'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionImmutableOrConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/allergens:
    get:
      operationId: listRecipeVersionAllergens
      tags:
        - Recipes
      summary: The frozen allergen label of a version
      description: |
        Both kinds of row, each labelled by `derivation`: what was computed
        from the ingredients (`derived`, carrying the ingredient that caused
        it) and what a human stated (`declared`). A screen showing only one
        would either hide the chef's shared-fryer warning or hide the sesame
        the tahini brings.

        `meta.derivation_state` says whether the label still matches the
        mappings underneath it. A label read without that answer looks equally
        authoritative whether it is current or three mapping edits old.

        Requires `recipe.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The label rows.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/RecipeVersionAllergen'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                          - derivation_state
                        properties:
                          count:
                            type: integer
                            minimum: 0
                          derivation_state:
                            $ref: '#/components/schemas/DerivationState'
                          derived_at:
                            type:
                              - string
                              - 'null'
                            format: date-time
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/readiness:
    get:
      operationId: showRecipeVersionReadiness
      tags:
        - Recipes
      summary: Whether a version could be published, and why not
      description: |
        A read of the gate `POST …/publish` enforces — the same evaluator, not
        a second opinion about it. `RecipeVersionReadiness` is asked here and
        thrown from there, so an answer of `publishable: true` is the answer
        the publish attempt will give a moment later against the same rows.

        What that buys is a review queue that can show thirty formulations'
        readiness without publishing any of them, and a version editor that can
        show what is still missing while somebody is fixing it rather than only
        after they press publish. Discovering a five-minute fix by attempting a
        publication is how a kitchen learns to attempt publications.

        Every reason is `{code, detail, context}`:

        - `code` is the stable machine key — the same vocabulary
          `catalogue.publish_blocked` uses in `details.reasons[].reason`.
        - `detail` is a sentence for a human. Clients that render their own
          copy per code should ignore it; clients that need to say *something*
          about a code they have not seen before should print it.
        - `context` carries whatever identifies the offending rows —
          `line_numbers`, `ingredient_ids`, `status`, `review_reason`. It is a
          nested object rather than sibling keys so that a reason carrying new
          structure never changes the shape of a reason. It is `{}` when the
          code says everything there is to say.

        The vocabulary: `version_quarantined`, `version_not_a_draft`,
        `no_lines`, `line_quantity_missing`, `ingredient_requires_review`,
        `allergen_unmapped`.

        **`allergen_unmapped` is reported here as an ordinary reason** even
        though publication raises it under its own error code
        (`catalogue.allergen_unmapped`, 422) rather than as a
        `publish_blocked` reason. Publication draws that distinction because
        the fix is in a different place — the ingredient's mapping editor, not
        the recipe. This endpoint is asked "why not", and that is one more
        answer.

        `publishable` is exactly `reasons == []`. Both are on the wire because
        a badge wants the boolean and a checklist wants the list, and deriving
        one from the other at every call site is how the two drift apart in a
        UI.

        Requires **`recipe.view_organisation`**, deliberately not
        `recipe.publish_organisation`. The chef who has to fix a formulation is
        the one who has to be able to see what is wrong with it; guarding the
        diagnosis behind the authority to publish would leave the only person
        who can see the problem unable to do anything about it.

        No `ETag` and no `If-Match`. Readiness is not a resource with a version
        — it is a verdict about one, recomputed on every call because its
        inputs (allergen mappings, ingredient verification) move underneath it.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The verdict, and every reason behind it.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/PublicationReadiness'
                  meta:
                    $ref: '#/components/schemas/RecipeVersionReadinessMeta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/publish:
    post:
      operationId: publishRecipeVersion
      tags:
        - Recipes
      summary: Publish a version
      description: |
        Evaluates readiness, freezes the allergen label, and demotes whatever
        was published before to `retired` — one transaction.

        The gates:

        - **State.** Only a `draft` publishes. A quarantined version
          (`review_required`) is blocked *structurally*: that is what the
          state is for.
        - **Substance.** A version with no lines is a name, and a line without
          a quantity and a unit cannot be made twice the same way.
        - **Ingredient trust.** An ingredient marked `requires_review` — the
          burghul/pita contradiction the source workbook contains — blocks
          publication until a human resolves it.
        - **Allergen determination.** Every line ingredient must carry at
          least one mapping row in some layer, **or** be `verified`, which is
          how "checked, and it carries nothing" is recorded. Silence is not a
          statement of absence.

        The first three are reported together as **409**
        `catalogue.publish_blocked` with `details.reasons` — every blocker at
        once, so a kitchen fixes them in one pass instead of one per attempt.
        The fourth is **422** `catalogue.allergen_unmapped` with
        `details.ingredient_ids`, because the fix is in a different place: the
        ingredient's mapping editor, not the recipe.

        Requires **`recipe.publish_organisation`** — a permission of its own.
        Freezing a label that reaches a diner and withdrawing what was live is
        a different authority from editing a draft.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The published version and the label it froze.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - version
                      - allergens
                    properties:
                      version:
                        $ref: '#/components/schemas/AdminRecipeVersion'
                      allergens:
                        type: array
                        items:
                          $ref: '#/components/schemas/RecipeVersionAllergen'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PublishBlocked'
        '422':
          $ref: '#/components/responses/AllergenUnmapped'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/retire:
    post:
      operationId: retireRecipeVersion
      tags:
        - Recipes
      summary: Retire a published version
      description: |
        Withdraws a published version. Terminal, deliberately: a retired
        version is history, and history is what makes an old allergen label
        reconstructable. Bringing a formulation back means publishing a new
        version, which recomputes the label against the mappings as they are
        now rather than as they were.

        Retiring anything that is not published is `409 resource.conflict`.

        Requires `recipe.publish_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The retired version.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeVersionEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/technical-sheet:
    get:
      operationId: showRecipeTechnicalSheet
      tags:
        - Recipes
      summary: The costed view of a formulation
      description: |
        The confidential cost projection — the only place in this API where a
        recipe line carries money.

        Requires **`recipe.view_costs_organisation`**, which is deliberately
        not `recipe.view_organisation`: a line cook needs the method and the
        allergen label to make the dish and must not thereby read the margin on
        it. Every other recipe endpoint serialises no cost field at all, to
        anybody, and a key sweep across that surface enforces it.

        The response answers what a paper technical sheet answers:

        - **What goes in and what it costs** — `lines`, with `unit_cost_amount`,
          `line_cost_amount` and `cost_currency_code`. The two amounts are both
          on the wire even though one is derivable from the other, because on
          an imported sheet they legitimately disagree: the source's own
          arithmetic is stored verbatim, errors included.
        - **What the whole thing costs** — `snapshots`, keyed by basis. Both
          keys are always present so a client never has to guess whether a
          missing key means "none" or "not asked for"; either may be `null`.
        - **What is missing** — `uncosted_line_numbers`, and `currency_conflict`
          for the import case where two currencies ended up on one sheet.

        **The snapshots are read, not recomputed.** A sheet that recalculated
        on every GET would show a figure no snapshot records, changing as
        ingredient prices move — precisely what a costed technical sheet exists
        to pin down. Recomputing is an explicit `POST` to `cost-snapshots`.

        Every successful read writes a `catalogue.technical_sheet_viewed` audit
        event carrying a purpose of use and the `confidential` classification.
        The event records identifiers and counts only: an audit row is readable
        with `audit.view_organisation`, so an amount there would route around
        the permission split entirely.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The costed sheet.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/TechnicalSheet'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/recipes/{recipe}/versions/{version}/cost-snapshots:
    get:
      operationId: listRecipeCostSnapshots
      tags:
        - Recipes
      summary: The cost history of a version, newest first
      description: |
        An append-only ledger: `UPDATE` and `DELETE` are revoked from the
        application database role, so a snapshot is superseded by writing a
        newer one and never by editing an old one.

        Paginated where the *version* list is not, and for the opposite reason:
        a version list is bounded by how many times one kitchen revised one
        formulation, while this grows every time anybody asks what the recipe
        costs today.

        Newest first, because the question is almost always "what does it cost
        now". The keyset runs over when the snapshot was **recorded**, not over
        `calculated_at`: the importer can record a sheet dated last spring, and
        a cursor over a value the importer chooses would let a back-dated
        import interleave itself into a page a client is halfway through
        walking.

        Requires `recipe.view_costs_organisation`.
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of cost snapshots, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CostSnapshot'
                  meta:
                    $ref: '#/components/schemas/PaginationMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createRecipeCostSnapshot
      tags:
        - Recipes
      summary: Cost this version now and append the result
      description: |
        Recomputes the version's cost from its own lines and appends a
        snapshot.

        Requires **both** `recipe.manage_organisation` (this writes) and
        `recipe.view_costs_organisation` (what it writes is money). Neither
        alone is enough.

        No `If-Match`: a snapshot appends to a ledger beside the version rather
        than mutating it, so there is no lost update for a validator to
        prevent.

        `basis` may only be `recalculated`. `as_recorded` is a real value of
        the column and is deliberately not accepted here — it means "this is
        what a source technical sheet stated", and the only thing that holds a
        source sheet is the private importer. An endpoint accepting it would
        let somebody type figures in and have them stored as evidence of a
        document nobody read.

        No amounts in the body at all: every figure is derived from the lines
        and the yield, and a derived value a client can supply is one that can
        disagree with its inputs.

        Three refusals. A **retired** version is `409 resource.conflict` —
        retirement is terminal and a fresh cost for a withdrawn formulation has
        nowhere to go. A version that is **not fully costed** is
        `422 validation.failed` naming `uncosted_line_numbers`, and no snapshot
        is written: a total that silently omits a line reads exactly like a
        total that did not. Lines in **more than one currency** are
        `422 validation.failed` naming them, because there is no exchange rate
        anywhere in this system.

        Success does **not** touch `completeness`. Only publication flips a
        version to `costed`; letting an ad-hoc recalculation promote a draft
        would make the field mean "somebody once pressed a button".
      parameters:
        - $ref: '#/components/parameters/RecipePath'
        - $ref: '#/components/parameters/RecipeVersionPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCostSnapshotRequest'
      responses:
        '201':
          description: The appended snapshot.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - snapshot
                    properties:
                      snapshot:
                        $ref: '#/components/schemas/CostSnapshot'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /reference/allergen-classes:
    get:
      operationId: listAllergenClasses
      tags:
        - Reference data
      summary: The allergen vocabulary
      description: |
        **Anonymous.** An allergy filter that only works after sign-in is not
        an allergy filter, so this endpoint takes no credential.

        A public projection: one server-localised `name` and `description`
        chosen from `Accept-Language` (`en` or `ar`; anything else falls back
        to English rather than failing), never both language columns.
        Deactivated classes are excluded — a class the platform has withdrawn
        must not be offered to a customer, while every historical reference to
        its code still resolves internally.

        The list is small and constant, so it is not paginated.
      security: []
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The active allergen classes, in display order.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PublicAllergenClass'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        properties:
                          count:
                            type: integer
                            minimum: 0
                          locale:
                            type: string
                            enum:
                              - en
                              - ar
                            description: The locale the names were rendered in.
              example:
                data:
                  - code: gluten
                    name: Cereals containing gluten
                    description: Wheat, rye, barley, oats and their hybrids.
                    regulatory_ref: ALG-01
                    is_eu_14: true
                    is_us_big_9: true
                    us_declaration_required: false
                    us_threshold_ppm: null
                    display_order: 1
                meta:
                  correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
                  count: 14
                  locale: en
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createAllergenClass
      tags:
        - Reference data
      summary: Create an allergen class
      description: |
        Platform operators only. Two gates, both required: `platform.context`
        (the selected organisation must itself be of type
        `platform_operator`) and the `reference.manage_platform` permission.
        An organisation that somehow held the permission still could not reach
        this endpoint, because an organisation type is not something a tenant
        can grant itself. Either gate denies with
        `authz.permission_denied` and `details.reason = policy_denied` or
        `permission_not_granted`.

        Adding a class is rare and irreversible in one respect: every
        organisation inherits it, and the `code` can never be taken back.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAllergenClassRequest'
      responses:
        '201':
          description: The allergen class was created.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AllergenClassEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /reference/allergen-classes/{code}:
    patch:
      operationId: updateAllergenClass
      tags:
        - Reference data
      summary: Update an allergen class
      description: |
        Names, translations, descriptions, market applicability and
        thresholds are editable. **The `code` is not.** It is a regulatory
        identity that mappings, customer declarations and frozen recipe
        labels all point at, so a request carrying `code` is refused with
        `422 validation.failed` rather than silently ignored.

        Inactive classes are reachable here on purpose: an administrator must
        be able to correct a withdrawn class's Arabic name so old labels keep
        rendering.

        Platform operators only — see `createAllergenClass` for the two gates.
      parameters:
        - $ref: '#/components/parameters/AllergenCodePath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAllergenClassRequest'
      responses:
        '200':
          description: The updated allergen class.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AllergenClassEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /reference/allergen-classes/{code}/deactivate:
    post:
      operationId: deactivateAllergenClass
      tags:
        - Reference data
      summary: Withdraw an allergen class
      description: |
        There is no DELETE on this resource and there never will be.
        Deactivation withdraws a class from the public vocabulary while
        leaving every mapping, customer declaration and frozen recipe label
        that references the code still resolvable to a readable name. A delete
        would either orphan those references or cascade through a food-safety
        record.

        Deactivating an already inactive class is `409 resource.conflict`.

        Platform operators only — see `createAllergenClass` for the two gates.
      parameters:
        - $ref: '#/components/parameters/AllergenCodePath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The withdrawn allergen class.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AllergenClassEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/sales-channels:
    get:
      operationId: listSalesChannels
      tags:
        - Sellable catalogue
      summary: List sales channels
      description: |
        The organisation's routes to market. Unpaginated: a kitchen has a
        handful of them, not a catalogue, and a cursor over five rows is
        ceremony.

        Inactive channels are included — a merchandiser assigning availability
        needs to see that the wholesale desk exists and is switched off.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every sales channel of the organisation.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminSalesChannel'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        properties:
                          count:
                            type: integer
                            minimum: 0
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createSalesChannel
      tags:
        - Sellable catalogue
      summary: Create a sales channel
      description: |
        A channel is a row an organisation owns, not a value from a closed
        union: two kitchens legitimately run two different wholesale desks with
        different availability and different price lists. `channel_kind` is the
        closed vocabulary a projection branches on; `code` is the kitchen's own
        name for one instance of it.

        `status` is not accepted: a new channel is always active, and one
        created inactive is a row nobody would notice was there.

        A duplicate code is `409 resource.conflict`, not a second channel.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSalesChannelRequest'
      responses:
        '201':
          description: The created channel.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesChannelEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/sales-channels/{channel}:
    get:
      operationId: showSalesChannel
      tags:
        - Sellable catalogue
      summary: A sales channel
      description: |
        Returns `ETag: "<lock_version>"` — the channel's validator, which a
        write sends back as `If-Match`.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/SalesChannelPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The channel.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesChannelEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updateSalesChannel
      tags:
        - Sellable catalogue
      summary: Update a sales channel
      description: |
        Neither `code` nor `channel_kind` is accepted. The code is what a price
        list, an availability rule and (from C1) an order's provenance name;
        the kind decides whether prices behind the channel are contract-private,
        and flipping a live b2b desk to `b2c_web` would reclassify every one of
        them in a single write with no record of what had been private.

        `status` **is** accepted here rather than being a POST action, unlike
        catalogue-item publication: activating a channel is operational
        configuration, not a decision about what reaches a customer.

        **`If-Match` required.** Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/SalesChannelPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSalesChannelRequest'
      responses:
        '200':
          description: The updated channel.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesChannelEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/references/next:
    get:
      operationId: nextCatalogueReference
      tags:
        - Sellable catalogue
      summary: The handle the next record of a kind will take
      description: |
        A reference is assigned on create — `ING-307`, `SAC-016` — and a create
        form draws it before there is a record to read it from. This is the same
        scan the create performs, exposed so the number on the form and the
        number on the record come from one implementation rather than two.

        **A preview, never a reservation.** Two people opening a create form at
        the same moment are both told `ING-307`; the first to save takes it and
        the second saves at `ING-308`. That is the honest behaviour of a series
        derived by scanning, and it is deliberate: a counter handing out
        reservations would leave permanent holes wherever a form was abandoned,
        in a sequence whose whole purpose is to be read down a column. Nothing
        may treat this answer as a promise — the record's reference is whatever
        the create response carries back.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: prefix
          in: query
          required: true
          description: |
            Which series to read, and each is counted in the table its existing
            handles are in: `ING-` among the ingredients, `RC-` among the
            recipes, and `SAC-` and `DRS-` among the catalogue items — where the
            import wrote forty-three sauces and nineteen dressings.
          schema:
            type: string
            enum:
              - ING-
              - RC-
              - SAC-
              - DRS-
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The next handle in that series.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - reference
                    properties:
                      reference:
                        type: string
                        maxLength: 40
                        example: ING-307
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items:
    get:
      operationId: listCatalogueItems
      tags:
        - Sellable catalogue
      summary: List catalogue items
      description: |
        Everything this organisation sells — products, meals and subscription
        plans in one cursor-paginated list, discriminated by `item_type`.

        Retired items are excluded unless `status=retired` is asked for by
        name: a list that offered withdrawn articles by default invites
        somebody to price one.

        **No item here carries a price, a cost or a margin**, and none ever
        will: money lives in the pricing family, and the admin contract's
        confidential `marginPercent` is a presentation figure with no
        server-side home.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/PageNumber'
        - $ref: '#/components/parameters/PerPage'
        - name: query
          in: query
          required: false
          description: Case-insensitive substring match over both names and the slug.
          schema:
            type: string
            maxLength: 160
        - name: status
          in: query
          required: false
          description: |
            Restrict to one publication state. Omitted, everything except
            `retired` is listed.
          schema:
            $ref: '#/components/schemas/CatalogueItemStatus'
        - name: item_type
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/CatalogueItemType'
        - name: product_category_id
          in: query
          required: false
          description: Exact match on the merchandising category.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of catalogue items.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminCatalogueItem'
                  meta:
                    $ref: '#/components/schemas/PaginatedOrNumberedMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createCatalogueItem
      tags:
        - Sellable catalogue
      summary: Create a catalogue item
      description: |
        Creates a **draft**. Nothing is ever created published: publication is
        a separate action, with a separate permission, behind a readiness gate.

        `slug` is derived from the English name unless one is supplied, and is
        immutable thereafter.

        `name_ar` may be omitted and is then stored as the empty string, which
        is the "not yet translated" state the publish gate refuses. Unlike a
        recipe, the Arabic name does **not** fall back to the English one: a
        catalogue item is customer-facing, so an untranslated name has to be
        visible as untranslated.

        `catalogue_id` may be omitted; the organisation's `default` catalogue
        is created on demand.

        Variants, ingredients, diet tags and channels are each their own
        set-replace endpoint and are not accepted here.

        Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCatalogueItemRequest'
      responses:
        '201':
          description: The created draft item.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogueItemEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}:
    get:
      operationId: showCatalogueItem
      tags:
        - Sellable catalogue
      summary: A catalogue item and everything hanging off it
      description: |
        Returns `ETag: "<lock_version>"` — the item's validator, which every
        write on it and on its sub-resources sends back as `If-Match`.

        Variants, the public ingredient list, diet tags and channel
        availability come with it: opening a listing and asking what it is made
        of is one interaction, not five, and each set is bounded by what a
        human typed into one form.

        Diet classifications are returned as **codes**, matching what the
        setter accepts.

        An item belonging to another organisation is indistinguishable from one
        that does not exist: both are `resource.not_found`.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The item with its variants, ingredients, diet tags and channels.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - item
                      - variants
                      - ingredients
                      - diet_classifications
                      - channels
                    properties:
                      item:
                        $ref: '#/components/schemas/AdminCatalogueItem'
                      variants:
                        type: array
                        items:
                          $ref: '#/components/schemas/AdminCatalogueItemVariant'
                      ingredients:
                        type: array
                        items:
                          $ref: '#/components/schemas/AdminCatalogueItemIngredient'
                      diet_classifications:
                        type: array
                        items:
                          type: string
                          maxLength: 40
                      channels:
                        type: array
                        items:
                          $ref: '#/components/schemas/AdminChannelAssignment'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updateCatalogueItem
      tags:
        - Sellable catalogue
      summary: Update a catalogue item
      description: |
        A rename changes the names. `slug` and `item_type` are **refused**
        rather than ignored — a slug is what a link, a marketplace listing and
        a partner's integration hold, and silently dropping a field a client
        believed it was writing teaches nobody anything.

        `status` is absent for the same reason it is on every lifecycle-bearing
        resource: publication and retirement are POST sub-resource actions with
        their own permission and their own audit events.

        A retired item is frozen and every write to it is `409
        resource.conflict`. A **published** one stays editable: an item is a
        listing, and fixing a typo in a live description must not require
        withdrawing it from sale.

        **`If-Match` required.** Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCatalogueItemRequest'
      responses:
        '200':
          description: The updated item.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogueItemEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/publish:
    post:
      operationId: publishCatalogueItem
      tags:
        - Sellable catalogue
      summary: Publish a catalogue item
      description: |
        Its own route, its own permission and its own audit action — never a
        `PATCH status`. Deciding what a customer can buy is a different
        authority from writing the listing.

        The **K1.4 gate is deliberately minimal** and the full readiness
        evaluator arrives later; what it can evaluate honestly today is:

        - only a `draft` publishes — `review_required` is a structural
          quarantine, `published` and `retired` are not candidates;
        - both language names are present;
        - a product has at least one `active` variant;
        - a meal has an allergen basis: a linked recipe with a published
          version, **or** at least one row on its public ingredient list;
        - a linked recipe carrying a live `review_required` version blocks the
          item too — a quarantine is an unresolved food-safety contradiction,
          and it propagates.

        Refusals are structured: `catalogue.publish_blocked` carries **every**
        reason at once, so a kitchen fixes them in one pass rather than one per
        attempt.

        The response includes the derived allergen set, because "what did I
        just tell a diner this contains" is the only question worth asking
        immediately afterwards.

        **`If-Match` required.** Requires `catalogue.publish_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The published item and the allergen set its listing will show.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - item
                      - allergens
                    properties:
                      item:
                        $ref: '#/components/schemas/AdminCatalogueItem'
                      allergens:
                        $ref: '#/components/schemas/DerivedAllergenSet'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CatalogueItemPublishBlocked'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/retire:
    post:
      operationId: retireCatalogueItem
      tags:
        - Sellable catalogue
      summary: Retire a catalogue item
      description: |
        The only withdrawal a sellable item has. There is no archive action and
        no delete anywhere: an order or a price snapshot may point at a row
        forever, so a retired item keeps its history and disappears from every
        consumer read.

        Available from every state except `retired` itself, **including
        `draft`** — refusing to retire a draft would leave a kitchen with no
        way at all to withdraw a listing it decided against.

        A `reason` is optional; where one is given it is recorded on the audit
        event, because "why did this disappear" is the first question asked six
        months later.

        **`If-Match` required.** Requires `catalogue.publish_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RetireCatalogueItemRequest'
      responses:
        '200':
          description: The retired item.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogueItemEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/variants:
    put:
      operationId: replaceCatalogueItemVariants
      tags:
        - Sellable catalogue
      summary: Replace an item's variants
      description: |
        A PUT because the body is the complete set of packs or configurations.
        A PATCH surface would make "I did not touch the packs" and "I deleted
        every pack" the same request, and on the thing a price points at that
        difference is the whole point.

        Rows are matched by `code`, not by position and not by identifier: a
        code is stable within the item and is what a price list will name, so a
        submitted set keeps the identifiers a price already points at. An `id`
        may be sent and is checked for agreement.

        **Absent codes are archived, not deleted** — the catalogue never loses
        a row an order might reference — and re-submitting an archived code
        revives it.

        `variant_type` is derived from the item's own type and is never
        accepted: a product has packs, a subscription plan has configurations,
        and a **meal has neither**, so a submission against one is refused.
        A pack must state its size; a plan configuration must not carry one.

        Exactly one variant is the default. Two marked defaults is a stated
        contradiction and is refused; none is not, and the first submitted
        wins.

        **`If-Match` carries the item's `lock_version`**, not a variant's: the
        set is the unit of change. Requires `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceCatalogueItemVariantsRequest'
      responses:
        '200':
          description: |
            The item and **every** variant it now has, including the ones this
            call archived — so a client sees what the submission did rather
            than what it sent.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - item
                      - variants
                    properties:
                      item:
                        $ref: '#/components/schemas/AdminCatalogueItem'
                      variants:
                        type: array
                        items:
                          $ref: '#/components/schemas/AdminCatalogueItemVariant'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/ingredients:
    put:
      operationId: replaceCatalogueItemIngredients
      tags:
        - Sellable catalogue
      summary: Replace an item's public ingredient list
      description: |
        What a diner is told is in the dish, in the order the kitchen chose to
        name it. **Not the formulation**: there is no quantity field here and
        none is stored, because quantities are confidential and a shape that
        accepted one would put a competitor's proportions behind an endpoint
        whose whole purpose is what a customer reads.

        The array order **is** `display_order`: naming order is a labelling
        convention (descending by weight, in most regimes), not something to
        re-sort alphabetically.

        These rows are also a meal's **allergen basis of last resort** — the
        fallback the derivation uses when no published recipe version exists.

        An empty array is a legitimate statement and is accepted.

        **`If-Match` (the item's) required.** Requires
        `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceCatalogueItemIngredientsRequest'
      responses:
        '200':
          description: The item and its public ingredient list.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - item
                      - ingredients
                    properties:
                      item:
                        $ref: '#/components/schemas/AdminCatalogueItem'
                      ingredients:
                        type: array
                        items:
                          $ref: '#/components/schemas/AdminCatalogueItemIngredient'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/diet-classifications:
    put:
      operationId: replaceCatalogueItemDietClassifications
      tags:
        - Sellable catalogue
      summary: Replace an item's diet tags
      description: |
        Tags arrive as platform **codes** (`vegan`, `keto`), which is what a
        human reads and what the public vocabulary endpoint serves. A code that
        does not resolve to an *active* classification is a validation failure,
        never an invitation to create one: a kitchen that could invent
        "keto-ish" would make the customer-side filter meaningless the first
        time two kitchens spelt one idea differently.

        A diet tag is a **preference filter, never an allergen statement**.
        `gluten_free` says the kitchen sells this as part of a gluten-free
        line; what the dish contains is the derived allergen set.

        **`If-Match` (the item's) required.** Requires
        `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceCatalogueItemDietClassificationsRequest'
      responses:
        '200':
          description: The item and its diet tags.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - item
                      - diet_classifications
                    properties:
                      item:
                        $ref: '#/components/schemas/AdminCatalogueItem'
                      diet_classifications:
                        type: array
                        items:
                          type: string
                          maxLength: 40
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/channels:
    put:
      operationId: replaceCatalogueItemChannels
      tags:
        - Sellable catalogue
      summary: Replace an item's channel availability
      description: |
        The complete availability list, applied at once. "Web shop yes,
        wholesale from March, marketplace no" is one decision, and applying
        half of it leaves an offering nobody meant to make. An empty array
        withdraws the item from every channel, which is a decision a kitchen
        makes rather than a field it forgot.

        A row may name a variant or not: naming none offers the item as a
        whole, naming one offers exactly that pack or configuration.

        Dates are **dates**, not timestamps — a kitchen decides "from Monday",
        not "from midnight in some timezone".

        Availability is not publication: an item can be a complete, published,
        sellable article that no channel offers yet. There is deliberately no
        price here either; what an offering costs is a price-list row.

        **`If-Match` (the item's) required.** Requires
        `catalogue.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceCatalogueItemChannelsRequest'
      responses:
        '200':
          description: The item and its channel assignments.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - item
                      - channels
                    properties:
                      item:
                        $ref: '#/components/schemas/AdminCatalogueItem'
                      channels:
                        type: array
                        items:
                          $ref: '#/components/schemas/AdminChannelAssignment'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/allergens:
    get:
      operationId: showCatalogueItemAllergens
      tags:
        - Sellable catalogue
      summary: An item's derived allergen set
      description: |
        Read-only, and **there is no writer** — that is the point. An item's
        allergens are never authored here: they come from a published recipe
        version's frozen label, or from the union of the item's own listed
        ingredients' effective mappings. An endpoint that let a merchandiser
        type an allergen onto a listing would be an endpoint that lets a
        merchandiser overrule a chef.

        Computed on read, never stored. There is no roll-up column and no
        per-item allergen table: the joins are cheap, and a stored copy goes
        stale the moment a mapping changes. Stored labels exist exactly where
        freezing is the point — on a published recipe version.

        `meta.basis` says which source answered:

        - `recipe_version` — the linked recipe's published version. Its frozen
          rows are the answer, with `declared` preserved as `declared`.
        - `item_ingredients` — the union of the listed ingredients' effective
          mappings, strongest containment winning, with `source_ingredient_id`
          naming the ingredient that made the stronger claim.
        - `none` — an empty set that means **"nobody has said"**, never "no
          allergens". Silence is not a statement of absence, and the publish
          gate refuses a meal in this state.

        Requires `catalogue.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The derived allergen set and the basis it was derived from.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - allergens
                    properties:
                      allergens:
                        type: array
                        items:
                          $ref: '#/components/schemas/DerivedAllergen'
                  meta:
                    $ref: '#/components/schemas/DerivedAllergenMeta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/availability:
    put:
      operationId: replaceCatalogueItemAvailability
      tags:
        - Sellable catalogue
      summary: Replace a meal's per-day availability
      description: |
        A PUT because the body is the complete calendar. **`days: []` clears
        every day**, which is a thing somebody may genuinely mean, and a PATCH
        surface would make it indistinguishable from "I did not touch the
        calendar".

        **Meals only.** A product or a subscription plan does not carry
        per-day availability, and a submission against one is `422
        validation.failed` with `details.item_type` naming what it actually is
        — note that this one refusal puts the offending value at the top of
        `details` rather than inside `fields`.

        **The request field is `remaining`; the response field is
        `remaining_portions`.** They are the same number and the asymmetry is
        real, not a documentation slip: the request says how many are left to
        sell, and the row records it as a portion count.

        `order_cut_off_at` is accepted as `HH:MM` or `HH:MM:SS` and is
        **always served back as `HH:MM`** — a cut-off is a wall-clock minute
        and the seconds were never meaningful.

        A date stated twice is refused rather than last-one-wins: two rows for
        one day is a contradiction the caller can see and the server cannot
        resolve.

        `If-Match` carries the **item's** `lock_version`, not a day's: the
        calendar is the unit of change, and a per-row validator would let two
        editors replace different halves of one week. Requires
        `catalogue.manage_organisation`, and the kitchen must be trading — a
        suspended one is `409 organisation.suspended`, refused before the
        permission is even consulted.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceCatalogueItemAvailabilityRequest'
      responses:
        '200':
          description: The item and the calendar it now has, in date order.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - item
                      - availability_days
                    properties:
                      item:
                        $ref: '#/components/schemas/AdminCatalogueItem'
                      availability_days:
                        type: array
                        items:
                          $ref: '#/components/schemas/CatalogueItemAvailabilityDay'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/items/{item}/readiness:
    get:
      operationId: showCatalogueItemReadiness
      tags:
        - Sellable catalogue
      summary: Whether a listing could be published, and why not
      description: |
        A read of the gate `POST …/publish` enforces — the same evaluator, not
        a second opinion about it. `CatalogueItemReadiness` is asked here and
        thrown from there, so an answer of `publishable: true` is the answer
        the publish attempt will give a moment later against the same rows.

        What that buys is a review queue that can show thirty listings'
        readiness without publishing any of them, and a plan editor that can
        show which cells of a price matrix are still empty while somebody is
        filling them in.

        Every reason is `{code, detail, context}`:

        - `code` is the stable machine key — the same vocabulary
          `catalogue.publish_blocked` uses in `details.reasons[].reason`.
        - `detail` is a sentence for a human. Clients that render their own
          copy per code should ignore it; clients that need to say *something*
          about a code they have not seen before should print it.
        - `context` carries whatever identifies the offending rows — `fields`,
          `status`, `review_reason`, `recipe_version_ids`,
          `catalogue_item_variant_ids` and the `configurations` beside them. It
          is a nested object rather than sibling keys so that a reason carrying
          new structure never changes the shape of a reason. It is `{}` when
          the code says everything there is to say.

        The vocabulary: `item_quarantined`, `item_not_a_draft`,
        `translation_incomplete`, `no_active_variant`, `no_allergen_basis`,
        `linked_recipe_quarantined`, and — for subscription plans only —
        `plan_profile_missing`, `no_active_plan_configuration`,
        `no_duration_assigned`, `plan_prices_incomplete`.

        `publishable` is exactly `reasons == []`. Both are on the wire because
        a badge wants the boolean and a checklist wants the list, and deriving
        one from the other at every call site is how the two drift apart in a
        UI.

        **`publishable: true` is not a statement about permission.** A
        subscription plan additionally needs `plan.publish_organisation`,
        checked inside the publish action because that is the only layer that
        has loaded the row and can therefore know it is a plan. This endpoint
        answers "is the data ready", never "may you"; conflating the two would
        make a merchandiser without plan authority read "not ready" about a
        plan that is perfectly ready.

        Requires **`catalogue.view_organisation`**, deliberately not
        `catalogue.publish_organisation`. Whoever has to complete a listing is
        the one who has to be able to see what it is still missing. It follows
        that a caller who can read this may learn that a plan is unpriced —
        which is a boolean about completeness and never an amount, the same
        line the confirmed-price port draws.

        No `ETag` and no `If-Match`. Readiness is not a resource with a version
        — it is a verdict about one, recomputed on every call because its
        inputs (prices, variants, the linked recipe's state) move underneath
        it.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The verdict, and every reason behind it.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/PublicationReadiness'
                  meta:
                    $ref: '#/components/schemas/CatalogueItemReadinessMeta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /reference/diet-classifications:
    get:
      operationId: listDietClassifications
      tags:
        - Reference data
      summary: The platform diet vocabulary
      security: []
      description: |
        **Anonymous.** A diet filter that only works after sign-in is not a
        diet filter — the argument the public allergen-class list makes,
        applied to the softer of the two vocabularies.

        Served through the public projection: one server-localised `name`
        chosen from `Accept-Language`, never both language columns. Inactive
        classifications are excluded.

        A classification is a **preference filter, never a medical
        restriction**. What a dish contains is its allergen declaration; this
        says how a person describes the pattern of what they eat.

        Unpaginated: the list is small and constant.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The active diet classifications, localised.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PublicDietClassification'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        properties:
                          count:
                            type: integer
                            minimum: 0
                          locale:
                            type: string
                            enum:
                              - en
                              - ar
                            description: The locale the names were rendered in.
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/price-lists:
    get:
      operationId: listPriceLists
      tags:
        - Pricing
      summary: List price lists
      description: |
        Every tariff this organisation quotes, cursor-paginated. Unlike the
        sales-channel list this one pages: a kitchen has a handful of channels
        but can accumulate a tariff per corporate client.

        Archived lists are excluded unless `status=archived` is asked for by
        name — a list offered by default is a list somebody assigns to a
        channel.

        **No amounts.** The header carries a currency and a status, never a
        price. Embedding rows here would serve an entire kitchen's negotiated
        pricing in one response, and a cursor page is exactly the shape that
        gets exported wholesale.

        Requires `price_list.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/PageNumber'
        - $ref: '#/components/parameters/PerPage'
        - name: query
          in: query
          required: false
          description: Case-insensitive substring match over both names and the code.
          schema:
            type: string
            maxLength: 160
        - name: status
          in: query
          required: false
          description: Restrict to one lifecycle state. Omitted, everything except `archived` is listed.
          schema:
            $ref: '#/components/schemas/PriceListStatus'
        - name: customer_scope
          in: query
          required: false
          description: Restrict to public tariffs or to negotiated agreements.
          schema:
            $ref: '#/components/schemas/PriceListCustomerScope'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of price lists.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminPriceList'
                  meta:
                    $ref: '#/components/schemas/PaginatedOrNumberedMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createPriceList
      tags:
        - Pricing
      summary: Create a price list
      description: |
        Creates a **draft**. One created active would price customers before
        anybody had entered a price.

        `currency_code` is required and has no default: falling back to the
        organisation's default currency is the kind of helpfulness that
        produces a USD tariff in an AED kitchen with no way to tell whether
        anybody meant it. It is normalised to upper case, so a lowercase ISO
        code is not a second currency.

        A duplicate `code` is `409 resource.conflict`, not a second list.

        Requires `price_list.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePriceListRequest'
      responses:
        '201':
          description: The created draft list.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PriceListEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/price-lists/{priceList}:
    get:
      operationId: showPriceList
      tags:
        - Pricing
      summary: A price list and its channel assignments
      description: |
        Returns `ETag: "<lock_version>"` — the validator **every** write in
        this family sends back, entries and channel assignments included. A
        tariff's rows are one document even though they live in three tables,
        and two merchandisers repricing at once is the race the validator
        catches.

        Channel assignments come with it; the entries do not. A tariff runs to
        hundreds of rows and has its own cursor-paginated endpoint.

        `meta.entry_counts` says how many standing rows there are and how many
        of them are real prices — "312 priced, 8 owed" without paging.

        Requires `price_list.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/PriceListPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The price list, with its channel assignments.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PriceListDetailEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updatePriceList
      tags:
        - Pricing
      summary: Update a price list
      description: |
        `code` is **rejected**, not ignored: a client that sent it believed it
        was writing something.

        `currency_code` **is** accepted, and only while the list holds no price
        rows at all — history included. After that it is
        `409 resource.conflict` with `details.reason: currency_locked`, because
        changing it would silently re-denominate every amount in the list and
        cannot re-denominate the closed rows a past order points at. The answer
        is a new list, not a retry.

        `status` is absent (master plan v2 §4.15): activation and archiving are
        POST actions.

        `customer_scope` is editable — a tariff drafted as public that turns
        out to be one client's deal has to be reclassifiable — and every change
        lands in the audit trail naming both scopes.

        **`If-Match` required.** Requires `price_list.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/PriceListPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePriceListRequest'
      responses:
        '200':
          description: The updated list.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PriceListEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PriceListConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/price-lists/{priceList}/publish:
    post:
      operationId: publishPriceList
      tags:
        - Pricing
      summary: Activate a price list
      description: |
        Draft → active. Its own route and its own audit action, never a
        `PATCH status`.

        The gate is short, and what it does **not** check matters as much:
        placeholder and market-priced rows are welcome on an active list. A
        kitchen with forty confirmed prices and eight owed should trade on the
        forty; a gate that demanded a complete tariff would push somebody to
        invent the missing numbers. Keeping the unpriced rows away from a
        customer is the public projection's job.

        What it does check is that the list prices *something*: a tariff with
        no standing rows assigned to a live channel is a silent "nothing is for
        sale here" that looks identical to a working configuration.

        Gated by `catalogue.publish_organisation` rather than a pricing code of
        its own — activating a tariff is the same authority as deciding what is
        on sale, held by the same two roles.

        **`If-Match` required.**
      parameters:
        - $ref: '#/components/parameters/PriceListPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The activated list.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PriceListEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PriceListPublishBlocked'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/price-lists/{priceList}/archive:
    post:
      operationId: archivePriceList
      tags:
        - Pricing
      summary: Archive a price list
      description: |
        Refused while any channel still names the list —
        `409 resource.conflict` with `details.reason:
        channel_assignments_active` and the channels holding it. Detaching is
        the act that actually withdraws a tariff; archiving one a channel still
        quotes from would be a label that changed nothing.

        Nothing is deleted. Entries, history and supersession chains all
        survive, so an order placed last spring can still be explained. An
        archived list refuses every further edit.

        **`If-Match` required.** Gated by `catalogue.publish_organisation`.
      parameters:
        - $ref: '#/components/parameters/PriceListPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The archived list.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PriceListEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PriceListConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/price-lists/{priceList}/entries:
    get:
      operationId: listPriceListEntries
      tags:
        - Pricing
      summary: The prices in a list
      description: |
        **Standing rows by default** — what the tariff says today, which is
        what a PUT will be diffed against. A default that returned history
        would make the obvious client (read, edit, put back) silently
        resurrect closed prices.

        `include_history=1` adds the closed rows and flips the ordering to
        **newest-first**: a history is read backwards from the most recent
        change, not paged to the end to find last week's price.

        Amounts are integers of minor units with `currency_code` repeated on
        every row — a row that travels into a table cell, a log line or an
        order snapshot must not arrive without its currency.

        Placeholder and market-priced rows are served **in full**, with their
        status and a null amount. This is the admin surface: the person whose
        job is to price the unpriced rows has to see which ones they are.

        Requires `price_list.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/PriceListPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - name: include_history
          in: query
          required: false
          description: |
            `1`, `true` or `yes` includes closed rows and walks newest-first.
            Anything else — including `0` — means no.
          schema:
            type: string
            enum:
              - '0'
              - '1'
              - 'true'
              - 'false'
              - 'yes'
              - 'no'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of price rows.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminPriceListEntry'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        required:
                          - include_history
                          - currency_code
                          - entry_counts
                        properties:
                          include_history:
                            type: boolean
                          currency_code:
                            $ref: '#/components/schemas/CurrencyCode'
                          entry_counts:
                            $ref: '#/components/schemas/PriceListEntryCounts'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: replacePriceListEntries
      tags:
        - Pricing
      summary: Replace the prices in a list
      description: |
        The body is the desired **current** pricing state — the whole tariff as
        it should stand after the call. The server works out what changed and
        writes the history:

        | submitted | standing | what happens |
        |---|---|---|
        | yes | no | a row opens, effective from today |
        | yes | yes, same amount and status | **nothing at all** |
        | yes | yes, different | the standing row closes and its replacement opens, linked by `superseded_by_id` |
        | no | yes | the standing row closes, with no successor |

        "Nothing at all" is load-bearing: a nightly importer reposting an
        unchanged sheet must not manufacture a supersession chain hundreds deep
        and destroy the ability to answer "when did this price actually
        change".

        A pricing point is `(catalogue_item_id, catalogue_item_variant_id,
        min_quantity)`. Stating one twice in a submission is `422`.

        There is no `effective_from`, no `effective_to` and no
        `superseded_by_id` in the body, deliberately: those are the server's
        account of when it was told, and a client that could write them could
        backdate a price change to before an order was taken.

        `unit_amount_minor` and `price_status` are **one fact**. A confirmed
        row must carry a positive integer; a placeholder or market-priced row
        must carry none. Both mismatches are `422` here and a CHECK constraint
        underneath, so no importer or backfill can produce them either.

        An empty `entries` array withdraws every price — closing each standing
        row rather than deleting it.

        **`If-Match` carries the price list's validator**, not a row's.
        Requires `price_list.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/PriceListPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplacePriceListEntriesRequest'
      responses:
        '200':
          description: The list and its standing rows after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PriceListEntriesEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PriceListConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/price-lists/{priceList}/channels:
    put:
      operationId: replacePriceListChannels
      tags:
        - Pricing
      summary: Replace the channels that quote from a list
      description: |
        The complete set, applied at once. **The array order is the priority**
        — index 0 is consulted first — so listing a negotiated sheet above a
        standing tariff states the override rather than requiring somebody to
        invent non-colliding integers for it. An explicit `priority` is still
        honoured, for an importer restoring a known configuration.

        A channel may hold several lists on purpose. Resolution walks them in
        priority order and **stops at the first that prices the point**: the
        lists are not merged, so a client's agreement can override two lines
        without restating the other four hundred, and a gap in it falls through
        to the tariff behind rather than becoming a hole.

        Naming one channel twice is `422` — that is a contradiction, not a
        richer configuration.

        An empty array detaches the list from every channel, which is how a
        tariff is withdrawn and the step `archive` insists on first.

        **`If-Match` required.** Requires `price_list.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/PriceListPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplacePriceListChannelsRequest'
      responses:
        '200':
          description: The list and its channel assignments after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PriceListChannelsEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PriceListConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plan-vocabulary/combinations:
    get:
      operationId: listMealCombinationOptions
      tags:
        - Subscription plans
      summary: The meal combinations a kitchen sells plans in
      description: |
        Which meals of the day a plan delivers — "lunch and dinner", "full
        day". A kitchen's own vocabulary: there is no platform library, because
        "full board" means what each kitchen sells it as.

        Not paginated. The vocabulary is a handful of rows a client renders as
        a set of options in one pass, and paging a set of radio buttons would
        hand a form half of itself.

        **Deactivated rows are served too**, with their `is_active` flag: there
        being no delete, one that vanished from the list would be withdrawn for
        good.

        `includes_*` and `meals_per_day` are **not** the same fact.
        The flags say which sittings are covered; the count says how many
        portions leave the kitchen, which is what a per-day price is quoted
        against. They agree for the ordinary combinations and stop agreeing the
        moment a kitchen sells "lunch, twice".

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every meal combination of the organisation, active or not.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MealCombinationOption'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                        properties:
                          count:
                            type: integer
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createMealCombinationOption
      tags:
        - Subscription plans
      summary: Add a meal combination
      description: |
        A duplicate `code` within the organisation is `409`, not a silent
        update: the caller believed it was creating something. The same code in
        another kitchen is not a collision — these are one organisation's words.

        A combination that covers no sitting at all is `422`: a plan built on it
        would be a subscription to an empty box.

        `is_active` is not a field. A combination is created offered, and
        withdrawal is the PATCH.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMealCombinationOptionRequest'
      responses:
        '201':
          description: The combination as stored.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MealCombinationOptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plan-vocabulary/combinations/{combination}:
    patch:
      operationId: updateMealCombinationOption
      tags:
        - Subscription plans
      summary: Edit or withdraw a meal combination
      description: |
        **No `If-Match`.** These rows carry no `lock_version`, and the
        concurrency contract applies only to resources that do: sending a
        validator a resource cannot honour would be worse than sending none.

        `code` is **refused, not ignored** — a plan configuration's identifier
        is derived from it, so moving it would rename variants a price already
        points at.

        `is_active: false` is the withdrawal, and the only one there is. A
        vocabulary row a configuration references cannot be deleted at all: the
        foreign keys are `RESTRICT`.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/PlanCombinationPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMealCombinationOptionRequest'
      responses:
        '200':
          description: The combination after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MealCombinationOptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plan-vocabulary/energy-bands:
    get:
      operationId: listEnergyBands
      tags:
        - Subscription plans
      summary: The calorie brackets a kitchen portions plans to
      description: |
        A **band, not a target**. The source kitchens sell brackets, and a
        bracket is an honest statement about portioning; a single number would
        imply a per-customer energy calculation this programme does not do.

        Ordered by `display_order` then by `min_kcal`, so a kitchen that never
        set an order still gets its bands lowest-first rather than
        alphabetically, where "1200–1500" and "800–1100" come out backwards.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every energy band of the organisation, active or not.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/EnergyBand'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                        properties:
                          count:
                            type: integer
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createEnergyBand
      tags:
        - Subscription plans
      summary: Add an energy band
      description: |
        `max_kcal` must be strictly above `min_kcal`: a band whose ends meet is
        a single value wearing a range's clothes.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEnergyBandRequest'
      responses:
        '201':
          description: The band as stored.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnergyBandEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plan-vocabulary/energy-bands/{band}:
    patch:
      operationId: updateEnergyBand
      tags:
        - Subscription plans
      summary: Edit or withdraw an energy band
      description: |
        Precondition-free, and `code` refused rather than ignored — see the
        combinations updater for both arguments.

        Submitting one end of the range alone is legitimate; the comparison runs
        against the merged row, so raising `min_kcal` past a stored `max_kcal`
        is `422`.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/EnergyBandPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEnergyBandRequest'
      responses:
        '200':
          description: The band after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnergyBandEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plan-vocabulary/durations:
    get:
      operationId: listPlanDurations
      tags:
        - Subscription plans
      summary: The runs a kitchen sells plans for
      description: |
        **`duration_kind` carries the meaning and `duration_days` carries the
        number.** A `one_off` row is a single purchase and has
        `duration_days: null`; a `fixed_days` row has a positive integer. There
        is no zero-day row and there will not be one — that sentinel is what
        this model replaced, and a client that divides by it would find out the
        hard way.

        Ordered with the one-off **first**: it is the shortest commitment there
        is, so it belongs at the top of a list a customer reads downwards into
        longer runs.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every duration of the organisation, active or not.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PlanDurationOption'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                        properties:
                          count:
                            type: integer
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createPlanDuration
      tags:
        - Subscription plans
      summary: Add a plan duration
      description: |
        The kind decides whether the number is required or refused, and both
        mismatches are `422`: a one-off that carries a length, and a fixed run
        that does not. `duration_days: 0` is refused with a message about the
        sentinel rather than a range violation, because a client sending it is
        following the old convention and needs to be told that it is gone.

        A CHECK constraint enforces the same correspondence underneath, so no
        importer or backfill can produce either shape.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePlanDurationRequest'
      responses:
        '201':
          description: The duration as stored.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanDurationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plan-vocabulary/durations/{duration}:
    patch:
      operationId: updatePlanDuration
      tags:
        - Subscription plans
      summary: Edit or withdraw a plan duration
      description: |
        Precondition-free, and `code` refused rather than ignored.

        The kind/days correspondence is applied to the **merged** row, so
        flipping a fixed run to `one_off` without also sending
        `duration_days: null` is `422` rather than a silent clearing: the two
        columns are one fact, and a client that changed half of it has stated a
        contradiction.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/PlanDurationPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePlanDurationRequest'
      responses:
        '200':
          description: The duration after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanDurationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plans/{item}/profile:
    get:
      operationId: showPlanProfile
      tags:
        - Subscription plans
      summary: A plan's commercial terms
      description: |
        **`profile` is null when nobody has written one**, rather than a
        fabricated set of defaults. "No commercial decision has been made about
        this plan" is exactly the state the publish gate refuses, and a surface
        that filled it in would leave a kitchen wondering why publication keeps
        failing on terms it can see on screen.

        A catalogue item that is not a subscription plan is `422`, not `404`:
        the caller can see the row perfectly well through
        `/catalogue/items/{item}`, so a 404 would send them hunting for a typo.

        The `ETag` is the **item's**, because that is the validator a PUT here
        has to carry.

        The one plan read that is **not** commercial, so it requires
        `catalogue.view_organisation` rather than `plan.manage_organisation`:
        these terms are what a customer will be shown on the plan page and what
        a chef needs to produce against. The discounts are the commercial part,
        and they are on `…/variant-durations`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The plan and its terms, or a null profile.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanProfileEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: putPlanProfile
      tags:
        - Subscription plans
      summary: Write a plan's commercial terms
      description: |
        A PUT because the profile is one commercial statement, read whole to
        decide any part of it. A PATCH over eight interdependent switches would
        make "I left pausing alone" and "I turned pausing off" the same request,
        on a right a subscriber has been sold.

        Every field is optional and omitted ones take their documented
        defaults — `both`, `per_day`, skippable, pausable, 24 hours — so `{}`
        is the legitimate "the ordinary terms".

        **`change_cutoff_hours` defaults to 24 and the default is evidence**:
        the kitchen operating rule and the legacy amendment window say the same
        thing from opposite directions, so the merge keeps one column with that
        default. `0` is accepted — "change it until the van leaves" is a policy
        somebody may genuinely have.

        **`If-Match` carries the item's validator** and the write bumps it: the
        profile has no `lock_version` of its own, being the commercial face of
        the item rather than a separate thing to lock.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PutPlanProfileRequest'
      responses:
        '200':
          description: The plan and its terms after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanProfileEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plans/{item}/variants:
    get:
      operationId: listPlanVariants
      tags:
        - Subscription plans
      summary: A plan's configuration matrix
      description: |
        Each cell carries its coordinates **and** the identity of the variant
        beneath it: the `catalogue_item_variant_id` a price row points at, the
        `code` a resubmission matches on, and the `status`.

        **Archived cells are served too**, so a grid can render "we used to sell
        this" differently from "nothing here" — which is what a merchandiser
        needs to know before trying to open a cell that is already occupied.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The plan and every cell of its matrix.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanVariantsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: replacePlanVariants
      tags:
        - Subscription plans
      summary: Replace a plan's configuration matrix
      description: |
        The body is *cells*; the server writes a `catalogue_item_variants` row
        and its plan profile for each, atomically. That is what keeps the thing
        a customer picks and the thing a price points at one object.

        **The code is derived and deterministic** —
        `combination-tier[-band]`, from the vocabulary rows' own codes — so the
        same cell submitted from two clients on two days produces the same
        variant and the same price keeps pointing at it. An explicit `code` is
        accepted for a kitchen migrating identifiers it already uses elsewhere.

        **Absent cells are archived, not deleted**, and their coordinates stay
        occupied: reviving a withdrawn cell means resubmitting *that* cell, not
        opening a second variant at the same address. Trying the second is
        `409` with the occupying code named.

        Stating one cell twice in a submission is `422` — and two cells that
        differ only by an omitted `energy_band_id` are the same cell, not two.

        `includes_snacks` and `snacks_per_day` are one fact stated twice, so
        the contradiction is `422` rather than silently resolved.

        A deactivated combination or band may keep a cell a plan already sells
        and may not open a new one.

        An empty `cells` array archives the whole matrix — a decision, not a
        forgotten field.

        **`If-Match` carries the item's validator.** Requires
        `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplacePlanVariantsRequest'
      responses:
        '200':
          description: |
            The plan and **every** cell it now has, including the ones this call
            archived — so a client sees what the submission did rather than what
            it sent.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanVariantsEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PlanCellConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plans/{item}/menu:
    get:
      operationId: getPlanMenu
      tags:
        - Subscription plans
      summary: A plan's fixed menu
      description: |
        What the plan serves, on which day of its cycle — ordered the way a
        chef reads it: day, then sitting in the order of the day rather than
        alphabetically, then the second lunch after the first.

        Each entry carries the dish's name beside its identifier, so an editor
        drawing a week of slots does not have to resolve forty identifiers to
        do it.

        `cycle` is served even when it is empty: `cycle_days: null` is the
        whole "this plan has no menu" fact, and it is the same answer whether
        the plan has no profile at all or a profile with no menu on it.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The plan, its cycle configuration and every entry on its menu.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanMenuEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: replacePlanMenu
      tags:
        - Subscription plans
      summary: Replace a plan's fixed menu
      description: |
        ## Publishing a menu changes what a subscription order does to stock

        **Today a confirmed fixed-menu subscription order deducts nothing.** No
        meal lines are generated for a plan whose subscribers have made no
        choices, and the plan line itself consumes nothing by design — the
        kitchen ships food while its recorded shelves never move.

        **The moment a menu exists for a plan, that stops, for that plan
        alone.** Generation fills each day's slots from the menu, those become
        real order lines, and confirming an order explodes each one through its
        published recipe and deducts the ingredients. What comes with it:

        * every meal on the menu with no published recipe version starts
          raising `no_recipe_version` consumption exceptions, one per confirmed
          order per day;
        * every meal whose recipe version has no yield piece count starts
          raising `no_yield_piece_count` the same way;
        * both are blocking reasons, so they stay open until a person settles
          them.

        There is no flag and no kitchen-wide switch: **publishing a menu is the
        opt-in, and it is per plan.** Recipe one plan, publish its menu, watch
        the exceptions, then do the next one.

        ## The menu is one document

        `entries`, `menu_cycle_days` and `menu_cycle_anchor_date` are submitted
        and stored together. Half of it is `422`: dishes with no cycle have no
        modulus to resolve a date against, and a cycle with no dishes would
        announce a menu whose every day is empty. Sending **no** entries and
        **no** cycle is the one legal half-state, because it is not one — it
        withdraws the menu and returns the plan to today's behaviour.

        The two cycle fields are columns on the plan profile and are **not**
        writable through `PUT …/profile`: that endpoint is a whole-document
        write, and a client sending a body written before those columns existed
        would silently withdraw a kitchen's menu.

        ## The rest

        **Entries are replaced, not merged**, and absent ones are deleted
        rather than archived — nothing references a menu entry, no price is
        quoted against one, and the dish is protected by its own delete
        restriction. Two days may therefore swap their dishes in one
        submission.

        A dish must be this kitchen's, must be a **meal**, and must be
        **published** — a menu entry is a promise to serve it. A `cycle_day`
        beyond the submitted cycle length is `422`, and so is one slot filled
        twice.

        **`If-Match` carries the item's validator.** Requires
        `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplacePlanMenuRequest'
      responses:
        '200':
          description: The plan, the cycle it now runs on, and every entry it now has.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanMenuEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/plans/{item}/variant-durations:
    get:
      operationId: listPlanVariantDurations
      tags:
        - Subscription plans
      summary: The runs each configuration may be bought for
      description: |
        **`discount_percent` is served as `null` where nobody has stated one** —
        never as `"0.00"` and never omitted. The absence has to be visible,
        because the person reading this screen is the person who has to go and
        find the number. `meta.unstated_discount_count` says how many rows are
        in that state.

        Requires `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The plan and every duration assignment across its configurations.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanVariantDurationsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: replacePlanVariantDurations
      tags:
        - Subscription plans
      summary: Replace a plan's duration assignments
      description: |
        The complete set of configuration × duration pairings, applied at once.
        A kitchen decides "every cell runs 5, 20 and 40 days, and the premium
        tier also runs 60" in one sitting, and splitting that across calls would
        make the half-applied states reachable and publishable.

        Rows name their configuration and duration by identifier **or by
        code**, so an importer holding codes does not have to round-trip through
        this API to learn identifiers it will never use again.

        **An absent `discount_percent` stays absent all the way to the
        column.** NULL is "nobody has stated one", `0` is "there is no
        discount", and this endpoint never turns the first into the second.

        **Absent rows are deleted**, and the asymmetry with the matrix is
        deliberate: an assignment carries nothing but itself, so there is
        nothing to strand. `is_available: false` is what says "we still run 40
        days, just not this month" without discarding the negotiated discount.

        A deactivated duration cannot be assigned.

        **`If-Match` carries the item's validator.** Requires
        `plan.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/CatalogueItemPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplacePlanVariantDurationsRequest'
      responses:
        '200':
          description: The plan and its duration assignments after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanVariantDurationsEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/delivery-zones:
    get:
      operationId: listDeliveryZones
      tags:
        - Delivery
      summary: The delivery zones this organisation has drawn
      description: |
        Cursor-paginated. A kitchen with one map has three zones and a kitchen
        covering a country has a zone per district per branch, and the second
        is the one that has to keep working.

        Archived zones are excluded unless `status=archived` asks for them.

        `branch_id=organisation` selects the organisation-wide map — the one
        thing a plain identifier filter cannot express, because an absent
        parameter already means "no filter".

        The areas are **not** embedded: a zone can name a hundred places, and
        `…/{zone}/areas` answers that question one zone at a time.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/PageNumber'
        - $ref: '#/components/parameters/PerPage'
        - $ref: '#/components/parameters/CursorLimit'
        - name: status
          in: query
          required: false
          description: Defaults to everything except `archived`.
          schema:
            $ref: '#/components/schemas/DeliveryZoneStatus'
        - name: branch_id
          in: query
          required: false
          description: |
            A branch identifier, or the literal `organisation` for the
            organisation-wide map.
          schema:
            oneOf:
              - $ref: '#/components/schemas/Uuid'
              - type: string
                const: organisation
        - name: query
          in: query
          required: false
          description: Case-insensitive substring over `name_en`, `name_ar` and `code`.
          schema:
            type: string
            maxLength: 120
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of delivery zones.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZonesEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createDeliveryZone
      tags:
        - Delivery
      summary: Draw a delivery zone
      description: |
        The zone is created `active` and covering nowhere, so there is no
        window in which a half-built map quotes a delivery. Areas are added
        through `PUT …/{zone}/areas`.

        `branch_id` is optional and usually absent: a zone with no branch is
        the organisation-wide map, and a branch-scoped zone overrides it for
        that branch.

        `currency_code` is optional and defaults to the organisation's own —
        unlike a price list, where a default would silently produce a USD
        tariff in an AED kitchen. A zone carries at most two amounts, in the
        kitchen's home market.

        A duplicate `code` inside the organisation is `409`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDeliveryZoneRequest'
      responses:
        '201':
          description: The zone as created.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/delivery-zones/{zone}:
    get:
      operationId: getDeliveryZone
      tags:
        - Delivery
      summary: One delivery zone
      description: |
        Returns `ETag: "<lock_version>"`. The **area set is versioned against
        this same number**: a zone and its map are one document, and two
        operators redrawing at once is the race the validator catches.

        `meta.area_count` comes with it, so a client can render "17 areas"
        without fetching them.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/DeliveryZonePath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The delivery zone.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneDetailEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updateDeliveryZone
      tags:
        - Delivery
      summary: Rename, reprice or suspend a delivery zone
      description: |
        `code` and `status` are **rejected**, not ignored: a client that sent
        one believed it was writing something. Archiving is a POST action;
        suspension is `is_active`.

        Changing `branch_id` re-scopes the zone **and its area claims
        together**, in one transaction. It is refused with `409
        area_already_served` when another zone already serves one of those
        areas at the destination, and nothing is half-moved.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/DeliveryZonePath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDeliveryZoneRequest'
      responses:
        '200':
          description: The zone after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DeliveryAreaConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/delivery-zones/{zone}/archive:
    post:
      operationId: archiveDeliveryZone
      tags:
        - Delivery
      summary: Withdraw a delivery zone
      description: |
        Terminal, and it **releases the zone's area claims**. An archived zone
        that kept them would occupy every place it ever served, and a kitchen
        redrawing its map would find the new zone refused by one nobody uses.

        A kitchen that wants to stop delivering *without* giving the areas up
        suspends the zone instead (`is_active: false` on the PATCH), which is
        reversible and keeps the map intact.

        Nothing is deleted: the fee, the promise and the name survive, so a
        delivery quoted from this zone last month can still be explained.

        There is deliberately no customer-address check. `customer_addresses`
        arrives in J1, so today there is nothing to consult and a blocker
        written against an empty table would be a permanently-green check.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/DeliveryZonePath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The archived zone.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/delivery-zones/{zone}/areas:
    get:
      operationId: listDeliveryZoneAreas
      tags:
        - Delivery
      summary: The places a zone covers
      description: |
        Unpaginated: the set is bounded by the gazetteer and is the unit a
        client edits, so a picker showing half a map with a cursor to fetch the
        rest would be a worse contract than one response.

        Each entry is the **platform area** in admin shape, `is_active`
        included — a kitchen must be able to see that a place it still serves
        has been withdrawn from the gazetteer. Those claims are kept rather
        than silently dropped, and `meta.inactive_area_count` is what a screen
        puts a warning badge on.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/DeliveryZonePath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The areas this zone covers.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneAreasEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: replaceDeliveryZoneAreas
      tags:
        - Delivery
      summary: Redraw the map of a zone
      description: |
        Set-replace: the body is the desired whole map, and areas absent from
        it are released. That release is the only way to hand a place to a
        different zone, because **one area is served by one zone per branch**.

        `If-Match` carries the **zone's** validator — a zone and its map are
        one document.

        Three refusals are worth knowing. A place already served by another
        zone **at the same scope** is `409 area_already_served`, naming the
        occupying zone rather than an index. An area in another country is
        `422 area_country_mismatch`: the gazetteer is keyed `(country, code)`
        precisely because place names repeat. An area the platform has
        withdrawn is `422 area_inactive` — unless this zone already claimed it,
        in which case the claim stands and `meta.inactive_area_count` says so.

        An organisation-wide zone and a branch-scoped zone **may** both claim
        one area. That is the override mechanism, not a hole: the branch claim
        wins when a branch is in context, and the organisation-wide one answers
        when none is.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/DeliveryZonePath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceDeliveryZoneAreasRequest'
      responses:
        '200':
          description: The areas this zone covers after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneAreasEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/DeliveryAreaConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/delivery-windows:
    get:
      operationId: listDeliveryWindows
      tags:
        - Delivery
      summary: The delivery slots this kitchen offers
      description: |
        Unpaginated, deliberately: a kitchen has morning, afternoon and
        evening, and perhaps a weekend slot. This is the set a checkout picker
        renders whole, so paging it would push every client into reassembling
        it.

        Deactivated windows are served **with** their flag rather than hidden —
        there is no delete, so the only way to see a withdrawn slot is to be
        shown it.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every delivery window, active and withdrawn.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryWindowsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createDeliveryWindow
      tags:
        - Delivery
      summary: Name a delivery slot
      description: |
        No `If-Match`: these rows carry no `lock_version`, and the concurrency
        contract applies only to resources that do.

        A duplicate `code` inside the organisation is `409`; the same code in
        another kitchen is not a collision, because a window is one kitchen's
        word for one of its own slots.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDeliveryWindowRequest'
      responses:
        '201':
          description: The window as created.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryWindowEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/delivery-windows/{window}:
    patch:
      operationId: updateDeliveryWindow
      tags:
        - Delivery
      summary: Edit or withdraw a delivery slot
      description: |
        **Precondition-free**: a window carries no `lock_version`, and
        `If-Match` applies only to resources that do.

        `code` is **rejected**, not ignored. `is_active: false` is the only
        withdrawal there is; there is no DELETE, because an order taken for the
        evening slot has to stay explainable.

        The pairing and ordering rules run against the **merged** row: sending
        only `ends_at` compares it with the stored `starts_at`. Turning a timed
        window back into an untimed one therefore means sending both as `null`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/DeliveryWindowPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDeliveryWindowRequest'
      responses:
        '200':
          description: The window after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryWindowEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /kitchen/branch-operating:
    get:
      operationId: getBranchOperating
      tags:
        - Kitchen operations
      summary: The current branch's operating week
      description: |
        The branch comes from `X-Branch-Id`. `branch.context` permits an absent
        header — an organisation-wide membership may legitimately select no
        branch — so this endpoint refuses that case with
        `400 context.branch_required` rather than guessing which location the
        caller meant.

        **Unconfigured days are absent, not filled in.** A week with three rows
        means four days nobody has decided about; inventing `is_open: false`
        for them would tell a customer the branch is shut on Thursday when the
        truth is that nobody has said. `meta.is_complete` is what a screen puts
        the "finish setting this up" prompt behind.

        No `ETag`: these rows carry no `lock_version`, because the week is
        replaced whole in one transaction.

        Requires `branch.view_current`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XBranchIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The branch's week, in weekday order.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BranchOperatingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: replaceBranchOperating
      tags:
        - Kitchen operations
      summary: Replace the current branch's operating week
      description: |
        Set-replace, in one transaction. The week is the unit of change — "we
        now close at six, and Sundays are off" is one decision, and applying
        half of it produces a schedule nobody agreed to. That is also why there
        is no `If-Match`: there is no half-week for a validator to protect.

        A **closed day is a row** with no times; an omitted weekday is a day
        nobody has decided about. Both are storable, and only that distinction
        lets a checkout say "we are shut on Sundays" rather than "we cannot
        answer". An empty `days` clears the week back to unconfigured.

        Three per-row rules, each refused with the day and the field named: a
        day is open with both times or closed with neither; a branch closes
        after it opens (no overnight service yet); and a closed day carries no
        cut-off. A cut-off **outside** the opening hours is accepted — "order
        by 22:00 the night before" is a real rule, and which day a cut-off
        governs is order capture's question rather than this table's.

        There is deliberately no `branch_id` in the body: the branch comes from
        `X-Branch-Id`, which is validated against the caller's membership
        scope.

        Requires `branch.manage_current`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XBranchIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceBranchOperatingRequest'
      responses:
        '200':
          description: The branch's week after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BranchOperatingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /reference/delivery-areas:
    get:
      operationId: listDeliveryAreas
      tags:
        - Reference data
      summary: The platform delivery gazetteer
      security: []
      description: |
        **Anonymous.** J1's onboarding asks a customer for their delivery area
        before an account exists, so an address form that only works after
        sign-in cannot be part of sign-up.

        Served through the public projection: one server-localised `name`
        chosen from `Accept-Language`, never both language columns. Inactive
        areas are excluded.

        **Cursor-paginated**, unlike the other two public vocabularies.
        Fourteen allergen classes and twelve diet classifications are
        constants; the gazetteer is 125 rows for one country today and grows
        with every market.

        `country_code` is **optional** — revised from K1.7's "required" when the
        consumer marketplace needed the whole gazetteer before it knew where
        the person was. The original argument was right about *defaults* and
        wrong about *absence*: a client that has not asked for a country has
        not asked for the wrong one, and `country_code` is embedded on every
        row, so an unfiltered page is unambiguous by construction. A code the
        platform does not recognise is still `400` rather than an empty page,
        which would read as "we do not deliver there yet".

        `region` is carried and is currently `null` on every row (OD-12): the
        source does not record which governorate a place belongs to, and a
        client that wants to group the picker can see that the platform has
        nothing to group by.
      parameters:
        - name: country_code
          in: query
          required: false
          description: |
            ISO 3166-1 alpha-2, case-insensitive. Omit for every active area
            in every market the platform serves.
          schema:
            type: string
            minLength: 2
            maxLength: 2
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of delivery areas, localised.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicDeliveryAreasEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /marketplace/kitchens:
    get:
      operationId: listMarketplaceKitchens
      tags:
        - Marketplace
      summary: List kitchens
      security: []
      description: |
        **Anonymous.** Every active kitchen with at least one open branch,
        cursor-paginated. A marketplace a person has to sign in to browse is not
        a marketplace.

        A kitchen appears when three things are true: its organisation type is
        `kitchen`, its own status is `active`, and it has at least one active
        branch. A suspended kitchen is **absent** rather than greyed out, and a
        kitchen with no open location has no calendar to offer.

        Served through a public projection (master plan v2 4.8). Several fields
        the consumer contract asks for have no column behind them and carry an
        honest empty value rather than an invented one: `tagline` and
        `description` are empty strings (the platform stores no marketing copy),
        `cuisines` is empty, `rating` is `null` with `rating_count: 0` (no
        reviews exist), and `is_verified` is `false` (there is no verification
        module yet, so the platform has verified nobody). `diet_classifications`
        **is** real: it is derived from the kitchen's own published meals.

        `name` is the organisation's single registered name and is *not*
        localised - a business name is not translated. Delivery-zone and area
        names are, and carry one server-chosen language, never both columns.

        `meta.unsupported_filters` names any parameter the caller sent that this
        deployment stores no data for, so a silently ignored filter is
        impossible.
      parameters:
        - name: query
          in: query
          required: false
          description: Free text over the kitchen name.
          schema:
            type: string
            maxLength: 120
        - name: country_code
          in: query
          required: false
          schema:
            type: string
            minLength: 2
            maxLength: 2
        - name: area
          in: query
          required: false
          description: |
            A platform delivery-area `code` from `GET /reference/delivery-areas`.
            Matches kitchens whose **active** zones claim that area, so a kitchen
            that has paused a zone stops appearing for it while keeping the claim.
          schema:
            type: string
            maxLength: 60
        - name: channels
          in: query
          required: false
          description: |
            Comma-separated consumer switches. `subscription`, `delivery` and
            `pickup` have no stored channel kind behind them and therefore match
            no kitchen - see `MarketplaceSalesChannels`.
          schema:
            type: string
            examples:
              - b2c,marketplace
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of kitchens.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MarketplaceKitchensEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /marketplace/kitchens/{kitchen}:
    get:
      operationId: getMarketplaceKitchen
      tags:
        - Marketplace
      summary: Read one kitchen
      security: []
      description: |
        **Anonymous.** One kitchen, with its branches, their active delivery
        zones and their operating weeks.

        `{kitchen}` accepts the identifier or the slug. A kitchen that is
        suspended, has no active branch, or is not a kitchen answers
        `404 resource.not_found` - the same answer an identifier that never
        existed gets, because telling an anonymous caller which organisations
        exist and what the platform has done to them is nobody's business but the
        tenant's.
      parameters:
        - $ref: '#/components/parameters/MarketplaceKitchenPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The kitchen.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/MarketplaceKitchen'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /marketplace/meals:
    get:
      operationId: listMarketplaceMeals
      tags:
        - Marketplace
      summary: List meals and products
      security: []
      description: |
        **Anonymous.** Published meals **and products**, offered through a consumer
        listing channel, carrying a confirmed price. Filter with `item_types`
        (`meal`, `product`, or both comma-separated); omit it to receive both.

        **An unpriced listing is absent.** A placeholder row and a market-priced row
        resolve to no price at all, and the consumer contract has no shape for a
        listing without one: rendering `0` would be a lie, and a nullable price
        would push "we do not know what this costs" onto a surface with no way to
        act on it. The kitchen sees the difference on its own readiness screen.

        **Prices come only from consumer listing channels.** The resolver is
        restricted to channel kinds the domain declares non-private for public
        listing (`b2c_web`, `marketplace`), so a negotiated wholesale, corporate
        or insurance tariff - and any volume tier on one - is structurally
        unreachable here rather than merely unrequested.

        `exclude_allergens` is an exclusion rather than an inclusion on purpose:
        a person filters by what they must avoid. It is applied over the derived
        allergen list at **both** containment levels, so a dish a kitchen cannot
        rule an allergen out of is excluded along with the ones that certainly
        contain it.

        `nutrition` and `serving` are nullable: a kitchen that has recorded
        source-labelled facts returns them, and a listing without a recorded
        source returns null. The demonstration menu uses `synthetic_prototype`
        facts that the client visibly labels as estimates; they are not a kitchen
        declaration or laboratory analysis.

        `meal_types`, `cuisines`, `preparation_minutes` and the ratings have no
        column behind them and carry honest empties.
      parameters:
        - name: query
          in: query
          required: false
          description: Free text over both name and both description columns.
          schema:
            type: string
            maxLength: 120
        - name: kitchen_ids
          in: query
          required: false
          description: Comma-separated kitchen identifiers.
          schema:
            type: string
        - name: category_slug
          in: query
          required: false
          description: |
            One published-category code from the platform product taxonomy
            (`frozen`, `beverage`, `sauce`, `dressing`, `meal`, `bread`, …).
            An unknown code matches nothing — the codes are navigation, and a
            stale chip should render an empty shelf, not a `400`.
          schema:
            type: string
            examples:
              - frozen
        - name: item_types
          in: query
          required: false
          description: |
            Comma-separated catalogue item types to include. Allowed values:
            `meal`, `product`, `sauce`, `dressing`. Omit to receive all.
            Unknown values are `400`.
          schema:
            type: string
            examples:
              - meal,product
              - sauce,dressing
        - name: diet_classifications
          in: query
          required: false
          description: |
            Comma-separated classification codes. Meals must carry **every** one:
            a person ticking vegan and gluten-free is stating two requirements.
          schema:
            type: string
        - name: exclude_allergens
          in: query
          required: false
          description: Comma-separated allergen codes. Meals carrying any of them are excluded.
          schema:
            type: string
            examples:
              - peanut,gluten
        - name: price_max
          in: query
          required: false
          description: Maximum price in minor units of the meal's own currency.
          schema:
            type: integer
            minimum: 0
        - name: available_on
          in: query
          required: false
          description: |
            Only meals orderable on this date. A date outside the published
            fortnight matches nothing: the kitchen has not said.
          schema:
            type: string
            format: date
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of meals.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MarketplaceMealsEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /marketplace/meals/{meal}:
    get:
      operationId: getMarketplaceMeal
      tags:
        - Marketplace
      summary: Read one meal
      security: []
      description: |
        **Anonymous.** One meal, with its derived allergen declaration and the
        fortnight of dates it can be ordered for.

        `{meal}` accepts the identifier or the slug. An unpriced, unpublished,
        retired or quarantined meal answers `404 resource.not_found`: a meal
        nobody has priced is not on sale, and a detail page for one would have to
        invent a price or show a gap where the buy button goes.
      parameters:
        - $ref: '#/components/parameters/MarketplaceMealPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The meal.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/MarketplaceMeal'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /marketplace/meal-plans:
    get:
      operationId: listMarketplaceMealPlans
      tags:
        - Marketplace
      summary: List subscription plans
      security: []
      description: |
        **Anonymous.** Published subscription plans of active kitchens.

        **An empty page is the expected answer today, and it is the correct
        one.** The imported real-kitchen world is entirely draft by design, and
        the demonstration plan is deliberately left unpublishable - one
        configuration carries no confirmed price, which is exactly what the plan
        publish gate refuses. Consumer plan pages therefore remain mock-backed
        after M1; this endpoint exists so that a kitchen finishing a plan makes
        it appear without another phase.

        Unlike a meal, a plan is not additionally gated on a channel-availability
        row: its own publish gate is the stronger statement, and a weaker second
        gate the publish path never asks for would hide a plan its kitchen had
        fully prepared.
      parameters:
        - name: query
          in: query
          required: false
          schema:
            type: string
            maxLength: 120
        - name: kitchen_ids
          in: query
          required: false
          description: Comma-separated kitchen identifiers.
          schema:
            type: string
        - name: duration
          in: query
          required: false
          description: A kitchen's own duration `code`, as published in `durations`.
          schema:
            type: string
            maxLength: 60
        - name: meals_per_day
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of subscription plans.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MarketplacePlansEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /marketplace/meal-plans/{plan}:
    get:
      operationId: getMarketplaceMealPlan
      tags:
        - Marketplace
      summary: Read one subscription plan
      security: []
      description: |
        **Anonymous.** One published plan: its terms, the configurations a
        customer can buy and the runs those configurations are offered for.

        `{plan}` accepts the identifier or the slug. A configuration with no
        confirmed weekly price is **absent from `variants`** rather than listed
        without one.
      parameters:
        - $ref: '#/components/parameters/MarketplacePlanPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The subscription plan.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/MarketplacePlan'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /carts:
    post:
      operationId: openCart
      tags:
        - Cart
      summary: Open or re-find the caller's basket on a channel
      description: |
        Get-or-create. **200, not 201**, and the verb is still POST: the second
        call returns the basket the first one opened, and a client that saw
        `201` twice would reasonably conclude it had two baskets. It is not a
        `GET` either, because the first call genuinely writes a row and a
        `cart.opened` audit event — and a `GET` that creates rows is one a proxy
        will cache and a crawler will trigger.

        **One open cart per customer per channel**, held by a partial unique
        index rather than by a check-then-insert: two taps on a slow connection
        are a race, and a race that produces two baskets loses one of them
        silently.

        `channel_code` is a code rather than an identifier, and it is resolved
        **across organisations**. A shopper knows they are buying through the
        consumer web channel; they do not hold a channel UUID, and a client that
        had to fetch one first would need a kitchen-admin read permission to do
        it. Which kitchen owns the channel is therefore the server's answer, not
        the client's assertion — there is no `organisation_id` in the body, and
        a code naming no *active* channel is `404`, never a 422, because
        confirming that a code exists but is switched off leaks a kitchen's
        commercial state to anybody who can guess a string.

        **No amount of any kind is returned, and none ever will be from this
        family.** `carts` and `cart_items` hold no price column: the server's
        probe establishes that a line is *orderable* and throws its price away,
        because a cart price is advisory — the tariff can move between adding a
        line and paying for it — and every line is repriced at placement. A
        subtotal here would be a number no table holds and no checkout will
        honour.

        `ETag` is served because the cart carries `lock_version`, but **no
        `If-Match` is demanded on any cart write**: the header prevents a lost
        update, and a basket has exactly one author.

        Authenticated and email-verified. No permission code — the authority is
        ownership, and every row is scoped to the caller's own customer account.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OpenCartRequest'
      responses:
        '200':
          description: The caller's open basket on that channel, with its lines.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CartEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /carts/{cart}/items:
    post:
      operationId: addCartItem
      tags:
        - Cart
      summary: Add a line to a basket
      description: |
        **The server validates on the way in.** A full price probe runs before
        anything is written: the article is published, the channel offers it on
        the requested day, there is a confirmed price, and that price is in the
        basket's currency. Accepting anything and discovering the problem at
        checkout is the shape of the prototype this programme replaces — a
        customer builds a basket for ten minutes and is told at the end that
        half of it was never orderable.

        **Lines merge rather than repeat.** Adding two of something the basket
        already holds one of produces one line of three. "Two of these" and "one
        of these twice" are the same basket, and a customer scrolling past three
        copies of one line has been shown the database's internal state instead
        of their order. The probe runs at the **combined** quantity, not the
        increment: a tier that begins at fifty is reached by the fiftieth unit
        however it was added.

        The response is still `201` on a merge. The status describes the
        request's outcome — here is the line you asked about — not the row count.

        A line's identity is `(article, pack, delivery date)`. Two quantities of
        the same meal for two different days are two lines.

        Refusals are **`422 cart.line_refused`** carrying `details.reasons`: one
        `{reason, …context}` entry per problem, **all of them at once**, from the
        vocabulary `item_unknown`, `item_not_published`, `variant_unknown`,
        `variant_not_active`, `channel_not_trading`, `channel_unavailable`,
        `unpriced`, `currency_mismatch`. A refusal that revealed one problem per
        attempt would turn one fix into three round trips.

        The whole cart comes back beside the line, because the write pushed the
        basket's expiry out and moved its `lock_version` — a response carrying
        only the line would hand a client two stale facts.
      parameters:
        - $ref: '#/components/parameters/CartPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCartItemRequest'
      responses:
        '201':
          description: The line, and the basket it now belongs to.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CartLineEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /carts/{cart}/items/{item}:
    patch:
      operationId: setCartItemQuantity
      tags:
        - Cart
      summary: Set a basket line to an exact quantity
      description: |
        **Set, not adjust.** The body carries the quantity the customer wants,
        never a delta: a delta applied to a basket that moved underneath the
        client produces a number nobody asked for, and the one client that would
        send a delta is the one whose render is stale.

        **The new quantity is reprobed**, and can be refused. Raising a line can
        cross a tier the kitchen prices in another currency; lowering it can fall
        below the only tier that priced the article at all. Neither is visible
        from the number alone, which is why this is not a write the server may
        simply accept. Refusals are `422 cart.line_refused`, the same structured
        shape as the add.

        `quantity` is the only field. The article, the pack and the delivery date
        are what make this line *this* line, so changing any of them is removing
        a line and adding another — a PATCH that accepted `catalogue_item_id`
        would let a client mutate a row into a different line and collide with
        the index that keeps baskets free of duplicates.

        Zero is refused. Removing a line is its own action, not a quantity of
        nothing, so a client that means "delete" is told so rather than silently
        having written a row it must then notice is gone.
      parameters:
        - $ref: '#/components/parameters/CartPath'
        - $ref: '#/components/parameters/CartItemPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetCartItemQuantityRequest'
      responses:
        '200':
          description: The line at its new quantity, and the basket around it.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CartLineEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    delete:
      operationId: removeCartItem
      tags:
        - Cart
      summary: Remove a line from a basket
      description: |
        **A real delete**, and one of very few on the platform. Almost
        everything else archives, retires or deactivates, because a price a
        customer was charged and a label a diner was shown have to stay
        reconstructable. A basket line is neither: nothing has been promised and
        nothing has been charged, and a line somebody changed their mind about is
        not evidence of anything. The removal is audited as `cart.line_removed`,
        so the decision is still traceable without the row.

        Its own action rather than a quantity of zero: "none of this" and "remove
        this" are different intentions, and collapsing them lets a client delete
        by arithmetic accident.

        **No `ETag` on the 204.** The cart's validator did move, and a client
        that holds one must re-read the basket rather than trust a header on an
        empty body — serving a validator for a representation the response does
        not contain is how a client ends up confident about a basket it has not
        seen.

        A line that is not in this basket, and a basket that is not the caller's,
        are both `404`.
      parameters:
        - $ref: '#/components/parameters/CartPath'
        - $ref: '#/components/parameters/CartItemPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '204':
          description: The line was removed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /checkouts/preview:
    post:
      operationId: previewCheckout
      tags:
        - Orders
      summary: What a basket would cost, priced for real
      description: |
        **A query, not a command.** Nothing is reserved, nothing is written and
        no `Idempotency-Key` is accepted — the same proposal always has the
        same answer, so there is nothing here for a replay to protect against.
        `POST` rather than `GET` because a cart identifier is not something to
        put in a query string a proxy will log.

        **Every amount is resolved exactly as `POST /orders` resolves it.**
        This runs the identical `LineProbe` and `ZoneResolver` paths
        `OrderPlacementService` runs at placement, so a preview and the
        placement it precedes never quote two different numbers for a basket
        nothing has changed about.

        **Nothing here is a refusal.** Placement collects a list of reasons
        and refuses the whole order with `409 order.placement_refused`; a
        preview has no transaction to abort, so the same facts — `cart_empty`,
        `address_missing`, `address_not_deliverable`, `area_not_served`,
        `zone_suspended`, `currency_mismatch`, plus the line-probe vocabulary —
        come back as `warnings` inside an ordinary `200`. Deliberately the
        *same words* placement would refuse with: a customer taught one
        vocabulary for "we don't deliver there" on the cart screen and a
        different one at checkout has been taught nothing at all.

        **`customer_address_id` is optional, unlike at placement.** A shopper
        previews a total before they have chosen where it goes — the cart
        screen has no address at all — and an absent address is reported as
        the `address_missing` warning rather than refused.

        **`delivery_fee_minor` is `null` whenever the fee is unresolved**,
        never `0`: no address named, an unserved area, a suspended zone or a
        zone with no fee configured are four different facts, and none of
        them is "delivery is free" (OQ-045).

        A cart or an address that is not the caller's own is **`404
        resource.not_found`**, exactly as `OrderLocator` answers it for
        placement: confirming that an identifier exists but belongs to
        somebody else is a disclosure either way.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PreviewCheckoutRequest'
      responses:
        '200':
          description: |
            What the basket would cost right now, and what stood in the way of
            pricing any part of it completely.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutPreviewEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /orders:
    post:
      operationId: placeOrder
      tags:
        - Orders
      summary: Place a basket as an order
      description: |
        The one irreversible act in the customer journey, and the place where
        every question the platform has been deferring is answered at once.

        **Every line is repriced here.** The cart's probe prices were advisory
        and were never stored; these are authoritative and become the snapshot.
        A tariff can change between filling a basket on Tuesday and paying for it
        on Friday, and the price that governs is the one standing when the
        customer commits. The consequence is that a line acceptable in the basket
        can be refused at checkout, and that is correct — the article was
        retired, the channel stopped offering it, the last confirmed price
        expired.

        **One currency, never converted.** The order takes the cart's currency,
        and any line — or delivery fee — priced in another refuses the whole
        placement. Converting would mean inventing an exchange rate, storing a
        number nobody quoted and producing a total that reconciles against
        nothing.

        **A replay repeats the original response exactly, status included.**
        `201` for a placement and `201` again on a replay, with
        `Idempotency-Replayed: true` to say which it was. A client that retried
        a request whose response it lost wants exactly the answer it missed —
        and the resource genuinely was created, so downgrading the status to
        `200` would be the reply that lies. The header is the signal; the status
        is the record.

        **`Idempotency-Key` is optional and is guarded twice.** The HTTP
        middleware answers a replay with the stored response envelope, which a
        service cannot do — by then the response is gone. The placement service
        claims the key *before* the order row exists, which is what makes a
        genuine race an insert conflict rather than a check-then-act two requests
        can both pass, and which also protects a placement made by a queued job.
        A key replayed against a **different** body is
        `409 request.idempotency_key_reused`; the fix is a new key, not a reload.

        Refusals are **`409 order.placement_refused`** carrying
        `details.reasons` — every blocker at once, from the vocabulary
        `account_not_ready`, `account_not_active`, `guest_not_verified`,
        `cart_empty`, `cart_not_open`, `address_not_owned`,
        `address_not_deliverable`, `area_not_served`, `zone_suspended`,
        `currency_mismatch`, `mixed_delivery_dates`, `cut_off_passed`,
        `branch_closed`, `date_in_the_past`, plus the whole line vocabulary
        folded in per line with the offending `cart_item_id`. A checkout that
        revealed one problem per attempt — verify your email, now add an address,
        now that article is withdrawn, now the cut-off has passed — is four round
        trips and a lost customer.

        A 409 rather than a 422 throughout: the request is well-formed and the
        customer meant it, and what moved is the world it describes.

        **`mixed_delivery_dates` is a refusal, not a flattening.** This phase's
        order carries exactly one delivery date; a basket asking for two days is
        a subscription-shaped order, and flattening it would deliver Wednesday's
        food on Monday.

        No `payment_method` in the body: there is one, it happens at the door,
        and a field with one legal value promises a choice the platform cannot
        honour. No total either — a body that could state one is a client quoting
        the server its own prices.

        The order comes back in the **customer** shape, which carries no
        `price_list_id`, `price_list_item_id`, `organisation_id`, `created_by`
        or `lock_version`.
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaceOrderRequest'
      responses:
        '201':
          description: |
            The order, with every line at the price it was placed at. A replay
            of a request this key already answered returns this same body and
            this same status, with `Idempotency-Replayed: true`; the command did
            not run a second time.
          headers:
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/orders:
    get:
      operationId: listMyOrders
      tags:
        - Orders
      summary: The caller's own order history
      description: |
        **Newest first, and the direction is a property of the endpoint rather
        than a parameter.** Order history is read from the top: the order
        somebody has a question about is almost always the last one. A collection
        walkable both ways from one cursor would need the direction inside the
        cursor to stay coherent, so no client may ask for the other.

        **No filters, deliberately.** A person has tens of orders, not thousands,
        and the whole history fits in a page or two; a parameter to document,
        version and test in exchange for saving a client one array filter is a
        bad trade. `GET /catalogue/orders` reads a book that grows for as long as
        the kitchen trades, which is why it has four — the asymmetry is the point
        rather than an inconsistency.

        **The lines come with each order.** A history showing only totals would
        send a client back once per row to render the one thing a person actually
        recognises: the names of the food.

        Served in the customer shape — no price-list provenance, no
        `organisation_id`, no `created_by`, no `lock_version`. That the shape
        carries no kitchen identity at all is a stated gap: naming the kitchen
        means a public projection of an organisation, which is the marketplace
        family's, not this one's.
      parameters:
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of the caller's orders, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CustomerOrder'
                  meta:
                    $ref: '#/components/schemas/PaginationMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/orders/{order}:
    get:
      operationId: showMyOrder
      tags:
        - Orders
      summary: One of the caller's own orders
      description: |
        Somebody else's order is **`404 resource.not_found`**, never 403.
        Confirming that an identifier exists while refusing to show it tells a
        stranger the order is real — and this is the resource a delivery address
        is snapshotted onto.

        **No `ETag`.** The row carries `lock_version` and this audience cannot
        write it: confirm, fulfil and cancel are the kitchen's actions behind the
        kitchen's permission. Serving a validator to a reader with no writer
        invites a client to send `If-Match` at an endpoint that does not exist
        for it. The kitchen's read of the same row does carry one.

        The allergen statement on each line is served exactly as it was
        snapshotted at placement — code and containment, never the derivation or
        the source ingredient. It is what the customer was told the food
        contains.
      parameters:
        - $ref: '#/components/parameters/OrderPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The order as the customer sees it.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/items:
    get:
      operationId: listStockItems
      tags:
        - Kitchen inventory & production
      summary: List stock items
      description: |
        Every shelf the organisation has. Stock items are **derived**
        (INV2.0), not declared: one per ingredient in the library — the
        platform's and the kitchen's own — and one per product the kitchen
        buys in to resell. `backing` says which. Not paginated: the list is
        fetched whole, the way suppliers and production orders already are.

        **The order is the ranking.** Shelves that hold something come
        first, then shelves that have ever moved, then the rest by name — a
        picker over the whole ingredient library is unusable alphabetically.
        A client that preserves the order it is given gets the shelves a
        kitchen actually receives at the top; `is_stocked` and `has_history`
        are published so it can mark the difference too.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every stock item.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockItemCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/levels:
    get:
      operationId: listStockLevels
      tags:
        - Kitchen inventory & production
      summary: On-hand stock levels
      description: |
        Every level the organisation holds, enriched with the item's own
        `item_code`, `item_name_en` and `ingredient_id` (O1) so a levels
        screen never has to join to render a row. `X-Branch-Id` narrows
        the list to one branch; absent, every branch's levels are
        returned.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XBranchId'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Stock levels, newest stock item first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockLevelCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/low-stock-count:
    get:
      operationId: countLowStockLevels
      tags:
        - Kitchen inventory & production
      summary: Low-stock level count
      description: |
        How many levels are low right now (INV1.3): a level with a
        `reorder_threshold` set whose `quantity` has reached or fallen to or
        below it. Computed in the query — the same rule the levels index
        applies per row as `is_low` — so a hub badge and the stock screen
        never disagree. `X-Branch-Id` narrows the count to one branch;
        absent, every branch's levels are counted. A null threshold is
        never low and is excluded.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XBranchId'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The count of low-stock levels in scope.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LowStockCountEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/threshold:
    patch:
      operationId: setStockThreshold
      tags:
        - Kitchen inventory & production
      summary: Set or clear a reorder threshold
      description: |
        Sets — or clears — the `reorder_threshold` (and optional
        `par_level`) for one (branch, stock item) pair (INV1.3). A null
        `reorder_threshold` clears it, which the low-stock computation reads
        as *never low*; nothing here stores a flag. The level row is created
        if the item has never moved at this branch, so a threshold can be set
        before the first receipt. A PATCH rather than the ops family's usual
        POST because it edits a level's property in place and is idempotent,
        where an adjustment or a waste appends a movement.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetStockThresholdRequest'
      responses:
        '200':
          description: The threshold was set and the level returned.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockLevelEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/adjustments:
    post:
      operationId: recordStockAdjustment
      tags:
        - Kitchen inventory & production
      summary: Record a stock adjustment
      description: |
        A signed correction to a level — a stock take finding more or
        less than the ledger says. `branch_id` is a body field rather
        than `X-Branch-Id`, because the movement this writes is
        organisation data, not a header-scoped read.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockAdjustmentRequest'
      responses:
        '201':
          description: The adjustment was recorded and the level updated.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockMovementEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/waste:
    post:
      operationId: recordStockWaste
      tags:
        - Kitchen inventory & production
      summary: Record stock waste
      description: |
        States the loss as a positive quantity; the server signs it
        negative on the movement ledger (`reason: waste`) — the same
        split as `POST .../production/orders/{productionOrder}/complete`'s
        `consumes`.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockWasteRequest'
      responses:
        '201':
          description: The waste was recorded and the level reduced.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockMovementEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/suppliers:
    get:
      operationId: listSuppliers
      tags:
        - Kitchen inventory & production
      summary: List suppliers
      description: |
        The organisation's supplier book, ordered by code, each row carrying its
        contact count and who to call.

        **Archived suppliers are excluded by default.** Every caller is a picker
        or a list, and a picker offering a supplier the kitchen stopped buying
        from is how an order gets sent to a shuttered warehouse. Send
        `include_archived=true` for the archive filter on the suppliers screen.

        Not paginated: a kitchen's supplier book is a bounded set a person
        maintains by hand.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: include_archived
          in: query
          required: false
          description: Include archived suppliers. Omitted or false serves the live book only.
          schema:
            type: boolean
            default: false
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every supplier, ordered by code.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplierCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createSupplier
      tags:
        - Kitchen inventory & production
      summary: Add a supplier
      description: |
        Adds a supplier to the organisation's book so a goods receipt can be
        posted against it. `code` is optional — omit it and the server mints a
        unique per-organisation code from the name. `currency_code` is a hint the
        receipt form pre-selects; it is not required and books nothing.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSupplierRequest'
      responses:
        '201':
          description: The supplier was created.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplierEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/suppliers/{supplierId}:
    get:
      operationId: getSupplier
      tags:
        - Kitchen inventory & production
      summary: One supplier
      description: |
        The supplier record with its named contacts embedded. Unlike a delivery
        zone's map, contacts are a bounded set a person maintains by hand — a
        handful of people, not a hundred places — so the page that shows the
        record is the page that edits them and one read serves both.

        An archived supplier is served normally rather than hidden: a receipt
        posted last month names it, and the screen explaining that receipt needs
        the record. `archived_at` is what tells a client to render it read-only.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/SupplierPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The supplier and its contacts.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplierDetailEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updateSupplier
      tags:
        - Kitchen inventory & production
      summary: Edit a supplier
      description: |
        A partial write: a field the body omits is left alone, a field sent as
        `null` is cleared. `code` is editable and its uniqueness check ignores
        this supplier's own row, so re-saving an unchanged form is never a
        conflict with itself.

        No `If-Match`. A supplier record carries no lock version — two people
        editing the same supplier's phone number is not the race a delivery
        map's area claims are.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/SupplierPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSupplierRequest'
      responses:
        '200':
          description: The supplier after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplierEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/suppliers/{supplierId}/archive:
    post:
      operationId: archiveSupplier
      tags:
        - Kitchen inventory & production
      summary: Archive a supplier
      description: |
        **Not a delete, and reversible.** The supplier leaves every picker and
        every automatic suggestion and keeps every goods receipt posted against
        it — a purchases ledger that forgets who it bought from is not a ledger.

        An action rather than a writable `archived_at`, so retiring a supplier
        is one deliberate request with its own audit event instead of something
        a form save can do by accident. Idempotent: archiving an archived
        supplier keeps the original timestamp.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/SupplierPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The archived supplier.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplierEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/suppliers/{supplierId}/restore:
    post:
      operationId: restoreSupplier
      tags:
        - Kitchen inventory & production
      summary: Restore an archived supplier
      description: |
        Puts an archived supplier back in the book — the counterpart to archive,
        and the reason archiving is safe to offer. A kitchen that stopped buying
        from somebody for a season starts again without re-typing the record or
        its contacts. Idempotent: restoring a live supplier changes nothing.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/SupplierPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The restored supplier.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplierEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/suppliers/{supplierId}/contacts:
    put:
      operationId: replaceSupplierContacts
      tags:
        - Kitchen inventory & production
      summary: Replace a supplier's contacts
      description: |
        Set-replace: the body is the whole desired contact set, applied in one
        transaction. Contacts carrying an `id` are updated, contacts without one
        are created, and contacts absent from the body are deleted.

        A `PUT` because the request is idempotent — sending it twice leaves the
        same set rather than a doubled one. The screen has one **Save contacts**
        action to match; a save fired per card would delete the card the user was
        halfway through adding.

        At most one contact may be primary, and every contact needs at least one
        of an email address, a telephone number or a WhatsApp number. Both are
        `422` before the database constraint behind them can fire.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/SupplierPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceSupplierContactsRequest'
      responses:
        '200':
          description: The contact set after the replace, in display order.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplierContactCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/supplier-links:
    put:
      operationId: upsertSupplierLink
      tags:
        - Kitchen inventory & production
      summary: Link a stock item to a supplier
      description: |
        Records "we buy this from them" (SUP2), or updates the link that is
        already there. Idempotent on the `(supplier_id, stock_item_id)` pair —
        sending the same body twice leaves the same row, which is why this is a
        `PUT`.

        **One mutation surface, not two mirrored replaces.** A supplier's
        supplied items and a stock item's suppliers are one table read from two
        ends; a set-replace at either end would silently undo what the other
        end had just written.

        `is_preferred: true` **moves** the flag — whichever supplier held it for
        this stock item loses it in the same transaction, because at most one
        preferred supplier per item is a database rule and a straight handover
        would otherwise collide with it mid-swap. `false` clears this link's own
        flag and promotes nobody.

        `supplier_item_ref` is the supplier's own catalogue reference, quoted
        back on an order sheet. Omitting it leaves the stored value alone;
        sending `null` clears it.

        Cross-organisation identifiers — either end — answer `404`, never a
        silent link. Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpsertSupplierLinkRequest'
      responses:
        '200':
          description: The link after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplierLinkEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    delete:
      operationId: deleteSupplierLink
      tags:
        - Kitchen inventory & production
      summary: Unlink a stock item from a supplier
      description: |
        Removes one supplier↔item link (SUP2). The identifying pair travels in
        query parameters rather than a body, because that is where every other
        delete in this API names its subject.

        A real delete, unlike a supplier's own record, which only archives: a
        link is current configuration, and every goods receipt posted against
        the supplier stays exactly as it is either way.

        Idempotent — unlinking something that is not linked answers `204`,
        because the caller's intention is already true. Cross-organisation
        identifiers still answer `404`. Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: supplier_id
          in: query
          required: true
          description: The supplier half of the link.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: stock_item_id
          in: query
          required: true
          description: The stock item half of the link.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '204':
          description: The link is gone — or was never there.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/item-purchases/latest:
    get:
      operationId: listItemLatestPurchases
      tags:
        - Kitchen inventory & production
      summary: Latest purchase per stock item
      description: |
        The newest priced purchase of each named shelf, **across every
        supplier**, with the supplier that sold it (SUP2).

        This endpoint exists because of a module boundary and it is worth
        saying plainly: the stock screen wants a "last purchase" column, but
        the stock list is served by Inventory and Inventory may not import
        Procurement. The price is therefore served here, keyed by
        `stock_item_id`, and the client joins the two lists it already holds. A
        batch read for the same reason the supplier page has one — sixty
        visible shelves must cost one request, not sixty.

        Derived live from `goods_receipt_lines`, never stored. Unpriced
        deliveries are skipped entirely rather than answered as a price of
        nothing, so an item received only on unpriced receipts is **absent**
        from the response — "never bought" is the absence of a purchase, and a
        row of nulls would be a purchase-shaped object claiming otherwise.

        Behind `inventory.view_organisation`, with the money behind
        `inventory.view_costs_organisation`: without the cost permission
        `unit_price_amount` and `cost_currency_code` are `null` and
        `meta.costs_redacted` is `true`, while the date, quantity, unit and
        supplier remain. A `403` would blank the column for somebody entitled
        to know *that* an item was last bought on Tuesday, and would make
        "hidden" and "never bought" indistinguishable.

        Two hundred identifiers is the cap.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: stock_item_ids
          in: query
          required: true
          description: The shelves to price, repeated as `stock_item_ids[]=…`. At most 200.
          style: form
          explode: true
          schema:
            type: array
            minItems: 1
            maxItems: 200
            items:
              $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The latest purchase of each requested item that has one.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ItemLatestPurchaseCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/supply-needs/count:
    get:
      operationId: countSupplyNeeds
      tags:
        - Kitchen inventory & production
      summary: Supply-needs count for one branch
      description: |
        How many shelves at one branch need ordering (SUP3) — the hub badge and
        the Supply orders landing metrics, in one read.

        The queue is the distinct union of two rules over the branch's existing
        levels: **out of stock** (`quantity <= 0`) and **low stock**
        (`reorder_threshold` set and `quantity` at or below it). A level meeting
        both is counted **once**, as out of stock, so `out_of_stock_count` plus
        `low_stock_count` always equals `count`.

        This is deliberately wider than `/catalogue/inventory/low-stock-count`,
        which is a *warning* and ignores levels with no threshold set. A kitchen
        that never set a threshold on olive oil and has run out of olive oil
        still needs to order olive oil, so emptiness joins lowness here.

        Answered by the same predicate the order proposal lists, so a badge can
        never disagree with the list it opens. A stock item with no level row at
        this branch has never moved here and is **not** a shortage; it reaches an
        order through the proposal's `stock_item_ids`.

        `branch_id` is required and is a query parameter rather than the
        `X-Branch-Id` header: stock is a quantity on a shelf at a site, there is
        no honest organisation-wide answer, and a manager holding an
        organisation-wide membership has no header branch yet must still be able
        to prepare an order for one.

        Requires `inventory.order_supplies_organisation`. Not the plain view
        code — this number is the front door of the order book.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: branch_id
          in: query
          required: true
          description: The branch whose shelves are counted. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The branch's shortage tallies.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupplyNeedsCountEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/order-proposal:
    get:
      operationId: getOrderProposal
      tags:
        - Kitchen inventory & production
      summary: What one branch should order
      description: |
        The supply-order builder's whole read (SUP3): the shortage queue in the
        order it should be worked, the quantity the system is willing to suggest
        for each row, and the suppliers each shortage could be bought from.

        **Nothing is created.** A `GET`, and it stays one: the builder re-reads
        this whenever an item is added or Refresh is pressed, and a screen that
        minted draft orders by being opened would fill a kitchen's order book
        with purchases nobody decided to make. Creating the drafts is a separate
        transactional write.

        **Row order is the answer, not a hint.** Out-of-stock rows come first,
        then low-stock rows, then explicitly requested ones; each group is
        ordered by item name with the item code as tie-break. Clients render the
        order they are given and never re-sort.

        `suggested_quantity` is `par_level - quantity_on_hand`, and only that.
        Never `reorder_threshold - quantity_on_hand`: the threshold is the
        *trigger* and is compared inclusively, so restocking exactly to it lands
        the shelf back on the boundary that raised the alarm. With no usable par
        the suggestion is `null` and `suggested_quantity_basis` is `none` — an
        honest blank the person fills in rather than a plausible wrong number.

        Supplier resolution is: the active preferred supplier, else the sole
        active linked supplier, else nobody. `supplier_options` carries every
        active candidate regardless, because the person may disagree with all
        three outcomes. Archived suppliers are excluded from the options but not
        from the diagnosis: `unassigned_reason` distinguishes `no_supplier`
        (nobody was ever linked) from `suppliers_archived` (links exist and every
        one of them is archived), because those have different fixes.

        `stock_item_ids` is the **Add another item** list — shelves that join the
        proposal whatever their level says, deduped against the queue so an item
        that was already short is one row rather than two. An id whose shelf has
        never moved at this branch answers `quantity_on_hand: "0.0000"` with null
        thresholds rather than a fabricated level row. At most 100.

        **There is no money in this response**, at any depth: no price, no cost,
        no currency. The supply-order permission is not the cost permission, and
        a payload carrying last prices "for reference" would hand the valuation
        ledger to whoever could open the builder. Cost-authorised screens fetch
        prices from the endpoint that gates them.

        Requires `inventory.order_supplies_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: branch_id
          in: query
          required: true
          description: The branch the proposal is for. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: stock_item_ids
          in: query
          required: false
          description: Manually added shelves, repeated as `stock_item_ids[]=…`. At most 100.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 100
            items:
              $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The branch's proposal rows, queue first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderProposalCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/purchase-orders:
    get:
      operationId: listPurchaseOrders
      tags:
        - Kitchen inventory & production
      summary: The order book
      description: |
        Every purchase order this kitchen has made, newest first (SUP4).

        **Three filters, and one of them is not a filter.** `status` and
        `supplier_id` narrow the book the way a person asks about it — "what is
        still out with the wholesaler". `ids` is a **batch read**: the shape a
        print preview needs when somebody issues four orders and lays them out
        as one document, so that four orders cost one request rather than four.
        At most fifty identifiers, and an identifier from another kitchen simply
        does not match rather than answering that it exists.

        Rows carry their full line sets, because there is one purchase-order
        shape rather than a list variant and a detail variant — a second shape
        is the one that drifts, and the batch read wants all of it anyway.
        Cursor-paginated over the platform's `(created_at, id)` keyset: an order
        book is written to while somebody walks it, and offset pagination
        silently skips and repeats rows when that happens.

        **No money, at any depth** — no price, no amount, no currency, no total.
        Actual prices belong to goods receipts, where each partial delivery
        carries the figure it was really invoiced at.

        Requires `inventory.order_supplies_organisation`. The **read** is gated
        too (§5): this is not a list of shelves, it is the record of who this
        kitchen buys from, how much of what, and how often.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: status
          in: query
          required: false
          description: Only orders in this state.
          schema:
            $ref: '#/components/schemas/PurchaseOrderStatus'
        - name: supplier_id
          in: query
          required: false
          description: Only orders addressed to this supplier. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: ids
          in: query
          required: false
          description: A batch read, repeated as `ids[]=…`. At most 50.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 50
            items:
              $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of purchase orders, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderCollection'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createPurchaseOrders
      tags:
        - Kitchen inventory & production
      summary: Create one draft order per supplier
      description: |
        The supply-order builder's commit (SUP4). A person grouped a shortage
        list by supplier, confirmed a preview, and this turns that preview into
        drafts.

        **All or nothing.** One transaction wraps the whole batch: eleven
        suppliers' drafts commit together or none of them do. Six of eight
        orders arriving with no way to tell which two are missing is the outcome
        the single transaction exists to make impossible. The orders come back
        **in request order**, because the person confirmed a list and the answer
        has to line up with it row for row.

        **One branch per batch.** Each order carries `branch_id` because every
        proposal row does, which is what makes a payload whose branch disagrees
        with its lines unrepresentable — and the server then insists they all
        agree, because §4 says a proposal is always *for one branch*. Two
        branches in one body is a `422`, not a guess.

        **A line is `stock_item_id` and `quantity`, and nothing else.** The item
        code, both names, the unit and the supplier's own catalogue reference are
        all resolved server-side and frozen onto the row; a client that could
        name a line could put anything at all on a document a supplier reads.
        There is no price field and none is coming.

        Quantities are decimal strings with at most four places — the stock
        column's precision. `2.55555` earns a `422` naming the exact line rather
        than being silently truncated.

        An **archived** supplier answers `409` with
        `details.reason = supplier_archived`, the same refusal issuing gives: a
        draft that could never be issued is not a useful thing to have created.
        A supplier, branch or shelf belonging to another kitchen answers `404`.

        Each order created is audited as `procurement.purchase_order_created`.
        Requires `inventory.order_supplies_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePurchaseOrdersRequest'
      responses:
        '201':
          description: The drafts created, in request order.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderCollection'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PurchaseOrderConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/purchase-orders/{purchaseOrder}:
    get:
      operationId: getPurchaseOrder
      tags:
        - Kitchen inventory & production
      summary: One purchase order
      description: |
        The same shape the list serves, because there is one purchase-order
        shape (SUP4).

        Two suppliers appear on an issued order and both are deliberate.
        `supplier` is the **live** record with its archive flag, because a
        screen offering to issue needs to know whether the supplier is still in
        the book. `recipient_snapshot` is who the document was **addressed to**
        at the instant it was issued, and it is `null` on a draft because a
        draft has been addressed to nobody yet. §3.5: draft previews use current
        supplier data, issued reprints use the snapshot.

        Requires `inventory.order_supplies_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PurchaseOrderPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The order, its lines and its recipient.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updatePurchaseOrder
      tags:
        - Kitchen inventory & production
      summary: Edit a draft order
      description: |
        Presence-keyed like every other partial write in this module: an omitted
        field is left alone, `notes: null` clears the note.

        `lines` is a **full replace** — the body states the whole desired set —
        because the alternative is a diff, and a diff needs an identity for a
        line the client does not name. Every line is re-snapshotted from the
        database rather than carried over, so a shelf renamed since the draft was
        made is renamed on the draft too. That is correct precisely because a
        draft has not been handed to anybody yet.

        **Draft only.** An issued, cancelled or received order answers `409`
        with `details.reason = purchase_order_not_draft`, and that is §3.5's
        immutability rather than a permission problem: the supplier is holding a
        copy of what was issued, and an order that could still change would make
        their copy a fiction.

        Requires `inventory.order_supplies_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PurchaseOrderPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePurchaseOrderRequest'
      responses:
        '200':
          description: The draft after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PurchaseOrderConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/purchase-orders/{purchaseOrder}/issue:
    post:
      operationId: issuePurchaseOrder
      tags:
        - Kitchen inventory & production
      summary: Issue a draft order
      description: |
        **Issue, not send** (§2). Phase 1 dispatches nothing through any
        channel: a person presses this, the lines freeze, and they print the
        sheet and hand it over. There is deliberately no `send` endpoint and no
        `sent_at`, because a later dispatch phase must have somewhere honest to
        record the real event.

        Two things happen and both are the point. The status moves `draft →
        issued`, after which every further edit is refused — the supplier is
        holding a copy, and an order that could still change would make their
        copy a fiction. And `recipient_snapshot` captures the supplier exactly
        as it stands now, so a reprint in June reproduces the document handed
        over in February even though the supplier has since moved premises and
        replaced its sales rep.

        Two refusals, both `409` with an explicit `details.reason`: an order
        that is not a draft (`purchase_order_not_draft`) and an archived
        supplier (`supplier_archived`). The second is §3.1's rule at the moment
        it matters — the supplier was live when the draft was made and has been
        archived since, which is exactly when a kitchen should be stopped rather
        than allowed to post an order to a shuttered warehouse.

        An action rather than a writable status field, so freezing a document is
        one deliberate request with its own audit event
        (`procurement.purchase_order_issued`). Requires
        `inventory.order_supplies_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PurchaseOrderPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The issued order, with its recipient snapshot.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PurchaseOrderConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/purchase-orders/{purchaseOrder}/cancel:
    post:
      operationId: cancelPurchaseOrder
      tags:
        - Kitchen inventory & production
      summary: Cancel a draft or issued order
      description: |
        **Nothing is deleted** (§3.5). The order, its lines and — if it was
        issued — its recipient snapshot all stay exactly where they are, and
        `issued_at` is left in place: cancelling does not un-issue, and a
        reprint of a cancelled order should still say when it went out. What
        changes is the status and one new stamp.

        Reachable from `draft` and `issued` and from nowhere else. An order with
        deliveries against it cannot be cancelled as though nothing happened, so
        a refusal answers `409` with
        `details.reason = purchase_order_not_cancellable`.

        Audited as `procurement.purchase_order_cancelled`. Requires
        `inventory.order_supplies_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PurchaseOrderPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The cancelled order, unchanged apart from its status and stamp.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PurchaseOrderConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/reference:
    get:
      operationId: getProcurementReference
      tags:
        - Kitchen inventory & production
      summary: Goods-receipt form reference data
      description: |
        The small reference sets the goods-receipt form needs, answered in one
        read: the active currencies a price can be booked in, the organisation's
        own default currency (the kitchen's currency, and the honest default for
        a receipt), and the measurement units a purchase line can be quoted in —
        each with its `dimension`, so the line editor offers only the units in
        the stock item's own dimension (conversion refuses across dimensions).

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Currencies, the organisation default, and measurement units.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProcurementReferenceEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/goods-receipts:
    get:
      operationId: listGoodsReceipts
      tags:
        - Kitchen inventory & production
      summary: List goods receipts
      description: |
        The most recent 50 receipts, newest first, each with its lines —
        a receipt without them is only half the record. `purchase_order_id`
        is always null in v1 (O2): there is no purchase-order surface yet
        to have created one.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Recent goods receipts.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoodsReceiptCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createGoodsReceipt
      tags:
        - Kitchen inventory & production
      summary: Post a goods receipt
      description: |
        Posts every line straight into the inventory ledger
        (`reason: receipt`) inside one transaction, raising each named
        stock item's level at the branch. There is no draft state: posting
        a receipt is the same act as recording that the stock arrived.

        **One endpoint for both kinds of delivery** (§6). A direct market
        purchase sends lines and nothing else; a delivery against an issued
        order adds `purchase_order_id` and a `purchase_order_line_id` per
        matched line, and the order's received state is updated inside the same
        transaction. There is deliberately no second receipt-writing service:
        two writers would be two chances for the purchase ledger and the stock
        ledger to disagree about one delivery.

        **Prices are accepted under `inventory.manage_organisation`** and are
        redacted from that same clerk's reads without
        `inventory.view_costs_organisation` — §5's blind-write model. A line may
        also be posted with no price at all when the invoice has not arrived,
        which puts the receipt in the **Unpriced receipts** queue.

        Four refusals are worth knowing about, all `422` unless stated:

        - **over-receipt** — more arriving than the order still has outstanding
          is refused until `over_receipt_confirmed` is true, and then still
          needs a `variance_note`. §3.5 asks for an explicit confirmation *and*
          a note: one records that somebody clicked, the other records what they
          knew.
        - **an unplanned item** — a line with no `purchase_order_line_id` on a
          delivery against an order needs the same `variance_note` (§4).
        - **the invoice arithmetic** — when `invoice_total_amount` is present it
          must equal `Σ line totals − discount + tax + delivery + other`. The
          refusal carries `details.expected_total_amount` and
          `details.difference_amount`, because the person reading it has the
          supplier's paper in their hand.
        - **two currencies** — every priced line and every header charge on one
          receipt shares one currency. There is no exchange rate in this system
          (§4.4). A header charge on a receipt with no priced line has no
          currency at all and is refused for the same reason.

        A `409` names an order-level refusal in `details.reason`: the order is
        not receivable, or its branch or supplier disagrees with the delivery.

        A priced line whose ingredient holds its valuation in another currency
        is **not** refused: the stock is posted, the supplier's price is kept
        exactly as written, and the line is flagged `valuation_pending_fx` while
        the receipt stays short of `complete` (§3.6).

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PostGoodsReceiptRequest'
      responses:
        '201':
          description: The receipt was posted and inventory updated.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoodsReceiptEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PurchaseOrderConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/goods-receipts/{goodsReceipt}:
    get:
      operationId: getGoodsReceipt
      tags:
        - Kitchen inventory & production
      summary: One goods receipt, with the order it settled
      description: |
        The delivery whole: its lines, its two document references, its business
        date, its cost status — and, when it was made against a purchase order,
        that order's per-line ordered/received/outstanding arithmetic (SUP5).

        Behind `inventory.manage_organisation`, the same code that posted it: a
        person who booked a delivery in has to be able to look at what they
        booked. The **money** is behind `inventory.view_costs_organisation` and
        is redacted inside the response rather than refused at the door, so a
        receiving clerk still sees quantities, the delivery note and whether the
        paperwork is outstanding.

        `cost_status`, the unpriced counts and `valuation_pending_fx` are **not**
        redacted, deliberately: they say whether this receipt still needs
        somebody's attention, not what anything cost.

        The `purchase_order` block is §5's manage-scoped subset — number, status
        and the line arithmetic somebody standing over a pallet compares against.
        There is no money on a purchase order at any depth, so nothing in it
        needs a gate.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/GoodsReceiptPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The receipt, its lines and its order match.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoodsReceiptDetailEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/goods-receipts/{goodsReceipt}/complete-prices:
    post:
      operationId: completeReceiptPrices
      tags:
        - Kitchen inventory & production
      summary: Complete the missing prices on a receipt
      description: |
        The invoice that turned up a week after the van did (§3.6).

        **It cannot move stock.** Completion "calls the costing path only for
        lines whose `costed_at` is null and never creates a second stock
        movement", and that is structural rather than a rule: neither the
        completion service nor the costing collaborator it delegates to has an
        inventory dependency at all.

        **Each line is priced once.** A line that has already been costed answers
        `422` naming it rather than being quietly skipped — a silent no-op would
        leave somebody believing they had corrected a figure they had not.
        Correcting a posted price is deliberately not this endpoint's job; §3.6
        puts that behind a reasoned, audited adjustment.

        A line already flagged `valuation_pending_fx` is refused with
        `details.reason = line_valuation_pending_fx`. Its price is already
        recorded exactly as the supplier wrote it; what is outstanding is an
        exchange-rate decision this system does not make. The reverse case is
        handled: a line priced here for the first time, in a currency the
        ingredient's valuation cannot take, is flagged the same way the post
        would have flagged it.

        `line_total_amount` is optional and is **checked** rather than stored as
        given: the total and the unit price are two views of one fact, and a
        mismatch answers `422` with `details.expected_total_amount`.

        Requires `inventory.view_costs_organisation`. Entering a price off the
        delivery note at the door is a warehouse job under the manage code;
        going back over the money afterwards is the cost holder's (§5). Audited
        as `procurement.goods_receipt_priced`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/GoodsReceiptPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteReceiptPricesRequest'
      responses:
        '200':
          description: The receipt after the prices were completed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoodsReceiptDetailEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/receivable-orders:
    get:
      operationId: listReceivableOrders
      tags:
        - Kitchen inventory & production
      summary: Orders a delivery could be received against
      description: |
        Every issued or partially received order for one branch, with each
        line's ordered, received and outstanding quantity — so the receive
        screen prefills a delivery with what is **still to come** rather than
        what was originally asked for (§4).

        Requires `inventory.manage_organisation`, not the ordering code, and §5
        grants that in as many words: "the receiving endpoint may expose an
        issued order's supplier, outstanding items and quantities to a receiver
        holding `inventory.manage_organisation` without granting the full
        supply-order book". The person unloading the van is rarely the person
        who decided to order it.

        So this is a **deliberate manage-scoped subset**: an order number, a
        supplier, an issue date and the outstanding lines. No notes, no
        recipient snapshot, no history — and, as everywhere on a purchase order,
        no money at any depth. The order book itself stays behind
        `inventory.order_supplies_organisation`.

        `branch_id` is a required query parameter rather than the `X-Branch-Id`
        header, for the reason the proposal endpoints give: a manager holding an
        organisation-wide membership has no header branch, and receiving is
        always at one site. At most fifty orders, oldest issue first.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: branch_id
          in: query
          required: true
          description: The branch taking delivery. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: supplier_id
          in: query
          required: false
          description: Only orders addressed to this supplier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Orders open for receiving at this branch, oldest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReceivableOrderCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/unpriced-receipts:
    get:
      operationId: listUnpricedReceipts
      tags:
        - Kitchen inventory & production
      summary: The unpriced-receipts work queue
      description: |
        Every receipt whose costing is not finished (§3.6): the delivery landed,
        the stock rose, and the invoice has not been entered — or was entered
        and could not be valued. The queue exists because the alternative is
        quietly omitting those receipts from a financial total that then looks
        complete.

        **Two counts, because they are two different jobs.**
        `unpriced_line_count` is "type these prices in".
        `valuation_pending_count` is "the prices are already here and an
        exchange-rate decision is not this screen's to make" — a row a person
        cannot action, and one they need to tell apart at a glance rather than
        by opening it.

        There is no money in these rows; the amounts live on the receipt detail
        behind the same gate. Ordered **oldest first**, the opposite of every
        other list in this module and the point of a work queue: the oldest
        outstanding invoice is the one that has been outstanding longest.

        Requires `inventory.view_costs_organisation` (§5).
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: branch_id
          in: query
          required: false
          description: Only receipts posted at this branch. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: supplier_id
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of receipts still waiting on their costing, oldest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnpricedReceiptCollection'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/purchases-ledger:
    get:
      operationId: listPurchasesLedger
      tags:
        - Kitchen inventory & production
      summary: Browse the purchases ledger
      description: |
        Every goods-receipt line, newest first, flattened with the date,
        supplier and item it belongs to — the browsable record behind the
        monthly spend figure (INV1.1). Filterable by date range, supplier,
        branch, stock item and ingredient.

        `stock_item_id` and `branch_id` arrive with SUP2 and are what the
        supplier page's "everything bought from this supplier" and the stock
        screen's per-row "History" land on. `stock_item_id` sits beside
        `ingredient_id` rather than replacing it: one ingredient can back both a
        shelf and a resold product, so "this ingredient" and "this shelf" are
        different questions. Both new filters are validated against the active
        organisation, so an identifier from another kitchen is a `422` rather
        than an empty page that reads like a fact.

        Behind `inventory.view_costs_organisation`: this surface exists to read
        the valuation, so a person without the cost permission gets a 403 here
        rather than a redacted page. Cursor-paginated, newest first. The
        presenter reads no recipe line, formulation quantity or derivation — a
        purchases ledger is a record of what was bought, nothing about how it is
        used.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
          description: Only lines whose receipt was received on or after this date.
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
          description: Only lines whose receipt was received on or before this date.
        - name: supplier_id
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: ingredient_id
          in: query
          required: false
          description: Only lines whose stock item is backed by this ingredient.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: stock_item_id
          in: query
          required: false
          description: Only lines that received this shelf. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: branch_id
          in: query
          required: false
          description: Only lines whose receipt was posted at this branch. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: purchase_order_id
          in: query
          required: false
          description: |
            Only lines whose receipt was made against this order (SUP5) — what
            an order detail's "everything delivered against this" link lands on.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: cost_status
          in: query
          required: false
          description: |
            Only lines whose receipt is in this costing state (SUP5). §3.4 lists
            price-completeness among the ledger's filters; it lands in the slice
            that lands the column rather than the slice that first draws a screen
            over it, because a parameter that quietly did nothing would be worse
            than its absence.
          schema:
            $ref: '#/components/schemas/ReceiptCostStatus'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of purchases-ledger lines, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchasesLedgerCollection'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/procurement/spend-summary:
    get:
      operationId: getProcurementSpendSummary
      tags:
        - Kitchen inventory & production
      summary: Weekly or monthly purchase financials
      description: |
        The same goods-receipt ledger the purchases screen browses line by line,
        rolled up into the periods a manager reconciles in (§3.7): ISO weeks or
        calendar months over the branch-local `received_on`. The aggregation is
        shared with the monthly cost report, exactly as §3.7 requires, so the
        two surfaces cannot disagree about a month's spend.

        **A period, then currencies inside it — never one total across them.**
        There is no exchange rate in this system (§4.4). An unpriced line has no
        currency at all, so the completeness facts live on the *period* and the
        money lives in `totals_by_currency`. A period whose deliveries are all
        unpriced answers an empty `totals_by_currency` with `is_complete: false`
        — §3.7's **Incomplete** made structural rather than a flag somebody has
        to remember to read.

        `unpriced_line_count` and `valuation_pending_line_count` are disjoint. A
        line with no price recorded contributes no money and is unpriced; a line
        whose price *is* recorded and whose valuation was refused for currency
        contributes its money and waits on an exchange-rate decision (§3.6).
        Either one keeps the period incomplete.

        **Item subtotal and invoice total stay separate named values**, on §3.7's
        own instruction: tax and delivery are not an ingredient's purchase price.
        The four charge totals are zero when nothing was recorded; `invoice_total`
        is `null` when no receipt in the bucket carried one, because an unknown
        invoice total is not a zero one.

        A `stock_item_id` filter narrows *lines*. A receipt's header charges are
        a fact about the whole delivery and are not apportioned to one shelf: the
        charges reported under that filter are those of the receipts the matching
        lines came from, counted once each. `item_subtotal` is unaffected and
        reconciles line for line to the purchases ledger under the same filters.

        **The window is always bounded.** Omit `from`/`to` and the answer covers
        the most recent 53 weeks or 24 months; an explicit range wider than that
        is refused with the cap named rather than silently truncated. The
        resolved bounds are echoed in `data.from`/`data.to`.

        Breakdowns are opt-in (`include=suppliers,items`) and cost exactly one
        query each, so the default payload stays a period list a screen can
        render whole. Requires `inventory.view_costs_organisation` (§5).
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: group_by
          in: query
          required: true
          description: |
            `week` groups by ISO week over `received_on` (Monday-started,
            labelled `2026-W34`); `month` groups by calendar month (`2026-08`).
          schema:
            type: string
            enum:
              - week
              - month
        - name: from
          in: query
          required: false
          description: Inclusive lower bound on the receipt's business date, `YYYY-MM-DD`.
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: false
          description: Inclusive upper bound on the receipt's business date, `YYYY-MM-DD`. Must not precede `from`.
          schema:
            type: string
            format: date
        - name: branch_id
          in: query
          required: false
          description: Only receipts posted at this branch. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: supplier_id
          in: query
          required: false
          description: Only receipts from this supplier. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: stock_item_id
          in: query
          required: false
          description: Only lines that received this shelf. Must belong to the active organisation.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: include
          in: query
          required: false
          description: |
            Comma-separated breakdowns to compute — `suppliers`, `items`, or
            both. An unrecognised token is refused rather than ignored, so a
            typo cannot look like an empty result.
          schema:
            type: string
            example: suppliers,items
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The spend summary, newest period first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpendSummaryCollection'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/reports/monthly-cost:
    get:
      operationId: getMonthlyCostReport
      tags:
        - Kitchen inventory & production
      summary: The monthly cost report
      description: |
        One kitchen's economics, month by month and currency by currency
        (INV1.4): purchasing **spend** (Σ goods-receipt line totals),
        **cost of goods sold** (Σ consume-movement cost, cancelled orders
        excluded), **waste value**, **revenue** (Σ order totals for
        confirmed/fulfilled orders), and the gross **margin** between revenue
        and COGS — plus the meal-versus-product revenue split and a
        data-quality flag for months whose COGS is understated by unresolved
        consumption exceptions.

        Every figure is grouped by its currency and never summed across
        currencies (there is no exchange rate — §4.4), so a row is one
        `(month, currency)` pair. Revenue is stored in minor units and is
        converted to the same major-unit scale as spend and COGS for display;
        every amount here is a major-unit decimal string beside its currency.

        Computed live over the receipt, movement and order ledgers — there is
        no period-close table — so it can never disagree with the purchases
        ledger and stock ledger it reconciles. Behind
        `inventory.view_costs_organisation`: it exposes costs and the margin
        reconstructable from them, so a person without the cost permission gets
        a 403. The report reads no recipe line, formulation quantity or
        supplier terms; a month is the finest grain it exposes.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: from
          in: query
          required: false
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[0-2])$
          description: Inclusive lower bound on the report month, as `YYYY-MM`.
        - name: to
          in: query
          required: false
          schema:
            type: string
            pattern: ^\d{4}-(0[1-9]|1[0-2])$
          description: Inclusive upper bound on the report month, as `YYYY-MM`.
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The monthly cost report, newest month first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonthlyCostReportCollection'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/consumption-exceptions:
    get:
      operationId: listConsumptionExceptions
      tags:
        - Kitchen inventory & production
      summary: Review consumption exceptions
      description: |
        Every thing a confirmed order could not deduct honestly (INV1.2),
        newest first, flattened with the order number, sold item and branch it
        belongs to (INV1.5). Filterable by resolution state and by the date the
        exception was raised, so a kitchen can walk what is still open or audit
        what was settled.

        Behind `inventory.view_organisation`: reading the queue is a plain ops
        read; settling or retrying an item is the gated write. Cursor-paginated,
        newest first. The presenter reads no recipe line, formulation quantity
        or ingredient cost — only which sale on which line could not be deducted
        and why.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: resolved
          in: query
          required: false
          schema:
            type: boolean
          description: Filter to resolved (`true`) or still-open (`false`) exceptions; omit for both.
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
          description: Only exceptions raised on or after this date.
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
          description: Only exceptions raised on or before this date.
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of consumption exceptions, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConsumptionExceptionCollection'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/consumption-exceptions/unresolved-count:
    get:
      operationId: countUnresolvedConsumptionExceptions
      tags:
        - Kitchen inventory & production
      summary: Unresolved consumption-exception count
      description: |
        How many consumption exceptions are still open right now (INV1.5) —
        `resolved_at IS NULL`, the count the hub badge and the review KPI read
        without pulling the whole list. `X-Branch-Id` narrows it to the
        exceptions whose order sits at one branch; absent, the whole
        organisation is counted.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XBranchId'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The count of unresolved consumption exceptions in scope.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConsumptionExceptionCountEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/consumption-exceptions/{exception}/resolve:
    post:
      operationId: resolveConsumptionException
      tags:
        - Kitchen inventory & production
      summary: Resolve a consumption exception
      description: |
        Marks one exception handled (INV1.5) — the kitchen has looked at it and
        accepted the gap, or fixed it elsewhere and is closing the record.
        Stamps `resolved_at`, who did it and an optional note.

        Idempotent: resolving an already-resolved exception changes nothing and
        returns the row as it stands. An exception from another organisation is
        indistinguishable from one that does not exist — both are
        `resource.not_found`.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: exception
          in: path
          required: true
          description: The consumption-exception identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolveConsumptionExceptionRequest'
      responses:
        '200':
          description: The exception, now resolved.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConsumptionExceptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/inventory/consumption-exceptions/{exception}/retry:
    post:
      operationId: retryConsumptionException
      tags:
        - Kitchen inventory & production
      summary: Retry a blocked consumption
      description: |
        Re-runs the consumption an exception blocks (INV1.5). The kitchen fixed
        what was missing — published the recipe, created the stock item,
        received stock — and asks the deduction to run again for that order line.

        The deduction path a confirm uses is reused exactly and guarded per
        (order line, stock item), so an ingredient a confirm already deducted is
        never deducted twice. When the line now consumes cleanly the exception
        auto-resolves; when it still cannot, its detail is refreshed and it stays
        open. Either way the (possibly settled) row is returned.

        Idempotent: retrying an already-resolved exception is a no-op. An
        exception from another organisation is `resource.not_found`.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: exception
          in: path
          required: true
          description: The consumption-exception identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The exception after the retry — resolved, or still open with a refreshed detail.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConsumptionExceptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/production/orders:
    get:
      operationId: listProductionOrders
      tags:
        - Kitchen inventory & production
      summary: List production orders
      description: |
        The most recent 50 orders, newest first. No production tasks are
        served here (O5) — an order's own checklist is not part of this
        surface.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Recent production orders.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductionOrderCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createProductionOrder
      tags:
        - Kitchen inventory & production
      summary: Plan a production order
      description: |
        Starts an order in `planned`. Nothing is consumed or yielded yet —
        that is what completing the order does.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProductionOrderRequest'
      responses:
        '201':
          description: The production order was planned.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductionOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/production/orders/{productionOrder}/complete:
    post:
      operationId: completeProductionOrder
      tags:
        - Kitchen inventory & production
      summary: Complete a production order
      description: |
        Records what the run actually consumed and yielded, one inventory
        movement per line (`reason: consume` and `reason: yield`), and
        moves the order to `completed` — all inside one transaction. Both
        lists are optional: an order with nothing yet consumed is a legal
        call, though an unusual one.

        Requires `inventory.manage_organisation`.
      parameters:
        - name: productionOrder
          in: path
          required: true
          description: The production order identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteProductionOrderRequest'
      responses:
        '200':
          description: The production order is completed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductionOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/quality-control/checks:
    get:
      operationId: listQualityChecks
      tags:
        - Kitchen inventory & production
      summary: List quality checks
      description: |
        The most recent 50 checks, newest first, over both subjects the
        allow-list permits (O3): a goods receipt or a production order.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Recent quality checks.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QualityCheckCollection'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createQualityCheck
      tags:
        - Kitchen inventory & production
      summary: Open a quality check
      description: |
        Starts a check in `pending` against a goods receipt or a
        production order (O3) — no other `subject_type` is accepted, and
        one that is not is `422 validation.failed`.

        Requires `inventory.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateQualityCheckRequest'
      responses:
        '201':
          description: The quality check was opened.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QualityCheckEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/quality-control/checks/{qualityCheck}/hold:
    post:
      operationId: holdQualityCheck
      tags:
        - Kitchen inventory & production
      summary: Hold a quality check
      description: |
        Moves the check to `hold` — status-only (O4): there is no separate
        hold record, and nothing else about the subject changes here.

        Requires `inventory.manage_organisation`.
      parameters:
        - name: qualityCheck
          in: path
          required: true
          description: The quality check identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The check is on hold.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QualityCheckEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/quality-control/checks/{qualityCheck}/release:
    post:
      operationId: releaseQualityCheck
      tags:
        - Kitchen inventory & production
      summary: Release a quality check
      description: |
        Moves the check to `released` — the only other side of the O4
        hold/release pair. There is no separate "pass" action in v1.

        Requires `inventory.manage_organisation`.
      parameters:
        - name: qualityCheck
          in: path
          required: true
          description: The quality check identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The check is released.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QualityCheckEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/orders:
    get:
      operationId: listKitchenOrders
      tags:
        - Kitchen orders
      summary: One kitchen's book
      description: |
        **The seller filter is not a filter — it is the isolation boundary.**
        `orders` carries no PostgreSQL policy and no global Eloquent scope,
        because the row belongs to a customer who is a member of nothing and an
        ambient organisation scope would hide an order from the person who placed
        it. The organisation therefore comes from the validated
        `X-Organisation-Id` context and never from a parameter, which would be a
        client asserting whose book it is reading.

        **Four filters, because this list grows without limit.** Unlike a
        person's own history, a kitchen's book accumulates for as long as the
        kitchen trades, and these are the questions staff actually ask it.

        `requested_delivery_date` matches the day the **food is wanted**, not the
        day the order was taken. Those diverge by design — an order placed on
        Monday for Friday is Friday's work — and a list keyed on the placement
        date would show a kitchen the wrong day's production every time.

        `query` searches the **order number only**, case-insensitively. Not the
        customer's name and not the address: a staff-facing search across
        confidential columns is how a directory of everybody a kitchen has ever
        delivered to gets built one query at a time. The number is what is
        printed on the receipt in the caller's hand.

        `branch_id` is a query parameter rather than `X-Branch-Id` because this
        is an organisation-wide book that may be *narrowed* to one production
        site. A kitchen manager holding an organisation-wide membership selects
        no branch, and an endpoint reading the header would show them nothing
        until they picked one.

        An omitted `status` hides nothing, unlike the catalogue lists. A retired
        article is a row nobody should price by accident; a cancelled order is
        part of the book, and a default that dropped it would make every total a
        kitchen reconciles against wrong.

        Served in the kitchen shape, which **does** carry `lock_version`: this is
        the audience that writes, and a screen that could not read the validator
        could not send the `If-Match` the lifecycle actions demand.

        Requires `order.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - name: status
          in: query
          required: false
          description: Restrict to one lifecycle state. Omitted, everything is listed.
          schema:
            $ref: '#/components/schemas/OrderStatus'
        - name: requested_delivery_date
          in: query
          required: false
          description: |
            The day the food is wanted, `YYYY-MM-DD`. Deliberately strict: a
            relative expression resolves to a different day depending on when
            the request lands, which is not a filter a kitchen can plan against.
          schema:
            type: string
            format: date
        - name: branch_id
          in: query
          required: false
          description: Narrow the organisation's book to one production site.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: query
          in: query
          required: false
          description: Case-insensitive substring match on the order number, and on nothing else.
          schema:
            type: string
            maxLength: 60
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of the organisation's orders, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/KitchenOrder'
                  meta:
                    $ref: '#/components/schemas/PaginationMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/orders/{order}:
    get:
      operationId: showKitchenOrder
      tags:
        - Kitchen orders
      summary: One order, as the kitchen sees it
      description: |
        Another kitchen's order is **`404 resource.not_found`**, the same answer
        as an order that does not exist. There is no 403 on purpose: this table
        has no database policy behind it, so the only isolation is the seller
        filter, and a 403 would confirm to one kitchen that a given identifier is
        another's live order.

        **Returns `ETag: "<lock_version>"`**, which the three lifecycle actions
        send back as `If-Match`. That round trip is the whole
        optimistic-concurrency contract for this resource, and it matters more
        here than almost anywhere else on the platform: a kitchen screen showing
        an order list is stale the moment it renders, and two staff members
        confirming the same order at once is an ordinary Tuesday.

        The full delivery snapshot comes with it, street included. It is
        confidential and it is also where the food has to go; withholding it from
        the kitchen would be privacy theatre performed on the one party that
        needs the data to do the job.

        Requires `order.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/OrderPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The order with its lines and its delivery snapshot.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KitchenOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/orders/{order}/confirm:
    post:
      operationId: confirmKitchenOrder
      tags:
        - Kitchen orders
      summary: Confirm an order
      description: |
        Its own route and its own audit action, **never a `PATCH status`**. The
        three lifecycle actions are three decisions with three consequences and
        three separate timestamps; a status field would collapse them into one
        write a client could aim anywhere, and `placed → fulfilled` would become
        expressible by typing.

        Reachable from `placed` only. An illegal move is `409 resource.conflict`
        naming both ends and the transitions that *are* allowed, because the
        caller usually knows only one of them — a client sending "confirm" is
        working from a screen that may be seconds stale, and "this order is
        already cancelled" is the answer rather than "confirm is invalid".

        **`If-Match` required.** The validator is checked inside the same
        conditional `UPDATE` that performs the transition, so there is no window
        between reading the version and writing the row. The state machine
        refuses an illegal move anyway — the header guards against a *lost
        update*, not against an impossible one. A header present but not a
        Healthy360 validator is **400**, not 409: the client has not lost a race,
        it has sent nonsense.

        `confirmed_at` is stamped, in its own column. Separate timestamps rather
        than one `status_changed_at` because "how long between taking an order
        and confirming it" is answerable from columns and unanswerable from a
        field that has been overwritten twice.

        Requires `order.manage_organisation` — seeing the book and committing the
        kitchen to cook are different authorities.
      parameters:
        - $ref: '#/components/parameters/OrderPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The confirmed order, with its new validator.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KitchenOrderEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/orders/{order}/fulfil:
    post:
      operationId: fulfilKitchenOrder
      tags:
        - Kitchen orders
      summary: Record an order as delivered
      description: |
        **Spelled `fulfil`**, and the path spells it that way because the
        platform is British throughout — `fulfilled_at`, the `fulfilled` status,
        `OrderLifecycle::fulfil()`. A URL disagreeing with the column, the enum
        and the method it drives would be one spelling nobody could predict from
        the other three.

        **The terminal state, and it is one-way.** A fulfilled order cannot be
        cancelled: the food has been delivered, and whatever happens next is a
        refund or a complaint — different objects with different money attached.
        Making that a transition would let a kitchen erase a delivery by changing
        a column.

        Reachable from `confirmed` only. Skipping confirmation is `409` rather
        than a convenience: an order nobody accepted is not an order anybody
        cooked.

        **`If-Match` required**, and it matters slightly more here than on the
        other two: this row is the last word on whether a customer received their
        food, and a lost update against it is a delivery the book says happened
        twice or not at all.

        Requires `order.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/OrderPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The fulfilled order, with its new validator.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KitchenOrderEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/orders/{order}/cancel:
    post:
      operationId: cancelKitchenOrder
      tags:
        - Kitchen orders
      summary: Cancel an order
      description: |
        **A POST action, never a DELETE.** A cancelled order is not a removed
        one: it keeps its number, its lines and the prices they were sold at,
        because a kitchen reconciling a week needs to know what it did not sell
        as much as what it did. DELETE would promise an erasure this platform
        will not perform.

        **The reason is required**, which is the one thing here worth arguing
        about. A nullable reason would be honoured by roughly every client in a
        hurry, and one order cancelled "for no stated reason" is the row that
        makes every count wrong — a kitchen cannot tell stock problems from
        changes of mind from a column that is sometimes empty. A fixed vocabulary
        rather than free text for the second reason: free text turns a count into
        a search problem, and it is also how a customer's name ends up in a
        column nobody classified.

        `payment_failed` is deliberately **not** in the vocabulary. There is
        nothing to fail — orders are paid at the door — and reserving the value
        now would be designing for a phase that has not been designed.

        Reachable from `placed` and `confirmed`, and from nowhere else.

        **`If-Match` required.** Cancelling something a colleague confirmed one
        second ago is exactly the lost update the validator exists to catch, and
        it is the transition where getting it wrong costs a customer their
        dinner.

        Requires `order.manage_organisation`.
      parameters:
        - $ref: '#/components/parameters/OrderPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelOrderRequest'
      responses:
        '200':
          description: The cancelled order, carrying the reason it was cancelled for.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KitchenOrderEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/orders/{order}/payments:
    post:
      operationId: recordOrderPayment
      tags:
        - Kitchen orders
      summary: Record that money arrived against an order
      description: |
        A sub-resource of the **order**, beside `confirm`, `fulfil` and `cancel`,
        rather than an order-desk operation. A receipt is not a desk artefact: a
        driver's cash comes back against an order the desk never touched, and a
        WISH reconciliation will one day write here with no desk in the room at
        all. The desk is the surface that usually records one; the order is what
        a receipt is always about.

        **`method` is deliberately not constrained to the order's own
        `payment_method`.** An order taken for cash at the counter and settled by
        a WISH transfer is an ordinary evening, and so is a delivery meant for
        cash at the door that the customer transferred while the driver waited.
        The order's field is the *intent* captured at placement and this one is
        how the money *arrived*; recording the divergence is the point of having
        two. Refusing it would push a desk into either lying in the request or
        editing the order, and the second rewrites what the customer agreed to.

        **The body carries no currency.** The receipt is denominated in the
        order's, taken from the order — a receipt in another currency is not a
        receipt for this order.

        **Over-payment is permitted and is not an error.** A customer hands over
        a round note and takes change from the till; the change is not a row
        here, because this ledger records money arriving and nothing else. Part
        payment is equally real — a deposit now and a balance on delivery are two
        rows — so `receipted` is `received_minor >= total_minor` and no amount is
        checked against the order's total.

        **Whether an order is paid is derived, never stored.** There is no
        payment-status column on an order and there will not be one: a stored
        flag beside an append-only ledger is a second answer to a question the
        ledger already answers.

        **A cancelled order refuses**, `409 resource.conflict`, with
        `details.status`, `details.allowed_statuses` and
        `details.current_lock_version`. Nobody records money arriving for an
        order that no longer exists commercially: the kitchen is not cooking it,
        and the figure would reconcile against nothing. If a cancelled order
        really was paid for, what happened is a refund. A **fulfilled** order
        does *not* refuse — cash on delivery arrives when the food does, and the
        driver's takings are keyed in afterwards.

        **`If-Match` required**, and it guards something subtler than the
        lifecycle operations do. Nothing about the order row is written here, so
        the validator is not protecting against a lost update; it protects
        against *acting on a stale view* — the screen that offered "take payment"
        was rendered before somebody cancelled the order. For the same reason the
        order's `lock_version` is **not** bumped by a receipt: a validator that
        moved every time a colleague keyed in cash would invalidate every screen
        holding it, and nothing they were looking at would have changed.

        **`Idempotency-Key` matters more here than on most commands.** Two
        genuine receipts for the same amount against one order are exactly what a
        deposit and a balance look like, so nothing about the ledger can tell a
        retry apart from a second payment. The same key with the same body
        replays the original `201` and the original receipt.

        Requires `order.manage_organisation`, the same authority as the lifecycle
        actions: recording that a kitchen has been paid is a write to its book,
        not a reading of it.
      parameters:
        - $ref: '#/components/parameters/OrderPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/IdempotencyKey'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StorePaymentReceiptRequest'
      responses:
        '201':
          description: |
            The receipt that was written, and the order's payment position with
            it counted in.

            No `ETag`: the order's validator has not moved, and serving one from
            an operation that did not change it would invite a client to treat
            this as a write to the order.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderPaymentReceiptEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/queue:
    get:
      operationId: listOrderDeskQueue
      tags:
        - Order desk
      summary: The open book, in the order it has to be worked
      description: |
        **Not a second order index.** `GET /catalogue/orders` is the kitchen's
        whole book, newest first, walked with a cursor — the surface for "what
        did we do last Tuesday". This is the surface for "what is next", which is
        a different sort and therefore a different operation. They could not be
        one: the due order is a computed expression across three tables, and the
        cursor pagination re-sorts everything it walks, so a cursor and this
        ordering are mutually exclusive by construction.

        **`due_at` is the sort, and it is never null.** It is the instant an
        order is actually due, built from the day the customer asked for, the
        start of the delivery window they chose, and the timezone of the branch
        that cooks it — with three fallbacks, each of which is a state the data
        really reaches. A window with no hours, or no window at all, falls to the
        **end** of the requested day: an order due "some time on Thursday" must
        not sort ahead of one due at nine that morning. No branch falls to UTC,
        the only zone that is not a guess. And no requested day at all falls to
        `placed_at` — an order nobody named a day for is not unscheduled, it is
        as soon as possible, and ordering it by when it arrived is the only
        ordering fair to whoever has waited longest.

        **Bounded, not paginated.** At most two hundred rows, with
        `meta.truncated` telling the screen the truth when more matched. A desk
        queue is a list somebody works through; a kitchen with more than two
        hundred open orders in one window has a problem the screen should state
        rather than hide behind a page two nobody would click.

        `window` is a vocabulary rather than a date range because these are the
        three questions a desk asks. `today` **includes orders with no requested
        date**; `overdue` deliberately does not, because a dateless order is
        never late.

        `branch_id` does two jobs. It narrows an organisation-wide queue to one
        production site — a *narrowing* of a book the caller can already see,
        which is why it is a query parameter and not `X-Branch-Id` — and it names
        the clock `today` is read on, because a branch is a place and a place has
        a working day. Unnamed, the day boundary is UTC. `meta.today` and
        `meta.timezone` say which was used.

        `query` searches the **order number only**, case-insensitively, on the
        order book's argument verbatim: a staff-facing search across confidential
        columns is how a directory of everybody a kitchen has ever delivered to
        gets built one query at a time. That the row beside it may carry a name
        changes nothing — reading a name the kitchen already has an order for is
        a disclosure with a purpose; matching on one turns the queue into a
        lookup by person.

        Requires `order.view_organisation`. The `customer` object additionally
        requires `order.view_customer_contact_organisation` and is **absent**
        without it — absent rather than null, because null is a fact about the
        customer and a screen cannot tell that apart from a fact about the reader
        unless the shapes differ.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: window
          in: query
          required: false
          description: |
            Which slice of the open book. Defaults to `today`, which also carries
            every order with no requested date at all.
          schema:
            $ref: '#/components/schemas/OrderDeskWindow'
        - name: branch_id
          in: query
          required: false
          description: |
            Narrow to one production site, and read `today` on that branch's
            clock. Omitted, the queue is organisation-wide and the day boundary
            is UTC.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: status[]
          in: query
          required: false
          description: |
            Restrict to a subset of the two open states. Omitted, both are
            listed. `fulfilled` and `cancelled` are not accepted at all: they are
            finished with, and they are the order book's business.

            **Repeat the bracketed name** — `?status[]=placed&status[]=confirmed`
            — which is the only form this server reads. PHP collapses repeated
            bare parameters to the last value, so the plain `status=placed&
            status=confirmed` that `style: form` with `explode: true` describes
            for an unbracketed name arrives at the controller as the single
            string `confirmed`, fails its `array` rule and earns a 422 on every
            multi-status request. The name carries the brackets so that the
            document describes what the endpoint actually accepts; this is the
            only array-valued query parameter in the specification, so there is
            no sibling convention it diverges from.
          schema:
            type: array
            items:
              type: string
              enum:
                - placed
                - confirmed
          style: form
          explode: true
        - name: delivery_window_code
          in: query
          required: false
          description: Restrict to one named delivery slot, by the window's own code.
          schema:
            type: string
            maxLength: 40
        - name: fulfilment_type
          in: query
          required: false
          description: |
            Restrict to one way of leaving the kitchen. Omitted, all three are
            listed.

            **A single value, unlike `status[]`.** The two filters look alike and
            are not. The statuses are a subset question — a desk watching for
            unconfirmed work wants `placed` alone, and selecting neither means
            both. The three fulfilment types are three different jobs done by
            three different people: a dispatch board wants deliveries, a
            collection counter wants pickups. Nobody asks for "deliveries and
            counter sales but not pickups", so a multi-select would be inventing
            a question to justify a control.
          schema:
            $ref: '#/components/schemas/FulfilmentType'
        - name: query
          in: query
          required: false
          description: Case-insensitive substring match on the order number, and on nothing else.
          schema:
            type: string
            maxLength: 60
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The open queue in due order, capped, with the day and clock it was measured against.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskQueueEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/quote:
    post:
      operationId: quoteOrderDesk
      tags:
        - Order desk
      summary: What this sale would cost, and everything standing in the way of it
      description: |
        **A POST that writes nothing.** The question has a basket in it and a
        basket does not fit in a query string — the same argument
        `POST /catalogue/checkout/preview` is a POST on. Nothing is created,
        nothing is reserved, and asking twice is asking once.

        ## Refusals are the answer, not a failure

        **This operation returns `200` with the problems in the body**, and it is
        the one place in this API where that is right. A withdrawn article, an
        unpriced one, an address nobody delivers to, a cut-off that passed at
        three: each arrives as a `reason` code beside the lines that *did* price,
        and the desk agent sees both halves at once. A `422` would give them one
        problem per round trip and lose the total, which is the number the
        customer is standing there waiting for.

        Line refusals sit on the line, in `lines[].refusals`, using the same
        vocabulary the placement refusal folds in per article — `item_unknown`,
        `item_not_published`, `variant_unknown`, `variant_not_active`,
        `channel_unavailable`, `unpriced`, `currency_mismatch`,
        `channel_not_trading`. Order-level refusals sit in the top-level
        `refusals`, and are the three families an agent can actually do something
        about: the fulfilment **shape** (`customer_required`,
        `address_required`, `address_not_applicable`), the **destination**
        (`address_not_deliverable`, `area_not_served`, `zone_suspended`, and a
        `currency_mismatch` whose `subject` is `delivery_fee`), and the
        **schedule** (`cut_off_passed`, `branch_closed`, `date_in_the_past`).

        **`quotable` is the field a screen branches on.** It is false whenever
        *any* refusal exists, line-level or order-level, because the placement
        operation refuses a whole order when any reason is collected. The totals
        are still computed — over the refusal-free lines only — because "drop the
        soup and it comes to eleven dollars" is the agent's next sentence. They
        are not what would be charged, which is exactly what `quotable` says.

        ## What is deliberately not asked

        **Eligibility.** A quote for a caller whose account was opened thirty
        seconds ago must not report `account_not_ready`: the desk bypasses that
        gate when it places, so raising it here would refuse a sale that is about
        to go through perfectly. **The corporate agreement gate** likewise — it
        is a no-op on the counter's `pos` channel, and running it would quote a
        wholesale minimum against a sandwich.

        ## Prices are the same prices

        Every line is probed through the kitchen's `desk` channel, on the
        requested day, in that channel's own tariff currency, by the same code
        path the placement uses — and the line total is rounded once, in minor
        units. Two implementations of "what does this cost" is how a quote and a
        receipt come to differ by a piastre while the customer is holding both.

        A kitchen with no `desk` channel, or one that is not trading, is
        **`409 resource.conflict`** rather than twelve `channel_unavailable`
        refusals: "the counter was never opened" is one sentence, and "none of
        this food is available" is the wrong twelve.

        Duplicate lines naming the same article and pack are **merged** before
        pricing, exactly as the placement merges them, so the quantity quoted is
        the quantity charged.

        Requires `order.view_organisation`. Selling is the sibling operation and
        needs `order.create_on_behalf_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuoteOrderDeskRequest'
      responses:
        '200':
          description: The priced basket, its totals, and every refusal standing between it and a sale.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskQuoteEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/orders:
    post:
      operationId: placeOrderDeskOrder
      tags:
        - Order desk
      summary: Sell something across the counter
      description: |
        The other side of `POST /orders`. That one is a customer converting their
        own basket; this is a member of staff placing an order **for** somebody —
        a walk-in at the counter, a caller on the telephone, a regular collecting
        at six — and the whole of what makes it different is that whoever pressed
        the button is not whoever is going to eat.

        ## `order.create_on_behalf_organisation`

        Its own code, and not `order.manage_organisation`. Confirming, fulfilling
        and cancelling change what a customer is already owed; **placing** decides
        they are owed anything at all, and it is the one order action with nobody
        on the other side to have agreed to it. It also carries a real bypass:
        naming the agent skips the activation checklist entirely, because the
        member of staff standing in front of the customer is the verification the
        checklist was asking for. A provisional account with no verified email
        can be sold to here and cannot check out for itself.

        ## `Idempotency-Key` is **mandatory**

        Absent or blank is `400 request.invalid` with `details.header`. It is
        required here and optional on `POST /orders` because the two have
        different fallbacks, not different standards: a customer's checkout
        converts a cart, and a double-tapped checkout finds that cart already
        converted and refuses. **A desk sale has no cart, and may have no
        customer at all.** Nothing in the schema would notice a second identical
        counter sale — two orders, two sets of stock movements at confirm, and a
        till out by one lunch with no row saying which one was the mistake.

        A replay returns the original envelope, the original `201` and
        `Idempotency-Replayed: true`. A key reused against a **different** body
        is `409 request.idempotency_key_reused`; the fix is a new key. The key is
        scoped to the **member of staff**, never to the customer: the customer
        may be an account opened during the call, or on a counter sale may not
        exist, and neither is a durable subject for a replay guard.

        ## The shape of a sale

        `fulfilment_type` decides which of the other fields are required,
        forbidden or merely allowed, and the rule is enforced as a **refusal**
        rather than as validation so that an order wrong in two ways says both:

        | | customer | address |
        |---|---|---|
        | `delivery` | required | required |
        | `pickup` | required | **forbidden** |
        | `counter` | optional | **forbidden** |

        A pickup is still cooked to a slot, so `delivery_window_code`,
        `requested_delivery_date` and the branch cut-off all apply to one. A
        counter sale is handed over now and has no day to be late for.

        ## One operation, two writes, and `payment` is what chooses

        A `delivery` or a `pickup` is **placed** and comes back `status: placed`:
        the kitchen has not confirmed it yet, nobody has been handed anything,
        and the money arrives later through `POST /catalogue/orders/{order}/
        payments`.

        A `counter` sale is **completed**. In one transaction it is placed,
        confirmed — which is where the ingredients come off the shelf —
        receipted for exactly its own total, and fulfilled; it comes back
        `status: fulfilled` with `confirmed_at` and `fulfilled_at` stamped and
        `lock_version: 2`, because by the time this response is rendered the
        customer has walked away with the food. There is nothing left for the
        desk to do to it and no confirm call to follow.

        The `payment` block is what decides which, and it is not a choice: it is
        **required** on `counter` and **prohibited** on the other two, `422
        validation.failed` either way round. A branch chosen by whether an
        optional object happened to be present would be a branch nobody could
        predict from the request, and these two differ by three status
        transitions and a row in the day's takings.

        **Insufficient stock does not refuse the sale.** The food has been cooked
        and handed over, and refusing the order would not put the ingredients
        back — so the sale completes, fulfilled, and the shortfall is recorded as
        a consumption exception for somebody to reconcile. The same is true of an
        article with no recipe, which is why a kitchen that has not finished
        setting up its inventory can still sell lunch.

        If any step fails, **all four roll back**: no order, no lines, no stock
        movements, no receipt and no audit rows. A half-made counter sale sitting
        in the book at a status nobody chose would be worse than no sale at all.

        ## No amounts in the body, ever

        Every line is repriced at placement against the tariff standing at that
        moment; the quote operation's numbers were advisory and were never
        stored. A body that could state a total would be a client quoting the
        server its own prices, at a counter, with nobody watching. The
        consequence is real and correct: a price that moved between the quote and
        the tap is charged at the new figure, and an article withdrawn in between
        refuses the placement outright.

        Refusals are **`409 order.placement_refused`** carrying `details.reasons`
        — every blocker at once, from the same vocabulary `POST /orders` uses
        plus the three shape reasons above. Duplicate lines naming the same
        article and pack are merged into one line before anything is priced,
        because `order_lines` holds one row per article and three taps of the
        same coffee is a quantity, not three lines.

        No `If-Match`: nothing existing is written, so there is no validator a
        screen could have been holding. No `Location` header: nothing in this API
        emits one.

        The order comes back in the **kitchen** shape — the audience is a member
        of staff reading their own book — so it carries `lock_version` for the
        confirm that follows and `placed_on_behalf_by` naming who took the order.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IdempotencyKeyRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaceOrderDeskRequest'
      responses:
        '201':
          description: |
            The order as the kitchen sees it, with every line at the price it was
            placed at. `status` is `placed` on a delivery or a pickup and
            `fulfilled` on a counter sale, which is the whole observable
            difference between the two writes this operation performs. A replay
            of a request this key already answered returns this same body and
            this same status, with `Idempotency-Replayed: true`; nothing ran a
            second time.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KitchenOrderEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/customers:
    get:
      operationId: searchOrderDeskCustomers
      tags:
        - Order desk
      summary: Who is this on the telephone?
      description: |
        The first thing a desk agent does on every call that is not a walk-in,
        and **the door every customer identifier the desk uses comes through**.
        The quote, the sale and the address operation all take a
        `customer_account_id`; this is where one legitimately comes from, which
        is what lets those operations trust an identifier without an
        organisation filter of their own.

        ## `order.view_customer_contact_organisation`, not the creation code

        The permission here is the **reading** one, and the split is the point of
        having two codes. Opening an account writes down somebody who told you
        their details; searching reads back the name and telephone number of
        people who never spoke to you, one query at a time — which is exactly the
        disclosure the contact code exists for, and the same pair the queue row
        carries behind the same code. A search revealing more than the queue
        would make the queue's gating pointless.

        ## Scope: two arms, and the application is the whole control

        An account is returned only when **either** this organisation holds at
        least one order against it, **or** its origin is `staff` and it was
        created by a user holding an **active** membership of this organisation.

        There is no third arm and no database predicate doing this work. A
        `customer_accounts` row carries no organisation — the same person orders
        from four kitchens with one account — and a staff-provisioned account has
        neither a user nor an organisation, which is the shape the row-level
        security policy admits to every kitchen session. The scoping rule, the
        permission code, and the fact that an account identifier is otherwise
        unobtainable are the entire isolation story, and the schema migration
        that made such rows possible states that rather than implying otherwise.

        A consequence worth naming: a member of staff who works at two kitchens
        holds an active membership of both, so a caller they wrote down at one is
        visible at the other. Nothing records which kitchen somebody was sitting
        in when they typed a name, and the alternative — dropping the second arm
        — would lose a caller between the tap that creates them and the tap that
        sells to them.

        ## Matching

        `query` matches the **display name** as a case-insensitive substring, and
        the **telephone number** exactly, after normalisation.

        The number is normalised by the same code that stored it, so
        `+961 70 123-456` finds `+96170123456`. A query that is not already in
        E.164 — a name, or a local number with no country code — simply does not
        engage the phone arm: the platform does not guess a country code, and a
        raw local number compared against a stored international one would match
        nothing while looking exactly like "we have no such customer".

        Minimum three characters (shorter is `422`), at most twenty rows, and no
        cursor: the agent has somebody on the telephone, and the answer to twenty
        matches is a longer query rather than a second page.

        No `branch_id`. A customer is not a branch's, and offering the parameter
        would invite a client to believe it narrowed something.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: query
          in: query
          required: true
          description: A name fragment, or a telephone number in E.164.
          schema:
            type: string
            minLength: 3
            maxLength: 60
          examples:
            name:
              value: hadd
            phone:
              value: '+96170123456'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The matching customers of this kitchen, capped, alphabetically by name.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskCustomersEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createOrderDeskCustomer
      tags:
        - Order desk
      summary: Write down a cold caller
      description: |
        Somebody rings a kitchen having never used the platform, and there is no
        account for the order to be placed against. **This is the one path where
        a member of staff creates a record about a member of the public who is
        not at a keyboard**, which is why it has its own code —
        `customer.create_on_behalf_organisation` — rather than riding on the one
        that sells them lunch. An account outlives the sale, and a kitchen may
        reasonably want an agent who can take an order for somebody already on
        file without being able to add people to the file.

        ## What it writes

        A consumer account with **no login**, `origin: staff`, provisional and
        with no expiry, plus the caller's telephone number as an account-owned
        contact point recorded as `staff` and **unverified**.

        Unverified because nobody proved anything: the desk heard a number over a
        telephone, which is provenance rather than proof. The platform's
        uniqueness rule applies only to *verified* contacts, which is what makes
        a duplicate legal — see below — and marking a heard number verified would
        both lie and let a desk claim a number away from whoever really holds it.

        The account is durable and claimable: the day this person registers, the
        guest-conversion path attaches a user to a row that already holds their
        number, their address and their order history. It is deliberately **not**
        a guest account, which the expiry sweeps would delete somewhere between
        two orders.

        The account is provisional and stays provisional. A cold caller satisfies
        none of the activation checklist, and the desk sells to them anyway
        because the sale operation names the agent and skips it — the member of
        staff in front of the customer is the verification the checklist was
        asking for.

        ## `Idempotency-Key` is **mandatory**

        Absent or blank is `400 request.invalid` with `details.header`. Nothing in
        the schema would notice a second identical customer: the one-account-per-
        person rule is enforced on the **login**, and a staff-provisioned account
        has none — so two taps are two perfectly legal people with no row
        anywhere saying which one was the mistake.

        ## Duplicates are surfaced, not refused

        **Search first** is the workflow. But two customers genuinely do share a
        telephone number — a household, a reception desk, an office floor — so a
        number already on file is not an error, and the account is created
        anyway. `possible_duplicates` then carries every **in-scope** account
        already holding that exact number, in the same shape the search returns,
        computed before the new row exists so a customer is never listed as a
        duplicate of themselves. The agent sees "we already have a Ramy on this
        number" while the customer is still on the line, which is the only moment
        merging is cheap.

        The list is scoped exactly as the search is. Answering "somebody,
        somewhere on the platform, has this number" would turn a duplicate
        warning into an oracle over every telephone number the platform holds.

        `phone` is refused (`422`, on the `phone` field) unless it is already in
        E.164. The platform does not infer a country code — guessing one would
        send somebody else's handset a passcode — so a desk agent asks for it the
        way they ask for the rest of the number.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IdempotencyKeyRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrderDeskCustomerRequest'
      responses:
        '201':
          description: |
            The customer as the desk sees them, and anybody already on that
            number. A replay of a request this key already answered returns the
            same body and the same status, with `Idempotency-Replayed: true`.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskCustomerEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/customers/{account}/addresses:
    post:
      operationId: addOrderDeskCustomerAddress
      tags:
        - Order desk
      summary: Where the food is going
      description: |
        The second half of writing down a cold caller, and a separate request
        from the first for a reason a desk agent would recognise: the name and
        the number come out in the first ten seconds of a call, and the address
        takes a minute and three corrections.

        ## This is the control the account shape leans on

        Staff-provisioned accounts have **no database-layer isolation** — they
        are ownerless rows, and the row-level-security policy admits every one of
        them to every kitchen session. This operation is where that matters most,
        because what it writes is a street somebody lives on.

        So the account is put through the **same scoping rule as the search** —
        an order with this kitchen, or provisioned by somebody with an active
        membership of it — and an identifier that fails it is `404`, not `403`.
        Confirming that an identifier exists but belongs to somebody else is
        itself the disclosure.

        ## The rules are the customer's own rules

        A delivery address in an area nobody serves is
        `422 address.area_not_served`, exactly as it is on the customer's own
        address book — told to the agent while the customer is still on the
        telephone, which is the only moment it is useful. The **first** address
        becomes the default whether or not it was asked for.

        `is_deliverable` on the response is recomputed on every read rather than
        frozen at save time, because coverage moves.

        ## What the body does not accept

        No `address_type`: the desk adds delivery addresses, and a billing
        address is an invoicing arrangement rather than something dictated over
        a telephone. No `contact_point_id`: on the customer's own form it names
        one of *their* contacts and only they could know which; here it would be
        an identifier this operation cannot prove belongs to this account. No
        `is_default`, because the first address is one already.

        **No `Idempotency-Key`**, matching the customer's own address operation.
        A double-tapped address is a visible duplicate on the screen the agent is
        already looking at; a double-tapped customer is a second person in the
        file nobody notices until the next call, which is why the operation
        beside this one requires a key and this one does not.

        Requires `customer.create_on_behalf_organisation` — the writing code,
        not the reading one.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: account
          in: path
          required: true
          description: The customer account, as returned by the desk's customer search or create.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddOrderDeskCustomerAddressRequest'
      responses:
        '201':
          description: The address was added, with whether anybody delivers to it today.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAddressEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/calendar:
    get:
      operationId: getOrderDeskCalendar
      tags:
        - Order desk
      summary: The shape of the week, counted three ways
      description: |
        **Not the queue with dates on it.** The queue is the open book in the
        order it has to be worked. This is how much is committed on each day and
        in each slot, so that somebody deciding whether to take one more delivery
        for Thursday can see Thursday.

        ## Three bases, and they are never added together

        Each day carries three counts, and they mean three different things:

        * `order` — real rows in the order book. Somebody agreed to something.
        * `scheduled` — subscription delivery days the generator has claimed and
          not yet placed an order for. A commitment with no order behind it yet.
        * `projected` — days computed from an active subscription's weekday
          pattern and remaining balance. **A forecast**; nothing asserts them.

        **Never sum them, and note that this response gives you no total to be
        tempted by.** The three overlap: a projected day becomes a claim, a claim
        becomes an order, and the forecast is optimistic about days a claim
        already exists for. A kitchen ordering ingredients against a total would
        over-buy, quietly, at the end of a month in which nothing looked wrong.
        Render three numbers.

        **`projected` is a forecast and degrades as one.** The projection behind
        it reads weekday patterns and remaining balances and does **not** consult
        the generator's own cursor, so after an outage — while the hourly tick
        catches up one delivery per hour — it keeps showing the pattern the
        customer bought rather than the backlog the kitchen is actually working.
        It is the right number for planning and the wrong number for promising,
        and a screen should label it.

        ## Which orders are counted

        Every non-cancelled order whose requested delivery date falls in the
        window, **whatever its status**. A fulfilled order still left the kitchen
        that day — that is what fulfilled means — and a calendar that dropped it
        would empty yesterday out as the day was worked, which is the opposite of
        a production record. Cancelled orders count nowhere.

        Orders with **no requested delivery date are excluded**, which is not the
        queue's rule. The queue shows a dateless order today, because a queue is
        a list of work in hand. A calendar is a grid of days, and an order nobody
        named a day for has no square to sit in.

        ## Windows

        `windows` splits a day's three counts by delivery slot. Work with no
        named slot — legitimate on both books — is an entry whose `code` is
        **`null`**, not a placeholder word: a code is a kitchen's own vocabulary,
        and any word this operation invented would be one a kitchen could also
        have typed. Named slots come first, alphabetically; the null bucket is
        last.

        Only slots that carry something appear on a day, and **every day of the
        window appears** even when all three of its counts are zero.

        ## Permissions and bounds

        Requires **both** `order.view_organisation` and
        `subscription.view_organisation`, because it is the one order-desk
        operation that unions two books the platform deliberately keeps apart.
        Holding either alone is a `403`.

        `from` and `to` are both required and the window is capped at **sixty
        days counted inclusively**: sixty answers, sixty-one is a `422` naming
        `to`. The cap exists because the projection behind the third basis costs
        one query per active subscription per request.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: from
          in: query
          required: true
          description: First day of the window, inclusive.
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          description: |
            Last day of the window, inclusive. Must not be before `from`, and
            must not put more than sixty days in the window.
          schema:
            type: string
            format: date
        - name: branch_id
          in: query
          required: false
          description: |
            Narrow every basis to one production site. Omitted, the calendar is
            organisation-wide. Unlike on the queue it names no clock: a
            calendar's days are the dates customers asked for rather than
            instants.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every day of the window, each with its three counts and their split by slot.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskCalendarEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/requirements:
    get:
      operationId: getOrderDeskRequirements
      tags:
        - Order desk
      summary: What one branch must buy to cook a window
      description: |
        The buy list. Every ingredient a window's demand needs, how much of it
        one branch already holds, and how short that leaves the kitchen.

        ## Three demand populations, one list

        The window's demand comes from three places, counted separately and then
        merged per shelf:

        * **Real order lines** on every non-cancelled order whose requested
          delivery date falls in the window — meals and resold products alike,
          whatever the order's status. A fulfilled order still ate its
          ingredients that day.
        * **Meal choices** standing against subscription days that are not orders
          yet. Somebody has said what fills the slot; generation has not written
          the line.
        * **Plan-menu days**: for those same subscription days, every slot the
          plan's menu names that no choice has claimed. This is what generation
          will write when it reaches the day, computed early.

        They cannot double-count: the subscription populations only ever cover
        days no order exists for, and within a day a slot with a choice belongs
        to the second population and a slot without to the third.

        Quantities are summed per dish **before** the recipe is exploded, so
        selling one dish sixty times in a window produces one explosion and one
        rounding step rather than sixty of each.

        ## `branch_id` is required here, and optional everywhere else on the desk

        Every other order-desk operation takes `branch_id` as a narrowing an
        organisation-wide agent may omit. This one refuses without it, because
        half the response is `available` and there is no honest organisation-wide
        value for that: summing three branches' shelves would tell a buyer they
        have flour while the kitchen that needs it has none.

        **The branch narrows availability, not demand.** A stock item is an
        organisation-level row while a stock level is a branch's, and both
        `orders` and `subscriptions` carry nullable branch columns that any
        narrowing rule would have had to invent a policy for. So this answers
        "everything this kitchen owes, measured against *this* site's shelf". For
        a single-site organisation that is exact. For a multi-site one it
        over-states that site's share and is never short.

        A stock item with **no level row at the branch** reads as `0`, not as
        unknown: a level row appears with the first movement, so its absence is a
        shelf holding none of that thing.

        ## `not_computable` is never folded into the numbers

        A dish with no published recipe, a plan whose menu was never written, a
        recipe line measured in a unit that will not convert into the shelf's —
        none of these produce a quantity, and **none of them produce a zero
        either**. "Buy nothing for that" and "we could not work out what to buy
        for that" are opposite statements. They are reported apart, as a count of
        days and a breakdown by reason, and a screen should show both.

        `insufficient_stock` is not a reason here: on this surface it is the
        output, spelled `short`.

        ## Bounds

        `from` and `to` are both required and the window is capped at
        **thirty-one days counted inclusively** — shorter than the calendar's
        sixty on purpose. A calendar is a view of commitments; a buy list beyond
        a month is speculation, and a number that precise about a month that
        vague invites somebody to act on it. Thirty-one so a whole calendar month
        always fits in one request.

        Requires `inventory.view_organisation`. Not the costs code: there is no
        money anywhere in this response.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: from
          in: query
          required: true
          description: First day of the window, inclusive.
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          description: |
            Last day of the window, inclusive. Must not be before `from`, and
            must not put more than thirty-one days in the window.
          schema:
            type: string
            format: date
        - name: branch_id
          in: query
          required: true
          description: |
            The shelf to compare against. **Required**, unlike on every other
            order-desk operation — this is the question rather than a filter over
            it, because there is no organisation-wide "available".
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The window's requirements against one branch, and the part of it nobody could compute.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskRequirementsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/requirements/shortfall-count:
    get:
      operationId: getOrderDeskShortfallCount
      tags:
        - Order desk
      summary: How many things the next week is short of
      description: |
        The badge behind the buy list: how many ingredients the next seven days
        need more of than the branch holds.

        ## `branch_id` is optional here and required on the list, deliberately

        The list refuses without a branch because there is no honest
        organisation-wide `available`. That refusal is right for a screen
        somebody navigated to and wrong for a badge nobody asked for — a hub tile
        returning `422` because the manager holds an organisation-wide membership
        would be an error where there is no mistake.

        So an absent branch is **answered**, with `shortfall_count: null` and
        `branch_id: null` beside it. **Null is not zero and must not render as
        one.** Zero shortfalls is good news; not knowing is not news at all. Show
        the number when there is one and show nothing when there is not — the
        same em-dash rule every other unknown on this platform follows.

        ## Seven days, fixed

        A badge takes no window, because its job is to be the same number every
        time anybody glances at it and a caller-chosen window would let two
        people looking at one hub disagree about how bad things are. Seven days
        from today counted inclusively — long enough that a delivery can still be
        arranged, short enough that the demand behind it is mostly real orders.
        "Today" is the application clock rather than the branch's.

        Counts rows whose shortfall is strictly above zero, and nothing else.
        Not the holes: a thing nobody could compute is a different problem with a
        different remedy, and folding it in would send a buyer looking for
        something to order.

        Requires `inventory.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: branch_id
          in: query
          required: false
          description: |
            The shelf to judge against. Omitted, the answer is `null` — see the
            description. Optional here and required on the list beside it.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The count, or null when no branch was named.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskShortfallCountEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/drivers:
    get:
      operationId: listOrderDeskDrivers
      tags:
        - Order desk
      summary: Who this kitchen can send out with its food
      description: |
        The picker behind `POST /delivery/jobs/{job}/assign`. That operation
        takes a `driver_user_id` and refuses anybody who is not an **active**
        member of the organisation; this is where one legitimately comes from,
        and the two apply the same predicate to the same table so that a list
        cannot offer somebody the assign call would then refuse.

        **Every active member is listed, and that is deliberate.** There is no
        driver role on this platform: a driver is staff of the kitchen whose jobs
        these are, and the driver's own surface narrows by who holds the run
        rather than by a permission code. A role here would mean a kitchen could
        not hand tonight's late delivery to the chef who offered to drop it off
        on the way home. So this answers a question of *fact* — who works here,
        right now — and **the picker is where a kitchen's operating judgement
        lives**. A dispatcher looking at a list of colleagues knows which of them
        drives; the server does not and has never been told.

        `active` and nothing else: an invitation nobody accepted, a suspension
        and an ended employment are all membership rows, and none of them is
        somebody a kitchen can send out with its food.

        `display_name` may be **null** — a member whose profile has not been
        completed is still a member, and refusing to list them would leave a
        dispatcher unable to assign a job to somebody demonstrably on the
        payroll. Nameless members sort last. Render the platform's em dash rather
        than inventing a placeholder.

        Requires `order.manage_organisation`, the same code the assign operation
        carries and the same audience. A hundred rows, alphabetically, with no
        cursor: a kitchen with more than a hundred active members has an
        organisation chart rather than a rota, and a picker is not the surface
        for browsing one.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: This organisation's active members, named where a name exists.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskDriversEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/order-desk/cash-report:
    get:
      operationId: getOrderDeskCashReport
      tags:
        - Order desk
      summary: Who took what, on one day
      description: |
        **The till-shift mitigation, and it is named as one.** The desk takes
        cash. Every note that crossed the counter is a receipt naming who says it
        arrived and when — and this platform has **no shift table**: nothing
        opens a drawer with a float, nothing closes it against a count, nothing
        reconciles the difference. That gap was accepted knowingly when receipts
        landed, on the condition that the money at least be *attributable*: a
        manager must be able to ask "what did each agent take yesterday?" and get
        an answer they can stand a cash box next to.

        This is that answer and deliberately not more. **It cannot tell anybody
        whether the cash box balances**, because nothing on this platform has
        ever been told what was in it. A real drawer reconciliation is a later
        table — an opening float, a closing count, a named variance — and when it
        lands, this operation keeps reading the same ledger rather than being
        replaced by it.

        ## The day boundary is UTC, and `meta.timezone` says so

        `confirmed_at` is an instant; a day is not. Turning one into the other
        needs a clock this operation does not have — a receipt carries no branch,
        and the order behind it may carry none either, because an
        organisation-wide delivery zone leaves `branch_id` null. There is no
        timezone true of every receipt in the answer, and using each order's
        branch where it had one would measure the two halves of a report on two
        different clocks.

        So the boundary is UTC, matching the queue's organisation-wide convention
        exactly: unnamed, the only zone that is not a guess. A kitchen whose
        evening runs past UTC midnight sees that evening split across two
        reports — a visible, explicable fact rather than a silent
        misattribution.

        ## Grouped by three things, and the third is what makes the sums legal

        Rows are `(confirmed_by, method, currency_code)`. The currency is part of
        the **key**, never an attribute of the group: summing inside a group is
        legal because every receipt in it is the same unit of the same currency,
        and a group that spanned two currencies would carry a number in no
        currency at all. A kitchen taking dollars and dirhams therefore sees two
        rows for one agent's cash, and two totals, which is the truth.

        `totals` is the same arithmetic per `(method, currency_code)` across the
        agents, derived from the rows so the two can never disagree. There is
        deliberately **no grand total**: it would have to add currencies.

        ## `branch_id` narrows through the order

        A receipt has no branch — money is taken by a person at a till, and which
        site cooked the food is a fact about the order — so the filter reaches the
        order. One consequence, stated because it is the same trap the queue
        documents: an order with a null `branch_id` is **excluded** by a branch
        filter. Right for "what did the Marina site take?", wrong as a default,
        which is why the default is no narrowing at all.

        Requires `order.manage_organisation` — a manager's reconciliation of
        their agents' takings, not an agent's view of their own work.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: date
          in: query
          required: true
          description: |
            The day to report, read on the UTC clock. Required, and one day
            rather than a range: reconciling a till is a daily act, and a report
            summing Monday and Tuesday is one nobody can stand a cash box next
            to.
          schema:
            type: string
            format: date
        - name: branch_id
          in: query
          required: false
          description: |
            Narrow to the site that cooked it, through the order. Omitted, the
            report is organisation-wide. Orders with no branch are excluded when
            this is set — see the description.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: One day's takings by agent, method and currency, with the per-method totals.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderDeskCashReportEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/sessions:
    post:
      operationId: startGuestSession
      tags:
        - Guest
      summary: Open a guest session
      security: []
      description: |
        **Anonymous.** Opens a guest customer account and mints the bearer token
        every other guest operation is reached with.

        **This is the only response in the API that carries a plaintext guest
        token, and it carries it once.** The row stores a digest;
        `GET /guest/session` has no `token` field and nothing anywhere can
        re-derive one. A client that loses it has lost the basket, which is the
        correct outcome — a "recover my guest session" surface could only
        authenticate against the credential that was lost.

        Two windows come back and they are not the same. `expires_at` is how
        long this token is accepted; `account_expires_at` is how long the guest
        identity behind it survives. A session that outlived its account would
        resolve to a dead party; an account that expired with its session would
        break "finish the checkout you started this morning".

        The caller's IP and User-Agent are hashed server-side into the session's
        abuse fingerprints. Neither is accepted in the body — a caller who can
        choose their own fingerprint does not have one — and the raw values are
        never stored.

        `Idempotency-Key` is deliberately **not** honoured: a replay would have
        to return the original token, which is not stored, and any other answer
        would be a second credential wearing the first one's key. Two guest
        sessions cost two rows and no obligation to anybody.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
        - $ref: '#/components/parameters/XClientPlatform'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartGuestSessionRequest'
      responses:
        '201':
          description: The guest session was opened and the token issued.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/StartedGuestSession'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/session:
    get:
      operationId: showGuestSession
      tags:
        - Guest
      summary: Read the current guest session
      security:
        - guestToken: []
      description: |
        The session's own state: its grade, whether a contact point has been
        proven, when the token lapses, and the guest account behind it. Called
        on resume, when a token has been in local storage since yesterday and
        the only honest way to learn whether it still works is to use it.

        **Never the token.** The caller already holds it — that is how they
        reached here — so echoing it would put a credential into a response a
        client may cache or log, and buy nothing.

        A token that is unknown, expired, revoked or under-graded answers
        `401 guest.session_invalid`, with nothing distinguishing the four. That
        is deliberately not a `200` carrying `live: false`: a body describing a
        session the caller may not use would make every consumer branch twice,
        and the second branch is the one people forget.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The session is live.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuestSessionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/contacts:
    post:
      operationId: requestGuestContactVerification
      tags:
        - Guest
      summary: Send a guest a passcode
      security:
        - guestToken: []
      description: |
        The guest supplies somewhere we can reach them and a passcode goes to
        it. The first half of the only act that raises a session's grade:
        `place_order` exists because an order is a promise that somebody will be
        told when it is late, and a destination nobody proved is a promise made
        to a typo.

        **`channel` and `delivery_channel` are not the same question.** The
        first says what kind of destination this is — an address or a number —
        and is a property of the contact point. The second says how the passcode
        travels, and is a property of the message: one number is reachable by
        SMS and by WhatsApp, and which is used may differ between two codes sent
        to the same number. `delivery_channel` is optional because the server
        picks better: it knows which drivers are real in this deployment and
        falls back when one is not.

        `202`, not `201`. Nothing durable is created that the caller may then
        read — a challenge is a message in flight with no `GET` of its own —
        and the delivery is a queued job, so a `201` would be claiming a message
        was handed over by a driver that has not run yet.

        The response is candid, unlike the erasure surface: the caller holds a
        live token and typed the destination themselves, so the masked echo, the
        attempts remaining and the resend countdown tell them nothing they did
        not bring with them.

        Verifying a guest's contact deliberately does **not** mark the contact
        point verified platform-wide. The proof is recorded on the session —
        this browser, holding this token, proved this destination — because a
        guest claiming the one verified slot for an address would let anybody
        deny it to its real owner by ordering a salad.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestGuestContactVerificationRequest'
      responses:
        '202':
          description: A passcode is on its way.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuestOtpChallengeEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/contacts/verify:
    post:
      operationId: confirmGuestContactVerification
      tags:
        - Guest
      summary: Submit a guest passcode
      security:
        - guestToken: []
      description: |
        The passcode comes back and, if it holds, the session is promoted to
        `place_order`. The one act that raises a guest's grade — and
        `guest_sessions_grade_proof_check` makes that structural, so a bug that
        skipped this endpoint could not write the higher grade at all.

        **The challenge is resolved by account, not by identifier.** The lookup
        is scoped to this session's own customer account and to the `guest_order`
        purpose; an identifier that never existed, one belonging to another
        guest, and one issued for a deletion all answer `404`. "That challenge
        exists but is not yours" only ever helps somebody it should not help.

        The failure vocabulary is the one place this surface maps a domain
        result onto HTTP. `422 otp.invalid` carries `details.attempts_remaining`,
        because a screen that cannot show how many tries are left has to guess
        when to stop asking. `422 otp.expired` covers both a timed-out challenge
        and a superseded one — the remedy is identical and the distinction would
        say how many are in flight. `429 otp.attempts_exceeded` is a limit
        reached rather than a value rejected. `429 otp.locked` carries
        `details.locked_until`, because a fresh challenge will not help either
        and a client offering "resend" would be lying.

        On success the response is the same shape `GET /guest/session` serves: a
        client that has just been promoted needs exactly what a client resuming
        needs.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmGuestContactVerificationRequest'
      responses:
        '200':
          description: The passcode held and the session was promoted.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuestSessionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/orders:
    post:
      operationId: placeGuestOrder
      tags:
        - Guest
      summary: Place a guest order
      security:
        - guestToken: []
      description: |
        A guest turns a basket into an obligation. **The same body, the same
        placement service and the same refusals as the authenticated
        `POST /orders`** — what differs is only which credential got the caller
        here.

        Requires a session of grade `place_order`; a session that has not proven
        a contact point is refused with the same indistinguishable `401` an
        unknown token gets, because a checkout answering "your session is real
        but not verified" would tell a token-guesser that a guess had landed.

        **The cart and the address must belong to this session's account.** A
        cart that does not exist and a cart belonging to another guest both
        answer `404`; the difference is only ever useful to somebody walking
        identifiers.

        **Nothing about money is accepted and nothing is trusted from the
        cart.** Every line is repriced at placement against the tariff standing
        at that moment, so a line acceptable in the basket can be refused at
        checkout — the article was retired, the channel stopped offering it, the
        last confirmed price expired. Those are things that really happened, and
        `409 order.placement_refused` names every one of them at once in
        `details.reasons` rather than one per attempt.

        `Idempotency-Key` is honoured twice over, and neither half subsumes the
        other. The middleware replays the *original envelope* — status, body and
        all — which a service cannot do, because by the time it is asked the
        response is gone; the placement service guarantees the *placement* runs
        once, which the middleware cannot promise for a checkout that arrives
        from a queue. A replay repeats the original response exactly and
        `meta.replayed: true`; a fresh placement answers `201`. A client shown
        `201` for an order it has already been told about will count it twice.
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: |
            A client-chosen replay key, scoped to this endpoint and this guest
            account. Replaying it with the same body returns the original
            envelope with `Idempotency-Replayed: true`; replaying it with a
            different body answers `409 request.idempotency_key_reused`.
          schema:
            type: string
            maxLength: 255
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaceGuestOrderRequest'
      responses:
        '201':
          description: |
            The order was placed. A replay of a request this
            `Idempotency-Key` already answered returns this same body and this
            same status, with `Idempotency-Replayed: true` to say which it was.
          headers:
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuestOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/orders/{order}:
    get:
      operationId: showGuestOrder
      tags:
        - Guest
      summary: Read a guest order
      security:
        - guestToken: []
      description: |
        "Where is my food?" for somebody who never made an account. Scoped to
        the session's own customer account: an identifier naming nobody's order,
        another guest's order and a registered customer's order all answer
        `404`.

        **A signed-URL alternative is deferred, deliberately.** The obvious
        pattern for an order confirmation email is a signed link, and it is not
        built here because it would be a *second* credential for the same data
        with a different lifetime, a different revocation story and a different
        leak surface: a signed URL survives in mail archives, forwarded threads
        and referrer headers long after the session it was minted beside has
        lapsed, and revoking the session would not revoke it. Token-auth reading
        is sufficient for the journey this phase serves — the guest is in the
        browser that placed the order, holding the token that placed it, and the
        token outlives the checkout by days. When customer messaging arrives,
        the right shape is a short-lived single-order link issued by that message
        and expiring with it, not a general-purpose read credential invented in
        advance of the surface that needs it.

        Line names carry **one** server-chosen language from `Accept-Language`,
        never both columns, and they are the frozen snapshot taken at placement
        rather than the catalogue as it stands now. A person shown "contains
        sesame" must still be shown it after the kitchen reformulates.
      parameters:
        - name: order
          in: path
          required: true
          description: The order's identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The order.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuestOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/convert:
    post:
      operationId: convertGuestAccount
      tags:
        - Guest
      summary: Turn a guest into a registered customer
      security:
        - guestToken: []
      description: |
        "Keep my basket, my addresses and my order history — I want an account
        now." Registration and conversion as one act, because they are one act.

        **The body is the registration body**, identical to `POST /auth/register`,
        and it is validated by the same action. Restating those rules here would
        create a second authority that disagrees with the first the day either
        changes — silently, because whichever ran first would win.

        **Both halves run in one transaction, and the failure that prevents is
        not a tidy one.** A registered user whose guest account never converted
        has two identities with no surface that can join them, because
        conversion is the only thing that ever could and it has been consumed.
        A guest account pointed at a rolled-back `user_id` is a foreign key into
        nothing. Neither is repairable: one person holds at most one `b2c`
        account by partial unique index, so nobody can simply make the missing
        half without first deciding whose addresses win and what happens to
        orders on both sides.

        `origin` stays `guest` after conversion and that is the interesting
        part. The account becomes `b2c` — it has a user now, which is the
        definition — but origin is history, and analytics asking "how many
        customers arrived through the guest door" would otherwise answer zero
        forever.

        **The guest token is revoked by this call** and stops working with this
        response; `meta.guest_token_revoked` says so, so a client does not
        discover it as a `401` on its next request. No session is issued here —
        minting a credential is authentication's job — so the client follows
        with `POST /auth/login`.

        `409 resource.conflict` with `details.reason:
        user_already_has_consumer_account` is the refusal worth reading: somebody
        who ordered as a guest and then registered with an address they already
        had an account under is asking for two, and the merge is a product
        decision this phase declines to invent.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
        - $ref: '#/components/parameters/XClientPlatform'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: The account was created and the guest account converted.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuestConversionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/deletion-requests:
    post:
      operationId: requestGuestDeletion
      tags:
        - Guest
      summary: Ask for a guest's data to be erased
      security: []
      description: |
        **Anonymous, necessarily.** The premise of a guest is that there is no
        account to sign into, so the request arrives as a bare address from
        somebody we cannot identify. That gives this endpoint two properties it
        must hold at once, and they pull against each other: it must not become
        an enumeration oracle, and it must not become a deletion weapon.

        The resolution is to **always accept, never confirm, and require proof
        before acting**. The response is built entirely from the submitted value
        and from configuration — no field is read from the database — so it is
        byte-identical whether the address is one we hold or one nobody has ever
        typed. There is no challenge identifier and no "we sent you a code",
        because a nullable identifier is a boolean in disguise and the boolean is
        "this address is known to us".

        Always `202`. Every alternative status answers the question the `202`
        exists to refuse: a `404` says the address is unknown, a `201` says a
        challenge was created, and a `200` implies the work is done when nothing
        has been erased at all.

        The client's script is therefore identical in both cases: a code will
        arrive if the address is known, and any code entered against an address
        that is not fails exactly the way a wrong code does.

        `422 validation.failed` is reachable and discloses nothing — it is a
        statement about the submitted string being neither an address nor a
        number, derived entirely from what the caller typed.

        **The residual leak is timing**, and it is stated rather than papered
        over: a request for a known address also issues a challenge and queues a
        message. Closing it means a constant-time budget over the whole handler,
        which is not faked here with a sleep that the first slow query would
        defeat.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestGuestDeletionRequest'
      responses:
        '202':
          description: |
            Accepted. The same body whether or not the address is known to the
            platform.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/GuestDeletionAcknowledgement'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /guest/deletion-requests/verify:
    post:
      operationId: confirmGuestDeletion
      tags:
        - Guest
      summary: Prove and carry out a guest erasure
      security: []
      description: |
        **Anonymous.** The passcode comes back and, if it holds, the erasure
        happens.

        **Enumeration resistance survives the second step or it was never
        there.** An attacker told nothing by step one would simply submit six
        digits and read the refusal instead. So there is exactly one failure
        shape, and the list it covers is the point: a wrong code against an
        address we hold, any code against one we do not, an expired challenge,
        and a contact point locked out after repeated failures. The last is the
        least obvious and the most important — a distinct "locked out" is a
        perfect oracle, because only a real contact point can produce it. The
        lockout is enforced and simply not described.

        Hence `202` for a refusal as well as for a purge, and hence no error
        envelope anywhere on this path: a `422` for a bad code would be a
        different-shaped response, and a different shape is a different answer.

        **The success shape is allowed to differ.** `purged: true` and the
        counts appear only when the passcode verified, which means the caller
        demonstrably received a message at that destination and has proven they
        hold it. `purged` is always present, so the two refusal cases cannot be
        told apart by key set either.

        The address is submitted again rather than carried in a challenge
        identifier from step one, for the reason step one has none: a nullable
        identifier would be the disclosure the whole design refuses.

        `orders_retained` in the report is the honest half. A guest order is a
        commercial and tax record with a statutory retention of its own and is
        deliberately not deleted; saying so is what stops "purged" being read as
        "everything is gone".
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmGuestDeletionRequest'
      responses:
        '202':
          description: |
            Accepted. One indistinguishable shape for every refusal; the purge
            report only when the passcode held.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    $ref: '#/components/schemas/GuestDeletionOutcome'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /verification/email/challenges:
    post:
      operationId: issueEmailVerificationChallenge
      tags:
        - Verification
      summary: Send a passcode to my email address
      description: |
        Issues a six-digit passcode to the caller's own login address and hands
        the message to the queue.

        **Reachable before the email is verified**, and that is the point rather
        than a concession: a caller behind the verification gate could never ask
        for the code that lifts it.

        There is no body. The destination is read from the account rather than
        accepted from the request — an endpoint that took an address would send
        codes to addresses the caller does not hold, and would answer "does this
        address have an account here" one attempt at a time.

        `202`, not `201`: a challenge row exists, but the thing the caller is
        waiting for is an email that has not arrived yet.

        Refusals arrive as `otp.cooldown_active` (a resend inside the 45-second
        window), `otp.locked` (too many failures on this contact) or
        `otp.channel_unavailable` (no driver can reach the destination); the
        first two are `429` and the last is `422`.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '202':
          description: The passcode has been queued for delivery.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OtpChallengeEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /verification/email/verify:
    post:
      operationId: verifyEmailWithPasscode
      tags:
        - Verification
      summary: Verify my email address with a passcode
      description: |
        The inline-OTP twin of the signed link. The verification message carries
        both, and this is the half a native client uses: a link opens a browser,
        and the round trip back into the app loses the session often enough to
        be the most common place an onboarding is abandoned.

        Both halves settle **the same two facts** — the contact point is proven
        and `users.email_verified_at` is stamped — inside one transaction, so
        the mirror can never disagree with the account.

        **Reachable before the email is verified**, necessarily.

        No challenge identifier is asked for. There is at most one live
        contact-verification challenge per contact — a partial unique index
        makes that structural — so the server finds the only candidate itself
        rather than asking a client to echo something it would have had to parse
        out of an email.

        No live challenge is `otp.expired` rather than `404`: the challenge is a
        transient credential, the caller has done nothing wrong, and the remedy
        the code names — ask for a new one — is the right one.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitPasscodeRequest'
      responses:
        '200':
          description: The address is now verified.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactVerifiedEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /verification/challenges/{challenge}:
    get:
      operationId: showVerificationChallenge
      tags:
        - Verification
      summary: Read a challenge's state
      description: |
        How long is left, how many attempts remain, when the resend button
        unlocks. A client that has been away — backgrounded on a phone, reloaded
        in a tab — cannot reconstruct a countdown from its own clock without
        being wrong for exactly the users whose clocks are wrong.

        **Never the passcode, and never a hint of it.** Every field is drawn
        from columns that outlive the request; the plaintext exists between
        generation and delivery and nowhere else.

        `is_live` is carried beside `status` because expiry is a moment rather
        than an event: a row stays `pending` past its window until something
        writes the transition, so a client branching on the column alone would
        offer an attempt that cannot succeed.

        A challenge belonging to somebody else is indistinguishable from one
        that does not exist: both are `resource.not_found`.
      parameters:
        - name: challenge
          in: path
          required: true
          description: The challenge identifier, as issued.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The challenge.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationChallengeEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /verification/challenges/{challenge}/verify:
    post:
      operationId: verifyChallenge
      tags:
        - Verification
      summary: Submit a passcode against a challenge
      description: |
        Checks the code and, if it is right, marks the destination proven — and
        when that destination is the login mirror, settles
        `users.email_verified_at` in the same transaction.

        The attempt is counted **before** the code is compared, under a row
        lock, so two concurrent requests see attempts 1 and 2 rather than both
        seeing 0, and a process killed between counting and answering has spent
        the attempt rather than granting a free one.

        Every failure has its own code, because the remedies differ:
        `otp.invalid` (422, carries `details.attempts_remaining`) means read the
        message again; `otp.expired` (422) means ask for a new code;
        `otp.attempts_exceeded` (429) means the challenge is burned; `otp.locked`
        (429, carries `details.locked_until`) means the *contact* is locked and a
        fresh challenge will not help either.

        `contact.already_in_use` (409) is possible and only ever on a **verified**
        collision: two people may both claim an address, and refusing an
        unverified claim would let anybody deny an address to its real owner by
        typing it first.
      parameters:
        - name: challenge
          in: path
          required: true
          description: The challenge identifier, as issued.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitPasscodeRequest'
      responses:
        '200':
          description: The passcode was correct and the destination is proven.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactVerifiedEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /verification/challenges/{challenge}/resend:
    post:
      operationId: resendChallenge
      tags:
        - Verification
      summary: Send the same challenge again
      description: |
        The code is **regenerated** rather than resent, because the plaintext is
        not kept — only its digest is stored, which is the property that makes a
        database dump useless. A resend is therefore a new code on the same
        challenge: the attempt counter and the expiry window carry over, so
        nobody buys three fresh attempts by pressing the button.

        `channel` is optional and its absence means "the same way as last time"
        rather than "the default for this contact" — somebody who chose WhatsApp
        once should not be quietly moved back to SMS. A channel that cannot
        reach the destination is `otp.channel_unavailable` (422) with
        `details.available_channels` naming what would work.

        Inside the cooldown it is `otp.cooldown_active` (429) with
        `details.retry_after_seconds`; past the allowance the challenge simply
        stops resending.
      parameters:
        - name: challenge
          in: path
          required: true
          description: The challenge identifier, as issued.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResendChallengeRequest'
      responses:
        '202':
          description: A fresh passcode has been queued for delivery.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OtpChallengeEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /verification/step-up/challenges:
    post:
      operationId: issueStepUpChallenge
      tags:
        - Verification
      summary: Ask for a step-up passcode
      description: |
        A step-up passcode answers a different question from a password
        confirmation. "Does this person know the secret" and "is this person
        present right now" are not the same assurance, which is why the two
        methods are tracked separately, neither satisfies the other, and the
        passcode window is ten minutes against the password's three hours.

        **The destination is chosen by the server** — the primary proven contact
        where there is one, any proven contact otherwise. A caller that could
        name a destination for a challenge unlocking account closure would be
        handed the one decision the server exists to make.

        `purpose` is confined to those that grant a step-up. A code obtained for
        contact verification must never be replayable against a payment change,
        which is what the purpose column exists to prevent.

        Requires a verified email address (`auth.email_unverified` otherwise) —
        a caller who has proven nothing has nowhere to receive a step-up.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StepUpChallengeRequest'
      responses:
        '202':
          description: The step-up passcode has been queued for delivery.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OtpChallengeEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /verification/step-up/confirm:
    post:
      operationId: confirmStepUp
      tags:
        - Verification
      summary: Spend a step-up passcode
      description: |
        Stamps the `otp` step-up method that `step-up:otp` reads, bound to the
        **calling credential** — the personal access token, or the session when
        there is none — so confirming in a browser cannot unlock a sensitive
        action for a phone holding a stolen token.

        The purpose is checked **before** the code is compared, and the ordering
        is the security property: verifying first would spend one of three
        attempts on a challenge that was never going to be honoured, letting an
        attacker burn a victim's allowance by pointing this endpoint at their
        contact-verification challenge. A challenge whose purpose does not grant
        a step-up is `validation.failed` on `challenge_id`.

        Confirming never marks a contact verified. Presence and proof of a
        destination are separate facts, and folding them together would let a
        closure step-up quietly satisfy an activation requirement.

        `expires_in_seconds` is the guard's own timeout rather than a number
        restated by the client, which would guess wrong.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmStepUpRequest'
      responses:
        '200':
          description: The step-up is confirmed for this credential.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StepUpConfirmationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /customer-account:
    get:
      operationId: showCustomerAccount
      tags:
        - Customer account
      summary: My customer account and what it still needs
      description: |
        The account plus the activation checklist, in one call. `outstanding`
        and `checklist` come from the same evaluator that guards activation, so
        the screen and the gate cannot disagree about what is missing — and the
        checklist carries the satisfied items too, because a list showing only
        what is absent loses the sense of progress that makes people finish it.

        **`404` when there is none**, rather than an empty object or an invented
        provisional shape. A kitchen operator has an identity and no customer
        account, and manufacturing one for them would put an onboarding
        checklist in front of somebody who is not onboarding.

        `status` is read-only here and everywhere. Nothing on this surface can
        set an account `active`.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The customer account.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAccountEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: openCustomerAccount
      tags:
        - Customer account
      summary: Open, or resume, my customer account
      description: |
        **An unmet requirement is not an error here**, and that is the shape of
        the whole endpoint. Onboarding is interruptible by design: `provisional`
        is a real state in which an account exists, holds addresses and
        declarations, and cannot order. Refusing a caller who has not yet added
        an address would turn a checklist into a form that must be completed in
        one sitting, and would give the client nothing to render.

        **Idempotent, and the status code says which happened**: `201` the first
        time, `200` every time after. One `b2c` account per person is enforced
        by a partial unique index, and the lifecycle converges on the existing
        row rather than colliding with it.

        **Activation is attempted, never requested.** The caller does not ask
        for `active` and could not: the evaluator is consulted and the account
        moves only if nothing is outstanding. An account that were active
        without meeting the requirements is one that can order food with no
        deliverable address and no answer to the allergy question.

        A closed account is never reopened by posting here — closure is
        terminal, and the answer is a new account.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The account already existed; its current state is returned.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAccountEnvelope'
        '201':
          description: The account was opened.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAccountEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/contacts:
    get:
      operationId: listMyContacts
      tags:
        - Customer account
      summary: My contact points
      description: |
        The destinations this person has told us about, retired ones excluded.

        **The value is served in the clear**, unlike the masked destination on a
        challenge. A mask exists so a challenge can name where a code went
        without telling an unauthenticated caller whose address it is; here the
        audience is the account holder reading their own list, `/me` already
        serves them their own email, and a list of `a•••@example.com` rows is
        one on which nobody can tell which number is the old one.

        `value_hash` appears nowhere and never will — publishing it would turn
        the duplicate-detection index into an oracle for "does this address have
        an account here".

        Not cursor-paginated: a person has a handful of destinations, and
        `meta.count` is what the screen needs.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The caller's live contact points.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerContactsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: addMyContact
      tags:
        - Customer account
      summary: Add a contact point
      description: |
        The row is created **unverified**, always. Declaring a destination proves
        that somebody typed it into a form, not that they hold it.

        **Idempotent by channel and value**: adding the same number twice yields
        one contact, so a double tap cannot litter an account with duplicates.
        The response is `201` either way — distinguishing "created" from
        "already yours" would report on the database's history rather than on
        the caller's request, which succeeded in both cases.

        A phone number must already be in E.164. The platform does not infer a
        country code, because guessing one would send somebody else's handset a
        passcode that unlocks this account.

        A value already **proven** by somebody else is `contact.already_in_use`
        (409) — and only when proven, so nobody can deny an address to its real
        owner by typing it first.

        Requires an open customer account; without one the answer is
        `account.verification_required` (403) carrying `account_opened: false`.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCustomerContactRequest'
      responses:
        '201':
          description: The contact point was recorded.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerContactEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/contacts/{contact}:
    delete:
      operationId: removeMyContact
      tags:
        - Customer account
      summary: Withdraw a contact point
      description: |
        **Retired, not deleted.** The row survives with a tombstone, which is
        what lets the value be claimed again by somebody else — the partial
        unique index excludes retired rows — while keeping the record of who
        held it, which a closure or an abuse investigation needs. `verified_at`
        is kept for the same reason: the destination *was* proven.

        **The login mirror cannot be withdrawn**, and the refusal is
        `resource.conflict` rather than a validation failure: nothing about the
        request is malformed, and the same request against any other contact
        would succeed. It is where a password reset and a verification message
        are sent, and an account whose only route back in has been retired is
        one nobody can recover.

        A contact belonging to somebody else, or already retired, is
        `resource.not_found`.
      parameters:
        - name: contact
          in: path
          required: true
          description: The contact point identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '204':
          description: The contact point was retired.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/contacts/{contact}/primary:
    post:
      operationId: makeContactPrimary
      tags:
        - Customer account
      summary: Nominate the primary contact point
      description: |
        A POST sub-resource action rather than a `PATCH is_primary`: setting the
        flag means clearing it on the incumbent, and the partial unique index
        refuses a second primary, so a caller performing it as a field write
        would collide with itself between the two statements. The transaction
        that demotes and promotes together is the one this endpoint asks for.

        **Step-up protected.** Changing the primary destination is how an
        attacker holding a hijacked session redirects the codes that would
        otherwise stop them — the same class of act as revoking a device, and
        guarded the same way. A caller without a recent confirmation is
        `auth.step_up_required` (403) with `details.confirmation_endpoint`.

        The default is per channel, so promoting a phone leaves the primary
        email alone.
      parameters:
        - name: contact
          in: path
          required: true
          description: The contact point identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The contact point is now primary for its channel.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerContactEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/StepUpRequired'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/addresses:
    get:
      operationId: listMyAddresses
      tags:
        - Customer account
      summary: My address book
      description: |
        Both types in one list. A checkout wants the delivery addresses and an
        invoice wants the billing one; `address_type` separates them in one
        round trip, where two endpoints would be two calls for one screen.

        `is_deliverable` is computed per row rather than trusted from save time,
        because coverage moves: a zone is paused, a kitchen closes, and an
        address reachable in March is not in April. A book showing only what was
        true when the address was typed would let somebody choose one nothing
        can be sent to, and they would find out at checkout.

        **An empty list when no customer account has been opened**, rather than
        a refusal: a person who has not started onboarding genuinely has no
        addresses, and that is a truer answer than a 403.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The caller's addresses, defaults first within each type.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAddressesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: addMyAddress
      tags:
        - Customer account
      summary: Add an address
      description: |
        The served-area rule is checked at save time and refused as
        `address.area_not_served` (422) — the area is a perfectly valid
        reference, and what fails is the usefulness of the combination rather
        than its shape.

        Checking now rather than at checkout is deliberate: a person moving to
        an uncovered district is told while they are looking at the address
        form, which is the only moment they can do anything about it. It applies
        to delivery addresses only — an invoice goes wherever the customer says,
        and applying the rule to a billing address would refuse a perfectly good
        one in a city no kitchen reaches.

        The **first** address of a type becomes the default whether or not it
        was asked for: an address book with no default makes the checkout ask a
        question that has exactly one possible answer.

        Requires an open customer account; without one the answer is
        `account.verification_required` (403).
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCustomerAddressRequest'
      responses:
        '201':
          description: The address was added.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAddressEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/addresses/{address}:
    patch:
      operationId: updateMyAddress
      tags:
        - Customer account
      summary: Correct an address
      description: |
        A partial update rather than a replace, because that is what the act is:
        somebody adds a floor number they left out, or renames "Home". A `PUT`
        would make a client resend every field to change one, and the first
        client that forgot a field would silently erase the directions a driver
        needs.

        **`address_type` is not accepted.** The served-area rule is checked
        against the type the address has and the attributes are written
        afterwards, so a body carrying a new type would be checked under the old
        rule and stored under the new one. Changing what an address is for is a
        new address.

        **`is_default` is not accepted either** — promotion is
        `POST /me/addresses/{address}/default`, which demotes the incumbent in
        the same transaction.

        `delivery_area_id` **is** accepted; moving house is the ordinary case,
        and coverage is rechecked before the write.

        No `If-Match`. The row carries `lock_version` and it is served on every
        read, but this resource has one writer — its owner, on their own device
        — so the race optimistic concurrency exists to solve does not arise.
      parameters:
        - name: address
          in: path
          required: true
          description: The address identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCustomerAddressRequest'
      responses:
        '200':
          description: The address as it now stands.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAddressEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    delete:
      operationId: removeMyAddress
      tags:
        - Customer account
      summary: Remove an address
      description: |
        A real delete, unlike a contact point. An address is not evidence of
        anything: no account recovery depends on it, no duplicate index has to
        remember it was claimed, and a person who has moved is entitled to have
        their old street stop being stored. The audit trail records that an
        address was removed **without recording the address**.

        Removing the default promotes a replacement inside the same transaction
        — an account with addresses and no default makes the checkout ask which
        one, every time, for no reason.

        Deleting the last delivery address is allowed and will make the account
        unable to activate. That is honest: the checklist says so on the next
        read, and refusing would trap somebody in a district nobody serves.
      parameters:
        - name: address
          in: path
          required: true
          description: The address identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '204':
          description: The address was removed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/addresses/{address}/default:
    post:
      operationId: makeAddressDefault
      tags:
        - Customer account
      summary: Nominate the default address
      description: |
        A POST sub-resource action, never a `PATCH is_default` (master plan v2
        §4.15), and here the reason is mechanical as well as stylistic:
        promoting means demoting the incumbent, the partial unique index refuses
        a second default per type, and a client doing it as two field writes
        would collide with itself between them.

        The default is **per type**, so promoting a billing address leaves the
        delivery default alone. They answer different questions, and one flag
        serving both would move an invoice to somebody's office because they
        ordered lunch there once.

        An address that is no longer deliverable may still be made default:
        coverage can change back, refusing would leave the account with no
        default at all, and `is_deliverable` on every read says plainly whether
        anything can currently be sent.
      parameters:
        - name: address
          in: path
          required: true
          description: The address identifier.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The address is now the default for its type.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAddressEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/dietary-profile:
    get:
      operationId: showMyDietaryProfile
      tags:
        - Customer account
      summary: My dietary declaration
      description: |
        The **store of record** for what this person eats. The onboarding wizard
        pre-fills from it and writes back to it, and the meal configurator
        prefers it over anything typed into a basket: one answer in one place,
        so a customer who declared a sesame allergy never has to declare it
        again.

        **`has_declared` is the field to branch on, not an empty allergen
        list.** An unanswered question and a confident "I have none" are
        different facts, and only the second may activate an account that is
        about to be sent food.

        A read that reads: a profile never written is presented as
        `has_declared: false` rather than created on the way past. The same
        answer is given when no customer account has been opened, because
        somebody who has not started onboarding has genuinely declared nothing.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The dietary profile, declared or not.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerDietaryProfileEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    put:
      operationId: replaceMyDietaryProfile
      tags:
        - Customer account
      summary: Record a complete dietary declaration
      description: |
        **A replace, not a merge — which is why it is a `PUT`.** A person
        removing an allergy from the list means they no longer have it, and
        under a merge there would be no way to say so through the ordinary path:
        the set would only ever grow. The client sends the complete set and the
        service swaps it inside one transaction, because a half-applied allergy
        list is the one state this table must never be observed in.

        **`allergens` is required even when empty.** An omitted key and an empty
        array would otherwise mean the same thing on the wire and different
        things to the person — "I did not touch this" against "I have no
        allergies" — and only the second may activate an account.

        Declaring is an act and the act is recorded: `declared_at` is stamped
        for an empty list too, because "I have none" is an answer and it is the
        one the activation gate is waiting for.

        Each exclusion must name **exactly one** of an ingredient, a diet
        classification or free text; naming two or none is `validation.failed`
        on the offending index.

        `200` rather than `201` on first write: the profile is a singleton of
        the account, has no identifier a client navigates to, and cannot be
        created twice.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceDietaryProfileRequest'
      responses:
        '200':
          description: The declaration as it now stands.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerDietaryProfileEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/consents:
    get:
      operationId: listMyConsents
      tags:
        - Customer account
      summary: My consent position
      description: |
        **One list, not two.** A screen receiving only the outstanding texts
        could not show somebody what they have already accepted, and a person
        who cannot see what they consented to cannot meaningfully withdraw it —
        which is the right the whole ledger exists to serve.

        `status` is computed against the **current version** of each definition,
        not against "has this code ever been granted". A grant is recorded
        against one version; when a text is reissued the old grant stays true
        history and the new version is outstanding again, and a status computed
        from the code alone would report somebody as having accepted words they
        have never seen.

        Filtered to the consumer audience plus the texts everybody holds.
        Without the filter this endpoint would start asking a kitchen's chef to
        confirm they are old enough to order food.

        The body of each text is not served here: it is two long localised
        columns, the list renders as a checklist, and a client wanting the words
        asks for them where they are published.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every consumer consent text and whether the caller holds it.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerConsentsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: grantMyConsents
      tags:
        - Customer account
      summary: Record acceptance of consent texts
      description: |
        A list rather than a single code, because that is how the screen works:
        an onboarding step presents several texts with several checkboxes and
        the person ticks them in one act. Splitting that into several requests
        would let a network failure record half a decision, and half a consent
        decision is the one thing a regulator will ask about.

        The grant is written against the **current version** of each definition,
        read at the moment of writing. A client cannot name a version — that is
        the guarantee rather than a limitation, because a body carrying one
        could accept a text that has since been reissued.

        Re-granting something already held is a no-op, so a retried request
        cannot duplicate the audit trail. Unknown codes are skipped rather than
        refused: a client sending one has a stale catalogue.

        `200` with the caller's full position rather than `201` with what was
        written — the answer to a grant is what the person now holds, from the
        same builder `GET /me/consents` uses, so the screen after the tick
        cannot disagree with the screen before it. Skipped codes are visible by
        still being `pending`.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GrantConsentsRequest'
      responses:
        '200':
          description: The caller's consent position after the grant.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerConsentsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/consents/{code}:
    delete:
      operationId: withdrawMyConsent
      tags:
        - Customer account
      summary: Withdraw a consent
      description: |
        **A state transition, never a deletion.** The grant row survives with
        `status = withdrawn` and a timestamp, because "this person consented on
        the 3rd and withdrew on the 9th" is the fact a regulator asks for and a
        deleted row answers neither half of it. The row-level-security policies
        enforce it from underneath: there is no DELETE policy at all.

        **A required consent may be withdrawn**, and the consequence is visible
        rather than prevented — the activation checklist reports it outstanding
        on the next read. Refusing would make a right conditional on not needing
        the service, which is not how consent works.

        Withdrawing something never granted is a no-op and still `204`: the
        caller's intent is satisfied either way, and an opt-out that failed for
        somebody who never opted in would be an unpleasant surprise.

        An **unknown code** is `404` — the one thing this endpoint refuses.
        Answering `204` to a typo would let it look like a successful withdrawal
        forever.
      parameters:
        - name: code
          in: path
          required: true
          description: |
            The consent definition's stable code, never a grant identifier. The
            person is withdrawing "marketing", and which version of it they hold
            is something only the ledger knows.
          schema:
            type: string
            maxLength: 80
            examples:
              - consent.marketing
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '204':
          description: The consent is withdrawn, or was never held.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /delivery/jobs:
    get:
      operationId: listDeliveryJobs
      tags:
        - Delivery
      summary: The dispatcher's view of delivery jobs
      description: |
        The organisation's delivery jobs, newest first — every driver's, every
        state, including `delivered` and `cancelled`. This is the dispatch
        board rather than a driver's run sheet, which is why it carries
        `driver_user_id` and the driver's own list does not.

        **Capped at 50 rows with no pagination.** A dispatch board shows what
        is happening now; a client that needs the history of a completed job
        reads the order.

        No permission code: the narrowing is the organisation, which
        `org.context` has already validated a membership against.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Up to 50 delivery jobs, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - delivery_jobs
                    properties:
                      delivery_jobs:
                        type: array
                        maxItems: 50
                        items:
                          $ref: '#/components/schemas/DeliveryJob'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /driver/jobs:
    get:
      operationId: listDriverJobs
      tags:
        - Delivery
      summary: The caller's own outstanding runs
      description: |
        The signed-in driver's jobs that are still live — everything except
        `delivered` and `cancelled` — oldest first, which is the order they
        should be worked.

        **Scoped to the caller in the query, not filtered afterwards.** The
        `where driver_user_id = me` *is* the isolation, and it is why this
        endpoint needs no permission code: a driver's run sheet is theirs by
        ownership, and there is no role that would make somebody else's run
        sheet theirs.

        **The row carries what somebody standing next to a van needs** (C3):
        the order number printed on the bag, the whole delivery snapshot — the
        street, the building, the floor, the flat, the customer's own directions,
        the area, the promised slot and day — the moment the run was handed over,
        and the number to ring when the building still cannot be found. Before
        C3 it served four identifiers and two status strings, which was honest
        while nothing created delivery jobs and there was no driver client to
        read them, and stopped being honest the moment confirming an order
        started projecting real runs.

        Every delivery field comes from the **order's snapshot** as it stood at
        placement, never from the customer's address book: a customer editing
        their address at eight o'clock has not changed where tonight's food is
        going.

        `driver_user_id` is deliberately **absent** from the rows here. It
        would say the same thing on every one of them, and the dispatch board
        is the surface where whose job it is matters.

        **No cap and no cursor**, unlike the dispatch board: a driver's
        outstanding list is bounded by a shift, and a page break in the middle
        of a run would be a page break in the middle of a run.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The caller's live jobs, oldest first. Empty when there is nothing to run.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - jobs
                    properties:
                      jobs:
                        type: array
                        items:
                          $ref: '#/components/schemas/DriverJob'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /driver/jobs/{job}/deliver:
    post:
      operationId: deliverDriverJob
      tags:
        - Delivery
      summary: Mark a run delivered
      description: |
        Sets the job to `delivered`, stamps the time, and stores whatever the
        driver typed as proof.

        **A job that is not the caller's is `404`, and it is decided before
        the body is looked at.** A request with somebody else's job and a
        thousand-character note answers `404`, not `422` — which is the right
        order, because the note is nobody's business until the job is.

        **`proof_of_delivery_notes` is replace-or-clear.** Omitting the key
        writes `null` over whatever was there. There is no partial update: the
        one field this endpoint owns is stated every time or not at all.

        **No state guard and no `Idempotency-Key`.** A job already delivered
        is delivered again, which succeeds — a driver on a bad connection
        pressing the button twice must not meet a conflict dialog — but it
        does overwrite the note and the timestamp, so a client should not
        retry blindly with a different body.
      parameters:
        - $ref: '#/components/parameters/DeliveryJobPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeliverDriverJobRequest'
      responses:
        '200':
          description: The job, delivered.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - job
                    properties:
                      job:
                        type: object
                        required:
                          - id
                          - status
                        properties:
                          id:
                            $ref: '#/components/schemas/Uuid'
                          status:
                            allOf:
                              - $ref: '#/components/schemas/DeliveryJobStatus'
                            description: |
                              Always `delivered` on success. Typed as the job
                              status enum rather than a bare string so a client
                              reads it with the same vocabulary the two lists
                              serve.
                        additionalProperties: false
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /delivery/jobs/{job}/assign:
    post:
      operationId: assignDeliveryJob
      tags:
        - Delivery
      summary: Give a run to a driver
      description: |
        Names the driver who is taking this delivery out, stamps the moment, and
        moves the job to `assigned`.

        **The first write on this table two people can race**, which is why
        `delivery_jobs` gained a `lock_version` with this operation. A dispatch
        board is a shared screen: two dispatchers can be looking at the same
        unassigned run and the same free driver, and without a validator the
        second write silently wins — one driver is told about a job that is no
        longer theirs, and the row remembers only the later decision.

        The version is folded into the conditional `UPDATE`'s own `WHERE`, so
        there is no window between reading it and writing the row. Absent is
        **428**, stale is **409** carrying `details.current_lock_version`,
        `details.status` and `details.driver_user_id` so a board can re-render
        without a second round trip. A header present but not a Healthy360
        validator is **400**, not 409: the client has not lost a race, it has
        sent nonsense.

        **Re-assignment is permitted**, under a fresh `If-Match`. Drivers get
        sick and vans break down, and a run handed to somebody who cannot do it
        is the most likely reason anybody opens this twice; refusing would leave
        a dispatcher with no move except to cancel a delivery the customer is
        still expecting. The validator is what makes it deliberate — re-assigning
        requires having read the job *since* it was last assigned.
        `assigned_at` is re-stamped, because the useful question is when the
        person now holding the run was given it; every earlier answer survives in
        the audit trail.

        **A `delivered` or `cancelled` run refuses**, `409` with
        `details.status`. Assignment sets `status = assigned`, and applying that
        to a finished job would leave a row simultaneously assigned and stamped
        `delivered_at`.

        **`driver_user_id` must name an active member of this organisation.**
        Anything else — a stranger, a former employee, an invitation nobody
        accepted, a perfectly real courier who works for the kitchen next door —
        is **422** `validation.failed` with the message on
        `details.fields.driver_user_id`. A 404 would be the wrong answer: the
        job is real and the caller may reach it; what is wrong is one field of
        the body.

        **`tracking_status` deliberately does not move.** The customer-facing
        vocabulary runs `awaiting_assignment → picked_up → en_route → arrived →
        delivered` and there is no value between the first two, because *a
        driver has been named but has not collected the food* is not a message a
        customer is owed. **The order is not touched either** — not its status,
        not its `lock_version`: a dispatcher naming a driver changes nothing the
        customer agreed to, and bumping the order's validator would invalidate
        every desk screen holding it for a change none of them can see.

        Requires `order.manage_organisation` — the same code that confirms the
        order this run came from. The three *reads* on this table carry no
        permission code because they are narrowed by ownership; deciding whose
        evening this is, is authority. A job belonging to another organisation is
        **404**, never 403.
      parameters:
        - $ref: '#/components/parameters/DeliveryJobPath'
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignDeliveryJobRequest'
      responses:
        '200':
          description: The job as it now stands, with its new validator.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - job
                    properties:
                      job:
                        $ref: '#/components/schemas/AssignedDeliveryJob'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /payments/intents:
    post:
      operationId: createPaymentIntent
      tags:
        - Orders
      summary: Open a payment intent against an order
      description: |
        Creates the intent and authorises it with the provider that handles
        the chosen method, in one call.

        **The amount and the currency come from the order, never from the
        request.** A client that could name either could name a different one,
        and a payment for an amount the order does not say is not a payment
        for that order.

        **One intent per order**, enforced by a unique index. A second attempt
        is `409 resource.conflict` — which is also, in practice, the only
        replay protection this endpoint has: there is **no `Idempotency-Key`**
        and none is accepted.

        `method_kind` accepts `invoice` at the validator and then fails at the
        provider with `400 request.invalid`: no provider is configured for it.
        The vocabulary is shared with the rest of the platform and this
        endpoint does not narrow it, so a client must treat `invoice` as
        unsupported here rather than as merely untested.

        The order must be one the caller owns — `OrderLocator` scopes to the
        signed-in person's own customer account and answers `404` otherwise.
        A signed-in person with no customer account at all is `403
        account.verification_required` with
        `details.outstanding: ["customer_account_missing"]`.

        **No `X-Organisation-Id` here, unlike capture and refund.** Opening an
        intent is the buyer's act and a customer is a member of no
        organisation; the authority is ownership of the order, and the intent
        takes its organisation from that order — the kitchen being paid. That
        organisation is what the capture and refund endpoints then require.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePaymentIntentRequest'
      responses:
        '201':
          description: The authorised intent.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentIntentEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /payments/intents/{paymentIntent}/capture:
    post:
      operationId: capturePaymentIntent
      tags:
        - Orders
      summary: Capture an authorised payment
      description: |
        Takes the money the intent was authorised for. The body is empty.

        **Capture is not idempotent.** An intent that has already been
        captured, or that failed or was cancelled, is `409
        resource.conflict`. A cash-on-delivery intent may be captured straight
        from `pending`, because for cash the authorisation and the collection
        are the same moment; every other method must be `authorized` first.

        There is no `If-Match` and no `Idempotency-Key`. The `409` is what
        stops a double capture, so a client that retries must be prepared to
        read it as "already done" rather than as a failure.

        **This is the kitchen's endpoint, not the customer's**, and it
        requires `X-Organisation-Id`. An intent belongs to the organisation
        being paid — the amount and the currency came from that kitchen's
        order — and taking the money is that kitchen's act. An intent belonging
        to any other organisation is **`404`**, exactly as an identifier that
        names nothing is: a `403` would confirm that the payment exists.

        No permission code. The narrowing is the organisation, which
        `org.context` has already validated a membership against.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PaymentIntentPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The captured intent.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentIntentEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /payments/intents/{paymentIntent}/refunds:
    post:
      operationId: createRefund
      tags:
        - Orders
      summary: Refund part or all of a captured payment
      description: |
        Records a refund against a captured intent. The currency is copied
        from the intent; only the amount is sent.

        **Only a `captured` intent may be refunded** — anything else is `409
        resource.conflict`.

        **The amount is capped at what is still refundable** — the captured
        amount less every refund already recorded against the intent. Over it
        is `409 payment.refund_exceeds_capture`, carrying `captured_minor`,
        `refunded_minor` and `refundable_minor` in `details` so a form can show
        the remaining balance rather than a bare rejection. The check and the
        write share one transaction against a locked row, so two refunds sent
        at once cannot both find room in the same balance.

        The refund is a row of its own. **The intent's status does not
        change**: there is no `refunded` state, and an intent that has been
        refunded in full still reads `captured`. `refundable_minor` on a
        refusal is how a client learns that nothing is left.

        **This is the kitchen's endpoint, not the customer's**, and it
        requires `X-Organisation-Id` — the same organisation the intent
        belongs to, which is the one the order was placed with. Any other
        organisation's intent is **`404`**, not `403`, because a `403` would
        confirm the payment exists.

        No permission code. The narrowing is the organisation, which
        `org.context` has already validated a membership against.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PaymentIntentPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRefundRequest'
      responses:
        '201':
          description: The refund, already completed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - refund
                    properties:
                      refund:
                        $ref: '#/components/schemas/Refund'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/RefundRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/catalogue/items:
    get:
      operationId: listB2bCatalogueItems
      tags:
        - B2B corporate catalogue
      summary: Articles this corporate buyer may order
      description: |
        The private catalogue behind a corporate buyer's own agreements: every
        published article offered today on a private-pricing sales channel run
        by a kitchen the buyer holds an **active agreement** with, each with
        the one price that agreement resolves to.

        **A row with no resolvable price is omitted rather than served
        priceless.** A corporate catalogue exists so somebody can commit
        budget against it; an article whose tariff has lapsed is not a
        cheaper article, it is an article this buyer cannot currently order.

        **No permission code**, only `org.context`. The authority is the
        buyer's own account shape and the agreements behind it — a corporate
        account is not a membership role, and a code granted to every
        corporate buyer at provisioning would answer "yes" for all of them.
        An account that is not a corporate buyer is **403
        `cart.channel_refused`**, the same code the cart raises when a
        personal account reaches for a private channel.

        **No pagination and no filters.** The result is bounded by what the
        buyer's own agreements put on the table, which is a merchandised set
        somebody negotiated rather than an open catalogue, so `meta` carries
        the correlation identifier and nothing else.

        `language` selects which of the two stored names is served. It is a
        presentation choice made per request rather than a stored preference,
        because the same buyer's procurement officer and warehouse may read
        in different languages.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bCatalogueLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The articles this buyer may order today, with their agreed prices.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - items
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/B2bCatalogueItem'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/CorporateBuyerRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/catalogue/items/{item}:
    get:
      operationId: showB2bCatalogueItem
      tags:
        - B2B corporate catalogue
      summary: One agreed article, with its price
      description: |
        The single-article read behind the same agreements as the list.

        **Everything that is not offered to this buyer is `404`, never
        `403`.** An article that is unpublished, that belongs to a kitchen the
        buyer holds no active agreement with, that is not assigned to an
        agreed channel today, or whose price does not resolve, all answer the
        same "no such article, as far as you are concerned". A refusal that
        distinguished them would turn an identifier into a probe for what
        other kitchens sell.

        The one refusal that is **not** a 404 is the account shape: a
        non-corporate account is `403 cart.channel_refused` before any lookup
        happens, because that is a fact about the caller rather than about
        the article.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bCatalogueItemPath'
        - $ref: '#/components/parameters/B2bCatalogueLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The article and its agreed price.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - item
                    properties:
                      item:
                        $ref: '#/components/schemas/B2bCatalogueItem'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/CorporateBuyerRequired'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/programmes:
    get:
      operationId: listCorporateProgrammes
      tags:
        - B2B corporate programmes
      summary: The programmes this organisation buys under
      description: |
        Every corporate programme the caller's active organisation is the
        **buyer** on, newest first.

        **No permission code.** A programme is the organisation's own standing
        arrangement with a kitchen, and every member sees the same list — the
        B7 rule that a quotation draft is org-shared rather than personal
        applies one level up as well. Membership, already proven by
        `org.context`, is the whole gate.

        `data` is the array itself rather than an object wrapping it, and
        `meta.count` is the number of rows served. There is no cursor: a
        programme is negotiated paperwork and an organisation has a handful,
        not a book that grows while the kitchen trades.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The buyer organisation's programmes.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CorporateProgramme'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                        properties:
                          count:
                            type: integer
                            minimum: 0
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/programmes/{programme}:
    get:
      operationId: showCorporateProgramme
      tags:
        - B2B corporate programmes
      summary: One corporate programme
      description: |
        The programme, resolved inside the buyer organisation `org.context`
        already validated. A programme belonging to another organisation is
        `404` — the tenant scope is the lookup, not a check bolted on after
        one.

        `lock_version` is carried in the body for symmetry with the other
        lock-versioned resources, but this endpoint serves **no `ETag`**:
        there is no write on a programme in this phase, so there is no
        validator for a client to hold.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CorporateProgrammePath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The programme.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CorporateProgrammeEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/programmes/{programme}/quotations:
    get:
      operationId: listQuotations
      tags:
        - B2B quotations
      summary: Every quotation drafted against one programme
      description: |
        Newest first, and **without lines**: `lines` is served as an empty
        array on every row here. A list is a navigation aid, and a set of
        priced lines is the thing somebody opens a quotation to read.
        `GET /b2b/quotations/{quotation}` carries them.

        Drafts are included, unlike the kitchen's view of the same table — a
        buyer's own unsent draft is exactly what they came back to finish.

        `data` is the array itself with `meta.count`; there is no cursor.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CorporateProgrammePath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The programme's quotations, newest first, without their lines.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Quotation'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                        properties:
                          count:
                            type: integer
                            minimum: 0
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createQuotation
      tags:
        - B2B quotations
      summary: Open a new quotation draft
      description: |
        A buyer opens a draft against a programme (B4). `lines` is optional:
        a draft may be opened empty and filled in by a later `PATCH`, which is
        how a client that wants a quotation identifier before the user has
        chosen anything gets one.

        **The currency is the programme's, never the caller's.** It is copied
        from the agreement's active price list at creation, so a quotation
        cannot be drafted against a programme whose agreement carries no
        catalogue tariff yet — that is `409 resource.conflict`.

        Every line must name an article **the programme's kitchen sells**; one
        that does not is `422 validation.failed` with the offending
        `lines.{index}.catalogue_item_id` in `details.fields`. A variant, when
        given, must belong to the article on its own line.

        **No permission code** (B7: org-shared server drafts). Any member of
        the buyer organisation may open one, and every member can see it
        afterwards.

        Answers `201` with the created draft and its `ETag` — the validator
        the very next `PATCH`, `submit` or decision must send back as
        `If-Match`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CorporateProgrammePath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateQuotationRequest'
      responses:
        '201':
          description: The new draft.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotationEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/quotations/{quotation}:
    get:
      operationId: showQuotation
      tags:
        - B2B quotations
      summary: One quotation, with its lines
      description: |
        The buyer's own read, addressed directly rather than through the
        programme: a client that walked the list holds the quotation's
        identifier, and a path that repeated the programme would let the two
        disagree.

        Returns `ETag: "<lock_version>"` — the client's half of the
        optimistic-concurrency contract. Read the quotation, keep the
        validator, send it back as `If-Match` when patching, submitting,
        accepting or declining.

        `lines` is the complete set, in `line_number` order.
        `unit_amount_minor` and `line_total_minor` are `null` on every line
        until the kitchen quotes: the absence **is** the statement that no
        price has been named, not a zero.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/QuotationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The quotation and its lines.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotationEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      operationId: updateQuotation
      tags:
        - B2B quotations
      summary: Edit a draft's notes, its lines, or both
      description: |
        **`lines`, when present, is the desired complete set** — the same
        PUT-shaped-as-PATCH convention a price list's entries use. A client
        sends every line it wants kept, not a delta, and the server replaces
        the set and renumbers `line_number` from 1. Omitting `lines`
        altogether leaves them untouched, which is what makes "save the note I
        just typed" a request that cannot lose a line.

        **A price never arrives here.** `unit_amount_minor` is not a field of
        this request at any nesting level: naming a price is the kitchen's
        move and it has its own endpoint.

        Refused outside `draft` with **409 `b2b.quotation_state_invalid`** —
        a submitted quotation is waiting on the kitchen, not on another edit,
        and there is deliberately no route back into `draft`. A buyer who
        wants to change a submitted set drafts a fresh quotation; a programme
        may hold many.

        `If-Match` is required. A stale validator is `409 resource.conflict`
        carrying `details.current_lock_version`; a malformed one — anything
        that is not the numeric validator this API issues — is `400
        request.invalid`, because that is a malformed request rather than a
        lost race.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/QuotationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateQuotationRequest'
      responses:
        '200':
          description: The quotation after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotationEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bQuotationStateInvalid'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/quotations/{quotation}/submit:
    post:
      operationId: submitQuotation
      tags:
        - B2B quotations
      summary: Ask the kitchen for prices
      description: |
        `draft` → `submitted`, the buyer's ask (B5). The body is empty: what
        is being sent is the quotation, and it is already on the server.

        **`200`, never `201`.** Nothing is created — an existing quotation
        moves state — and a `201` would invite a client to look for a
        `Location` header naming something new.

        **An empty draft is `422 b2b.quotation_empty`**, its own code rather
        than `validation.failed`: nothing about the request is malformed, the
        resource itself has nothing in it to send for pricing. Submitting from
        any state other than `draft` is **409
        `b2b.quotation_state_invalid`** with the allowed moves in
        `details.allowed_transitions`.

        `If-Match` is required, for the same reason the edit needs one: a
        stale tab must not submit a set of lines it never saw.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/QuotationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The submitted quotation.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotationEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bQuotationStateInvalid'
        '422':
          $ref: '#/components/responses/B2bQuotationEmpty'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/quotations/{quotation}/accept:
    post:
      operationId: acceptQuotation
      tags:
        - B2B quotations
      summary: Accept the kitchen's prices
      description: |
        `quoted` → `accepted` (B5). The body is empty.

        **The only side-effect is that nothing new happens (B6).** Catalogue
        access for this buyer is already governed entirely by the agreement's
        active price list, resolved fresh on every browse, and acceptance
        changes neither the agreement nor the price list. What acceptance
        records is that the buyer confirmed a total — there is no grant to
        refresh and no catalogue to unlock.

        **A quotation past its `expires_at` is refused even before the sweep
        has moved it.** The nightly job that writes `expired` runs on a queue;
        the buyer's deadline does not wait for a queue worker, so accepting a
        lapsed quotation is `409 b2b.quotation_state_invalid` with
        `details.status: expired`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/QuotationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The accepted quotation.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotationEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bQuotationStateInvalid'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/quotations/{quotation}/decline:
    post:
      operationId: declineQuotation
      tags:
        - B2B quotations
      summary: Decline the kitchen's prices
      description: |
        `quoted` → `declined` (B5). `reason` is optional and is stored on the
        quotation as `decline_reason`, where the kitchen can read it — a
        decline with a sentence behind it is the difference between a lost
        deal and a renegotiated one.

        Like acceptance, a quotation past its `expires_at` is `409
        b2b.quotation_state_invalid` rather than a decline: the deadline
        already decided.

        Declining is terminal. There is no route back to `quoted`, and a
        buyer who changes their mind drafts a fresh quotation.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/QuotationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeclineQuotationRequest'
      responses:
        '200':
          description: The declined quotation.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotationEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bQuotationStateInvalid'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/kitchen/quotations:
    get:
      operationId: listKitchenQuotations
      tags:
        - B2B quotations
      summary: Quotations submitted to this kitchen
      description: |
        The same table read from the **seller's** side: every quotation
        drafted against a programme the caller's active organisation is the
        kitchen for.

        **Drafts are excluded.** A kitchen has no business seeing a buyer's
        unsent draft, only what was actually asked for — and the exclusion is
        in the query rather than in a client filter, so there is no shape of
        request that reveals one.

        `Quotation.organisation_id` is the *buyer*, so the tenant scope that
        protects a buyer's own read is exactly what has to be stepped around
        here; ownership is proven instead by joining through the programme's
        `kitchen_organisation_id`. That is why this half of the surface
        carries a permission code where the buyer's half carries none:
        `b2b_quotation.view_organisation`, granted to `kitchen_manager` and
        `commercial_manager`.

        Lines are omitted, as on the buyer's list.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The quotations submitted to this kitchen, newest first, without their lines.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Quotation'
                  meta:
                    allOf:
                      - $ref: '#/components/schemas/Meta'
                      - type: object
                        required:
                          - count
                        properties:
                          count:
                            type: integer
                            minimum: 0
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/kitchen/quotations/{quotation}:
    get:
      operationId: showKitchenQuotation
      tags:
        - B2B quotations
      summary: One submitted quotation, from the kitchen's side
      description: |
        The seller's read of one quotation and its lines, resolved through the
        programmes this kitchen owns. A quotation belonging to another
        kitchen's programme is `404`.

        **A quotation still in `draft` is `404` here too**, not `403`. The
        kitchen is not being told "there is a draft you may not read"; as far
        as the seller's side of this relationship is concerned, a draft does
        not exist.

        Serves `ETag: "<lock_version>"` — the validator the kitchen sends back
        as `If-Match` when it names its prices.

        Requires `b2b_quotation.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/QuotationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The quotation and its lines.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotationEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/kitchen/quotations/{quotation}/quote:
    post:
      operationId: quoteKitchenQuotation
      tags:
        - B2B quotations
      summary: Name a price for every line
      description: |
        `submitted` → `quoted` (B4, B5). The kitchen sets a unit price on
        every line and the server computes each `line_total_minor` as
        `quantity × unit_amount_minor`, rounded to the nearest minor unit — a
        total the buyer is asked to accept is the server's arithmetic, never
        the client's.

        **Every line, never fewer, never more.** A partial quote would leave
        the buyer deciding against a total that is not really the total, so a
        missing line is `422 validation.failed` with
        `details.fields.prices.missing_quotation_line_ids`. A `prices` entry
        naming a line that is not on this quotation, or naming the same line
        twice, is `422` against the offending
        `prices.{index}.quotation_line_id`.

        **Amounts are whole minor units.** `1250` means 12.50 in the
        quotation's own currency; a decimal or a formatted string is `422`.
        Zero is accepted — a kitchen may quote a line at no charge — and a
        negative amount is not.

        On success the quotation gains `quoted_at` and an `expires_at` **seven
        days later**, after which the buyer can no longer accept or decline it
        and a nightly sweep writes `expired`.

        Requires `b2b_quotation.quote_organisation` — a separate authority
        from reading the queue, because naming a price is the one act neither
        the buyer nor the platform performs for the kitchen. Held by
        `kitchen_manager` and `commercial_manager`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/QuotationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuoteQuotationRequest'
      responses:
        '200':
          description: The quoted quotation, with every line priced.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotationEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bQuotationStateInvalid'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications:
    post:
      operationId: createB2bApplication
      tags:
        - B2B applications
      summary: Begin a B2B application
      description: |
        Creates an empty draft. The body is empty on purpose: a draft is a
        container, and asking somebody to name their company before they have
        seen the form is how a wizard acquires a step nobody wanted.
        Everything arrives through the section PATCHes.

        **One live application per person.** A second attempt while one is in
        progress is `409 resource.conflict` naming the existing application
        and its status, never a silent second draft that orphans the first.

        No permission code. The applicant is a member of nothing; ownership is
        stamped here and is what every later write is checked against.
      parameters:
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '201':
          description: The new draft.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    get:
      operationId: listB2bApplications
      tags:
        - B2B applications
      summary: The caller's own applications
      description: |
        Scoped to the caller in the query rather than filtered afterwards:
        `b2b_applications` is deliberately not organisation-scoped and carries
        no row-level-security policy, so the `where` **is** the isolation.

        **Newest first.** There is normally one live application and then a
        tail of history — a withdrawn attempt, a decline from last year — and
        the question this list answers is "where is the one I am filling in".
        The platform queue walks the other way, because a queue is fair in
        arrival order.

        The rows carry the summary shape rather than the full file: a list is
        a navigation aid, and confidential detail belongs on a resource
        somebody deliberately opened.
      parameters:
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of the caller's applications.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/B2bApplicationSummary'
                  meta:
                    $ref: '#/components/schemas/PaginationMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}:
    get:
      operationId: showB2bApplication
      tags:
        - B2B applications
      summary: One application, with its contacts and locations
      description: |
        Returns `ETag: "<lock_version>"` — the client's half of the
        optimistic-concurrency contract: read the resource, keep the
        validator, send it back as `If-Match` when writing a section,
        submitting or withdrawing.

        The contacts and the locations come with it, because opening an
        application and immediately asking "who did we name, and where did we
        say to deliver" is one interaction rather than three, and both sets
        are bounded by what a human typed into one form.

        Documents and agreements are **not** here. Both have their own
        lifecycle and their own review, and folding them in would mean a
        wizard polling a passport scan's review state every time it re-read
        the form.

        `writable_sections` is computed rather than stored, and in
        `info_requested` it is the reviewer's narrowing — a wizard that had to
        infer it from the status alone would reopen the whole form.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The application, its contacts and its locations.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationDetailEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/sections/{section}:
    patch:
      operationId: updateB2bApplicationSection
      tags:
        - B2B applications
      summary: Save one step of the wizard
      description: |
        **The section is in the path, not the body**, and that is what makes
        the endpoint honest: a wizard step is a unit of intent, and naming it
        in the URL lets the server refuse a payload that wandered outside it.

        A key the named section does not own is **refused, never ignored**,
        and the refusal says which section does own it. Silently dropping a
        field somebody believed they were saving turns a five-minute bug into
        a support ticket about vanishing data six weeks later.

        In `info_requested` the writable set narrows to the sections the
        reviewer named, and a write to any other section is `409
        b2b.application_state_invalid` carrying `details.writable_sections`.

        `If-Match` is required despite there being one editor, and the reason
        is the reviewer rather than the applicant: a stale tab that saved over
        a narrowing it never saw would put answers into fields the review had
        already closed.

        An unknown `{section}` is `400 request.invalid` with
        `details.parameter`, not a 404 — the application exists, and the
        client sent a word this API does not have.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/B2bApplicationSectionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateB2bApplicationSectionRequest'
      responses:
        '200':
          description: The application after the write.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bApplicationStateInvalid'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/contacts:
    put:
      operationId: replaceB2bApplicationContacts
      tags:
        - B2B applications
      summary: Replace the people the company names
      description: |
        Set-replace, not a merge: a client that removed a row has to be able
        to say so, and `{"contacts": []}` is the payload that expresses it.

        **One contact per role.** The four slots are the shape; a duplicate
        role is refused by name. There is no `id` and no ordering, because the
        rows are replaced wholesale and an identifier a client held would be
        stale the moment it was used.

        A contact needs a name and at least one way to reach them. That rule
        is answered per row — `contacts.2.email` — rather than as two
        independent field errors that neither explain.

        **No `If-Match` and no `ETag`.** The set is replaced in one
        transaction, so there is no half-list for a validator to protect, and
        these rows carry no `lock_version` because a per-row validator would
        let two editors replace different halves of one roster.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceB2bApplicationContactsRequest'
      responses:
        '200':
          description: The stored contacts, as normalised.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - contacts
                    properties:
                      contacts:
                        type: array
                        items:
                          $ref: '#/components/schemas/B2bApplicationContact'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/locations:
    put:
      operationId: replaceB2bApplicationLocations
      tags:
        - B2B applications
      summary: Replace where the company wants food
      description: |
        Set-replace, on the same argument as the contacts endpoint: a company
        that closed an office has to be able to say so, and
        `{"locations": []}` is how.

        `delivery_area_id` anchors a location to the platform gazetteer where
        the gazetteer covers it, and is **optional** because it often does
        not. A corporate park in a city nobody has mapped is still somewhere a
        van can go, and refusing the address until the gazetteer catches up
        would make the platform's incompleteness the applicant's problem.

        At most one primary and at most one billing address. Both are answered
        as errors about the *set*, because blaming an arbitrary row for a
        problem the row does not have helps nobody.

        No `If-Match` and no `ETag`, for the reasons the contacts endpoint
        gives.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceB2bApplicationLocationsRequest'
      responses:
        '200':
          description: The stored locations, as normalised.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: object
                    required:
                      - locations
                    properties:
                      locations:
                        type: array
                        items:
                          $ref: '#/components/schemas/B2bApplicationLocation'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/documents:
    post:
      operationId: uploadKycDocument
      tags:
        - B2B applications
      summary: Upload an identity or registration document
      description: |
        **`multipart/form-data`** — the one operation in this family that is
        not JSON in. Base64 in a JSON body would inflate every upload by a
        third and put ten megabytes through the request pipeline as a value
        rather than as a file on disk.

        **The client's `Content-Type` and filename decide nothing.** The
        server sniffs the leading bytes, and *that* result becomes
        `mime_type`; the claim is kept in `declared_mime_type` because a
        mismatch is evidence. An unrecognised or unaccepted format is refused
        naming what the platform accepts — never what it thought the file was,
        because that would make this endpoint a scanner people could query.
        The uploader's filename never reaches the object key.

        `signed_agreement` is **not** an acceptable `document_kind` here. A
        countersigned agreement is produced by the signing flow; accepting one
        from a client would let an applicant supply their own idea of what
        they signed.

        `expires_on` is accepted for every kind and stored only for the kinds
        that expire. Refusing it would mean a client had to know the expiry
        table to send a valid request.

        **Re-uploading identical bytes returns the existing document** rather
        than refusing, which is what makes a flaky connection or a
        double-tapped button harmless. The status is still `201`: the client
        asked for the document to exist, and it does.

        Every document B1 stores has `scan_status: not_scanned`. Nothing may
        present it as safe (INT-008).
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UploadKycDocumentRequest'
            encoding:
              file:
                contentType: application/pdf, image/jpeg, image/png, image/webp, image/heic
      responses:
        '201':
          description: The stored document's metadata.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KycDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    get:
      operationId: listKycDocuments
      tags:
        - B2B applications
      summary: The document pack — metadata only
      description: |
        **There is no URL in this response and there must never be one.** The
        only route to the bytes is a short-lived signed link from the download
        operation, which records who asked and why. A listing that carried
        links would defeat the expiry, the audit trail and the per-access
        purpose at once — and a listing is exactly the response somebody
        pastes into a chat.

        **Rejected and superseded documents are listed too**, with their
        reasons. An applicant looking at their own paperwork needs to see the
        scan that was turned down and why, or the rejection is a dead end.

        Not paginated: the set is bounded by a checklist of ten document kinds
        plus re-uploads.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every document filed against this application.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/KycDocument'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/documents/{document}/download:
    get:
      operationId: downloadKycDocument
      tags:
        - B2B applications
      summary: A temporary link to a document's bytes
      description: |
        **A URL in the envelope, not a `302`.** A redirect would be the
        shorter implementation and the worse contract: the link is a signed,
        expiring grant against object storage, and handing it over as data
        means a client knows it holds a credential with a deadline — it can
        show the expiry, refresh before acting, and decline to cache it. A
        redirect makes the same credential invisible, landing it in browser
        history, the referrer chain and every proxy log on the way to the
        bucket.

        **`purpose` is required**, non-blank, and is written onto the access
        audit event. A document read with no stated reason is the read an
        audit trail cannot explain afterwards. Neither the object path nor the
        URL is ever recorded there — either would put a live grant of access
        into a table read by more people than the document is.

        `expires_at` is computed before the signature is minted, so it is
        always at or before the moment the link actually stops working.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/KycDocumentPath'
        - $ref: '#/components/parameters/KycAccessPurpose'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A short-lived signed URL and when it stops working.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KycDocumentDownloadEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/submit:
    post:
      operationId: submitB2bApplication
      tags:
        - B2B applications
      summary: Send the application for review
      description: |
        Its own operation and its own audit event, never a `PATCH status`
        (§4.15). Sending is a decision made once; modelling it as a field
        would put it on the same code path as correcting a typo.

        **The server decides what complete means.** `completed_sections` is
        the applicant's own progress claim and is never consulted: readiness
        is computed from the data actually present *and* from the required
        document kinds, so a checklist enforced only in a wizard is not one an
        API client can skip.

        Both failures come back at once — `details.missing_fields` and
        `details.missing_documents` — because somebody should learn everything
        that is wrong in one round trip. The **code** is
        `422 b2b.documents_incomplete` when anything is missing from the
        document checklist and `422 validation.failed` when only form fields
        are, because those send a client to different screens.

        Duplicate registrations are recorded on the submission event and shown
        to the reviewer. They **surface and never decide**: a second
        application against the same registration is routine, and
        auto-rejecting it would make the platform's convenience the customer's
        problem.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The submitted application.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bApplicationStateInvalid'
        '422':
          $ref: '#/components/responses/B2bDocumentsIncomplete'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/withdraw:
    post:
      operationId: withdrawB2bApplication
      tags:
        - B2B applications
      summary: Withdraw the application
      description: |
        The applicant's own exit, and a **different terminal state from a
        decline**. Collapsing the two would make "how many companies did we
        turn down" an unanswerable question, and it would also be a small lie
        told to the person who changed their mind.

        Not a `DELETE`: nothing is removed. The application stays, the
        documents stay, the trail of who applied and what happened stays — and
        the applicant's one live slot is freed, so they may start again.

        `If-Match` decides the race that actually happens here: an applicant
        withdrawing at the moment a reviewer approves. Exactly one may win.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The withdrawn application.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bApplicationStateInvalid'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/agreements:
    get:
      operationId: listB2bAgreements
      tags:
        - B2B applications
      summary: Every version of the agreement
      description: |
        **All of them, including superseded ones**, which is the point of
        versioning rather than editing. An active agreement is immutable; the
        way to change what is in force is a new version that supersedes the
        old, and a list showing only the current terms would hide the history
        a company needs to answer "what were we on before".
        `supersedes_agreement_id` chains them.

        Not paginated: versions are counted on one hand — a renegotiation is a
        conversation, not an event stream.

        There is no separate consumer shape. An agreement is a document
        *between* two parties, so there is nothing on it the company may not
        see; the one internal field, the passcode challenge behind the
        signature, is reduced to a boolean for everybody.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every agreement version, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/B2bAgreement'
                  meta:
                    $ref: '#/components/schemas/Meta'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/agreements/{agreement}:
    get:
      operationId: showB2bAgreement
      tags:
        - B2B applications
      summary: One version of the terms
      description: |
        Returns `ETag: "<lock_version>"`. Signing does not consume it — the
        service refuses on the agreement's *state*, which is a stronger
        guarantee than "only if nobody edited it since you looked" — but the
        validator is still the honest thing to serve.

        The agreement is resolved **inside** the application in the path, so
        one company's identifier can never reach another's negotiated
        position. That matters more here than anywhere else: a credit limit
        and a price list are the most commercially sensitive rows the platform
        stores.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/B2bAgreementPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The agreement version.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bAgreementEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/agreements/{agreement}/signature-challenges:
    post:
      operationId: createB2bSignatureChallenge
      tags:
        - B2B applications
      summary: Send the named signatory a passcode
      description: |
        The question this operation answers is not "may this person sign?" but
        "**is this person the one the company said would sign?**" Only the
        second is answerable from the data.

        Three steps, and the order matters.

        1. The application's `signatory_email` is recorded as an
           application-owned contact point — evidence of *who was named*, kept
           whether or not they ever sign, and recorded before the ownership
           check so it survives a refusal.
        2. The authenticated caller must hold that same address as one of
           their own contacts, compared on normalised values so
           `Ali@Example.COM ` and `ali@example.com` are one address. Failing
           this is **403 `b2b.signatory_required`**: the named signatory has
           to sign in and sign for themselves, and an office manager who can
           reach the mailbox is not the person the company bound itself
           through.
        3. The challenge is issued against the **caller's own** contact, so
           `otp_challenges.user_id` is set. A challenge raised against the
           ownerless application contact would carry no user and could never
           satisfy the check signing performs.

        The caller's contact does not have to be verified already: the
        passcode is what proves control of the address, and demanding prior
        proof would be demanding the thing being established.

        `202`, not `201` — a challenge has been raised and a message
        dispatched; whether it arrives is the channel's business.
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/B2bAgreementPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '202':
          description: |
            The challenge, with a masked destination, a resend countdown and
            an attempts counter. `debug_code` appears only in local and
            testing environments with code exposure switched on.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OtpChallengeEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/B2bSignatoryRequired'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /b2b/applications/{application}/agreements/{agreement}/sign:
    post:
      operationId: signB2bAgreement
      tags:
        - B2B applications
      summary: Accept the terms
      description: |
        Records click-wrap evidence: the digest of the exact bytes the
        signatory was shown, their typed name, their stated capacity, the
        consent wording verbatim, and keyed digests of the address and user
        agent the acceptance came from. The last two are hashed because their
        job is corroboration, and they are **taken from the request** rather
        than accepted in the body — a client-supplied corroboration is not
        corroboration.

        **A verified passcode is required, not recorded hopefully.** The code
        is spent first, and the service then independently demands a
        *consumed* `b2b_signatory` challenge belonging to the person signing.
        That is not redundant: the service is reachable from a console command
        and a queued job, and a rule that depended on a controller remembering
        to enforce it holds only until somebody adds a second caller.

        The challenge is matched **by owner and by purpose before the code is
        compared**, so a challenge belonging to somebody else, or raised for
        logging in, is refused without spending one of three attempts —
        otherwise anybody could burn a signatory's allowance. All such
        refusals answer `403 b2b.signatory_required` with no detail about
        which condition failed.

        Only an agreement in `pending_signature` may be signed; anything else
        is `409`.

        **This is never a qualified electronic signature** and no surface may
        describe it as one (INT-007 covers real e-sign).
      parameters:
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/B2bAgreementPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignB2bAgreementRequest'
      responses:
        '200':
          description: The agreement, now active.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bAgreementEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/B2bSignatoryRequired'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/applications:
    get:
      operationId: listPlatformB2bApplications
      tags:
        - B2B platform review
      summary: The review queue
      description: |
        **Oldest first**, unlike every other list in the platform. This is a
        queue rather than a feed: the fair order to work applications in is
        the order they arrived, and newest-first would leave a company that
        applied in March behind one that applied this morning for as long as
        the queue keeps growing.

        **Drafts are excluded unless `status=draft` asks for them.** A draft
        is somebody's half-typed form that has not been sent to anybody;
        putting it in a reviewer's queue would show the platform a company's
        private working-out and invite somebody to act on an application that
        was never submitted.

        `query` matches the legal name, the trading name and the reference —
        the three things a person holds when they ring up.

        Requires `b2b_application.view_platform` inside the platform-operator
        organisation.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - name: status
          in: query
          required: false
          description: |
            Restrict to one workflow state. Omitted, everything except `draft`
            is listed.
          schema:
            $ref: '#/components/schemas/B2bApplicationStatus'
        - name: query
          in: query
          required: false
          description: Case-insensitive substring match over the legal name, the trading name and the reference.
          schema:
            type: string
            maxLength: 160
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of the review queue.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/B2bApplicationSummary'
                  meta:
                    $ref: '#/components/schemas/PaginationMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/applications/{application}:
    get:
      operationId: showPlatformB2bApplication
      tags:
        - B2B platform review
      summary: The whole file, as a reviewer sees it
      description: |
        Everything a decision rests on arrives in one response — the answers,
        the people, the sites, the document pack, what the checklist still
        owes, and the duplicate matches — because a reviewer who has to make
        five calls to form one judgement will eventually make four.

        `decision_note` and `reviewed_by` appear here and **only** here: the
        internal half of a verdict is not something the company it concerns
        should read.

        **Documents are metadata only, and opening one is a second
        permission's business.** The pack is listed so a reviewer can see what
        has been filed; fetching the bytes needs `kyc_document.view_platform`,
        which is deliberately narrower — being able to work a queue is not by
        itself a reason to look at somebody's passport.

        `duplicate_matches` is the count and `duplicate_applications` is the
        list behind it. A count with no way to reach the matches is a number a
        reviewer cannot act on. Nothing here is a verdict: duplicates surface
        and never decide.

        Requires `b2b_application.view_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The application, its people, its pack and its duplicates.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationReviewEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/applications/{application}/claim:
    post:
      operationId: claimB2bApplication
      tags:
        - B2B platform review
      summary: Claim an application for review
      description: |
        Stamps the reviewer and takes the application to `in_review`, which is
        what stops two people working the same case in parallel and finding
        out afterwards.

        **`If-Match` is what makes the claim exclusive.** Two reviewers who
        both opened the queue hold the same validator, and the second write
        finds `lock_version` moved and loses with a `409` naming the current
        state. Without it both would succeed and the last one would silently
        own the case.

        There is no unclaim operation, and that absence is deliberate: the
        state machine has no route from `in_review` back to `submitted`,
        because "I picked this up and put it down again" is not a state the
        applicant should watch their application oscillate through. A reviewer
        who cannot finish hands over by asking for information, or by
        deciding.

        Requires `b2b_application.review_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The claimed application.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationReviewOnlyEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bApplicationStateInvalid'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/applications/{application}/request-information:
    post:
      operationId: requestB2bApplicationInformation
      tags:
        - B2B platform review
      summary: Hand the application back with a question
      description: |
        This is what makes `info_requested` worth being a state rather than a
        flag: editing rights go back to the applicant **for named sections
        only**, while the application keeps its place in the queue.

        The narrowing is the point. "We need your trade terms again" reopens
        the trade-terms fields and nothing else, so a company cannot use a
        question about payment as an opportunity to restate a legal name a
        reviewer has already checked against a certificate. Naming **no**
        sections is legitimate and means "we need documents, not answers" —
        nothing on the form reopens.

        `information_request` is prose and is required. What a reviewer needs
        is rarely a field, and a closed vocabulary would force every real
        question through an `other` that says nothing. The applicant reads it
        verbatim.

        The answer rejoins the queue as `submitted` rather than `in_review`:
        whether the same reviewer picks it up again is their decision.

        Requires `b2b_application.review_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestB2bApplicationInformationRequest'
      responses:
        '200':
          description: The application, now awaiting the applicant.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationReviewOnlyEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bApplicationStateInvalid'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/applications/{application}/approve:
    post:
      operationId: approveB2bApplication
      tags:
        - B2B platform review
      summary: Approve the application
      description: |
        **Approval records the decision and creates nothing.** The
        organisation, the trading account and the first invitations arrive
        from the provisioning operation, behind a third permission again. That
        split is the important one: an approval can be revisited and a
        provisioned tenant cannot be un-provisioned, so the person who signs
        off commercially need not be the person trusted to create tenants.

        Both notes are optional, and the asymmetry with a decline is
        deliberate: an approval is self-explanatory, and demanding a sentence
        would produce "approved" typed four hundred times.
        `internal_note` is never served to the applicant; `applicant_message`
        is what the company reads.

        Requires `b2b_application.decide_platform` — the judgement, and a
        **separate code** from working the queue.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApproveB2bApplicationRequest'
      responses:
        '200':
          description: The approved application.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationReviewOnlyEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bApplicationStateInvalid'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/applications/{application}/decline:
    post:
      operationId: declineB2bApplication
      tags:
        - B2B platform review
      summary: Decline the application
      description: |
        **`applicant_message` is required**, and a blank one is refused. An
        unexplained refusal is not a decision the company can act on: they
        cannot tell whether to correct a document, apply again next year, or
        stop trying. Thirty seconds of a reviewer's time is the difference
        between a decision and a wall.

        A decline is terminal and **distinct from a withdrawal**, which is the
        applicant's own exit. Collapsing them would make "how many did we turn
        down" unanswerable. Declining frees the applicant's one live slot, so
        a company that fixes what was wrong may start again.

        Requires `b2b_application.decide_platform` — the same authority as
        approving, because a refusal is as much a decision as an acceptance.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeclineB2bApplicationRequest'
      responses:
        '200':
          description: The declined application.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bApplicationReviewOnlyEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bApplicationStateInvalid'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/applications/{application}/provision:
    post:
      operationId: provisionB2bApplication
      tags:
        - B2B platform review
      summary: Turn an approval into a tenant
      description: |
        One transaction creates the corporate `Organisation`, opens the `b2b`
        `CustomerAccount`, stamps the links back onto the application and its
        agreements, and issues an invitation to every named contact that has
        an email address.

        **No memberships are created.** A membership is granted when a named
        person signs in and accepts their invitation, not when a reviewer
        clicks provision — creating them here would mint access for addresses
        nobody has proved control of, which is the exact failure the
        invitation token exists to prevent.

        **`Idempotency-Key` is mandatory.** Absent or blank is `400
        request.invalid` with `details.header`. A retry with no key against a
        command that creates an organisation, a trading account and a set of
        invitations would be unrecoverable rather than merely wasteful. A
        replay of the *same* request is answered from the stored envelope with
        `Idempotency-Replayed: true`; two genuinely different requests — a
        reviewer with two tabs open — are answered by the service, which finds
        the application already linked and returns the existing records.
        `invitations_issued` is `0` on that path, so a caller can tell a
        replay from fresh work.

        **No `If-Match`.** `If-Match` protects a lost update, and this command
        has no such shape: it refuses unless the status is `approved` and
        refuses to run twice at all. A validator would only stop a reviewer
        who had re-read the file in another tab.

        `200`, not `201`: several records were created and no one of them is
        *the* thing this operation made, so a `Location` header would have to
        pick one and lie about the rest.

        Requires `b2b_application.provision_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/IdempotencyKeyRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: What now exists.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/B2bProvisioningEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/B2bApplicationStateInvalid'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/applications/{application}/documents/{document}/download:
    get:
      operationId: downloadPlatformKycDocument
      tags:
        - B2B platform review
      summary: Open an applicant's identity document
      description: |
        Requires `kyc_document.view_platform`, which is **narrower than the
        application read on purpose**. A KYC pack is identity documents
        belonging to a named person, and being able to work a review queue is
        not by itself a reason to open one.

        `purpose` is required and goes straight onto the audit event. This is
        the operation that rule exists for: an applicant fetching back their
        own upload is unremarkable, and a member of platform staff opening a
        stranger's passport is the access an audit trail must be able to
        explain afterwards. Neither the object path nor the signed URL is
        recorded there.

        A URL in the envelope rather than a `302`, for the reason the
        applicant's operation gives.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/B2bApplicationPath'
        - $ref: '#/components/parameters/KycDocumentPath'
        - $ref: '#/components/parameters/KycAccessPurpose'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A short-lived signed URL and when it stops working.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KycDocumentDownloadEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/documents/{document}/review:
    post:
      operationId: reviewKycDocument
      tags:
        - B2B platform review
      summary: Accept or reject a document
      description: |
        **Two permissions, stacked**: `kyc_document.view_platform` *and*
        `b2b_application.review_platform`. Deciding whether a passport scan is
        acceptable needs both the authority to look at it and the authority to
        work the case; somebody who may open documents but not review
        applications is an auditor, and somebody who may review applications
        but not open documents cannot have formed a view about this one.

        The document is addressed **without its application in the path**,
        unlike every other document operation here: a verdict is about the
        file rather than about the file's owner.

        Accepting **supersedes** the previously accepted document of the same
        kind, so a company that re-uploads a clearer scan has one current
        registration document and the old row stays as the trail of what was
        looked at. A second verdict on the same document is `409` — a decision
        that could be quietly retaken is not a decision.

        A rejection must carry a `rejection_reason` from the closed
        vocabulary, because that half is what the applicant is shown and
        "please re-upload, it was illegible" is actionable where a reviewer's
        private note is not. `note` stays internal.

        No `If-Match`: `kyc_documents` carries no `lock_version`, and the
        already-reviewed conflict is a stronger guarantee than a validator —
        it refuses a second verdict outright rather than only a stale one.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/KycDocumentPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReviewKycDocumentRequest'
      responses:
        '200':
          description: The reviewed document, with the internal note.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KycDocumentReviewEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /organisations/{organisation}/invitations:
    post:
      operationId: createOrganisationInvitation
      tags:
        - Invitations
      summary: Invite somebody into an organisation
      description: |
        **The token is never in this response.** It is returned exactly once,
        to the mail that carries it; only its SHA-256 reaches the database. So
        a database read, a backup and a replica are all useless for accepting
        an invitation, and the platform genuinely cannot resend the original —
        re-inviting issues a new token and revokes the old, which is both the
        honest behaviour and the safe one.

        Issuing to an address that already has a live offer **supersedes** it
        rather than failing on the partial unique index: two live tokens for
        one seat means revoking one achieves nothing.

        The `{organisation}` in the path must be the one `X-Organisation-Id`
        selected; a mismatch is **404**, because "no such organisation, as far
        as you are concerned" is the honest answer and confirming that another
        tenant exists is not.

        Requires `membership.invite_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OrganisationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrganisationInvitationRequest'
      responses:
        '201':
          description: The invitation — without its token.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganisationInvitationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    get:
      operationId: listOrganisationInvitations
      tags:
        - Invitations
      summary: Who has been offered a place
      description: |
        **Accepted and revoked invitations are listed too**, and only the
        `status` filter narrows to the live ones. An administrator asking "who
        did we invite" needs the ones that were taken up and the ones that
        were withdrawn as much as the ones still outstanding.

        `status` is **derived, not stored** — an invitation is live, accepted,
        revoked or expired, and three of those four are already facts on the
        row. A stored column would be a fourth copy that has to be kept in
        step with a clock, and expiry would be the first thing to drift.

        Newest first: an invitation list is read to find something recent.

        Requires `membership.view_organisation`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OrganisationPath'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - name: status
          in: query
          required: false
          description: Restrict to one derived state.
          schema:
            $ref: '#/components/schemas/OrganisationInvitationStatus'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of invitations.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/OrganisationInvitation'
                  meta:
                    $ref: '#/components/schemas/PaginationMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /organisations/{organisation}/invitations/{invitation}:
    delete:
      operationId: revokeOrganisationInvitation
      tags:
        - Invitations
      summary: Withdraw an outstanding offer
      description: |
        **`DELETE`, but nothing is deleted.** `revoked_at` and `revoked_by`
        are stamped and the row stays, because who was invited and who
        withdrew it is exactly the trail an access review reads. The verb
        describes the effect the caller cares about — the offer is gone — and
        the record of it having existed is not theirs to remove. The retention
        job eventually clears *expired* offers, which are the ones holding an
        address for no remaining purpose; revoked and accepted rows are kept.

        Revoking an already-revoked invitation is a no-op that still answers
        `204`: the caller asked for the offer to be gone and it is gone, and
        making a retry fail would punish a client for a lost response.
        Revoking an *accepted* one is `409`, because the answer there is
        different work — remove the membership.

        Requires `membership.end_organisation` — the same authority as ending
        a membership, because withdrawing an offer somebody has not yet
        accepted and removing somebody who has are the same decision taken at
        two moments.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OrganisationPath'
        - $ref: '#/components/parameters/OrganisationInvitationPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '204':
          description: The offer is withdrawn. No body, per the envelope's documented exception.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /invitations/{token}:
    get:
      operationId: showInvitationByToken
      tags:
        - Invitations
      summary: What the link says, before deciding anything
      description: |
        **Anonymous — the token is the capability.** The mailed link lands
        somebody on a screen that has to render *before* it can ask them to
        sign in: "join Cedar Kitchen as an owner" is the reason to sign in, and
        requiring authentication first would be asking a person to sign in to
        something nobody has told them the name of. This is the same bargain
        the guest family makes with `X-Guest-Token`, with the token in the path
        because that is the shape the email delivered.

        **It reads and nothing else.** The token is not consumed, no state
        moves, and acceptance keeps every gate it had — `POST
        /invitations/{token}/accept` is still signed-in, email-verified and
        address-matched. Looking at an invitation brings it no closer to being
        spent.

        **The address comes back masked, and the masking is server-side.**
        `o•••@example.com`: the domain in full because that is the half that
        makes a person recognise their own address, the local part hidden
        because an unauthenticated endpoint that turns a link into a working
        email address is a harvesting endpoint. A client cannot render what it
        was never sent.

        **Only the invitation's own facts.** No organisation identifier, no
        branch, no inviter, no message — an anonymous endpoint that served them
        would answer questions about a tenant to anybody holding one link into
        it. The organisation is named, because a person deciding whether to
        join needs to know whose workspace it is, and `language_code` is its
        own default so the name renders in the right script and direction.

        **`status` reports the terminal states, deliberately.** A screen whose
        only possible message was "this link does not work" would send people
        to support to be told what the platform already knew, so `accepted`,
        `revoked` and `expired` are all said plainly. `pending` is `live` under
        the name a person waiting to accept uses for it. There is **no
        `superseded`**: re-inviting the same address revokes the outstanding
        offer and issues a new token, so a superseded invitation *is* a revoked
        one, and inventing a fifth state would describe something the row
        cannot be in.

        **Enumeration.** A token this platform never issued is `404`; so is one
        whose invitation has been purged, and the two are one response with one
        message. Reporting state for a *known* token is the line: a caller who
        gets that far is holding 256 bits the platform generated. The
        `invitation-lookup` limiter — twenty a minute per address rather than
        the usual sixty — is the floor under that argument, because this is the
        one anonymous read where a wrong answer is still an answer.
      security: []
      parameters:
        - $ref: '#/components/parameters/OrganisationInvitationTokenPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The invitation's own facts, with the address masked.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicInvitationEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /invitations/{token}/accept:
    post:
      operationId: acceptOrganisationInvitation
      tags:
        - Invitations
      summary: Take up an offer of membership
      description: |
        **Authenticated and email-verified, with no organisation context, and
        that absence is the entire point** — the acceptor is not a member of
        anything yet, so requiring a context would mean requiring the
        membership this request exists to create. They must be signed in so
        acceptance is attributable, and verified so the identity is reachable.

        **Acceptance now grants the membership.** B1 shipped this as a shell —
        the row was stamped and nothing else happened, because the membership
        write belongs to Organisations and AccessControl and B2B cannot call
        across that edge. PA1 supplied the write, so `membership_created` is
        `true` on a normal acceptance and `membership` carries the new
        membership's identifier. Both keys were present all along precisely so
        the wire shape would not have to change when the write arrived, and
        both still report `false`/`null` when nothing was granted — which is
        the point of reporting them at all. A response that quietly implied a
        provisioned member would let a client show somebody a workspace they
        cannot enter.

        `membership` is `{ "id": "…" }` rather than the full membership: what
        the acceptor needs next is `GET /me`, which returns their memberships
        hydrated with roles, scope and permissions. Repeating a partial copy
        here would be a second answer to that question.

        **Every failure is the same `404`** — wrong token, expired, revoked,
        already accepted. Distinguishing them would tell somebody holding a
        guessed token that they guessed right.

        **The one exception is a token offered by the wrong person**, which
        answers `403 authz.permission_denied`. The signed-in user's own email
        must be the address the invitation was sent to. There is nothing left
        to conceal on this path — the caller already holds a valid token, so a
        `404` would hide nothing and would send somebody looking for a broken
        link — and "sign in as the person this was sent to" is the only useful
        thing to say.

        The token is a path segment because that is what the emailed link
        carries. The cost is real and worth naming — path segments reach
        access logs, referrer headers and browser history in a way bodies do
        not — and what makes it tolerable is that the token is single-use and
        consumed here, short-lived, stored only as a digest, and useless
        without an authenticated session.
      parameters:
        - $ref: '#/components/parameters/OrganisationInvitationTokenPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The accepted invitation, and an honest statement of what it granted.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcceptedInvitationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/InvitationAcceptanceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/organisations/kitchens:
    get:
      operationId: listPlatformKitchens
      tags:
        - Platform administration
      summary: Every kitchen on the platform
      description: |
        **Newest first**, unlike the B2B review queue. That queue is work to be
        done and the fair order is arrival; this is an inventory, and the
        kitchen an operator is looking for is overwhelmingly the one somebody
        just created or just complained about.

        `status` accepts any organisation status, **including `closed`**.
        Offboarding does not currently produce closed kitchens — it winds up
        corporate customers — but narrowing the vocabulary to the subset that
        happens to be reachable today would be a rule to unpick later rather
        than a safeguard.

        `query` matches the name and the slug, case-insensitively. Those are
        the two things somebody holds when they arrive from a support ticket.

        Each row carries its owner list's *size* and its catalogue counts
        rather than the rows behind them, so a page of twenty kitchens is three
        grouped queries rather than sixty. The read is audited as a classified
        access: a list of kitchens is unremarkable, and the owner names and
        addresses that hang off each one are personal data.

        Requires `organisation.manage_platform` inside the platform-operator
        organisation.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/CursorLimit'
        - $ref: '#/components/parameters/Cursor'
        - name: status
          in: query
          required: false
          description: |
            Restrict to one organisation status. Omitted, every kitchen is
            listed whatever state it is in — the console's whole job is the
            suspended ones.
          schema:
            type: string
            enum:
              - active
              - suspended
              - pending
              - closed
        - name: query
          in: query
          required: false
          description: Case-insensitive substring match over the kitchen's name and its slug.
          schema:
            type: string
            maxLength: 160
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: A page of kitchens, newest first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                type: object
                required:
                  - data
                  - meta
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PlatformKitchenSummary'
                  meta:
                    $ref: '#/components/schemas/PaginationMeta'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      operationId: createPlatformKitchen
      tags:
        - Platform administration
      summary: Bring a kitchen into existence
      description: |
        **`Idempotency-Key` is mandatory**, on the same terms as B2B
        provisioning. A tenant cannot be un-created, and a retried request that
        produced a second kitchen would be discovered by whoever went looking
        for the first one. The unique slug would refuse an exact duplicate, but
        only after the branch and the channel had been written and rolled back,
        and only with a validation error where a replay belongs. Absent or
        blank is `400 request.invalid`.

        **No `If-Match`.** Nothing existing is written, so there is no lost
        update for a validator to protect against.

        The kitchen is created `active`, not `pending`. A kitchen created by
        the platform has already been decided about — the decision *is* the
        console click — so there is nothing for `pending` to mean here; the
        status that would be earned by a self-service signup queue belongs to
        that queue, if it ever exists.

        `201` carries the same body the single-kitchen read returns, so a
        console can navigate straight to the new kitchen without a second
        round trip, and the `ETag` it needs for the first lifecycle write comes
        with it.

        Requires `organisation.manage_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/IdempotencyKeyRequired'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePlatformKitchenRequest'
      responses:
        '201':
          description: The new kitchen, in full, with its validator.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformKitchenEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '422':
          $ref: '#/components/responses/PlatformKitchenSlugTaken'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/organisations/kitchens/{organisation}:
    get:
      operationId: getPlatformKitchen
      tags:
        - Platform administration
      summary: One kitchen, in full
      description: |
        Everything the console shows on a kitchen's own screen: the counts, the
        active owner memberships with the names and addresses behind them, the
        branches, and — when it is suspended — the note and the operator who
        wrote it.

        **Carries an `ETag` holding the organisation's `lock_version`**,
        because the two lifecycle actions on this resource demand an
        `If-Match`. A client that could not obtain a validator from the read
        would have no way to send one on the write, which is the failure the
        precondition exists to prevent rather than to cause.

        An organisation that exists but is **not** a kitchen answers `404`, not
        `403`: the kitchen console is not the place to confirm the existence of
        clinics. A malformed identifier is the same `404` rather than a driver
        error.

        Requires `organisation.manage_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PlatformKitchenPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The kitchen, its owners and its branches.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformKitchenEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/organisations/kitchens/{organisation}/suspend:
    post:
      operationId: suspendPlatformKitchen
      tags:
        - Platform administration
      summary: Withdraw a kitchen from trading
      description: |
        **What suspension actually does**, stated here because the effects live
        at half a dozen seams and a caller should not have to find them:

        - The kitchen and its meals and plans **vanish from every public
          marketplace read**. An anonymous browser sees no trace of it.
        - **Subscription quoting, order placement and subscription-composed
          orders refuse** with `409 organisation.suspended`, carrying
          `details.organisation_id`. A suspended kitchen that carried on taking
          orders because only the shop window checked is exactly the failure
          this replaced.
        - The kitchen's **own staff keep read access to their workspace** —
          they sign in, they read their recipes, their orders and their ops
          screens, and they read the banner explaining why the publish button
          now refuses. But **catalogue, recipe, pricing, plan and
          delivery-zone writes refuse with the same code**. A tenant locked out
          entirely cannot be told anything, which is why this is a route gate
          on the writing half rather than a step in the permission decision.
        - **Day-to-day operations writes stay open**: inventory, goods
          receipts, production, quality control and the kitchen display. This
          stops a kitchen selling; it does not stop it finishing the food it
          already owes people, and a stock count that refuses is a stock count
          that gets written on paper instead.

        `reason` is **optional prose, not a code vocabulary**. Requiring one
        would produce reasons — "n/a", "see ticket", "asked to" — and a column
        full of those is worse than a column full of nulls because it looks
        like it has been filled in. A closed vocabulary would be worse still:
        no shortlist survives contact with the things people actually need to
        write here, and every real case would end up behind an `other` that
        says nothing. The operator who has something to say says it; the audit
        entry records the decision either way.

        **`If-Match` is required.** Two operators sharing a console is the
        ordinary case, and last-write-wins on "may this business trade" is not
        a defensible way to settle it. Absent is `428`; stale is `409
        resource.conflict` carrying `details.current_lock_version`.

        Only an `active` kitchen can be suspended; anything else is `409` with
        `details.status` naming what was found. `closed` in particular is not
        reachable from here — a closed organisation has been offboarded, and
        suspending one would suggest the state is recoverable.

        Requires `organisation.manage_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PlatformKitchenPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuspendPlatformKitchenRequest'
      responses:
        '200':
          description: The suspended kitchen, and its new validator.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformKitchenSummaryEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PlatformKitchenLifecycleConflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/organisations/kitchens/{organisation}/reactivate:
    post:
      operationId: reactivatePlatformKitchen
      tags:
        - Platform administration
      summary: Let a kitchen trade again
      description: |
        The twin of suspend, and a separate endpoint rather than the same one
        with a different payload — two decisions with different consequences
        and different audit entries should not be one request with a different
        string in it.

        **No request body at all.** Reactivating clears the suspension note,
        so asking for a reason to un-suspend would invite somebody to write the
        counter-argument into a column the next suspension wipes. Why a kitchen
        was let back in belongs to the audit log, which keeps it.

        A `pending` kitchen — one created here and never handed to an owner —
        reactivates to `active` the same as a suspended one, because "let this
        trade" is the same decision either way. It is only the *suspending*
        side that insists on `active`.

        **A `closed` organisation cannot be reactivated**, and that is `409`
        rather than a quiet success. Closing is offboarding: memberships were
        ended, the agreement terminated and personal data purged. Bringing the
        row back to `active` would produce a tenant nobody can sign into and a
        history that no longer exists — the honest answer is that this is a new
        kitchen's worth of work, not a status change.

        A kitchen that is already `active` is also `409`, with
        `details.status`. **`If-Match` is required**, for the reason suspend
        gives.

        Requires `organisation.manage_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PlatformKitchenPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The kitchen, trading again, with its new validator.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformKitchenSummaryEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/PlatformKitchenLifecycleConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/organisations/kitchens/{organisation}/owners/invitations:
    post:
      operationId: invitePlatformKitchenOwner
      tags:
        - Platform administration
      summary: Offer somebody ownership of a kitchen
      description: |
        **There is no `role_code` field, and its absence is the whole shape of
        this endpoint.** The role is always `organisation_owner` and is not a
        caller's choice. `POST /organisations/{organisation}/invitations`
        already exists for a member of an organisation to invite anybody to any
        role inside it; this is a different act — the platform naming the
        person who will run a tenant. Letting the caller pick the role would
        make this the general endpoint again, with a platform gate in front of
        it for no reason.

        `organisation_owner` is a platform template role every organisation
        shares, so nothing has to be created for a brand-new kitchen before its
        owner can be invited.

        **Re-inviting the same address supersedes the outstanding offer rather
        than adding a second.** The live invitation is revoked inside the same
        transaction that issues the new one, because two live tokens for one
        seat means revoking one achieves nothing. So this is safe to press
        twice, which is what an operator does when somebody says they never got
        the email.

        **`mailed` reports honestly whether the email went out.** A transport
        failure does not lose the invitation — the row and its token exist, and
        re-inviting supersedes cleanly — but it does mean nobody has been told,
        and a console that showed an unqualified success would leave an
        operator waiting for a reply to a message that was never sent. It is
        deliberately not an error: failing the request would imply the
        invitation had not been created, and the operator would retry and
        wonder why the list had two entries.

        **The token is not in this response and never may be.** It is returned
        exactly once, to the mail that carries it; only its SHA-256 reaches the
        database.

        `name` is used only in the email greeting and is **never stored**. The
        person's real name arrives with their profile when they accept, and a
        name typed by an operator into an invitation form would be a second,
        worse copy of it.

        Requires `organisation.manage_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PlatformKitchenPath'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvitePlatformKitchenOwnerRequest'
      responses:
        '201':
          description: The invitation — without its token — and whether the mail was sent.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformKitchenOwnerInvitationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/organisations/kitchens/{organisation}/owners/{membership}/revoke:
    post:
      operationId: revokePlatformKitchenOwner
      tags:
        - Platform administration
      summary: End an owner's membership
      description: |
        **`POST .../revoke` rather than `DELETE .../owners/{membership}`,
        because nothing is deleted.** The membership becomes `ended` — the same
        terminal state a business-access revocation writes — and the row
        survives as the record that this person was an owner. The audit entry
        beside it refers to something that has to still be there.

        **The last owner may be revoked.** A kitchen whose only owner has left,
        or whose owner is the reason the platform is intervening, has to be
        able to have that membership ended before a replacement exists; a rule
        that refused would leave the console unable to do the one thing it was
        opened to do. The platform acts deliberately, and a guard rail here
        would be a policy pretending to be a safety feature.

        So `remaining_owners` comes back instead, and the console warns rather
        than the API refusing: report the consequence, and let the operator
        decide.

        The revoked owner's tokens are **not** destroyed. Somebody who stops
        running one kitchen has not stopped existing — they may shop on the
        platform, they may run another kitchen — and signing them out of all of
        it is a punishment the platform has not decided to hand out. Their
        permission version is bumped, which removes this organisation from
        their context immediately, and that is the actual requirement.

        A membership that does not belong to **this** kitchen is `404`, never
        `403`. One that has already ended is `409` with `details.status`.

        Requires `organisation.manage_platform`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/PlatformKitchenPath'
        - $ref: '#/components/parameters/PlatformKitchenMembershipPath'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The ended membership, and how many owners this kitchen has left.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformKitchenOwnerRevocationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ResourceConflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /subscriptions:
    post:
      operationId: createSubscription
      tags:
        - Subscriptions
      summary: Start a subscription
      description: |
        Buying twenty days of food.

        **Not under `/me`, and the asymmetry with `GET /me/subscriptions` is the
        one `/carts` and `/me/orders` already draw**: a creation names no
        existing resource of the caller's, while a list of standing arrangements
        is a *collection of mine*.

        **`Idempotency-Key`, exactly as `POST /orders` carries it**, and this is
        the more expensive of the two: a double tap on a slow connection produces
        a second standing arrangement and the customer finds out when twice the
        food starts arriving every morning. A replay repeats the original
        response exactly — `201` again, with `Idempotency-Replayed: true`. The
        key is optional for the reason it is everywhere: refusing unkeyed
        requests would break every caller that has ever worked, to protect them
        from a risk they may not have.

        The refusal carries **every** reason at once, and the price is resolved
        and captured server-side — there is no field a client could quote a price
        in.
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSubscriptionRequest'
      responses:
        '201':
          description: The subscription, with its captured price and its opening balance.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /subscriptions/quote:
    get:
      operationId: quoteSubscription
      tags:
        - Subscriptions
      summary: What a plan configuration would cost
      description: |
        A quote and not a hold: nothing is reserved and nothing is written.

        **Authenticated rather than anonymous**, which is the one thing about
        this operation worth arguing over. `price_list_items` is the single
        catalogue table with a row-level-security policy, on the ground that a
        negotiated price says what a kitchen will accept and from whom. The
        marketplace's plan pages already carry a *published consumer* price
        through the public projection, which is a different number reached a
        different way; serving this resolver anonymously would hand anybody a
        probe for every tariff a kitchen has attached to a channel, one
        configuration at a time.

        A refusal is a `409`, not an empty `200`: `unpriced`,
        `duration_not_offered`, `duration_ambiguous`, `duration_not_fixed`,
        `plan_not_subscription` and `pricing_basis_unsupported` are all facts
        about the world rather than about the request's shape.
        `pricing_basis_unsupported` names itself deliberately — a per-week plan
        is refused on principle, and collapsing it into "unpriced" would send a
        kitchen hunting for a tariff row that is not missing.

        **Reachable from what the storefront publishes.** The operation used to
        take a `sales_channel_id` and a `plan_duration_id`, and a shopper could
        obtain neither: sales channels live behind `/catalogue/sales-channels`,
        a tenant surface, and the public plan read publishes durations as
        `{code, kind, days}` with no identifier. Both are resolved server-side
        now. The channel comes from the plan's own kitchen and is restricted to
        its *consumer* channels, which is narrower than the validator it
        replaced — a `b2b`, `corporate` or `insurance` tariff can no longer be
        probed through this operation at all. The run is named by its number of
        days, the fact the public projection already carries; nothing was added
        to that projection to make this work.

        `available_weekdays` is the point of the enrichment. Until it existed a
        configurator learnt which days a plan delivers on by pricing the same
        subscription seven times and reading which answers carried a warning.
      parameters:
        - name: catalogue_item_id
          in: query
          required: true
          description: The plan.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: catalogue_item_variant_id
          in: query
          required: true
          description: The configuration — the matrix cell.
          schema:
            $ref: '#/components/schemas/Uuid'
        - name: plan_duration_days
          in: query
          required: true
          description: |
            The run being priced, in delivery days — `MarketplacePlanDuration.days`
            from the public plan read. Days rather than the kitchen's own
            duration `code`, because the code is an authored slug and the day
            count is the fact; two offered runs sharing a day count are a
            configuration mistake and are refused `duration_ambiguous` rather
            than guessed between.
          schema:
            type: integer
            minimum: 1
            maximum: 400
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The current quote for this configuration on this run.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanQuoteEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '409':
          $ref: '#/components/responses/SubscriptionRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions:
    get:
      operationId: listMySubscriptions
      tags:
        - Subscriptions
      summary: The caller's own standing arrangements
      description: |
        **Unpaginated, and unlike the order history that is not a shortcut.** A
        person holds one subscription, or two, or a handful across kitchens; the
        collection has a natural ceiling that order history does not, and a
        cursor on a list nobody can fill would be a parameter to document,
        version and test in exchange for nothing.

        **Live ones first, then the rest by recency.** Somebody opening this
        screen is nearly always looking at what is running now.

        **No `balance` on the list rows.** Computing it counts skipped days per
        subscription, which is a query per row; the list carries
        `remaining_days` and `next_delivery_date` from columns the row already
        holds, and the single read carries the full balance.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every subscription the caller holds, live ones first.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionsEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}:
    get:
      operationId: showMySubscription
      tags:
        - Subscriptions
      summary: One of the caller's own subscriptions, with its balance
      description: |
        Somebody else's is `404 resource.not_found`, never 403: the row carries a
        delivery address, a weekday pattern and a price somebody negotiated, and
        confirming that a guessed identifier names a real standing arrangement is
        a disclosure on its own.

        Carries an `ETag`. Nothing requires the client to send it back — the
        writes accept `If-Match` rather than demanding it, because a subscription
        has one author and the race would have to be run by a customer against
        themselves — but a client holding two tabs open cannot protect itself
        with a validator it was never given.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The subscription and its balance.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/pause:
    post:
      operationId: pauseSubscription
      tags:
        - Subscriptions
      summary: Stop the deliveries, keep the days
      description: |
        A POST sub-resource action, never a `PATCH status`. Pausing, resuming and
        cancelling are three decisions with three consequences, three timestamps
        and three journal events; a status field would collapse them into one
        write a client could aim anywhere.

        **Nothing here consumes a balance day**, and the response carries the
        balance so the screen can say so rather than implying it. A paused
        subscription has no `next_delivery_date` at all.

        The refusal a customer most often sees is `inside_cut_off`: the next
        delivery is nearer than the plan's window allows, so it proceeds and the
        pause takes effect after it.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The paused subscription and its untouched balance.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionChangeRefused'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/resume:
    post:
      operationId: resumeSubscription
      tags:
        - Subscriptions
      summary: Start again from the first day a change can still reach
      description: |
        The response's `next_delivery_date` is the whole answer, and is why this
        is not a `PATCH status`: resuming does not restart from tomorrow, it
        restarts from the first weekday the subscription delivers on that is
        still outside the plan's change window — because a resume producing a
        delivery the customer had no chance to stop would be worse than one that
        waited a day.

        `exhausted` is the refusal worth knowing about: a spent balance cannot be
        resumed, and the answer is a renewal rather than a change.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The resumed subscription, with the recomputed next delivery date.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionChangeRefused'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/cancel:
    post:
      operationId: cancelSubscription
      tags:
        - Subscriptions
      summary: End a subscription and record what is owed
      description: |
        **The credit memo comes back in the same response, and that is the point
        of the operation rather than a convenience.** The refund is recorded as
        an obligation for manual settlement — no money moves — and a customer
        who cancels twelve unused days of a plan they paid for is owed a number
        on the screen at that moment. A client that had to make a second call
        would render "cancelled" with nothing beside it, which reads as "and that
        is that".

        `credit_memo` is **null** when nothing is owed, which is not the same as
        an empty object: a zero memo would be an obligation somebody eventually
        tries to settle.

        There is no DELETE. A cancelled subscription keeps its captured price and
        its history, because the memo multiplies the price it actually paid and a
        deleted row could not explain the number.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelSubscriptionRequest'
      responses:
        '200':
          description: The cancelled subscription and the credit memo, or null when nothing is owed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionCancellationEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionChangeRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/skips:
    post:
      operationId: skipSubscriptionDay
      tags:
        - Subscriptions
      summary: Skip one delivery day
      description: |
        **A sub-collection rather than an action, because a skip is a thing that
        exists afterwards**: it creates a delivery row for the named day with
        `status: skipped_customer` and `consumed: false`, which the ledger then
        shows. `201` for the same reason.

        **It costs nothing, and the response proves it** — the balance comes back
        beside the created day so the screen can show `remaining_days` unchanged.

        The row is written *before* the day is ever generated, which is what
        makes the unique index on `(subscription_id, delivery_date)` the thing
        that stops generation producing an order for a day already skipped.
        Skipping a settled day is `already_settled` rather than a silent no-op:
        the food is coming and the customer needs to know.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SkipSubscriptionDayRequest'
      responses:
        '201':
          description: The skipped day, and the unchanged balance beside it.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionSkipEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionChangeRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/address:
    put:
      operationId: updateSubscriptionAddress
      tags:
        - Subscriptions
      summary: Move a subscription to another of the caller's addresses
      description: |
        **A `PUT` on a named sub-resource rather than a `PATCH` on the
        subscription**, and the three of address, window and weekdays are
        separated because each asks a different question of the world. Changing
        the address re-runs the delivery map — the kitchen may not go to the new
        area at all — while changing the window asks nothing of it. A single
        `PATCH` accepting all three would have to run every check on every call,
        and a client changing a delivery slot would be refused because it moved
        house in the same body.

        **The subscription follows the customer; the orders already placed do
        not.** Each generated order snapshots the address at placement, so
        somebody who moves has tomorrow's delivery follow them while last week's
        still says where it went.

        `area_not_served` and `zone_suspended` are worth rendering distinctly:
        the first means nobody delivers there, the second that this kitchen's
        zone is suspended and somebody else might.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSubscriptionAddressRequest'
      responses:
        '200':
          description: The subscription, now delivering to the named address.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionChangeRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/window:
    put:
      operationId: updateSubscriptionWindow
      tags:
        - Subscriptions
      summary: Change which slot the food arrives in
      description: |
        The lightest of the three replacements: it asks nothing of the delivery
        map and nothing of the plan's rights, only whether the next delivery is
        still changeable.

        `null` is a legal value and means "no preference"; the field is required
        and nullable so that both saying and unsaying are expressible.

        The code is **not** checked against the branch's configured windows.
        Which slots a kitchen offers on which weekdays is scheduling data that
        changes without reference to standing subscriptions, and validating here
        would turn a kitchen retiring a slot into a refusal on somebody else's
        unrelated edit.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSubscriptionWindowRequest'
      responses:
        '200':
          description: The subscription, with its new delivery window.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionChangeRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/weekdays:
    put:
      operationId: updateSubscriptionWeekdays
      tags:
        - Subscriptions
      summary: Change which weekdays the subscription delivers on
      description: |
        A replacement rather than a merge: dropping Wednesday means no longer
        wanting Wednesday, and a merge cannot express a removal.

        **The generation cursor is recomputed, and the response is where a client
        sees it.** The old one may name a day the subscription no longer delivers
        on, and a cursor pointing at a day that will never come is a subscription
        that silently stops.

        **The balance does not change.** Fewer weekdays means the same number of
        days spread over more calendar time, not fewer days.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSubscriptionWeekdaysRequest'
      responses:
        '200':
          description: The subscription, with the new weekday set and the recomputed next delivery date.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionChangeRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/choices:
    put:
      operationId: replaceSubscriptionChoices
      tags:
        - Subscriptions
      summary: Choose ahead on a Free Selection day
      description: |
        **One day per call, named in the body rather than in the path.** A date
        is not an identifier of anything — the delivery row usually does not
        exist yet, which is the whole point — so `.../choices/{date}` would be a
        path segment that looks like a resource and is not one.

        **An empty `meals` array is a real payload**: it is a customer saying "I
        have not chosen; send me the kitchen's default".

        Only `customer`-source rows are replaced. A `substituted` row is
        generation's record that something chosen was unsafe and what went
        instead, and letting a later `PUT` delete it would erase the substitution
        audit — the one audit an allergy complaint needs most. A day that has
        already been generated is refused outright rather than partially
        rewritten.

        **No allergen verdict in the response, and that is deliberate.** Safety
        is checked at generation against the customer's declarations as they
        stand *then*; answering "this is safe" here would be a verdict that goes
        stale the moment somebody adds an allergy, and a client once told "safe"
        would have no reason to ask again.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/IfMatch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceSubscriptionChoicesRequest'
      responses:
        '200':
          description: The day's chosen meals as recorded.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionChoicesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SubscriptionChangeRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/subscriptions/{subscription}/deliveries:
    get:
      operationId: listSubscriptionDeliveries
      tags:
        - Subscriptions
      summary: The subscription's ledger
      description: |
        **The rows that exist, and only those.** Deliberately *not* the
        customer's version of the kitchen's projection: it lists real delivery
        days — generated, skipped, delivered — newest first. What is *coming* is
        `next_delivery_date` plus the weekday pattern, which the client already
        holds; serving projections here would put days into somebody's ledger
        that nothing has committed to, and the first support question would be
        why a delivery "disappeared" when the plan was paused.

        **The meal choices ride along**, carrying `source` and
        `replaced_catalogue_item_id`: the substitution promise is worth nothing
        if the only place it is recorded is an audit table.

        `unsafe_allergen_classes` is not served — it is health data, and what a
        customer needs from this screen is that a substitution happened, not a
        restatement of their medical profile on a delivery row.
      parameters:
        - $ref: '#/components/parameters/SubscriptionPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: Every delivery day this subscription has, newest first, with the balance in `meta`.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionDeliveriesEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /catalogue/subscription-schedule:
    get:
      operationId: listSubscriptionSchedule
      tags:
        - Subscriptions
      summary: The kitchen's forward book of standing arrangements
      description: |
        **The operation that makes one-day-ahead generation affordable.** A
        single delivery is generated ahead so the order book stays truthful, and
        the obvious objection is that a kitchen then cannot see next week. It
        sees next week here: the delivery rows that exist, laid over the weekday
        patterns of every active subscription, bounded by each one's remaining
        balance. **Nothing on this path writes anything** — a projection that
        materialised rows would be the phantom orders the design exists to avoid,
        and would have to be un-materialised on every skip, pause and
        cancellation.

        **Every row says whether it is `actual` or `projected`**, and a kitchen
        planning production must never confuse them: an actual day has an order
        behind it, and a projected one is a customer who has not yet had the
        chance to skip. `meta.daily_counts` is the number a production plan is
        built from, with skipped and cancelled days already excluded.

        `subscription.view_organisation` — not `order.view_organisation`, because
        a subscription is a standing commercial arrangement with a captured price
        and reading today's order list is not by itself a reason to see who is
        committed to what and for how long; and not
        `plan.manage_organisation`, because a production planner needs to read
        the schedule without being able to change what the kitchen sells.

        Organisation-scoped, not branch-scoped: a manager with an
        organisation-wide membership selects no branch, and an operation reading
        `X-Branch-Id` would show them nothing until they picked one.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/SubscriptionScheduleFrom'
        - $ref: '#/components/parameters/SubscriptionScheduleTo'
        - $ref: '#/components/parameters/SubscriptionScheduleBranch'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The window's deliveries, actual and projected, with the daily counts in `meta`.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionScheduleEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/closure-requests:
    post:
      operationId: openClosureRequest
      tags:
        - Account closure
      summary: Ask to stop the marketing, or to be forgotten
      description: |
        **One operation for both scopes, because it is one screen.** Somebody who
        has decided to stop hearing from a service is frequently deciding whether
        to stop being a customer, and making them find two different pages is how
        a platform ends up with an opt-out nobody could find and a closure nobody
        meant.

        **The status says which half happened.** `marketing_opt_out` comes back
        `200` already `completed` — the consents are withdrawn and the
        suppression written before the response returns, because the expectation
        is that "stop emailing me" has taken effect by the time the page reloads.
        `full` comes back `202`: accepted, passcode in flight, nothing erased.

        **A blocked request is a `202`, not an error.** `blocked` and `blockers`
        are fields in a successful body and the request survives so somebody can
        come back when their last order has arrived. No passcode is sent for a
        request that cannot proceed — asking somebody to prove their identity for
        an act already decided against is friction for nothing.

        **No step-up middleware**, and this is the most destructive act a
        customer can perform. The journey carries a stronger proof of its own: a
        passcode sent to the account's verified destination and bound to *this
        request* on its row. A purpose-scoped step-up flag would stop a
        contact-verification code unlocking a closure and would not stop *a*
        closure code finalising *another* one.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OpenClosureRequestRequest'
      responses:
        '200':
          description: A marketing opt-out, already completed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClosureAcknowledgementEnvelope'
        '202':
          description: A full closure, accepted. A passcode is in flight, or the request is blocked and says so.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClosureAcknowledgementEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '409':
          $ref: '#/components/responses/ClosureRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/closure-requests/live:
    get:
      operationId: showLiveClosureRequest
      tags:
        - Account closure
      summary: The closure request in flight, and what stands in its way
      description: |
        **`/live` rather than an index, because there is at most one.** A
        collection would be a list that is always empty or a singleton, and every
        client would have to write `[0]`. Completed and cancelled requests are
        deliberately not listed at all: a history of somebody's previous attempts
        to leave is not something the platform has a reason to hand back.

        **`closure_request` is null when nothing is in flight, and `blockers` is
        filled in regardless.** That pairing is the operation: a closure screen
        has to render *before* anybody asks, and re-evaluating on render is what
        makes it truthful — a subscription started this morning is exactly what a
        verdict cached on the request row would miss.

        The verdicts include `advisory` ones, so a client must branch on `status`
        rather than on the presence of a row. `blocked` summarises the `blocking`
        ones alone.
      parameters:
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The live request if there is one, and the freshly evaluated blockers either way.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiveClosureRequestEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/closure-requests/{closureRequest}/verify:
    post:
      operationId: verifyClosureRequest
      tags:
        - Account closure
      summary: Prove a closure with the passcode
      description: |
        **The authenticated user is the actor, always**, and that is not
        decoration: the server refuses when the actor is not the person the
        request is about. Support may *open* a closure on somebody's behalf and
        may never finish one, because staff who could do both would be staff who
        can erase anybody.

        **There is no separate challenge operation.** The passcode is issued when
        the request is opened and bound to it on the row, which is what stops a
        code obtained for one closure finalising another. A client that needs a
        fresh code uses `POST /verification/challenges/{challenge}/resend`; a
        second issuing surface here would be a second place for the binding to be
        got wrong.

        **A blocked request comes back `200` and stays `verified`.** The person
        proved who they were, and making them do it again after their last order
        arrives would be punishing them for the platform's scheduling.

        Otherwise the request moves to `scheduled` and finalisation is queued for
        the moment the grace window closes — zero-length by default.
        `scheduled_for` is the moment after which there is nothing left to
        cancel.

        Every way of failing the passcode — mistyped, expired, superseded, locked
        out — answers `422 otp.invalid` with nothing that tells them apart.
      parameters:
        - $ref: '#/components/parameters/ClosureRequestPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyClosureRequestRequest'
      responses:
        '200':
          description: The proven request — scheduled, or still verified and blocked.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClosureAcknowledgementEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ClosureRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /me/closure-requests/{closureRequest}:
    delete:
      operationId: cancelClosureRequest
      tags:
        - Account closure
      summary: Call off a closure request
      description: |
        **A `DELETE` that stamps rather than removes.** The row is marked
        `cancelled` with `cancelled_because`, and who asked to leave and then
        changed their mind is exactly the trail a data-protection enquiry reads.
        Nothing is erased by cancelling an erasure.

        **It answers with the row rather than `204`.** Somebody calling off an
        account deletion needs to see that it is off, not an empty body.

        Legal up to and including `scheduled`. After finalisation there is
        nothing left to cancel — the data is gone — and offering it would be an
        undo that does not exist.
      parameters:
        - $ref: '#/components/parameters/ClosureRequestPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The cancelled request.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CancelledClosureRequestEnvelope'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/EmailUnverified'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ClosureRefused'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/customer-accounts/{account}/closure-requests:
    post:
      operationId: openClosureRequestForCustomer
      tags:
        - Account closure
      summary: Open a closure on a customer's behalf
      description: |
        Support opening a closure — a phone call, an accessibility need.

        **Support may open one and may never finish one**, and that asymmetry is
        the entire reason this is separate from the customer's own operation.
        There is no platform `verify` route and there never will be: the passcode
        goes to the *customer's* verified destination, the server refuses a
        support actor entering it, and the customer's inbox is the second factor
        — the only one they have.

        **Two gates.** `platform.context` asserts the selected organisation *is*
        the platform operator; `customer_account.close_platform` asserts the
        member holds the code. A tenant that somehow acquired the permission
        still cannot reach this, because an organisation type is not something a
        tenant can grant itself — and this is the one operation on the platform
        that starts the deletion of a named person's data.

        **Keyed on the customer account rather than the user**: an agent is
        looking at an account, and an operation taking a `users` identifier would
        make "which person is this login" a lookup against a table they have no
        other reason to read. `b2c` only — a corporate account belongs to the
        company, outlives the individual who opened it, and is offboarding's to
        end. An account with no owner is `422`: closure is about an identity, and
        a guest has their own deletion journey.

        The actor is recorded and every audit row carries
        `purpose_of_use = support` rather than `self_service`.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - name: account
          in: path
          required: true
          description: The customer account whose owner is being closed.
          schema:
            $ref: '#/components/schemas/Uuid'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OpenClosureRequestRequest'
      responses:
        '200':
          description: A marketing opt-out, already completed.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClosureAcknowledgementEnvelope'
        '202':
          description: A full closure, accepted. The passcode has gone to the customer.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClosureAcknowledgementEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/ClosureRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings:
    post:
      operationId: startOffboarding
      tags:
        - B2B offboarding
      summary: Serve notice and open a wind-up
      description: |
        **Platform-driven, with no organisation-side variant.** A tenant does not
        decide that its own trading relationship ends. An organisation *request*
        to be offboarded is a real future need and is deferred rather than
        approximated: it needs a state before `notice_served` and a human
        decision at the platform, and an operation that let a company put itself
        straight into `notice_served` would look like the same thing while ending
        its own trading with nobody in the loop.

        **`201`, and what it creates is the wind-up rather than the ending.**
        Notice is served, `effective_on` is computed once from the agreement's
        own `notice_period_days` and copied onto the row, and nothing has been
        taken away from anybody. Every subsequent step is a separate deliberate
        act, because revocation ends people's access to a system they use for
        work.

        An organisation already being wound up is refused here as well as by the
        partial unique index, so an operator gets a sentence rather than a
        constraint violation.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartOffboardingRequest'
      responses:
        '201':
          description: The wind-up, at `notice_served`, with its computed effective date.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OffboardingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/OffboardingRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}:
    get:
      operationId: showOffboarding
      tags:
        - B2B offboarding
      summary: Where a wind-up has got to
      description: |
        **The states exist so an operator can see where a wind-up has stalled**,
        which is the whole reason nothing cascades. So the shape carries every
        stage's timestamps rather than a single `status` and a guess.

        `allowed_transitions` travels with the row: a screen renders buttons, and
        a client that had to reimplement the state machine to know which ones to
        enable would be a second copy of it — the one that disagrees after the
        first change.

        The `ETag` is served, and none of the actions demands `If-Match`: the
        transitions are guarded by the state machine, which is a stronger
        guarantee than "only if nobody touched it since you looked" —
        `signed_off` to `signed_off` is illegal whatever validator a caller
        holds.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The wind-up in full.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OffboardingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/settlement-checks:
    post:
      operationId: runOffboardingSettlementChecks
      tags:
        - B2B offboarding
      summary: Run every settlement check and record what each found
      description: |
        **A sub-collection rather than an action, because re-running is the
        normal case.** The company pays, somebody runs the checks again, and
        `settlement_pending` to `settlement_pending` is a legal transition for
        exactly that reason.

        **The response is where the honesty lands.** A check that cannot run
        reports `not_applicable` with a reason rather than `clear`, so a screen
        renders "2 clear, 1 not checked (no payments module)" instead of "all
        clear" — a summary that reads as an all-clear when it is a gap is the
        specific failure this shape exists to prevent.

        A clear assessment moves the wind-up to `awaiting_signoff` in the same
        call and writes **two** audit rows: "the checks ran" and "settlement
        resolved" are separate facts, and a wind-up that jumped straight to
        `awaiting_signoff` would leave no record that anything was ever checked.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The wind-up with the assessment recorded, moved to `awaiting_signoff` if everything was clear.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OffboardingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/OffboardingRefused'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/settlement-waiver:
    post:
      operationId: waiveOffboardingSettlement
      tags:
        - B2B offboarding
      summary: Set aside an outstanding settlement position
      description: |
        The escape hatch, treated as one.

        **A second permission, stacked on top.**
        `b2b_offboarding.waive_settlement_platform` beside
        `b2b_offboarding.manage_platform`: driving a wind-up is an operational
        job, while deciding that a company may stop owing money and leave anyway
        is a commercial concession, and the two are not the same person on any
        organisation chart. A waiver reachable by everybody who can click through
        the other eight steps would be the escape hatch quietly becoming the
        path.

        The service demands a **callable authoriser** rather than reading a code
        itself, and the argument is required so a call site cannot default it to
        true by omission — which is what keeps a console command or a queued job
        from reaching this without consulting anything.

        **The waiver writes its own audit action**, never `settlement_cleared`,
        because a waiver recorded as a clearance erases the only difference a
        dispute turns on. The reason is required and is carried on the row.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WaiveOffboardingSettlementRequest'
      responses:
        '200':
          description: The wind-up, settlement waived, at `awaiting_signoff`.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OffboardingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/OffboardingRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/signoff-challenges:
    post:
      operationId: issueOffboardingSignoffChallenge
      tags:
        - B2B offboarding
      summary: Send the signatory their sign-off passcode
      description: |
        **To the agreement's signatory, never to whoever is driving the
        wind-up.** The destination is resolved from the agreement's
        `signatory_user_id` through that person's login contact — the one row
        that must agree with the login identity — so there is no question which
        of somebody's addresses is meant and no way for an operator to nominate
        one. The person who signed a company in is the person who signs it out.

        **That makes this different in kind from the agreement-signing
        challenge**, where the caller *is* the signatory and the code goes to
        their own contact. Here the caller is a platform operator and the
        recipient is somebody else entirely, which is why the response says only
        that a code was sent and where, in the masked form the verification
        module authors.

        `202`: a challenge is a message in flight, not a resource the caller may
        then read.

        An agreement naming a signatory the platform cannot reach is a `422` and
        a real state rather than a defect — the answer is to amend the agreement,
        not to invent a destination.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '202':
          description: The challenge, with a masked destination and a countdown. Never the code.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OtpChallengeEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/OffboardingRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/signoff:
    post:
      operationId: signOffOffboarding
      tags:
        - B2B offboarding
      summary: Record the signatory's acceptance
      description: |
        **The permission is necessary and nowhere near sufficient.** The server
        demands a challenge that exists, carries purpose `b2b_signatory`, has
        been *consumed*, and belongs to the caller — so an operator driving the
        wind-up cannot sign it off from their own session, because they hold no
        such challenge. All four failures answer identically as
        `403 b2b.signatory_required`.

        **The passcode is verified before this operation, not by it.** The
        challenge must already be spent through
        `POST /verification/challenges/{challenge}/verify`, which is deliberate:
        evidence a caller asserts is not evidence, and an operation that also
        accepted the code would be one round trip in which both the proof and the
        thing it proves are claimed by the same request.

        Settlement is checked before the evidence: a wind-up whose settlement is
        neither cleared nor waived answers
        `409 offboarding.settlement_outstanding` with every blocker named.

        The two session hashes are computed server-side from what the connection
        carried and are deliberately not served back.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignOffOffboardingRequest'
      responses:
        '200':
          description: The wind-up, signed off, with the evidence recorded.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OffboardingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/OffboardingSettlementOutstanding'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/revoke-access:
    post:
      operationId: revokeOffboardingAccess
      tags:
        - B2B offboarding
      summary: Start taking access away
      description: |
        **The irreversible one, and the only step in this family that carries
        `Idempotency-Key`.** Everything before it can be called off; from here it
        cannot, because "cancelling" a revocation would mean silently re-granting
        access somebody deliberately removed.

        The key is optional, as everywhere else. The state machine already
        refuses a second call — `revoking` to `revoking` is not a legal
        transition — so the key buys a clean replayed envelope rather than the
        only line of defence.

        **`202`: the transition is written here and the work is done by a job.**
        Ending every membership in an organisation, deleting tokens and closing
        the organisation is more work than a request should hold, and a queued job
        can be retried — the property the one step that can partially fail most
        needs. The response comes back at `revoking` with the counts still null;
        read the wind-up to see them fill in.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/IdempotencyKey'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '202':
          description: The wind-up at `revoking`. The work is queued.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
            Idempotency-Replayed:
              $ref: '#/components/headers/IdempotencyReplayed'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OffboardingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/OffboardingRefused'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/archive:
    post:
      operationId: archiveOffboarding
      tags:
        - B2B offboarding
      summary: Purge the people, keep the company
      description: |
        **Re-runnable on purpose, which is why it has no `Idempotency-Key`.**
        `archiving` to `archiving` is a legal transition so a partial failure is
        retried rather than declared finished, and every write inside is
        idempotent — nulling an already-null column and bringing forward an
        already-past retention deadline both do nothing the second time. A replay
        guard would refuse the retry this step is designed to accept.

        **What goes and what stays is the decision worth reading.** The named
        contacts' names, emails, phones and notes go; the signatory block on the
        application goes; identity documents are made *due* rather than deleted,
        so the purge job removes them through the one code path that knows to
        delete the object before the row. What survives is the legal entity —
        legal name, registration number, tax number, incorporation date — because
        a corporate record carries retention obligations the people named on it do
        not.

        `archive.legal_entity_retained` is stated rather than left to be inferred
        from the summary's silence.

        The wind-up reaches `completed` in the same call — the one place this
        family does cascade, and barely: the purge either finished or it threw,
        and a state between "archived everything" and "done" would be one nothing
        could ever leave.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The completed wind-up, with the purge summary.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OffboardingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/OffboardingRefused'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/cancel:
    post:
      operationId: cancelOffboarding
      tags:
        - B2B offboarding
      summary: Call a wind-up off
      description: |
        **A POST rather than a DELETE**, unlike a customer's own closure request,
        and the difference is what the caller is doing: an operator cancelling a
        wind-up is recording a commercial decision about a relationship that
        continues, and it needs a written reason a `DELETE` has nowhere to put.

        Legal right up to the moment memberships start ending, and not
        afterwards. From `revoking` the only legal transition is `archiving`: the
        honest answer to "can we undo this" is no, and re-granting access
        somebody deliberately removed is a different act needing a different
        door.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelOffboardingRequest'
      responses:
        '200':
          description: The cancelled wind-up, with the reason on the row.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OffboardingEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/OffboardingRefused'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/exports:
    post:
      operationId: requestRecordExport
      tags:
        - B2B offboarding
      summary: Ask for the company's records, packaged
      description: |
        **Nested under the wind-up rather than under the organisation**, which is
        a deliberate narrowing: a top-level export operation would package every
        record a company holds, available whenever anybody wanted one. Nesting it
        means an export is always attached to a reason. A standalone export
        surface is a real future need — a data-subject request against a
        corporate customer — and it should arrive with its own reason rather than
        by widening this one.

        **Its own permission, stacked on top.**
        `record_export.create_platform` beside
        `b2b_offboarding.manage_platform`: taking a complete copy of everything a
        company gave the platform is a different act with a different risk, the
        same argument that makes the KYC document read narrower than the
        application read.

        **`202`, because requesting is not building.** Assembling the bundle
        reads an organisation's whole history and hashes it; doing that inside a
        request would make the latency a function of how long a company has been
        a customer. The response comes back `requested` with no size and no
        digest.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '202':
          description: The bundle request. Building is queued.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordExportEnvelope'
        '400':
          $ref: '#/components/responses/ContextRequired'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /platform/b2b/offboardings/{offboarding}/exports/{export}:
    get:
      operationId: showRecordExport
      tags:
        - B2B offboarding
      summary: Read a bundle, and optionally take a copy
      description: |
        **One operation for the status and the download, and `?purpose=` is what
        separates them.** Without it this is a read — the manifest, the row
        counts, the digest, whether it is ready. With it, a fifteen-minute signed
        URL is minted and the access is recorded as a `Confidential` read with
        the stated reason. Two operations would mean a client polling for
        readiness had to poll one and then call the other, and the second would
        be the one whose audit rows said "downloaded" for every poll.

        **The URL is in the envelope, never a 302.** A redirect would put an
        expiring credential into browser history, the referrer chain and every
        proxy log on the way to the bucket.

        **Fifteen minutes rather than the five a KYC document gets**: a bundle is
        a large download over whatever connection the recipient has, and a URL
        that expires mid-transfer is a failure the person cannot diagnose.
        Fifteen rather than a day, because a signed URL is a bearer credential
        and every extra hour is another hour it can be forwarded.

        A bundle that is still building, that failed, or whose window has closed
        is `409 record_export.unavailable` with `details.status` — never a 404,
        because the caller is looking at a row they may see.
      parameters:
        - $ref: '#/components/parameters/XOrganisationIdRequired'
        - $ref: '#/components/parameters/OffboardingPath'
        - $ref: '#/components/parameters/RecordExportPath'
        - $ref: '#/components/parameters/RecordExportPurpose'
        - $ref: '#/components/parameters/AcceptLanguage'
        - $ref: '#/components/parameters/XClientRequestId'
      responses:
        '200':
          description: The bundle, with a signed download URL when a purpose was stated.
          headers:
            X-Correlation-Id:
              $ref: '#/components/headers/XCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordExportEnvelope'
        '400':
          $ref: '#/components/responses/RequestInvalid'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/ContextOrPermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/RecordExportUnavailable'
        '429':
          $ref: '#/components/responses/TooManyRequests'
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: healthy360_session
      description: |
        The Sanctum first-party session cookie, issued by `POST /auth/login`
        to clients whose `Origin` is a configured stateful domain.

        Before the first state-changing request the client calls
        `GET /sanctum/csrf-cookie` (outside `/api/v1`, part of the session
        handshake rather than the resource API) and then sends the
        `X-XSRF-TOKEN` header taken from the `XSRF-TOKEN` cookie.
    bearerToken:
      type: http
      scheme: bearer
      description: |
        A Sanctum personal access token from `POST /auth/token`, bound to a
        registered device and revocable at `DELETE /me/devices/{device}`.
    guestToken:
      type: apiKey
      in: header
      name: X-Guest-Token
      description: |
        The opaque capability token issued once by `POST /guest/sessions`
        (G1). It is not an authentication credential and names nobody: it
        carries a *capability* — a basket, and after a verified contact the
        right to place one order against it — and expires in seventy-two
        hours.

        Its own header rather than `Authorization`, so that a client holding
        both a personal access token and a guest token cannot send the wrong
        one by accident and a proxy cannot confuse the two.

        Unknown, expired, revoked and insufficiently graded all answer
        `401 guest.session_invalid` with nothing that tells them apart —
        distinguishing them would only help somebody who does not already hold
        the token.
  headers:
    XCorrelationId:
      description: |
        The server-generated correlation identifier for this request (UUIDv7).
        Returned on every response and repeated in the body.
      required: true
      schema:
        $ref: '#/components/schemas/Uuid'
    RetryAfter:
      description: Seconds to wait before retrying.
      required: true
      schema:
        type: integer
        minimum: 0
    ETag:
      description: |
        The current `lock_version` of a lock-versioned resource, quoted. A
        client keeps it and sends it back as `If-Match` when it writes.
      required: true
      schema:
        type: string
        examples:
          - '"3"'
    IdempotencyReplayed:
      description: |
        Present and `true` when the response was replayed from a stored
        envelope rather than produced by running the command again. A client
        that retried after a lost response can tell the difference between
        "it worked this time" and "it had already worked".
      required: false
      schema:
        type: string
        examples:
          - 'true'
  parameters:
    XOrganisationId:
      name: X-Organisation-Id
      in: header
      required: false
      description: |
        The active organisation. Never trusted without server-side validation
        against an active membership.
      schema:
        $ref: '#/components/schemas/Uuid'
    XOrganisationIdRequired:
      name: X-Organisation-Id
      in: header
      required: true
      description: |
        The active organisation. Never trusted without server-side validation
        against an active membership.
      schema:
        $ref: '#/components/schemas/Uuid'
    XBranchId:
      name: X-Branch-Id
      in: header
      required: false
      description: |
        The active branch. Validated against the membership scope: a
        branch-scoped membership may only work inside its own branch.
      schema:
        $ref: '#/components/schemas/Uuid'
    AcceptLanguage:
      name: Accept-Language
      in: header
      required: false
      description: Locale negotiation. Regional subtags are accepted.
      schema:
        type: string
        examples:
          - ar-LB
          - en
    XClientRequestId:
      name: X-Client-Request-Id
      in: header
      required: false
      description: |
        An opaque client-generated identifier for support correlation. Logged
        and echoed back; never used as the correlation identifier.
      schema:
        type: string
        maxLength: 128
    XAppMode:
      name: X-App-Mode
      in: header
      required: false
      description: The build family. Diagnostic only; never an authorisation input.
      schema:
        type: string
        enum:
          - customer
          - staff
          - kiosk
          - driver
          - all-dev
    XClientVersion:
      name: X-Client-Version
      in: header
      required: false
      description: The client version, for diagnostics and deprecation telemetry.
      schema:
        type: string
        examples:
          - 1.4.0
    XClientPlatform:
      name: X-Client-Platform
      in: header
      required: false
      description: The client platform.
      schema:
        type: string
        enum:
          - web
          - ios
          - android
    IfMatch:
      name: If-Match
      in: header
      required: true
      description: |
        The `ETag` the resource was last served with. Required on writes to
        lock-versioned resources: absent is **428**
        `request.precondition_required`, stale is **409** `resource.conflict`
        carrying `details.current_lock_version`. `*` is accepted and means
        "as long as the resource exists".
      schema:
        type: string
        examples:
          - '"3"'
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      description: |
        A client-chosen key that makes this command safe to retry (§4.14). The
        same key with the same request body replays the original envelope,
        status included, and carries `Idempotency-Replayed: true`. The same key
        with a *different* body is **409** `request.idempotency_key_reused` —
        the caller has reused a key that already means something else.

        Keys are scoped per endpoint and per caller and are honoured for
        twenty-four hours. The client attaches one deliberately; it is never
        inferred server-side.
      schema:
        type: string
        maxLength: 255
    IdempotencyKeyRequired:
      name: Idempotency-Key
      in: header
      required: true
      description: |
        As `Idempotency-Key`, but mandatory. Used where the command creates
        records that cannot be un-created — provisioning a tenant — and where
        a retry with no key would therefore be unrecoverable rather than
        merely wasteful. Absent is **400** `request.invalid`.
      schema:
        type: string
        maxLength: 255
    Cursor:
      name: cursor
      in: query
      required: false
      description: |
        The `meta.next_cursor` of the previous page. Opaque — echo it back,
        never construct one. A cursor this endpoint did not issue is
        `400 request.invalid`, never a silent restart from the beginning.
      schema:
        type: string
        maxLength: 200
    CursorLimit:
      name: limit
      in: query
      required: false
      description: Page size.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    PageNumber:
      name: page
      in: query
      required: false
      description: |
        Ask for a numbered page instead of walking the cursor. 1-based.

        Only the kitchen catalogue accepts this — see `docs/api/conventions.md`
        for why offset is safe on these five collections and on nothing else.
        Sending it switches the response `meta` to `NumberedPaginationMeta`;
        omitting it leaves the keyset behaviour exactly as it was, so a client
        that has never heard of `page` is unaffected.

        A page past the last is `400 request.invalid`, not an empty list. Page
        1 of an empty collection is not: that is a legitimate empty answer.
      schema:
        type: integer
        minimum: 1
    PerPage:
      name: per_page
      in: query
      required: false
      description: |
        Page size for a numbered page. A synonym for `limit`, accepted so a
        client does not have to change which word it sends when it starts
        asking for pages; `per_page` wins if both are present.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    MarketplaceKitchenPath:
      name: kitchen
      in: path
      required: true
      description: The kitchen's identifier or its slug.
      schema:
        type: string
        maxLength: 160
    MarketplaceMealPath:
      name: meal
      in: path
      required: true
      description: The meal's identifier or its slug.
      schema:
        type: string
        maxLength: 160
    MarketplacePlanPath:
      name: plan
      in: path
      required: true
      description: The subscription plan's identifier or its slug.
      schema:
        type: string
        maxLength: 160
    IngredientPath:
      name: ingredient
      in: path
      required: true
      description: The ingredient identifier.
      schema:
        $ref: '#/components/schemas/Uuid'
    AllergenCodePath:
      name: code
      in: path
      required: true
      description: The canonical allergen class code.
      schema:
        $ref: '#/components/schemas/AllergenCode'
    RecipePath:
      name: recipe
      in: path
      required: true
      description: The recipe identifier.
      schema:
        $ref: '#/components/schemas/Uuid'
    RecipeVersionPath:
      name: version
      in: path
      required: true
      description: |
        The version identifier, or its `version_number`. Both are accepted
        because both are natural — a client that walked the list holds
        identifiers, a human reading a technical sheet holds "version 3" — and
        a number cannot be mistaken for a UUID. The version is always resolved
        inside the recipe in the path, so one recipe's number can never reach
        another's version.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            pattern: ^\d+$
    CatalogueItemPath:
      name: item
      in: path
      required: true
      description: |
        The item identifier, or its `slug`. Both are accepted because both are
        natural — a client that walked the list holds identifiers, a
        marketplace integration or a support engineer holds
        `harissa-paste-250g` — and a slug is unique per organisation and
        immutable, so the two answers cannot drift apart.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 140
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    SalesChannelPath:
      name: channel
      in: path
      required: true
      description: The channel identifier, or its `code`.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 40
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    PriceListPath:
      name: priceList
      in: path
      required: true
      description: |
        The price list identifier, or its `code`. Both are accepted for the
        reason the sales-channel path gives: a client that walked the list
        holds an identifier, an operator or an importer holds
        `healthy360-b2b-usd`, and the code is unique per organisation.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 40
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    PlanCombinationPath:
      name: combination
      in: path
      required: true
      description: The meal combination identifier, or its `code`.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 40
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    EnergyBandPath:
      name: band
      in: path
      required: true
      description: The energy band identifier, or its `code`.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 40
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    PlanDurationPath:
      name: duration
      in: path
      required: true
      description: The plan duration identifier, or its `code`.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 40
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    XBranchIdRequired:
      name: X-Branch-Id
      in: header
      required: true
      description: |
        The active branch. Validated against the membership scope: a
        branch-scoped membership may only work inside its own branch.

        Required here rather than optional, because the resource *is* one
        branch's. An absent header is `400 context.branch_required` — the
        endpoint refuses rather than guessing which location the caller meant.
      schema:
        $ref: '#/components/schemas/Uuid'
    SupplierPath:
      name: supplierId
      in: path
      required: true
      description: |
        The supplier identifier. An identifier only, unlike a delivery zone's
        path: a supplier `code` is frequently minted by the server and is
        editable afterwards, so a URL that accepted it would break the moment a
        kitchen corrected one.
      schema:
        $ref: '#/components/schemas/Uuid'
    PurchaseOrderPath:
      name: purchaseOrder
      in: path
      required: true
      description: |
        The purchase-order identifier, never its `number`. The number is the
        human handle both sides quote down a phone line; the URL takes the
        identifier, on the same grounds a supplier's does.
      schema:
        $ref: '#/components/schemas/Uuid'
    GoodsReceiptPath:
      name: goodsReceipt
      in: path
      required: true
      description: The goods-receipt identifier.
      schema:
        $ref: '#/components/schemas/Uuid'
    DeliveryZonePath:
      name: zone
      in: path
      required: true
      description: The delivery zone identifier, or its `code`.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 40
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    DeliveryWindowPath:
      name: window
      in: path
      required: true
      description: The delivery window identifier, or its `code`.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 30
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    CartPath:
      name: cart
      in: path
      required: true
      description: |
        The basket identifier. An identifier only — a cart has no stable code a
        human would hold, and there is nothing to guess at.
      schema:
        $ref: '#/components/schemas/Uuid'
    CartItemPath:
      name: item
      in: path
      required: true
      description: The basket line identifier.
      schema:
        $ref: '#/components/schemas/Uuid'
    OrderPath:
      name: order
      in: path
      required: true
      description: |
        The order identifier. **Not** the order number, tempting though it is:
        the number is printed on a receipt that passes through a courier's
        hands, and a URL that accepted it would turn a scrap of paper into an
        address. The number is searchable through `GET /catalogue/orders`,
        behind the read permission.
      schema:
        $ref: '#/components/schemas/Uuid'
    B2bApplicationPath:
      name: application
      in: path
      required: true
      description: |
        The application identifier, or its `reference`. Both are accepted
        because both are natural — a client that walked the list holds
        identifiers, and a person reading an email holds `B2B-2026-7F3A9C21`
        — and a reference cannot be mistaken for a UUID.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            pattern: ^[A-Za-z0-9-]{6,40}$
    B2bApplicationSectionPath:
      name: section
      in: path
      required: true
      description: |
        The wizard step being saved. The section is in the path so the server
        can refuse a payload that wandered outside it; an unknown value is
        `400 request.invalid` with `details.parameter`, not a 404.
      schema:
        $ref: '#/components/schemas/B2bApplicationSection'
    B2bAgreementPath:
      name: agreement
      in: path
      required: true
      description: The agreement version's identifier. Always resolved inside the application in the path.
      schema:
        $ref: '#/components/schemas/Uuid'
    KycDocumentPath:
      name: document
      in: path
      required: true
      description: The document identifier.
      schema:
        $ref: '#/components/schemas/Uuid'
    KycAccessPurpose:
      name: purpose
      in: query
      required: true
      description: |
        Why the document is being opened, written verbatim onto the access
        audit event. Required and non-blank: a document read with no stated
        reason is the read an audit trail cannot explain afterwards, and an
        optional argument is one every call site eventually omits. Blank or
        absent is `400 request.invalid` with `details.parameter`.
      schema:
        type: string
        minLength: 1
        maxLength: 160
        examples:
          - kyc_review
          - support_query_4821
    DeliveryJobPath:
      name: job
      in: path
      required: true
      description: |
        The delivery job identifier. On the driver routes, one that is not the
        caller's own answers 404, decided before the request body is looked at;
        on the dispatcher's `assign`, one belonging to another organisation
        answers 404 for the same reason — a 403 would confirm that the
        identifier names something real.
      schema:
        $ref: '#/components/schemas/Uuid'
    PaymentIntentPath:
      name: paymentIntent
      in: path
      required: true
      description: The payment intent identifier.
      schema:
        $ref: '#/components/schemas/Uuid'
    CorporateProgrammePath:
      name: programme
      in: path
      required: true
      description: |
        The programme identifier. Resolved inside the buyer organisation
        `org.context` already validated — another organisation's programme is
        `404`, never `403`.
      schema:
        $ref: '#/components/schemas/Uuid'
    QuotationPath:
      name: quotation
      in: path
      required: true
      description: |
        The quotation identifier. A malformed value is `404` rather than a
        server error: an identifier that cannot exist is a client's typo, not
        a database question.
      schema:
        $ref: '#/components/schemas/Uuid'
    B2bCatalogueItemPath:
      name: item
      in: path
      required: true
      description: |
        The article identifier. Unlike the kitchen-facing catalogue paths this
        one takes an identifier only, never a slug: a corporate buyer reaches
        articles by walking their own agreed list, and a guessable key on a
        private catalogue is a way to ask what other kitchens sell.
      schema:
        $ref: '#/components/schemas/Uuid'
    B2bCatalogueLanguage:
      name: language
      in: query
      required: false
      description: |
        Which of the two stored names to serve. `ar` selects the Arabic name;
        anything else — including an absent parameter — serves the English
        one. A presentation choice made per request rather than a stored
        preference, because the same buyer's procurement officer and warehouse
        may read in different languages.
      schema:
        type: string
        enum:
          - en
          - ar
        default: en
    OrganisationPath:
      name: organisation
      in: path
      required: true
      description: |
        The organisation identifier. Must match the organisation
        `X-Organisation-Id` selected; a mismatch is **404**, never 403 —
        confirming that another tenant exists is not something this API does.
      schema:
        $ref: '#/components/schemas/Uuid'
    OrganisationInvitationPath:
      name: invitation
      in: path
      required: true
      description: The invitation identifier. Always resolved inside the organisation in the path.
      schema:
        $ref: '#/components/schemas/Uuid'
    OrganisationInvitationTokenPath:
      name: token
      in: path
      required: true
      description: |
        The single-use token from the invitation email. Never an identifier —
        only its SHA-256 is stored, so this value cannot be recovered from the
        platform and a lost one is replaced by re-inviting.
      schema:
        type: string
        minLength: 32
        maxLength: 128
    PlatformKitchenPath:
      name: organisation
      in: path
      required: true
      description: |
        The kitchen's identifier **or its slug**. Both are accepted because an
        operator reading a support ticket has the slug in front of them and not
        a UUID, and a slug is unique across the platform.

        An organisation that exists but is not a kitchen answers **404**, never
        403: the kitchen console is not the place to confirm the existence of
        clinics. A malformed identifier is the same 404 rather than a driver
        error, because it is substituted rather than queried.
      schema:
        oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: string
            maxLength: 100
            pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
    PlatformKitchenMembershipPath:
      name: membership
      in: path
      required: true
      description: |
        The membership identifier. Always resolved inside the kitchen in the
        path; one belonging to another organisation is `404`, never `403`.
      schema:
        $ref: '#/components/schemas/Uuid'
    SubscriptionPath:
      name: subscription
      in: path
      required: true
      description: |
        The subscription identifier. Always resolved inside the caller's own
        customer account: somebody else's is `404 resource.not_found`, never a
        403, because the row carries a delivery address, a weekday pattern and
        a price somebody negotiated.
      schema:
        $ref: '#/components/schemas/Uuid'
    ClosureRequestPath:
      name: closureRequest
      in: path
      required: true
      description: |
        The closure request identifier, always resolved inside the caller's own
        identity. Somebody else's is `404 resource.not_found`: the row says that
        a named person is leaving and why.
      schema:
        $ref: '#/components/schemas/Uuid'
    OffboardingPath:
      name: offboarding
      in: path
      required: true
      description: The offboarding identifier. Platform-only; there is no tenant read path.
      schema:
        $ref: '#/components/schemas/Uuid'
    RecordExportPath:
      name: export
      in: path
      required: true
      description: |
        The records bundle identifier, always resolved *through* the offboarding
        in the path. An export resolved by identifier alone would let a caller
        read one company's bundle through another company's wind-up.
      schema:
        $ref: '#/components/schemas/Uuid'
    RecordExportPurpose:
      name: purpose
      in: query
      required: false
      description: |
        Why the bundle is being taken, written verbatim onto the access audit
        event. **Its presence is what turns a status check into a download**:
        without it the operation reads the manifest and nothing is issued; with
        it a fifteen-minute signed URL is minted in the envelope and the access
        is recorded as a `Confidential` read. Blank is
        `400 request.invalid` with `details.parameter` — an access with no
        stated reason is the access an audit trail cannot explain afterwards.
      schema:
        type: string
        minLength: 1
        maxLength: 160
        examples:
          - handover_to_customer
          - dsar_response_4821
    SubscriptionScheduleFrom:
      name: from
      in: query
      required: false
      description: The first day of the window. Defaults to today.
      schema:
        type: string
        format: date
    SubscriptionScheduleTo:
      name: to
      in: query
      required: false
      description: |
        The last day of the window. Defaults to thirteen days after `from`, and
        may be at most **sixty** days after it: the projection is
        O(subscriptions x days), so an unbounded window is a request a client can
        make that the kitchen cannot afford. Beyond that is
        `400 request.invalid` with `details.max_window_days`.
      schema:
        type: string
        format: date
    SubscriptionScheduleBranch:
      name: branch_id
      in: query
      required: false
      description: |
        Narrow to one production site. A *narrowing* of a book the caller can
        already see, never a widening of one they cannot — which is why it is a
        query filter and not `X-Branch-Id`.
      schema:
        $ref: '#/components/schemas/Uuid'
  responses:
    Accepted:
      description: Accepted for delivery.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            type: object
            required:
              - data
              - meta
            properties:
              data:
                type: object
                required:
                  - sent
                properties:
                  sent:
                    type: boolean
                    const: true
              meta:
                $ref: '#/components/schemas/Meta'
    LoginOutcome:
      description: |
        The sign-in resolved. `two_factor_required: false` means a session now
        exists; `true` means the client must complete
        `POST /auth/two-factor-challenge`.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            type: object
            required:
              - data
              - meta
            properties:
              data:
                type: object
                required:
                  - two_factor_required
                properties:
                  two_factor_required:
                    type: boolean
              meta:
                $ref: '#/components/schemas/Meta'
    PasswordConfirmationStatus:
      description: The step-up state of the calling credential.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            type: object
            required:
              - data
              - meta
            properties:
              data:
                type: object
                required:
                  - confirmed
                  - timeout_seconds
                properties:
                  confirmed:
                    type: boolean
                  timeout_seconds:
                    type: integer
                    minimum: 0
                    description: How long a confirmation remains valid.
              meta:
                $ref: '#/components/schemas/Meta'
    TwoFactorStateResponse:
      description: |
        The two-factor state of the account. `enabled` means a secret exists;
        `confirmed` means two-factor authentication is armed and will be
        demanded at the next sign-in.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            type: object
            required:
              - data
              - meta
            properties:
              data:
                $ref: '#/components/schemas/TwoFactorState'
              meta:
                $ref: '#/components/schemas/Meta'
    RecoveryCodes:
      description: Single-use recovery codes.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            type: object
            required:
              - data
              - meta
            properties:
              data:
                type: object
                required:
                  - recovery_codes
                properties:
                  recovery_codes:
                    type: array
                    items:
                      type: string
              meta:
                $ref: '#/components/schemas/Meta'
    RequestInvalid:
      description: |
        The request could not be processed as sent — typically a session
        endpoint reached without a first-party `Origin`.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: request.invalid
              message: This endpoint serves first-party session clients only; native clients must use /api/v1/auth/token.
              details:
                required_headers:
                  - Origin
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    ContextRequired:
      description: |
        A context the endpoint needs was not supplied —
        `context.organisation_required` for a missing `X-Organisation-Id`, and
        `context.branch_required` for a missing `X-Branch-Id` on the
        branch-scoped operations. Distinct from
        `context.branch_out_of_scope` (403): the caller has not asked for a
        branch they may not have, they have not asked for one at all.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: context.organisation_required
              message: An X-Organisation-Id header is required for this endpoint.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    Unauthenticated:
      description: No usable credential was presented.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: auth.unauthenticated
              message: Authentication is required for this endpoint.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    EmailUnverified:
      description: The address has not been verified.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: auth.email_unverified
              message: Your email address must be verified before you can use this endpoint.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    StepUpRequired:
      description: |
        The action is sensitive and needs a recent password confirmation.
        **HTTP 403, never 423** (plan §13).
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: auth.step_up_required
              message: This action requires you to confirm your password again.
              details:
                confirmation_endpoint: /api/v1/auth/confirm-password
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    ContextDenied:
      description: |
        The organisation or branch could not be validated for this person. The
        message never reveals whether the organisation exists.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: context.organisation_forbidden
              message: You do not have an active membership in the requested organisation.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    ContextOrPermissionDenied:
      description: |
        The context was refused (`context.organisation_forbidden`,
        `context.branch_out_of_scope`) or the membership's roles do not grant
        the required permission (`authz.permission_denied`, with the denying
        RBAC step in `details.reason`).
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: authz.permission_denied
              message: You do not have permission to perform this action.
              details:
                reason: permission_not_granted
                permission: organisation.view_current
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    NotFound:
      description: The resource does not exist, or is not the caller's to see.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: resource.not_found
              message: The requested resource does not exist.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    InvalidCredentials:
      description: |
        The credentials were rejected. Deliberately not a field-level
        validation error: clients branch on the code.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: auth.invalid_credentials
              message: These credentials do not match our records.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    ValidationFailed:
      description: The submitted data is invalid.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: validation.failed
              message: The submitted data is invalid.
              details:
                fields:
                  email:
                    - The email field must be a valid email address.
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    TooManyRequests:
      description: The rate limit for this endpoint was exceeded.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
        Retry-After:
          $ref: '#/components/headers/RetryAfter'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: rate_limit.exceeded
              message: Too many requests. Please retry later.
              details:
                retry_after_seconds: 47
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    PreconditionRequired:
      description: |
        The resource is lock-versioned and the write carried no `If-Match`.
        **HTTP 428, not 409 and not 400**: the client has not lost a race — it
        never entered one — and the fix is to read the resource and retry with
        its validator.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: request.precondition_required
              message: This resource requires an If-Match header carrying the version you last read.
              details:
                required_headers:
                  - If-Match
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    ResourceConflict:
      description: |
        The change conflicts with the current state. On a lock-versioned
        write this is a lost race, and `details.current_lock_version` is the
        value to reload against, so a client can offer "reload" or "keep
        mine" without a second round trip.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: resource.conflict
              message: This resource changed since you last read it. Reload it and reapply your change.
              details:
                current_lock_version: 4
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    PurchaseOrderConflict:
      description: |
        The order's own state refuses the change (§3.5, §6). `details.reason`
        names which rule, and there are four:

        - `purchase_order_not_draft` — the order has been issued, cancelled or
          received against, and issued orders are immutable. `details.status`
          carries the state it is actually in.
        - `purchase_order_not_cancellable` — cancellation is reachable from
          `draft` and `issued` only; an order with deliveries against it cannot
          be cancelled as though nothing happened.
        - `supplier_archived` — the supplier has left the book. Restore it or
          choose another; `details.supplier_id` names it.
        - `purchase_order_received_against` — something has already been
          delivered against this order (SUP5). Checked before the status, so
          that when both apply a client is told what actually happened rather
          than being told the status again.
        - `purchase_order_not_receivable` — a delivery was posted against an
          order that is not `issued` or `partially_received`.
        - `purchase_order_branch_mismatch` / `purchase_order_supplier_mismatch`
          — §4: a receipt against an order must use that order's branch and
          supplier. Every identifier is well formed; what refuses is the
          relationship between them, which is why this is a `409` and not a
          `422`.

        No `current_lock_version`: purchase orders are not lock-versioned, so
        there is no race to reload against. Re-reading the order is what tells a
        client what happened to it.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: resource.conflict
              message: Only a draft purchase order can be edited.
              details:
                reason: purchase_order_not_draft
                status: issued
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    RefundRefused:
      description: |
        The refund was refused. Either the intent is not `captured`
        (`resource.conflict`), or the amount is larger than what is still
        refundable (`payment.refund_exceeds_capture`), which carries
        `details.captured_minor`, `details.refunded_minor` and
        `details.refundable_minor` so a form can show the remaining balance
        rather than a bare rejection.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: payment.refund_exceeds_capture
              message: This refund is larger than the amount still refundable on this payment.
              details:
                captured_minor: 4000
                refunded_minor: 2750
                refundable_minor: 1250
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    AllergenBaselineWeakened:
      description: |
        The submitted data is invalid. On an allergen replacement this is
        usually the upgrade-only rule:
        `details.weakened_allergen_classes` names the platform baseline
        classes the request would have dropped or weakened.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: validation.failed
              message: An organisation mapping may add to the platform allergen baseline but never weaken it.
              details:
                fields:
                  mappings:
                    - The platform baseline for this ingredient must be kept or strengthened.
                weakened_allergen_classes:
                  - gluten
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    RecipeArchiveBlocked:
      description: |
        The recipe still has a published version.
        `details.published_version_ids` names them. Withdrawing something from
        sale is retiring the version — its own action, its own permission —
        not a side effect of archiving.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: catalogue.in_use
              message: This recipe still has a published version. Retire it before archiving the recipe.
              details:
                published_version_ids:
                  - 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d40
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    IngredientArchiveConflict:
      description: |
        Three codes share this status, because three different things can be
        wrong and the remedies differ.

        **`catalogue.in_use`** — the ingredient is still named by a recipe
        version that has not been retired. `details.recipe_ids` and
        `details.recipe_version_ids` name them, so the caller knows what to
        retire first. Retired versions are never a blocker: history is allowed
        to reference an archived ingredient.

        **`resource.conflict`** — either the ingredient is already archived, or
        the write lost a race and `details.current_lock_version` is the value
        to reload against.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: catalogue.in_use
              message: This ingredient is still used by a recipe version that has not been retired.
              details:
                recipe_ids:
                  - 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d41
                recipe_version_ids:
                  - 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d42
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    VersionImmutableOrConflict:
      description: |
        Either the version is frozen (`catalogue.version_immutable` — it is
        published or retired, and no amount of reloading will make it writable;
        the answer is a new draft version) or the write lost a race
        (`resource.conflict`, carrying `details.current_lock_version`). Two
        codes, because the remedies are different.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: catalogue.version_immutable
              message: A published recipe version cannot be changed. Create a new draft version instead.
              details:
                status: published
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    PublishBlocked:
      description: |
        The readiness evaluator refused. `details.reasons` carries **every**
        blocker rather than the first one found — a gate that reveals one
        problem per attempt turns a five-minute fix into five round trips.
        Each entry is a stable machine `reason` plus whatever identifies the
        offending rows.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: catalogue.publish_blocked
              message: This recipe version is not ready to be published.
              details:
                reasons:
                  - reason: line_quantity_missing
                    line_numbers:
                      - 2
                      - 5
                  - reason: ingredient_requires_review
                    ingredient_ids:
                      - 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d43
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    AllergenUnmapped:
      description: |
        At least one line ingredient carries no allergen determination at all.
        Its own code rather than a `publish_blocked` reason because the fix is
        in a different place — the ingredient's mapping editor — and because
        silence must never be mistaken for a clean result. An ingredient
        passes when it holds a mapping row in any layer, **or** when its
        `verification_status` is `verified`, which is how "checked, and it
        carries nothing" is recorded.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: catalogue.allergen_unmapped
              message: Every ingredient in a published recipe must carry an allergen determination.
              details:
                ingredient_ids:
                  - 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d44
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    CatalogueItemPublishBlocked:
      description: |
        The K1.4 readiness gate refused. `details.reasons` carries **every**
        blocker rather than the first one found, so a kitchen fixes them in one
        pass.

        The vocabulary is `item_quarantined`, `item_not_a_draft`,
        `translation_incomplete`, `no_active_variant`, `no_allergen_basis` and
        `linked_recipe_quarantined`. The full readiness evaluator will add
        reasons to this list rather than replace the mechanism.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: catalogue.publish_blocked
              message: This catalogue item is not ready to be published.
              details:
                reasons:
                  - reason: translation_incomplete
                    fields:
                      - name_ar
                  - reason: linked_recipe_quarantined
                    recipe_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d41
                    recipe_version_ids:
                      - 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d42
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    PriceListConflict:
      description: |
        The write conflicts with the state of the price list. `details.reason`
        distinguishes the cases, so a client knows whether to reload or to do
        something different:

        - `currency_locked` — the list already holds prices, so its currency
          cannot move. `details.current_currency`, `details.requested_currency`
          and `details.entry_count` say why. The answer is a new list.
        - `channel_assignments_active` — the list is still assigned to the
          channels in `details.sales_channel_ids`. Detach it first.
        - no `reason` — a plain lost race: the `If-Match` validator was stale,
          and `details.current_lock_version` carries the current one. Reload
          and retry.

        An archived list also answers `409` on every write.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: resource.conflict
              message: This price list already holds amounts in USD. A currency cannot be changed under existing prices — create a new list instead.
              details:
                reason: currency_locked
                current_currency: USD
                requested_currency: AED
                entry_count: 312
                current_lock_version: 4
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    PriceListPublishBlocked:
      description: |
        The activation gate refused. `details.reasons` carries **every**
        blocker rather than the first one found, the rule the recipe and
        catalogue-item gates already follow.

        The vocabulary is `price_list_not_a_draft`, `no_open_rows` and
        `amount_status_mismatch`. The last is structurally unreachable — a
        CHECK constraint refuses the combination at the database — and is
        stated anyway, because "the database would have stopped it" is not
        something a client can see.

        Note what is **not** a blocker: placeholder and market-priced rows.
        They are welcome on an active list, and the public projection is what
        keeps them from a customer.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: catalogue.publish_blocked
              message: This price list is not ready to be activated.
              details:
                reasons:
                  - reason: no_open_rows
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    PlanCellConflict:
      description: |
        A matrix cell is already held by a different configuration —
        `details.existing_configuration` names its `code`, and
        `details.cell_index` points at the offending entry in the submission.

        The occupant may be **archived**, and that is the case worth
        understanding: a withdrawn cell keeps its coordinates because a price
        row still points at its variant, so re-selling that cell means reviving
        that configuration rather than opening a second one at the same address.

        Also served for a stale `If-Match`, as everywhere else.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: resource.conflict
              message: This matrix cell already exists on this plan as configuration "lunch-dinner-standard".
              details:
                cell_index: 0
                existing_configuration: lunch-dinner-standard
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    DeliveryAreaConflict:
      description: |
        A place is already served by another zone **at the same scope**, and
        `details.occupying_zones` names it by `code` while
        `details.conflicts` pairs each offending area with the zone holding it.

        The rule is one area, one zone **per branch**. An organisation-wide
        zone and a branch-scoped zone may both claim the same area — that is
        the override, resolved branch-first — so this conflict only ever means
        two zones at *one* scope.

        `details.scope` says which scope collided: `organisation` for the
        organisation-wide map, `branch` for one location's.

        Also served for a stale `If-Match`, as everywhere else.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: resource.conflict
              message: Another zone at the same scope already serves one of these areas. An area is served by one zone per branch — move it out of the other zone first.
              details:
                reason: area_already_served
                scope: organisation
                conflicts:
                  - delivery_area_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
                    delivery_zone_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d40
                occupying_zones:
                  - beirut-inner
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    B2bApplicationStateInvalid:
      description: |
        The application cannot make the move that was asked for.
        `details.status` is where it is, `details.requested_status` is where
        the caller wanted it, and `details.allowed_transitions` is what it
        *can* do — so a client learns the state machine from the refusal
        rather than from documentation it may not have read.

        Distinct from a plain `resource.conflict` on purpose: a reviewer's
        console has to tell "somebody got there first" from "that is not a
        move this application can make", and those are different screens.

        `details.current_lock_version` rides along, because a caller that lost
        an `If-Match` race needs the current validator to retry with.

        The section PATCH answers this code for a second reason — writing to a
        section the reviewer has not reopened — and then carries
        `details.writable_sections` instead.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: b2b.application_state_invalid
              message: An application that is declined cannot become approved.
              details:
                status: declined
                requested_status: approved
                allowed_transitions: []
                current_lock_version: 7
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
    B2bDocumentsIncomplete:
      description: |
        The application is not complete enough to send. **Both lists come back
        at once** — `details.missing_fields` and `details.missing_documents` —
        because an applicant should learn everything that is wrong in one
        round trip rather than one field at a time.

        This code is used when the *document* checklist is short. When only
        form fields are missing the answer is `422 validation.failed` with the
        same two lists, because finding a certificate and filling a blank
        field are different journeys and a client should not have to parse a
        details bag to know which screen to open.

        `missing_documents` names **kinds**, not files. A rejected or
        superseded document does not count towards the requirement; a pending
        one does, because review happens after submission and requiring an
        accepted document to submit would be a loop with no entrance.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: b2b.documents_incomplete
              message: This application is not complete enough to send yet.
              details:
                missing_fields:
                  - signatory_title
                missing_documents:
                  - signatory_identification
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
    B2bSignatoryRequired:
      description: |
        Signing requires the person the company **named** as its signatory,
        proving they are present with a passcode sent to the address on the
        application.

        One code covers every way of failing that, and carries no detail about
        which: the application has not named a signatory; the caller does not
        hold the named address; the challenge does not exist, belongs to
        somebody else, was raised for another purpose, or has not been spent.
        A caller holding somebody else's challenge identifier learns only that
        it did not work.

        The fix is never a permission. The named signatory signs in and signs
        for themselves — an office manager who can reach the mailbox is not
        the person the company bound itself through.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: b2b.signatory_required
              message: Only the signatory this application names may sign it. Ask them to sign in with the address on the application and sign for themselves.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
    B2bQuotationStateInvalid:
      description: |
        The quotation cannot make the move that was asked for — the same
        shape of refusal `b2b.application_state_invalid` gives an
        application, for a quotation instead: `details.status` is where it
        is, `details.requested_status` is where the caller wanted it, and
        `details.allowed_transitions` is what it *can* do next.

        Also the answer when a `quoted` quotation's `expires_at` has already
        passed and a buyer tries to accept or decline it — the sweep has not
        caught up yet, but the deadline already has.

        `details.current_lock_version` rides along for a caller that lost an
        `If-Match` race and needs the current validator to retry with.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: b2b.quotation_state_invalid
              message: A quotation that is quoted cannot become quoted.
              details:
                status: quoted
                requested_status: quoted
                allowed_transitions:
                  - accepted
                  - declined
                current_lock_version: 3
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
    B2bQuotationEmpty:
      description: |
        Submission was refused because the draft carries no lines. Its own
        code rather than `validation.failed`: nothing about the request body
        is malformed — the resource itself has nothing in it yet to send for
        pricing.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: b2b.quotation_empty
              message: This quotation has no lines to submit.
              details:
                current_lock_version: 0
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
    CorporateBuyerRequired:
      description: |
        The corporate catalogue refused the caller, and the code says which
        kind of refusal it was.

        `cart.channel_refused` is the one specific to this family: private
        pricing exists for corporate buyers, and an account that is not one is
        told so plainly rather than shown an empty catalogue. It is the same
        code the cart raises when a personal account reaches for a private
        channel, because it is the same fact about the same account.

        The rest are the ordinary gates in front of every authenticated,
        organisation-scoped read: `auth.email_unverified`,
        `context.organisation_forbidden`, and
        `account.verification_required` when the signed-in person has no
        customer account behind their membership yet.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: cart.channel_refused
              message: This sales channel is only available to corporate buyer accounts.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
    SubscriptionRefused:
      description: |
        The subscription cannot be started as it stands, with **every** reason
        at once. `details.reasons` is a list of `{reason, ...context}`.

        All of them rather than the first, and the argument is sharper than it
        is for a checkout: a plan is a longer, more considered purchase, and
        telling somebody one problem per attempt — the plan is not published,
        now the duration is not offered, now nobody delivers to your address —
        is three round trips through a form they have already filled in.

        Its own code rather than `order.placement_refused`, which the domain
        borrowed while the error vocabulary was closed to it: a client branches
        on the code to decide which screen to render, and a checkout basket and
        a twenty-day plan are not the same screen.

        `GET /subscriptions/quote` answers with this code too, for the subset of
        reasons that are about the plan and its price — `plan_not_subscription`,
        `unpriced`, `duration_not_offered`, `duration_ambiguous`,
        `duration_not_fixed`, `pricing_basis_unsupported`.

        `duration_ambiguous` is the one that names a *configuration mistake*
        rather than a limit: the quote resolves a run by its number of days, and
        two offered runs sharing a day count cannot be told apart. It carries the
        competing `codes`, so a kitchen reading its own error can see which two.
        Guessing between them would quote one discount and capture the other.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: subscription.refused
              message: This subscription cannot be started as it stands.
              details:
                reasons:
                  - reason: duration_not_offered
                    plan_duration_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
                  - reason: area_not_served
                    delivery_area_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d40
                    served_by_anyone: false
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    SubscriptionChangeRefused:
      description: |
        The change cannot be made to the subscription as it stands.

        **Mostly this is the change window.** Skip, pause, resume, address,
        window, weekdays and meal choices all take effect only for deliveries
        more than the plan's `change_cutoff_hours` away, and inside that window
        the next delivery proceeds as scheduled. `inside_cut_off` carries
        `cut_off_at` and `effective_from`, so a client can say "changes to
        Tuesday closed at 18:00 on Monday; the earliest day you can change is
        Wednesday" rather than "no".

        The rest of the vocabulary: `not_permitted` (the plan itself forbids the
        right — the kitchen's stored `skip_allowed` / `pause_allowed`, not an
        assumption), `invalid_transition`, `already_settled`,
        `not_a_delivery_day`, `exhausted` (the balance is spent and the answer
        is a renewal), `stale_version`, `address_not_owned`,
        `address_not_deliverable`, `area_not_served`, `zone_suspended`,
        `weekdays_empty`, `weekdays_invalid`, and for meal choices
        `meal_unknown`, `meal_not_a_meal` and `meal_not_published`.

        A 409 rather than a validation failure: the request is well-formed and
        the customer meant it. What has happened is that time passed.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: subscription.change_refused
              message: This change cannot be made to the subscription as it stands.
              details:
                reasons:
                  - reason: inside_cut_off
                    delivery_date: '2026-08-11'
                    cut_off_hours: 24
                    cut_off_at: '2026-08-09T21:00:00+00:00'
                    effective_from: '2026-08-13'
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    ClosureRefused:
      description: |
        A closure request the platform will not handle as asked.
        `details.reason` is a stable string.

        **This is not the code for a blocked closure**, and the distinction is
        the whole shape of the journey. A blocker — open orders, a live
        subscription, a pending signature — is information a customer acts on,
        comes back inside a `200`/`202` acknowledgement as `blocked` and
        `blockers`, and leaves the request alive so they can return when their
        last order has arrived. An error code there would turn a checklist into
        a failure.

        What this code covers: `closure_already_in_flight` (a second request
        while one is still going somewhere — refused rather than silently
        returning the existing one, because the two may differ in scope and
        quietly handing back an older request would answer a question nobody
        asked), `closure_not_awaiting_verification`, `closure_not_cancellable`,
        and `closure_no_verifiable_contact`.

        Three closure refusals deliberately answer with **other** codes: a
        support actor trying to finish somebody else's erasure is
        `403 authz.permission_denied`, a passcode that did not verify is
        `422 otp.invalid` (one indistinguishable shape for wrong, expired,
        superseded and locked out), and a reason outside the vocabulary is
        `422 validation.failed`.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: closure.refused
              message: A closure request is already in progress for this account.
              details:
                reason: closure_already_in_flight
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    OffboardingRefused:
      description: |
        The offboarding cannot do that from where it is. `details.reason` names
        which — `offboarding.already_in_flight`,
        `offboarding.transition_not_allowed`, `offboarding.no_active_agreement`
        — and `details.allowed_transitions` names what *is* legal from here, so
        a wind-up screen renders buttons from the refusal rather than
        reimplementing the state machine.

        `already_in_flight` is refused here as well as by the partial unique
        index, so an operator gets a sentence rather than a constraint
        violation, and the index stays as the thing nothing can route around.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: offboarding.refused
              message: An offboarding that is revoking cannot become cancelled.
              details:
                reason: offboarding.transition_not_allowed
                status: revoking
                requested_status: cancelled
                allowed_transitions:
                  - archiving
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    OffboardingSettlementOutstanding:
      description: |
        Sign-off refused because settlement is neither cleared nor waived.
        `details.blockers` names **every** outstanding check rather than the
        first: somebody winding up a relationship wants the list, not a sequence
        of discoveries.

        Its own code rather than `offboarding.refused` because the remedy is
        different and a client has to branch on it — settle, or waive through
        `POST .../settlement-waiver` with a written reason and the second
        permission.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: offboarding.settlement_outstanding
              message: Settlement is not resolved, so this offboarding cannot move to sign-off.
              details:
                reason: offboarding.settlement_outstanding
                blockers:
                  - open_buyer_orders
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    InvitationAcceptanceForbidden:
      description: |
        Two codes share this status, and the difference matters to the person
        holding the link.

        **`auth.email_unverified`** — the acceptor is signed in but has not
        verified their address. Acceptance has to be attributable to a
        reachable identity, so the remedy is to verify and click again.

        **`authz.permission_denied`** — the invitation was sent to somebody
        else. The one failure on this endpoint that is not a `404`: the caller
        already holds a valid token, so there is nothing left to conceal, and
        "sign in as the person this was sent to" is the only useful thing to
        say.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: authz.permission_denied
              message: This invitation was sent to a different email address. Sign in as that person to accept it.
              details: {}
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    PlatformKitchenSlugTaken:
      description: |
        The submitted data is invalid. On kitchen creation this is almost
        always the slug: `details.fields.slug` says so, and the check lives in
        the service rather than in a validation rule because the uniqueness
        question and the insert have to be the same decision — a validator that
        answered it separately would leave a race between the check and the
        write.

        The reference codes — `country_code`, `default_currency_code`,
        `default_language_code` — are checked against their platform tables and
        produce ordinary field messages rather than a foreign-key violation.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: validation.failed
              message: That slug is already taken.
              details:
                fields:
                  slug:
                    - That slug is already taken.
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    PlatformKitchenLifecycleConflict:
      description: |
        One code, `resource.conflict`, for two different things — and
        `details` tells them apart.

        **`details.status`** — the kitchen is not in the state this action
        starts from. Suspension insists on `active`; reactivation refuses one
        that is already `active`, and refuses a `closed` organisation outright,
        because closing is offboarding and its memberships were ended and its
        personal data purged. Reloading will not help; the state is the answer.

        **`details.current_lock_version`** — the write lost a race. Somebody
        else moved this kitchen since the validator was read, and the value to
        reload against comes back so a console can offer "reload" or "keep
        mine" without a second round trip.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: resource.conflict
              message: Only an active kitchen can be suspended.
              details:
                status: suspended
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
    RecordExportUnavailable:
      description: |
        The bundle exists and cannot be downloaded. `details.status` says which
        of the three it is: still `building` or `requested`, `failed`, or
        `expired` — the window has closed and `purgeExpired` has deleted the
        bytes while keeping the row, because deleting the row would erase the
        evidence that an export was ever made.

        A 409 rather than a 404 deliberately: the caller is looking at an export
        that exists and that they may see, and a 404 would be a lie about a row
        in front of them.
      headers:
        X-Correlation-Id:
          $ref: '#/components/headers/XCorrelationId'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            error:
              code: record_export.unavailable
              message: This bundle is not available to download.
              details:
                reason: record_export.not_downloadable
                status: building
              correlation_id: 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3d
  schemas:
    Uuid:
      type: string
      format: uuid
      description: A UUIDv7 identifier (plan §8).
      examples:
        - 0198c5f2-7d3a-7b1e-9c4d-2f6a8b0e1d3c
    Meta:
      type: object
      required:
        - correlation_id
      properties:
        correlation_id:
          $ref: '#/components/schemas/Uuid'
      additionalProperties: true
      description: |
        Envelope metadata. Always carries the correlation identifier;
        collections add `count`, and `/me` adds `permissions_version`.
    ErrorCode:
      type: string
      description: |
        The stable Healthy360 error vocabulary. Mirrors
        `Healthy360\Support\Api\ErrorCode`; a Pest test asserts the two agree.
      enum:
        - validation.failed
        - auth.unauthenticated
        - auth.invalid_credentials
        - auth.email_unverified
        - auth.two_factor_required
        - auth.two_factor_invalid
        - auth.step_up_required
        - auth.csrf_token_mismatch
        - auth.invalid_signature
        - context.organisation_required
        - context.organisation_forbidden
        - context.branch_out_of_scope
        - context.branch_required
        - authz.permission_denied
        - request.invalid
        - request.precondition_required
        - request.idempotency_key_reused
        - resource.not_found
        - resource.conflict
        - organisation.suspended
        - catalogue.in_use
        - catalogue.version_immutable
        - catalogue.allergen_unmapped
        - catalogue.publish_blocked
        - otp.invalid
        - otp.expired
        - otp.attempts_exceeded
        - otp.cooldown_active
        - otp.locked
        - otp.channel_unavailable
        - contact.already_in_use
        - account.verification_required
        - address.area_not_served
        - guest.session_invalid
        - cart.line_refused
        - cart.channel_refused
        - order.placement_refused
        - b2b.application_state_invalid
        - b2b.documents_incomplete
        - b2b.signatory_required
        - b2b.quotation_state_invalid
        - b2b.quotation_empty
        - subscription.refused
        - subscription.change_refused
        - closure.refused
        - offboarding.refused
        - offboarding.settlement_outstanding
        - record_export.unavailable
        - payment.refund_exceeds_capture
        - inventory.insufficient_stock
        - unit.conversion_unsupported
        - rate_limit.exceeded
        - server.internal_error
    Error:
      type: object
      required:
        - code
        - message
        - details
        - correlation_id
      properties:
        code:
          $ref: '#/components/schemas/ErrorCode'
        message:
          type: string
          description: A safe, human-readable summary. Never a stack trace.
        details:
          type: object
          description: |
            Structured, code-specific detail. `validation.failed` carries a
            `fields` map of field name to messages; `authz.permission_denied`
            carries the denying RBAC step; `auth.step_up_required` carries the
            confirmation endpoint.
          additionalProperties: true
        correlation_id:
          $ref: '#/components/schemas/Uuid'
      additionalProperties: false
    ErrorEnvelope:
      type: object
      required:
        - error
      properties:
        error:
          $ref: '#/components/schemas/Error'
      additionalProperties: false
    User:
      type: object
      required:
        - id
        - email
        - email_verified
        - two_factor_enabled
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        email:
          type: string
          format: email
        email_verified:
          type: boolean
        two_factor_enabled:
          type: boolean
          description: True only once enrolment has been confirmed.
    Profile:
      description: |
        Person data for a global identity. Null until a profile exists.
      oneOf:
        - type: 'null'
        - type: object
          required:
            - given_name
            - family_name
            - preferred_language_code
            - timezone
            - numbering_system
          properties:
            given_name:
              type: string
            family_name:
              type: string
            preferred_language_code:
              type: string
              minLength: 2
              maxLength: 2
              examples:
                - ar
            country_code:
              type:
                - string
                - 'null'
              minLength: 2
              maxLength: 2
              examples:
                - LB
            timezone:
              type: string
              examples:
                - Asia/Beirut
            numbering_system:
              type: string
              enum:
                - latn
                - arab
            date_of_birth:
              type:
                - string
                - 'null'
              format: date
    Organisation:
      type: object
      required:
        - id
        - slug
        - name
        - type
        - status
        - country_code
        - default_currency_code
        - default_language_code
        - capabilities
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        slug:
          type: string
          examples:
            - cedar-clinic
        name:
          type: string
          examples:
            - Cedar Clinic
        type:
          type:
            - string
            - 'null'
          description: The organisation type code.
          examples:
            - clinic
        status:
          type: string
          enum:
            - pending
            - active
            - suspended
            - closed
        country_code:
          type: string
          minLength: 2
          maxLength: 2
        default_currency_code:
          type: string
          minLength: 3
          maxLength: 3
        default_language_code:
          type: string
          minLength: 2
          maxLength: 2
        capabilities:
          type: array
          description: Enabled capability codes.
          items:
            type: string
          examples:
            - - clinic_services
    Branch:
      type: object
      required:
        - id
        - name
        - city
        - timezone
        - status
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        city:
          type:
            - string
            - 'null'
        timezone:
          type: string
        status:
          type: string
          enum:
            - active
            - closed
    Membership:
      type: object
      required:
        - id
        - status
        - branch_id
        - joined_at
        - organisation
        - roles
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
            - invited
            - active
            - suspended
          description: |
            Ended memberships are history and are never listed.
        branch_id:
          description: Set when the membership is scoped to a single branch.
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        joined_at:
          type:
            - string
            - 'null'
          format: date-time
        organisation:
          $ref: '#/components/schemas/Organisation'
        roles:
          type: array
          description: Assigned role codes.
          items:
            type: string
          examples:
            - - organisation_owner
    ActiveContext:
      description: |
        The validated working context. Null when nothing valid was claimed.
      oneOf:
        - type: 'null'
        - type: object
          required:
            - organisation
            - branch
            - membership_id
            - permissions
            - entitlements
          properties:
            organisation:
              $ref: '#/components/schemas/Organisation'
            branch:
              oneOf:
                - $ref: '#/components/schemas/Branch'
                - type: 'null'
            membership_id:
              $ref: '#/components/schemas/Uuid'
            permissions:
              type: array
              description: Calculated permission codes, in `domain.action_scope` form.
              items:
                type: string
              examples:
                - - organisation.view_current
                  - branch.view_current
            entitlements:
              type: array
              description: Feature codes the organisation is currently entitled to.
              items:
                type: string
    PendingConsent:
      type: object
      required:
        - code
        - version
        - purpose
      properties:
        code:
          type: string
          examples:
            - consent.health_data_processing
        version:
          type: integer
          minimum: 1
        purpose:
          type: string
          examples:
            - health_data_processing
    Device:
      type: object
      required:
        - id
        - device_name
        - platform
        - app_version
        - last_seen_at
        - created_at
        - is_current
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        device_name:
          type: string
          examples:
            - Nadia iPhone
        platform:
          type: string
          enum:
            - ios
            - android
            - web
        app_version:
          type:
            - string
            - 'null'
        last_seen_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        is_current:
          type: boolean
          description: Whether this device holds the credential making the request.
    TwoFactorState:
      type: object
      required:
        - enabled
        - confirmed
        - confirmed_at
      properties:
        enabled:
          type: boolean
          description: A secret exists.
        confirmed:
          type: boolean
          description: Enrolment is complete and two-factor authentication is armed.
        confirmed_at:
          type:
            - string
            - 'null'
          format: date-time
    RegisterRequest:
      type: object
      required:
        - email
        - password
        - password_confirmation
        - given_name
        - family_name
        - accepts_terms
        - accepts_privacy
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          minLength: 8
        password_confirmation:
          type: string
          format: password
        given_name:
          type: string
          maxLength: 255
        family_name:
          type: string
          maxLength: 255
        preferred_language_code:
          type: string
          minLength: 2
          maxLength: 2
          description: An active platform language. Defaults to `en`.
        country_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        timezone:
          type: string
          maxLength: 64
          description: An IANA timezone. Defaults to `UTC`.
        accepts_terms:
          type: boolean
          const: true
          description: Acceptance of the terms of service. Recorded as a consent grant.
        accepts_privacy:
          type: boolean
          const: true
          description: Acknowledgement of the privacy notice. Recorded as a consent grant.
        account_type:
          type: string
          enum:
            - b2c
          description: |
            Opens the provisional consumer account in the same transaction as
            the identity (J1). Omitted for a kitchen operator, who has an
            identity and no customer account; `b2b` and `guest` accounts are
            provisioned by their own journeys and may not be declared here.
    LoginRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
        remember:
          type: boolean
          default: false
    IssueTokenRequest:
      type: object
      required:
        - email
        - password
        - device_name
        - platform
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
        device_name:
          type: string
          maxLength: 255
          description: A human-readable device name, shown in device management.
        platform:
          type: string
          enum:
            - ios
            - android
            - web
        app_version:
          type:
            - string
            - 'null'
          maxLength: 64
        two_factor_code:
          type:
            - string
            - 'null'
          maxLength: 32
          description: Required when the account has confirmed two-factor authentication.
        recovery_code:
          type:
            - string
            - 'null'
          maxLength: 64
          description: An alternative to `two_factor_code`. Consumed and replaced on use.
    ResetPasswordRequest:
      type: object
      required:
        - token
        - email
        - password
        - password_confirmation
      properties:
        token:
          type: string
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          minLength: 8
        password_confirmation:
          type: string
          format: password
    TwoFactorChallengeRequest:
      type: object
      description: Supply either `code` or `recovery_code`.
      properties:
        code:
          type:
            - string
            - 'null'
          description: The current one-time password.
        recovery_code:
          type:
            - string
            - 'null'
          description: A single-use recovery code.
    PaginationMeta:
      allOf:
        - $ref: '#/components/schemas/Meta'
        - type: object
          required:
            - count
            - next_cursor
            - has_more
          properties:
            count:
              type: integer
              minimum: 0
              description: Items on this page, after the extra look-ahead row is trimmed.
            next_cursor:
              type:
                - string
                - 'null'
              description: Pass as `cursor` for the next page. Null when there is none.
            has_more:
              type: boolean
              description: |
                Whether another page exists. Answered by reading one row
                beyond the page rather than by a separate count, which would
                disagree with the page under concurrent writes.
    PaginatedOrNumberedMeta:
      description: |
        Which of the two the endpoint answers with is decided by the request:
        send `page` and it is `NumberedPaginationMeta`, omit it and it is
        `PaginationMeta`. Only the kitchen catalogue offers the choice.
      oneOf:
        - $ref: '#/components/schemas/PaginationMeta'
        - $ref: '#/components/schemas/NumberedPaginationMeta'
    NumberedPaginationMeta:
      description: |
        What the kitchen catalogue answers with when `page` was sent. The
        cursor fields are absent: a numbered page has no cursor to hand back,
        and a client that sent `page` is not walking.
      allOf:
        - $ref: '#/components/schemas/Meta'
        - type: object
          required:
            - count
            - page
            - per_page
            - total_count
            - total_pages
          properties:
            count:
              type: integer
              minimum: 0
              description: Rows on this page. Below `per_page` only on the last one.
            page:
              type: integer
              minimum: 1
              description: The page served, echoing the request.
            per_page:
              type: integer
              minimum: 1
              maximum: 100
            total_count:
              type: integer
              minimum: 0
              description: |
                Rows in the whole collection **after filtering** — so a page
                control under a filtered list counts what the filter left, not
                what the catalogue holds.
            total_pages:
              type: integer
              minimum: 0
              description: |
                `0` for an empty collection, so nothing renders "page 1 of 0".

                Costs a `COUNT` the keyset path deliberately avoids, and can
                therefore disagree with the page under a concurrent write. That
                staleness is accepted: a control that says "of 12" a moment
                after a thirteenth page appeared is a smaller problem than a
                control that cannot say "of" at all.
    AllergenCode:
      type: string
      maxLength: 20
      pattern: ^[a-z][a-z0-9_]*$
      description: |
        A canonical allergen class code — an immutable regulatory identity.
        Never renamed and never deleted; a class that should no longer be
        offered is deactivated instead. The fourteen seeded values match the
        universal client's `AllergenCode` union one for one.
      examples:
        - gluten
        - tree_nut
        - sulphites
    IngredientStatus:
      type: string
      enum:
        - active
        - inactive
        - archived
      description: |
        The operational lifecycle of an ingredient. Deliberately not the
        publication family (`draft`/`published`/…): an ingredient is never
        sold, so it has no publication state of its own.
    IngredientVerificationStatus:
      type: string
      enum:
        - verified
        - unverified
        - requires_review
      description: |
        How much the platform trusts what it holds about this ingredient.
        `requires_review` is a recorded contradiction awaiting a human — the
        seeded burghul and pita rows carry it because the source workbook
        tags them allergen-free while its own allergen key does not.
    AvailabilityTier:
      type:
        - string
        - 'null'
      enum:
        - core
        - common
        - specialty_imported
        - null
      description: Local availability in the launch market. Null where the source does not say.
    IngredientNutrientAmount:
      type: object
      required:
        - nutrient_id
        - unit
        - value
      properties:
        nutrient_id:
          type: string
          enum:
            - energy
            - protein
            - carbohydrate
            - fat
            - fibre
            - sugars
            - saturated_fat
            - sodium
        unit:
          type: string
          enum:
            - kcal
            - kJ
            - g
            - mg
        value:
          type: number
          minimum: 0
      additionalProperties: false
    IngredientNutritionPer100g:
      type: object
      description: |
        Per-100 g reference facts for an ingredient — the landing zone the
        later recipe-rollup phase reads. The same envelope family as a
        catalogue item's `nutrition_facts`, kept to what an operator can
        actually assert today: a basis pinned to `per_100g` and the eight
        core nutrient amounts. Source/calculation provenance arrives with
        the rollup phase, not before.
      required:
        - basis
        - amounts
      properties:
        basis:
          type: string
          enum:
            - per_100g
        amounts:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/IngredientNutrientAmount'
      additionalProperties: false
    AdminIngredient:
      type: object
      required:
        - id
        - organisation_id
        - is_platform
        - is_editable
        - slug
        - name_en
        - name_ar
        - ingredient_category_id
        - ingredient_subcategory_id
        - default_unit_id
        - yield_factor
        - status
        - verification_status
        - lock_version
      description: |
        The administrative shape of an ingredient. Carries **both** names and
        ignores `Accept-Language` for them: a bilingual editor has to see what
        it is editing. There is no public ingredient projection.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Null for a platform-library row.
        is_platform:
          type: boolean
          description: |
            Whether this is a platform-library row — which library it is in,
            and nothing more. Read `is_editable` before drawing an edit
            control: a platform row is editable by the platform operator and
            read-only to every kitchen, so the two answers differ by caller.
        is_editable:
          type: boolean
          description: |
            Whether **this caller** may write this row, so a client never
            offers a control that 403s. True for a caller's own rows, and for
            platform-library rows when the selected organisation is the
            platform operator.
        slug:
          type: string
          maxLength: 120
        name_en:
          type: string
        name_ar:
          type: string
          description: Falls back to the English name where no Arabic exists.
        ingredient_category_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        ingredient_subcategory_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        default_unit_id:
          $ref: '#/components/schemas/Uuid'
        default_unit_code:
          type:
            - string
            - 'null'
        purchase_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The pack this is bought in; the default unit stays the usage unit.
        purchase_unit_code:
          type:
            - string
            - 'null'
        composition:
          type:
            - string
            - 'null'
          description: |
            The source workbook's "Made From" transcription — a coarse,
            kitchen-facing ingredient list, never a quantified formulation.
        items_per_unit:
          type:
            - string
            - 'null'
          description: Pieces per purchase pack, as a two-place decimal string.
        nutrition_per_100g:
          oneOf:
            - $ref: '#/components/schemas/IngredientNutritionPer100g'
            - type: 'null'
        b2b_price_amount:
          type:
            - string
            - 'null'
          description: |
            Trade list price in major currency units, as a six-place decimal
            string so no client rounds it. A list price on the article — not
            a cost (that is org-specific and lives in the inventory valuation
            tables) and not a channel tariff (that is a price list).
          examples:
            - '5.000000'
        b2c_price_amount:
          type:
            - string
            - 'null'
          description: Consumer list price, same units and shape as `b2b_price_amount`.
          examples:
            - '3.000000'
        unit_price_amount:
          type:
            - string
            - 'null'
          description: |
            List price of one stock unit, same units and shape as
            `b2b_price_amount`. Still a list price and still not a cost: what a
            kitchen actually paid moves with every receipt and lives in the
            inventory valuation tables.
          examples:
            - '3.500000'
        price_currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
          description: |
            ISO 4217 code all three amounts are quoted in. Never null while any
            of them is set — a monetary value without its currency is not a
            monetary value.
        is_sellable:
          type: boolean
          description: |
            Offered for sale as-is, outside recipes. Distinct from
            `availability_tier`, which describes how hard the ingredient is to
            source rather than whether it is on sale. Defaults to false: an
            ingredient is a raw material until somebody says otherwise.
        yield_factor:
          type: string
          description: Decimal with four places, as a string so no client rounds it.
          examples:
            - '1.0000'
        forked_from_ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        availability_tier:
          $ref: '#/components/schemas/AvailabilityTier'
        status:
          $ref: '#/components/schemas/IngredientStatus'
        verification_status:
          $ref: '#/components/schemas/IngredientVerificationStatus'
        notes:
          type:
            - string
            - 'null'
        source_system:
          type:
            - string
            - 'null'
        source_ref:
          type:
            - string
            - 'null'
        lock_version:
          type: integer
          minimum: 0
          description: Also served as the `ETag` of the single-resource GET.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
        allergens:
          type: array
          description: |
            The allergen mappings visible to this caller — the platform
            baseline, plus the caller's own overlay in a tenant context. Both
            layers, each labelled, for the reason the dedicated sub-resource
            gives: a list showing only the overlay would let a kitchen believe
            an ingredient carries no allergens when the baseline says milk.

            Present on the collection and the single resource. An empty array
            means "none declared"; the property being absent means the
            endpoint did not load them, and a client must not render the
            second as the first.
          items:
            $ref: '#/components/schemas/IngredientAllergenMapping'
      additionalProperties: false
    IngredientEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - ingredient
          properties:
            ingredient:
              $ref: '#/components/schemas/AdminIngredient'
        meta:
          $ref: '#/components/schemas/Meta'
    IngredientAlias:
      type: object
      required:
        - id
        - alias
        - alias_normalised
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        alias:
          type: string
          maxLength: 160
          description: Verbatim, as the source or the operator wrote it.
        alias_normalised:
          type: string
          maxLength: 160
          description: |
            Server-derived: lower-cased, trimmed, internal whitespace
            collapsed. Deliberately conservative — it does not stem or strip
            punctuation, because "Paprika Sweet" and "Paprika Smoked" are
            different ingredients.
        locale:
          type:
            - string
            - 'null'
          enum:
            - en
            - ar
            - null
        source_system:
          type:
            - string
            - 'null'
        source_ref:
          type:
            - string
            - 'null'
      additionalProperties: false
    IngredientCategory:
      type: object
      required:
        - id
        - organisation_id
        - is_platform
        - parent_id
        - code
        - name_en
        - name_ar
        - display_order
        - is_active
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        is_platform:
          type: boolean
        parent_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Null for a top-level category.
        code:
          type: string
          maxLength: 40
        name_en:
          type: string
        name_ar:
          type: string
        display_order:
          type: integer
        is_active:
          type: boolean
      additionalProperties: false
    IngredientCategoryEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - category
          properties:
            category:
              $ref: '#/components/schemas/IngredientCategory'
        meta:
          $ref: '#/components/schemas/Meta'
    IngredientAllergenMapping:
      type: object
      required:
        - id
        - allergen_code
        - layer
        - organisation_id
        - containment
        - market_scope
        - source
        - verification_status
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        allergen_code:
          $ref: '#/components/schemas/AllergenCode'
        layer:
          type: string
          enum:
            - platform_baseline
            - organisation_overlay
          description: |
            Derived, not stored. The client renders a baseline row read-only
            and its own overlay editable; asking it to infer that from a
            nullable identifier is how a UI ends up offering an edit control
            that always 403s.
        organisation_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        containment:
          type: string
          enum:
            - contains
            - may_contain
          description: An ingredient of the ingredient, or a cross-contact risk. Legally different statements.
        market_scope:
          type: string
          enum:
            - all
            - us_only
            - eu_only
          description: |
            EU-14 and US Big-9 are different lists. Coconut is a tree nut
            under US law and not an EU allergen, so it is carried as a
            `us_only` mapping rather than a note nobody reads.
        source:
          type: string
          enum:
            - master_list
            - technical_sheet
            - supplier_declaration
            - kitchen_declared
            - inferred
        verification_status:
          type: string
          enum:
            - verified
            - unverified
            - requires_review
            - requires_supplier_confirmation
        evidence:
          type:
            - string
            - 'null'
          description: Free text supporting the statement — a supplier reference, a sheet, a note.
      additionalProperties: false
    IngredientAllergenCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/IngredientAllergenMapping'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateIngredientRequest:
      type: object
      required:
        - name_en
        - default_unit_id
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
          description: Falls back to the English name when absent.
        slug:
          type:
            - string
            - 'null'
          maxLength: 110
          pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
          description: Derived from the name when absent, and de-duplicated within the organisation.
        ingredient_category_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        ingredient_subcategory_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        default_unit_id:
          $ref: '#/components/schemas/Uuid'
        purchase_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        composition:
          type:
            - string
            - 'null'
          maxLength: 2000
        items_per_unit:
          type:
            - number
            - 'null'
          exclusiveMinimum: 0
        nutrition_per_100g:
          oneOf:
            - $ref: '#/components/schemas/IngredientNutritionPer100g'
            - type: 'null'
        b2b_price_amount:
          type:
            - number
            - 'null'
          minimum: 0
          description: Trade list price in major currency units. Requires `price_currency_code`.
        b2c_price_amount:
          type:
            - number
            - 'null'
          minimum: 0
          description: Consumer list price in major currency units. Requires `price_currency_code`.
        unit_price_amount:
          type:
            - number
            - 'null'
          minimum: 0
          description: List price of one stock unit, in major currency units. Requires `price_currency_code`.
        price_currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
          description: ISO 4217 code. Required whenever any amount is sent.
        is_sellable:
          type: boolean
          description: Offered for sale as-is, outside recipes.
        yield_factor:
          type: number
          exclusiveMinimum: 0
          maximum: 99.9999
        availability_tier:
          $ref: '#/components/schemas/AvailabilityTier'
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
      additionalProperties: false
    UpdateIngredientRequest:
      type: object
      minProperties: 1
      description: |
        A partial update. `slug` and `status` are absent by design — the slug
        is the handle a re-import converges on, and status changes are
        lifecycle actions with their own routes.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type: string
          maxLength: 255
        ingredient_category_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        ingredient_subcategory_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        default_unit_id:
          $ref: '#/components/schemas/Uuid'
        purchase_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        composition:
          type:
            - string
            - 'null'
          maxLength: 2000
        items_per_unit:
          type:
            - number
            - 'null'
          exclusiveMinimum: 0
        nutrition_per_100g:
          oneOf:
            - $ref: '#/components/schemas/IngredientNutritionPer100g'
            - type: 'null'
        b2b_price_amount:
          type:
            - number
            - 'null'
          minimum: 0
          description: Trade list price in major currency units. Requires `price_currency_code`.
        b2c_price_amount:
          type:
            - number
            - 'null'
          minimum: 0
          description: Consumer list price in major currency units. Requires `price_currency_code`.
        unit_price_amount:
          type:
            - number
            - 'null'
          minimum: 0
          description: List price of one stock unit, in major currency units. Requires `price_currency_code`.
        price_currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
          description: ISO 4217 code. Required whenever any amount is sent.
        is_sellable:
          type: boolean
          description: Offered for sale as-is, outside recipes.
        yield_factor:
          type: number
          exclusiveMinimum: 0
          maximum: 99.9999
        availability_tier:
          $ref: '#/components/schemas/AvailabilityTier'
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
      additionalProperties: false
    ReplaceIngredientAllergensRequest:
      type: object
      required:
        - mappings
      properties:
        market_scope:
          type: string
          enum:
            - all
            - us_only
            - eu_only
          default: all
          description: The scope whose set is being replaced. Other scopes are untouched.
        mappings:
          type: array
          maxItems: 50
          description: |
            The complete statement for this scope and this layer. An empty
            array means "no allergens", which is a statement rather than a
            missing field — and is refused where a platform baseline would be
            weakened by it.
          items:
            type: object
            required:
              - allergen_code
              - containment
            properties:
              allergen_code:
                $ref: '#/components/schemas/AllergenCode'
              containment:
                type: string
                enum:
                  - contains
                  - may_contain
              source:
                type:
                  - string
                  - 'null'
                enum:
                  - master_list
                  - technical_sheet
                  - supplier_declaration
                  - kitchen_declared
                  - inferred
                  - null
              verification_status:
                type:
                  - string
                  - 'null'
                enum:
                  - verified
                  - unverified
                  - requires_review
                  - requires_supplier_confirmation
                  - null
              evidence:
                type:
                  - string
                  - 'null'
                maxLength: 2000
            additionalProperties: false
      additionalProperties: false
    RecipeStatus:
      type: string
      enum:
        - active
        - archived
      description: |
        The operational lifecycle of a recipe *identity*. Deliberately not the
        publication family: a recipe is never published, its versions are.
        There is no `inactive` either — "temporarily not in use" is expressed
        by having no published version.
    RecipeConfidentiality:
      type: string
      enum:
        - internal
        - confidential
      default: confidential
      description: |
        How guarded a formulation is. Defaults to `confidential` because a
        recipe is a kitchen's commercial secret until somebody decides
        otherwise, and a default that leaks is a default that is wrong exactly
        once. Neither value makes a formulation public: recipe lines are on
        the public denylist regardless.
    RecipeVersionStatus:
      type: string
      enum:
        - draft
        - review_required
        - published
        - retired
      description: |
        The publication lifecycle of a version. `review_required` is a
        **stored** quarantine state, not a flag beside one: a critical
        allergen contradiction has to block publication structurally, and a
        boolean next to a status is something a publish path can forget to
        read. A quarantined version is still editable — the point is that
        somebody fixes it — and is as unpublishable as a draft.

        "Publishable" is never stored; it is the readiness evaluator's verdict
        at the moment of publication.
    RecipeCompleteness:
      type: string
      enum:
        - indicative
        - costed
      description: |
        `indicative` is a formulation without costs — which is what the
        sauces-and-dressings source provides, and recording that honestly
        beats inventing figures. `costed` is what a technical sheet is.
    DerivationState:
      type: string
      enum:
        - current
        - stale
        - failed
      description: |
        Whether the frozen allergen label still matches the ingredient
        mappings it was computed from. A new version is `stale` — nothing has
        been derived for it, and claiming `current` for an empty label would be
        the most dangerous default available. `failed` records that a recompute
        was attempted and could not complete, which must never be
        indistinguishable from "not tried yet".
    AllergenDerivation:
      type: string
      enum:
        - declared
        - derived
      description: |
        Where a label row came from. `derived` was computed from the effective
        ingredient mappings and carries the ingredient that caused it;
        `declared` is a human statement — a chef who knows the fryer is shared
        — that no mapping implies. A derivation never weakens a declaration.
    AdminRecipe:
      type: object
      required:
        - id
        - organisation_id
        - slug
        - name_en
        - name_ar
        - confidentiality
        - status
        - published_version_number
        - lock_version
      description: |
        The administrative shape of a recipe identity. Carries **both** names
        and ignores `Accept-Language` for them: a bilingual editor has to see
        what it is editing. There is no public recipe projection.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        slug:
          type: string
          maxLength: 120
        name_en:
          type: string
        name_ar:
          type: string
          description: Falls back to the English name where no Arabic exists.
        recipe_category:
          type:
            - string
            - 'null'
          maxLength: 40
          description: |
            Free text, deliberately not an enumeration: the source vocabulary
            (sauce, dressing, marination, patty, topping, component) describes
            how one kitchen files its sheets, not a regulated identity, and an
            enum would turn "we started making dips" into a migration.
        source_kind:
          type:
            - string
            - 'null'
          maxLength: 40
          description: The source sheet's `Kind`, verbatim.
        confidentiality:
          $ref: '#/components/schemas/RecipeConfidentiality'
        status:
          $ref: '#/components/schemas/RecipeStatus'
        notes:
          type:
            - string
            - 'null'
        published_version_number:
          type:
            - integer
            - 'null'
          minimum: 1
          description: |
            The live version, or null when nothing is published. Computed, not
            stored: there is no `current_version_id` column to fall out of
            step with the versions themselves.
        source_system:
          type:
            - string
            - 'null'
        source_ref:
          type:
            - string
            - 'null'
        lock_version:
          type: integer
          minimum: 0
          description: Also served as the `ETag` of the single-resource GET.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    RecipeEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - recipe
          properties:
            recipe:
              $ref: '#/components/schemas/AdminRecipe'
        meta:
          $ref: '#/components/schemas/Meta'
    AdminRecipeVersion:
      type: object
      required:
        - id
        - recipe_id
        - version_number
        - status
        - completeness
        - waste_coefficient_percent
        - derivation_state
        - lock_version
      description: |
        The header of a recipe version. `derived_input_hash` is deliberately
        absent from the wire: it is an internal fingerprint, and publishing it
        would invite clients to compare hashes instead of reading
        `derivation_state`.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        recipe_id:
          $ref: '#/components/schemas/Uuid'
        version_number:
          type: integer
          minimum: 1
        status:
          $ref: '#/components/schemas/RecipeVersionStatus'
        completeness:
          $ref: '#/components/schemas/RecipeCompleteness'
        yield_quantity:
          type:
            - string
            - 'null'
          description: Decimal with four places, as a string so no client rounds it.
          examples:
            - '1000.0000'
        yield_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        yield_piece_count:
          type:
            - integer
            - 'null'
          minimum: 1
        input_quantity_total:
          type:
            - string
            - 'null'
          description: |
            The sum of the input lines, kept alongside the yield because the
            source sheets state both and they legitimately differ.
        waste_coefficient_percent:
          type: string
          description: |
            Decimal with two places, as a string. Defaults to `3.00` — the flat
            allowance the source technical sheets apply across the board.
          examples:
            - '3.00'
        b2b_price_amount:
          type:
            - string
            - 'null'
          description: |
            Trade list price for one unit of the yield, in **major** currency
            units, as a string so no client rounds it. A list price and not a
            cost: what the version is offered at to a kitchen or corporate
            buyer, rather than what its inputs cost to buy.
          examples:
            - '18.500000'
        b2c_price_amount:
          type:
            - string
            - 'null'
          description: |
            Consumer list price for one unit of the yield — the counterpart to
            `b2b_price_amount`, quoted in the same currency.
          examples:
            - '24.000000'
        price_currency_code:
          oneOf:
            - $ref: '#/components/schemas/CurrencyCode'
            - type: 'null'
          description: |
            One currency for both amounts. A version quoted in two currencies
            is a price list, not a column.
        derivation_state:
          $ref: '#/components/schemas/DerivationState'
        derived_at:
          type:
            - string
            - 'null'
          format: date-time
        published_at:
          type:
            - string
            - 'null'
          format: date-time
        review_reason:
          type:
            - string
            - 'null'
          maxLength: 200
          description: Why this version is quarantined, when it is.
        notes:
          type:
            - string
            - 'null'
        lock_version:
          type: integer
          minimum: 0
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    RecipeVersionEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - version
          properties:
            version:
              $ref: '#/components/schemas/AdminRecipeVersion'
        meta:
          $ref: '#/components/schemas/Meta'
    RecipeLine:
      type: object
      required:
        - id
        - line_number
        - ingredient_id
      description: |
        One formulation line.

        **No cost fields.** `recipe_version_lines` carries
        `unit_cost_amount`, `line_cost_amount` and `cost_currency_code`, and
        this projection reads none of them. The cost surface is K1.3, behind
        `recipe.view_costs_organisation`; until that permission exists there is
        no way to serve a cost to the right people, so the honest projection
        serves it to nobody.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        line_number:
          type: integer
          minimum: 1
          description: Server-authored from the submitted order.
        ingredient_id:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type:
            - string
            - 'null'
          description: |
            Decimal with four places, as a string. Null is legal only while the
            version is unpublished — the publish gate is where nullability
            ends.
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        source_designation:
          type:
            - string
            - 'null'
          maxLength: 160
          description: The import source wording, verbatim.
        comment:
          type:
            - string
            - 'null'
          maxLength: 255
      additionalProperties: false
    PackagingBasis:
      type: string
      enum:
        - fills_yield
        - per_container
        - per_batch
      description: |
        How a packaging quantity is arrived at.

        * `fills_yield` — the container the yield goes into. The server divides
          the yield by the item's recorded capacity and rounds up, so the
          quantity is a container count nobody types.
        * `per_container` — one per container filled, multiplied by the count
          `fills_yield` produced. A cap goes on every bottle.
        * `per_batch` — a flat quantity per batch, independent of the yield.
          The only basis that accepts `quantity`.
    RecipePackagingLine:
      type: object
      required:
        - id
        - line_number
        - ingredient_id
        - basis
        - quantity
      description: |
        One packaging line, as stored.

        **No cost fields**, for `RecipeLine`'s reason and one of its own: the
        unit cost of a box is read from the catalogue row rather than carried
        here, so the costed view is the technical sheet and this projection has
        nothing to withhold.

        `basis` travels beside `quantity` and always. Without it the quantity
        is unreadable — `6` means "the yield fills six of these" on one basis
        and "somebody typed six" on another, and only the first is still right
        after the yield changes.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        line_number:
          type: integer
          minimum: 1
          description: Server-authored from the submitted order.
        ingredient_id:
          $ref: '#/components/schemas/Uuid'
        basis:
          $ref: '#/components/schemas/PackagingBasis'
        quantity:
          type: string
          description: |
            Decimal with four places, as a string. Derived on `fills_yield` and
            `per_container`; as submitted on `per_batch`.
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        comment:
          type:
            - string
            - 'null'
          maxLength: 255
      additionalProperties: false
    RecipeOutput:
      type: object
      required:
        - id
        - ingredient_id
        - output_quantity
        - unit_id
        - is_primary
      description: |
        What a version produces. An ingredient with a row here is what the
        design used to call an "intermediate"; there is no kind column
        anywhere, because being an intermediate is a fact about some version's
        outputs and not a property of the ingredient.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        ingredient_id:
          $ref: '#/components/schemas/Uuid'
        output_quantity:
          type: string
          examples:
            - '1000.0000'
        unit_id:
          $ref: '#/components/schemas/Uuid'
        is_primary:
          type: boolean
          description: At most one per version, enforced by a partial unique index.
      additionalProperties: false
    RecipeStep:
      type: object
      required:
        - id
        - step_number
        - instruction_en
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        step_number:
          type: integer
          minimum: 1
        instruction_en:
          type: string
        instruction_ar:
          type:
            - string
            - 'null'
        minutes:
          type:
            - integer
            - 'null'
          minimum: 0
      additionalProperties: false
    RecipeVersionAllergen:
      type: object
      required:
        - id
        - allergen_code
        - containment
        - derivation
      description: |
        One row of a version's frozen label — the only part of a recipe version
        that is ever meant to reach a diner.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        allergen_code:
          $ref: '#/components/schemas/AllergenCode'
        containment:
          type: string
          enum:
            - contains
            - may_contain
        derivation:
          $ref: '#/components/schemas/AllergenDerivation'
        source_ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            The line ingredient a derived row came from. Provenance is what
            makes a warning trusted: "sesame, from tahini" is acted on where a
            bare "sesame" is clicked past.
        source_note:
          type:
            - string
            - 'null'
          maxLength: 255
          description: |
            For a derived row, the market scopes that contributed. The label is
            one row per class, so a scope column would either duplicate rows or
            pick one scope and lose the rest.
      additionalProperties: false
    CostBasis:
      type: string
      enum:
        - as_recorded
        - recalculated
      description: |
        Where a snapshot's arithmetic came from.

        `recalculated` is arithmetic this system performed over the version's
        lines: reproducible, and re-derivable from data still in the database.

        `as_recorded` is what a source technical sheet stated, stored verbatim
        **including its errors**. Several of the source sheets do their own
        arithmetic and do it wrong; correcting one on import destroys the
        evidence that it needs correcting, and trusting one puts a wrong number
        behind a right-looking label. Storing both and saying which is which is
        the only honest option. Written by the private importer only.
    TechnicalSheetLine:
      type: object
      required:
        - id
        - line_number
        - ingredient_id
      description: |
        One costed formulation line. Served **only** from the technical sheet,
        behind `recipe.view_costs_organisation`; the ordinary `RecipeLine`
        projection carries none of these fields.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        line_number:
          type: integer
          minimum: 1
        ingredient_id:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type:
            - string
            - 'null'
          description: Decimal with four places, as a string so no client rounds it.
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        unit_cost_amount:
          type:
            - string
            - 'null'
          description: |
            Major currency units with six decimal places, as a string. Floats
            cannot represent `0.1`, and a cost whose total depends on the order
            the lines were summed in is not a cost.
          examples:
            - '3.900000'
        line_cost_amount:
          type:
            - string
            - 'null'
          description: |
            On the wire beside `unit_cost_amount` even though one is derivable
            from the other, because on an imported sheet they legitimately
            disagree: the source's own arithmetic is stored verbatim, and a
            reader who could only see the inputs would have no way to notice.
          examples:
            - '9.750000'
        cost_currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
        source_designation:
          type:
            - string
            - 'null'
          maxLength: 160
        comment:
          type:
            - string
            - 'null'
          maxLength: 255
      additionalProperties: false
    CostSnapshot:
      type: object
      required:
        - id
        - recipe_version_id
        - basis
        - currency_code
        - total_input_cost_amount
        - waste_coefficient_percent
        - basis_mismatch
        - calculated_at
      description: |
        What one version cost, at one moment, on one basis. Append-only:
        `UPDATE` and `DELETE` are revoked from the application database role,
        so a snapshot is superseded by a newer one and never edited.

        The per-unit figures are null when the version does not say what it
        yields. Inventing a denominator to fill a column is the fabrication
        this programme exists to avoid.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        recipe_version_id:
          $ref: '#/components/schemas/Uuid'
        basis:
          $ref: '#/components/schemas/CostBasis'
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: Every amount on this snapshot is in this one currency. Nothing converts between currencies.
        total_input_cost_amount:
          type: string
          description: The sum of the line costs, major units, six decimal places.
          examples:
            - '10.000000'
        cost_per_yield_unit_amount:
          type:
            - string
            - 'null'
          description: Total ÷ `yield_quantity`. Null unless the version states both a yield quantity and a yield unit.
        yield_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The unit `cost_per_yield_unit_amount` is per. A per-unit cost without its unit is a number, not a cost.
        cost_per_piece_amount:
          type:
            - string
            - 'null'
          description: Total ÷ `yield_piece_count`. Null unless the version counts pieces.
        waste_coefficient_percent:
          type: string
          description: |
            Copied from the version at calculation time rather than joined at
            read time: a kitchen that later revises its allowance must not
            retroactively rewrite what an old snapshot said. The source sheets
            apply a flat `3.00`.
          examples:
            - '3.00'
        cost_per_yield_unit_with_waste_amount:
          type:
            - string
            - 'null'
          description: The per-unit figure × (1 + waste ÷ 100), rounded once.
        cost_per_piece_with_waste_amount:
          type:
            - string
            - 'null'
        source_label:
          type:
            - string
            - 'null'
          maxLength: 60
          description: |
            The source sheet's own wording, verbatim — "Cost per kg", "U.P.".
            Kept as evidence and **never** consulted to decide what a number
            means: at least five source sheets label a figure "cost per kg"
            beside a piece count. The basis is derived from the version's yield
            fields instead.
        basis_mismatch:
          type: boolean
          description: |
            The label claims something the version's yield cannot support.
            Served with the figures and never without them: a number that looks
            authoritative while it is under review is worse than no number.
            Conservative by design — wording nobody can classify raises
            nothing, because a flag that cannot be substantiated teaches
            reviewers to ignore the flag.
        calculated_at:
          type: string
          format: date-time
        created_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    TechnicalSheet:
      type: object
      required:
        - version
        - completeness
        - currency_conflict
        - lines
        - uncosted_line_numbers
        - snapshots
      description: The confidential cost projection of one recipe version.
      properties:
        version:
          $ref: '#/components/schemas/AdminRecipeVersion'
        completeness:
          $ref: '#/components/schemas/RecipeCompleteness'
        currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
          description: |
            The single currency the costed lines share. Null when nothing is
            costed **or** when an import left more than one behind;
            `currency_conflict` says which, because "no currency" and "several
            currencies" need different fixes.
        currency_conflict:
          type: boolean
        lines:
          type: array
          items:
            $ref: '#/components/schemas/TechnicalSheetLine'
        uncosted_line_numbers:
          type: array
          description: |
            Lines that contributed nothing to a total, in line order — no
            quantity, no unit cost, or no currency. While this is non-empty no
            `recalculated` snapshot can be written, because a total that
            silently omits a line reads exactly like a total that did not.
          items:
            type: integer
            minimum: 1
        snapshots:
          type: object
          required:
            - recalculated
            - as_recorded
          description: |
            The latest snapshot of each basis. Both keys are always present, so
            a client never has to guess whether a missing key means "none" or
            "not asked for"; either value may be null.
          properties:
            recalculated:
              oneOf:
                - $ref: '#/components/schemas/CostSnapshot'
                - type: 'null'
            as_recorded:
              oneOf:
                - $ref: '#/components/schemas/CostSnapshot'
                - type: 'null'
          additionalProperties: false
      additionalProperties: false
    CreateRecipeRequest:
      type: object
      required:
        - name_en
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
          description: Falls back to the English name when absent.
        slug:
          type:
            - string
            - 'null'
          maxLength: 110
          pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
          description: Derived from the name when absent, and de-duplicated within the organisation.
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        recipe_category:
          type:
            - string
            - 'null'
          maxLength: 40
        source_kind:
          type:
            - string
            - 'null'
          maxLength: 40
        confidentiality:
          $ref: '#/components/schemas/RecipeConfidentiality'
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
      additionalProperties: false
    UpdateRecipeRequest:
      type: object
      minProperties: 1
      description: |
        A partial update. `slug` and `status` are absent by design — the slug
        is the handle a re-import converges on, and status changes are
        lifecycle actions with their own routes.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type: string
          maxLength: 255
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        recipe_category:
          type:
            - string
            - 'null'
          maxLength: 40
        source_kind:
          type:
            - string
            - 'null'
          maxLength: 40
        confidentiality:
          $ref: '#/components/schemas/RecipeConfidentiality'
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
      additionalProperties: false
    CreateRecipeVersionRequest:
      type: object
      description: |
        An empty body opens a blank draft. `copy_from_version` is a version
        *number* — what a human reads on a technical sheet — resolved inside
        this recipe only.
      properties:
        copy_from_version:
          type:
            - integer
            - 'null'
          minimum: 1
      additionalProperties: false
    UpdateRecipeVersionRequest:
      type: object
      minProperties: 1
      description: |
        A partial update of a version header. `status`, `derivation_state`,
        `derived_at`, `published_at` and `published_by` are absent by design:
        each is a conclusion the server reached.
      properties:
        completeness:
          $ref: '#/components/schemas/RecipeCompleteness'
        yield_quantity:
          type:
            - number
            - 'null'
          exclusiveMinimum: 0
          maximum: 99999999.9999
        yield_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        yield_piece_count:
          type:
            - integer
            - 'null'
          minimum: 1
        input_quantity_total:
          type:
            - number
            - 'null'
          exclusiveMinimum: 0
          maximum: 99999999.9999
        waste_coefficient_percent:
          type: number
          minimum: 0
          maximum: 999.99
        b2b_price_amount:
          type:
            - number
            - 'null'
          minimum: 0
          maximum: 1000000000000
          description: |
            Trade list price per unit of yield, in **major** currency units.
            Zero is a real price — a staff meal, a component carried at cost —
            and `null` is how a price is cleared, which is a different act.
        b2c_price_amount:
          type:
            - number
            - 'null'
          minimum: 0
          maximum: 1000000000000
          description: Consumer list price per unit of yield, in **major** currency units.
        price_currency_code:
          oneOf:
            - $ref: '#/components/schemas/CurrencyCode'
            - type: 'null'
          description: |
            Required alongside either amount: a monetary value without its
            currency is not a monetary value (§4.4).
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
      additionalProperties: false
    ReplaceRecipeLinesRequest:
      type: object
      required:
        - lines
      properties:
        lines:
          type: array
          maxItems: 200
          description: |
            The complete formulation. An empty array is a legitimate statement
            ("this version has no lines yet"), not a missing field. The array
            order is the line sequence, and duplicate ingredients are allowed.
          items:
            type: object
            required:
              - ingredient_id
            properties:
              ingredient_id:
                $ref: '#/components/schemas/Uuid'
              quantity:
                type:
                  - number
                  - 'null'
                exclusiveMinimum: 0
                maximum: 99999999.9999
              unit_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              unit_cost_amount:
                type:
                  - number
                  - string
                  - 'null'
                description: |
                  What one `unit_id` of this ingredient costs, in **major**
                  currency units with up to six decimal places — `3.9` is three
                  dollars ninety (master plan v2 §4.4). Prices are a different
                  concept on a different table and stay integer minor units;
                  nothing sums one into the other.

                  Zero is legal and meaningful: a donated or self-produced
                  input costs nothing, and saying so is not the same as saying
                  nothing.

                  Requires `recipe.view_costs_organisation` **in addition to**
                  `recipe.manage_organisation`, otherwise `403`. So does
                  replacing a set that already carries costs *without* them —
                  that erases money the caller could not see, and positional
                  line identity means it cannot be put back.

                  `line_cost_amount` is not accepted anywhere: it is quantity ×
                  unit cost, the server derives it, and a derived value a
                  client can supply is one that can disagree with its inputs.
                minimum: 0
                maximum: 1000000000000
              cost_currency_code:
                type:
                  - string
                  - 'null'
                minLength: 3
                maxLength: 3
                description: |
                  ISO 4217, case-insensitive on the way in and canonical on the
                  way out. Required whenever `unit_cost_amount` is present and
                  refused when it is absent — an amount without a currency is
                  not a monetary value, and a currency without an amount is not
                  one either.

                  Every costed line of one version must share a single
                  currency; a second is `422 validation.failed`.
              source_designation:
                type:
                  - string
                  - 'null'
                maxLength: 160
              comment:
                type:
                  - string
                  - 'null'
                maxLength: 255
            additionalProperties: false
      additionalProperties: false
    ReplaceRecipePackagingRequest:
      type: object
      required:
        - packaging
      properties:
        packaging:
          type: array
          maxItems: 50
          description: |
            The complete packaging set. An empty array is a legitimate
            statement ("this version packs into nothing yet"), not a missing
            field. The array order is the line sequence.
          items:
            type: object
            required:
              - ingredient_id
              - basis
            properties:
              ingredient_id:
                $ref: '#/components/schemas/Uuid'
              basis:
                $ref: '#/components/schemas/PackagingBasis'
              quantity:
                type:
                  - number
                  - 'null'
                exclusiveMinimum: 0
                maximum: 99999999.9999
                description: |
                  Required on `per_batch` and refused on the other two bases,
                  which derive their own. The server answers with what it
                  stored, which on those two is not what was sent.
              comment:
                type:
                  - string
                  - 'null'
                maxLength: 255
            additionalProperties: false
      additionalProperties: false
    CreateCostSnapshotRequest:
      type: object
      required:
        - basis
      description: |
        No amounts, deliberately. Every figure on a `recalculated` snapshot is
        derived from the version's own lines and yield.
      properties:
        basis:
          type: string
          enum:
            - recalculated
          description: |
            Only `recalculated` is accepted here. `as_recorded` exists on the
            table and means "this is what a source technical sheet stated" —
            the private importer's basis, never an endpoint's.
      additionalProperties: false
    ReplaceRecipeOutputsRequest:
      type: object
      required:
        - outputs
      properties:
        outputs:
          type: array
          maxItems: 20
          description: |
            An empty array is legitimate. A non-empty one must name exactly one
            primary, and may not name the same ingredient twice.
          items:
            type: object
            required:
              - ingredient_id
              - output_quantity
              - unit_id
            properties:
              ingredient_id:
                $ref: '#/components/schemas/Uuid'
              output_quantity:
                type: number
                exclusiveMinimum: 0
                maximum: 99999999.9999
              unit_id:
                $ref: '#/components/schemas/Uuid'
              is_primary:
                type:
                  - boolean
                  - 'null'
                default: false
            additionalProperties: false
      additionalProperties: false
    ReplaceRecipeStepsRequest:
      type: object
      required:
        - steps
      properties:
        steps:
          type: array
          maxItems: 100
          description: The complete method. The array order is the step sequence.
          items:
            type: object
            required:
              - instruction_en
            properties:
              instruction_en:
                type: string
                maxLength: 4000
              instruction_ar:
                type:
                  - string
                  - 'null'
                maxLength: 4000
              minutes:
                type:
                  - integer
                  - 'null'
                minimum: 0
                maximum: 10000
            additionalProperties: false
      additionalProperties: false
    PublicAllergenClass:
      type: object
      required:
        - code
        - name
        - regulatory_ref
        - is_eu_14
        - is_us_big_9
        - us_declaration_required
        - display_order
      description: |
        The anonymous projection of an allergen class: exactly one
        server-localised name and description, plus the regulatory metadata a
        customer needs to read a label correctly.
      properties:
        code:
          $ref: '#/components/schemas/AllergenCode'
        name:
          type: string
          description: Localised from `Accept-Language` (`en` or `ar`).
        description:
          type:
            - string
            - 'null'
        regulatory_ref:
          type: string
          maxLength: 20
          examples:
            - ALG-01
        is_eu_14:
          type: boolean
        is_us_big_9:
          type: boolean
        us_declaration_required:
          type: boolean
          description: Whether US labelling requires a declaration above a threshold.
        us_threshold_ppm:
          type:
            - integer
            - 'null'
          description: The declaration threshold in parts per million — 10 for sulphites.
        severe_by_default:
          type: boolean
          description: |
            Whether a declaration form should pre-mark this class as serious.
            A **presentation default, never a diagnosis**: what a particular
            person's reaction is remains theirs to state, and nothing here
            overrides it. Seven classes carry it — crustaceans, egg, fish,
            peanut, tree nut, sesame, mollusc — because their reactions are
            most often systemic rather than local.
        display_order:
          type: integer
      additionalProperties: false
    AdminAllergenClass:
      type: object
      required:
        - code
        - name_en
        - name_ar
        - regulatory_ref
        - is_eu_14
        - is_us_big_9
        - us_declaration_required
        - display_order
        - is_active
      description: |
        The governance shape: both languages, and the activation flag the
        public projection has no business carrying.
      properties:
        code:
          $ref: '#/components/schemas/AllergenCode'
        name_en:
          type: string
        name_ar:
          type: string
        description_en:
          type:
            - string
            - 'null'
        description_ar:
          type:
            - string
            - 'null'
        regulatory_ref:
          type: string
          maxLength: 20
        is_eu_14:
          type: boolean
        is_us_big_9:
          type: boolean
        us_declaration_required:
          type: boolean
        us_threshold_ppm:
          type:
            - integer
            - 'null'
        severe_by_default:
          type: boolean
        display_order:
          type: integer
        is_active:
          type: boolean
      additionalProperties: false
    AllergenClassEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - allergen_class
          properties:
            allergen_class:
              $ref: '#/components/schemas/AdminAllergenClass'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateAllergenClassRequest:
      type: object
      required:
        - code
        - name_en
        - name_ar
        - regulatory_ref
        - is_eu_14
        - is_us_big_9
      properties:
        code:
          $ref: '#/components/schemas/AllergenCode'
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type: string
          maxLength: 255
        description_en:
          type:
            - string
            - 'null'
          maxLength: 2000
        description_ar:
          type:
            - string
            - 'null'
          maxLength: 2000
        regulatory_ref:
          type: string
          maxLength: 20
        is_eu_14:
          type: boolean
        is_us_big_9:
          type: boolean
        us_declaration_required:
          type:
            - boolean
            - 'null'
        us_threshold_ppm:
          type:
            - integer
            - 'null'
          minimum: 1
        severe_by_default:
          type:
            - boolean
            - 'null'
        display_order:
          type:
            - integer
            - 'null'
          minimum: 0
      additionalProperties: false
    UpdateAllergenClassRequest:
      type: object
      minProperties: 1
      description: |
        `code` is deliberately absent and is actively rejected rather than
        ignored: a request that appeared to rename a regulatory identity and
        quietly did nothing would be worse than one that failed.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type: string
          maxLength: 255
        description_en:
          type:
            - string
            - 'null'
          maxLength: 2000
        description_ar:
          type:
            - string
            - 'null'
          maxLength: 2000
        regulatory_ref:
          type: string
          maxLength: 20
        is_eu_14:
          type: boolean
        is_us_big_9:
          type: boolean
        us_declaration_required:
          type: boolean
        us_threshold_ppm:
          type:
            - integer
            - 'null'
          minimum: 1
        severe_by_default:
          type: boolean
        display_order:
          type: integer
          minimum: 0
      additionalProperties: false
    CatalogueItemType:
      type: string
      enum:
        - product
        - meal
        - subscription_plan
        - sauce
        - dressing
      description: |
        Which kind of sellable thing an item is — the discriminator that lets
        products, meals and subscription plans share one table, one price path,
        one availability table and one publication gate. What genuinely differs
        between them is nullable columns and child tables, not the apparatus
        around them.
    CatalogueItemStatus:
      type: string
      enum:
        - draft
        - review_required
        - published
        - retired
      description: |
        The publication lifecycle of a sellable item — the **same** four-state
        family a recipe version carries, and deliberately not the
        `active | archived` pair the operational tables use.

        `review_required` is a stored **quarantine**, not a queue position: a
        critical allergen contradiction must make publication structurally
        impossible rather than decorate the row with a flag a publish path can
        forget to read.

        `retired` is terminal for consumer visibility and keeps the row,
        because an order or a price snapshot may point at it forever. Sellable
        items **retire; they never archive**.
    CatalogueVariantType:
      type: string
      enum:
        - pack
        - plan_configuration
      description: |
        Whether a variant is a pack of a product or one configuration of a
        subscription plan. Derived from the item's own type and never accepted
        from a client; a meal has neither.
    CatalogueVariantStatus:
      type: string
      enum:
        - draft
        - active
        - archived
      description: |
        The operational lifecycle of a variant. **Not** the publication family,
        and the asymmetry with the parent item is deliberate: a pack is not
        separately published. Publishing the 500 g jar while the 1 kg jar sits
        in draft is not a state a kitchen wants, it is a state a kitchen ends
        up in.
    CataloguePackFormat:
      type: string
      enum:
        - bottle
        - bag
        - can
        - gallon
        - bunch
        - loose
      description: |
        The container a pack comes in. `loose` is a real answer, not a missing
        one, which is why the field is nullable *and* carries this value: "the
        kitchen said loose" and "nobody has said" are different facts.
    CatalogueProductionMode:
      type: string
      enum:
        - production
        - supplier
        - both
      description: |
        Whether the kitchen makes this, buys it in, or both. `both` is not
        indecision: several source articles are produced in-house when volume
        allows and bought in when it does not.
    SalesChannelKind:
      type: string
      enum:
        - b2c_web
        - b2b
        - pos
        - marketplace
        - corporate
        - insurance
      description: |
        What sort of route to market a channel is. The *kind* is a closed
        vocabulary a projection branches on; the *channel* is a row an
        organisation owns, because two kitchens legitimately run two different
        wholesale desks. The universal client's `SalesChannel` union is the
        prototype's flattening of the two.
    SalesChannelStatus:
      type: string
      enum:
        - active
        - inactive
      description: |
        Whether a channel is currently trading. Operational, never the
        publication family: nothing a customer sees is a channel — what a
        customer sees is the items available through one.
    AdminSalesChannel:
      type: object
      required:
        - id
        - organisation_id
        - code
        - channel_kind
        - name_en
        - name_ar
        - status
        - lock_version
      description: |
        The administrative shape of a sales channel. Carries **both** names and
        ignores `Accept-Language` for them.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 40
          description: Immutable after creation — a price list and an order's provenance name it.
        channel_kind:
          $ref: '#/components/schemas/SalesChannelKind'
        name_en:
          type: string
        name_ar:
          type: string
        order_source:
          type:
            - string
            - 'null'
          maxLength: 30
          description: |
            How orders arriving here label themselves in a partner's system.
            Deliberately unconstrained: that is somebody else's vocabulary.
        status:
          $ref: '#/components/schemas/SalesChannelStatus'
        lock_version:
          type: integer
          minimum: 0
          description: Also served as the `ETag` of the single-resource GET.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    SalesChannelEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - sales_channel
          properties:
            sales_channel:
              $ref: '#/components/schemas/AdminSalesChannel'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateSalesChannelRequest:
      type: object
      required:
        - code
        - channel_kind
        - name_en
      properties:
        code:
          type: string
          maxLength: 40
          pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
        channel_kind:
          $ref: '#/components/schemas/SalesChannelKind'
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        order_source:
          type:
            - string
            - 'null'
          maxLength: 30
      additionalProperties: false
    UpdateSalesChannelRequest:
      type: object
      minProperties: 1
      description: |
        `code` and `channel_kind` are deliberately absent. Changing either
        means creating the new channel and deactivating the old one — which is
        what actually happened.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type: string
          maxLength: 255
        order_source:
          type:
            - string
            - 'null'
          maxLength: 30
        status:
          $ref: '#/components/schemas/SalesChannelStatus'
      additionalProperties: false
    AdminCatalogueItem:
      type: object
      required:
        - id
        - organisation_id
        - catalogue_id
        - item_type
        - slug
        - name_en
        - name_ar
        - is_market_priced
        - is_assorted
        - status
        - data_quality_flags
        - lock_version
      description: |
        The administrative shape of a sellable item. Carries **both** names and
        ignores `Accept-Language` for them: a bilingual editor has to see what
        it is editing.

        **No price, cost or margin field exists here or on any child shape**,
        because no such column exists on any table in this family. Money lives
        in the pricing tables; the admin contract's confidential
        `marginPercent` is a presentation figure derived from a confirmed price
        and a recipe cost per serving, and has no server-side home in this
        slice.

        There is no public projection yet. When one arrives it will be a
        separate schema with its own denylist sweep, never this one with fields
        removed.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_id:
          $ref: '#/components/schemas/Uuid'
        item_type:
          $ref: '#/components/schemas/CatalogueItemType'
        slug:
          type: string
          maxLength: 140
          description: |
            Immutable after creation. A rename changes the names and nothing
            else: a slug is what a link, a marketplace listing and a partner's
            integration hold.
        name_en:
          type: string
        name_ar:
          type: string
          description: |
            The empty string is the "not yet translated" state, and the publish
            gate refuses it. Unlike a recipe, there is **no fallback** to the
            English name: a customer-facing name has to be visible as
            untranslated.
        description_en:
          type:
            - string
            - 'null'
        description_ar:
          type:
            - string
            - 'null'
        product_category_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        product_category_code:
          type:
            - string
            - 'null'
          description: |
            The platform code of the published category, when the relation is
            loaded — the value a client should file the item under instead of
            resolving the id itself.
        composition:
          type:
            - string
            - 'null'
          description: |
            The source workbook's "Recipe (Made From)" transcription — coarse,
            kitchen-facing prose, never a quantified formulation.
        kitchen_category:
          type:
            - string
            - 'null'
          description: The kitchen's own nested filing label, transcribed text.
        kitchen_subcategory:
          type:
            - string
            - 'null'
          description: The merchandising shelf, not the purchasing taxonomy.
        production_mode:
          oneOf:
            - $ref: '#/components/schemas/CatalogueProductionMode'
            - type: 'null'
        recipe_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            The recipe a meal is produced from. Its **published** version is
            the first choice of allergen basis.
        ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The ingredient a resold raw good simply is.
        purchasing_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        usage_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        is_market_priced:
          type: boolean
          description: |
            Goods priced at the day's market rate carry no confirmed amount at
            all. Not a price field — a fact about the article that pricing
            reads.
        is_assorted:
          type: boolean
          description: True for a row that stands for a mixed selection rather than one article.
        status:
          $ref: '#/components/schemas/CatalogueItemStatus'
        review_reason:
          type:
            - string
            - 'null'
          maxLength: 200
          description: Why the item is quarantined.
        image_placeholder_id:
          type:
            - string
            - 'null'
          maxLength: 80
        data_quality_flags:
          type: array
          description: Unresolved import findings, e.g. `dual_pack_single_price`.
          items:
            type: string
        source_system:
          type:
            - string
            - 'null'
        source_ref:
          type:
            - string
            - 'null'
        lock_version:
          type: integer
          minimum: 0
          description: |
            Also served as the `ETag` of the single-resource GET, and the
            validator every sub-resource write sends back as `If-Match`.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    CatalogueItemEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - item
          properties:
            item:
              $ref: '#/components/schemas/AdminCatalogueItem'
        meta:
          $ref: '#/components/schemas/Meta'
    AdminCatalogueItemVariant:
      type: object
      required:
        - id
        - variant_type
        - code
        - is_default
        - status
        - pack
        - lock_version
      description: The thing a price actually points at — one pack, or one plan configuration.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        variant_type:
          $ref: '#/components/schemas/CatalogueVariantType'
        code:
          type: string
          maxLength: 60
          description: Stable within the item, and what a price list names.
        name_en:
          type:
            - string
            - 'null'
        name_ar:
          type:
            - string
            - 'null'
        is_default:
          type: boolean
          description: |
            Exactly one per item, enforced by a partial unique index rather
            than by a service remembering to clear the incumbent first.
        status:
          $ref: '#/components/schemas/CatalogueVariantStatus'
        pack:
          oneOf:
            - $ref: '#/components/schemas/CataloguePackDetail'
            - type: 'null'
          description: Present for a pack variant, null for a plan configuration.
        lock_version:
          type: integer
          minimum: 0
      additionalProperties: false
    CataloguePackDetail:
      type: object
      required:
        - pack_quantity
        - pack_unit_id
      properties:
        pack_quantity:
          type: string
          description: A decimal(12,4) as a string, so no precision is lost in transport.
        pack_unit_id:
          $ref: '#/components/schemas/Uuid'
        pack_piece_count:
          type:
            - integer
            - 'null'
          minimum: 1
          description: Units inside the pack — a tray of twelve is `12`.
        pack_format:
          oneOf:
            - $ref: '#/components/schemas/CataloguePackFormat'
            - type: 'null'
        net_weight_grams:
          type:
            - integer
            - 'null'
          minimum: 1
          description: |
            Nullable, and **never derived** from `pack_quantity`: that
            conversion needs a density this system does not hold, and
            fabricating one for a bunch of parsley is exactly the invented
            figure this programme refuses to produce.
      additionalProperties: false
    AdminCatalogueItemIngredient:
      type: object
      required:
        - id
        - ingredient_id
        - is_representative
        - display_order
      description: |
        One line of the public ingredient list — what a diner is told is in the
        dish. **Not a formulation**: there is no quantity here and there will
        not be.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        ingredient_id:
          $ref: '#/components/schemas/Uuid'
        is_representative:
          type: boolean
          description: Whether a listing card shows this one among the handful it has room for.
        display_order:
          type: integer
          minimum: 1
          description: Server-authored from the submitted order.
      additionalProperties: false
    AdminChannelAssignment:
      type: object
      required:
        - id
        - sales_channel_id
        - catalogue_item_variant_id
        - is_available
        - available_from
        - available_to
      description: |
        One statement that a channel offers this item, optionally narrowed to
        one variant and one date window. There is deliberately no price here.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        sales_channel_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Null offers the item as a whole.
        is_available:
          type: boolean
        available_from:
          type:
            - string
            - 'null'
          format: date
        available_to:
          type:
            - string
            - 'null'
          format: date
      additionalProperties: false
    DerivedAllergen:
      type: object
      required:
        - allergen_code
        - containment
        - derivation
        - source_ingredient_id
      properties:
        allergen_code:
          $ref: '#/components/schemas/AllergenCode'
        containment:
          type: string
          enum:
            - contains
            - may_contain
          description: |
            The strongest claim any source makes for this class. `contains`
            beats `may_contain`, never the other way round.
        derivation:
          type: string
          enum:
            - declared
            - derived
          description: |
            `declared` is a human's statement that no mapping implies — a chef
            who knows the fryer is shared. It survives a recomputation and is
            never weakened by one. Only a recipe version can carry a
            declaration; the ingredient-list basis is always `derived`.
        source_ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Which ingredient implied it. "Why does this dish say sesame" has to
            be answerable from the label alone.
      additionalProperties: false
    DerivedAllergenSet:
      type: object
      required:
        - basis
        - recipe_version_id
        - allergens
      properties:
        basis:
          type: string
          enum:
            - recipe_version
            - item_ingredients
            - none
          description: |
            Which source answered. `none` is an empty set that means "nobody
            has said", never "no allergens".
        recipe_version_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        allergens:
          type: array
          items:
            $ref: '#/components/schemas/DerivedAllergen'
      additionalProperties: false
    DerivedAllergenMeta:
      allOf:
        - $ref: '#/components/schemas/Meta'
        - type: object
          required:
            - basis
            - recipe_version_id
          properties:
            basis:
              type: string
              enum:
                - recipe_version
                - item_ingredients
                - none
            recipe_version_id:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
    ReadinessReason:
      type: object
      required:
        - code
        - detail
        - context
      properties:
        code:
          type: string
          description: |
            The stable machine key. The same vocabulary
            `catalogue.publish_blocked` carries in `details.reasons[].reason`,
            because the same evaluator produces both.
          examples:
            - line_quantity_missing
        detail:
          type: string
          description: |
            One sentence a human can act on. A client that renders its own copy
            per code should ignore this; a client that meets a code it does not
            know should print it rather than say nothing.
        context:
          type: object
          additionalProperties: true
          description: |
            Whatever identifies the offending rows — line numbers, identifiers,
            a status, a review reason, the configuration codes of unpriced
            cells. Nested rather than flattened beside `code` so that a reason
            carrying new structure never changes the shape of a reason. `{}`
            when the code says everything there is to say.
      additionalProperties: false
    PublicationReadiness:
      type: object
      required:
        - publishable
        - reasons
      properties:
        publishable:
          type: boolean
          description: |
            Exactly `reasons == []`. Both are on the wire because a badge wants
            the boolean and a checklist wants the list, and deriving one from
            the other at every call site is how the two drift apart in a UI.

            A verdict about the *data*, never about the caller's authority: a
            subscription plan additionally needs `plan.publish_organisation`,
            which is checked where the item type is known.
        reasons:
          type: array
          items:
            $ref: '#/components/schemas/ReadinessReason'
          description: |
            Every blocker, not the first one found — the rule the publish
            refusal has followed since K1.2, for the same reason: a gate that
            reveals one problem per attempt turns a five-minute fix into five
            round trips.
      additionalProperties: false
    RecipeVersionReadinessMeta:
      allOf:
        - $ref: '#/components/schemas/Meta'
        - type: object
          required:
            - recipe_id
            - version_number
            - status
            - derivation_state
          properties:
            recipe_id:
              $ref: '#/components/schemas/Uuid'
            version_number:
              type: integer
            status:
              $ref: '#/components/schemas/RecipeVersionStatus'
            derivation_state:
              $ref: '#/components/schemas/DerivationState'
    CatalogueItemReadinessMeta:
      allOf:
        - $ref: '#/components/schemas/Meta'
        - type: object
          required:
            - slug
            - item_type
            - status
          properties:
            slug:
              type: string
            item_type:
              $ref: '#/components/schemas/CatalogueItemType'
            status:
              $ref: '#/components/schemas/CatalogueItemStatus'
    CreateCatalogueItemRequest:
      type: object
      required:
        - item_type
        - name_en
      description: |
        Derived and server-authored values are absent by construction:
        `status` (always `draft`), `lock_version`, `organisation_id` and the
        source-provenance fields are not accepted, and neither are variants,
        ingredients, diet tags or channels — each of those is its own
        set-replace endpoint.
      properties:
        item_type:
          $ref: '#/components/schemas/CatalogueItemType'
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        slug:
          type:
            - string
            - 'null'
          maxLength: 130
          pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
        description_en:
          type:
            - string
            - 'null'
          maxLength: 4000
        description_ar:
          type:
            - string
            - 'null'
          maxLength: 4000
        composition:
          type:
            - string
            - 'null'
          maxLength: 2000
        kitchen_category:
          type:
            - string
            - 'null'
          maxLength: 120
        kitchen_subcategory:
          type:
            - string
            - 'null'
          maxLength: 120
        catalogue_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Omit to use (and create on demand) the organisation's `default` catalogue.
        product_category_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        production_mode:
          oneOf:
            - $ref: '#/components/schemas/CatalogueProductionMode'
            - type: 'null'
        recipe_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        purchasing_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        usage_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        is_market_priced:
          type: boolean
        is_assorted:
          type: boolean
        image_placeholder_id:
          type:
            - string
            - 'null'
          maxLength: 80
      additionalProperties: false
    UpdateCatalogueItemRequest:
      type: object
      minProperties: 1
      description: |
        `slug` and `item_type` are deliberately absent and are actively
        **rejected** rather than ignored: a request that appeared to move a
        slug and quietly did nothing would be worse than one that failed.
        `status` is absent because publication and retirement are POST
        sub-resource actions.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        description_en:
          type:
            - string
            - 'null'
          maxLength: 4000
        description_ar:
          type:
            - string
            - 'null'
          maxLength: 4000
        composition:
          type:
            - string
            - 'null'
          maxLength: 2000
        kitchen_category:
          type:
            - string
            - 'null'
          maxLength: 120
        kitchen_subcategory:
          type:
            - string
            - 'null'
          maxLength: 120
        product_category_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        production_mode:
          oneOf:
            - $ref: '#/components/schemas/CatalogueProductionMode'
            - type: 'null'
        recipe_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        purchasing_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        usage_unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        is_market_priced:
          type: boolean
        is_assorted:
          type: boolean
        image_placeholder_id:
          type:
            - string
            - 'null'
          maxLength: 80
      additionalProperties: false
    RetireCatalogueItemRequest:
      type: object
      description: |
        A kitchen withdrawing a seasonal line owes nobody an explanation, so a
        reason is optional; where one is given it is recorded on the audit
        event.
      properties:
        reason:
          type:
            - string
            - 'null'
          maxLength: 200
      additionalProperties: false
    ReplaceCatalogueItemVariantsRequest:
      type: object
      required:
        - variants
      properties:
        variants:
          type: array
          maxItems: 100
          description: |
            The complete set. An empty array is a legitimate statement ("this
            item has no variants") and is accepted; absent codes are archived,
            not deleted.
          items:
            type: object
            required:
              - code
            properties:
              id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
                description: |
                  Optional. Checked against this item and against the code, so
                  a client holding an identifier cannot silently retarget it.
              code:
                type: string
                maxLength: 60
              name_en:
                type:
                  - string
                  - 'null'
                maxLength: 255
              name_ar:
                type:
                  - string
                  - 'null'
                maxLength: 255
              is_default:
                type: boolean
                description: |
                  Two marked defaults is a stated contradiction and is refused.
                  None is not: the first submitted wins.
              status:
                $ref: '#/components/schemas/CatalogueVariantStatus'
              pack:
                oneOf:
                  - $ref: '#/components/schemas/CataloguePackDetail'
                  - type: 'null'
                description: |
                  Required for a pack — a pack with no size is not a pack — and
                  refused for a plan configuration.
            additionalProperties: false
      additionalProperties: false
    ReplaceCatalogueItemIngredientsRequest:
      type: object
      required:
        - ingredients
      properties:
        ingredients:
          type: array
          maxItems: 100
          description: |
            The complete list, in the order it should be named. There is no
            `display_order` field: the array order is the sequence.
          items:
            type: object
            required:
              - ingredient_id
            properties:
              ingredient_id:
                $ref: '#/components/schemas/Uuid'
              is_representative:
                type: boolean
            additionalProperties: false
      additionalProperties: false
    ReplaceCatalogueItemDietClassificationsRequest:
      type: object
      required:
        - diet_classifications
      properties:
        diet_classifications:
          type: array
          maxItems: 20
          description: |
            Platform codes. One that does not resolve to an *active*
            classification is a validation failure carrying
            `details.unknown`, never an invitation to create one.
          items:
            type: string
            maxLength: 40
      additionalProperties: false
    ReplaceCatalogueItemChannelsRequest:
      type: object
      required:
        - channels
      properties:
        channels:
          type: array
          maxItems: 50
          description: |
            The complete availability list. An empty array withdraws the item
            from every channel.
          items:
            type: object
            required:
              - sales_channel_id
            properties:
              sales_channel_id:
                $ref: '#/components/schemas/Uuid'
              catalogue_item_variant_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              is_available:
                type: boolean
                default: true
              available_from:
                type:
                  - string
                  - 'null'
                format: date
              available_to:
                type:
                  - string
                  - 'null'
                format: date
            additionalProperties: false
      additionalProperties: false
    PublicDietClassification:
      type: object
      required:
        - code
        - name
        - display_order
      description: |
        The public projection of a diet classification: **one** server-localised
        name, never both language columns.
      properties:
        code:
          type: string
          maxLength: 40
          description: |
            The platform vocabulary, matching the universal client's
            `DietClassification` union one for one.
        name:
          type: string
        display_order:
          type: integer
          minimum: 0
      additionalProperties: false
    CurrencyCode:
      type: string
      minLength: 3
      maxLength: 3
      pattern: ^[A-Z]{3}$
      description: |
        An ISO 4217 code. Every amount in the pricing family carries or
        inherits exactly one, and no operation performs cross-currency
        arithmetic.
      examples:
        - AED
        - USD
    PriceListStatus:
      type: string
      enum:
        - draft
        - active
        - archived
      description: |
        The **operational** lifecycle of a tariff, deliberately not the
        sellable family (`draft | review_required | published | retired`). A
        price list is not published content; it is the instrument that prices
        content, so it has no review state and no retirement. What a customer
        sees is a *price*, and whether one reaches them is decided by the
        channel assignment and the public projection.

        An `active` list is still editable — that is the point of
        effective-dating. An `archived` one refuses every write.
    PriceListCustomerScope:
      type: string
      enum:
        - public
        - agreement
      description: |
        Who the tariff is for. `public` is the tariff anybody may be shown;
        `agreement` is one customer's negotiated position, and showing it to a
        second customer is the failure this module is built to prevent.

        A column rather than an inference from the channel kind, because one
        channel legitimately carries both.
    PriceStatus:
      type: string
      enum:
        - confirmed
        - placeholder
        - market_priced
      description: |
        What the row actually claims — the honest badge.

        - `confirmed` — a real number the kitchen stands behind. Carries
          `unit_amount_minor`. **The only status a public surface may serve.**
        - `placeholder` — the source stated no price and nobody has supplied
          one. Amount null, never public: a placeholder rendered as a price is
          a lie with a currency symbol on it, and rendered as zero it is a
          worse one.
        - `market_priced` — quoted at the time of sale. Amount null, and the
          absence *is* the statement, which is why it is a status rather than a
          missing row.

        The pairing is enforced by a database CHECK —
        `(price_status = 'confirmed') = (unit_amount_minor IS NOT NULL)` — so a
        client may branch on either half and get the same answer.
    AdminPriceList:
      type: object
      required:
        - id
        - organisation_id
        - code
        - name_en
        - name_ar
        - currency_code
        - customer_scope
        - status
        - lock_version
      description: |
        The administrative shape of a price list header. Carries **both** names
        and ignores `Accept-Language` for them.

        No amounts: the header is a name, a currency and a status. The numbers
        are behind `…/entries`.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Usually null. A tariff is normally an organisation-wide instrument;
            naming a branch is for a kitchen that genuinely prices one location
            differently.
        code:
          type: string
          maxLength: 40
          description: Immutable after creation — an importer, a channel assignment and a runbook already name it.
        name_en:
          type: string
        name_ar:
          type: string
        currency_code:
          $ref: '#/components/schemas/CurrencyCode'
        customer_scope:
          $ref: '#/components/schemas/PriceListCustomerScope'
        status:
          $ref: '#/components/schemas/PriceListStatus'
        valid_from:
          type:
            - string
            - 'null'
          format: date
          description: Inclusive.
        valid_to:
          type:
            - string
            - 'null'
          format: date
          description: |
            **Inclusive** — a human writing "valid to 30 June" means through
            the 30th. Deliberately not the convention an entry's `effective_to`
            uses, which is exclusive because it is written by the supersession
            machinery rather than by a person.
        source_system:
          type:
            - string
            - 'null'
          maxLength: 40
        source_ref:
          type:
            - string
            - 'null'
          maxLength: 160
        lock_version:
          type: integer
          minimum: 0
          description: |
            Also served as the `ETag` of the single-resource GET, and the
            validator **every** write in this family sends back — the entries
            and the channel assignments included.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    AdminPriceListEntry:
      type: object
      required:
        - id
        - catalogue_item_id
        - currency_code
        - price_status
        - effective_from
      description: |
        One price, for one pricing point, over one interval of time.

        The pricing point is `(catalogue_item_id, catalogue_item_variant_id,
        min_quantity)`. The **standing** row of a point is the one whose
        `effective_to` is null; everything else is history, and history is
        closed rather than edited.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Null prices the article as a whole. Naming a variant prices exactly
            that pack or configuration; resolution prefers a variant's own row
            and falls back to the article's.
        min_quantity:
          type:
            - string
            - 'null'
          pattern: ^\d+\.\d{4}$
          description: |
            The tier threshold, as a fixed-scale decimal string. Null is the
            base price. A row applies from its threshold **upwards**, and
            resolution picks the highest threshold at or below the quantity
            asked for.

            A string rather than a number so the four decimal places survive
            the round trip: a client that parsed `10` and re-sent `10.0` would
            otherwise be describing a different key.
        unit_amount_minor:
          type:
            - integer
            - 'null'
          minimum: 1
          description: |
            The price, as a whole number of the **minor units** of
            `currency_code` — `4500` for 45.00 AED. Never a decimal, never
            formatted, never zero.

            Null exactly when `price_status` is not `confirmed`. The pair is a
            single fact and a database CHECK enforces it.
        currency_code:
          $ref: '#/components/schemas/CurrencyCode'
        price_status:
          $ref: '#/components/schemas/PriceStatus'
        effective_from:
          type: string
          format: date
          description: Inclusive.
        effective_to:
          type:
            - string
            - 'null'
          format: date
          description: |
            **Exclusive**, and null on the standing row. The row governed
            `[effective_from, effective_to)`.

            Exclusive is forced rather than chosen: a replacement made today
            closes the incumbent at today and opens its successor at today, so
            an inclusive end would leave two rows both claiming to be the price
            on the day of the change. A row changed twice in one day therefore
            leaves a zero-length interval, which is honest — the price never
            governed a whole day, and the record that somebody stated it
            survives.
        superseded_by_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            The row that replaced this one. Null on the standing row, and also
            null on a row that was simply withdrawn — "we stopped pricing this"
            is a different fact from "we now price it differently".
        created_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    PriceListChannelAssignment:
      type: object
      required:
        - id
        - sales_channel_id
        - price_list_id
        - priority
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        sales_channel_id:
          $ref: '#/components/schemas/Uuid'
        price_list_id:
          $ref: '#/components/schemas/Uuid'
        priority:
          type: integer
          description: Lower is consulted first. Assigned from the submitted array order unless stated.
      additionalProperties: false
    PriceListEntryCounts:
      type: object
      required:
        - open
        - confirmed
      description: |
        How many standing rows the list has, and how many of those are real
        prices. The pair is what a merchandiser reads as "312 priced, 8 owed".
      properties:
        open:
          type: integer
          minimum: 0
        confirmed:
          type: integer
          minimum: 0
      additionalProperties: false
    PriceListEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - price_list
          properties:
            price_list:
              $ref: '#/components/schemas/AdminPriceList'
        meta:
          $ref: '#/components/schemas/Meta'
    PriceListDetailEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - price_list
            - channels
          properties:
            price_list:
              $ref: '#/components/schemas/AdminPriceList'
            channels:
              type: array
              items:
                $ref: '#/components/schemas/PriceListChannelAssignment'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - entry_counts
              properties:
                entry_counts:
                  $ref: '#/components/schemas/PriceListEntryCounts'
    PriceListEntriesEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - price_list
            - entries
          properties:
            price_list:
              $ref: '#/components/schemas/AdminPriceList'
            entries:
              type: array
              description: The **standing** rows after the write, so a client need not fetch them back.
              items:
                $ref: '#/components/schemas/AdminPriceListEntry'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - currency_code
              properties:
                currency_code:
                  $ref: '#/components/schemas/CurrencyCode'
    PriceListChannelsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - price_list
            - channels
          properties:
            price_list:
              $ref: '#/components/schemas/AdminPriceList'
            channels:
              type: array
              items:
                $ref: '#/components/schemas/PriceListChannelAssignment'
        meta:
          $ref: '#/components/schemas/Meta'
    CreatePriceListRequest:
      type: object
      required:
        - code
        - name_en
        - currency_code
      properties:
        code:
          type: string
          maxLength: 40
          pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: Normalised to upper case, so a lowercase ISO code is not a second currency.
        customer_scope:
          $ref: '#/components/schemas/PriceListCustomerScope'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        valid_from:
          type:
            - string
            - 'null'
          format: date
        valid_to:
          type:
            - string
            - 'null'
          format: date
      additionalProperties: false
    UpdatePriceListRequest:
      type: object
      minProperties: 1
      description: |
        `code` is deliberately absent and is **rejected** rather than ignored
        if sent. `status` is absent too: activation and archiving are POST
        sub-resource actions.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: |
            Accepted only while the list holds no price rows at all, history
            included. Afterwards it is `409` with
            `details.reason: currency_locked`.
        customer_scope:
          $ref: '#/components/schemas/PriceListCustomerScope'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        valid_from:
          type:
            - string
            - 'null'
          format: date
        valid_to:
          type:
            - string
            - 'null'
          format: date
      additionalProperties: false
    ReplacePriceListEntriesRequest:
      type: object
      required:
        - entries
      description: |
        The desired **current** pricing state. `entries` is required but may be
        empty: an empty array withdraws every price, which is a decision a
        merchandiser makes and not a field they forgot.
      properties:
        entries:
          type: array
          maxItems: 500
          items:
            type: object
            required:
              - catalogue_item_id
              - price_status
            properties:
              catalogue_item_id:
                $ref: '#/components/schemas/Uuid'
              catalogue_item_variant_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              min_quantity:
                type:
                  - number
                  - string
                  - 'null'
                description: |
                  The tier threshold, above zero. Omitted or null is the base
                  price. Normalised to four decimal places, so `12`, `12.0` and
                  `"12.0000"` are one pricing point rather than three.
              unit_amount_minor:
                type:
                  - integer
                  - 'null'
                minimum: 1
                description: |
                  Required and positive when `price_status` is `confirmed`;
                  refused otherwise. Whole minor units — `1250` for 12.50 —
                  never a decimal and never a formatted string.
              price_status:
                $ref: '#/components/schemas/PriceStatus'
            additionalProperties: false
      additionalProperties: false
    ReplacePriceListChannelsRequest:
      type: object
      required:
        - channels
      description: |
        The complete set of channels quoting from this list. Empty detaches it
        from all of them — the step `archive` insists on first.
      properties:
        channels:
          type: array
          maxItems: 50
          items:
            type: object
            required:
              - sales_channel_id
            properties:
              sales_channel_id:
                $ref: '#/components/schemas/Uuid'
              priority:
                type:
                  - integer
                  - 'null'
                description: |
                  Lower is consulted first. Omitted, it comes from the array
                  order, which is what a merchandiser is stating when they list
                  a negotiated sheet above a standing tariff.
            additionalProperties: false
      additionalProperties: false
    PlanDurationKind:
      type: string
      enum:
        - one_off
        - fixed_days
      description: |
        Whether a duration is a single purchase or a fixed run of days.

        **This is what replaced the zero-day sentinel.** The legacy model wrote
        "not a subscription, just a one-off order" as a duration of `0` days â€” a
        magic value that reads as data, that every consumer has to know the
        convention for, and that a per-day calculation divides by. Branch on
        this, never on the number.
    PlanType:
      type: string
      enum:
        - both
        - subscription
        - limited_time
      description: |
        How a plan may be bought. Deliberately not two booleans: "sold both
        ways" is a third answer a kitchen gives rather than the conjunction of
        the other two, and a pair of flags would additionally let a plan say
        neither.
    PlanPricingBasis:
      type: string
      enum:
        - per_day
        - per_week
        - total
      description: |
        What the number on a price row *means* for this plan. It lives on the
        plan rather than on the price because it is a fact about the plan's
        commercial shape: two lists quoting the same plan on two different bases
        would be two answers to "what does a week cost".
    ServiceTier:
      type: string
      enum:
        - standard
        - premium
      description: One of the four coordinates of a matrix cell.
    MealCombinationOption:
      type: object
      required:
        - id
        - code
        - name_en
        - name_ar
        - includes_breakfast
        - includes_lunch
        - includes_dinner
        - meals_per_day
        - display_order
        - is_active
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 40
        name_en:
          type: string
        name_ar:
          type: string
        includes_breakfast:
          type: boolean
        includes_lunch:
          type: boolean
        includes_dinner:
          type: boolean
        meals_per_day:
          type: integer
          minimum: 1
          description: |
            Portions leaving the kitchen per day â€” **not** derived from the
            flags above, so "lunch, twice" stays representable.
        display_order:
          type: integer
        is_active:
          type: boolean
          description: |
            False is the withdrawal, and the only one there is: a combination a
            configuration references cannot be deleted.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    MealCombinationOptionEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - combination
          properties:
            combination:
              $ref: '#/components/schemas/MealCombinationOption'
        meta:
          $ref: '#/components/schemas/Meta'
    EnergyBand:
      type: object
      required:
        - id
        - code
        - name_en
        - name_ar
        - min_kcal
        - max_kcal
        - display_order
        - is_active
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 40
        name_en:
          type: string
        name_ar:
          type: string
        min_kcal:
          type: integer
          minimum: 0
        max_kcal:
          type: integer
          description: Strictly above `min_kcal` â€” a band whose ends meet is a single value.
        display_order:
          type: integer
        is_active:
          type: boolean
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    EnergyBandEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - energy_band
          properties:
            energy_band:
              $ref: '#/components/schemas/EnergyBand'
        meta:
          $ref: '#/components/schemas/Meta'
    PlanDurationOption:
      type: object
      required:
        - id
        - code
        - duration_kind
        - duration_days
        - name_en
        - name_ar
        - display_order
        - is_active
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 40
        duration_kind:
          $ref: '#/components/schemas/PlanDurationKind'
        duration_days:
          type:
            - integer
            - 'null'
          minimum: 1
          description: |
            **Null exactly when `duration_kind` is `one_off`**, and a positive
            integer exactly when it is `fixed_days`. Never `0`: that sentinel is
            what this model removed, and it is not coming back on the wire
            either.
        name_en:
          type: string
        name_ar:
          type: string
        display_order:
          type: integer
        is_active:
          type: boolean
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    PlanDurationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - duration
          properties:
            duration:
              $ref: '#/components/schemas/PlanDurationOption'
        meta:
          $ref: '#/components/schemas/Meta'
    PlanProfile:
      type: object
      required:
        - catalogue_item_id
        - plan_type
        - pricing_basis
        - allows_free_selection
        - skip_allowed
        - pause_allowed
        - change_cutoff_hours
      description: |
        The commercial terms of one subscription plan â€” how it is sold, how a
        price is quoted, and what a subscriber may do to a delivery once it is
        scheduled.
      properties:
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        plan_type:
          $ref: '#/components/schemas/PlanType'
        pricing_basis:
          $ref: '#/components/schemas/PlanPricingBasis'
        allows_free_selection:
          type: boolean
          description: Whether a subscriber chooses the dishes, or the kitchen decides.
        skip_allowed:
          type: boolean
        pause_allowed:
          type: boolean
        change_cutoff_hours:
          type: integer
          minimum: 0
          description: |
            How long before a delivery a subscriber may still change it.
            Defaults to **24**, which is not a guess: the kitchen operating rule
            and the legacy amendment window state the same thing from opposite
            directions. `0` means "until the van leaves".
        summary_en:
          type:
            - string
            - 'null'
        summary_ar:
          type:
            - string
            - 'null'
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    PlanProfileEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - item
            - profile
          properties:
            item:
              $ref: '#/components/schemas/AdminCatalogueItem'
            profile:
              oneOf:
                - $ref: '#/components/schemas/PlanProfile'
                - type: 'null'
              description: |
                Null when nobody has written terms yet â€” the state the publish
                gate refuses, and deliberately not filled in with defaults.
        meta:
          $ref: '#/components/schemas/Meta'
    PlanMenuSlot:
      type: string
      enum:
        - breakfast
        - lunch
        - dinner
        - snack
      description: |
        The four sittings, the same vocabulary a subscription meal choice
        stores — generation copies this value straight onto a choice row.
    PlanMenuEntry:
      type: object
      required:
        - id
        - cycle_day
        - slot
        - sequence
        - meal_catalogue_item_id
        - meal_name_en
        - meal_name_ar
      description: |
        One dish, in one slot, on one day of the plan's cycle.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        cycle_day:
          type: integer
          minimum: 1
          maximum: 366
          description: |
            1-based day of the cycle. **Day 1 is the anchor date itself**, and
            the cycle is anchored to the *plan*, not to each subscriber — every
            subscriber eats the same dish on the same date, which is what makes
            a day one production run rather than an à la carte service.
        slot:
          $ref: '#/components/schemas/PlanMenuSlot'
        sequence:
          type: integer
          minimum: 1
          maximum: 12
          description: |
            Disambiguates the kitchen that serves lunch twice. Defaults to 1.
        meal_catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        meal_name_en:
          type:
            - string
            - 'null'
        meal_name_ar:
          type:
            - string
            - 'null'
      additionalProperties: false
    PlanMenuCycle:
      type: object
      required:
        - cycle_days
        - anchor_date
      description: |
        The rotation the entries sit on. **Both null means no menu is
        published**, which is the behaviour every plan has by default and the
        state in which a subscription order deducts no stock.
      properties:
        cycle_days:
          type:
            - integer
            - 'null'
          minimum: 1
          maximum: 366
        anchor_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            The date cycle day 1 falls on. A date rather than an instant: a
            cycle turns over at the kitchen's midnight.
    PlanMenuEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - item
            - cycle
            - entries
          properties:
            item:
              $ref: '#/components/schemas/AdminCatalogueItem'
            cycle:
              $ref: '#/components/schemas/PlanMenuCycle'
            entries:
              type: array
              items:
                $ref: '#/components/schemas/PlanMenuEntry'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
              properties:
                count:
                  type: integer
    ReplacePlanMenuRequest:
      type: object
      required:
        - entries
        - menu_cycle_days
        - menu_cycle_anchor_date
      description: |
        The complete menu. All three fields are required to be *present* and
        may all be null or empty: that combination withdraws the menu, which is
        a decision a kitchen makes and not a field they forgot.

        They move together. Entries with no cycle length, or a cycle length
        with no entries, or a cycle length with no anchor, are each `422` —
        see the operation description.

        `catalogue_item_id` is not a field: the plan is in the URL.
      properties:
        entries:
          type: array
          maxItems: 400
          items:
            type: object
            required:
              - cycle_day
              - slot
              - meal_catalogue_item_id
            properties:
              cycle_day:
                type: integer
                minimum: 1
                maximum: 366
              slot:
                $ref: '#/components/schemas/PlanMenuSlot'
              sequence:
                type:
                  - integer
                  - 'null'
                minimum: 1
                maximum: 12
                description: Defaults to 1.
              meal_catalogue_item_id:
                allOf:
                  - $ref: '#/components/schemas/Uuid'
                description: |
                  Must be a **published meal** of this kitchen. A menu entry is
                  a promise to serve the dish, so a draft or a retired one is
                  `422`.
            additionalProperties: false
        menu_cycle_days:
          type:
            - integer
            - 'null'
          minimum: 1
          maximum: 366
        menu_cycle_anchor_date:
          type:
            - string
            - 'null'
          format: date
      additionalProperties: false
    PlanVariantCell:
      type: object
      required:
        - catalogue_item_variant_id
        - code
        - status
        - meal_combination_option_id
        - energy_band_id
        - service_tier
        - includes_snacks
        - meals_per_day
        - snacks_per_day
      description: |
        One cell of a plan's availability matrix, together with the variant that
        carries it. **The variant is the priceable thing**: a price row names
        `catalogue_item_variant_id`, exactly as it does for a pack.
      properties:
        catalogue_item_variant_id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 60
          description: |
            Derived from the cell's own coordinates unless one was supplied, so
            resubmitting a cell keeps the variant a price already points at.
        status:
          $ref: '#/components/schemas/CatalogueVariantStatus'
        name_en:
          type:
            - string
            - 'null'
        name_ar:
          type:
            - string
            - 'null'
        meal_combination_option_id:
          $ref: '#/components/schemas/Uuid'
        energy_band_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Null for a kitchen that does not portion by calories â€” a different
            fact from a band nobody filled in, and part of the cell's identity
            either way.
        service_tier:
          $ref: '#/components/schemas/ServiceTier'
        includes_snacks:
          type: boolean
        meals_per_day:
          type: integer
          minimum: 1
        snacks_per_day:
          type: integer
          minimum: 0
      additionalProperties: false
    PlanVariantsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - item
            - cells
          properties:
            item:
              $ref: '#/components/schemas/AdminCatalogueItem'
            cells:
              type: array
              items:
                $ref: '#/components/schemas/PlanVariantCell'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
              properties:
                count:
                  type: integer
    PlanDurationAssignment:
      type: object
      required:
        - id
        - catalogue_item_variant_id
        - plan_duration_id
        - discount_percent
        - is_available
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          $ref: '#/components/schemas/Uuid'
        variant_code:
          type:
            - string
            - 'null'
          maxLength: 60
        plan_duration_id:
          $ref: '#/components/schemas/Uuid'
        discount_percent:
          type:
            - string
            - 'null'
          description: |
            A decimal string, or **null when nobody has stated a discount** â€”
            which is commercially different from stating that there is none.
            Never returned as `0` for an unstated one, and never a float: it is
            served as `"0.00"` only when somebody actually said zero.
        is_available:
          type: boolean
          description: |
            False is "we still run this length, just not right now" â€” it keeps
            the negotiated discount, where dropping the row would discard it.
      additionalProperties: false
    PlanVariantDurationsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - item
            - assignments
          properties:
            item:
              $ref: '#/components/schemas/AdminCatalogueItem'
            assignments:
              type: array
              items:
                $ref: '#/components/schemas/PlanDurationAssignment'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
                - unstated_discount_count
              properties:
                count:
                  type: integer
                unstated_discount_count:
                  type: integer
                  description: How many assignments still carry no stated discount.
    CreateMealCombinationOptionRequest:
      type: object
      required:
        - code
        - name_en
        - meals_per_day
      properties:
        code:
          type: string
          maxLength: 40
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
          description: |
            Falls back to the English name: a vocabulary row is a label on a
            control, and a blank option cannot be chosen.
        includes_breakfast:
          type:
            - boolean
            - 'null'
        includes_lunch:
          type:
            - boolean
            - 'null'
        includes_dinner:
          type:
            - boolean
            - 'null'
        meals_per_day:
          type: integer
          minimum: 1
          maximum: 12
        display_order:
          type:
            - integer
            - 'null'
          minimum: 0
      additionalProperties: false
    UpdateMealCombinationOptionRequest:
      type: object
      description: |
        `code` is refused rather than ignored: a configuration identifier is
        derived from it.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type: string
          maxLength: 255
        includes_breakfast:
          type: boolean
        includes_lunch:
          type: boolean
        includes_dinner:
          type: boolean
        meals_per_day:
          type: integer
          minimum: 1
          maximum: 12
        display_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean
      additionalProperties: false
    CreateEnergyBandRequest:
      type: object
      required:
        - code
        - name_en
        - min_kcal
        - max_kcal
      properties:
        code:
          type: string
          maxLength: 40
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        min_kcal:
          type: integer
          minimum: 0
          maximum: 20000
        max_kcal:
          type: integer
          minimum: 1
          maximum: 20000
        display_order:
          type:
            - integer
            - 'null'
          minimum: 0
      additionalProperties: false
    UpdateEnergyBandRequest:
      type: object
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type: string
          maxLength: 255
        min_kcal:
          type: integer
          minimum: 0
          maximum: 20000
        max_kcal:
          type: integer
          minimum: 1
          maximum: 20000
        display_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean
      additionalProperties: false
    CreatePlanDurationRequest:
      type: object
      required:
        - code
        - duration_kind
        - name_en
      description: |
        `duration_days` is required exactly when `duration_kind` is
        `fixed_days`, and refused exactly when it is `one_off`. Both mismatches
        are `422`, and a CHECK constraint refuses them underneath.
      properties:
        code:
          type: string
          maxLength: 40
        duration_kind:
          $ref: '#/components/schemas/PlanDurationKind'
        duration_days:
          type:
            - integer
            - 'null'
          minimum: 1
          maximum: 3650
          description: |
            `0` is refused: it is the sentinel this model removed, not a
            zero-length subscription.
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        display_order:
          type:
            - integer
            - 'null'
          minimum: 0
      additionalProperties: false
    UpdatePlanDurationRequest:
      type: object
      description: |
        The kind/days correspondence is applied to the **merged** row, so
        turning a fixed run into a one-off means sending `duration_days: null`
        as well.
      properties:
        duration_kind:
          $ref: '#/components/schemas/PlanDurationKind'
        duration_days:
          type:
            - integer
            - 'null'
          minimum: 1
          maximum: 3650
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type: string
          maxLength: 255
        display_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean
      additionalProperties: false
    PutPlanProfileRequest:
      type: object
      description: |
        The whole small document. Every field is optional and omitted ones take
        their documented defaults, so `{}` legitimately means "the ordinary
        terms".
      properties:
        plan_type:
          oneOf:
            - $ref: '#/components/schemas/PlanType'
            - type: 'null'
        pricing_basis:
          oneOf:
            - $ref: '#/components/schemas/PlanPricingBasis'
            - type: 'null'
        allows_free_selection:
          type:
            - boolean
            - 'null'
        skip_allowed:
          type:
            - boolean
            - 'null'
          description: |
            Defaults to true â€” a plan whose profile says nothing about skipping
            is one a subscriber may skip.
        pause_allowed:
          type:
            - boolean
            - 'null'
        change_cutoff_hours:
          type:
            - integer
            - 'null'
          minimum: 0
          maximum: 720
          description: Defaults to 24. Zero is accepted and means "until the van leaves".
        summary_en:
          type:
            - string
            - 'null'
          maxLength: 2000
        summary_ar:
          type:
            - string
            - 'null'
          maxLength: 2000
      additionalProperties: false
    ReplacePlanVariantsRequest:
      type: object
      required:
        - cells
      description: |
        The complete matrix. `cells` is required but may be empty: an empty
        array archives every configuration, which is a decision a kitchen makes
        and not a field they forgot.

        `variant_type` is not a field â€” it is derived from the item's own type â€”
        and neither is `catalogue_item_id`, which is in the URL.
      properties:
        cells:
          type: array
          maxItems: 200
          items:
            type: object
            required:
              - meal_combination_option_id
              - meals_per_day
            properties:
              code:
                type:
                  - string
                  - 'null'
                maxLength: 60
                description: |
                  Omitted is the ordinary case: the server derives
                  `combination-tier[-band]` from the cell's own coordinates, so
                  a 4 Ã— 2 Ã— 5 matrix needs no hand-typed identifiers.
              name_en:
                type:
                  - string
                  - 'null'
                maxLength: 255
              name_ar:
                type:
                  - string
                  - 'null'
                maxLength: 255
              status:
                oneOf:
                  - $ref: '#/components/schemas/CatalogueVariantStatus'
                  - type: 'null'
              meal_combination_option_id:
                $ref: '#/components/schemas/Uuid'
              energy_band_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              service_tier:
                oneOf:
                  - $ref: '#/components/schemas/ServiceTier'
                  - type: 'null'
              includes_snacks:
                type:
                  - boolean
                  - 'null'
                description: |
                  Must agree with `snacks_per_day`: the contradiction is 422,
                  not a silent resolution.
              meals_per_day:
                type: integer
                minimum: 1
                maximum: 12
              snacks_per_day:
                type:
                  - integer
                  - 'null'
                minimum: 0
                maximum: 12
            additionalProperties: false
      additionalProperties: false
    ReplacePlanVariantDurationsRequest:
      type: object
      required:
        - assignments
      description: |
        The complete set of configuration Ã— duration pairings. Empty removes
        every one of them.

        Each row names its configuration by `catalogue_item_variant_id` **or**
        `variant_code`, and its duration by `plan_duration_id` **or**
        `duration_code`.
      properties:
        assignments:
          type: array
          maxItems: 500
          items:
            type: object
            properties:
              catalogue_item_variant_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              variant_code:
                type:
                  - string
                  - 'null'
                maxLength: 60
              plan_duration_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              duration_code:
                type:
                  - string
                  - 'null'
                maxLength: 40
              discount_percent:
                type:
                  - number
                  - string
                  - 'null'
                minimum: 0
                exclusiveMaximum: 100
                description: |
                  **Omitted or null means nobody has stated a discount**, and it
                  is stored and returned as null. Send `0` to state that there
                  is none â€” that is a different row for every reader afterwards.
              is_available:
                type:
                  - boolean
                  - 'null'
            additionalProperties: false
      additionalProperties: false
    DeliveryZoneStatus:
      type: string
      description: |
        The **operational** lifecycle of a zone, not the sellable family. A
        zone is configuration; what a customer sees is whether their address
        can be delivered to.

        `inactive` is a suspension a kitchen expects to reverse and it **keeps**
        its area claims; `archived` is terminal and **releases** them.
      enum:
        - active
        - inactive
        - archived
    DeliveryZoneScope:
      type: string
      description: |
        Whether the zone is the organisation's default map or one branch's
        override. Stated rather than inferred from a null `branch_id`, so a
        client does not have to know that NULL is a meaningful value here.
      enum:
        - organisation
        - branch
    DeliveryZone:
      type: object
      required:
        - id
        - organisation_id
        - branch_id
        - scope
        - code
        - name_en
        - name_ar
        - currency_code
        - delivery_fee_minor
        - minimum_order_minor
        - estimated_minutes
        - status
        - is_active
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Null is the organisation-wide map.
        scope:
          $ref: '#/components/schemas/DeliveryZoneScope'
        code:
          type: string
          maxLength: 40
          description: Fixed at creation. A request carrying it on update is refused.
        name_en:
          type: string
        name_ar:
          type: string
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: |
            ISO 4217. Both amounts on this zone are denominated in it, so they
            are comparable by construction and there is no cross-currency
            arithmetic anywhere.
        delivery_fee_minor:
          type:
            - integer
            - 'null'
          minimum: 0
          description: |
            Integer minor units, never a formatted string and never a float.
            **Null means nobody has decided what delivery costs**; `0` means
            delivery is free. Those are different commercial statements and one
            is never rendered as the other.
        minimum_order_minor:
          type:
            - integer
            - 'null'
          minimum: 0
          description: Integer minor units. Null means no minimum has been stated.
        estimated_minutes:
          type:
            - integer
            - 'null'
          minimum: 1
          description: A promise, not a measurement. Null means none has been made.
        status:
          $ref: '#/components/schemas/DeliveryZoneStatus'
        is_active:
          type: boolean
          description: |
            Whether this zone actually serves. Derived from `status`, so it
            cannot disagree with it — only an `active` zone quotes a delivery.
        source_system:
          type:
            - string
            - 'null'
          maxLength: 40
        source_ref:
          type:
            - string
            - 'null'
          maxLength: 160
        lock_version:
          type: integer
          minimum: 0
          description: |
            The optimistic-concurrency validator, echoed as the `ETag`. The
            zone's **area set** is versioned against this same number.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    DeliveryZoneEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - delivery_zone
          properties:
            delivery_zone:
              $ref: '#/components/schemas/DeliveryZone'
        meta:
          $ref: '#/components/schemas/Meta'
    DeliveryZoneDetailEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - delivery_zone
          properties:
            delivery_zone:
              $ref: '#/components/schemas/DeliveryZone'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                area_count:
                  type: integer
                  minimum: 0
                  description: How many places this zone covers. The places themselves are one request away.
    DeliveryZonesEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DeliveryZone'
        meta:
          $ref: '#/components/schemas/PaginatedOrNumberedMeta'
    AdminDeliveryArea:
      type: object
      required:
        - id
        - country_code
        - code
        - name_en
        - name_ar
        - region
        - display_order
        - is_active
      description: |
        A platform place as an administrator sees it — both language columns
        and the active flag, because a kitchen choosing areas needs to know
        that one it already serves has been withdrawn.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        country_code:
          type: string
          minLength: 2
          maxLength: 2
        code:
          type: string
          maxLength: 60
          description: Unique within the country, never globally — place names repeat across borders.
        name_en:
          type: string
        name_ar:
          type: string
        region:
          type:
            - string
            - 'null'
          maxLength: 60
          description: |
            Governorate or district. **Null on every row today** (OD-12): the
            source does not record it, and an invented grouping would look
            exactly like data.
        display_order:
          type: integer
        is_active:
          type: boolean
      additionalProperties: false
    MarketplaceMoney:
      type: object
      description: |
        An integer number of minor units and its currency code. `4200` with
        `AED` is 42.00 AED. Amounts are never formatted server-side and never
        travel without their currency (master plan v2 §4.4).
      required:
        - amount
        - currency
      properties:
        amount:
          type: integer
          minimum: 0
        currency:
          type: string
          minLength: 3
          maxLength: 3
      additionalProperties: false
    MarketplaceSalesChannels:
      type: object
      description: |
        The eight consumer switches, derived from the kitchen's **active**
        sales channels. The platform stores six channel *kinds*, and the
        mapping is: `b2c_web` sets `b2c`, `b2b` sets `b2b`, `pos` sets `pos`,
        `marketplace` sets `marketplace`, `corporate` sets `corporate`, and
        `insurance` sets nothing (there is no consumer switch for it, and
        mapping it onto `corporate` would tell a diner that a kitchen with an
        insurer agreement takes corporate orders).

        **`subscription`, `delivery` and `pickup` are always `false`.** That is
        a statement rather than an omission: `subscription` is a property of
        what is sold, not of a route to market, and no column anywhere records a
        delivery or pickup arrangement. Deriving them from something adjacent —
        "it has a delivery zone, so it delivers" — would publish an inference as
        a fact on the one surface where a customer acts on it.
      required:
        - b2c
        - b2b
        - marketplace
        - pos
        - subscription
        - delivery
        - pickup
        - corporate
      properties:
        b2c:
          type: boolean
        b2b:
          type: boolean
        marketplace:
          type: boolean
        pos:
          type: boolean
        subscription:
          type: boolean
        delivery:
          type: boolean
        pickup:
          type: boolean
        corporate:
          type: boolean
      additionalProperties: false
    MarketplaceDeliveryZone:
      type: object
      description: |
        One kitchen's published terms for reaching a set of places. The fee and
        the minimum are what the kitchen *charges*, never what the food costs
        it — which is why they are publishable at all.
      required:
        - id
        - name
        - area
        - country_code
        - delivery_fee
        - minimum_order
        - estimated_minutes
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          description: Localised. One server-chosen language, never both columns.
        area:
          type: string
          description: |
            The zone's claimed areas, localised and joined in the locale's own
            punctuation. The consumer contract carries a single free-form label
            while a zone claims a set, and publishing only the first would tell a
            customer in one place that a zone covers another.
        country_code:
          type: string
          maxLength: 2
        delivery_fee:
          oneOf:
            - $ref: '#/components/schemas/MarketplaceMoney'
            - type: 'null'
        minimum_order:
          oneOf:
            - $ref: '#/components/schemas/MarketplaceMoney'
            - type: 'null'
        estimated_minutes:
          type:
            - integer
            - 'null'
          minimum: 0
      additionalProperties: false
    MarketplaceOpeningHours:
      type: object
      description: |
        One configured day. A closed day is a **row with no times**, not a
        missing one: "we are shut on Friday" and "nobody has filled in Friday"
        are different facts, and only the days a kitchen has configured appear.
      required:
        - weekday
        - opens_at
        - closes_at
        - order_cut_off_at
      properties:
        weekday:
          type: integer
          minimum: 1
          maximum: 7
          description: '`1` is Monday, per ISO 8601.'
        opens_at:
          type:
            - string
            - 'null'
          description: '`HH:MM`, in the branch''s own timezone.'
        closes_at:
          type:
            - string
            - 'null'
        order_cut_off_at:
          type:
            - string
            - 'null'
          description: Last time an order for that same day is accepted.
      additionalProperties: false
    MarketplaceBranch:
      type: object
      required:
        - id
        - kitchen_id
        - name
        - area
        - country_code
        - time_zone
        - delivery_zones
        - opening_hours
        - supports_pickup
        - is_active
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kitchen_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        area:
          type: string
          description: |
            The branch's city. The street address is deliberately not published:
            it is where a kitchen takes deliveries, and printing it beside an
            opening time turns a discovery page into a directory of unstaffed
            back doors.
        country_code:
          type: string
          maxLength: 2
        time_zone:
          type: string
        delivery_zones:
          type: array
          items:
            $ref: '#/components/schemas/MarketplaceDeliveryZone'
        opening_hours:
          type: array
          items:
            $ref: '#/components/schemas/MarketplaceOpeningHours'
        supports_pickup:
          type: boolean
          description: |
            Always `false`. No column records a pickup arrangement anywhere in
            the platform, and an honest constant beats a guess.
        is_active:
          type: boolean
      additionalProperties: false
    MarketplaceKitchen:
      type: object
      required:
        - id
        - name
        - slug
        - tagline
        - description
        - country_code
        - cuisines
        - diet_classifications
        - channels
        - branches
        - delivery_windows
        - rating
        - rating_count
        - image_placeholder_id
        - is_verified
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          description: The registered organisation name. Not localised — a business name is not translated.
        slug:
          type: string
        tagline:
          type: string
          description: Always empty; the platform stores no marketing copy.
        description:
          type: string
          description: Always empty; the platform stores no marketing copy.
        country_code:
          type: string
          maxLength: 2
        cuisines:
          type: array
          description: Always empty; nothing records how a kitchen cooks.
          items:
            type: string
        diet_classifications:
          type: array
          description: Derived from the kitchen's own published meals.
          items:
            type: string
        channels:
          $ref: '#/components/schemas/MarketplaceSalesChannels'
        branches:
          type: array
          items:
            $ref: '#/components/schemas/MarketplaceBranch'
        delivery_windows:
          type: array
          description: |
            Active delivery slots the kitchen publishes for checkout. Empty when
            the kitchen has not configured windows yet — the client must not
            invent codes the kitchen does not recognise.
          items:
            $ref: '#/components/schemas/MarketplaceDeliveryWindow'
        rating:
          type:
            - number
            - 'null'
          description: Always null; no reviews exist.
        rating_count:
          type: integer
          minimum: 0
        image_placeholder_id:
          type: string
          description: |
            A generated placeholder identifier (`kitchen-<slug>`), never a remote
            image URL. The clients load no third-party assets.
        is_verified:
          type: boolean
          description: |
            Always `false`. There is no verification module yet, so the platform
            has verified nobody — which is the fact rather than a placeholder.
      additionalProperties: false
    MarketplaceDeliveryWindow:
      type: object
      description: One named delivery slot a customer may pick at checkout.
      required:
        - code
        - label
        - starts_at
        - ends_at
        - weekdays
      properties:
        code:
          type: string
          description: Stable slot code sent on cart/checkout (`morning`, `evening`, …).
        label:
          type: string
          description: Localised display label. One server-chosen language from Accept-Language.
        starts_at:
          type: string
          description: Kitchen-local `HH:mm`.
          example: '09:00'
        ends_at:
          type: string
          description: Kitchen-local `HH:mm`.
          example: '12:00'
        weekdays:
          type: array
          description: |
            ISO weekdays the window runs on (1 = Monday … 7 = Sunday). An empty
            array means every day.
          items:
            type: integer
            minimum: 1
            maximum: 7
      additionalProperties: false
    MarketplaceAvailability:
      type: object
      description: |
        One day of the fourteen-day ordering calendar, derived from the
        branches' operating weeks and cut-offs. A day is orderable when at least
        one active branch is open on it and that branch's cut-off has not
        passed; a day nobody has configured is not orderable.
      required:
        - date
        - available
        - remaining
        - order_cut_off_at
      properties:
        date:
          type: string
          format: date
        available:
          type: boolean
        remaining:
          type:
            - integer
            - 'null'
          description: Always null — the platform stores no stock counts, and a zero would say sold out.
        order_cut_off_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    MarketplaceServing:
      type: object
      description: The named portion to which the meal facts apply.
      required:
        - label
        - quantity
        - unit
        - grams
        - millilitres
        - household_measure
      properties:
        label:
          type: string
          examples:
            - 1 bowl
        quantity:
          type: number
        unit:
          type: string
          enum:
            - g
            - kg
            - ml
            - l
            - piece
            - slice
            - portion
            - cup
            - tbsp
            - tsp
        grams:
          type:
            - number
            - 'null'
        millilitres:
          type:
            - number
            - 'null'
        household_measure:
          type:
            - string
            - 'null'
      additionalProperties: false
    MarketplaceNutrientAmount:
      type: object
      required:
        - nutrient_id
        - unit
        - value
        - kind
        - tolerance
      properties:
        nutrient_id:
          type: string
          examples:
            - energy
            - protein
            - carbohydrate
            - fat
            - fibre
        unit:
          type: string
          enum:
            - kcal
            - kJ
            - g
            - mg
            - ug
            - ml
            - IU
        value:
          type: number
        kind:
          type: string
          enum:
            - planned
            - actual
            - target
        tolerance:
          type: 'null'
      additionalProperties: false
    MarketplaceNutritionSource:
      type: object
      required:
        - kind
        - label
        - version
        - calculated_at
      properties:
        kind:
          type: string
          enum:
            - synthetic_prototype
            - ingredient_derived
            - laboratory
            - manufacturer
            - professional_entry
        label:
          type: string
        version:
          type: string
        calculated_at:
          type: string
          format: date-time
      additionalProperties: false
    MarketplaceNutritionCalculation:
      type: object
      required:
        - method
        - basis
        - calculated_at
        - prototype
        - rounding
        - notes
      properties:
        method:
          type: string
        basis:
          type: string
          enum:
            - per_serving
            - per_100g
            - per_recipe
            - per_meal
            - per_day
            - per_week
        calculated_at:
          type: string
          format: date-time
        prototype:
          type: boolean
        rounding:
          type: string
        notes:
          type: array
          items:
            type: string
      additionalProperties: false
    MarketplaceNutritionFacts:
      type: object
      description: Per-serving nutrition facts with their source and calculation method.
      required:
        - basis
        - kind
        - serving
        - total_grams
        - amounts
        - source
        - calculation
      properties:
        basis:
          type: string
          enum:
            - per_serving
            - per_100g
            - per_recipe
            - per_meal
            - per_day
            - per_week
        kind:
          type: string
          enum:
            - planned
            - actual
            - target
        serving:
          oneOf:
            - $ref: '#/components/schemas/MarketplaceServing'
            - type: 'null'
        total_grams:
          type:
            - number
            - 'null'
        amounts:
          type: array
          items:
            $ref: '#/components/schemas/MarketplaceNutrientAmount'
        source:
          $ref: '#/components/schemas/MarketplaceNutritionSource'
        calculation:
          $ref: '#/components/schemas/MarketplaceNutritionCalculation'
      additionalProperties: false
    MarketplaceMeal:
      type: object
      required:
        - id
        - kitchen_id
        - kitchen_name
        - item_type
        - published_category
        - name
        - slug
        - description
        - meal_types
        - diet_classifications
        - cuisines
        - allergens
        - serving
        - nutrition
        - price
        - preparation_minutes
        - image_placeholder_id
        - availability
        - channels
        - rating
        - rating_count
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kitchen_id:
          $ref: '#/components/schemas/Uuid'
        kitchen_name:
          type: string
        item_type:
          type: string
          enum:
            - meal
            - product
            - sauce
            - dressing
          description: |
            Whether this listing is a prepared meal, a resold product
            (frozen pack, drink, bread and so on), a kitchen-made sauce or a
            dressing. All appear on the kitchen menu; clients filter with
            `item_types`.
        published_category:
          oneOf:
            - type: object
              required:
                - code
                - name
              properties:
                code:
                  type: string
                name:
                  type: string
                  description: Localised — one server-chosen language.
              additionalProperties: false
            - type: 'null'
          description: |
            The customer-facing shelf this listing is published under, from
            the platform product taxonomy. Null when the kitchen has not
            filed it anywhere. Filter with `category_slug`.
        name:
          type: string
          description: Localised. One server-chosen language, never both columns.
        slug:
          type: string
        description:
          type: string
        meal_types:
          type: array
          description: |
            Always empty. Nothing on a catalogue item records whether a dish is a
            breakfast or a dinner; a kitchen selling a lunch box and a breakfast
            box sells two items.
          items:
            type: string
        diet_classifications:
          type: array
          items:
            type: string
        cuisines:
          type: array
          items:
            type: string
        allergens:
          type: array
          description: |
            Derived from the published recipe version's frozen label, or from the
            meal's own ingredient list. Carries **both** containment levels:
            dropping "may contain" would let a filter for "no peanuts" return a
            dish made where peanuts cannot be ruled out.
          items:
            $ref: '#/components/schemas/AllergenCode'
        serving:
          description: |
            The serving declared alongside recorded nutrition, or null when the
            kitchen has not recorded any facts for this meal.
          oneOf:
            - $ref: '#/components/schemas/MarketplaceServing'
            - type: 'null'
        nutrition:
          description: |
            Nutrition facts with their source and calculation notes, or null when
            no facts are recorded. A `synthetic_prototype` source is a visibly
            labelled demonstration estimate, not a kitchen declaration or a
            laboratory analysis.
          oneOf:
            - $ref: '#/components/schemas/MarketplaceNutritionFacts'
            - type: 'null'
        price:
          $ref: '#/components/schemas/MarketplaceMoney'
        preparation_minutes:
          type:
            - integer
            - 'null'
          description: Always null; no column records how long a dish takes to make.
        image_placeholder_id:
          type: string
        availability:
          type: array
          items:
            $ref: '#/components/schemas/MarketplaceAvailability'
        channels:
          $ref: '#/components/schemas/MarketplaceSalesChannels'
        rating:
          type:
            - number
            - 'null'
        rating_count:
          type: integer
          minimum: 0
      additionalProperties: false
    MarketplaceEnergyBand:
      type: object
      required:
        - min
        - max
      properties:
        min:
          type: integer
        max:
          type: integer
      additionalProperties: false
    MarketplacePlanVariant:
      type: object
      required:
        - id
        - plan_id
        - name
        - energy_range
        - protein_range
        - carbohydrate_range
        - fat_range
        - meals_per_day
        - snacks_per_day
        - price_per_week
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        plan_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        energy_range:
          description: |
            A calorie **bracket**, never a per-person target: a single number
            would imply an energy calculation this programme does not do. Null
            when the configuration names no band.
          oneOf:
            - $ref: '#/components/schemas/MarketplaceEnergyBand'
            - type: 'null'
        protein_range:
          type: 'null'
        carbohydrate_range:
          type: 'null'
        fat_range:
          type: 'null'
        meals_per_day:
          type: integer
          minimum: 1
        snacks_per_day:
          type: integer
          minimum: 0
        price_per_week:
          description: |
            Derived from the confirmed price on the basis the plan declares:
            `per_week` unchanged, `per_day` multiplied by seven. A plan priced as
            a `total` has no weekly figure and its configurations are omitted
            rather than divided into one nobody quoted.
          oneOf:
            - $ref: '#/components/schemas/MarketplaceMoney'
            - type: 'null'
      additionalProperties: false
    MarketplacePlanDuration:
      type: object
      description: |
        **A documented widening of the proposed draft** (master plan v2 §4.3,
        amending OD-3). The draft typed a duration as one of `1w`, `2w`, `4w`,
        `12w`. The platform stores an explicit `kind` (`one_off` or
        `fixed_days`) and a nullable number of days, precisely so that
        "not a subscription, just one order" stops being a magic zero a per-day
        calculation divides by — and real kitchen runs of 5, 20, 40 and 60 days
        are not expressible in the draft's enumeration at all.
      required:
        - code
        - name
        - kind
        - days
        - discount_percent
        - total_price
      properties:
        code:
          type: string
          description: The kitchen's own duration code.
        name:
          type: string
          description: Localised.
        kind:
          type: string
          enum:
            - one_off
            - fixed_days
        days:
          type:
            - integer
            - 'null'
          minimum: 1
          description: Null exactly when `kind` is `one_off`.
        discount_percent:
          type:
            - string
            - 'null'
          description: |
            A decimal string, or null. Null means **nobody has stated a
            discount**, which is a different fact from stating there is none —
            and null again when two configurations state different ones, because
            a plan page showing "10% off" when only one configuration gets it
            would be a price claim the kitchen did not make.
        total_price:
          description: |
            The whole-run price, and null unless every number in it was agreed:
            a confirmed per-day price and a fixed number of days. Rounded
            half-up to the minor unit once, at the end.
          oneOf:
            - $ref: '#/components/schemas/MarketplaceMoney'
            - type: 'null'
      additionalProperties: false
    MarketplacePlan:
      type: object
      required:
        - id
        - kitchen_id
        - name
        - slug
        - summary
        - description
        - category_slugs
        - diet_classifications
        - variants
        - durations
        - sample_meal_ids
        - image_placeholder_id
        - rating
        - rating_count
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kitchen_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        slug:
          type: string
        summary:
          type: string
        description:
          type: string
        category_slugs:
          type: array
          description: Always empty; nothing groups plans into marketing categories.
          items:
            type: string
        diet_classifications:
          type: array
          items:
            type: string
        variants:
          type: array
          items:
            $ref: '#/components/schemas/MarketplacePlanVariant'
        durations:
          type: array
          items:
            $ref: '#/components/schemas/MarketplacePlanDuration'
        sample_meal_ids:
          type: array
          maxItems: 12
          description: |
            The first dishes of the plan's own fixed menu, distinct and in the
            order the kitchen serves them — cycle day, then sitting in the order
            of the day, then sequence. Published dishes only: a withdrawn one is
            omitted rather than advertised.

            **Empty when the plan publishes no menu**, which is the ordinary
            case for a free-selection plan and for a plan whose kitchen has not
            written its menu down. The identifiers are always read from the
            kitchen's stated menu and never assembled from its catalogue — a
            sample the platform composed itself would be the platform writing a
            menu.

            Capped at twelve: it is a sample of the menu, not the menu.
          items:
            $ref: '#/components/schemas/Uuid'
        image_placeholder_id:
          type: string
        rating:
          type:
            - number
            - 'null'
        rating_count:
          type: integer
          minimum: 0
      additionalProperties: false
    MarketplaceListMeta:
      allOf:
        - $ref: '#/components/schemas/PaginationMeta'
        - type: object
          required:
            - locale
            - unsupported_filters
          properties:
            locale:
              type: string
              enum:
                - en
                - ar
              description: The locale the localised fields were rendered in.
            unsupported_filters:
              type: array
              description: |
                Parameters the caller sent that this deployment stores no data
                for. They had no effect and are named rather than ignored: a
                person who filters to "under 500 kcal" and is shown everything
                has been told a falsehood by omission.
              items:
                type: string
    MarketplaceKitchensEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/MarketplaceKitchen'
        meta:
          $ref: '#/components/schemas/MarketplaceListMeta'
    MarketplaceMealsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/MarketplaceMeal'
        meta:
          $ref: '#/components/schemas/MarketplaceListMeta'
    MarketplacePlansEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/MarketplacePlan'
        meta:
          $ref: '#/components/schemas/MarketplaceListMeta'
    PublicDeliveryArea:
      type: object
      required:
        - id
        - country_code
        - code
        - name
        - region
        - display_order
      description: |
        The public projection: **one** server-localised `name`, never both
        language columns.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        country_code:
          type: string
          minLength: 2
          maxLength: 2
        code:
          type: string
          maxLength: 60
        name:
          type: string
          description: Rendered in the locale `meta.locale` names.
        region:
          type:
            - string
            - 'null'
          maxLength: 60
        display_order:
          type: integer
      additionalProperties: false
    PublicDeliveryAreasEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PublicDeliveryArea'
        meta:
          allOf:
            - $ref: '#/components/schemas/PaginationMeta'
            - type: object
              properties:
                locale:
                  type: string
                  enum:
                    - en
                    - ar
                  description: The locale the names were rendered in.
                country_code:
                  type: string
                  minLength: 2
                  maxLength: 2
    DeliveryZoneAreasEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AdminDeliveryArea'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                count:
                  type: integer
                  minimum: 0
                inactive_area_count:
                  type: integer
                  minimum: 0
                  description: |
                    How many of these places the platform has since withdrawn.
                    The claims are kept rather than silently dropped; this is
                    what a screen puts a warning badge on.
                scope:
                  $ref: '#/components/schemas/DeliveryZoneScope'
    DeliveryWindow:
      type: object
      required:
        - id
        - organisation_id
        - code
        - name_en
        - name_ar
        - starts_at
        - ends_at
        - weekdays
        - display_order
        - is_active
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 30
          description: Fixed at creation. A request carrying it on update is refused.
        name_en:
          type: string
        name_ar:
          type: string
        starts_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
          description: |
            A clock face in the branch's own timezone, `HH:MM` — not an
            instant. Null exactly when `ends_at` is: a window somebody named
            before deciding its hours is a legitimate half-finished state.
        ends_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
          description: |
            Strictly after `starts_at`. **Overnight windows are not supported
            yet** — a rollover rule order capture has not agreed to would be a
            rule nobody has tested — so 22:00–02:00 is modelled as two windows.
        weekdays:
          type: array
          maxItems: 7
          items:
            type: integer
            minimum: 1
            maximum: 7
          description: |
            ISO-8601 weekdays, 1 = Monday … 7 = Sunday. **An empty array means
            every day**, and it is the only encoding of that fact: all seven
            normalises to `[]` on the way in, so two windows that run daily
            never compare as different.
        display_order:
          type: integer
        is_active:
          type: boolean
          description: The only withdrawal there is. No DELETE exists.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    DeliveryWindowEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - delivery_window
          properties:
            delivery_window:
              $ref: '#/components/schemas/DeliveryWindow'
        meta:
          $ref: '#/components/schemas/Meta'
    DeliveryWindowsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DeliveryWindow'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                count:
                  type: integer
                  minimum: 0
                active_count:
                  type: integer
                  minimum: 0
    BranchOperatingDay:
      type: object
      required:
        - id
        - branch_id
        - weekday
        - is_open
        - opens_at
        - closes_at
        - order_cut_off_at
      description: |
        One weekday of one branch's week. A **closed day is a row** with both
        times null — "we are shut on Sunday" and "nobody has filled in Sunday"
        are different facts, and only the first is stored as a row.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        weekday:
          type: integer
          minimum: 1
          maximum: 7
          description: 'ISO-8601: 1 = Monday … 7 = Sunday.'
        is_open:
          type: boolean
          description: Derived from the times, so it cannot disagree with them.
        opens_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
          description: A clock face in the branch's own timezone. Null exactly when `closes_at` is.
        closes_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
          description: Strictly after `opens_at`. No overnight service in this phase.
        order_cut_off_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
          description: |
            The last moment an order for this day is accepted. **Null means
            until the van leaves** — a kitchen with no cut-off has none, and
            that is an answer rather than an omission. Only an open day may
            carry one; it may fall outside the opening hours.
      additionalProperties: false
    BranchOperatingEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          maxItems: 7
          items:
            $ref: '#/components/schemas/BranchOperatingDay'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                configured_weekdays:
                  type: array
                  maxItems: 7
                  items:
                    type: integer
                    minimum: 1
                    maximum: 7
                open_day_count:
                  type: integer
                  minimum: 0
                  maximum: 7
                is_complete:
                  type: boolean
                  description: Whether all seven days have been decided. Days nobody has decided about are absent, never invented.
    CreateDeliveryZoneRequest:
      type: object
      required:
        - code
        - name_en
      description: |
        No `status`: a zone is created active, archiving is a POST action and
        suspension is `is_active` on the update (master plan v2 §4.15).
      properties:
        code:
          type: string
          maxLength: 40
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
          description: Defaults to the organisation's own currency.
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Omitted or null draws the organisation-wide map.
        delivery_fee_minor:
          type:
            - integer
            - 'null'
          minimum: 0
          description: Leave it out to say nobody has decided. Send `0` to say delivery is free.
        minimum_order_minor:
          type:
            - integer
            - 'null'
          minimum: 0
        estimated_minutes:
          type:
            - integer
            - 'null'
          minimum: 1
      additionalProperties: false
    UpdateDeliveryZoneRequest:
      type: object
      description: |
        Every field optional. `code` and `status` are **rejected**, not
        ignored — a client that sent one believed it was writing something.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Re-scopes the zone **and its area claims together**. Refused with
            `409 area_already_served` when the destination scope is taken.
        delivery_fee_minor:
          type:
            - integer
            - 'null'
          minimum: 0
        minimum_order_minor:
          type:
            - integer
            - 'null'
          minimum: 0
        estimated_minutes:
          type:
            - integer
            - 'null'
          minimum: 1
        is_active:
          type: boolean
          description: |
            The reversible half of the lifecycle: `false` suspends the zone and
            **keeps** its area claims. Archiving is the POST action, and it
            releases them.
      additionalProperties: false
    ReplaceDeliveryZoneAreasRequest:
      type: object
      required:
        - service_area_ids
      description: |
        The desired **whole** map. Areas absent from it are released, which is
        the only way to hand a place to a different zone. An empty array is a
        zone that covers nowhere — a legitimate intermediate state while a
        kitchen redraws.
      properties:
        service_area_ids:
          type: array
          maxItems: 500
          items:
            $ref: '#/components/schemas/Uuid'
      additionalProperties: false
    CreateDeliveryWindowRequest:
      type: object
      required:
        - code
        - name_en
      properties:
        code:
          type: string
          maxLength: 30
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        starts_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9](:[0-5][0-9])?$
          description: Both times or neither. Refused with the missing half named.
        ends_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9](:[0-5][0-9])?$
        weekdays:
          type:
            - array
            - 'null'
          maxItems: 7
          items:
            type: integer
            minimum: 1
            maximum: 7
          description: Omitted, null, empty or all seven all mean **every day**, and all store as `[]`.
        display_order:
          type:
            - integer
            - 'null'
          minimum: 0
        is_active:
          type:
            - boolean
            - 'null'
      additionalProperties: false
    UpdateDeliveryWindowRequest:
      type: object
      description: |
        Every field optional; `code` is **rejected**, not ignored. The pairing
        and ordering rules run against the **merged** row, so sending only one
        end compares it with the stored other.
      properties:
        name_en:
          type: string
          maxLength: 255
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        starts_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9](:[0-5][0-9])?$
        ends_at:
          type:
            - string
            - 'null'
          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9](:[0-5][0-9])?$
        weekdays:
          type:
            - array
            - 'null'
          maxItems: 7
          items:
            type: integer
            minimum: 1
            maximum: 7
        display_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean
      additionalProperties: false
    ReplaceBranchOperatingRequest:
      type: object
      required:
        - days
      description: |
        The desired **whole** week. An omitted weekday is a day nobody has
        decided about; a day sent with no times is closed. An empty array
        clears the week back to unconfigured.

        There is no `branch_id`: the branch comes from `X-Branch-Id`, validated
        against the caller's membership scope.
      properties:
        days:
          type: array
          maxItems: 7
          items:
            type: object
            required:
              - weekday
            properties:
              weekday:
                type: integer
                minimum: 1
                maximum: 7
                description: ISO-8601. Each weekday at most once — two rows for one day is a contradiction, not a split shift.
              opens_at:
                type:
                  - string
                  - 'null'
                pattern: ^([01][0-9]|2[0-3]):[0-5][0-9](:[0-5][0-9])?$
              closes_at:
                type:
                  - string
                  - 'null'
                pattern: ^([01][0-9]|2[0-3]):[0-5][0-9](:[0-5][0-9])?$
              order_cut_off_at:
                type:
                  - string
                  - 'null'
                pattern: ^([01][0-9]|2[0-3]):[0-5][0-9](:[0-5][0-9])?$
                description: Only an open day may carry one. It may fall outside the opening hours.
            additionalProperties: false
      additionalProperties: false
    CartStatus:
      type: string
      description: |
        Three states, two of them terminal. `converted` and `expired` both mean
        "no longer shoppable" and are kept apart because only one is a success:
        a converted cart has an order behind it, an expired one is somebody who
        changed their mind. Collapsing them would make basket abandonment
        unanswerable.
      enum:
        - open
        - converted
        - expired
    OrderStatus:
      type: string
      description: |
        `placed → confirmed → fulfilled`, with `cancelled` reachable from the
        first two and from nowhere else. A fulfilled order is not cancellable:
        the food has been delivered, and what happens next is a refund or a
        complaint — different objects with different money attached.

        Deliberately absent: anything about payment (there is one method and it
        happens at the door), anything about production stages (the kitchen
        display's vocabulary), and any `pending` or `draft` — an order that has
        not been placed is a cart.
      enum:
        - placed
        - confirmed
        - fulfilled
        - cancelled
    CancellationReason:
      type: string
      description: |
        Why an order was cancelled. A fixed vocabulary rather than free text
        because the answer is read by machines as often as by people: free text
        turns a count into a search problem, and it is how personal data ends up
        in a column nobody classified.

        `delivery_unavailable` is separate from `kitchen_unable_to_fulfil`
        because the food could be made but not delivered — the kitchen's fault
        in a different way, and the one an operations team can fix.
        `payment_failed` is **not** here: there is nothing to fail.
      enum:
        - customer_requested
        - kitchen_unable_to_fulfil
        - delivery_unavailable
        - address_unreachable
    PaymentMethod:
      type: string
      description: |
        **Three cases, and each one has a table behind it.** That is the whole
        rule this vocabulary is governed by, and it is the same rule that kept it
        at one case for the whole of the first phase: a payment method exists
        here when there is somewhere in the schema that records money arriving
        that way, and not a moment sooner.

        * `cash_on_delivery` — a driver is handed money at a door. The original
          case, and still the way most orders are settled.
        * `cash_at_counter` — money handed over in the room, at the order desk.
        * `wish` — a transfer through the WISH app, **manually confirmed**.
          Nothing verifies it: a desk person looks at the merchant app, sees that
          the money arrived, and says so, and the receipt's `confirmed_by` names
          who.

        What has not changed is the refusal. There is still no `card`, no
        `wallet` and no `online` reserved here, and no case is added in
        anticipation of a table — a reserved case is a value something eventually
        writes, and this value is read to decide what evidence a payment is
        expected to carry.

        The vocabulary is used by two fields that are deliberately **not**
        constrained to agree. An order's `payment_method` is the *intent*
        captured at placement; a payment receipt's `method` is how the money
        actually turned up. An order taken for cash at the counter and settled by
        WISH is a real evening, and the pair of fields is how it stays legible.
      enum:
        - cash_on_delivery
        - cash_at_counter
        - wish
    FulfilmentType:
      type: string
      description: |
        How an order leaves the kitchen. **Three cases, and each one is a
        different set of facts the order must carry** — which is what makes this
        a vocabulary rather than a flag. `orders_fulfilment_shape_check` states
        the column half of it in the database, so the two cannot drift.

        * `delivery` — a courier takes it to a door. A customer and an address,
          both required. The only way this platform sold anything before the
          order desk, and still how most orders leave.
        * `pickup` — the customer collects it. A customer, a **promised slot**,
          and deliberately **no address**: "be here at six" is a promise whether
          or not the food travels, but a destination on an order nobody is
          delivering is a courier instruction that will never be followed.
        * `counter` — somebody bought lunch at the desk. The customer is
          **optional** — a regular is worth naming, a stranger is not — and the
          address is forbidden. The desk may know *who* and must never claim
          *where*, which is the asymmetry that makes this its own case rather
          than "pickup without an account".

        `delivery` is the column default, so every order placed before this
        vocabulary existed carries it.

        What this is **not** is a delivery *status*. Where an order has got to is
        `status`, which moves; this never moves after placement. An order taken
        for delivery and collected by an impatient customer is a delivery that
        was handed over early, not a pickup — rewriting the type would make the
        fee already snapshotted on the row unexplainable.
      enum:
        - delivery
        - pickup
        - counter
    OpenCartRequest:
      type: object
      required:
        - channel_code
      properties:
        channel_code:
          type: string
          maxLength: 40
          description: |
            The sales channel's own code, resolved across organisations. Which
            kitchen owns it is the server's answer, not the client's assertion —
            which is why there is no `organisation_id` here.
          examples:
            - b2c_web
      additionalProperties: false
    AddCartItemRequest:
      type: object
      required:
        - catalogue_item_id
      properties:
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The pack or plan configuration, when the article has variants.
        quantity:
          type:
            - number
            - string
          description: |
            How many, defaulting to 1. Numeric rather than integer because a
            line can be 0.35 kg, and accepted as a string as well as a number so
            a client holding a decimal can send it without a float round trip.
            Zero is refused: removing a line is its own action, not a quantity
            of nothing.
          examples:
            - '0.3500'
            - 2
        delivery_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            The day the line is wanted, `YYYY-MM-DD`. Part of the line's
            identity rather than a decoration: two quantities of the same meal
            for two different days are two lines.
      additionalProperties: false
    SetCartItemQuantityRequest:
      type: object
      required:
        - quantity
      properties:
        quantity:
          type:
            - number
            - string
          description: |
            The quantity the customer wants, not a delta. Reprobed by the
            server, and refusable — raising can cross a tier priced in another
            currency, lowering can fall below the only tier that priced the
            article at all.
          examples:
            - '3.0000'
            - 3
      additionalProperties: false
    CartLine:
      type: object
      required:
        - id
        - catalogue_item_id
        - catalogue_item_variant_id
        - quantity
        - delivery_date
        - created_at
        - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          type:
            - string
            - 'null'
          format: uuid
        quantity:
          type: string
          description: |
            A decimal **string**, never a number. It is matched against pricing
            tiers of the same scale, and a 0.3 kg line that arrived back as
            0.29999999 would sit on the wrong side of a tier boundary.
          examples:
            - '0.3500'
        delivery_date:
          type:
            - string
            - 'null'
          format: date
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    Cart:
      type: object
      required:
        - id
        - organisation_id
        - sales_channel_id
        - branch_id
        - status
        - currency_code
        - expires_at
        - lock_version
        - line_count
        - lines
        - created_at
        - updated_at
      description: |
        **No amount of any kind appears here, and none ever will.** The cart
        tables hold no price column: the server's probe establishes that a line
        is orderable and discards its price, because a cart price is advisory and
        every line is repriced at placement. A subtotal here would be a number no
        table holds and no checkout will honour.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        sales_channel_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Where the food would be produced. Null until the customer has
            chosen: naming one for them would silently pick whose cut-off and
            whose delivery terms apply.
        status:
          $ref: '#/components/schemas/CartStatus'
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          examples:
            - AED
        expires_at:
          type: string
          format: date-time
          description: |
            When an untouched basket is closed by the sweep. Carried because a
            basket that quietly vanishes is the complaint the TTL would
            otherwise generate; every line write pushes it out.
        lock_version:
          type: integer
          minimum: 0
          description: |
            Served as an `ETag` so a client can tell a stale render from a
            current one. **No cart write demands `If-Match`**: the header
            prevents a lost update, and a basket has exactly one author.
        line_count:
          type: integer
          minimum: 0
        lines:
          type: array
          items:
            $ref: '#/components/schemas/CartLine'
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    CartEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - cart
          properties:
            cart:
              $ref: '#/components/schemas/Cart'
        meta:
          $ref: '#/components/schemas/Meta'
    CartLineEnvelope:
      type: object
      required:
        - data
        - meta
      description: |
        The line the request was about, **and** the basket around it. The write
        moved the cart's validator and pushed its expiry out, so a response
        carrying only the line would hand a client two stale facts.
      properties:
        data:
          type: object
          required:
            - line
            - cart
          properties:
            line:
              $ref: '#/components/schemas/CartLine'
            cart:
              $ref: '#/components/schemas/Cart'
        meta:
          $ref: '#/components/schemas/Meta'
    PlaceOrderRequest:
      type: object
      required:
        - cart_id
        - customer_address_id
      properties:
        cart_id:
          $ref: '#/components/schemas/Uuid'
        customer_address_id:
          $ref: '#/components/schemas/Uuid'
        delivery_window_code:
          type:
            - string
            - 'null'
          maxLength: 40
          description: |
            The slot the customer chose, as the window's own code. Whether that
            slot is offered on that day by that branch is a scheduling question
            answered during placement, with the cut-off rules in hand.
          examples:
            - evening
        requested_delivery_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            The day asked for. Wins over a day the basket's lines name, and a
            disagreement is `mixed_delivery_dates` rather than a silent
            preference: this phase's order carries exactly one delivery date.
      additionalProperties: false
      description: |
        Four fields, and none of them is a price. Every amount is decided during
        placement, against the tariff standing at that moment; a body that could
        state a total would be a client quoting the server its own prices.
    PreviewCheckoutRequest:
      type: object
      required:
        - cart_id
      properties:
        cart_id:
          $ref: '#/components/schemas/Uuid'
        customer_address_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Optional, unlike at `POST /orders`. A shopper previews a total
            before choosing where it goes — the cart screen has no address at
            all — and an absent address is reported as the `address_missing`
            warning rather than refused.
        delivery_window_code:
          type:
            - string
            - 'null'
          maxLength: 40
          examples:
            - evening
        requested_delivery_date:
          type:
            - string
            - 'null'
          format: date
      additionalProperties: false
      description: |
        The same four fields `PlaceOrderRequest` takes, and none of them is a
        price here either — the server prices, the same way, for the same
        reason.
    CheckoutPreview:
      type: object
      required:
        - cart_id
        - currency_code
        - subtotal_minor
        - delivery_fee_minor
        - total_minor
        - line_count
        - warnings
      description: |
        What a basket would cost, priced by the same `LineProbe` and
        `ZoneResolver` `POST /orders` runs at placement — a preview and the
        placement it precedes never quote two different numbers for a basket
        nothing has changed about.

        Nothing here is a refusal: every fact placement would refuse the whole
        order over comes back in `warnings` instead, in the same vocabulary,
        beside whatever total could still be priced.
      properties:
        cart_id:
          $ref: '#/components/schemas/Uuid'
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
        subtotal_minor:
          type: integer
          description: |
            The sum of every priceable line. A line the probe refuses
            contributes nothing here and its reason to `warnings`, instead of
            aborting the whole preview.
        delivery_fee_minor:
          type:
            - integer
            - 'null'
          description: |
            Null whenever the fee is unresolved — no address named, an
            unserved area, a suspended zone, a currency mismatch, or a zone
            with no fee configured. Never treated as zero: an unresolved fee
            is a fact worth a warning, not a discount.
        total_minor:
          type: integer
          description: The subtotal plus the delivery fee — or the subtotal alone, when the fee is null.
        line_count:
          type: integer
          minimum: 0
        warnings:
          type: array
          items:
            type: string
          description: |
            Every unresolved fact about the cart or the address, in the same
            vocabulary `409 order.placement_refused` uses at placement:
            `cart_empty`, `address_missing`, `address_not_deliverable`,
            `area_not_served`, `zone_suspended`, `currency_mismatch`, plus the
            line-probe vocabulary. Deduplicated — three lines refused for the
            same reason read as one fact, not three repeats of it.
      additionalProperties: false
    CheckoutPreviewEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - preview
          properties:
            preview:
              $ref: '#/components/schemas/CheckoutPreview'
        meta:
          $ref: '#/components/schemas/Meta'
    CancelOrderRequest:
      type: object
      required:
        - reason
      properties:
        reason:
          $ref: '#/components/schemas/CancellationReason'
      additionalProperties: false
    OrderLineAllergen:
      type: object
      required:
        - allergen_code
        - containment
      description: |
        The customer-facing allergen label, and nothing else. The derivation and
        the source ingredient are dropped at snapshot time: a source ingredient
        names part of a formulation, and an order line is the most likely row on
        the platform to be handed to a courier or a marketplace partner.
      properties:
        allergen_code:
          $ref: '#/components/schemas/AllergenCode'
        containment:
          type: string
      additionalProperties: false
    CustomerOrderLine:
      type: object
      required:
        - id
        - catalogue_item_id
        - catalogue_item_variant_id
        - name_en
        - name_ar
        - variant_label
        - quantity
        - unit_price_minor
        - line_total_minor
        - currency_code
        - allergens
        - pack_summary
      description: |
        The article as it stood the moment the order was placed — a snapshot,
        not a join. Renaming the catalogue item tomorrow does not rewrite what a
        customer bought.

        `price_list_id` and `price_list_item_id` exist on the row and are
        **never** on this shape: they answer "why was this the price?" a year
        later, and on a receipt they would name a kitchen's tariff structure to
        the person being charged by it.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          type:
            - string
            - 'null'
          format: uuid
        name_en:
          type: string
        name_ar:
          type: string
        variant_label:
          type:
            - string
            - 'null'
        quantity:
          type: string
          description: The decimal string the line total was computed from, never a number.
          examples:
            - '2.0000'
        unit_price_minor:
          type: integer
          description: Minor units. Never formatted server-side — `4500` with `AED`, never `"45.00 AED"`.
        line_total_minor:
          type: integer
          description: |
            Rounded exactly once, here. The unit price times the quantity is an
            integer times a decimal, and rounding the unit price instead would
            compound the error across the order.
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
        allergens:
          type: array
          items:
            $ref: '#/components/schemas/OrderLineAllergen'
        pack_summary:
          type:
            - object
            - 'null'
          additionalProperties: true
          description: |
            The pack as the customer bought it — size, unit, pieces, net weight.
            Never a cost and never a supplier.
      additionalProperties: false
    KitchenOrderLine:
      allOf:
        - $ref: '#/components/schemas/CustomerOrderLine'
        - type: object
          required:
            - price_list_id
            - price_list_item_id
          properties:
            price_list_id:
              type:
                - string
                - 'null'
              format: uuid
            price_list_item_id:
              type:
                - string
                - 'null'
              format: uuid
          description: |
            The snapshot's provenance, served to the seller and to nobody else.
            An `allOf` here is a documentation convenience only — the server
            builds the two shapes independently rather than by subtraction,
            because a narrow shape produced by unsetting keys from a wide one is
            one careless refactor away from leaking.
    CustomerOrderDelivery:
      type: object
      required:
        - label
        - line_one
        - line_two
        - city
        - area_name_en
        - area_name_ar
        - area_id
        - building
        - floor
        - apartment
        - directions
        - contact_point_id
        - window_code
        - requested_date
      description: |
        The address as it stood when the order was placed, copied. Editing the
        address later must not change where this order was sent.

        **The whole block is null on an order that is not being delivered.**
        A `pickup` is collected and a `counter` sale is handed over in the room;
        neither has a destination, and `orders_fulfilment_shape_check` refuses
        an address on either. Read `fulfilment_type` on the order to tell "there
        is nowhere to take this" from "the address is missing" — they are
        different facts and this block looks the same for both.

        `building`, `floor`, `apartment` and `directions` are the half a courier
        actually navigates by: the street gets somebody to the building, and
        these get them to the door. They were mirrored onto the order by the
        fulfilment migration; before it, a snapshot was a strictly poorer copy
        of the address it came from.
      properties:
        label:
          type:
            - string
            - 'null'
        line_one:
          type:
            - string
            - 'null'
          description: |
            Null on a pickup or a counter sale, which carry no address at all.
            On a delivery whose customer has since closed their account it is
            the redaction marker rather than the street: a reader has to be able
            to tell "this was erased" from "this was never filled in".
        line_two:
          type:
            - string
            - 'null'
        city:
          type:
            - string
            - 'null'
          description: |
            The gazetteer's region — the governorate or district. Usually null:
            the platform data leaves it unpopulated, and the field exists because
            a courier manifest has a city line and a snapshot that could not fill
            it would send somebody back to a table that has since changed.
        area_name_en:
          type:
            - string
            - 'null'
        area_name_ar:
          type:
            - string
            - 'null'
        area_id:
          type:
            - string
            - 'null'
          format: uuid
        building:
          type:
            - string
            - 'null'
          maxLength: 120
        floor:
          type:
            - string
            - 'null'
          maxLength: 40
        apartment:
          type:
            - string
            - 'null'
          maxLength: 40
        directions:
          type:
            - string
            - 'null'
          description: |
            Free text the customer wrote for whoever has to find them. Never
            parsed and never matched against — it is prose, and treating it as
            structure is how "ring twice" becomes a routing decision.
        contact_point_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The number the courier was actually given, as a reference rather
            than a copied string. A snapshot of *which* contact was named at
            placement, not of what it said: a person who changes their telephone
            changes it once, and the verification state travels with the row.
        window_code:
          type:
            - string
            - 'null'
        requested_date:
          type:
            - string
            - 'null'
          format: date
      additionalProperties: false
    KitchenOrderDelivery:
      allOf:
        - $ref: '#/components/schemas/CustomerOrderDelivery'
        - type: object
          required:
            - zone_id
          properties:
            zone_id:
              type:
                - string
                - 'null'
              format: uuid
              description: |
                Which slice of the kitchen's delivery map the address fell into,
                and what set the fee. The seller's operating arrangement, so it
                is not on the customer's shape.
    StockItem:
      type: object
      required:
        - id
        - code
        - name_en
        - unit_code
        - ingredient_id
        - catalogue_item_id
        - backing
        - is_stocked
        - has_history
      description: |
        A shelf a kitchen holds. Derived (INV2.0) from one of two things: an
        ingredient it cooks with, or a product it buys in to resell.

        `ingredient_id` is always set on a derived shelf, resold products
        included — moving-average cost, COGS and the monthly report are all
        keyed by ingredient, so a shelf without one would be a shelf with no
        valuation. For a resold product that ingredient is an implementation
        detail the kitchen never manages; `catalogue_item_id` is the product
        it actually is, and `backing` is the field to branch on.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 64
          description: Minted once from the source slug and stable thereafter.
        name_en:
          type: string
          maxLength: 160
        unit_code:
          type: string
          maxLength: 16
          description: |
            The ingredient's own unit, or for a resold product its purchasing
            unit — a bottle of oil is bought by the bottle, not by the litre a
            recipe would divide.
        ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Null only on a row that pre-dates derivation and has not been
            adopted yet; every derived shelf has one.
        catalogue_item_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The resold product this shelf is, or null when it is an ingredient.
        backing:
          type: string
          enum:
            - ingredient
            - product
          description: Which book this shelf belongs in.
        is_stocked:
          type: boolean
          description: Some branch holds a non-zero quantity of it.
        has_history:
          type: boolean
          description: It has moved at least once — received, adjusted, wasted or consumed.
      additionalProperties: false
    StockItemCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - stock_items
          properties:
            stock_items:
              type: array
              items:
                $ref: '#/components/schemas/StockItem'
        meta:
          $ref: '#/components/schemas/Meta'
    StockLevel:
      type: object
      required:
        - id
        - branch_id
        - stock_item_id
        - quantity
        - reorder_threshold
        - par_level
        - is_low
        - item_code
        - item_name_en
        - ingredient_id
      description: |
        One stock item's on-hand quantity at one branch. `item_code`,
        `item_name_en` and `ingredient_id` are denormalised from the stock
        item, so a levels screen never has to round-trip to render a row.
        `is_low` is computed on read (INV1.3): true when `reorder_threshold`
        is set and `quantity` is at or below it, false when no threshold is
        set — never a stored flag.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: string
          description: A decimal string, never a float — precision the wire must not round away.
        reorder_threshold:
          oneOf:
            - type: string
            - type: 'null'
          description: The reorder point as a decimal string, or null when no threshold is set (never low).
        par_level:
          oneOf:
            - type: string
            - type: 'null'
          description: The level to restock back up to, as a decimal string, or null when not set.
        is_low:
          type: boolean
          description: Computed — the threshold is set and quantity is at or below it.
        item_code:
          type: string
        item_name_en:
          type: string
        ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
      additionalProperties: false
    StockLevelEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - level
          properties:
            level:
              $ref: '#/components/schemas/StockLevel'
        meta:
          $ref: '#/components/schemas/Meta'
    LowStockCountEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - count
          properties:
            count:
              type: integer
              minimum: 0
              description: How many levels in scope are low right now.
        meta:
          $ref: '#/components/schemas/Meta'
    SetStockThresholdRequest:
      type: object
      required:
        - branch_id
        - stock_item_id
        - reorder_threshold
      description: |
        Sets or clears a level's reorder threshold. `reorder_threshold` is
        required as a field but nullable as a value — passing null clears the
        threshold. `par_level` is optional and independently nullable.
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        reorder_threshold:
          oneOf:
            - type: number
              minimum: 0
            - type: 'null'
          description: The reorder point; null clears the threshold (the item is then never low).
        par_level:
          oneOf:
            - type: number
              minimum: 0
            - type: 'null'
          description: Optional target level to restock back up to; null clears it.
      additionalProperties: false
    StockLevelCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - levels
          properties:
            levels:
              type: array
              items:
                $ref: '#/components/schemas/StockLevel'
        meta:
          $ref: '#/components/schemas/Meta'
    StockMovement:
      type: object
      required:
        - id
        - quantity_delta
        - reason
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        quantity_delta:
          type: string
          description: A signed decimal string — negative for waste and consumption, positive for receipts, yields and upward adjustments.
        reason:
          type: string
          enum:
            - adjust
            - waste
            - receipt
            - consume
            - yield
      additionalProperties: false
    StockMovementEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - movement
          properties:
            movement:
              $ref: '#/components/schemas/StockMovement'
        meta:
          $ref: '#/components/schemas/Meta'
    StockAdjustmentRequest:
      type: object
      required:
        - branch_id
        - stock_item_id
        - quantity_delta
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        quantity_delta:
          type: number
          description: Signed — a correction that raises the level is positive, one that lowers it is negative.
        notes:
          type:
            - string
            - 'null'
          maxLength: 255
      additionalProperties: false
    StockWasteRequest:
      type: object
      required:
        - branch_id
        - stock_item_id
        - quantity
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: number
          exclusiveMinimum: 0
          description: Always positive. The endpoint states the loss; the server signs it negative on the ledger.
        notes:
          type:
            - string
            - 'null'
          maxLength: 255
      additionalProperties: false
    Supplier:
      type: object
      required:
        - id
        - code
        - name_en
        - name_ar
        - currency_code
        - contact_email
        - contact_phone
        - address
        - payment_terms
        - lead_time_days
        - notes
        - archived_at
        - contact_count
        - supplied_item_count
        - primary_contact
      description: |
        One supplier in the organisation's book. The same shape everywhere a
        supplier is served — list row and freshly created record alike — so a
        client writes one mapper rather than three.

        `contact_email`/`contact_phone` are the **general office** details, not
        a duplicate of the primary named person: when a sales rep leaves, the
        office line is still correct and their mobile is not.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        name_en:
          type: string
        name_ar:
          type:
            - string
            - 'null'
        currency_code:
          type:
            - string
            - 'null'
          description: The currency this supplier usually invoices in (ISO 4217), or null (INV1.1).
        contact_email:
          type:
            - string
            - 'null'
          description: The general office address, not the primary contact's.
        contact_phone:
          type:
            - string
            - 'null'
          description: The general office line, not the primary contact's.
        address:
          type:
            - string
            - 'null'
          description: |
            Free text, transcribed as the supplier gives it and printed on order
            sheets — a building name, a market stall number, "gate 4, behind the
            cold store". Never geocoded, so never structured.
        payment_terms:
          type:
            - string
            - 'null'
          description: Free text — "net 30", "cash on delivery", "50% up front". Never computed against.
        lead_time_days:
          type:
            - integer
            - 'null'
          minimum: 0
          maximum: 365
          description: Days between issuing an order and expecting it. 0 is a same-morning market run.
        notes:
          type:
            - string
            - 'null'
        archived_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            Set when the supplier was archived. An archived supplier keeps every
            receipt posted against it and leaves every picker; clients render
            the record read-only rather than hiding it.
        contact_count:
          type: integer
          minimum: 0
          description: How many named contacts this supplier has on file.
        supplied_item_count:
          type: integer
          minimum: 0
          description: |
            How many stock items this supplier is linked to (SUP2). Counted on
            the list query rather than derived from `supplied_items`, which a
            list row does not carry.
        primary_contact:
          oneOf:
            - $ref: '#/components/schemas/SupplierPrimaryContact'
            - type: 'null'
          description: |
            Who to call, summarised for a list row — the contact flagged
            primary, falling back to the first in display order. Null only when
            the supplier has no named contacts at all.
        contacts:
          type: array
          description: |
            Every named contact, in the kitchen's own display order. Served on
            the supplier **detail** response only, where `SupplierDetail`
            requires it — a list row carries `contact_count` and
            `primary_contact` instead, because fetching every contact of every
            supplier to render a book is the N+1 that summary exists to avoid.
          items:
            $ref: '#/components/schemas/SupplierContact'
        supplied_items:
          type: array
          description: |
            What this supplier sells the kitchen, preferred link first then by
            item name (SUP2). Detail response only, on the same terms as
            `contacts` — a list row carries `supplied_item_count` instead.
          items:
            $ref: '#/components/schemas/SuppliedItem'
        costs_redacted:
          type: boolean
          description: |
            `true` when the reader lacks `inventory.view_costs_organisation`
            and every `supplied_items[].last_purchase` money field was served
            as `null`. Detail response only.
      additionalProperties: false
    SupplierPrimaryContact:
      type: object
      required:
        - name
        - phone
      description: The "who do I call" summary a supplier list row shows.
      properties:
        name:
          type: string
        phone:
          type:
            - string
            - 'null'
          description: The telephone number, falling back to the WhatsApp number — a contact reachable only on WhatsApp is still reachable.
      additionalProperties: false
    SupplierContact:
      type: object
      required:
        - id
        - name
        - role_title
        - email
        - phone
        - whatsapp_phone
        - is_primary
        - display_order
      description: |
        One named person at a supplier — distinct from the supplier's own office
        line. At least one of `email`, `phone` and `whatsapp_phone` is always
        present: a contact nobody can reach is not a contact.

        `whatsapp_phone` is its own field rather than a flag on `phone` because
        in this trade they genuinely differ — the landline takes the call, the
        mobile takes the order photo.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        role_title:
          type:
            - string
            - 'null'
        email:
          type:
            - string
            - 'null'
        phone:
          type:
            - string
            - 'null'
        whatsapp_phone:
          type:
            - string
            - 'null'
        is_primary:
          type: boolean
          description: At most one per supplier, enforced by a partial unique index.
        display_order:
          type: integer
          minimum: 0
      additionalProperties: false
    SupplierDetail:
      description: |
        A supplier with its full contact set and everything it supplies — the
        supplier's own page.

        The canonical `Supplier` with `contacts`, `supplied_items` and
        `costs_redacted` promoted from optional to required. Composed rather
        than restated so that a field added to the book cannot arrive on the
        list and go missing from the detail.

        All three are restated in the second subschema rather than only listed
        as required: `Supplier` closes itself with `additionalProperties: false`
        (which is why the fields are declared there too, optionally), and a
        `required`-only branch generates as an index signature rather than as
        the array a client needs.
      allOf:
        - $ref: '#/components/schemas/Supplier'
        - type: object
          required:
            - contacts
            - supplied_items
            - costs_redacted
          properties:
            contacts:
              type: array
              items:
                $ref: '#/components/schemas/SupplierContact'
            supplied_items:
              type: array
              items:
                $ref: '#/components/schemas/SuppliedItem'
            costs_redacted:
              type: boolean
    SuppliedItem:
      type: object
      required:
        - stock_item
        - is_preferred
        - supplier_item_ref
        - last_purchase
      description: |
        One "we buy this from them" row on the supplier's page (SUP2).

        The stock item is embedded rather than referenced by id alone because
        the table beside it shows a name, a code and a unit; a client resolving
        three fields per row against a separate list would be doing a join the
        server already has open.

        `last_purchase` is `null` when this supplier has never been recorded
        selling this item at a price — a distinct state from a *present*
        `last_purchase` whose money is redacted. **Never bought here** and
        **Hidden** are two different facts and clients must render them
        differently (§3.4).
      properties:
        stock_item:
          oneOf:
            - $ref: '#/components/schemas/SuppliedStockItem'
            - type: 'null'
          description: The shelf this link points at. Null only if it vanished between the read and the render.
        is_preferred:
          type: boolean
          description: At most one supplier per stock item may hold this, enforced by a partial unique index.
        supplier_item_ref:
          type:
            - string
            - 'null'
          maxLength: 64
          description: |
            The supplier's own catalogue reference, transcribed from their
            price list so an order sheet can quote it back. Named `_ref` rather
            than `_code`: it is their identifier, not this kitchen's.
        last_purchase:
          oneOf:
            - $ref: '#/components/schemas/LastPurchase'
            - type: 'null'
          description: The newest priced receipt line from this supplier for this item, or null if there is none.
      additionalProperties: false
    SuppliedStockItem:
      type: object
      required:
        - id
        - code
        - name_en
        - unit_code
        - backing
      description: The shelf a supplier link points at, summarised for the row that renders it.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        name_en:
          type: string
        unit_code:
          type: string
        backing:
          type: string
          enum:
            - ingredient
            - product
          description: Which of the two books this shelf belongs to — the same field the inventory list publishes.
      additionalProperties: false
    LastPurchase:
      type: object
      required:
        - goods_receipt_id
        - document_ref
        - received_at
        - quantity
        - unit_id
        - unit_code
        - unit_price_amount
        - cost_currency_code
      description: |
        What this was last bought for (§3.4) — derived live from the newest
        priced `goods_receipt_lines` row, never stored.

        Unpriced deliveries are skipped entirely, so this is always a real
        price: an item received only on unpriced receipts has no
        `last_purchase` at all rather than one with a null amount.

        **The money is the only redacted part.** Without
        `inventory.view_costs_organisation`, `unit_price_amount` and
        `cost_currency_code` are `null` and the enclosing response flags
        `costs_redacted`. The date, quantity and unit survive: those are
        warehouse facts a receiving clerk entered, not the valuation the cost
        permission gates.

        The amount is always quoted **per `unit_code`** and always beside its
        own currency. A bare `6.90` without "USD per kg" is not a price, and
        currencies are never converted — there is no exchange rate in this
        system.
      properties:
        goods_receipt_id:
          $ref: '#/components/schemas/Uuid'
        document_ref:
          type:
            - string
            - 'null'
          description: The supplier delivery note or invoice number the price came from, as written.
        received_at:
          type:
            - string
            - 'null'
          format: date-time
        quantity:
          type: string
          description: The quantity that line received, as a decimal string. Never redacted.
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The unit the price is quoted per; null means the stock item's own unit.
        unit_code:
          type:
            - string
            - 'null'
          description: That unit's code, resolved for display — `kg`, `l`, `piece`.
        unit_price_amount:
          type:
            - string
            - 'null'
          description: Major-unit decimal string per `unit_code`, or null when costs are redacted.
        cost_currency_code:
          type:
            - string
            - 'null'
          description: ISO 4217, or null when costs are redacted.
      additionalProperties: false
    ItemLatestPurchase:
      description: |
        One stock item's newest purchase **across every supplier** (SUP2), with
        the supplier that sold it.

        `LastPurchase` plus the two fields that make it answerable outside a
        supplier's own page: the item it belongs to, so a client can join it to
        the stock list, and who sold it, because a price without the vendor
        beside it is not actionable.

        The supplier is nullable: a direct market-run receipt records no
        supplier, and the price it captured is still the last price of that
        item.
      allOf:
        - $ref: '#/components/schemas/LastPurchase'
        - type: object
          required:
            - stock_item_id
            - supplier
          properties:
            stock_item_id:
              $ref: '#/components/schemas/Uuid'
            supplier:
              oneOf:
                - $ref: '#/components/schemas/SupplierRef'
                - type: 'null'
    ItemLatestPurchaseCollection:
      type: object
      required:
        - data
        - meta
      description: |
        Requested items with no priced receipt are **absent** rather than
        present with nulls, so `meta.count` may be smaller than
        `meta.requested_count`. `meta.costs_redacted` says whether the money
        was served at all.
      properties:
        data:
          type: object
          required:
            - purchases
          properties:
            purchases:
              type: array
              items:
                $ref: '#/components/schemas/ItemLatestPurchase'
        meta:
          $ref: '#/components/schemas/Meta'
    SupplyNeedsCountEnvelope:
      type: object
      required:
        - data
        - meta
      description: |
        The three tallies partition each other: `out_of_stock_count` plus
        `low_stock_count` equals `count`, because a level that is both empty and
        below its threshold is counted once, as out of stock.
      properties:
        data:
          type: object
          required:
            - count
            - out_of_stock_count
            - low_stock_count
          properties:
            count:
              type: integer
              minimum: 0
              description: Shelves at this branch that need ordering.
            out_of_stock_count:
              type: integer
              minimum: 0
              description: Of those, the ones with nothing left (`quantity <= 0`).
            low_stock_count:
              type: integer
              minimum: 0
              description: Of those, the ones at or below a set reorder threshold but not yet empty.
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/Meta'
    SupplierOption:
      type: object
      required:
        - id
        - code
        - name_en
        - name_ar
        - is_preferred
        - lead_time_days
      description: |
        One active supplier a proposal row could be bought from. Archived
        suppliers never appear here — a picker offering a shuttered warehouse is
        how an order gets sent to one.

        No price, and none is coming: this object exists so a person can choose
        who to buy from, not to compare what they charge.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        name_en:
          type: string
        name_ar:
          type:
            - string
            - 'null'
        is_preferred:
          type: boolean
          description: This supplier holds the preferred flag for this stock item.
        lead_time_days:
          type:
            - integer
            - 'null'
          description: Typical days from order to delivery, when the kitchen recorded one.
      additionalProperties: false
    OrderProposalItem:
      type: object
      required:
        - stock_item_id
        - item_code
        - item_name_en
        - unit_id
        - unit_code
        - branch_id
        - quantity_on_hand
        - reorder_threshold
        - par_level
        - is_out_of_stock
        - is_low
        - origin
        - suggested_quantity
        - suggested_quantity_basis
        - supplier_options
        - suggested_supplier_id
        - unassigned_reason
      description: |
        One row of the supply-order builder (SUP3).

        `is_out_of_stock` and `is_low` are independent readings of the numbers
        beside them — a shelf at zero with a threshold set is genuinely both.
        `origin` is the single label the union produces, and it is where the
        "counted once, as out of stock" rule lives.

        A `requested` row's `is_out_of_stock` still follows from its
        `quantity_on_hand`: a shelf that has never moved at this branch reports
        `"0.0000"`, and reporting `false` beside it would be a flag a client
        could not reconcile with the number it describes.

        No field here carries a price, a cost or a currency, at any depth.
      properties:
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        item_code:
          type: string
        item_name_en:
          type: string
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        unit_code:
          type: string
          description: The unit the quantity is counted in — `kg`, `l`, `piece`.
        branch_id:
          $ref: '#/components/schemas/Uuid'
        quantity_on_hand:
          type: string
          description: A decimal string at scale 4, never a float. `"0.0000"` when the shelf has no level row at this branch.
        reorder_threshold:
          type:
            - string
            - 'null'
          description: The reorder point as a decimal string, or null when none is set.
        par_level:
          type:
            - string
            - 'null'
          description: The level to restock back up to, as a decimal string, or null when none is set.
        is_out_of_stock:
          type: boolean
          description: Computed — `quantity_on_hand` is at or below zero.
        is_low:
          type: boolean
          description: Computed — a threshold is set and `quantity_on_hand` is at or below it.
        origin:
          type: string
          enum:
            - out_of_stock
            - low_stock
            - requested
          description: Why this row is in the proposal. A row satisfying both shortage rules is labelled `out_of_stock`.
        suggested_quantity:
          type:
            - string
            - 'null'
          description: |
            `par_level - quantity_on_hand` as a decimal string when a par is set
            and the difference is positive; null otherwise, meaning the person
            must type a quantity.
        suggested_quantity_basis:
          type: string
          enum:
            - par
            - none
          description: |
            How the suggestion was reached. `none` accompanies a null
            suggestion. There is deliberately no `threshold` basis — see the
            endpoint description.
        supplier_options:
          type: array
          description: Every active linked supplier, preferred first then by name.
          items:
            $ref: '#/components/schemas/SupplierOption'
        suggested_supplier_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            The active preferred supplier, else the sole active option, else
            null. Null with two or more options means the person must choose,
            which is not the same as `unassigned_reason` being set.
        unassigned_reason:
          type:
            - string
            - 'null'
          enum:
            - no_supplier
            - suppliers_archived
            - null
          description: |
            Why this row has no supplier to offer at all. `no_supplier`: nothing
            was ever linked. `suppliers_archived`: links exist and every one of
            them is archived, which is a different problem with a different fix.
            Null whenever at least one active option exists.
      additionalProperties: false
    OrderProposalCollection:
      type: object
      required:
        - data
        - meta
      description: |
        Rows in the order they must be rendered: out of stock, then low, then
        requested. `meta` counts the queue only — `out_of_stock_count` and
        `low_stock_count` exclude requested rows, so they agree exactly with
        `/catalogue/procurement/supply-needs/count` for the same branch.
      properties:
        data:
          type: object
          required:
            - items
          properties:
            items:
              type: array
              items:
                $ref: '#/components/schemas/OrderProposalItem'
        meta:
          $ref: '#/components/schemas/Meta'
    PurchaseOrderStatus:
      type: string
      enum:
        - draft
        - issued
        - partially_received
        - received
        - cancelled
      description: |
        Where an order has got to (§3.5). `draft → issued →
        partially_received → received`, with `cancelled` reachable from the two
        states in which nothing has arrived yet.

        **`issued`, never `sent`** (§2). Phase 1 dispatches nothing through any
        channel — a person presses Issue, the lines freeze, and they print the
        sheet and hand it over. Calling that `sent` would claim an event the
        system did not perform.

        `partially_received` and `received` are reached by **posting a goods
        receipt**, never by an action of their own: the quantities received are
        summed per order line and the status is whatever that sum says it is.
        There is no "mark as received" action and there must not be one — it
        would let an order claim a delivery that never turned up. The one
        apparent exception proves the rule: closing a short delivery still goes
        through a receipt, and what the flag adds is a reason for writing off the
        remainder rather than a status change of its own.

        `received` and `cancelled` are terminal: a delivery that turned out wrong
        is a receipt correction or a return, which are different objects with
        different quantities attached.
    PurchaseOrderLine:
      type: object
      required:
        - id
        - stock_item_id
        - item_code
        - item_name_en
        - item_name_ar
        - quantity
        - received_quantity
        - outstanding_quantity
        - unit_code
        - supplier_item_ref
        - notes
        - display_order
      description: |
        One requested shelf, as the document says it (§3.5).

        Every display field is a **snapshot** taken when the line was written,
        not a live read of the stock item — that is what makes an issued order
        immutable in practice rather than only in status. A shelf renamed in
        March does not silently rename a line on an order issued in February,
        because the supplier is holding a printed copy of the February wording.

        `stock_item_id` rides alongside as a **pointer**: a client deep-links to
        the shelf with it and the receiving slice matches a delivery line to
        this one with it. It is never the source of the label beside it.

        **No price, no amount, no currency**, here or anywhere on a purchase
        order. Actual prices belong to goods receipts.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        item_code:
          type: string
          maxLength: 64
          description: Snapshotted — the kitchen's own identifier for the shelf.
        item_name_en:
          type: string
          maxLength: 160
        item_name_ar:
          type:
            - string
            - 'null'
          maxLength: 160
          description: |
            Snapshotted from the backing ingredient or catalogue item, because
            `stock_items` carry `name_en` only. Null when neither has one — an
            honest blank, rather than English text printed under an Arabic
            heading.
        quantity:
          type: string
          description: A decimal string at scale 4, never a float. Always above zero. What was asked for.
        received_quantity:
          type: string
          description: |
            How much of this line has actually turned up, summed across every
            delivery against it (SUP5). Expressed in this line's own `unit_code`:
            a delivery quoted per kilogram against a shelf counted in grams is
            converted before it is summed, so the three quantities on one row
            always add up. A quantity, not money.
        outstanding_quantity:
          type: string
          description: |
            What is still to come, and what the receive screen prefills (§4).
            Floored at zero — an over-receipt is a real event its variance note
            records, and a negative amount still to come would be an arithmetic
            curiosity rather than an instruction.
        unit_code:
          type: string
          maxLength: 16
          description: Snapshotted — the unit that prints on the order sheet.
        supplier_item_ref:
          type:
            - string
            - 'null'
          maxLength: 64
          description: |
            The supplier's own catalogue reference, read from the saved
            supplier↔item link at write time so the sheet can quote it back at
            them. Null when this supplier has no link for this shelf, which is
            ordinary for a one-off purchase.
        notes:
          type:
            - string
            - 'null'
          maxLength: 255
          description: Reserved for a per-line remark. Nothing writes it in this version.
        display_order:
          type: integer
          minimum: 0
          description: The sequence the person building the order chose. Clients render it and never re-sort.
      additionalProperties: false
    RecipientSnapshot:
      type: object
      required:
        - supplier_id
        - code
        - name_en
        - name_ar
        - address
        - payment_terms
        - lead_time_days
        - contact_email
        - contact_phone
        - contacts
      description: |
        Who the order was addressed to, at the instant it was issued (§3.5).

        Captured on issue and never touched again. A supplier that moves
        premises in March must not silently rewrite the February order it is
        holding a copy of — so a **draft** preview reads the live supplier and an
        **issued** reprint reads this.

        It is a document, not a reference: the fields are the ones a printed
        order sheet names, and the supplier may since have been archived,
        renamed or emptied of contacts without any of it changing here.
      properties:
        supplier_id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        name_en:
          type: string
        name_ar:
          type:
            - string
            - 'null'
        address:
          type:
            - string
            - 'null'
          description: Free text as the supplier gives it. Never geocoded.
        payment_terms:
          type:
            - string
            - 'null'
        lead_time_days:
          type:
            - integer
            - 'null'
        contact_email:
          type:
            - string
            - 'null'
          description: The general office address, not the primary contact's.
        contact_phone:
          type:
            - string
            - 'null'
          description: The general office line, not the primary contact's.
        contacts:
          type: array
          description: Every named contact the supplier had at issue time, primary first.
          items:
            $ref: '#/components/schemas/RecipientSnapshotContact'
      additionalProperties: false
    RecipientSnapshotContact:
      type: object
      required:
        - name
        - role_title
        - email
        - phone
        - whatsapp_phone
        - is_primary
      description: |
        One named person as they stood at issue time. No identifier: this is a
        transcription onto a document, not a pointer at a row that may since
        have been deleted.
      properties:
        name:
          type: string
        role_title:
          type:
            - string
            - 'null'
        email:
          type:
            - string
            - 'null'
        phone:
          type:
            - string
            - 'null'
        whatsapp_phone:
          type:
            - string
            - 'null'
        is_primary:
          type: boolean
      additionalProperties: false
    PurchaseOrder:
      type: object
      required:
        - id
        - number
        - status
        - branch
        - supplier
        - recipient_snapshot
        - notes
        - line_count
        - issued_at
        - received_at
        - closed_at
        - close_short_reason
        - cancelled_at
        - created_at
        - lines
        - receipts
      description: |
        One request the kitchen makes of one supplier, for one branch (§3.5).

        **One shape for the list, the detail and the create response.** A second
        summary variant is the one that drifts, and the batch read a print
        preview makes wants the whole thing anyway.

        **Two suppliers on an issued order, and both are deliberate.**
        `supplier` is the live record with its archive flag, because a screen
        offering to issue needs to know whether the supplier is still in the
        book. `recipient_snapshot` is who the document was addressed to at the
        instant it was issued, and it is `null` on a draft because a draft has
        been addressed to nobody yet. A client renders the live supplier on a
        draft and the snapshot from `issued` onward.

        `number` is the human handle both sides quote — `PO-` plus eight
        Crockford base-32 characters, minted **random rather than sequential**
        because a sequential number printed on a supplier's copy would tell them
        this kitchen's purchasing volume.

        **No money, at any depth.** Not a price, an amount, a currency or a
        total, on the order, on a line or inside the snapshot.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
          maxLength: 32
          description: Unique within the organisation. `PO-` plus eight Crockford base-32 characters.
        status:
          $ref: '#/components/schemas/PurchaseOrderStatus'
        branch:
          oneOf:
            - $ref: '#/components/schemas/PurchaseOrderBranch'
            - type: 'null'
          description: The site that is short of the goods.
        supplier:
          oneOf:
            - $ref: '#/components/schemas/PurchaseOrderSupplier'
            - type: 'null'
          description: The **live** supplier record, archive flag included.
        recipient_snapshot:
          oneOf:
            - $ref: '#/components/schemas/RecipientSnapshot'
            - type: 'null'
          description: Null on a draft; present from `issued` onward.
        notes:
          type:
            - string
            - 'null'
          description: A remark for the whole order, printed on the sheet.
        line_count:
          type: integer
          minimum: 0
        issued_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            When the document was frozen. Survives cancellation — cancelling an
            issued order does not un-issue it.
        received_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            When the last outstanding line actually arrived (SUP5). An order
            closed short reaches `received` with this still null and `closed_at`
            set instead: nothing was last fulfilled, and a date here would be a
            small lie in the one place a person checks.
        closed_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            When the order stopped expecting anything more — set on both routes
            to `received`, whether every line arrived or the remainder was
            written off.
        close_short_reason:
          type:
            - string
            - 'null'
          maxLength: 255
          description: |
            Why the rest of this order was written off (§3.5). Present only on an
            order somebody deliberately closed short; a delivery that completed
            the order has nothing to explain and carries none.
        cancelled_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        lines:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderLine'
        receipts:
          type: array
          description: |
            Every delivery made against this order, oldest first (SUP5) — a
            reference rather than the receipt itself. The date, the delivery note
            and the line count are what an order detail lists; the money on those
            lines is behind a permission this response does not check, so a
            client that wants the figures opens the receipt.
          items:
            $ref: '#/components/schemas/PurchaseOrderReceiptRef'
      additionalProperties: false
    PurchaseOrderReceiptRef:
      type: object
      required:
        - id
        - received_on
        - document_ref
        - line_count
      description: One delivery made against an order, as the order lists it (SUP5).
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        received_on:
          type:
            - string
            - 'null'
          format: date
          description: The branch-local business day the delivery was filed under.
        document_ref:
          type:
            - string
            - 'null'
          description: The supplier delivery note, as written.
        line_count:
          type: integer
          minimum: 0
      additionalProperties: false
    PurchaseOrderBranch:
      type: object
      required:
        - id
        - name
      description: |
        The branch an order is for. `name` rather than `name_en`:
        `organisation_branches` carries one name column, and a bilingual pair on
        the wire would promise an Arabic branch name the table has no room for.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
      additionalProperties: false
    PurchaseOrderSupplier:
      type: object
      required:
        - id
        - code
        - name_en
        - name_ar
        - archived_at
      description: |
        The **live** supplier an order names — not the snapshot.
        `archived_at` is on it deliberately: an issued order for a
        since-archived supplier is an ordinary situation a detail screen has to
        explain, and a reference without the flag would leave it guessing why
        issuing is refused.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        name_en:
          type: string
        name_ar:
          type:
            - string
            - 'null'
        archived_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    PurchaseOrderEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - purchase_order
          properties:
            purchase_order:
              $ref: '#/components/schemas/PurchaseOrder'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/Meta'
    PurchaseOrderCollection:
      type: object
      required:
        - data
        - meta
      description: |
        A page of the order book, newest first — and the same envelope the batch
        create answers with, where the orders are in **request order** rather
        than newest first, because the person confirmed a list and the answer
        has to line up with it.
      properties:
        data:
          type: object
          required:
            - purchase_orders
          properties:
            purchase_orders:
              type: array
              items:
                $ref: '#/components/schemas/PurchaseOrder'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/PaginationMeta'
    CreatePurchaseOrderLine:
      type: object
      required:
        - stock_item_id
        - quantity
      description: |
        Everything a client may say about a line, and it is deliberately two
        fields. The item code, both names, the unit and the supplier's own
        catalogue reference are resolved server-side (§6) — a client that could
        name a line could put anything at all on a document a supplier reads.
        There is no price field and none is coming.
      properties:
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: string
          pattern: ^\d+(\.\d{1,4})?$
          description: |
            A decimal string above zero with at most four places — the stock
            column's own precision. A string rather than a number because
            `0.125` kg of saffron must arrive as `0.125`, and a float round trip
            is how that becomes `0.12499999999999999`.
      additionalProperties: false
    CreatePurchaseOrdersRequest:
      type: object
      required:
        - orders
      description: |
        One draft per supplier, created together or not at all.

        `branch_id` sits on each order because every proposal row carries the
        branch it was read for, which is what makes a payload whose branch
        disagrees with its lines unrepresentable. The server then insists they
        all agree: §4 says a proposal is always *for one branch*, so two
        branches in one body is a `422` rather than a guess.
      properties:
        orders:
          type: array
          minItems: 1
          maxItems: 20
          items:
            $ref: '#/components/schemas/CreatePurchaseOrderRequest'
      additionalProperties: false
    CreatePurchaseOrderRequest:
      type: object
      required:
        - supplier_id
        - branch_id
        - lines
      properties:
        supplier_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        lines:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: '#/components/schemas/CreatePurchaseOrderLine'
      additionalProperties: false
    UpdatePurchaseOrderRequest:
      type: object
      description: |
        Presence-keyed: an omitted field is left alone, `notes: null` clears the
        note. `lines` is a **full replace** — the body states the whole desired
        set — because a diff would need an identity for a line the client does
        not name.

        Draft only. An issued order answers `409` with
        `details.reason = purchase_order_not_draft`.
      properties:
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
        lines:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: '#/components/schemas/CreatePurchaseOrderLine'
      additionalProperties: false
    UpsertSupplierLinkRequest:
      type: object
      required:
        - supplier_id
        - stock_item_id
      description: |
        The pair that identifies the link, plus the two things about it a
        kitchen edits. Idempotent: sending the same body twice leaves the same
        row.

        `is_preferred` and `supplier_item_ref` are keyed on **presence**. An
        omitted field is left alone; `supplier_item_ref: null` clears a
        reference typed by mistake. `is_preferred: true` takes the flag off
        whichever supplier held it for this item; `false` clears this link's own
        flag and promotes nobody.
      properties:
        supplier_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        is_preferred:
          type:
            - boolean
            - 'null'
        supplier_item_ref:
          type:
            - string
            - 'null'
          maxLength: 64
      additionalProperties: false
    SupplierLink:
      type: object
      required:
        - supplier_id
        - stock_item_id
        - is_preferred
        - supplier_item_ref
      description: |
        The link after a write, and nothing more.

        Every ops write in this workspace answers the minimum and lets the
        screen re-read what it changed. A supplied item's last purchase price is
        derived from the receipt ledger and cannot change because somebody saved
        a link, so returning it here would invent a read the caller did not ask
        for.
      properties:
        supplier_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        is_preferred:
          type: boolean
        supplier_item_ref:
          type:
            - string
            - 'null'
      additionalProperties: false
    SupplierLinkEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - supplier_link
          properties:
            supplier_link:
              $ref: '#/components/schemas/SupplierLink'
        meta:
          $ref: '#/components/schemas/Meta'
    SupplierCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - suppliers
          properties:
            suppliers:
              type: array
              items:
                $ref: '#/components/schemas/Supplier'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateSupplierRequest:
      type: object
      required:
        - name_en
      description: |
        `code` is optional — omit it and the server mints a unique
        per-organisation code from the name. `currency_code` is a hint the
        receipt form pre-selects and is never required.

        Contacts are **not** accepted here: they are their own section-level
        replace, and a create that took them would give a kitchen two ways to
        write the same set. The lightweight create inside the goods-receipt
        dialog sends a name and nothing else.
      properties:
        name_en:
          type: string
          minLength: 1
          maxLength: 160
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 160
        code:
          type:
            - string
            - 'null'
          maxLength: 64
        currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
        contact_email:
          type:
            - string
            - 'null'
          maxLength: 160
        contact_phone:
          type:
            - string
            - 'null'
          maxLength: 40
        address:
          type:
            - string
            - 'null'
          maxLength: 2000
        payment_terms:
          type:
            - string
            - 'null'
          maxLength: 120
        lead_time_days:
          type:
            - integer
            - 'null'
          minimum: 0
          maximum: 365
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
      additionalProperties: false
    UpdateSupplierRequest:
      type: object
      description: |
        Every field of the record, all optional — a partial write. A field the
        body omits is left alone; a field sent as `null` is cleared, so a
        kitchen can genuinely remove an address it typed by mistake.

        `code` **is** editable, unlike a delivery zone's or a price list's. The
        difference is who wrote it: those are minted by an operator who meant
        them, whereas a supplier code is frequently minted by the server from a
        name typed into a goods-receipt dialog at the loading bay, and refusing
        to correct `GULF-FRESH-TRADING-CO-2` would leave the kitchen stuck with
        an accident. Uniqueness ignores this supplier's own row.

        `archived_at` is not reachable from here — archiving is its own action
        with its own audit event, so a form save cannot retire a supplier by
        writing a field. Contacts are their own set-replace.
      properties:
        name_en:
          type: string
          minLength: 1
          maxLength: 160
        name_ar:
          type:
            - string
            - 'null'
          maxLength: 160
        code:
          type: string
          minLength: 1
          maxLength: 64
        currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
        contact_email:
          type:
            - string
            - 'null'
          maxLength: 160
        contact_phone:
          type:
            - string
            - 'null'
          maxLength: 40
        address:
          type:
            - string
            - 'null'
          maxLength: 2000
        payment_terms:
          type:
            - string
            - 'null'
          maxLength: 120
        lead_time_days:
          type:
            - integer
            - 'null'
          minimum: 0
          maximum: 365
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
      additionalProperties: false
    ReplaceSupplierContactsRequest:
      type: object
      required:
        - contacts
      description: |
        The **whole desired contact set**. Contacts carrying an `id` are
        updated, contacts without one are created, and contacts absent from the
        body are deleted — one request, one transaction, one **Save contacts**
        button on the screen. Send `[]` to clear the set.

        Two rules are refused with `422` before the database can raise a
        constraint violation: at most one `is_primary` across the set, and at
        least one of `email`, `phone` and `whatsapp_phone` on every contact.

        Twenty is the cap. A supplier with more than twenty named people is not
        a supplier record, it is a directory.
      properties:
        contacts:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/ReplaceSupplierContact'
      additionalProperties: false
    ReplaceSupplierContact:
      type: object
      required:
        - name
      description: One contact in a replace request. `id` names an existing contact to update; omit it to create.
      properties:
        id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The contact to update. Omit or send null to create a new one.
        name:
          type: string
          minLength: 1
          maxLength: 120
        role_title:
          type:
            - string
            - 'null'
          maxLength: 120
        email:
          type:
            - string
            - 'null'
          maxLength: 160
        phone:
          type:
            - string
            - 'null'
          maxLength: 40
        whatsapp_phone:
          type:
            - string
            - 'null'
          maxLength: 40
        is_primary:
          type:
            - boolean
            - 'null'
          description: At most one contact in the set may set this.
        display_order:
          type:
            - integer
            - 'null'
          minimum: 0
          maximum: 65535
          description: Defaults to the contact's position in the submitted array.
      additionalProperties: false
    SupplierEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - supplier
          properties:
            supplier:
              $ref: '#/components/schemas/Supplier'
        meta:
          $ref: '#/components/schemas/Meta'
    SupplierDetailEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - supplier
          properties:
            supplier:
              $ref: '#/components/schemas/SupplierDetail'
        meta:
          $ref: '#/components/schemas/Meta'
    SupplierContactCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - contacts
          properties:
            contacts:
              type: array
              items:
                $ref: '#/components/schemas/SupplierContact'
        meta:
          $ref: '#/components/schemas/Meta'
    CurrencyOption:
      type: object
      required:
        - code
        - name_en
      description: One active currency a receipt price can be booked in.
      properties:
        code:
          type: string
          minLength: 3
          maxLength: 3
        name_en:
          type: string
      additionalProperties: false
    MeasurementUnitOption:
      type: object
      required:
        - id
        - code
        - dimension
        - name_en
      description: |
        One active measurement unit a purchase line can be quoted in.
        `dimension` (e.g. `mass`, `volume`, `count`) is what makes conversion
        safe — the line editor offers only units in the stock item's own
        dimension, because the conversion service refuses across dimensions.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        dimension:
          type: string
        name_en:
          type: string
      additionalProperties: false
    ProcurementReference:
      type: object
      required:
        - currencies
        - default_currency_code
        - measurement_units
      description: |
        The reference sets the goods-receipt form needs (INV1.1). `default_currency_code`
        is the organisation's own currency and the honest default for a receipt;
        it is `null` only when the organisation has none on file.
      properties:
        currencies:
          type: array
          items:
            $ref: '#/components/schemas/CurrencyOption'
        default_currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
        measurement_units:
          type: array
          items:
            $ref: '#/components/schemas/MeasurementUnitOption'
      additionalProperties: false
    ProcurementReferenceEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          $ref: '#/components/schemas/ProcurementReference'
        meta:
          $ref: '#/components/schemas/Meta'
    ReceiptCostStatus:
      type: string
      enum:
        - unpriced
        - partial
        - complete
      description: |
        Whether a receipt's costing is finished (§3.6). Derived from the lines
        and never set by hand: a line counts as settled when it carries
        `costed_at`, the receipt is `complete` when every line does, `unpriced`
        when none does and `partial` in between.

        `costed_at` is stamped when there is nothing further to do with a line's
        money — the blend went through, **or** there was no ingredient to blend
        into (packaging, cleaning supplies). Both are finished states, and
        leaving the second one null would park its receipt in the work queue
        forever with no action available.

        A receipt whose only line is `valuation_pending_fx` therefore reads
        `unpriced` even though its price is recorded, because its costing is
        genuinely not finished. `valuation_pending_count` beside it is what tells
        a person which kind of unfinished it is.

        Not redacted without the cost permission: this says whether the paperwork
        needs attention, not what anything cost.
    GoodsReceiptLine:
      type: object
      required:
        - id
        - stock_item_id
        - purchase_order_line_id
        - quantity
        - unit_id
        - unit_price_amount
        - line_total_amount
        - cost_currency_code
        - costed_at
        - valuation_pending_fx
      description: |
        The money fields (INV1.1) are served as `null` when the reader lacks
        `inventory.view_costs_organisation` — see `GoodsReceipt.costs_redacted`.
        `quantity` and `unit_id` are warehouse facts and are never redacted, and
        neither are `costed_at` and `valuation_pending_fx`, which say whether the
        line still needs somebody's attention rather than what it cost.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        purchase_order_line_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            The ordered line this delivery fulfils (SUP5). Null for a direct
            market purchase and for an unplanned extra item on an ordered
            delivery, both of which §4 treats as ordinary.
        quantity:
          type: string
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The unit the price is quoted per (INV1.1).
        unit_price_amount:
          type:
            - string
            - 'null'
          description: Major currency units per unit_id (§4.4), or null when unpriced or redacted.
        line_total_amount:
          type:
            - string
            - 'null'
        cost_currency_code:
          type:
            - string
            - 'null'
        costed_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            When this line's money was settled (§3.6). Null means the costing
            path has not run and price completion may still act on it — the guard
            that stops one quantity being costed twice.
        valuation_pending_fx:
          type: boolean
          description: |
            The price is recorded exactly as the supplier wrote it and could not
            be blended into the ingredient's valuation currency (§3.6). Physical
            receiving is never lost to a currency, and no exchange rate is ever
            invented; a later accounting phase may resolve it.
      additionalProperties: false
    GoodsReceipt:
      type: object
      required:
        - id
        - branch_id
        - supplier
        - document_ref
        - supplier_invoice_ref
        - invoice_date
        - variance_note
        - purchase_order_id
        - received_at
        - received_on
        - cost_status
        - unpriced_line_count
        - valuation_pending_count
        - currency_code
        - receipt_total_amount
        - discount_amount
        - tax_amount
        - delivery_amount
        - other_charges_amount
        - invoice_total_amount
        - costs_redacted
        - lines
      description: |
        What arrived, from whom, and what it cost (§3.6).

        **Two dates, and they are two different facts.** `received_at` is the
        exact instant; `received_on` is the branch-local calendar day the
        delivery belongs to, and it is what §3.7 groups spend by — a van unloaded
        at 21:30 in Dubai is a Tuesday delivery, and reading the UTC instant's
        date would file it on Monday.

        **Two document references, likewise.** `document_ref` is the delivery
        note the driver handed over; `supplier_invoice_ref` and `invoice_date`
        are the invoice, which often arrives days later.

        `receipt_total_amount` is the **item subtotal** in `currency_code`,
        offered only when every priced line shares one currency (there is no
        exchange rate in this system, §4.4). The header charges are reported
        **separately** for the reason §3.6 gives: tax and delivery are not an
        ingredient's purchase price, and a screen that added them into one figure
        would be mislabelling them. They carry no currency of their own — they
        are in the receipt's line currency, and the post refuses any other state.

        When the reader lacks `inventory.view_costs_organisation` every money
        field is null and `costs_redacted` is true. `cost_status` and the two
        counts are **not** redacted.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        supplier:
          oneOf:
            - $ref: '#/components/schemas/SupplierRef'
            - type: 'null'
        document_ref:
          type:
            - string
            - 'null'
          description: The supplier delivery note, as written (INV1.1).
        supplier_invoice_ref:
          type:
            - string
            - 'null'
          description: The supplier's invoice number — a different document from the delivery note (SUP5).
        invoice_date:
          type:
            - string
            - 'null'
          format: date
        variance_note:
          type:
            - string
            - 'null'
          maxLength: 255
          description: |
            Why this delivery differs from what was ordered (§4). Required for an
            over-receipt and for an unplanned extra item — a required explanation
            that was then discarded would be the plainest kind of silent
            behaviour, so it is kept and shown.
        purchase_order_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        received_at:
          type:
            - string
            - 'null'
          format: date-time
        received_on:
          type:
            - string
            - 'null'
          format: date
          description: The branch-local business day this delivery is filed under (§3.6).
        cost_status:
          $ref: '#/components/schemas/ReceiptCostStatus'
        unpriced_line_count:
          type: integer
          minimum: 0
          description: Lines whose money is not settled — the work still outstanding on this receipt.
        valuation_pending_count:
          type: integer
          minimum: 0
          description: Lines whose price is recorded and whose valuation is waiting on an exchange-rate decision.
        currency_code:
          type:
            - string
            - 'null'
        receipt_total_amount:
          type:
            - string
            - 'null'
          description: The item subtotal — Σ line totals. Never includes the header charges.
        discount_amount:
          type:
            - string
            - 'null'
        tax_amount:
          type:
            - string
            - 'null'
        delivery_amount:
          type:
            - string
            - 'null'
        other_charges_amount:
          type:
            - string
            - 'null'
        invoice_total_amount:
          type:
            - string
            - 'null'
          description: What the supplier invoiced in total, when it is known.
        costs_redacted:
          type: boolean
          description: True when the reader lacks inventory.view_costs_organisation and money fields were nulled (INV1.1).
        lines:
          type: array
          items:
            $ref: '#/components/schemas/GoodsReceiptLine'
      additionalProperties: false
    GoodsReceiptDetail:
      allOf:
        - $ref: '#/components/schemas/GoodsReceipt'
        - type: object
          required:
            - purchase_order
          description: |
            The receipt, plus the order it settled (SUP5). The order block is
            §5's manage-scoped subset — number, status and the per-line
            arithmetic somebody standing over a pallet compares against — and is
            null for a direct market purchase.
          properties:
            purchase_order:
              oneOf:
                - $ref: '#/components/schemas/ReceiptPurchaseOrderMatch'
                - type: 'null'
    ReceiptPurchaseOrderMatch:
      type: object
      required:
        - id
        - number
        - status
        - lines
      description: |
        The order a delivery settled, as the receipt shows it. No money at any
        depth, because there is none on a purchase order at all.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
        status:
          $ref: '#/components/schemas/PurchaseOrderStatus'
        lines:
          type: array
          items:
            $ref: '#/components/schemas/ReceiptPurchaseOrderMatchLine'
      additionalProperties: false
    ReceiptPurchaseOrderMatchLine:
      type: object
      required:
        - purchase_order_line_id
        - stock_item_id
        - item_code
        - item_name_en
        - unit_code
        - ordered_quantity
        - received_quantity
        - outstanding_quantity
      description: |
        One ordered line and how much of it has arrived, all three quantities in
        the line's own `unit_code`.
      properties:
        purchase_order_line_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        item_code:
          type: string
        item_name_en:
          type: string
        unit_code:
          type: string
        ordered_quantity:
          type: string
        received_quantity:
          type: string
        outstanding_quantity:
          type: string
      additionalProperties: false
    GoodsReceiptDetailEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - goods_receipt
          properties:
            goods_receipt:
              $ref: '#/components/schemas/GoodsReceiptDetail'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/Meta'
    ReceivableOrder:
      type: object
      required:
        - id
        - number
        - status
        - branch_id
        - supplier
        - issued_at
        - line_count
        - outstanding_line_count
        - lines
      description: |
        One order a delivery could be received against (§4, §6) — a deliberate
        manage-scoped subset of the order book, not the order book. No notes, no
        recipient snapshot, no history, and no money at any depth.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        number:
          type: string
        status:
          $ref: '#/components/schemas/PurchaseOrderStatus'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        supplier:
          oneOf:
            - $ref: '#/components/schemas/SupplierRef'
            - type: 'null'
        issued_at:
          type:
            - string
            - 'null'
          format: date-time
        line_count:
          type: integer
          minimum: 0
        outstanding_line_count:
          type: integer
          minimum: 0
          description: |
            Lines with something still to come. Fully delivered lines stay in
            `lines` — a person checking a delivery against a sheet needs to see
            the row accounted for rather than missing — and this is the count a
            picker sorts and labels by.
        lines:
          type: array
          items:
            $ref: '#/components/schemas/ReceivableOrderLine'
      additionalProperties: false
    ReceivableOrderLine:
      type: object
      required:
        - purchase_order_line_id
        - stock_item_id
        - item_code
        - item_name_en
        - item_name_ar
        - unit_code
        - unit_id
        - ordered_quantity
        - received_quantity
        - outstanding_quantity
      description: |
        One ordered line with its outstanding quantity — what the receive screen
        prefills a row with (§4). The unit is fixed from the order line, so a
        delivery cannot silently be counted in something else.
      properties:
        purchase_order_line_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        item_code:
          type: string
        item_name_en:
          type: string
        item_name_ar:
          type:
            - string
            - 'null'
        unit_code:
          type: string
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        ordered_quantity:
          type: string
        received_quantity:
          type: string
        outstanding_quantity:
          type: string
      additionalProperties: false
    ReceivableOrderCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - receivable_orders
          properties:
            receivable_orders:
              type: array
              items:
                $ref: '#/components/schemas/ReceivableOrder'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/Meta'
    UnpricedReceipt:
      type: object
      required:
        - id
        - received_on
        - supplier
        - document_ref
        - supplier_invoice_ref
        - purchase_order_id
        - cost_status
        - line_count
        - unpriced_line_count
        - valuation_pending_count
      description: |
        One row of the unpriced-receipts work queue (§3.6). Deliberately not the
        whole receipt: this is a list somebody scans to decide what to open next.

        The two counts are two different jobs. `unpriced_line_count` is "type
        these prices in"; `valuation_pending_count` is "the prices are already
        here and an exchange-rate decision is not this screen's to make". A queue
        showing one number for both would send people to rows they cannot action.

        No money at all — the amounts are on the receipt detail, behind the same
        gate this list sits on.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        received_on:
          type:
            - string
            - 'null'
          format: date
        supplier:
          oneOf:
            - $ref: '#/components/schemas/SupplierRef'
            - type: 'null'
        document_ref:
          type:
            - string
            - 'null'
        supplier_invoice_ref:
          type:
            - string
            - 'null'
        purchase_order_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        cost_status:
          $ref: '#/components/schemas/ReceiptCostStatus'
        line_count:
          type: integer
          minimum: 0
        unpriced_line_count:
          type: integer
          minimum: 0
        valuation_pending_count:
          type: integer
          minimum: 0
      additionalProperties: false
    UnpricedReceiptCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - unpriced_receipts
          properties:
            unpriced_receipts:
              type: array
              items:
                $ref: '#/components/schemas/UnpricedReceipt'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/PaginationMeta'
    CompleteReceiptPriceLine:
      type: object
      required:
        - goods_receipt_line_id
        - unit_price_amount
        - cost_currency_code
      description: |
        One line's missing price. `line_total_amount` is optional and is
        **checked** against quantity × unit price rather than stored as given:
        the total and the price are two views of one fact, and a database holding
        two answers for it is worse than one that refuses.
      properties:
        goods_receipt_line_id:
          $ref: '#/components/schemas/Uuid'
        unit_price_amount:
          type: number
          minimum: 0
          description: Major currency units per the line's own unit (§4.4).
        line_total_amount:
          type:
            - number
            - 'null'
          minimum: 0
        cost_currency_code:
          type: string
          minLength: 3
          maxLength: 3
      additionalProperties: false
    CompleteReceiptPricesRequest:
      type: object
      required:
        - lines
      description: |
        Prices only. The quantities are untouchable here — §3.6 is explicit that
        posted quantities are never edited in place, and nothing on this request
        could change what arrived.

        Every line named must still have a null `costed_at`, and every currency
        on the request must agree with itself and with the receipt's
        already-priced lines.
      properties:
        lines:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/CompleteReceiptPriceLine'
      additionalProperties: false
    SupplierRef:
      type: object
      required:
        - id
        - code
        - name_en
      description: A supplier named on a receipt or ledger line (INV1.1).
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        name_en:
          type: string
      additionalProperties: false
    GoodsReceiptCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - goods_receipts
          properties:
            goods_receipts:
              type: array
              items:
                $ref: '#/components/schemas/GoodsReceipt'
        meta:
          $ref: '#/components/schemas/Meta'
    GoodsReceiptLineInput:
      type: object
      required:
        - stock_item_id
        - quantity
      description: |
        A line's price is optional (INV1.1). `cost_currency_code` is required
        with a price — a price with no currency is not a price. `unit_id` is
        optional: omit it and the price is taken as per the stock item's own
        unit, which is how a delivery note reads. A priced line whose stock item
        is backed by an ingredient blends into that ingredient's weighted
        moving-average cost; an unpriced line moves stock only.
      properties:
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        purchase_order_line_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            The ordered line this delivery fulfils (SUP5). Only valid when the
            receipt names a `purchase_order_id`, and it must belong to that order
            and stock the same item. Omitted on a direct purchase and on an
            unplanned extra item, which needs the receipt's `variance_note`
            instead (§4).
        quantity:
          type: number
          exclusiveMinimum: 0
        unit_id:
          $ref: '#/components/schemas/Uuid'
        unit_price_amount:
          type: number
          minimum: 0
          description: Major currency units per unit_id (§4.4).
        cost_currency_code:
          type: string
          minLength: 3
          maxLength: 3
      additionalProperties: false
    PostGoodsReceiptRequest:
      type: object
      required:
        - branch_id
        - lines
      description: |
        Posting a receipt writes every line straight into the inventory
        ledger (`reason: receipt`) inside one transaction — there is no
        draft state to save and return to. Since INV1.1 it also records the
        supplier and document reference and blends each priced line's cost, and
        since SUP5 it settles a purchase order in the same transaction.

        Amounts are non-negative magnitudes. A **discount** is sent positive and
        subtracted by the arithmetic rather than sent negative, so the sign
        convention lives in one place rather than in every client.
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        supplier_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Who the stock was bought from (INV1.1). Required in practice when
            `purchase_order_id` is sent, because §4 insists a delivery against an
            order names that order's supplier.
        document_ref:
          type:
            - string
            - 'null'
          description: The supplier delivery note, as written (INV1.1).
        supplier_invoice_ref:
          type:
            - string
            - 'null'
          maxLength: 120
          description: The supplier's invoice number — a different document from the delivery note (SUP5).
        invoice_date:
          type:
            - string
            - 'null'
          format: date
        received_on:
          type:
            - string
            - 'null'
          format: date
          description: |
            The branch-local business day this delivery belongs to (§3.6). Omit
            it and the server reads today in the receiving branch's own timezone.
            A future date is refused — a delivery that has not happened is not a
            delivery, and slice 6 would file its money in a period that has not
            started.
        variance_note:
          type:
            - string
            - 'null'
          maxLength: 255
          description: |
            Why this delivery differs from what was ordered. Required for an
            over-receipt and for any line with no `purchase_order_line_id` on a
            delivery against an order (§3.5, §4).
        purchase_order_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            The order this delivery settles (SUP5). It must be `issued` or
            `partially_received` and must name this receipt's branch and
            supplier; its received state is updated in the same transaction.
        discount_amount:
          type:
            - number
            - 'null'
          minimum: 0
        tax_amount:
          type:
            - number
            - 'null'
          minimum: 0
        delivery_amount:
          type:
            - number
            - 'null'
          minimum: 0
        other_charges_amount:
          type:
            - number
            - 'null'
          minimum: 0
        invoice_total_amount:
          type:
            - number
            - 'null'
          minimum: 0
          description: |
            When present it must equal `Σ line totals − discount + tax + delivery
            + other charges` (§3.6), else `422` naming the difference.
        over_receipt_confirmed:
          type:
            - boolean
            - 'null'
          description: |
            Confirms that more is arriving than the order still has outstanding.
            Required to record an over-receipt at all, and not sufficient on its
            own: §3.5 asks for an explicit confirmation **and** a variance note.
        close_short:
          type:
            - boolean
            - 'null'
          description: |
            Close the rest of this order (§3.5). Only valid with a
            `purchase_order_id`, and the order becomes `received` with a
            `closed_at` stamp. If the delivery turns out to complete the order
            anyway, no reason is stored, because there is nothing left to explain.
        close_short_reason:
          type:
            - string
            - 'null'
          maxLength: 255
          description: Required with `close_short`. A blank is not an explicit reason.
        lines:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/GoodsReceiptLineInput'
      additionalProperties: false
    GoodsReceiptEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - goods_receipt
          properties:
            goods_receipt:
              type: object
              required:
                - id
                - received_on
                - cost_status
              description: |
                The identifier, plus the two facts a client acts on straight
                away: which day this delivery was filed under, and whether its
                paperwork is done.
              properties:
                id:
                  $ref: '#/components/schemas/Uuid'
                received_on:
                  type:
                    - string
                    - 'null'
                  format: date
                cost_status:
                  $ref: '#/components/schemas/ReceiptCostStatus'
        meta:
          $ref: '#/components/schemas/Meta'
    PurchasesLedgerLine:
      type: object
      required:
        - id
        - goods_receipt_id
        - received_at
        - received_on
        - supplier
        - document_ref
        - purchase_order_id
        - cost_status
        - stock_item_id
        - item_code
        - item_name_en
        - ingredient_id
        - quantity
        - unit_id
        - unit_price_amount
        - line_total_amount
        - cost_currency_code
        - valuation_pending_fx
        - costs_redacted
      description: One purchases-ledger row — a receipt line with its date, supplier and item (INV1.1).
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        goods_receipt_id:
          $ref: '#/components/schemas/Uuid'
        received_at:
          type:
            - string
            - 'null'
          format: date-time
        received_on:
          type:
            - string
            - 'null'
          format: date
          description: The branch-local business day the receipt is filed under (SUP5).
        supplier:
          oneOf:
            - $ref: '#/components/schemas/SupplierRef'
            - type: 'null'
        document_ref:
          type:
            - string
            - 'null'
        purchase_order_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        cost_status:
          oneOf:
            - $ref: '#/components/schemas/ReceiptCostStatus'
            - type: 'null'
          description: The parent receipt's costing state (SUP5) — what the completeness filter reads.
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        item_code:
          type:
            - string
            - 'null'
        item_name_en:
          type:
            - string
            - 'null'
        ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        quantity:
          type: string
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        unit_price_amount:
          type:
            - string
            - 'null'
        line_total_amount:
          type:
            - string
            - 'null'
        cost_currency_code:
          type:
            - string
            - 'null'
        valuation_pending_fx:
          type: boolean
          description: |
            This line's price is recorded and its valuation is waiting on an
            exchange-rate decision (§3.6). A work state, not money, so it is
            never redacted.
        costs_redacted:
          type: boolean
      additionalProperties: false
    PurchasesLedgerCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - purchases
          properties:
            purchases:
              type: array
              items:
                $ref: '#/components/schemas/PurchasesLedgerLine'
        meta:
          $ref: '#/components/schemas/PaginationMeta'
    SpendSummarySupplierBreakdown:
      type: object
      required:
        - supplier
        - received_line_count
        - item_subtotal
      description: |
        One supplier's item spend inside one period and currency (§3.7). Money
        and counts only — the receipt-level charges are a fact about a whole
        delivery and are reported once at the currency level rather than divided
        up here. A `null` supplier is a direct market purchase.
      properties:
        supplier:
          oneOf:
            - $ref: '#/components/schemas/SupplierRef'
            - type: 'null'
        received_line_count:
          type: integer
          minimum: 0
        item_subtotal:
          type: string
          description: Σ line totals for this supplier in this period and currency, major units.
      additionalProperties: false
    SpendSummaryStockItemBreakdown:
      type: object
      required:
        - stock_item_id
        - item_code
        - item_name_en
        - received_line_count
        - item_subtotal
      description: One shelf's item spend inside one period and currency (§3.7).
      properties:
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        item_code:
          type: string
        item_name_en:
          type: string
        received_line_count:
          type: integer
          minimum: 0
        item_subtotal:
          type: string
      additionalProperties: false
    SpendSummaryCurrencyTotals:
      type: object
      required:
        - currency_code
        - receipt_count
        - received_line_count
        - item_subtotal
        - discount_total
        - tax_total
        - delivery_total
        - other_charges_total
        - invoice_total
        - invoiced_receipt_count
        - by_supplier
        - by_stock_item
      description: |
        One currency's money inside one period (§3.7). Figures are never summed
        across currencies; every amount is a major-unit decimal string.

        `item_subtotal` is Σ receipt-line totals — what the goods themselves
        cost, and the figure that reconciles line for line to the purchases
        ledger. The four charge totals and `invoice_total` are receipt-level and
        deliberately separate: tax and delivery are not an ingredient's purchase
        price. A charge nobody recorded is `0`; an absent `invoice_total` is
        `null`, because an unknown invoice total is not a zero one.

        The two breakdowns are `null` when they were not requested and a list
        (possibly empty) when they were.
      properties:
        currency_code:
          type: string
          description: ISO 4217 currency every amount in this row is denominated in.
        receipt_count:
          type: integer
          minimum: 0
          description: Receipts contributing money in this currency.
        received_line_count:
          type: integer
          minimum: 0
          description: Priced lines in this currency. Unpriced lines have no currency and are counted on the period.
        item_subtotal:
          type: string
        discount_total:
          type: string
          description: Recorded positive; the invoice arithmetic subtracts it (§3.6).
        tax_total:
          type: string
        delivery_total:
          type: string
        other_charges_total:
          type: string
        invoice_total:
          type:
            - string
            - 'null'
          description: Σ supplier-invoice totals where one was recorded; null when none was.
        invoiced_receipt_count:
          type: integer
          minimum: 0
          description: How many receipts in this bucket carried an invoice total.
        by_supplier:
          type:
            - array
            - 'null'
          items:
            $ref: '#/components/schemas/SpendSummarySupplierBreakdown'
        by_stock_item:
          type:
            - array
            - 'null'
          items:
            $ref: '#/components/schemas/SpendSummaryStockItemBreakdown'
      additionalProperties: false
    SpendSummaryPeriod:
      type: object
      required:
        - period
        - period_start
        - period_end
        - receipt_count
        - unpriced_receipt_count
        - unpriced_line_count
        - valuation_pending_line_count
        - is_complete
        - totals_by_currency
      description: |
        One ISO week or calendar month of purchasing (§3.7).

        The completeness facts sit here rather than inside a currency row because
        an unpriced line has no currency to belong to. A period with only
        unpriced deliveries therefore carries an empty `totals_by_currency` and
        `is_complete: false`.

        `unpriced_line_count` and `valuation_pending_line_count` are disjoint and
        both keep `is_complete` false: the first is "type these prices in", the
        second is "the price is recorded and its valuation waits on an
        exchange-rate decision" (§3.6).
      properties:
        period:
          type: string
          description: '`IYYY-Www` for a week (`2026-W34`), `YYYY-MM` for a month (`2026-08`).'
        period_start:
          type: string
          format: date
          description: First calendar day this period covers — a week starts Monday.
        period_end:
          type: string
          format: date
        receipt_count:
          type: integer
          minimum: 0
          description: Receipts with at least one line in this period and filter scope.
        unpriced_receipt_count:
          type: integer
          minimum: 0
          description: Of those, the ones carrying at least one line whose money is not settled.
        unpriced_line_count:
          type: integer
          minimum: 0
          description: Lines with no price recorded. They contribute quantity history and no money (§3.7).
        valuation_pending_line_count:
          type: integer
          minimum: 0
          description: Lines whose price is recorded and whose valuation waits on an exchange rate (§3.6).
        is_complete:
          type: boolean
          description: True only when neither count above is non-zero.
        totals_by_currency:
          type: array
          items:
            $ref: '#/components/schemas/SpendSummaryCurrencyTotals'
      additionalProperties: false
    SpendSummaryCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - group_by
            - from
            - to
            - periods
          properties:
            group_by:
              type: string
              enum:
                - week
                - month
            from:
              type: string
              format: date
              description: The resolved lower bound actually read, whether it was sent or defaulted.
            to:
              type: string
              format: date
            periods:
              type: array
              items:
                $ref: '#/components/schemas/SpendSummaryPeriod'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/Meta'
    MonthlyCostReportRow:
      type: object
      required:
        - month
        - currency_code
        - spend_amount
        - cogs_amount
        - waste_amount
        - waste_quantity
        - revenue_amount
        - gross_margin_amount
        - gross_margin_percent
        - meal_revenue_amount
        - product_revenue_amount
        - other_revenue_amount
        - meal_cogs_amount
        - product_cogs_amount
        - other_cogs_amount
        - has_data_quality_flag
        - exception_count
        - is_spend_complete
        - unpriced_line_count
        - valuation_pending_line_count
      description: 'One month of one kitchen''s economics in one currency (INV1.4). Every amount is a major-unit decimal string; figures are never summed across currencies. Two data-quality flags, never merged: one says the month''s COGS is understated by unresolved consumption exceptions, the other says its spend is understated because a delivery''s invoice has not been entered (SUP6, §3.6). The three spend-completeness fields are month facts rather than currency facts — an unpriced line has no currency — so, like waste_quantity, they repeat across a month''s currency rows.'
      properties:
        month:
          type: string
          pattern: ^\d{4}-(0[1-9]|1[0-2])$
          description: The report month, `YYYY-MM`.
        currency_code:
          type: string
          description: ISO 4217 currency every amount in this row is denominated in.
        spend_amount:
          type: string
          description: Purchasing spend — Σ goods-receipt line totals, major units.
        cogs_amount:
          type: string
          description: Cost of goods sold — Σ consume-movement cost, cancelled orders excluded, major units.
        waste_amount:
          type:
            - string
            - 'null'
          description: Value of wasted stock where a cost was recorded, major units; null when no waste carried a cost.
        waste_quantity:
          type:
            - string
            - 'null'
          description: Total wasted quantity this month, the honest note when waste carries no cost.
        revenue_amount:
          type: string
          description: Selling revenue — Σ order totals for confirmed/fulfilled orders, converted from minor to major units.
        gross_margin_amount:
          type: string
          description: Revenue minus COGS, major units.
        gross_margin_percent:
          type:
            - string
            - 'null'
          description: Margin as a percentage of revenue; null when revenue is zero.
        meal_revenue_amount:
          type: string
          description: Revenue from meal lines, major units.
        product_revenue_amount:
          type: string
          description: Revenue from resold-product lines, major units.
        other_revenue_amount:
          type: string
          description: Revenue from other lines (subscription-plan and anything not a meal or product), major units.
        meal_cogs_amount:
          type: string
          description: Cost of goods sold on meal lines — Σ exploded-recipe consume cost, major units (INV1.5).
        product_cogs_amount:
          type: string
          description: Cost of goods sold on resold-product lines — Σ own moving-average consume cost, major units (INV1.5).
        other_cogs_amount:
          type: string
          description: Cost of goods sold not attributed to a meal or product line (e.g. a consume predating INV1.5), major units. The three COGS splits reconcile to cogs_amount.
        has_data_quality_flag:
          type: boolean
          description: True when unresolved consumption exceptions mean this month's COGS is understated.
        exception_count:
          type: integer
          description: Count of unresolved consumption exceptions on non-cancelled orders anchored to this month.
        is_spend_complete:
          type: boolean
          description: False when this month holds a delivery whose price is missing, or one whose recorded price could not be valued in the ingredient's currency — spend_amount is then understated (§3.6). A month whose only activity is unpriced receipts produces no row at all; the spend summary is where such a period appears.
        unpriced_line_count:
          type: integer
          minimum: 0
          description: Receipt lines in this month with no price recorded. A month fact, repeated across the month's currency rows.
        valuation_pending_line_count:
          type: integer
          minimum: 0
          description: Receipt lines in this month whose price is recorded and whose valuation waits on an exchange rate.
      additionalProperties: false
    MonthlyCostReportCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - report
          properties:
            report:
              type: array
              items:
                $ref: '#/components/schemas/MonthlyCostReportRow'
        meta:
          $ref: '#/components/schemas/Meta'
    ConsumptionExceptionReasonCode:
      type: string
      description: Why a confirmed order could not deduct a line honestly (INV1.2). A closed vocabulary the consumption service raises; a client renders it as a human label rather than branching on it.
      enum:
        - no_branch
        - no_catalogue_item
        - no_recipe_version
        - no_yield_piece_count
        - unquantified_recipe_line
        - no_ingredient_link
        - no_stock_item
        - no_stock_unit
        - unit_conversion_unsupported
        - no_ingredient_cost
        - insufficient_stock
    ConsumptionException:
      type: object
      required:
        - id
        - order_id
        - order_number
        - order_line_id
        - catalogue_item_id
        - item_name_en
        - branch_id
        - branch_name
        - reason_code
        - detail
        - resolved
        - resolved_at
        - resolved_by
        - resolution_note
        - created_at
      description: One thing a confirmed order could not deduct honestly (INV1.2), with its resolution state (INV1.5), joined to the human-readable names of the order, sold item and branch it points at. Nothing confidential — no recipe line, formulation quantity, ingredient cost or supplier term — passes through.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        order_number:
          type:
            - string
            - 'null'
        order_line_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        catalogue_item_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        item_name_en:
          type:
            - string
            - 'null'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        branch_name:
          type:
            - string
            - 'null'
        reason_code:
          $ref: '#/components/schemas/ConsumptionExceptionReasonCode'
        detail:
          type:
            - string
            - 'null'
        resolved:
          type: boolean
          description: True when the exception has been settled — the same as resolved_at being non-null.
        resolved_at:
          type:
            - string
            - 'null'
          format: date-time
        resolved_by:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        resolution_note:
          type:
            - string
            - 'null'
        created_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    ConsumptionExceptionCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - exceptions
          properties:
            exceptions:
              type: array
              items:
                $ref: '#/components/schemas/ConsumptionException'
        meta:
          $ref: '#/components/schemas/PaginationMeta'
    ConsumptionExceptionEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - exception
          properties:
            exception:
              $ref: '#/components/schemas/ConsumptionException'
        meta:
          $ref: '#/components/schemas/Meta'
    ConsumptionExceptionCountEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - count
          properties:
            count:
              type: integer
              minimum: 0
              description: How many consumption exceptions are unresolved in scope.
        meta:
          $ref: '#/components/schemas/Meta'
    ResolveConsumptionExceptionRequest:
      type: object
      description: Optionally records why an exception is being marked handled.
      properties:
        note:
          type:
            - string
            - 'null'
          maxLength: 500
          description: A free-text reason the reviewer typed, or null.
      additionalProperties: false
    ProductionOrder:
      type: object
      required:
        - id
        - recipe_version_id
        - status
        - branch_id
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        recipe_version_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
            - planned
            - in_progress
            - completed
            - cancelled
        branch_id:
          $ref: '#/components/schemas/Uuid'
      additionalProperties: false
    ProductionOrderCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - production_orders
          properties:
            production_orders:
              type: array
              items:
                $ref: '#/components/schemas/ProductionOrder'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateProductionOrderRequest:
      type: object
      required:
        - branch_id
        - recipe_version_id
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        recipe_version_id:
          $ref: '#/components/schemas/Uuid'
        planned_yield:
          type:
            - number
            - 'null'
      additionalProperties: false
    ProductionOrderEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - production_order
          properties:
            production_order:
              type: object
              required:
                - id
                - status
              properties:
                id:
                  $ref: '#/components/schemas/Uuid'
                status:
                  type: string
                  enum:
                    - planned
                    - in_progress
                    - completed
                    - cancelled
        meta:
          $ref: '#/components/schemas/Meta'
    ProductionMovementInput:
      type: object
      required:
        - stock_item_id
        - quantity
      properties:
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type: number
      additionalProperties: false
    CompleteProductionOrderRequest:
      type: object
      description: |
        No production tasks here (O5) — completing an order is entirely
        about the stock it consumed and yielded, not a checklist.
      properties:
        consumes:
          type: array
          items:
            $ref: '#/components/schemas/ProductionMovementInput'
          description: Stock taken from the branch's inventory to run this order. Omitted or empty is legal.
        yields:
          type: array
          items:
            $ref: '#/components/schemas/ProductionMovementInput'
          description: Stock produced by this order and added back to inventory.
      additionalProperties: false
    QualityCheckSubjectType:
      type: string
      enum:
        - goods_receipt
        - production_order
      description: |
        The only two subjects a check may attach to (O3). Anything else is
        `422 validation.failed`.
    QualityCheck:
      type: object
      required:
        - id
        - subject_type
        - subject_id
        - status
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        subject_type:
          $ref: '#/components/schemas/QualityCheckSubjectType'
        subject_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
            - pending
            - passed
            - hold
            - released
          description: |
            A hold is status-only in v1 (O4) — there is no separate hold
            record, and releasing simply moves the same check back off
            `hold`.
      additionalProperties: false
    QualityCheckCollection:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - quality_checks
          properties:
            quality_checks:
              type: array
              items:
                $ref: '#/components/schemas/QualityCheck'
        meta:
          $ref: '#/components/schemas/Meta'
    CreateQualityCheckRequest:
      type: object
      required:
        - subject_type
        - subject_id
      properties:
        subject_type:
          $ref: '#/components/schemas/QualityCheckSubjectType'
        subject_id:
          $ref: '#/components/schemas/Uuid'
        notes:
          type:
            - string
            - 'null'
      additionalProperties: false
    QualityCheckEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - quality_check
          properties:
            quality_check:
              type: object
              required:
                - id
                - status
              properties:
                id:
                  $ref: '#/components/schemas/Uuid'
                status:
                  type: string
                  enum:
                    - pending
                    - passed
                    - hold
                    - released
        meta:
          $ref: '#/components/schemas/Meta'
    CustomerOrder:
      type: object
      required:
        - id
        - order_number
        - status
        - currency_code
        - subtotal_minor
        - delivery_fee_minor
        - total_minor
        - payment_method
        - fulfilment_type
        - delivery
        - placed_at
        - confirmed_at
        - fulfilled_at
        - cancelled_at
        - cancellation_reason
        - line_count
        - lines
        - created_at
      description: |
        The receipt. **Six things on the row are deliberately absent**, each
        for its own reason:

        * `price_list_id` / `price_list_item_id` — a kitchen's tariff structure
          named to the person being charged by it; on an agreement list, one
          customer's negotiated position is reconstructable from which list
          priced their line.
        * `organisation_id` — the seller's internal key. The customer bought
          from a kitchen, not from a UUID. *That this shape carries no kitchen
          identity at all is a stated gap*: naming the kitchen means a public
          projection of an organisation, which belongs to the marketplace
          family.
        * `created_by` — for a self-service order this is the customer and says
          nothing; for a staff-placed order it names an employee to a member of
          the public.
        * `placed_on_behalf_by` — the same disclosure, sharper. Where
          `created_by` merely *might* be an employee, this is non-null on
          exactly the orders a member of staff took for somebody else, so
          serving it would name a named individual to the customer they served
          every single time. Who answered the telephone is the kitchen's record
          of its own shift; a customer with a complaint has the kitchen to
          complain to, not an employee to identify. `fulfilment_type` **is**
          served here and is the counter-example: how the food reaches somebody
          is a term of their own order, and withholding it would leave an empty
          delivery block with no explanation for it.
        * `lock_version` — the validator of a resource this audience cannot
          write. Serving one with no writer invites `If-Match` at an endpoint
          that would ignore it.
        * `branch_id`, `sales_channel_id`, `delivery_zone_id` — the kitchen's
          operating arrangements; none of them changes anything the customer can
          do.

        The **amounts are here**, and that is not an oversight. What somebody was
        charged is printed on their own receipt; what the food cost the kitchen
        is the confidential figure, and no column on either table carries it.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_number:
          type: string
          description: The human-readable reference, unique across the platform.
          examples:
            - H360-2026-000173
        status:
          $ref: '#/components/schemas/OrderStatus'
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
        subtotal_minor:
          type: integer
        delivery_fee_minor:
          type:
            - integer
            - 'null'
          description: Null when no zone charged one — which is not the same as zero.
        total_minor:
          type: integer
        payment_method:
          $ref: '#/components/schemas/PaymentMethod'
        fulfilment_type:
          $ref: '#/components/schemas/FulfilmentType'
        delivery:
          $ref: '#/components/schemas/CustomerOrderDelivery'
        placed_at:
          type: string
          format: date-time
        confirmed_at:
          type:
            - string
            - 'null'
          format: date-time
        fulfilled_at:
          type:
            - string
            - 'null'
          format: date-time
        cancelled_at:
          type:
            - string
            - 'null'
          format: date-time
        cancellation_reason:
          oneOf:
            - $ref: '#/components/schemas/CancellationReason'
            - type: 'null'
        line_count:
          type: integer
          minimum: 0
        lines:
          type: array
          items:
            $ref: '#/components/schemas/CustomerOrderLine'
        created_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    KitchenOrder:
      type: object
      required:
        - id
        - order_number
        - organisation_id
        - customer_account_id
        - sales_channel_id
        - branch_id
        - status
        - currency_code
        - subtotal_minor
        - delivery_fee_minor
        - total_minor
        - payment_method
        - fulfilment_type
        - delivery
        - placed_at
        - confirmed_at
        - fulfilled_at
        - cancelled_at
        - cancellation_reason
        - created_by
        - placed_on_behalf_by
        - lock_version
        - line_count
        - lines
        - created_at
        - updated_at
      description: |
        The seller's view of the same row. Built independently of the customer
        shape rather than as a superset of it: a wide shape and a narrow one
        derived from it by subtraction is how a column added next year quietly
        reaches a receipt.

        `lock_version` is here because this is the audience that **writes** — a
        screen that could not read the validator could not send the `If-Match`
        the three lifecycle actions demand.

        **`customer_account_id` is nullable and `fulfilment_type` says when.**
        A counter sale may be a stranger buying a sandwich, and there is nobody
        to name; a delivery and a pickup always have somebody. The pair has to
        be read together — a null customer is a fact about a walk-in, never a
        row that failed to load.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_number:
          type: string
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        customer_account_id:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            Null only on a `counter` sale nobody named. Present on every
            delivery and every pickup — `orders_fulfilment_shape_check` requires
            it — and present on a counter sale for a regular the desk chose to
            record.
        sales_channel_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          type:
            - string
            - 'null'
          format: uuid
        status:
          $ref: '#/components/schemas/OrderStatus'
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
        subtotal_minor:
          type: integer
        delivery_fee_minor:
          type:
            - integer
            - 'null'
        total_minor:
          type: integer
        payment_method:
          $ref: '#/components/schemas/PaymentMethod'
        fulfilment_type:
          $ref: '#/components/schemas/FulfilmentType'
        delivery:
          $ref: '#/components/schemas/KitchenOrderDelivery'
        placed_at:
          type: string
          format: date-time
        confirmed_at:
          type:
            - string
            - 'null'
          format: date-time
        fulfilled_at:
          type:
            - string
            - 'null'
          format: date-time
        cancelled_at:
          type:
            - string
            - 'null'
          format: date-time
        cancellation_reason:
          oneOf:
            - $ref: '#/components/schemas/CancellationReason'
            - type: 'null'
        created_by:
          type:
            - string
            - 'null'
          format: uuid
        placed_on_behalf_by:
          type:
            - string
            - 'null'
          format: uuid
          description: |
            The member of staff who took this order **for** somebody else, and
            null on every self-service order.

            Deliberately not `created_by`, which is written on every path — a
            customer placing their own order writes their own user id there — so
            "did a person at a desk take this" is not answerable from it without
            asking whether that user happens to be an employee. Seller-only: the
            customer's shape carries neither.
        lock_version:
          type: integer
          minimum: 0
        line_count:
          type: integer
          minimum: 0
        lines:
          type: array
          items:
            $ref: '#/components/schemas/KitchenOrderLine'
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    CustomerOrderEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - order
          properties:
            order:
              $ref: '#/components/schemas/CustomerOrder'
        meta:
          $ref: '#/components/schemas/Meta'
    KitchenOrderEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - order
          properties:
            order:
              $ref: '#/components/schemas/KitchenOrder'
        meta:
          $ref: '#/components/schemas/Meta'
    StorePaymentReceiptRequest:
      type: object
      required:
        - method
        - amount_minor
      description: |
        One statement that money arrived against an order.

        **There is no `currency_code` field, and that is a decision rather than
        an omission.** The receipt is denominated in the order's own currency,
        taken from the order by the server: a receipt in another currency is not
        a receipt for this order, it is a conversion nobody performed and a
        figure that cannot be summed against the order's total. Accepting the
        field would mean either validating it against the order — a round trip to
        be told what the server already knows — or trusting it, and a trusted
        mismatch is a kitchen's takings quietly wrong in two currencies at once.

        `method` is **not** constrained to the order's own `payment_method`. See
        the operation: recording the divergence is the point.
      properties:
        method:
          $ref: '#/components/schemas/PaymentMethod'
        amount_minor:
          type: integer
          minimum: 1
          description: |
            What arrived, in minor units of the order's currency.

            **At least one**, although the column's own constraint admits zero.
            The two answer different questions: the constraint refuses a
            *negative* figure, because giving money back is a refund and belongs
            to the payments surface. This operation is stricter because a receipt
            is evidence — somebody asserting that no money changed hands has
            recorded a keystroke, and the row would sit in a day's takings as a
            payment event worth nothing.

            There is no upper bound and none against the order's total.
            Over-payment is ordinary: a customer hands over a round note and
            takes change from the till, which is not a row in this ledger.
          examples:
            - 3000
        reference:
          type:
            - string
            - 'null'
          maxLength: 120
          description: |
            The transaction identifier the desk read off the merchant app — a
            WISH transfer's, in practice, because cash has no such thing. Free
            text because it is somebody else's vocabulary, and bounded because it
            is confidential: it points at a real transaction between two named
            parties.
          examples:
            - WSH-77213
        notes:
          type:
            - string
            - 'null'
          maxLength: 300
          description: |
            Anything the person taking the money needs the next person to know.
            Confidential and bounded for the same reason: a free-text note
            written at a counter is exactly where somebody puts a customer's
            name.
      additionalProperties: false
    OrderPaymentReceipt:
      type: object
      required:
        - id
        - order_id
        - method
        - amount_minor
        - currency_code
        - reference
        - confirmed_by
        - confirmed_at
        - notes
        - created_at
      description: |
        One recorded arrival of money, and the person who says it arrived.

        **Append-only.** There is no update operation and no delete operation for
        a receipt, and there will not be: money arriving is not an event that
        un-happens, and giving it back is a refund on the payments surface with
        its own authority and its own record.

        No `organisation_id`: a receipt is reachable only through an order the
        caller's own kitchen owns, so the field would restate the caller's
        organisation to itself.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_id:
          $ref: '#/components/schemas/Uuid'
        method:
          $ref: '#/components/schemas/PaymentMethod'
        amount_minor:
          type: integer
          minimum: 0
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: The order's currency, always. Never taken from the request.
        reference:
          type:
            - string
            - 'null'
          description: Null for cash, which has no transaction identifier.
        confirmed_by:
          $ref: '#/components/schemas/Uuid'
        confirmed_at:
          type: string
          format: date-time
          description: |
            When the money arrived, stamped by the server. Not taken from the
            request: a desk that could name the moment could name a moment in
            another shift, and this is what a day's takings are cut on.
        notes:
          type:
            - string
            - 'null'
        created_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    OrderPaymentSummary:
      type: object
      required:
        - method
        - received_minor
        - receipted
      description: |
        Where an order stands on being paid, derived on read and **stored
        nowhere**. There is no `payment_status` column behind this and there will
        not be one: a stored flag beside an append-only ledger is a second answer
        to a question the ledger already answers, and two answers drift.

        `method` is the order's **intended** method, not any receipt's. A desk
        reads this before the money arrives — it is how somebody knows what to
        ask the customer for — so the useful fact here is what the order was
        taken on. What actually arrived is on each receipt, under its own name.

        **`receipted`, not `paid`.** The word is doing real work: this platform
        holds no proof that money exists, only that somebody wrote down that it
        arrived. It is `received_minor >= total_minor` — an inequality in both
        directions, because a part payment leaves it false until the balance
        lands and an over-payment does not make it truer.
      properties:
        method:
          $ref: '#/components/schemas/PaymentMethod'
        received_minor:
          type: integer
          minimum: 0
          description: |
            The sum of every receipt against this order, in the order's currency.
            Zero when there are none — never null, because an order whose payment
            position is unknown is not a state this platform reaches.
        receipted:
          type: boolean
      additionalProperties: false
    OrderPaymentReceiptEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - receipt
            - payment
          properties:
            receipt:
              $ref: '#/components/schemas/OrderPaymentReceipt'
            payment:
              $ref: '#/components/schemas/OrderPaymentSummary'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/Meta'
    OrderDeskWindow:
      type: string
      enum:
        - today
        - overdue
        - next_7
      description: |
        Which slice of the open book the desk is looking at — three questions
        somebody at a desk actually asks, rather than a date range that could
        express a hundred nobody does.

        **`today` includes orders with no requested date at all.** An order whose
        customer named no day is not scheduled for nothing, it is scheduled for
        as soon as possible, and a plain range would silently drop exactly the
        set nobody has committed to a day yet. `overdue` does not inherit that
        rule: a dateless order is never late, because there is no day it has
        missed. `next_7` runs from today to seven days after it, inclusive at
        both ends.
    OrderDeskDeliveryJob:
      type: object
      required:
        - id
        - status
        - tracking_status
        - driver_user_id
        - assigned_at
        - lock_version
      description: |
        The run this order became: which driver has it, what state it is in, and
        when it was assigned.

        **`null` is a real answer, and it is true of three different orders.** A
        pickup or a counter sale is never driven anywhere. A delivery order still
        `placed` has no run yet, because a job is projected on *confirm* — the
        moment a kitchen commits to cook — and a placed order may still be
        cancelled without a driver hearing about it. And a delivery order
        confirmed before C3 shipped was never projected and will not be
        retrospectively. All three read as `null`; a screen that needs to
        distinguish them has `fulfilment_type` and `status` on the same row.

        **No address here.** Where the food is going is already on the row's own
        `delivery` block, and a second copy inside the job would be two answers
        to one question.

        This seat was declared nullable and empty before anything could fill it,
        so the row would not change shape under a client when C3 arrived — the
        same argument the payment seat was declared on.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/DeliveryJobStatus'
        tracking_status:
          $ref: '#/components/schemas/DeliveryJobTrackingStatus'
        driver_user_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: '`null` until dispatch assigns one.'
        assigned_at:
          type:
            - string
            - 'null'
          format: date-time
        lock_version:
          type: integer
          description: |
            The job's own validator, carried on the queue row so an Assign dialog
            has its `If-Match` without re-reading the job. It is **not** the
            order's `lock_version` and the two move independently — assigning a
            driver deliberately does not touch the order.
      additionalProperties: false
    OrderDeskCustomerContact:
      type: object
      required:
        - display_name
        - phone
      description: |
        The customer's name, and the number somebody can ring them on.

        **Present only when the caller holds
        `order.view_customer_contact_organisation`**, and *absent* — not null —
        without it. Null here is a fact about the customer ("we hold no number
        for them"), and a screen could not tell that apart from a fact about the
        reader unless the two cases differed in shape.

        Either field may be null on its own: an anonymised account has no name,
        and a customer who never gave a number has no number. The number is
        served **verified or not** — a courier ringing about tonight's delivery
        needs the number the customer gave, not one the platform has proved.

        This is the whole of the disclosure. No address beyond the delivery
        snapshot the order book already serves, no email, no allergen
        declaration, no order history, and no account identifier to pivot on.
      properties:
        display_name:
          type:
            - string
            - 'null'
          description: |
            The account's, always. There is no snapshot of a name on an order
            and there should not be: a name is who somebody is, which does not
            change per delivery.
        phone:
          type:
            - string
            - 'null'
          description: |
            E.164, as the contact point stores it.

            **The number the courier was given, when the order snapshot carries
            one; the account's primary otherwise.** A delivery order records the
            contact point the customer nominated for that delivery, and it is
            preferred here — a caller ordering to their mother's flat gives their
            mother's landline, and the account's primary is then a mobile in
            somebody's pocket in another building. Every pickup, every counter
            sale and every delivery placed before the snapshot column existed
            falls through to the account's own number, which is what this field
            has always been.
      additionalProperties: false
    OrderDeskRow:
      allOf:
        - $ref: '#/components/schemas/KitchenOrder'
        - type: object
          required:
            - due_at
            - payment
            - delivery_job
          properties:
            due_at:
              type: string
              format: date-time
              description: |
                The instant this order is actually due, as a UTC ISO-8601
                timestamp, and **never null**. It is what the queue is sorted by,
                computed in SQL from the requested day, the delivery window's
                start and the branch's timezone, falling through the end of the
                requested day when there are no window hours, through UTC when
                there is no branch, and through `placed_at` when the customer
                named no day at all.
            payment:
              $ref: '#/components/schemas/OrderPaymentSummary'
            delivery_job:
              oneOf:
                - $ref: '#/components/schemas/OrderDeskDeliveryJob'
                - type: 'null'
            customer:
              $ref: '#/components/schemas/OrderDeskCustomerContact'
          description: |
            One row of the desk queue: everything the order book's row carries,
            plus the queue's own arithmetic, the order's payment position, and
            the run it became.

            `payment` is **never null**. Every order has a payment position, and
            "nothing has been paid" is `received_minor: 0` with
            `receipted: false` rather than an absent object — null would be a
            third state meaning nothing a desk could act on. It is derived for
            the whole page in one grouped aggregate over the receipts ledger,
            never a query per row.

            `delivery_job` **is** nullable, and the difference is the point:
            "there is no run" is a fact about an order rather than a gap in the
            server's knowledge. It is resolved for the whole page in one
            statement, the same batching rule the payment aggregate follows.

            The `allOf` is honest here rather than a documentation convenience.
            This shape really is the kitchen shape **plus** fields — the server
            composes it — and that is safe where a narrowing would not be,
            because the desk is served to the same audience behind the same
            `order.view_organisation`. Rebuilding the seller's twenty-odd fields
            a second time would mean two hand-maintained copies, and the day
            somebody added a column to one the desk would quietly serve a row the
            book does not.

            `customer` is **not** in `required`: it is present only for a caller
            holding `order.view_customer_contact_organisation`.
    OrderDeskQueueEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/OrderDeskRow'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
                - limit
                - truncated
                - window
                - today
                - timezone
              properties:
                count:
                  type: integer
                  minimum: 0
                  description: Rows in this response.
                limit:
                  type: integer
                  description: |
                    The most rows this operation will ever return. There is no
                    second page — the computed sort makes a cursor impossible —
                    so the cap is stated rather than implied.
                truncated:
                  type: boolean
                  description: |
                    More orders matched than were returned. A screen showing a
                    truncated queue has to say so: a silent truncation would hide
                    exactly the backlog the queue exists to surface.
                window:
                  $ref: '#/components/schemas/OrderDeskWindow'
                today:
                  type: string
                  format: date
                  description: The day the window was measured from.
                timezone:
                  type: string
                  description: |
                    The IANA zone that day was computed in — the named branch's,
                    or `UTC` when no branch was named. Echoed because the client
                    did not choose it and cannot derive it.
    OrderDeskBasketLine:
      type: object
      required:
        - catalogue_item_id
        - quantity
      description: |
        One tap at the counter. The desk sends what the agent pressed, duplicates
        and all: the server merges lines naming the same article **and the same
        pack** into one line whose quantity is the sum, first-seen position
        preserved, because `order_lines` holds one row per article and three taps
        of the same coffee is a quantity rather than three lines. An article
        ordered plain and the same article ordered in a pack stay two lines —
        they are two different things to sell.

        Both the quote and the placement merge identically, so the quantity
        quoted is the quantity charged.
      properties:
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The pack, when the article has variants.
        quantity:
          type:
            - number
            - string
          description: |
            How many. Numeric rather than integer because a counter sells 0.35 kg
            as readily as three coffees, and accepted as a string as well as a
            number so a client holding a decimal can send it without a float
            round trip. Zero and negatives are `422`.
          examples:
            - '0.3500'
            - 3
      additionalProperties: false
    OrderDeskSaleBase:
      type: object
      description: |
        The fields a desk quote and a desk sale share — everything except how the
        customer is paying, which only a sale needs to state.

        `fulfilment_type` decides which of the rest are required, forbidden or
        merely allowed. The rule is **not** enforced as validation: it arrives as
        `customer_required` / `address_required` / `address_not_applicable`, in
        the quote's `refusals` or in the placement's refusal envelope, so that a
        request wrong in two ways says both instead of surfacing one as a `422`
        and the other as a `409`.
      required:
        - fulfilment_type
        - lines
      properties:
        fulfilment_type:
          $ref: '#/components/schemas/FulfilmentType'
        lines:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderDeskBasketLine'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            The branch that will produce this order. The same fact the queue
            takes as a query parameter, in a body because a POST states its facts
            in one — and it is not a filter here: it decides **which cut-off
            applies** and **which branch-scoped delivery zone wins**, both of
            which change the answer. A branch belonging to another kitchen is
            `422`.
        customer_account_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Who this is for. Required for a delivery and a pickup, optional for a
            counter sale — a regular is worth naming and a stranger is not.

            Resolved **by identifier**, never inferred from the caller: the
            caller is a member of staff, and inferring would place the order
            against the agent's own account or the kitchen's corporate buyer.
            Where an identifier legitimately comes from is the desk's customer
            search, which is the org-scoped door.
        customer_address_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Where it goes. **Delivery only** — a pickup or a counter sale
            carrying one is refused rather than silently ignored, because an
            address supplied and dropped means the caller believed something
            about this order that is not true of it.

            It must belong to `customer_account_id`; one that does not is `404`,
            deliberately the same answer as "no such address", because confirming
            that an identifier exists but is somebody else's is itself the
            disclosure.
        requested_delivery_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            The day asked for, `YYYY-MM-DD`. Applies to a pickup as well as a
            delivery: a collection is cooked to a slot like anything else, so the
            branch cut-off is asked about it. A counter sale is handed over now
            and has no day to be late for.
        delivery_window_code:
          type:
            - string
            - 'null'
          maxLength: 40
          description: |
            The slot, by the window's own code. Not validated against the
            kitchen's windows here — whether that slot is offered on that day by
            that branch is a scheduling question answered with the cut-off rules
            in hand.
    QuoteOrderDeskRequest:
      allOf:
        - $ref: '#/components/schemas/OrderDeskSaleBase'
        - type: object
          additionalProperties: false
          description: |
            A basket to price, and the shape of the sale it would become. No
            payment method: nothing is being paid for yet.
    PlaceOrderDeskRequest:
      allOf:
        - $ref: '#/components/schemas/OrderDeskSaleBase'
        - type: object
          required:
            - payment_method
          properties:
            payment_method:
              $ref: '#/components/schemas/PaymentMethod'
            payment:
              $ref: '#/components/schemas/OrderDeskCounterPayment'
          additionalProperties: false
          description: |
            The quote's body plus two fields, and the two do different jobs.

            `payment_method` is required on every shape and has no default. It
            was hardcoded to cash on delivery for the whole of the first order
            phase and stopped being able to be the moment somebody could pay at a
            counter; a default here would put that decision back one layer up,
            where it would be even less visible.

            It captures **intent**, and is deliberately not constrained to agree
            with any receipt recorded later: an order taken for cash at the
            counter and settled by a WISH transfer is an ordinary evening, and
            recording the divergence is why the two columns exist.

            `payment` is the money **actually handed over**, and it is
            conditional on `fulfilment_type`: **required** on `counter`,
            **prohibited** on `delivery` and `pickup`. Both violations are `422
            validation.failed`. Sending it is what makes this operation complete
            the sale rather than merely place it — see the operation description.

            There is still no amount of any kind, on either field. Every line is
            repriced at placement against the standing tariff and the receipt is
            written for exactly that total, so a body that could state a figure
            would be a client quoting the server its own prices — at a counter,
            with nobody watching.
    OrderDeskCounterPayment:
      type: object
      required:
        - method
      additionalProperties: false
      description: |
        Money handed over at the counter, recorded as it happens.

        Present on a `counter` sale and refused on the other two, because that is
        what the three words mean. A walk-in pays now: the customer is in the
        room, the food is in front of them, and there is no later moment at which
        the money arrives. A counter order left unpaid would be a *pickup*
        wearing the wrong label — something the kitchen is holding for somebody
        who will settle when they come back — and nothing downstream could tell
        it from a counter sale whose receipt was lost.

        A delivery is settled at the door and a pickup when the customer
        collects, both afterwards through `POST /catalogue/orders/{order}/
        payments`, recorded by whoever actually took the money with their own
        `confirmed_by` and their own moment. Receipting one of those here would
        put cash into a day's takings that is still in a customer's pocket.

        **There is no amount.** The receipt is written for exactly the order's
        `total_minor`. A discrepancy at the till is a till problem, not an order
        problem: rewriting the receipt to match the cash in the drawer would make
        the sale unreconcilable against the tariff that produced it. Part
        payments and over-payments are real elsewhere and arrive as their own
        rows through the receipts operation.
      properties:
        method:
          allOf:
            - $ref: '#/components/schemas/PaymentMethod'
          description: |
            How the money turned up, which is deliberately **not** constrained to
            equal `payment_method`. That one is the intent captured at placement;
            this is what actually happened, and a counter sale taken as cash and
            settled by a WISH transfer while the customer was standing there is
            an ordinary evening.
        reference:
          type:
            - string
            - 'null'
          maxLength: 120
          description: |
            The transfer identifier on a WISH payment, or whatever the desk
            writes down to find this money again. Confidential at rest: it points
            at a real transaction between two named parties.
          examples:
            - WISH-8841203
        notes:
          type:
            - string
            - 'null'
          maxLength: 300
          description: |
            Free text explaining the payment, bounded rather than open because a
            note written at a counter is exactly where somebody puts a customer's
            name. Confidential at rest.
    OrderDeskQuoteRefusal:
      type: object
      required:
        - reason
      description: |
        One thing standing between this basket and a sale, as **data**.

        `reason` is a stable machine key and every other property is whatever
        identifies the offending thing — `catalogue_item_id` on a line refusal,
        `delivery_area_id` on a zone one, `fulfilment_type` on a shape one,
        `cut_off_at` and `branch_id` on a schedule one. The shape is open on
        purpose: it is the same `{reason, …context}` vocabulary the placement
        refusal carries, and pinning the context keys per reason would freeze a
        vocabulary both endpoints extend together.
      properties:
        reason:
          type: string
          description: |
            The stable key. Line-level: `item_unknown`, `item_not_published`,
            `variant_unknown`, `variant_not_active`, `channel_unavailable`,
            `unpriced`, `currency_mismatch`, `channel_not_trading`. Order-level:
            `customer_required`, `address_required`, `address_not_applicable`,
            `address_not_deliverable`, `area_not_served`, `zone_suspended`,
            `currency_mismatch` (with `subject: delivery_fee`), `cut_off_passed`,
            `branch_closed`, `date_in_the_past`.
      additionalProperties: true
    OrderDeskQuoteLine:
      type: object
      required:
        - catalogue_item_id
        - catalogue_item_variant_id
        - quantity
        - name_en
        - name_ar
        - unit_price_minor
        - line_total_minor
        - currency_code
        - refusals
      description: |
        One line of the quote, priced or explained.

        The four price-bearing fields are **null together** whenever the line was
        refused: there is no price to show for an article the kitchen has
        withdrawn, and a zero would read as free. `refusals` is what says why, and
        an empty array is what says the line is sellable.

        `quantity` is the **merged** quantity — duplicate taps summed — because
        that is the number the placement will use and therefore the number the
        agent has to see.
      properties:
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          type:
            - string
            - 'null'
          format: uuid
        quantity:
          type: string
          description: A decimal string, never a number, for the reason every quantity on this platform is one.
          examples:
            - '3.000000'
        name_en:
          type:
            - string
            - 'null'
          description: Null when the article could not be read at all — an identifier from another kitchen, or one that no longer exists.
        name_ar:
          type:
            - string
            - 'null'
        unit_price_minor:
          type:
            - integer
            - 'null'
        line_total_minor:
          type:
            - integer
            - 'null'
          description: Minor units, rounded once at the line total rather than at the unit price, so rounding cannot compound across an order.
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: The price's currency where there is one, and the counter's own tariff currency where there is not.
        refusals:
          type: array
          items:
            $ref: '#/components/schemas/OrderDeskQuoteRefusal'
      additionalProperties: false
    OrderDeskQuote:
      type: object
      required:
        - lines
        - subtotal_minor
        - delivery_fee_minor
        - total_minor
        - currency_code
        - refusals
        - quotable
      description: |
        What this basket would come to, and everything standing in the way of it.

        **The totals are summed over the refusal-free lines only**, and they are
        offered even when the sale cannot proceed because "drop the soup and it
        comes to eleven dollars" is the agent's next sentence. They are not what
        would be charged, which is what `quotable` is for.
      properties:
        lines:
          type: array
          items:
            $ref: '#/components/schemas/OrderDeskQuoteLine'
        subtotal_minor:
          type: integer
          minimum: 0
        delivery_fee_minor:
          type:
            - integer
            - 'null'
          description: |
            **Null rather than zero when no fee applies**, and the difference is
            real: zero is a fee somebody decided on — a free-delivery zone — and
            a pickup or a counter sale has no fee at all. A screen that could not
            tell them apart would print "Delivery: 0.00" on a counter sale. Null
            also when the destination was refused, because an unserved area has
            no fee to quote.
        total_minor:
          type: integer
          minimum: 0
          description: '`subtotal_minor` plus the fee where there is one. No discount slot exists anywhere in this schema.'
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: |
            The counter channel's own tariff currency — the currency of the first
            price list it would actually consult, falling back to the kitchen's
            default when it has none. Nothing is ever converted.
        refusals:
          type: array
          description: Order-level refusals — the shape, the destination and the schedule. Line refusals are on their lines.
          items:
            $ref: '#/components/schemas/OrderDeskQuoteRefusal'
        quotable:
          type: boolean
          description: |
            Whether this basket could be placed as it stands. **False whenever
            any refusal exists at all**, line-level or order-level, because the
            placement operation refuses a whole order when any reason is
            collected — a quote showing a confident total for a basket the sale
            would then reject is a number the agent reads out loud and has to
            take back.
      additionalProperties: false
    OrderDeskQuoteEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - quote
          properties:
            quote:
              $ref: '#/components/schemas/OrderDeskQuote'
        meta:
          $ref: '#/components/schemas/Meta'
    OrderDeskCustomer:
      type: object
      required:
        - id
        - display_name
        - phone
        - origin
        - has_orders_with_org
      description: |
        A customer as the order desk sees them: enough to pick the right one out
        of a list of five, and nothing more.

        `display_name` and `phone` are the disclosure this shape exists for, and
        they are the same pair the queue row carries behind the same permission
        code. The two surfaces deliberately agree — a search revealing more than
        the queue would make the queue's gating pointless.

        **What is absent is each its own decision.** No email: a number is what a
        desk rings, and an address is what a marketing list is built from. No
        account number — it is confidential, it is what a person quotes to
        *support*, and a desk that could read one could quote it back to a caller
        who is not the account holder. No addresses: a street somebody lives on
        belongs to the order going there, not to a search result. No order
        history and no count of other kitchens, so this never becomes the surface
        on which one kitchen reads another's customer relationships. No status,
        because the desk bypasses the activation checklist and it would change
        nothing an agent could do.
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type:
            - string
            - 'null'
          description: Null on an anonymised account.
        phone:
          type:
            - string
            - 'null'
          description: |
            E.164, as the contact point stores it — the account's primary number
            where it has one, otherwise the account holder's. Resolved across
            both ownership arms because a registered customer's number hangs off
            their identity and a desk-provisioned caller's hangs off the account,
            and answering for only one would read as "no phone" rather than as a
            bug. Served verified or not.
        origin:
          type: string
          enum:
            - self_service
            - guest
            - b2b_provisioning
            - staff
            - import
          description: |
            How the record came to exist. `staff` is a caller some kitchen wrote
            down at a desk; it is what an agent checks when deciding whether the
            details on file were typed or were given by the customer themselves.
        has_orders_with_org:
          type: boolean
          description: |
            Whether **this** kitchen holds at least one order against the
            account — a regular versus a name in the book. Scoped to the asking
            organisation and silent about anybody else's trade. It is also the
            first arm of the scoping rule restated as a fact: a `false` here
            means the row is visible on the second arm alone, because somebody
            who works at this kitchen wrote them down.
    OrderDeskCustomersEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/OrderDeskCustomer'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
                - limit
              properties:
                count:
                  type: integer
                  minimum: 0
                limit:
                  type: integer
                  description: |
                    The most rows this operation will ever return. There is no
                    second page: the agent has somebody on the telephone, and
                    the answer to a full list is a longer query.
    OrderDeskCustomerEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - customer
            - possible_duplicates
          properties:
            customer:
              $ref: '#/components/schemas/OrderDeskCustomer'
            possible_duplicates:
              type: array
              description: |
                Accounts **already** holding the number just recorded, in this
                kitchen's scope, computed before the new row existed. Empty is
                the ordinary case and is always present rather than omitted.

                This is a warning, never a refusal: two customers genuinely share
                a telephone — a household, a reception desk, an office floor —
                and the platform's uniqueness rule applies only to verified
                contacts, so a second unverified one is legal. Refusing would
                make an existing customer's flatmate unserveable at a counter
                with somebody waiting.
              items:
                $ref: '#/components/schemas/OrderDeskCustomer'
        meta:
          $ref: '#/components/schemas/Meta'
    OrderDeskCalendarCounts:
      type: object
      required:
        - order
        - scheduled
        - projected
      additionalProperties: false
      description: |
        Three counts of three different kinds of thing, and **they are never
        summed**. There is deliberately no total anywhere in this schema.

        They overlap by construction: a projected day becomes a claimed one, a
        claimed one becomes an order, and the forecast is optimistic about days a
        claim already exists for. Adding them over-counts, and a kitchen ordering
        ingredients from the result would over-buy quietly. Render three numbers.
      properties:
        order:
          type: integer
          minimum: 0
          description: |
            Real orders due this day — every non-cancelled status, because a
            fulfilled order still left the kitchen that day. Orders with no
            requested delivery date are counted on no day at all.
        scheduled:
          type: integer
          minimum: 0
          description: |
            Subscription delivery days the generator has **claimed** and not yet
            turned into an order. A commitment that exists as a row. Skipped days
            are not deliveries and are counted nowhere.
        projected:
          type: integer
          minimum: 0
          description: |
            **A forecast, not a fact.** Days computed from an active
            subscription's weekday pattern and remaining balance, which no row
            asserts. The projection does not consult the generator's own cursor,
            so after an outage — while the hourly tick catches up one delivery
            per hour — it keeps showing the pattern the customer bought rather
            than the backlog the kitchen is working. Right for planning, wrong
            for promising; label it on the screen. Paused subscriptions project
            nothing.
    OrderDeskCalendarWindow:
      type: object
      required:
        - code
        - counts
      additionalProperties: false
      properties:
        code:
          type:
            - string
            - 'null'
          maxLength: 40
          description: |
            The delivery slot's own code, or **`null`** for the day's unslotted
            work — legitimate on both books. Null rather than a placeholder word
            because a code is a kitchen's own vocabulary and any word invented
            here would be one a kitchen could also have typed. The null entry, if
            present, is last.
        counts:
          $ref: '#/components/schemas/OrderDeskCalendarCounts'
    OrderDeskCalendarDay:
      type: object
      required:
        - date
        - counts
        - windows
      additionalProperties: false
      properties:
        date:
          type: string
          format: date
        counts:
          $ref: '#/components/schemas/OrderDeskCalendarCounts'
        windows:
          type: array
          description: |
            The day's counts split by delivery slot, named slots alphabetically
            and the unslotted bucket last. Only slots carrying something appear —
            a screen already knows its own slots, and sixty days of empty ones
            would be mostly zeroes.
          items:
            $ref: '#/components/schemas/OrderDeskCalendarWindow'
    OrderDeskCalendarEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - days
          properties:
            days:
              type: array
              description: |
                Every day of the requested window in date order, **including the
                empty ones**: a calendar with holes is one the client has to
                reconstruct, and "nothing that day" is itself an answer.
              items:
                $ref: '#/components/schemas/OrderDeskCalendarDay'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - from
                - to
                - day_count
                - max_window_days
              properties:
                from:
                  type: string
                  format: date
                to:
                  type: string
                  format: date
                day_count:
                  type: integer
                  minimum: 1
                  description: |
                    How many squares the grid has. A count of **days**, never of
                    work: there is no count of work anywhere in this response,
                    because that would be the total the three bases must not
                    have.
                max_window_days:
                  type: integer
                  description: |
                    The longest window this operation will answer, counted
                    inclusively. Echoed so a client can bound its own date picker
                    instead of discovering the limit as a 422.
    OrderDeskRequirementRow:
      type: object
      required:
        - ingredient_id
        - stock_item_id
        - code
        - name_en
        - unit_id
        - unit_code
        - required
        - available
        - short
        - suggested_buy
      additionalProperties: false
      properties:
        ingredient_id:
          $ref: '#/components/schemas/Uuid'
        stock_item_id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          description: The shelf's own code. Rows are ordered by it, so the list reads the way a store cupboard is walked.
        name_en:
          type: string
          description: |
            The shelf's name. English only, because `stock_items` carries one
            name column — the same field the levels list serves as
            `item_name_en`, not a translation this operation withheld.
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The measurement unit every quantity on this row is expressed in. Null when the shelf has no resolved unit.
        unit_code:
          type:
            - string
            - 'null'
          description: That unit's code, for rendering. Null when there is no resolved unit — show the em dash rather than a bare number.
        required:
          type: string
          description: |
            How much the window needs, at **six** decimal places — the scale the
            explosion computes and the movement ledger stores. Published at six
            rather than rounded to the column's four, because truncating a
            computed figure to match what it is compared against would publish a
            rounder number than the arithmetic produced.
        available:
          type: string
          description: |
            What the branch holds, at four places. `0` when the shelf has no
            level row here — that is a shelf holding none, not an unknown.
        short:
          type: string
          description: |
            `max(0, required − available)`, at four places. **Compared at four**
            so a rounding tail five decimal places down cannot manufacture a
            shortfall on a row that balances exactly.
        suggested_buy:
          type: string
          description: |
            What to order. Where the level has a par: `par − (available −
            required)`, the quantity that leaves the shelf at par once the window
            has been cooked. Where it has none, or where that is not positive
            (a par written below the window's own demand), the bare shortfall —
            so a par nobody has maintained can never reduce a buy below what the
            kitchen needs.
    OrderDeskNotComputable:
      type: object
      required:
        - days
        - reasons
      additionalProperties: false
      description: |
        The part of the window nobody could turn into a number. **Never a zero
        in the list above** — "buy nothing for that" and "we could not work out
        what to buy for that" are opposite statements.
      properties:
        days:
          type: integer
          minimum: 0
          description: |
            Distinct **dates** carrying at least one hole. A date rather than a
            demand item: two orders and a subscription day all failing on one
            Tuesday is one day the buy list is incomplete for, which is the
            question the number answers.
        reasons:
          type: object
          description: |
            Hole counts by reason code, most days first. A count is **days that
            reason spoiled**, not occurrences: three ingredients of one dish all
            failing for the same reason on one day is one day, so an elaborate
            recipe does not look like a bigger problem than a simple one with the
            same consequence.

            Only reasons that actually occurred appear. The vocabulary is
            `plan_has_no_menu`, `menu_dish_withdrawn`, `meal_has_no_recipe`,
            `no_yield_piece_count`, `unquantified_recipe_line`,
            `no_ingredient_link`, `no_stock_item`, `no_stock_unit` and
            `unit_conversion_unsupported`; the map is left open so a code added
            later reaches a client as data rather than as a schema violation.
          additionalProperties:
            type: integer
            minimum: 1
    OrderDeskRequirementsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - requirements
            - not_computable
          properties:
            requirements:
              type: array
              description: One row per shelf the window needs anything of, ordered by stock-item code.
              items:
                $ref: '#/components/schemas/OrderDeskRequirementRow'
            not_computable:
              $ref: '#/components/schemas/OrderDeskNotComputable'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - from
                - to
                - branch_id
                - max_window_days
              properties:
                from:
                  type: string
                  format: date
                to:
                  type: string
                  format: date
                branch_id:
                  $ref: '#/components/schemas/Uuid'
                max_window_days:
                  type: integer
                  description: |
                    The longest window this operation will answer, counted
                    inclusively. Echoed so a client can bound its own date picker
                    instead of discovering the limit as a 422.
    OrderDeskShortfallCountEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - shortfall_count
            - branch_id
          additionalProperties: false
          properties:
            shortfall_count:
              type:
                - integer
                - 'null'
              minimum: 0
              description: |
                How many ingredients the next seven days are short of at this
                branch. **`null` when no `branch_id` was given** — render nothing
                at all for it, never a zero.
            branch_id:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
              description: The branch the count is about, echoed so the answer says what it is about. Null exactly when the count is.
        meta:
          $ref: '#/components/schemas/Meta'
    OrderDeskDriver:
      type: object
      required:
        - user_id
        - display_name
      additionalProperties: false
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type:
            - string
            - 'null'
          description: |
            The person's name, from their profile. **Null when they have no
            profile yet** — a member whose account was created but never
            completed is still a member, and leaving them out would make them
            unassignable. Render the em dash the platform uses for every other
            unknown rather than inventing a placeholder.
    OrderDeskDriversEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/OrderDeskDriver'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
                - limit
              properties:
                count:
                  type: integer
                  minimum: 0
                limit:
                  type: integer
                  description: |
                    The most rows this operation will ever return. There is no
                    second page: a kitchen with more active members than this has
                    an organisation chart rather than a rota.
    CreateOrderDeskCustomerRequest:
      type: object
      required:
        - display_name
        - phone
      additionalProperties: false
      properties:
        display_name:
          type: string
          maxLength: 255
          description: |
            What the caller says their name is. One field rather than a
            given/family pair, because the platform's customer may be a company.
        phone:
          type: string
          maxLength: 255
          description: |
            **Required, because a cold caller is a telephone number.** It is how
            the kitchen rings back about tonight's delivery and what the next
            agent searches on when the same person calls again; an account
            without one is a name nobody can act on. Further numbers are added
            afterwards through the customer's own contact operations.

            Already in E.164, or `422` on this field. The platform does not infer
            a country code from a local number — guessing would send somebody
            else's handset a passcode that unlocks an account — so the desk asks
            for it.
          examples:
            - '+96170123456'
        preferred_language_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
          description: A language the platform knows; unknown codes are `422` rather than a foreign-key failure.
        country_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
    AddOrderDeskCustomerAddressRequest:
      type: object
      required:
        - delivery_area_id
        - line_one
      additionalProperties: false
      description: |
        The customer's own address body minus `address_type`, `contact_point_id`
        and `is_default` — see the operation for why each is absent. Every length
        is the column's.

        `delivery_area_id` is a `uuid` and nothing more. Whether anybody
        delivers there is asked at save time and answered as
        `address.area_not_served`, which is the sentence the agent reads out to
        the customer; an `exists` rule would refuse an unknown identifier as
        "invalid" and still say nothing about coverage.
      properties:
        delivery_area_id:
          $ref: '#/components/schemas/Uuid'
        label:
          type:
            - string
            - 'null'
          maxLength: 60
        line_one:
          type: string
          maxLength: 255
        line_two:
          type:
            - string
            - 'null'
          maxLength: 255
        building:
          type:
            - string
            - 'null'
          maxLength: 120
        floor:
          type:
            - string
            - 'null'
          maxLength: 40
        apartment:
          type:
            - string
            - 'null'
          maxLength: 40
        directions:
          type:
            - string
            - 'null'
          maxLength: 1000
        postal_code:
          type:
            - string
            - 'null'
          maxLength: 20
    OrderDeskCashReportRow:
      type: object
      required:
        - confirmed_by
        - display_name
        - method
        - currency_code
        - receipt_count
        - amount_minor_sum
      additionalProperties: false
      description: |
        What one agent took, one way, in one currency, on one day.

        The row's identity is all three of `(confirmed_by, method, currency_code)`
        — an agent who took cash and a WISH transfer is two rows, and an agent who
        took dollars and dirhams is two more. That is not a normalisation
        accident: the currency belongs to the key because `amount_minor_sum` is
        only meaningful within one, and a shape that let a group span two would
        be a shape in which a nonsense number could be written.
      properties:
        confirmed_by:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type:
            - string
            - 'null'
          description: |
            **Null for an agent whose profile was never completed.** They still
            took the money, so they are still listed — dropping the row to avoid a
            null would be losing cash from a reconciliation to protect a
            formatting concern. Render the platform's em dash. Nameless agents
            sort last.
        method:
          $ref: '#/components/schemas/PaymentMethod'
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: ISO 4217, taken from the order the receipt was written against.
        receipt_count:
          type: integer
          minimum: 1
          description: How many separate receipts make up the sum beside it.
        amount_minor_sum:
          type: integer
          description: |
            The sum of those receipts, in minor units of `currency_code`. Legal
            because every receipt in the group is the same unit of the same
            currency — which is exactly what makes the currency part of the key.
    OrderDeskCashReportTotal:
      type: object
      required:
        - method
        - currency_code
        - receipt_count
        - amount_minor_sum
      additionalProperties: false
      description: |
        The same money one level up: one method, one currency, across every agent.

        Derived from the rows rather than queried separately, so a total can never
        disagree with the table above it. **There is no grand total anywhere in
        this response and there will not be** — it would have to add currencies,
        and the only reliable defence against that is a shape with nowhere to put
        it.
      properties:
        method:
          $ref: '#/components/schemas/PaymentMethod'
        currency_code:
          type: string
          minLength: 3
          maxLength: 3
        receipt_count:
          type: integer
          minimum: 1
        amount_minor_sum:
          type: integer
    OrderDeskCashReportEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - rows
            - totals
          additionalProperties: false
          properties:
            rows:
              type: array
              description: |
                Named agents first, nameless last, then a total order over the
                remaining key columns — so two identical requests cannot return
                the same figures in two orders. Empty on a day nobody took
                anything, which is a real answer rather than a gap.
              items:
                $ref: '#/components/schemas/OrderDeskCashReportRow'
            totals:
              type: array
              description: Ordered by first appearance in `rows`, and therefore as deterministic as they are.
              items:
                $ref: '#/components/schemas/OrderDeskCashReportTotal'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - date
                - branch_id
                - timezone
                - count
              properties:
                date:
                  type: string
                  format: date
                  description: The day reported, echoed because the boundary below is what it means.
                branch_id:
                  type:
                    - string
                    - 'null'
                  format: uuid
                  description: The site the report was narrowed to, or null for the whole organisation.
                timezone:
                  type: string
                  description: |
                    The clock the day was measured on. **Always `UTC`** — see the
                    operation. Echoed rather than assumed so a screen can say
                    which midnight it is showing instead of implying the reader's
                    own.
                count:
                  type: integer
                  minimum: 0
                  description: Rows in this response. Not a count of receipts.
    GuestSessionGrade:
      type: string
      enum:
        - checkout_draft
        - place_order
      description: |
        How much a guest token is allowed to do — the entire authorisation model
        for the guest journey, in two values. `checkout_draft` is what an
        anonymous browser is handed on its first request: enough to build a
        basket, price it and choose a slot, none of which creates an obligation
        to anybody. `place_order` is what that becomes once a passcode has
        proven a contact point.

        The grade is raised only by an act the server witnessed, and the
        database refuses the higher value without a `contact_verified_at`.
    GuestContactChannel:
      type: string
      enum:
        - email
        - phone
      description: |
        What kind of destination a contact point names — deliberately not the
        transport. An address has one way to reach it; a number has three, and
        which is used is a property of the message.
    GuestOtpDeliveryChannel:
      type: string
      enum:
        - email
        - sms
        - whatsapp
      description: |
        How a passcode travels. Optional on every request that accepts it: the
        server knows which drivers are real in this deployment and falls back
        when one is not, so a client that insisted would get a refusal where the
        server would have found a route.
    GuestCustomerAccount:
      type: object
      description: |
        The guest's own customer account. A guest **is** a `customer_accounts`
        row in one of the three shapes that table has always enforced — not a
        new kind of thing — which is why addresses, orders and dietary
        declarations all work on one without knowing a guest exists.

        `account_number` is absent: it is confidential, no guest screen needs
        it, and the reference somebody quotes on the telephone is the order
        number.
      required:
        - id
        - account_type
        - status
        - origin
        - preferred_language_code
        - country_code
        - guest_expires_at
        - converted_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        account_type:
          type: string
          enum:
            - b2c
            - b2b
            - guest
          description: '`guest` until conversion, `b2c` after it.'
        status:
          type: string
          enum:
            - provisional
            - active
            - suspended
            - closed
        origin:
          type: string
          enum:
            - self_service
            - guest
            - b2b_provisioning
            - staff
            - import
          description: |
            Stays `guest` after conversion. Origin is history: an account that
            arrived through the guest door came from there permanently.
        preferred_language_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        country_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        guest_expires_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            When the guest identity itself lapses and its data is purged. Null
            once the account has converted — an account with a user is no longer
            on a retention clock.
        converted_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    GuestSession:
      type: object
      description: |
        A guest session's own state. **There is no `token` field and there
        cannot be one**: the plaintext exists only in the response to
        `POST /guest/sessions`, and the digest is a credential derivative that is
        never served. The IP and User-Agent fingerprints are absent too — they
        exist for abuse investigation, and echoing a person's own pseudonymised
        fingerprint back to them helps nobody but whoever gets hold of the
        response.
      required:
        - id
        - grade
        - contact_verified
        - contact_verified_at
        - expires_at
        - last_used_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        grade:
          $ref: '#/components/schemas/GuestSessionGrade'
        contact_verified:
          type: boolean
          description: |
            What a checkout screen branches on. Carried beside the timestamp
            rather than derived from it, so the schema's own CHECK constraint
            does not have to be reimplemented in JavaScript.
        contact_verified_at:
          type:
            - string
            - 'null'
          format: date-time
        expires_at:
          type: string
          format: date-time
          description: |
            Absolute, never a countdown. A "seconds remaining" computed on the
            server is stale the moment it is serialised.
        last_used_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    StartedGuestSession:
      type: object
      description: The one payload in the API that carries a guest credential.
      required:
        - token
        - grade
        - expires_at
        - account_expires_at
        - customer_account
      properties:
        token:
          type: string
          description: |
            The plaintext bearer token, sent as `X-Guest-Token` on every
            subsequent guest request. **Returned exactly once and stored
            nowhere** — the row keeps a digest. It must never be logged, and a
            client that loses it has lost the basket.
        grade:
          $ref: '#/components/schemas/GuestSessionGrade'
        expires_at:
          type: string
          format: date-time
          description: When this token stops being accepted.
        account_expires_at:
          type: string
          format: date-time
          description: |
            When the guest identity behind the token lapses. The longer of the
            two windows, so a client whose token expired can be told honestly
            that the basket is gone rather than that the person never existed.
        customer_account:
          $ref: '#/components/schemas/GuestCustomerAccount'
      additionalProperties: false
    GuestSessionEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - session
            - customer_account
          properties:
            session:
              $ref: '#/components/schemas/GuestSession'
            customer_account:
              $ref: '#/components/schemas/GuestCustomerAccount'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/Meta'
    StartGuestSessionRequest:
      type: object
      description: |
        Every field is optional and the empty body is the normal case. A
        required field here would mean a person cannot open a basket until they
        have answered a question, which is the friction the guest journey exists
        to remove. Both values are presentation hints, not references: an
        unrecognised code degrades to the platform default rather than refusing
        the session.
      properties:
        preferred_language_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        country_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
      additionalProperties: false
    RequestGuestContactVerificationRequest:
      type: object
      required:
        - channel
        - value
      properties:
        channel:
          $ref: '#/components/schemas/GuestContactChannel'
        value:
          type: string
          maxLength: 255
          description: |
            The address or number itself. Carries no format rule beyond a length
            guard: what counts as a valid destination is the normaliser's single
            judgement, and a second opinion in the request layer would refuse
            values the store accepts.
        delivery_channel:
          $ref: '#/components/schemas/GuestOtpDeliveryChannel'
      additionalProperties: false
    ConfirmGuestContactVerificationRequest:
      type: object
      required:
        - challenge_id
        - code
      properties:
        challenge_id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 16
          description: |
            A string, never an integer — a passcode with a leading zero is a
            passcode, and JSON numbers eat leading zeros. No length rule: how
            long a code is belongs to the verification configuration, and a
            client-facing rule restating it would start refusing valid codes the
            day an operator lengthened them.
      additionalProperties: false
    GuestOtpChallenge:
      type: object
      description: |
        A passcode in flight. Every field exists because a screen cannot be
        built without it: the resend countdown is absolute so a client with a
        wrong clock still gets it right, `attempts_remaining` is the difference
        between retyping carefully and being locked out without warning, and
        `destination_masked` is server-authored because a client-side mask of a
        value the client was given would mean the value was given.
      required:
        - challenge_id
        - purpose
        - channel
        - destination_masked
        - expires_at
        - resend_available_at
        - resend_cooldown_seconds
        - attempts_remaining
        - resends_remaining
        - available_channels
        - simulated
      properties:
        challenge_id:
          $ref: '#/components/schemas/Uuid'
        purpose:
          type: string
          const: guest_order
        channel:
          $ref: '#/components/schemas/GuestOtpDeliveryChannel'
        destination_masked:
          type: string
          examples:
            - n••••••••••@example.test
        expires_at:
          type: string
          format: date-time
        resend_available_at:
          type: string
          format: date-time
        resend_cooldown_seconds:
          type: integer
          minimum: 0
        attempts_remaining:
          type: integer
          minimum: 0
        resends_remaining:
          type: integer
          minimum: 0
        available_channels:
          type: array
          description: What "try another way" may offer in this deployment.
          items:
            type: object
            required:
              - channel
              - simulated
            properties:
              channel:
                $ref: '#/components/schemas/GuestOtpDeliveryChannel'
              simulated:
                type: boolean
            additionalProperties: false
        simulated:
          type: boolean
          description: |
            Whether this challenge was delivered by a driver that does not
            really send anything. Never true outside local and testing.
        debug_code:
          type: string
          description: |
            Present only when the deployment exposes codes *and* the environment
            is local or testing, so the developer affordance for channels that
            do not really deliver cannot exist in production.
      additionalProperties: false
    GuestOtpChallengeEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - challenge
          properties:
            challenge:
              $ref: '#/components/schemas/GuestOtpChallenge'
          additionalProperties: false
        meta:
          $ref: '#/components/schemas/Meta'
    PlaceGuestOrderRequest:
      type: object
      description: |
        The same body as the authenticated `POST /orders`. Nothing about money
        and nothing about the seller: the currency, the totals, the organisation
        and the channel are all decided at placement from the cart and the
        tariff standing at that moment. A body carrying a total would make the
        client the authority on what the kitchen charges.
      required:
        - cart_id
        - customer_address_id
      properties:
        cart_id:
          $ref: '#/components/schemas/Uuid'
        customer_address_id:
          $ref: '#/components/schemas/Uuid'
        delivery_window_code:
          type:
            - string
            - 'null'
          maxLength: 40
          description: The chosen slot, as the delivery window's own code.
        requested_delivery_date:
          type:
            - string
            - 'null'
          format: date
          description: |
            A plain date, never a datetime. The order stores a day; accepting an
            instant would invite a timezone argument nobody can win about which
            day `2026-08-02T23:30:00Z` is.
      additionalProperties: false
    GuestOrderStatus:
      type: string
      enum:
        - placed
        - confirmed
        - fulfilled
        - cancelled
    GuestOrderDelivery:
      type: object
      description: |
        Where the food goes, as it stood when the order was placed. The area
        name carries one server-chosen language; the free-text parts are the
        customer's own address, copied onto the order so a later edit to the
        address book cannot rewrite where a delivered order went.
      required:
        - label
        - line_one
        - line_two
        - city
        - area
        - window_code
        - requested_date
      properties:
        label:
          type:
            - string
            - 'null'
        line_one:
          type: string
        line_two:
          type:
            - string
            - 'null'
        city:
          type:
            - string
            - 'null'
        area:
          type:
            - string
            - 'null'
        window_code:
          type:
            - string
            - 'null'
        requested_date:
          type:
            - string
            - 'null'
          format: date
      additionalProperties: false
    GuestOrderLineAllergen:
      type: object
      description: |
        The frozen allergen statement, exactly as the customer was shown it.
        Never re-derived from the catalogue on read — a person told "contains
        sesame" must still be told it after the kitchen reformulates.
      required:
        - allergen_code
        - containment
      properties:
        allergen_code:
          $ref: '#/components/schemas/AllergenCode'
        containment:
          type: string
      additionalProperties: false
    GuestOrderLine:
      type: object
      description: |
        One article on the order, as it stood the moment the order was placed.
        `name` is **one** server-chosen language rather than a pair of columns
        (master plan v2 §4.8), and the price-list identifiers behind the amount
        are absent: they exist so "why was this the price?" is answerable a year
        later and they never leave the server.
      required:
        - id
        - catalogue_item_id
        - name
        - variant_label
        - quantity
        - unit_price_minor
        - line_total_minor
        - currency_code
        - allergens
        - pack_summary
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        variant_label:
          type:
            - string
            - 'null'
        quantity:
          type: string
          description: |
            A decimal string, not a number. `2.0000` survives a round trip
            through JSON and back into PostgreSQL's numeric; `2.0` does not
            reliably survive anything.
          examples:
            - '2.0000'
        unit_price_minor:
          type: integer
          minimum: 0
        line_total_minor:
          type: integer
          minimum: 0
        currency_code:
          $ref: '#/components/schemas/CurrencyCode'
        allergens:
          type: array
          items:
            $ref: '#/components/schemas/GuestOrderLineAllergen'
        pack_summary:
          type:
            - object
            - 'null'
          additionalProperties: true
      additionalProperties: false
    GuestOrder:
      type: object
      description: |
        A guest's own order. Amounts are integers of minor units with the
        currency beside them and are never formatted server-side — `4500` with
        `AED`, never `"45.00 AED"` — because a server that formats money has
        decided a locale on the client's behalf and made the number unusable for
        arithmetic.

        `lock_version` is absent, and so is an `ETag`: a guest has no write
        surface on an order, so a validator would be a token for a request
        nobody can make. The seller's identifiers are absent too — naming the
        kitchen properly means a projection, and that shape already exists on
        the marketplace surface.
      required:
        - id
        - order_number
        - status
        - currency_code
        - subtotal_minor
        - delivery_fee_minor
        - total_minor
        - payment_method
        - delivery
        - placed_at
        - confirmed_at
        - cancelled_at
        - cancellation_reason
        - lines
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_number:
          type: string
          description: The reference a person quotes on the telephone.
        status:
          $ref: '#/components/schemas/GuestOrderStatus'
        currency_code:
          $ref: '#/components/schemas/CurrencyCode'
        subtotal_minor:
          type: integer
          minimum: 0
        delivery_fee_minor:
          type:
            - integer
            - 'null'
          minimum: 0
          description: |
            Null is a value: nobody decided a fee for this zone. Never
            flattened to `0`, which would say delivery is free.
        total_minor:
          type: integer
          minimum: 0
        payment_method:
          type: string
          enum:
            - cash_on_delivery
          description: |
            One case **on this surface**, narrower than the platform's
            `PaymentMethod` vocabulary on purpose. A guest checkout takes cash at
            the door and offers nothing else: the desk's two methods need
            somebody at a desk, and neither is reachable by a customer placing
            their own order. Serving the wider enum here would promise a value
            this endpoint cannot produce.
        delivery:
          $ref: '#/components/schemas/GuestOrderDelivery'
        placed_at:
          type: string
          format: date-time
        confirmed_at:
          type:
            - string
            - 'null'
          format: date-time
        cancelled_at:
          type:
            - string
            - 'null'
          format: date-time
        cancellation_reason:
          type:
            - string
            - 'null'
          enum:
            - customer_requested
            - kitchen_unable_to_fulfil
            - delivery_unavailable
            - address_unreachable
            - null
        lines:
          type: array
          items:
            $ref: '#/components/schemas/GuestOrderLine'
      additionalProperties: false
    GuestOrderEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - order
          properties:
            order:
              $ref: '#/components/schemas/GuestOrder'
          additionalProperties: false
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                replayed:
                  type: boolean
                  description: |
                    True when this envelope is an idempotent replay of a
                    placement that already happened. Present on
                    `POST /guest/orders` only.
    GuestConversionEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - customer_account
          properties:
            customer_account:
              $ref: '#/components/schemas/GuestCustomerAccount'
          additionalProperties: false
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - guest_token_revoked
              properties:
                guest_token_revoked:
                  type: boolean
                  const: true
                  description: |
                    The `X-Guest-Token` used to reach this endpoint stopped
                    working with this response. Stated in the body so a client
                    does not discover it as a `401` on its next request.
    RequestGuestDeletionRequest:
      type: object
      required:
        - channel
        - value
      properties:
        channel:
          $ref: '#/components/schemas/GuestContactChannel'
        value:
          type: string
          maxLength: 255
        delivery_channel:
          $ref: '#/components/schemas/GuestOtpDeliveryChannel'
      additionalProperties: false
    ConfirmGuestDeletionRequest:
      type: object
      description: |
        The address is submitted again rather than carried in a challenge
        identifier from step one, because step one has none to give: a nullable
        identifier is a boolean in disguise, and the boolean is "this address is
        known to us".
      required:
        - channel
        - value
        - code
      properties:
        channel:
          $ref: '#/components/schemas/GuestContactChannel'
        value:
          type: string
          maxLength: 255
        code:
          type: string
          maxLength: 16
      additionalProperties: false
    GuestDeletionAcknowledgement:
      type: object
      description: |
        The same answer whether or not there is anything to delete. Every field
        is derived from the request: the mask comes from the value just
        submitted, and the two remaining fields come from configuration.
      required:
        - accepted
        - destination_masked
        - verification_required
        - expires_in_seconds
      properties:
        accepted:
          type: boolean
          const: true
        destination_masked:
          type: string
          description: |
            Masked from the value the caller just submitted, so the echo needs
            no lookup and therefore cannot depend on one.
          examples:
            - n••••••••••@example.test
        verification_required:
          type: boolean
        expires_in_seconds:
          type: integer
          minimum: 30
      additionalProperties: false
    GuestPurgeReport:
      type: object
      description: |
        What the purge did — counts only, never identities. An erasure that
        recorded what it erased would have rebuilt in one place exactly what it
        removed from another.
      required:
        - contacts_deleted
        - addresses_deleted
        - challenges_deleted
        - sessions_deleted
        - suppressions_written
        - account_anonymised
        - orders_retained
      properties:
        contacts_deleted:
          type: integer
          minimum: 0
        addresses_deleted:
          type: integer
          minimum: 0
        challenges_deleted:
          type: integer
          minimum: 0
        sessions_deleted:
          type: integer
          minimum: 0
        suppressions_written:
          type: integer
          minimum: 0
          description: |
            The one thing an erasure is allowed to leave behind: a hash of each
            destination, so the next import does not silently re-add the person
            who asked to be forgotten.
        account_anonymised:
          type: boolean
        orders_retained:
          type: integer
          minimum: 0
          description: |
            How many order records this purge deliberately left alone. A guest
            order is a commercial and tax record with a statutory retention of
            its own; saying so is what stops "purged" being read as "everything
            is gone".
      additionalProperties: false
    GuestDeletionOutcome:
      type: object
      description: |
        One indistinguishable shape for every refusal — a wrong code, any code
        against an unknown address, an expired challenge, a locked-out contact
        — and the report only for a caller who has proven they hold the
        destination.
      required:
        - accepted
        - purged
      properties:
        accepted:
          type: boolean
          const: true
        purged:
          type: boolean
        report:
          $ref: '#/components/schemas/GuestPurgeReport'
      additionalProperties: false
    OtpPurpose:
      type: string
      enum:
        - contact_verification
        - guest_order
        - guest_deletion
        - closure_step_up
        - payment_details_step_up
        - b2b_signatory
      description: |
        What a passcode is being asked for. The vocabulary is complete rather
        than grown per phase, because a purpose scopes the one-live-challenge
        index and is what a step-up consumer checks before honouring a
        verification — a code obtained for a harmless purpose must not be
        replayable against a dangerous one.
    StepUpPurpose:
      type: string
      enum:
        - closure_step_up
        - payment_details_step_up
      description: |
        The purposes that grant an `otp` step-up. Contact verification is
        deliberately absent: a code proving somebody holds an address must never
        unlock a payment change.
    OtpDeliveryChannel:
      type: string
      enum:
        - email
        - sms
        - whatsapp
      description: |
        How a passcode travels. Distinct from a contact point's `channel`: an
        email address has one way to reach it, a phone number has three, and
        which is used is a property of the message rather than of the number.
    OtpChallengeState:
      type: string
      enum:
        - pending
        - verified
        - expired
        - failed
        - superseded
      description: |
        The stored lifecycle of a challenge. Not sufficient on its own — expiry
        is a moment rather than an event, so a row stays `pending` past its
        window until something writes the transition. Read `is_live` instead.
    OtpAvailableChannel:
      type: object
      required:
        - channel
        - simulated
      properties:
        channel:
          $ref: '#/components/schemas/OtpDeliveryChannel'
        simulated:
          type: boolean
          description: |
            The driver writes to a log rather than delivering. Never true
            outside local and testing, so a production client can offer every
            listed channel without checking.
    OtpChallengeResult:
      type: object
      required:
        - challenge_id
        - purpose
        - channel
        - destination_masked
        - expires_at
        - resend_available_at
        - resend_cooldown_seconds
        - attempts_remaining
        - resends_remaining
        - available_channels
        - simulated
      properties:
        challenge_id:
          $ref: '#/components/schemas/Uuid'
        purpose:
          $ref: '#/components/schemas/OtpPurpose'
        channel:
          $ref: '#/components/schemas/OtpDeliveryChannel'
        destination_masked:
          type: string
          description: |
            **Server-authored.** The client never sees the address, so it cannot
            mask it — and a client-side mask of a value the client was given
            would mean the value was given.
          examples:
            - a•••@example.com
        expires_at:
          type: string
          format: date-time
        resend_available_at:
          type: string
          format: date-time
          description: |
            When the resend button unlocks. Sent as an instant rather than left
            to the client to compute, because a client using its own clock gets
            it wrong for exactly the users whose clocks are wrong.
        resend_cooldown_seconds:
          type: integer
          minimum: 0
        attempts_remaining:
          type: integer
          minimum: 0
          description: |
            "2 tries left" is the difference between a person retyping carefully
            and a person locked out without warning.
        resends_remaining:
          type: integer
          minimum: 0
        available_channels:
          type: array
          description: What "try another way" may offer.
          items:
            $ref: '#/components/schemas/OtpAvailableChannel'
        simulated:
          type: boolean
        debug_code:
          type: string
          description: |
            The plaintext passcode. Present **only** when
            `verification.otp.expose_codes` is set *and* the environment is
            local or testing, so the developer affordance for channels that do
            not really deliver cannot exist in production. Never present on a
            deployed environment.
      description: |
        What a caller learns when a challenge is issued or resent. Every field
        exists because a screen cannot be built without it.
    OtpChallengeEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - challenge
          properties:
            challenge:
              $ref: '#/components/schemas/OtpChallengeResult'
        meta:
          $ref: '#/components/schemas/Meta'
    VerificationChallenge:
      type: object
      required:
        - challenge_id
        - purpose
        - channel
        - destination_masked
        - status
        - is_live
        - attempts_remaining
        - resends_remaining
        - expires_at
      properties:
        challenge_id:
          $ref: '#/components/schemas/Uuid'
        purpose:
          $ref: '#/components/schemas/OtpPurpose'
        channel:
          $ref: '#/components/schemas/OtpDeliveryChannel'
        destination_masked:
          type: string
          examples:
            - a•••@example.com
        status:
          $ref: '#/components/schemas/OtpChallengeState'
        is_live:
          type: boolean
          description: |
            Still `pending` **and** still inside its window. The field a client
            branches on; `status` alone would offer an attempt that cannot
            succeed.
        attempts_remaining:
          type: integer
          minimum: 0
        resends_remaining:
          type: integer
          minimum: 0
        expires_at:
          type: string
          format: date-time
        resend_available_at:
          type:
            - string
            - 'null'
          format: date-time
        last_sent_at:
          type:
            - string
            - 'null'
          format: date-time
        verified_at:
          type:
            - string
            - 'null'
          format: date-time
      description: |
        A challenge's state. Carries no passcode and no derivative of one — every
        field is drawn from columns that outlive the request.
    VerificationChallengeEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - challenge
          properties:
            challenge:
              $ref: '#/components/schemas/VerificationChallenge'
        meta:
          $ref: '#/components/schemas/Meta'
    VerifiedContact:
      type: object
      required:
        - id
        - channel
        - destination_masked
        - is_primary
        - is_login_identity
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        channel:
          $ref: '#/components/schemas/ContactChannel'
        destination_masked:
          type: string
          description: |
            Taken from the challenge rather than recomputed: the challenge
            recorded what the person was shown when the code was sent, and
            answering with anything else would tell them a code went somewhere
            it did not.
          examples:
            - a•••@example.com
        is_primary:
          type: boolean
        is_login_identity:
          type: boolean
        verified_at:
          type:
            - string
            - 'null'
          format: date-time
    ContactVerifiedEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - verified
            - contact
          properties:
            verified:
              type: boolean
              const: true
              description: |
                Always `true` when this envelope is served. Every other outcome
                is an error with its own `otp.*` code, because the remedies
                differ and a boolean cannot carry them.
            contact:
              $ref: '#/components/schemas/VerifiedContact'
        meta:
          $ref: '#/components/schemas/Meta'
    StepUpConfirmationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - confirmed
            - method
            - expires_in_seconds
          properties:
            confirmed:
              type: boolean
              const: true
            method:
              type: string
              const: otp
              description: |
                Never `password`. The two methods are tracked separately and
                neither satisfies the other — a shared flag would mean a
                password confirmation silently unlocking a passcode-gated
                action, defeating the only reason to ask for a passcode.
            expires_in_seconds:
              type: integer
              minimum: 1
              description: |
                Much shorter than a password confirmation's, because the
                assurance is about the present moment rather than a secret the
                person knows: a passcode confirmed at breakfast says nothing
                about who is at the keyboard after lunch.
        meta:
          $ref: '#/components/schemas/Meta'
    SubmitPasscodeRequest:
      type: object
      required:
        - code
      properties:
        code:
          type: string
          pattern: ^[0-9]{4,10}$
          description: |
            A **string** of digits, never a number. Leading zeros are half the
            reason the generator pads them: `012345` cast to an integer becomes
            a different code, and throwing a tenth of a million-wide keyspace
            away at the parsing layer is not a rounding error.
          examples:
            - '012345'
    ResendChallengeRequest:
      type: object
      properties:
        channel:
          $ref: '#/components/schemas/OtpDeliveryChannel'
      description: |
        `channel` is optional and its absence means "the same way as last time"
        rather than "the default for this contact" — somebody who chose WhatsApp
        once should not be quietly moved back to SMS by pressing resend.
    StepUpChallengeRequest:
      type: object
      required:
        - purpose
      properties:
        purpose:
          $ref: '#/components/schemas/StepUpPurpose'
      description: |
        No `channel`. A step-up goes to the destination the account has already
        proven, and letting the caller pick the transport for a challenge that
        unlocks account closure would hand an attacker the one decision the
        server should be making.
    ConfirmStepUpRequest:
      type: object
      required:
        - challenge_id
        - code
      properties:
        challenge_id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          pattern: ^[0-9]{4,10}$
          examples:
            - '012345'
      description: |
        The challenge is named in the body rather than in the path, unlike every
        other verification endpoint: a client that has just been refused with
        `auth.step_up_required` holds an identifier and a code, not a resource it
        is navigating to.
    ContactChannel:
      type: string
      enum:
        - email
        - phone
      description: |
        The kind of destination a contact point names — deliberately not the
        transport. Modelling it as `sms | whatsapp | email` would mean storing
        the same number twice and asking a customer to verify it twice.
    CustomerAccountStatus:
      type: string
      enum:
        - provisional
        - active
        - suspended
        - closed
      description: |
        `provisional` is a real state rather than a nicer word for incomplete:
        an account in it exists, holds addresses and declarations, and cannot
        order — which is what lets onboarding be interrupted and resumed.
        `active` is the activation evaluator's verdict made durable; nothing
        else writes it. Closure is terminal.
    CustomerAccountChecklistItem:
      type: object
      required:
        - code
        - satisfied
        - required
      properties:
        code:
          type: string
          enum:
            - account.email_unverified
            - account.phone_unverified
            - account.no_served_address
            - account.dietary_declaration_missing
            - account.consents_outstanding
        satisfied:
          type: boolean
        required:
          type: boolean
          description: |
            `account.phone_unverified` is **not** required by default. Demanding
            a proven phone while the SMS driver writes to a log file would lock
            every real customer out of their own account, so the requirement is
            configuration plus the existence of a channel that really delivers.
      description: |
        One requirement, met or not, in a stable order. Distinct from
        `outstanding` because a checklist showing only what is missing loses the
        sense of progress that makes people finish it — and because "phone
        verification is not required here" is itself something the screen has to
        be able to say.
    CustomerAccountOutstandingReason:
      type: object
      required:
        - code
        - context
      properties:
        code:
          type: string
          examples:
            - account.no_served_address
        context:
          type: object
          additionalProperties: true
          description: |
            Structured detail the screen needs. `account.no_served_address`
            carries `has_address`, because "add an address" and "we do not
            deliver there yet" need different words.
          examples:
            - has_address: true
    CustomerAccount:
      type: object
      required:
        - id
        - account_number
        - account_type
        - status
        - origin
        - is_ready
        - outstanding
        - checklist
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        account_number:
          type: string
          description: |
            `Confidential` on the record and served anyway, because this shape
            reaches only the person the number belongs to — it is what they
            quote to support.
        account_type:
          type: string
          enum:
            - b2c
            - b2b
            - guest
        status:
          $ref: '#/components/schemas/CustomerAccountStatus'
        origin:
          type: string
          enum:
            - self_service
            - guest
            - b2b_provisioning
            - staff
            - import
        display_name:
          type:
            - string
            - 'null'
        preferred_language_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        country_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        is_ready:
          type: boolean
          description: |
            Nothing is outstanding. Not the same as `status == active`: an
            account can be ready and still provisional for as long as it takes
            the activation attempt to run.
        outstanding:
          type: array
          items:
            $ref: '#/components/schemas/CustomerAccountOutstandingReason'
        checklist:
          type: array
          items:
            $ref: '#/components/schemas/CustomerAccountChecklistItem'
        activated_at:
          type:
            - string
            - 'null'
          format: date-time
        provisional_expires_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            Stamped at creation and never recomputed — an account opened under a
            thirty-day window must not silently acquire a shorter one when the
            setting changes. Coming back resets nothing but does protect the
            account from the abandonment purge.
        last_activity_at:
          type:
            - string
            - 'null'
          format: date-time
        lock_version:
          type: integer
          minimum: 0
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
    CustomerAccountEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - account
          properties:
            account:
              $ref: '#/components/schemas/CustomerAccount'
        meta:
          $ref: '#/components/schemas/Meta'
    CustomerContact:
      type: object
      required:
        - id
        - channel
        - value
        - is_login_identity
        - is_primary
        - is_verified
        - source
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        channel:
          $ref: '#/components/schemas/ContactChannel'
        value:
          type: string
          description: |
            The canonical form — lowercased for an address, E.164 for a number —
            served **in the clear**, because the audience is the person who
            typed it and a masked address book is one on which nobody can tell
            which number is the old one.
        label:
          type:
            - string
            - 'null'
        is_login_identity:
          type: boolean
          description: |
            The notification mirror of `users.email`. Cannot be retired: an
            account whose only route back in has been withdrawn is one nobody
            can recover.
        is_primary:
          type: boolean
        is_verified:
          type: boolean
        verified_at:
          type:
            - string
            - 'null'
          format: date-time
        source:
          type: string
          examples:
            - self_service
            - registration
        created_at:
          type:
            - string
            - 'null'
          format: date-time
    CustomerContactEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - contact
          properties:
            contact:
              $ref: '#/components/schemas/CustomerContact'
        meta:
          $ref: '#/components/schemas/Meta'
    CustomerContactsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CustomerContact'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
              properties:
                count:
                  type: integer
                  minimum: 0
    AddCustomerContactRequest:
      type: object
      required:
        - channel
        - value
      properties:
        channel:
          $ref: '#/components/schemas/ContactChannel'
        value:
          type: string
          maxLength: 255
          description: |
            An email address, or a phone number **already in E.164**. The
            platform does not infer a country code: guessing one would send
            somebody else's handset a passcode that unlocks this account.
          examples:
            - '+96170123456'
        label:
          type:
            - string
            - 'null'
          maxLength: 60
        is_primary:
          type:
            - boolean
            - 'null'
      description: |
        `is_login_identity` and `verified_at` are absent by construction. The
        login mirror is written once, at registration; and a contact one may
        declare proven is a contact nobody has proven.
    CustomerAddressType:
      type: string
      enum:
        - delivery
        - billing
      description: |
        Only `delivery` is checked against the served areas. An invoice goes
        wherever the customer says.
    CustomerAddress:
      type: object
      required:
        - id
        - address_type
        - delivery_area_id
        - line_one
        - is_default
        - is_deliverable
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        address_type:
          $ref: '#/components/schemas/CustomerAddressType'
        delivery_area_id:
          $ref: '#/components/schemas/Uuid'
        label:
          type:
            - string
            - 'null'
        line_one:
          type: string
        line_two:
          type:
            - string
            - 'null'
        building:
          type:
            - string
            - 'null'
        floor:
          type:
            - string
            - 'null'
        apartment:
          type:
            - string
            - 'null'
        directions:
          type:
            - string
            - 'null'
        postal_code:
          type:
            - string
            - 'null'
        contact_point_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: The number a driver calls, when it is not the primary one.
        is_default:
          type: boolean
        is_deliverable:
          type: boolean
          description: |
            Whether anybody serves this area **today**, recomputed on every read.
            Not the same as "was accepted": a zone can be paused after an address
            was saved, and a book that showed only what was true at save time
            would let somebody choose an address nothing can be sent to.
        lock_version:
          type: integer
          minimum: 0
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
    CustomerAddressEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - address
          properties:
            address:
              $ref: '#/components/schemas/CustomerAddress'
        meta:
          $ref: '#/components/schemas/Meta'
    CustomerAddressesEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CustomerAddress'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
              properties:
                count:
                  type: integer
                  minimum: 0
    AddCustomerAddressRequest:
      type: object
      required:
        - address_type
        - delivery_area_id
        - line_one
      properties:
        address_type:
          $ref: '#/components/schemas/CustomerAddressType'
        delivery_area_id:
          $ref: '#/components/schemas/Uuid'
        label:
          type:
            - string
            - 'null'
          maxLength: 60
        line_one:
          type: string
          maxLength: 255
        line_two:
          type:
            - string
            - 'null'
          maxLength: 255
        building:
          type:
            - string
            - 'null'
          maxLength: 120
        floor:
          type:
            - string
            - 'null'
          maxLength: 40
        apartment:
          type:
            - string
            - 'null'
          maxLength: 40
        directions:
          type:
            - string
            - 'null'
          maxLength: 1000
        postal_code:
          type:
            - string
            - 'null'
          maxLength: 20
        contact_point_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        is_default:
          type:
            - boolean
            - 'null'
          description: |
            The first address of a type becomes the default whether or not this
            is set. An address book with no default makes the checkout ask a
            question that has one possible answer.
    UpdateCustomerAddressRequest:
      type: object
      properties:
        delivery_area_id:
          $ref: '#/components/schemas/Uuid'
        label:
          type:
            - string
            - 'null'
          maxLength: 60
        line_one:
          type: string
          maxLength: 255
        line_two:
          type:
            - string
            - 'null'
          maxLength: 255
        building:
          type:
            - string
            - 'null'
          maxLength: 120
        floor:
          type:
            - string
            - 'null'
          maxLength: 40
        apartment:
          type:
            - string
            - 'null'
          maxLength: 40
        directions:
          type:
            - string
            - 'null'
          maxLength: 1000
        postal_code:
          type:
            - string
            - 'null'
          maxLength: 20
        contact_point_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
      description: |
        `address_type` and `is_default` are absent, and both would be quietly
        wrong rather than merely unsupported — see the operation description.
    AllergenSeverity:
      type: string
      enum:
        - avoidance
        - intolerance
        - allergy
        - anaphylaxis
      description: |
        How badly this person reacts. `avoidance` is a preference;
        `anaphylaxis` excludes traces, and a kitchen reads the difference.
    FoodExclusionKind:
      type: string
      enum:
        - dislike
        - forbidden
      description: |
        `forbidden` is frequently a belief rather than a preference, which is
        why an exclusion is handled as carefully as an allergy even though
        nobody's airway depends on it.
    CustomerAllergenDeclaration:
      type: object
      required:
        - allergen_code
        - severity
        - declared_at
      properties:
        allergen_code:
          type: string
          examples:
            - sesame
        severity:
          $ref: '#/components/schemas/AllergenSeverity'
        notes:
          type:
            - string
            - 'null'
        declared_at:
          type: string
          format: date-time
      description: |
        Carries no identifier of its own. The set is replaced whole on every
        write, so a row id would be a handle onto something that does not
        survive the next `PUT`; the allergen code is the identity that matters.
    CustomerFoodExclusion:
      type: object
      required:
        - id
        - kind
        - subject_kind
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kind:
          $ref: '#/components/schemas/FoodExclusionKind'
        subject_kind:
          type: string
          enum:
            - ingredient
            - diet_classification
            - free_text
          description: |
            Which of the three columns is the one to read, so a client can render
            an exclusion without inspecting all three and guessing.
        ingredient_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        diet_classification_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        free_text:
          type:
            - string
            - 'null'
    CustomerDietaryProfile:
      type: object
      required:
        - has_declared
        - declares_no_allergens
        - allergens
        - exclusions
      properties:
        id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Null when nothing has ever been written.
        has_declared:
          type: boolean
          description: |
            **The field to branch on.** An unanswered question and a confident
            "I have none" are different facts, and only the second may activate
            an account that is about to be sent food.
        declared_at:
          type:
            - string
            - 'null'
          format: date-time
        declares_no_allergens:
          type: boolean
        diet_classification_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        religious_requirement:
          type:
            - string
            - 'null'
        notes:
          type:
            - string
            - 'null'
        allergens:
          type: array
          items:
            $ref: '#/components/schemas/CustomerAllergenDeclaration'
        exclusions:
          type: array
          items:
            $ref: '#/components/schemas/CustomerFoodExclusion'
        lock_version:
          type:
            - integer
            - 'null'
          minimum: 0
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
    CustomerDietaryProfileEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - dietary_profile
          properties:
            dietary_profile:
              $ref: '#/components/schemas/CustomerDietaryProfile'
        meta:
          $ref: '#/components/schemas/Meta'
    ReplaceDietaryProfileRequest:
      type: object
      required:
        - allergens
      properties:
        allergens:
          type: array
          maxItems: 60
          description: |
            **Required even when empty.** An omitted key and an empty array would
            otherwise mean the same thing on the wire and different things to the
            person.
          items:
            type: object
            required:
              - allergen_code
            properties:
              allergen_code:
                type: string
                maxLength: 60
              severity:
                $ref: '#/components/schemas/AllergenSeverity'
              notes:
                type:
                  - string
                  - 'null'
                maxLength: 500
        exclusions:
          type: array
          maxItems: 100
          items:
            type: object
            description: |
              Must name **exactly one** of `ingredient_id`,
              `diet_classification_id` or `free_text`. Naming two or none is
              `validation.failed` on the offending index.
            properties:
              kind:
                $ref: '#/components/schemas/FoodExclusionKind'
              ingredient_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              diet_classification_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              free_text:
                type:
                  - string
                  - 'null'
                maxLength: 200
        diet_classification_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        religious_requirement:
          type:
            - string
            - 'null'
          maxLength: 120
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
    CustomerConsent:
      type: object
      required:
        - code
        - version
        - purpose
        - audience
        - is_required
        - status
        - display_order
      properties:
        code:
          type: string
          examples:
            - consent.marketing
        version:
          type: integer
          minimum: 1
        purpose:
          type: string
          examples:
            - marketing
        audience:
          type: string
          enum:
            - all
            - d2c
          description: |
            `all` is what everybody accepts; `d2c` is written for consumers.
            Without the filter this surface would start asking a kitchen's chef
            to confirm they are old enough to order food.
        is_required:
          type: boolean
          description: |
            Read from the consent catalogue rather than hard-coded, so a new
            required text takes effect by being seeded. A required consent still
            blocks activation when withdrawn — and may still be withdrawn.
        status:
          type: string
          enum:
            - granted
            - pending
          description: |
            Computed against **this** version. A code granted at version 1 is
            `pending` again once version 2 is published, because a status
            computed from the code alone would report somebody as having
            accepted words they have never seen.
        display_order:
          type: integer
          minimum: 0
    CustomerConsentsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CustomerConsent'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
              properties:
                count:
                  type: integer
                  minimum: 0
    GrantConsentsRequest:
      type: object
      required:
        - codes
      properties:
        codes:
          type: array
          minItems: 1
          maxItems: 40
          items:
            type: string
            maxLength: 80
          examples:
            - - consent.marketing
              - consent.health_data_processing
      description: |
        No version. A grant is written against the current version of each
        definition, read at the moment of writing — a body that named one could
        accept a text that has since been reissued.
    B2bApplicationStatus:
      type: string
      description: |
        The seven states a B2B application passes through.

        `info_requested` is not a rejection and not a pause: it hands editing
        rights back for named sections while the application keeps its place
        in the queue, which is what lets a status panel say "we need two
        things from you" instead of the same "under review" it showed
        yesterday.

        `withdrawn` is the applicant's exit and `declined` is the platform's.
        Collapsing them would make "how many did we turn down" unanswerable.
      enum:
        - draft
        - submitted
        - in_review
        - info_requested
        - approved
        - declined
        - withdrawn
    B2bApplicationSection:
      type: string
      description: |
        The field group a wizard step writes. A section is the unit of
        writing: a PATCH names one and carries only its fields, and anything
        else is refused with the name of the section that owns it.

        Documents are deliberately **not** a section — they have their own
        lifecycle and their own review, and folding them in would put a PATCH
        of a text box and an upload of a passport on one code path.
      enum:
        - company
        - signatory
        - trade_terms
        - logistics
    B2bApplicationContactRole:
      type: string
      description: |
        The four people an applicant company names, one per slot.

        `signatory` overlaps the `signatory_*` fields on the application and
        that is not duplication: the fields are the *declaration* about who
        can bind the company, and the contact row is where to reach that
        person about the paperwork. A company whose chief executive signs and
        whose office manager chases the emails has one of each.
      enum:
        - primary
        - billing
        - operations
        - signatory
    B2bPaymentTerms:
      type: string
      description: |
        How long a corporate buyer has to pay. The same vocabulary appears on
        the application (what was *asked for*) and on the agreement (what was
        *granted*), because the whole point of the review is to compare them.

        **Recorded, not enforced.** No invoicing exists — PAY1 is
        discovery-gated — so nothing in the platform will chase a net-30
        balance, and no surface may imply otherwise.
      enum:
        - prepaid
        - net_15
        - net_30
        - net_60
    B2bDocumentKind:
      type: string
      description: |
        What a document is supposed to prove. Two kinds are required before an
        application may be submitted — `commercial_registration` and
        `signatory_identification` — because one says the company exists and
        the other says the signatory is a real person who can be held to what
        they sign. Everything else is asked for when the case calls for it.

        `signed_agreement` is produced by the signing flow and is **not**
        accepted from a client: an applicant supplying their own idea of what
        they signed is the one document that must never be self-asserted.

        `other` exists because reviewers ask for things nobody anticipated,
        and the alternative is an applicant mislabelling a document.
      enum:
        - commercial_registration
        - tax_certificate
        - trade_licence
        - signatory_identification
        - authorisation_letter
        - proof_of_address
        - food_safety_certificate
        - insurance_certificate
        - signed_agreement
        - other
    KycDocumentReviewStatus:
      type: string
      description: |
        A human's verdict on a document. `superseded` is not a verdict anybody
        gives — it is what happens to an accepted document when a newer one of
        the same kind replaces it, so the earlier decision trail survives a
        re-upload.
      enum:
        - pending
        - accepted
        - rejected
        - superseded
    KycDocumentRejectionReason:
      type: string
      description: |
        Why a reviewer turned a document down. A closed vocabulary rather than
        free text, because this is the half of a rejection the applicant is
        shown and "please re-upload, it was illegible" is actionable in a way
        a reviewer's internal note is not.
      enum:
        - unreadable
        - wrong_document
        - expired
        - incomplete
        - mismatch
        - other
    KycDocumentScanStatus:
      type: string
      description: |
        Whether anything has looked inside the file for malware. **Every
        document B1 stores is `not_scanned`**, and this field exists so that
        fact is in the data rather than only in a risk register.

        Sniffing the leading bytes proves a PDF is a PDF; it proves nothing
        about what the PDF contains. `clean` and `infected` are declared and
        unreachable in this phase, and nothing may present a document as safe
        until a scanner fills them in (INT-008).
      enum:
        - not_scanned
        - clean
        - infected
    B2bAgreementStatus:
      type: string
      description: |
        `draft → pending_signature → active → suspended ⇄ active →
        terminated`.

        The transition that is *not* here is any route back into `draft` once
        terms have been put in front of a counterparty. Changing what somebody
        was asked to sign, in place, is the failure this vocabulary is shaped
        against; the supported move is a new version that supersedes the old.

        `suspended` is reversible and `terminated` is not.
      enum:
        - draft
        - pending_signature
        - active
        - suspended
        - terminated
    OrganisationInvitationStatus:
      type: string
      description: |
        **Derived, never stored.** `accepted_at`, `revoked_at` and
        `expires_at` are already on the row, and a status column would be a
        fourth copy that has to be kept in step with a clock — expiry being
        the first thing to drift.
      enum:
        - live
        - accepted
        - revoked
        - expired
    B2bApplication:
      type: object
      description: |
        A company's request to become a corporate buyer, as the applicant sees
        it.

        `completed_sections` is the applicant's own **claim** about their
        progress and is never consulted by the submission gate; the server's
        definition of complete arrives as `missing_fields` /
        `missing_documents` on the refusal. A surface that treated the claim
        as the answer would show a green wizard next to a 422.

        `writable_sections` is computed. In `info_requested` it is the
        reviewer's narrowing rather than the whole form.
      required:
        - id
        - reference
        - status
        - applicant_user_id
        - completed_sections
        - writable_sections
        - requires_invoice_per_location
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        reference:
          type: string
          description: The code a person reads down a phone line.
          examples:
            - B2B-2026-7F3A9C21
        status:
          $ref: '#/components/schemas/B2bApplicationStatus'
        applicant_user_id:
          $ref: '#/components/schemas/Uuid'
        legal_name:
          type:
            - string
            - 'null'
        legal_name_ar:
          type:
            - string
            - 'null'
        trading_name:
          type:
            - string
            - 'null'
        business_type:
          type:
            - string
            - 'null'
        country_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        commercial_registration_number:
          type:
            - string
            - 'null'
        tax_registration_number:
          type:
            - string
            - 'null'
        incorporated_on:
          type:
            - string
            - 'null'
          format: date
        website:
          type:
            - string
            - 'null'
        signatory_name:
          type:
            - string
            - 'null'
        signatory_title:
          type:
            - string
            - 'null'
        signatory_email:
          type:
            - string
            - 'null'
          format: email
          description: |
            The address the company says can bind it. The signature challenge
            requires the authenticated caller to hold this same address.
        signatory_phone:
          type:
            - string
            - 'null'
        requested_payment_terms:
          oneOf:
            - $ref: '#/components/schemas/B2bPaymentTerms'
            - type: 'null'
        requested_credit_limit_minor:
          type:
            - integer
            - 'null'
          minimum: 0
          description: |
            Minor units of `currency_code`. **Null is a value**: an applicant
            asking for nothing is asking to pay up front, which is a complete
            answer and why this is not required at submission.
        currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
        expected_volume_band:
          type:
            - string
            - 'null'
        expected_order_frequency:
          type:
            - string
            - 'null'
        product_categories:
          type: array
          items:
            type: string
        preferred_delivery_window:
          type:
            - string
            - 'null'
        lead_time_days:
          type:
            - integer
            - 'null'
          minimum: 0
        requires_invoice_per_location:
          type: boolean
        delivery_notes:
          type:
            - string
            - 'null'
        completed_sections:
          type: array
          items:
            $ref: '#/components/schemas/B2bApplicationSection'
        writable_sections:
          type: array
          items:
            $ref: '#/components/schemas/B2bApplicationSection'
        information_request:
          type:
            - string
            - 'null'
          description: What the reviewer asked for, verbatim. The applicant reads this.
        information_requested_sections:
          type: array
          items:
            $ref: '#/components/schemas/B2bApplicationSection'
        information_requested_at:
          type:
            - string
            - 'null'
          format: date-time
        applicant_message:
          type:
            - string
            - 'null'
          description: The half of a verdict the applicant is shown. Required on a decline.
        submitted_at:
          type:
            - string
            - 'null'
          format: date-time
        review_started_at:
          type:
            - string
            - 'null'
          format: date-time
        decided_at:
          type:
            - string
            - 'null'
          format: date-time
        withdrawn_at:
          type:
            - string
            - 'null'
          format: date-time
        provisioned_organisation_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Null until the approval has been provisioned. Moves together with `customer_account_id` — a CHECK says so.
        customer_account_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        lock_version:
          type: integer
          minimum: 0
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
    B2bApplicationReview:
      description: |
        The reviewer's view: everything the applicant sees, plus the internal
        half of the decision. `decision_note` never reaches the applicant —
        "we were not comfortable with the credit history" and "we are unable
        to approve your application at this time" can both be true, and only
        one of them travels.
      allOf:
        - $ref: '#/components/schemas/B2bApplication'
        - type: object
          properties:
            decision_note:
              type:
                - string
                - 'null'
              description: The reviewer's own record of why. Internal, always.
            reviewed_by:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
            duplicate_of_application_id:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
              description: Set only by a human confirming that two applications are the same company.
    B2bApplicationSummary:
      type: object
      description: |
        The list row. Deliberately thin: a queue answers "which of these do I
        open next", and every confidential field carried in a list is a field
        displayed on a screen somebody walks past.
      required:
        - id
        - reference
        - status
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        reference:
          type: string
        status:
          $ref: '#/components/schemas/B2bApplicationStatus'
        legal_name:
          type:
            - string
            - 'null'
        trading_name:
          type:
            - string
            - 'null'
        country_code:
          type:
            - string
            - 'null'
        submitted_at:
          type:
            - string
            - 'null'
          format: date-time
        decided_at:
          type:
            - string
            - 'null'
          format: date-time
        reviewed_by:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        lock_version:
          type: integer
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
    B2bApplicationContact:
      type: object
      description: |
        A person the applicant company named. **Transcribed paperwork, not a
        verified destination** — nothing here has been proved, which is why
        these are not contact points.
      required:
        - id
        - role
        - name
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        role:
          $ref: '#/components/schemas/B2bApplicationContactRole'
        name:
          type: string
          maxLength: 160
        title:
          type:
            - string
            - 'null'
          maxLength: 120
        email:
          type:
            - string
            - 'null'
          format: email
        phone:
          type:
            - string
            - 'null'
        notes:
          type:
            - string
            - 'null'
    B2bApplicationLocation:
      type: object
      description: Somewhere the company wants food delivered, anchored to the platform gazetteer where the gazetteer covers it.
      required:
        - id
        - label
        - address_line1
        - is_primary
        - is_billing_address
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        label:
          type: string
          maxLength: 120
        delivery_area_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        address_line1:
          type: string
        address_line2:
          type:
            - string
            - 'null'
        city:
          type:
            - string
            - 'null'
        country_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        contact_name:
          type:
            - string
            - 'null'
        contact_phone:
          type:
            - string
            - 'null'
        delivery_notes:
          type:
            - string
            - 'null'
        is_primary:
          type: boolean
        is_billing_address:
          type: boolean
        expected_headcount:
          type:
            - integer
            - 'null'
          minimum: 1
    KycDocument:
      type: object
      description: |
        An identity, registration or licensing document — **metadata only**.
        There is no `url`, no `disk` and no `path` here and there never may
        be: the only route to the bytes is a short-lived signed link that
        expires and records who asked and why.

        `media_type_mismatch` is stated rather than left for a client to
        compute, because the difference between what the uploader claimed and
        what the bytes are is evidence.
      required:
        - id
        - document_kind
        - original_filename
        - mime_type
        - media_type_mismatch
        - byte_size
        - sha256
        - scan_status
        - review_status
        - uploaded_at
        - has_expired
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        document_kind:
          $ref: '#/components/schemas/B2bDocumentKind'
        original_filename:
          type: string
          description: Kept for a human to read. Never used to locate the object.
        mime_type:
          type: string
          description: What the leading bytes actually are — never the client's claim.
        declared_mime_type:
          type:
            - string
            - 'null'
          description: What the client said. Kept because a mismatch is evidence.
        media_type_mismatch:
          type: boolean
        byte_size:
          type: integer
          minimum: 1
        sha256:
          type: string
          pattern: ^[0-9a-f]{64}$
          description: What an agreement's signature evidence names.
        scan_status:
          $ref: '#/components/schemas/KycDocumentScanStatus'
        review_status:
          $ref: '#/components/schemas/KycDocumentReviewStatus'
        rejection_reason:
          oneOf:
            - $ref: '#/components/schemas/KycDocumentRejectionReason'
            - type: 'null'
        reviewed_at:
          type:
            - string
            - 'null'
          format: date-time
        uploaded_by:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        uploaded_at:
          type: string
          format: date-time
        expires_on:
          type:
            - string
            - 'null'
          format: date
        has_expired:
          type: boolean
    KycDocumentReview:
      description: |
        The platform shape: the same document plus the reviewer's private note
        and the retention deadline. `review_note` is internal and stays that
        way; `purge_after` is here rather than on the applicant shape because
        the retention window is a **placeholder** pending OQ-029, and showing
        an applicant a deadline the platform has not decided would present a
        guess as a commitment.
      allOf:
        - $ref: '#/components/schemas/KycDocument'
        - type: object
          properties:
            review_note:
              type:
                - string
                - 'null'
            reviewed_by:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
            owner_kind:
              type: string
              enum:
                - user
                - b2b_application
                - organisation
            b2b_application_id:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
            purge_after:
              type: string
              format: date-time
    KycDocumentDownload:
      type: object
      description: |
        A signed, expiring grant against object storage. Handed over as data
        rather than as a redirect so a client knows it holds a credential with
        a deadline — it can show the expiry, refresh before acting, and
        decline to cache it.
      required:
        - url
        - expires_at
      properties:
        url:
          type: string
          format: uri
          description: Treat as a secret. Do not log it, cache it or share it.
        expires_at:
          type: string
          format: date-time
          description: Computed before the signature is minted, so it is never later than the moment the link stops working.
    B2bAgreement:
      type: object
      description: |
        One version of the terms between the platform and a corporate buyer.

        The signature block is **evidence**: the digest of the bytes shown,
        the typed name, the stated capacity and the consent wording verbatim.
        What is absent is anything replayable — the hashed address and user
        agent stay server-side, and the passcode challenge is reduced to
        `signature_otp_verified`.

        **Never a qualified electronic signature.** This is a record that
        somebody clicked, with good evidence about who and when (INT-007
        covers real e-sign).

        Money is integers of minor units beside `currency_code`, never
        formatted.
      required:
        - id
        - b2b_application_id
        - version
        - status
        - title
        - auto_renews
        - signature_otp_verified
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        b2b_application_id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Stamped by provisioning. Null until the company is a tenant.
        version:
          type: integer
          minimum: 1
        supersedes_agreement_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        status:
          $ref: '#/components/schemas/B2bAgreementStatus'
        title:
          type: string
        currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
        price_list_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Always a list whose `customer_scope` is `agreement`. Pointing an
            agreement at a public tariff would quietly publish a negotiated
            position.
        payment_terms:
          oneOf:
            - $ref: '#/components/schemas/B2bPaymentTerms'
            - type: 'null'
        credit_limit_minor:
          type:
            - integer
            - 'null'
          minimum: 0
        minimum_order_minor:
          type:
            - integer
            - 'null'
          minimum: 0
        delivery_lead_time_days:
          type:
            - integer
            - 'null'
          minimum: 0
        notice_period_days:
          type:
            - integer
            - 'null'
          minimum: 0
        starts_on:
          type:
            - string
            - 'null'
          format: date
        ends_on:
          type:
            - string
            - 'null'
          format: date
        auto_renews:
          type: boolean
        terms_summary:
          type:
            - string
            - 'null'
        signature_document_sha256:
          type:
            - string
            - 'null'
          pattern: ^[0-9a-f]{64}$
        signatory_name:
          type:
            - string
            - 'null'
        signatory_title:
          type:
            - string
            - 'null'
        signatory_user_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        signature_consent_statement:
          type:
            - string
            - 'null'
          description: The wording the signatory accepted, verbatim — not a version number pointing at copy somebody may later edit.
        signature_otp_verified:
          type: boolean
          description: |
            True exactly when a consumed `b2b_signatory` challenge belonging
            to the signatory is on file. Derived from the column, never from a
            caller's claim.
        signed_at:
          type:
            - string
            - 'null'
          format: date-time
        activated_at:
          type:
            - string
            - 'null'
          format: date-time
        suspended_at:
          type:
            - string
            - 'null'
          format: date-time
        terminated_at:
          type:
            - string
            - 'null'
          format: date-time
        termination_reason:
          type:
            - string
            - 'null'
        lock_version:
          type: integer
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
    OrganisationInvitation:
      type: object
      description: |
        An offer of membership. **There is no `token` field and there never
        may be** — the plaintext is delivered once, in the email, and only its
        SHA-256 is stored.
      required:
        - id
        - organisation_id
        - email
        - role_code
        - status
        - expires_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        email:
          type: string
          format: email
        role_code:
          type: string
          description: The role the person is being invited into. A platform template role, or one this organisation defined.
        status:
          $ref: '#/components/schemas/OrganisationInvitationStatus'
        message:
          type:
            - string
            - 'null'
          description: The sentence that travelled with the invitation. An invitation with no context is an email people assume is phishing, and rightly.
        expires_at:
          type: string
          format: date-time
        accepted_at:
          type:
            - string
            - 'null'
          format: date-time
        accepted_by_user_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        revoked_at:
          type:
            - string
            - 'null'
          format: date-time
        revoked_by:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        invited_by:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
    RecipeRollupWarning:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          enum:
            - rollup.unknown_ingredient
            - nutrition_unavailable
            - rollup.missing_facts
            - rollup.mixed_cost_currency
            - rollup.missing_cost
          description: |
            The closed set the preview can emit.
            `nutrition_unavailable` is on **every** response while the
            roll-up is not computed server-side, which is why `warnings` is
            never empty.
        message:
          type: string
          description: A safe English summary. Clients translate from `code`, never from this.
        ingredient_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
          description: |
            Present on `rollup.unknown_ingredient` (exactly one) and
            `rollup.missing_facts` (however many). Absent on the rest —
            absent, not empty.
      description: One thing the caller should know about the figures they are being shown.
    RecipeRollupAllergenSource:
      type: object
      required:
        - allergen_code
        - containment
        - ingredient_ids
      properties:
        allergen_code:
          type: string
        containment:
          type: string
          description: How the allergen is present — the recipe module's containment vocabulary.
        ingredient_ids:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/Uuid'
      additionalProperties: false
      description: |
        One allergen, at one containment, and every line that put it there.
        Grouped by `allergen_code` + `containment` and ordered by that pair.
    RecipeRollupPreview:
      type: object
      required:
        - per_recipe
        - per_serving
        - per_100g
        - allergen_sources
        - estimated_cost
        - warnings
      properties:
        per_recipe:
          type: 'null'
          description: |
            **Always `null`.** Nutrition roll-up is not computed on the server
            yet. The key exists because it is the shape the published version
            will carry, and a client that had to grow three fields later would
            have shipped a screen with nothing to put in them.
        per_serving:
          type: 'null'
          description: Always `null`. See `per_recipe`.
        per_100g:
          type: 'null'
          description: Always `null`. See `per_recipe`.
        allergen_sources:
          type: array
          items:
            $ref: '#/components/schemas/RecipeRollupAllergenSource'
        estimated_cost:
          oneOf:
            - type: object
              required:
                - amount
                - currency
              properties:
                amount:
                  type: string
                  description: |
                    A decimal **string** at six places, not a number. Money
                    that has been through a binary float is money somebody
                    cannot reconcile.
                  examples:
                    - '12.450000'
                currency:
                  type: string
                  minLength: 3
                  maxLength: 3
              additionalProperties: false
            - type: 'null'
          description: |
            `null` when the caller does not hold
            `recipe.view_costs_organisation`, when no line is fully costed,
            when the costed lines disagree about currency, or when a line is
            costed only halfway. The last three each add a `warnings` entry
            naming which.
        warnings:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/RecipeRollupWarning'
      description: What a draft formulation would declare, computed and thrown away.
    PreviewRecipeRollupLine:
      type: object
      required:
        - ingredient_id
      properties:
        ingredient_id:
          $ref: '#/components/schemas/Uuid'
        quantity:
          type:
            - number
            - 'null'
          exclusiveMinimum: 0
          maximum: 99999999.9999
        unit_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: A measurement unit that does not exist is `422`, unlike an ingredient that does not.
        unit_cost_amount:
          type:
            - number
            - 'null'
          minimum: 0
          maximum: 1000000000000
        cost_currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
          description: |
            Upper-cased server-side. An amount without a currency, or a
            currency without an amount, is `422` — a half-stated cost is a
            contradiction rather than a partial answer.
      additionalProperties: false
    PreviewRecipeRollupRequest:
      type: object
      required:
        - servings
        - lines
      properties:
        recipe_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: |
            Accepted and validated, but does not currently change the answer.
            Part of the request because it is part of the question.
        servings:
          type: number
          exclusiveMinimum: 0
          maximum: 9999
          description: Required. Does not currently change the answer — the per-serving figures are `null`.
        waste_percent:
          type:
            - number
            - 'null'
          minimum: 0
          maximum: 100
          description: Applied to `estimated_cost.amount` only.
        lines:
          type: array
          maxItems: 200
          items:
            $ref: '#/components/schemas/PreviewRecipeRollupLine'
          description: |
            Must be **present**, and may be `[]` — an empty draft is a
            legitimate thing to preview, and the answer is the warnings.
      additionalProperties: false
    CatalogueItemAvailabilityDay:
      type: object
      required:
        - id
        - date
        - is_available
        - remaining_portions
        - order_cut_off_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        date:
          type: string
          format: date
        is_available:
          type: boolean
        remaining_portions:
          type:
            - integer
            - 'null'
          minimum: 0
          description: |
            The request calls this `remaining`. Same number, different name —
            the request says how many are left to sell, the row records a
            portion count.
        order_cut_off_at:
          type:
            - string
            - 'null'
          pattern: ^[0-2][0-9]:[0-5][0-9]$
          description: |
            Always `HH:MM`, even when `HH:MM:SS` was sent. A cut-off is a
            wall-clock minute and the seconds were never meaningful.
          examples:
            - '18:00'
      additionalProperties: false
    CatalogueItemAvailabilityDayInput:
      type: object
      required:
        - date
        - is_available
      properties:
        date:
          type: string
          format: date
          description: '`YYYY-MM-DD`. Stating the same date twice is refused, not last-one-wins.'
        is_available:
          type: boolean
        remaining:
          type:
            - integer
            - 'null'
          minimum: 0
          description: Served back as `remaining_portions`.
        order_cut_off_at:
          type:
            - string
            - 'null'
          description: '`HH:MM` or `HH:MM:SS`. Served back as `HH:MM`.'
          examples:
            - '18:00'
            - '18:00:00'
      additionalProperties: false
    ReplaceCatalogueItemAvailabilityRequest:
      type: object
      required:
        - days
      properties:
        days:
          type: array
          maxItems: 366
          items:
            $ref: '#/components/schemas/CatalogueItemAvailabilityDayInput'
          description: |
            The complete calendar. **`[]` clears every day** — a thing
            somebody may genuinely mean, which is why this is a PUT.
      additionalProperties: false
    DeliveryJobStatus:
      type: string
      enum:
        - pending
        - assigned
        - in_transit
        - delivered
        - failed
        - cancelled
      description: Where the job is in its life.
    DeliveryJobTrackingStatus:
      type: string
      enum:
        - awaiting_assignment
        - picked_up
        - en_route
        - arrived
        - delivered
      description: |
        What the customer would be told. A second axis rather than a finer
        `status`, because "assigned but not yet collected" and "collected"
        are the same dispatch state and different customer messages.
    DeliveryJob:
      type: object
      required:
        - id
        - order_id
        - status
        - tracking_status
        - driver_user_id
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/DeliveryJobStatus'
        tracking_status:
          $ref: '#/components/schemas/DeliveryJobTrackingStatus'
        driver_user_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: '`null` until dispatch assigns one.'
      additionalProperties: false
      description: A delivery job as the dispatch board sees it.
    DriverJobDelivery:
      type: object
      required:
        - line_one
        - building
        - floor
        - apartment
        - directions
        - area_name_en
        - area_name_ar
        - window_code
        - requested_date
        - phone
      description: |
        Where this run is going, **as the order recorded it at placement** — never
        as the customer's address book stands now. A customer who edits their
        address at eight o'clock has not changed where tonight's food is going,
        and a run sheet reading the live address would send a driver to a door
        the order was never for.

        Every field is nullable, and the nulls are ordinary: an order placed
        before the snapshot was widened carries no building or floor, an
        as-soon-as-possible order names no day, and a kitchen that has not named
        its slots has no window code.
      properties:
        line_one:
          type:
            - string
            - 'null'
          description: The street. What gets somebody to the building.
        building:
          type:
            - string
            - 'null'
        floor:
          type:
            - string
            - 'null'
        apartment:
          type:
            - string
            - 'null'
        directions:
          type:
            - string
            - 'null'
          description: Free text the customer wrote for the courier. Never parsed.
        area_name_en:
          type:
            - string
            - 'null'
        area_name_ar:
          type:
            - string
            - 'null'
        window_code:
          type:
            - string
            - 'null'
        requested_date:
          type:
            - string
            - 'null'
          format: date
        phone:
          type:
            - string
            - 'null'
          description: |
            E.164, and it is **the number the order was given** — resolved from
            the contact point the snapshot names, not the customer's best current
            number. `null` when the order named none, with no fallback: a
            substitution would answer a different question while looking like the
            same field, and this route carries no permission code to hold a
            customer-contact lookup behind. The desk has
            `order.view_customer_contact_organisation` for that.
      additionalProperties: false
    DriverJob:
      type: object
      required:
        - id
        - order_id
        - order_number
        - status
        - tracking_status
        - assigned_at
        - delivery
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_id:
          $ref: '#/components/schemas/Uuid'
        order_number:
          type:
            - string
            - 'null'
          description: The number printed on the bag, so a courier can match one to the other.
        status:
          $ref: '#/components/schemas/DeliveryJobStatus'
        tracking_status:
          $ref: '#/components/schemas/DeliveryJobTrackingStatus'
        assigned_at:
          type:
            - string
            - 'null'
          format: date-time
          description: When this run was handed to the caller. `null` on a job nobody has assigned.
        delivery:
          $ref: '#/components/schemas/DriverJobDelivery'
      additionalProperties: false
      description: |
        The same job on the driver's own run sheet, carrying what somebody
        standing next to a van needs: which order this is, where the food is
        going in enough detail to find a door, when it was promised, when they
        were given it, and the number to ring when they still cannot find the
        building.

        `driver_user_id` is absent because it would say the same thing on every
        row — the caller's own id, which they already have — and the dispatch
        board is the surface where whose job it is is worth answering. Money is
        absent for a related reason: a driver collecting cash on delivery is a
        real flow and it belongs to the receipts ledger, not bolted onto a run
        sheet.
    AssignDeliveryJobRequest:
      type: object
      required:
        - driver_user_id
      properties:
        driver_user_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: |
            An **active member** of this organisation. There is deliberately no
            null: unassigning a run is a different decision and would need its
            own route and its own audit action rather than arriving as an omitted
            key.
      additionalProperties: false
    AssignedDeliveryJob:
      type: object
      required:
        - id
        - order_id
        - status
        - tracking_status
        - driver_user_id
        - assigned_at
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/DeliveryJobStatus'
        tracking_status:
          $ref: '#/components/schemas/DeliveryJobTrackingStatus'
        driver_user_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        assigned_at:
          type:
            - string
            - 'null'
          format: date-time
        lock_version:
          type: integer
          description: The validator to send as `If-Match` on the next write.
      additionalProperties: false
      description: |
        The dispatch board's shape plus what the assignment just decided. A
        superset rather than a different shape, so a board refreshing one row
        from this response does not have to reconcile two vocabularies.
    DeliverDriverJobRequest:
      type: object
      properties:
        proof_of_delivery_notes:
          type:
            - string
            - 'null'
          maxLength: 1000
          description: |
            **Replace-or-clear.** Omitting the key writes `null` over whatever
            was there; there is no partial update of this field.
      additionalProperties: false
    PaymentMethodKind:
      type: string
      enum:
        - cash_on_delivery
        - card
        - invoice
      description: |
        How a payment is settled. `invoice` is part of the vocabulary but has
        no provider behind it — `POST /payments/intents` accepts it at the
        validator and then answers `400 request.invalid`.
    PaymentIntentStatus:
      type: string
      enum:
        - pending
        - authorized
        - captured
        - failed
        - cancelled
    PaymentIntent:
      type: object
      required:
        - id
        - order_id
        - status
        - method_kind
        - currency_code
        - amount_minor
        - provider
        - provider_ref
        - authorized_at
        - captured_at
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        order_id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/PaymentIntentStatus'
        method_kind:
          $ref: '#/components/schemas/PaymentMethodKind'
        currency_code:
          allOf:
            - $ref: '#/components/schemas/CurrencyCode'
          description: Copied from the order. The request cannot name it.
        amount_minor:
          type: integer
          minimum: 0
          description: Copied from the order's total. The request cannot name it.
        provider:
          type:
            - string
            - 'null'
          maxLength: 48
          description: Which provider authorised it.
        provider_ref:
          type:
            - string
            - 'null'
          maxLength: 120
          description: The provider's own reference, when it issues one.
        authorized_at:
          type:
            - string
            - 'null'
          format: date-time
        captured_at:
          type:
            - string
            - 'null'
          format: date-time
        lock_version:
          type: integer
          minimum: 0
          description: |
            Carried in the body, but **no `ETag` is served** and no `If-Match`
            is read on this family: capture and refund are guarded by state
            (`409`) rather than by version.
      additionalProperties: false
    PaymentIntentEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - payment_intent
          properties:
            payment_intent:
              $ref: '#/components/schemas/PaymentIntent'
        meta:
          $ref: '#/components/schemas/Meta'
    CreatePaymentIntentRequest:
      type: object
      required:
        - order_id
        - method_kind
      properties:
        order_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: An order the caller owns. Anybody else's is `404`.
        method_kind:
          $ref: '#/components/schemas/PaymentMethodKind'
      additionalProperties: false
    Refund:
      type: object
      required:
        - id
        - payment_intent_id
        - amount_minor
        - currency_code
        - status
        - completed_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        payment_intent_id:
          $ref: '#/components/schemas/Uuid'
        amount_minor:
          type: integer
          minimum: 1
        currency_code:
          allOf:
            - $ref: '#/components/schemas/CurrencyCode'
          description: Copied from the intent.
        status:
          type: string
          description: Always `completed` on this endpoint. The column also allows `pending` and `failed`.
          examples:
            - completed
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
      additionalProperties: false
    CreateRefundRequest:
      type: object
      required:
        - amount_minor
      properties:
        amount_minor:
          type: integer
          minimum: 1
          description: |
            Whole minor units of the intent's currency. **Not checked against
            the captured amount or against refunds already recorded** — a
            client building a refund form is what stands between an operator
            and a typo.
      additionalProperties: false
    CorporateProgrammeStatus:
      type: string
      enum:
        - active
        - suspended
        - closed
      description: |
        Whether a programme is currently something quotations may be drafted
        against. Extensible: clients must tolerate a value they do not know.
    QuotationStatus:
      type: string
      enum:
        - draft
        - submitted
        - quoted
        - accepted
        - declined
        - expired
      description: |
        The life of one corporate quotation (B5):
        `draft → submitted → quoted → accepted | declined | expired`.

        The buyer owns `draft` and `submitted`. The kitchen owns the move out
        of `submitted` into `quoted` — naming the prices is the one act
        neither the buyer nor the platform performs for them. From `quoted`
        the buyer decides again, or the clock runs out and a scheduled sweep
        writes `expired` seven days after `quoted_at`.

        **There is deliberately no route back into `draft`.** A buyer who
        wants to change a submitted set drafts a fresh quotation; a programme
        may hold many, and that costs them nothing.
    CorporateProgramme:
      type: object
      required:
        - id
        - organisation_id
        - kitchen_organisation_id
        - b2b_agreement_id
        - code
        - name_en
        - name_ar
        - description
        - status
        - lock_version
        - created_at
        - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: The **buyer** organisation. This is the row's tenant.
        kitchen_organisation_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: The **seller** kitchen the programme is with.
        b2b_agreement_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: |
            The signed agreement the programme hangs off. Its active price
            list is what fixes a quotation's currency and what the buyer's
            catalogue prices resolve through.
        code:
          type: string
          description: A short human-quotable code for the programme.
        name_en:
          type: string
        name_ar:
          type: string
          description: |
            May be the empty string. Served as stored rather than hidden — an
            empty Arabic name is a fact about the data, and a client that
            wants to fall back can.
        description:
          type:
            - string
            - 'null'
        status:
          $ref: '#/components/schemas/CorporateProgrammeStatus'
        lock_version:
          type: integer
          minimum: 0
          description: |
            The optimistic-concurrency validator. Carried for symmetry; there
            is no write on a programme in this phase, so no `ETag` is served.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      description: One corporate programme — a buyer organisation's standing arrangement with one kitchen.
    CorporateProgrammeEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - programme
          properties:
            programme:
              $ref: '#/components/schemas/CorporateProgramme'
        meta:
          $ref: '#/components/schemas/Meta'
    QuotationLine:
      type: object
      required:
        - id
        - line_number
        - catalogue_item_id
        - catalogue_item_variant_id
        - quantity
        - unit_amount_minor
        - line_total_minor
        - note
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        line_number:
          type: integer
          minimum: 1
          description: |
            The line's position, renumbered from 1 every time the set is
            replaced. Not a stable identifier — `id` is.
        catalogue_item_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: The article, which must be one the programme's kitchen sells.
        catalogue_item_variant_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: A variant of that article, when the line names one.
        quantity:
          type: string
          description: |
            A decimal **string** at four places — `"12.0000"`. A string rather
            than a number because a quantity that survives a round trip
            through JSON's binary floats is a quantity somebody can reconcile
            an invoice against.
          examples:
            - '12.0000'
        unit_amount_minor:
          type:
            - integer
            - 'null'
          minimum: 0
          description: |
            The price the kitchen named for one unit, in whole minor units of
            the quotation's `currency_code`. `null` until the kitchen quotes —
            the absence **is** the statement that no price has been named, not
            a zero.
        line_total_minor:
          type:
            - integer
            - 'null'
          minimum: 0
          description: |
            `quantity × unit_amount_minor`, rounded to the nearest minor unit
            and computed by the server. `null` until the kitchen quotes.
        note:
          type:
            - string
            - 'null'
          maxLength: 255
      description: One line of a quotation — what the buyer asked for, and what the kitchen said it costs.
    Quotation:
      type: object
      required:
        - id
        - organisation_id
        - corporate_programme_id
        - reference
        - status
        - currency_code
        - notes
        - decline_reason
        - submitted_at
        - quoted_at
        - expires_at
        - decided_at
        - lock_version
        - lines
        - created_at
        - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: The **buyer** organisation. This is the row's tenant, which is why the kitchen's side of this surface reaches it through the programme instead.
        corporate_programme_id:
          $ref: '#/components/schemas/Uuid'
        reference:
          type: string
          description: A reference a person can read over the phone.
          examples:
            - QUO-2026-7F3A9C21
        status:
          $ref: '#/components/schemas/QuotationStatus'
        currency_code:
          allOf:
            - $ref: '#/components/schemas/CurrencyCode'
          description: |
            Copied from the agreement's active price list when the draft is
            opened, and fixed for the quotation's life. The buyer does not
            choose it.
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
        decline_reason:
          type:
            - string
            - 'null'
          maxLength: 2000
          description: Set only when the buyer declined, and only when they gave one.
        submitted_at:
          type:
            - string
            - 'null'
          format: date-time
        quoted_at:
          type:
            - string
            - 'null'
          format: date-time
        expires_at:
          type:
            - string
            - 'null'
          format: date-time
          description: |
            Seven days after `quoted_at`. A `quoted` quotation past this
            instant behaves as expired even before the nightly sweep has
            caught up with it.
        decided_at:
          type:
            - string
            - 'null'
          format: date-time
          description: When the buyer accepted or declined.
        lock_version:
          type: integer
          minimum: 0
          description: The optimistic-concurrency validator, served as the `ETag` on the single-quotation reads and every write.
        lines:
          type: array
          items:
            $ref: '#/components/schemas/QuotationLine'
          description: |
            The complete set, in `line_number` order. **Always an empty array
            on the two list endpoints** — a list is a navigation aid, and the
            lines belong on a quotation somebody deliberately opened.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
      description: One corporate quotation — a buyer's ask and a kitchen's answer.
    QuotationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - quotation
          properties:
            quotation:
              $ref: '#/components/schemas/Quotation'
        meta:
          $ref: '#/components/schemas/Meta'
    QuotationLineInput:
      type: object
      required:
        - catalogue_item_id
        - quantity
      properties:
        catalogue_item_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: |
            An article the **programme's kitchen** sells. One that does not
            exist, or belongs to another kitchen, is `422 validation.failed`
            against this line's own path.
        catalogue_item_variant_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: When given, must belong to the article on this line.
        quantity:
          type: number
          exclusiveMinimum: 0
          description: Stored to four decimal places and served back as a string.
        note:
          type:
            - string
            - 'null'
          maxLength: 255
      description: |
        One line as the buyer sends it. **There is no price field**: naming a
        price is the kitchen's move, and this shape has nowhere to put one.
    CreateQuotationRequest:
      type: object
      properties:
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
        lines:
          type:
            - array
            - 'null'
          minItems: 1
          items:
            $ref: '#/components/schemas/QuotationLineInput'
          description: |
            Optional. A draft may be opened empty and filled in by a later
            `PATCH` — which is how a client that wants an identifier before
            the user has chosen anything gets one. When present it may not be
            empty; send no `lines` key at all instead.
      additionalProperties: false
    UpdateQuotationRequest:
      type: object
      properties:
        notes:
          type:
            - string
            - 'null'
          maxLength: 2000
        lines:
          type:
            - array
            - 'null'
          items:
            $ref: '#/components/schemas/QuotationLineInput'
          description: |
            **The desired complete set**, not a delta — send every line to be
            kept. `[]` clears the draft. Omitting the key entirely leaves the
            existing lines untouched, which is what makes "save the note I
            just typed" a request that cannot lose a line.
      additionalProperties: false
    DeclineQuotationRequest:
      type: object
      properties:
        reason:
          type:
            - string
            - 'null'
          maxLength: 2000
          description: Stored as the quotation's `decline_reason`, where the kitchen can read it.
      additionalProperties: false
    QuotationPriceInput:
      type: object
      required:
        - quotation_line_id
        - unit_amount_minor
      properties:
        quotation_line_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: A line of **this** quotation. Any other identifier, or the same one twice, is `422`.
        unit_amount_minor:
          type: integer
          minimum: 0
          description: |
            Whole minor units of the quotation's own currency — `1250` for
            12.50. Never a decimal and never a formatted string. Zero is
            accepted; a kitchen may quote a line at no charge.
      additionalProperties: false
    QuoteQuotationRequest:
      type: object
      required:
        - prices
      properties:
        prices:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/QuotationPriceInput'
          description: |
            One entry for **every** line of the quotation — never fewer, never
            more. A short set is `422` with the outstanding identifiers in
            `details.fields.prices.missing_quotation_line_ids`.
      additionalProperties: false
    B2bCatalogueItem:
      type: object
      required:
        - id
        - name
        - item_type
        - seller_organisation_id
        - sales_channel_id
        - price
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          description: |
            The single name for the `language` asked for — the corporate
            catalogue serves one, not a bilingual pair, because a procurement
            screen shows one and the choice is per request.
        item_type:
          $ref: '#/components/schemas/CatalogueItemType'
        seller_organisation_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: The kitchen that sells it.
        sales_channel_id:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          description: |
            The private channel the price came from. Carried so a later basket
            can be opened on the same channel the price was quoted on.
        price:
          oneOf:
            - type: object
              required:
                - amount_minor
                - currency_code
              properties:
                amount_minor:
                  type: integer
                  description: Whole minor units — never formatted.
                currency_code:
                  $ref: '#/components/schemas/CurrencyCode'
              additionalProperties: false
            - type: 'null'
          description: |
            The one price this buyer's agreement resolves to. Never `null` on
            these two endpoints — a row whose price does not resolve is
            omitted from the list and is a `404` on the single read, because
            an article with no current tariff is not a cheaper article, it is
            one this buyer cannot order.
      description: One article as a corporate buyer sees it — one name, one price, no tariff paperwork.
    B2bCustomerAccount:
      type: object
      description: |
        The trading account an approval earns. Deliberately thin — the
        addresses, dietary profile and consents hanging off a customer account
        are somebody else's endpoints, and a fat shape here would make this
        the accidental canonical read.
      required:
        - id
        - account_number
        - account_type
        - status
        - origin
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        account_number:
          type: string
          description: The human-quotable identity — what a support conversation starts with.
          examples:
            - H360-7K2M9PQR3T
        account_type:
          type: string
          enum:
            - b2c
            - b2b
            - guest
        organisation_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        status:
          type: string
          enum:
            - provisional
            - active
            - suspended
            - closed
        origin:
          type: string
          enum:
            - self_service
            - guest
            - b2b_provisioning
            - staff
            - import
        display_name:
          type:
            - string
            - 'null'
        activated_at:
          type:
            - string
            - 'null'
          format: date-time
        created_at:
          type:
            - string
            - 'null'
          format: date-time
    UpdateB2bApplicationSectionRequest:
      type: object
      description: |
        The fields of the section named in the path, and **only** those. A key
        belonging to another section is refused with the name of the section
        that owns it, rather than dropped — silently discarding a field
        somebody believed they were saving is how a five-minute bug becomes a
        support ticket six weeks later.

        `status`, `completed_sections` and `lock_version` are not accepted:
        they are the server's, and `completed_sections` in particular is
        written as a *consequence* of a section being saved rather than as a
        claim a client may post.
      additionalProperties: false
      properties:
        legal_name:
          type:
            - string
            - 'null'
          maxLength: 255
        legal_name_ar:
          type:
            - string
            - 'null'
          maxLength: 255
        trading_name:
          type:
            - string
            - 'null'
          maxLength: 255
        business_type:
          type:
            - string
            - 'null'
          maxLength: 80
        country_code:
          type:
            - string
            - 'null'
          minLength: 2
          maxLength: 2
        commercial_registration_number:
          type:
            - string
            - 'null'
          maxLength: 80
        tax_registration_number:
          type:
            - string
            - 'null'
          maxLength: 80
        incorporated_on:
          type:
            - string
            - 'null'
          format: date
        website:
          type:
            - string
            - 'null'
          maxLength: 255
        signatory_name:
          type:
            - string
            - 'null'
          maxLength: 160
        signatory_title:
          type:
            - string
            - 'null'
          maxLength: 120
        signatory_email:
          type:
            - string
            - 'null'
          format: email
          maxLength: 255
        signatory_phone:
          type:
            - string
            - 'null'
          maxLength: 32
        requested_payment_terms:
          oneOf:
            - $ref: '#/components/schemas/B2bPaymentTerms'
            - type: 'null'
        requested_credit_limit_minor:
          type:
            - integer
            - 'null'
          minimum: 0
        currency_code:
          type:
            - string
            - 'null'
          minLength: 3
          maxLength: 3
        expected_volume_band:
          type:
            - string
            - 'null'
          maxLength: 40
        expected_order_frequency:
          type:
            - string
            - 'null'
          maxLength: 40
        product_categories:
          type:
            - array
            - 'null'
          maxItems: 40
          items:
            type: string
            maxLength: 80
        preferred_delivery_window:
          type:
            - string
            - 'null'
          maxLength: 60
        lead_time_days:
          type:
            - integer
            - 'null'
          minimum: 0
          maximum: 365
        requires_invoice_per_location:
          type: boolean
        delivery_notes:
          type:
            - string
            - 'null'
          maxLength: 2000
    ReplaceB2bApplicationContactsRequest:
      type: object
      description: |
        The complete set. `{"contacts": []}` is the payload that says "nobody,
        yet" — and is why the property is required-but-possibly-empty rather
        than optional.
      required:
        - contacts
      properties:
        contacts:
          type: array
          maxItems: 4
          items:
            type: object
            required:
              - role
              - name
            properties:
              role:
                $ref: '#/components/schemas/B2bApplicationContactRole'
              name:
                type: string
                maxLength: 160
              title:
                type:
                  - string
                  - 'null'
                maxLength: 120
              email:
                type:
                  - string
                  - 'null'
                format: email
                maxLength: 255
              phone:
                type:
                  - string
                  - 'null'
                maxLength: 32
              notes:
                type:
                  - string
                  - 'null'
                maxLength: 1000
    ReplaceB2bApplicationLocationsRequest:
      type: object
      description: |
        The complete set. `{"locations": []}` is how a company says it has
        none yet.
      required:
        - locations
      properties:
        locations:
          type: array
          maxItems: 50
          items:
            type: object
            required:
              - label
              - address_line1
            properties:
              label:
                type: string
                maxLength: 120
              delivery_area_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
              address_line1:
                type: string
                maxLength: 255
              address_line2:
                type:
                  - string
                  - 'null'
                maxLength: 255
              city:
                type:
                  - string
                  - 'null'
                maxLength: 120
              country_code:
                type:
                  - string
                  - 'null'
                minLength: 2
                maxLength: 2
              contact_name:
                type:
                  - string
                  - 'null'
                maxLength: 160
              contact_phone:
                type:
                  - string
                  - 'null'
                maxLength: 32
              delivery_notes:
                type:
                  - string
                  - 'null'
                maxLength: 1000
              is_primary:
                type:
                  - boolean
                  - 'null'
              is_billing_address:
                type:
                  - boolean
                  - 'null'
              expected_headcount:
                type:
                  - integer
                  - 'null'
                minimum: 1
                maximum: 100000
    UploadKycDocumentRequest:
      type: object
      description: |
        A `multipart/form-data` body. The size bound is checked before the
        bytes are read; **what the file is** is decided by sniffing, not by
        the part's `Content-Type` or its filename.
      required:
        - file
        - document_kind
      properties:
        file:
          type: string
          format: binary
          description: At most 10 MB. A PDF or a photograph — enforced on the sniffed type, never on the declared one.
        document_kind:
          allOf:
            - $ref: '#/components/schemas/B2bDocumentKind'
          description: '`signed_agreement` is refused: it is produced by the signing flow, never supplied by an applicant.'
        expires_on:
          type:
            - string
            - 'null'
          format: date
          description: Accepted for every kind, stored only for the kinds that expire.
    RequestB2bApplicationInformationRequest:
      type: object
      required:
        - information_request
      properties:
        information_request:
          type: string
          maxLength: 2000
          description: |
            Prose, and required. What a reviewer needs is rarely a field, and
            a closed vocabulary would force every real question through an
            `other` that says nothing. The applicant reads this verbatim.
        sections:
          type:
            - array
            - 'null'
          maxItems: 4
          description: |
            The sections to reopen. **An empty or absent list is meaningful**:
            it means the reviewer wants documents rather than answers, so
            nothing on the form reopens.
          items:
            $ref: '#/components/schemas/B2bApplicationSection'
    ApproveB2bApplicationRequest:
      type: object
      description: Both fields optional. An approval is self-explanatory; demanding a sentence would produce "approved" typed four hundred times.
      properties:
        internal_note:
          type:
            - string
            - 'null'
          maxLength: 2000
          description: The reviewer's own record. Never served to the applicant.
        applicant_message:
          type:
            - string
            - 'null'
          maxLength: 2000
          description: What the company reads.
    DeclineB2bApplicationRequest:
      type: object
      description: |
        `applicant_message` is required, and blank is refused. An unexplained
        refusal is not a decision the company can act on.
      required:
        - applicant_message
      properties:
        applicant_message:
          type: string
          minLength: 1
          maxLength: 2000
        internal_note:
          type:
            - string
            - 'null'
          maxLength: 2000
    SignB2bAgreementRequest:
      type: object
      description: |
        Every field is evidence. There is no `signature_ip_hash` or
        `signature_user_agent_hash` here and there must not be: those are
        observations of the request, taken by the server, and a
        client-supplied corroboration is not corroboration.
      required:
        - challenge_id
        - code
        - signatory_name
        - signatory_title
        - document_sha256
        - consent_statement
      properties:
        challenge_id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          maxLength: 16
          description: The passcode from the signature challenge. Spent by this request.
        signatory_name:
          type: string
          maxLength: 160
        signatory_title:
          type: string
          maxLength: 120
          description: The capacity they sign in. Recorded because "who signed" without "on what authority" is half an answer.
        document_sha256:
          type: string
          pattern: ^[0-9a-fA-F]{64}$
          description: |
            The digest of the exact bytes shown. Sixty-four hexadecimal
            characters or nothing — a truncated or base64 value would be
            recorded, would look plausible, and would prove nothing when it
            mattered.
        consent_statement:
          type: string
          maxLength: 2000
          description: The wording accepted, verbatim. A version number would be a pointer into copy somebody may later edit.
    ReviewKycDocumentRequest:
      type: object
      required:
        - verdict
      properties:
        verdict:
          type: string
          enum:
            - accepted
            - rejected
          description: |
            The two values a human may give. `pending` is where a document
            starts and `superseded` is what happens to an accepted one when a
            newer scan replaces it — neither is a decision.
        rejection_reason:
          oneOf:
            - $ref: '#/components/schemas/KycDocumentRejectionReason'
            - type: 'null'
          description: Required when rejecting. This half is what the applicant is shown.
        note:
          type:
            - string
            - 'null'
          maxLength: 2000
          description: The reviewer's private note. Never served to the applicant.
    CreateOrganisationInvitationRequest:
      type: object
      required:
        - email
        - role_code
      properties:
        email:
          type: string
          format: email
          maxLength: 255
          description: A live offer already outstanding to this address is superseded rather than refused.
        role_code:
          type: string
          maxLength: 60
          description: A platform template role, or one this organisation defined. Resolved inside the tenant scope.
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
          description: Must belong to this organisation.
        message:
          type:
            - string
            - 'null'
          maxLength: 1000
    B2bApplicationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - application
          properties:
            application:
              $ref: '#/components/schemas/B2bApplication'
        meta:
          $ref: '#/components/schemas/Meta'
    B2bApplicationDetailEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - application
            - contacts
            - locations
          properties:
            application:
              $ref: '#/components/schemas/B2bApplication'
            contacts:
              type: array
              items:
                $ref: '#/components/schemas/B2bApplicationContact'
            locations:
              type: array
              items:
                $ref: '#/components/schemas/B2bApplicationLocation'
        meta:
          $ref: '#/components/schemas/Meta'
    B2bApplicationReviewOnlyEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - application
          properties:
            application:
              $ref: '#/components/schemas/B2bApplicationReview'
        meta:
          $ref: '#/components/schemas/Meta'
    B2bApplicationReviewEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - application
            - contacts
            - locations
            - documents
            - missing_documents
            - duplicate_matches
            - duplicate_applications
          properties:
            application:
              $ref: '#/components/schemas/B2bApplicationReview'
            contacts:
              type: array
              items:
                $ref: '#/components/schemas/B2bApplicationContact'
            locations:
              type: array
              items:
                $ref: '#/components/schemas/B2bApplicationLocation'
            documents:
              type: array
              items:
                $ref: '#/components/schemas/KycDocumentReview'
            missing_documents:
              type: array
              description: The document kinds the checklist still owes. Computed from the data, never from the applicant's progress claim.
              items:
                $ref: '#/components/schemas/B2bDocumentKind'
            duplicate_matches:
              type: integer
              minimum: 0
              description: How many other applications claim the same registration or tax number. Surfaces; never decides.
            duplicate_applications:
              type: array
              items:
                $ref: '#/components/schemas/B2bApplicationSummary'
        meta:
          $ref: '#/components/schemas/Meta'
    KycDocumentEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - document
          properties:
            document:
              $ref: '#/components/schemas/KycDocument'
        meta:
          $ref: '#/components/schemas/Meta'
    KycDocumentReviewEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - document
          properties:
            document:
              $ref: '#/components/schemas/KycDocumentReview'
        meta:
          $ref: '#/components/schemas/Meta'
    KycDocumentDownloadEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - download
          properties:
            download:
              $ref: '#/components/schemas/KycDocumentDownload'
        meta:
          $ref: '#/components/schemas/Meta'
    B2bAgreementEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - agreement
          properties:
            agreement:
              $ref: '#/components/schemas/B2bAgreement'
        meta:
          $ref: '#/components/schemas/Meta'
    OrganisationInvitationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - invitation
          properties:
            invitation:
              $ref: '#/components/schemas/OrganisationInvitation'
        meta:
          $ref: '#/components/schemas/Meta'
    PublicInvitationStatus:
      type: string
      enum:
        - pending
        - accepted
        - revoked
        - expired
      description: |
        Derived from the row's own columns, never stored. `pending` is the
        platform's `live` under the name a person waiting to accept uses.

        **There is no `superseded`.** Re-inviting an address revokes the
        outstanding offer and issues a new token, so a superseded invitation is
        a revoked one; a fifth value would name a state the row cannot be in.
    PublicInvitation:
      type: object
      required:
        - id
        - status
        - role_code
        - email_masked
        - expires_at
        - organisation
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/PublicInvitationStatus'
        role_code:
          type: string
          description: |
            The role the membership would carry, e.g. `kitchen_owner`. The code
            rather than a label: the label is the client's to translate, and a
            server-chosen one would be untranslatable the moment the screen
            renders in Arabic.
          examples:
            - kitchen_owner
        email_masked:
          type: string
          description: |
            The invited address with its local part hidden —
            `o•••@example.com`. Masked **server-side**, so a client cannot
            render what it was never sent. The number of dots is fixed rather
            than tracking the real length, which would leak how long the
            address is.
          examples:
            - o•••@example.com
        expires_at:
          type: string
          format: date-time
          description: |
            Always present, and in the past when `status` is `expired`. A
            screen can therefore say *when* the link stopped working rather
            than only that it has.
        organisation:
          type: object
          required:
            - name
            - language_code
          properties:
            name:
              type: string
              description: |
                Whose workspace this is. `Organisation` carries a single name
                rather than a bilingual pair, so there is nothing to localise.
            language_code:
              type: string
              description: The organisation's own default language, so the name renders in the right script and direction.
              examples:
                - en
                - ar
          additionalProperties: false
          description: |
            The organisation, named and nothing more. No identifier, no slug,
            no status — an anonymous endpoint that served them would answer
            questions about a tenant to anybody holding one link into it.
      description: |
        An invitation as the person holding the link may see it, before they
        have signed in. A deliberately narrower shape than
        `OrganisationInvitation`, which serves members already inside the
        organisation.
    PublicInvitationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - invitation
          properties:
            invitation:
              $ref: '#/components/schemas/PublicInvitation'
        meta:
          $ref: '#/components/schemas/Meta'
    AcceptedInvitationEnvelope:
      type: object
      description: |
        `membership` and `membership_created` are present and honest. B1
        marked the invitation and stopped there; PA1 supplied the membership
        write, so both now report what actually happened rather than a
        permanent `false`/`null`. The keys existed all along precisely so this
        change would not alter the wire shape.
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - invitation
            - membership
            - membership_created
          properties:
            invitation:
              $ref: '#/components/schemas/OrganisationInvitation'
            membership:
              oneOf:
                - type: object
                  required:
                    - id
                  properties:
                    id:
                      $ref: '#/components/schemas/Uuid'
                  additionalProperties: false
                - type: 'null'
              description: |
                The membership acceptance created — its identifier and nothing
                more. `GET /me` returns memberships hydrated with roles, scope
                and permissions, and a partial copy here would be a second
                answer to that question. Null when nothing was granted.
            membership_created:
              type: boolean
              description: Whether this acceptance created a membership. A response that quietly implied a provisioned member would let a client show somebody a workspace they cannot enter.
        meta:
          $ref: '#/components/schemas/Meta'
    B2bProvisioningEnvelope:
      type: object
      description: |
        What now exists. `agreement` is null when no version is in force —
        provisioning does not require a signed agreement, because approval is
        the gate and the terms may still be under negotiation.
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - application
            - organisation
            - customer_account
            - agreement
            - invitations_issued
          properties:
            application:
              $ref: '#/components/schemas/B2bApplicationReview'
            organisation:
              $ref: '#/components/schemas/Organisation'
            customer_account:
              $ref: '#/components/schemas/B2bCustomerAccount'
            agreement:
              oneOf:
                - $ref: '#/components/schemas/B2bAgreement'
                - type: 'null'
            invitations_issued:
              type: integer
              minimum: 0
              description: |
                How many invitations **this call** sent. Zero on a replay, so
                a caller can tell a repeat from fresh work. A named contact
                whose email address is malformed is skipped rather than
                aborting the transaction — the alternative would strand an
                approved company behind a field its applicant can no longer
                edit.
        meta:
          $ref: '#/components/schemas/Meta'
    SubscriptionStatus:
      type: string
      description: |
        `active` generates; `paused` keeps the balance and stops the cursor;
        `cancelled` and `completed` are terminal. A paused subscription has no
        `next_delivery_date` at all — nulling the cursor is what takes it out of
        the sweep, rather than a status predicate the sweep has to remember.
      enum:
        - active
        - paused
        - cancelled
        - completed
    SubscriptionDeliveryStatus:
      type: string
      description: |
        Where one delivery day got to. The three `skipped_*` values are kept
        apart on purpose: a customer skipping a Tuesday, a day nobody could
        assemble a safe meal for, and a day the kitchen could not serve are
        three different conversations, and only the first is the customer's
        doing.
      enum:
        - scheduled
        - generated
        - skipped_customer
        - skipped_no_safe_meal
        - skipped_unavailable
        - delivered
        - cancelled
    MealChoiceSource:
      type: string
      description: |
        Who chose the meal on a Free Selection day. `customer` picked it before
        the cut-off; `kitchen_default` filled the slot because they did not; and
        `substituted` means something chosen was unavailable or unsafe and
        generation replaced it. Collapsing these into one value would make the
        substitution audit unreadable, which is the one audit an allergy
        complaint needs most.
      enum:
        - customer
        - kitchen_default
        - substituted
    CreditMemoStatus:
      type: string
      description: |
        `recorded` means the platform has computed what is owed; `settled`
        means a human has told it the money changed hands. There is deliberately
        no `paid` or `refunded` — the platform has no payment rail, and a status
        implying it had one would be the first thing a report believed.
      enum:
        - recorded
        - settled
    SubscriptionBalance:
      type: object
      description: |
        What is left, what was used, and what is coming. **`skipped_days` is
        beside them rather than subtracted from them**: a skip costs nothing,
        and the number a customer most wants to check is that the two facts are
        both true at once.
      required:
        - status
        - balance_days_total
        - balance_days_consumed
        - remaining_days
        - per_day_minor
        - currency_code
        - weekdays
        - next_delivery_date
        - skipped_days
      properties:
        status:
          $ref: '#/components/schemas/SubscriptionStatus'
        balance_days_total:
          type: integer
        balance_days_consumed:
          type: integer
          description: A cached count of the delivery rows that consumed a day. The ledger is the truth; this is the index.
        remaining_days:
          type: integer
        per_day_minor:
          type: integer
          description: The grandfathered price actually paid, in minor units. A cancellation refund is this multiplied by the unused days.
        currency_code:
          $ref: '#/components/schemas/CurrencyCode'
        weekdays:
          type: array
          items:
            type: integer
            minimum: 1
            maximum: 7
        next_delivery_date:
          type:
            - string
            - 'null'
          format: date
        skipped_days:
          type: integer
    Subscription:
      type: object
      description: |
        A customer's own standing arrangement.

        **The captured price is shown in full** — all three columns of it — and
        that is a deliberate exception to its `Confidential` classification. The
        classification is a statement about *other* readers: what one customer
        was grandfathered at is not what the plan costs today, and publishing it
        would advertise a price nobody else can have. The person paying it is not
        another reader, and showing `captured_unit_price_minor` beside
        `effective_day_price_minor` is what makes "why am I paying 1 800 when the
        plan says 2 000" answerable on the screen instead of through support.

        **`captured_price_list_id` and `captured_price_list_item_id` are not
        served.** The provenance exists so the platform can explain a price years
        later; on a customer's own screen it names a kitchen's tariff structure
        to the person being charged by it. Nor is `organisation_id`,
        `created_by` or `updated_by`.

        **`lock_version` is served, unlike on the customer's order shape**,
        because this resource has a customer-facing writer. `If-Match` is
        honoured on every write and demanded by none.

        ## The named facts

        The row is a row of identifiers, which is the right shape for a table and
        the wrong one for the screen where somebody cancels something. `plan`,
        `kitchen`, `configuration`, `duration` and `delivery_address` are the
        joins that make it readable, and the only reason they were once absent
        was **projection thinness, not confidentiality** — nothing here is
        withheld from its owner because it is sensitive. They are gathered in a
        fixed number of queries for the whole list, so the unpaginated read did
        not become an N+1 in exchange.

        `duration.days` is the run that was bought and is **never**
        `balance_days_total`, which counts delivery days: a four-week plan
        delivering five weekdays buys twenty of them, not twenty-eight.

        `weekly_price_minor` is the effective per-day price times the number of
        delivery weekdays. It is served rather than left to the client because a
        client multiplying a price is a second implementation of the rule that
        decides what somebody pays.

        The sales channel's **name** is still not served — `SalesChannel` is
        classified `Internal`, and which desk a purchase was routed through is
        the kitchen's bookkeeping. Nor is `branch_id`, on the terms the customer
        order shape states: the kitchen's operating arrangements are not the
        customer's business.
      required:
        - id
        - status
        - catalogue_item_id
        - catalogue_item_variant_id
        - plan_duration_id
        - sales_channel_id
        - currency_code
        - captured_unit_price_minor
        - captured_discount_percent
        - effective_day_price_minor
        - captured_at
        - weekdays
        - delivery_window_code
        - customer_address_id
        - no_substitutions
        - balance_days_total
        - balance_days_consumed
        - remaining_days
        - next_delivery_date
        - pause_count
        - lock_version
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/SubscriptionStatus'
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          $ref: '#/components/schemas/Uuid'
        plan_duration_id:
          $ref: '#/components/schemas/Uuid'
        sales_channel_id:
          $ref: '#/components/schemas/Uuid'
        currency_code:
          $ref: '#/components/schemas/CurrencyCode'
        captured_unit_price_minor:
          type: integer
          description: What the tariff said per day at purchase, before the duration discount.
        captured_discount_percent:
          type:
            - string
            - 'null'
          description: |
            What the duration took off. **Null is a value**: it means nobody
            stated a discount, which is commercially different from stating
            there is none, and it is never returned as `0`.
        effective_day_price_minor:
          type: integer
          description: The grandfathered per-day price actually paid.
        captured_at:
          type: string
          format: date-time
        weekdays:
          type: array
          description: ISO weekdays, 1 = Monday through 7 = Sunday. Never empty.
          items:
            type: integer
            minimum: 1
            maximum: 7
        delivery_window_code:
          type:
            - string
            - 'null'
        customer_address_id:
          $ref: '#/components/schemas/Uuid'
        no_substitutions:
          type: boolean
          description: The opt-out — skip the slot rather than substitute. Per subscription, not per customer, because two arrangements can reasonably differ.
        balance_days_total:
          type: integer
        balance_days_consumed:
          type: integer
        remaining_days:
          type: integer
        next_delivery_date:
          type:
            - string
            - 'null'
          format: date
        paused_at:
          type:
            - string
            - 'null'
          format: date-time
        resumed_at:
          type:
            - string
            - 'null'
          format: date-time
        pause_count:
          type: integer
        cancelled_at:
          type:
            - string
            - 'null'
          format: date-time
        cancellation_reason:
          type:
            - string
            - 'null'
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
        lock_version:
          type: integer
        created_at:
          type:
            - string
            - 'null'
          format: date-time
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
        plan:
          oneOf:
            - $ref: '#/components/schemas/SubscriptionPlanRef'
            - type: 'null'
        kitchen:
          oneOf:
            - $ref: '#/components/schemas/SubscriptionKitchenRef'
            - type: 'null'
        configuration:
          oneOf:
            - $ref: '#/components/schemas/SubscriptionConfigurationRef'
            - type: 'null'
        duration:
          oneOf:
            - $ref: '#/components/schemas/SubscriptionDurationRef'
            - type: 'null'
        delivery_address:
          oneOf:
            - $ref: '#/components/schemas/SubscriptionAddressRef'
            - type: 'null'
        starts_on:
          type:
            - string
            - 'null'
          format: date
          description: |
            The first delivery the ledger holds. Falls back to the next
            generation date for an arrangement bought moments ago whose first day
            has not been generated, and to the purchase date beyond that.
        weekly_price_minor:
          type: integer
          description: The effective per-day price times the number of delivery weekdays.
        allows_free_selection:
          type: boolean
        change_cutoff_hours:
          type: integer
        skipped_dates:
          type: array
          description: Days that were scheduled and skipped, whoever skipped them. They consumed nothing.
          items:
            type: string
            format: date
        chosen_catalogue_item_ids:
          type: array
          description: |
            The dishes the customer chose for themselves, distinct. Never the
            kitchen's defaults or its substitutions — presenting either as a
            choice would tell somebody they picked a meal they did not.
          items:
            $ref: '#/components/schemas/Uuid'
        balance:
          $ref: '#/components/schemas/SubscriptionBalance'
    SubscriptionPlanRef:
      type: object
      description: The plan, named. One server-chosen language, per §4.8.
      required:
        - id
        - name
        - slug
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        slug:
          type: string
    SubscriptionKitchenRef:
      type: object
      description: |
        The kitchen that cooks it. `name` is served as the business registered
        it, whatever `Accept-Language` says — `organisations.name` is a single
        column, and a business name is not translated.
      required:
        - id
        - name
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
    SubscriptionConfigurationRef:
      type: object
      description: The matrix cell — the variant of the plan that was bought.
      required:
        - id
        - code
        - name
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        name:
          type: string
          description: Empty when the kitchen named the variant in neither language.
    SubscriptionDurationRef:
      type: object
      description: |
        The run that was bought. `days` is the duration's own length and is
        never `balance_days_total`, which counts delivery days.
      required:
        - id
        - code
        - kind
        - days
        - name
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        kind:
          type: string
          enum:
            - one_off
            - fixed_days
        days:
          type:
            - integer
            - 'null'
          description: Null exactly when `kind` is `one_off`.
        name:
          type: string
    SubscriptionAddressRef:
      type: object
      description: |
        Where it goes, in full, to the person who typed it. A masked address
        book is a screen on which nobody can tell which address is which — the
        same reason the customer's own address list serves every line.
      required:
        - id
        - label
        - line_one
        - line_two
        - delivery_area_id
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        label:
          type:
            - string
            - 'null'
        line_one:
          type: string
        line_two:
          type:
            - string
            - 'null'
        building:
          type:
            - string
            - 'null'
        floor:
          type:
            - string
            - 'null'
        apartment:
          type:
            - string
            - 'null'
        directions:
          type:
            - string
            - 'null'
        delivery_area_id:
          $ref: '#/components/schemas/Uuid'
    SubscriptionEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - subscription
          properties:
            subscription:
              $ref: '#/components/schemas/Subscription'
        meta:
          $ref: '#/components/schemas/Meta'
    SubscriptionsEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Subscription'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              properties:
                count:
                  type: integer
    CreditMemo:
      type: object
      description: |
        What a kitchen owes a customer whose subscription was cancelled early —
        a record of an **obligation**, never of a payment.

        `settlement` is stated on the wire rather than left to be inferred from
        the status vocabulary. It is the one sentence a customer cancelling most
        needs: no money has moved, and somebody will be in touch. A client left
        to infer it would eventually render `recorded` as "refunded".
      required:
        - id
        - reason
        - unused_days
        - per_day_minor
        - amount_minor
        - currency_code
        - status
        - settlement
        - recorded_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        reason:
          type: string
          examples:
            - subscription_cancelled
        unused_days:
          type: integer
        per_day_minor:
          type: integer
          description: The effective price actually paid, so the discount already enjoyed on delivered days is not clawed back.
        amount_minor:
          type: integer
        currency_code:
          $ref: '#/components/schemas/CurrencyCode'
        status:
          $ref: '#/components/schemas/CreditMemoStatus'
        settlement:
          type: string
          enum:
            - manual
          description: Always `manual` in this phase. The platform has no payment rail.
        recorded_at:
          type: string
          format: date-time
        settled_at:
          type:
            - string
            - 'null'
          format: date-time
    SubscriptionMealChoice:
      type: object
      description: |
        What fills one slot of one day. `replaced_catalogue_item_id` is what the
        choice stands in for when generation substituted it — the customer is
        owed the difference between "you chose this" and "we sent this instead".

        `unsafe_allergen_classes` is deliberately **not** served: a list of
        allergen classes attached to a named person's delivery is health data,
        and what a customer needs from this row is that a substitution happened,
        not a restatement of their medical profile.
        **`name` is server-derived and there is no request field for it.** The
        write sends a `catalogue_item_id` alone, which is the only defensible
        arrangement on a surface this close to safety: a caller-supplied label
        could disagree with the dish actually recorded, and the screen showing it
        would be describing food nobody is going to cook. It is resolved from the
        catalogue in the caller's language (§4.8) and is an empty string for an
        item since removed — an honest blank rather than a stale name kept alive
        by a client.
      required:
        - slot
        - sequence
        - catalogue_item_id
        - name
        - source
      properties:
        slot:
          type: string
          examples:
            - lunch
        sequence:
          type: integer
          description: The kitchen that sells "lunch, twice" — a slot alone would collapse it.
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        name:
          type: string
          description: The dish, in the caller's language. Empty when the item no longer exists.
          examples:
            - Grilled Chicken & Quinoa
        source:
          $ref: '#/components/schemas/MealChoiceSource'
        replaced_catalogue_item_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        replaced_name:
          type:
            - string
            - 'null'
          description: |
            What the substituted dish was called. Null when nothing was
            substituted, so a client can tell "no substitution" from "a
            substitution whose item has since gone".
    SubscriptionDelivery:
      type: object
      description: |
        One delivery day — the row a balance day is spent on.

        **`consumed` is stored, not derived from `status`**, and reading it is
        the only correct way to ask whether a day was spent. A client
        recomputing it from the status would eventually disagree with the
        balance.
      required:
        - id
        - delivery_date
        - status
        - consumed
        - meals
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        delivery_date:
          type: string
          format: date
        delivery_window_code:
          type:
            - string
            - 'null'
        status:
          $ref: '#/components/schemas/SubscriptionDeliveryStatus'
        consumed:
          type: boolean
        skip_reason:
          type:
            - string
            - 'null'
        order_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        generated_at:
          type:
            - string
            - 'null'
          format: date-time
        settled_at:
          type:
            - string
            - 'null'
          format: date-time
        meals:
          type: array
          items:
            $ref: '#/components/schemas/SubscriptionMealChoice'
    SubscriptionDeliveriesEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/SubscriptionDelivery'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - count
                - balance
              properties:
                count:
                  type: integer
                balance:
                  $ref: '#/components/schemas/SubscriptionBalance'
    SubscriptionSkipEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - delivery
            - balance
          properties:
            delivery:
              $ref: '#/components/schemas/SubscriptionDelivery'
            balance:
              $ref: '#/components/schemas/SubscriptionBalance'
        meta:
          $ref: '#/components/schemas/Meta'
    SubscriptionCancellationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - subscription
            - credit_memo
          properties:
            subscription:
              $ref: '#/components/schemas/Subscription'
            credit_memo:
              description: |
                Null when nothing is owed, and that is not the same as an empty
                object: a zero memo would be an obligation somebody eventually
                tries to settle, so none is written.
              oneOf:
                - $ref: '#/components/schemas/CreditMemo'
                - type: 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    PlanQuote:
      type: object
      description: |
        What a configuration would cost. **A quote and not a hold**: nothing is
        reserved, nothing is written, and the number is guaranteed only for as
        long as the tariff behind it stands. `POST /subscriptions` re-resolves it
        and captures *that* answer.

        All three numbers travel, because the discount is the reason somebody
        chose the longer run and a screen showing only the final figure cannot
        say what it saved them. `price_list_id` and `price_list_item_id` do not.

        **The three non-price fields are what make this one read instead of
        eight.** A configurator needs the price, the days it may deliver on, and
        the two rules that decide what it may offer — whether the customer picks
        the dishes, and how long before a delivery a change stops being
        accepted. Splitting them across separate reads produced the seven-probe
        weekday hack this operation exists to delete.
      required:
        - currency_code
        - days
        - list_price_minor
        - discount_percent
        - per_day_minor
        - total_minor
        - duration_code
        - duration_kind
        - available_weekdays
        - allows_free_selection
        - change_cutoff_hours
      properties:
        currency_code:
          $ref: '#/components/schemas/CurrencyCode'
        days:
          type: integer
        list_price_minor:
          type: integer
          description: The per-day list price, before the duration discount.
        discount_percent:
          type:
            - string
            - 'null'
          description: Null means nobody stated one. Never returned as `0`.
        per_day_minor:
          type: integer
        total_minor:
          type: integer
        duration_code:
          type:
            - string
            - 'null'
          description: |
            The kitchen's own code for the run that was resolved, echoed so a
            configurator can show its wording rather than the day count it asked
            with.
        duration_kind:
          type:
            - string
            - 'null'
          enum:
            - one_off
            - fixed_days
            - null
        available_weekdays:
          type: array
          description: |
            ISO weekdays this kitchen delivers on, `1` Monday to `7` Sunday,
            ascending. Derived from its active delivery windows, where an empty
            `weekdays` means every day. A kitchen with no active window is
            **unconstrained rather than closed** and answers all seven: telling
            somebody a kitchen delivers on no day, when nobody has configured
            days at all, would empty a configurator on a configuration gap.
          items:
            type: integer
            minimum: 1
            maximum: 7
        allows_free_selection:
          type: boolean
          description: Whether the customer picks the dishes, or the kitchen decides.
        change_cutoff_hours:
          type: integer
          description: |
            How long before a delivery a change stops being accepted. The plan's
            own `change_cutoff_hours`, not the platform default — the 24-hour
            rule is configuration.
    PlanQuoteEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - quote
          properties:
            quote:
              $ref: '#/components/schemas/PlanQuote'
        meta:
          $ref: '#/components/schemas/Meta'
    SubscriptionScheduleRow:
      type: object
      description: |
        One day of the kitchen's forward book. **`basis` is the field that must
        never be ignored**: an `actual` day has a delivery row and possibly an
        order behind it, and a `projected` one is computed from the weekday
        pattern and the remaining balance — a customer who has not yet had the
        chance to skip.

        No customer name, no address and no allergen list: a production planner
        counts portions per window per day, and every one of those would be
        personal data on a screen that does not need it.
      required:
        - delivery_date
        - subscription_id
        - customer_account_id
        - basis
        - status
      properties:
        delivery_date:
          type: string
          format: date
        subscription_id:
          $ref: '#/components/schemas/Uuid'
        customer_account_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        delivery_window_code:
          type:
            - string
            - 'null'
        basis:
          type: string
          enum:
            - actual
            - projected
        status:
          $ref: '#/components/schemas/SubscriptionDeliveryStatus'
    SubscriptionScheduleEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/SubscriptionScheduleRow'
        meta:
          allOf:
            - $ref: '#/components/schemas/Meta'
            - type: object
              required:
                - from
                - to
                - count
                - daily_counts
              properties:
                from:
                  type: string
                  format: date
                to:
                  type: string
                  format: date
                count:
                  type: integer
                daily_counts:
                  type: object
                  description: Deliveries per day, with skipped and cancelled days already excluded. The number a production plan is built from.
                  additionalProperties:
                    type: integer
    CreateSubscriptionRequest:
      type: object
      description: |
        Checkout-shaped on purpose: the same three coordinates a plan is sold on
        — the plan, the configuration cell, the duration — plus everything a
        delivery needs.

        **No price field exists.** Every amount is decided by the server at
        purchase and captured onto the row; a body that could state a per-day
        price would be a client quoting the server its own prices, and the first
        thing anybody would do with it is quote a lower one.
      required:
        - sales_channel_id
        - catalogue_item_id
        - catalogue_item_variant_id
        - plan_duration_id
        - customer_address_id
        - weekdays
      properties:
        sales_channel_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        catalogue_item_id:
          $ref: '#/components/schemas/Uuid'
        catalogue_item_variant_id:
          $ref: '#/components/schemas/Uuid'
        plan_duration_id:
          $ref: '#/components/schemas/Uuid'
        customer_address_id:
          $ref: '#/components/schemas/Uuid'
        weekdays:
          type: array
          minItems: 1
          maxItems: 7
          items:
            type: integer
            minimum: 1
            maximum: 7
        delivery_window_code:
          type:
            - string
            - 'null'
          maxLength: 40
        no_substitutions:
          type: boolean
        start_from:
          type:
            - string
            - 'null'
          format: date
          description: |
            The earliest delivery date wanted. A **request, not a promise**: the
            server moves it forward to the first weekday the subscription
            actually delivers on that is still outside the plan's change window,
            because a first delivery already inside its own cut-off would be an
            order the customer could never have changed.
    SkipSubscriptionDayRequest:
      type: object
      required:
        - date
      properties:
        date:
          type: string
          format: date
          description: |
            The day to skip. A **date**, not a delivery identifier: the day
            usually has no row yet, so an endpoint keyed on one could only ever
            skip the day it is nearly too late to skip.
    UpdateSubscriptionAddressRequest:
      type: object
      required:
        - customer_address_id
      properties:
        customer_address_id:
          $ref: '#/components/schemas/Uuid'
    UpdateSubscriptionWindowRequest:
      type: object
      required:
        - delivery_window_code
      properties:
        delivery_window_code:
          type:
            - string
            - 'null'
          maxLength: 40
          description: |
            Present and nullable, which is the difference between "no
            preference" and "leave it alone": a `PUT` replaces the field whole,
            so `null` is how a customer says they no longer mind.
    UpdateSubscriptionWeekdaysRequest:
      type: object
      required:
        - weekdays
      properties:
        weekdays:
          type: array
          minItems: 1
          maxItems: 7
          items:
            type: integer
            minimum: 1
            maximum: 7
    ReplaceSubscriptionChoicesRequest:
      type: object
      description: |
        One day's chosen meals, replaced whole. An **empty `meals` array is a
        real payload**: it is a customer saying "I have not chosen; send me the
        kitchen's default", and a merge-shaped write could not express it.
      required:
        - date
        - meals
      properties:
        date:
          type: string
          format: date
        meals:
          type: array
          maxItems: 12
          items:
            type: object
            required:
              - slot
              - catalogue_item_id
            properties:
              slot:
                type: string
                maxLength: 20
                examples:
                  - lunch
              sequence:
                type:
                  - integer
                  - 'null'
                minimum: 1
                maximum: 20
              catalogue_item_id:
                $ref: '#/components/schemas/Uuid'
              catalogue_item_variant_id:
                oneOf:
                  - $ref: '#/components/schemas/Uuid'
                  - type: 'null'
    SubscriptionChoicesEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - delivery_date
            - meals
          properties:
            delivery_date:
              type: string
              format: date
            meals:
              type: array
              items:
                $ref: '#/components/schemas/SubscriptionMealChoice'
        meta:
          $ref: '#/components/schemas/Meta'
    CancelSubscriptionRequest:
      type: object
      properties:
        reason:
          type:
            - string
            - 'null'
          maxLength: 40
          pattern: ^[a-z][a-z0-9_]*$
          description: |
            A short vocabulary word, never prose. The journal writes it into
            audit metadata, and a free-text box filled in by somebody in an
            unhappy moment is how a name or a complaint about a named member of
            staff ends up in a column nobody classified. Defaults to
            `customer_request`.
    ClosureScope:
      type: string
      description: |
        Two values, and they are not degrees of the same thing.
        `marketing_opt_out` is a preference change that deletes nothing; `full`
        is an erasure. They live on one journey because they are one journey
        from the customer's side.
      enum:
        - marketing_opt_out
        - full
    ClosureReasonCode:
      type: string
      description: |
        Why somebody is leaving. A fixed vocabulary rather than free text: the
        answer is read by machines as often as by people — "how many customers
        left because we do not deliver to them any more" is a count, not a
        search — and free text is how personal data ends up in a column nobody
        classified. `other` is last and deliberately vague: a vocabulary without
        an escape hatch makes people pick the nearest wrong answer, which is
        indistinguishable from a real signal.
      enum:
        - no_longer_needed
        - too_expensive
        - moving_away
        - dietary_needs_unmet
        - service_quality
        - privacy_concerns
        - duplicate_account
        - other
    ClosureRequestStatus:
      type: string
      description: |
        `requested` is asked but unproven; `verified` is the passcode accepted
        and bound to this request; `scheduled` is the grace window running — the
        state that exists so "I changed my mind" has somewhere to land.
        `completed` and `cancelled` are terminal.
      enum:
        - requested
        - verified
        - scheduled
        - completed
        - cancelled
    ClosureBlockerStatus:
      type: string
      description: |
        **Four values, and two of them are not "fine".** `not_applicable` means
        nothing was checked — because no module in this deployment could answer
        — and it is never a synonym for `clear`: a closure screen saying "no
        outstanding payments" when no payment module exists has not checked
        anything, and the customer cannot tell the difference. `advisory` means
        something real *was* found and it does not stop the closure; today that
        is an unsettled credit memo, which is a debt the kitchen owes the
        customer and therefore not something the kitchen may hold their erasure
        hostage to.
      enum:
        - blocking
        - clear
        - advisory
        - not_applicable
    ClosureBlockerVerdict:
      type: object
      description: |
        What one blocker found. **`count` and `reason` are present on every
        verdict, including the clear ones**, so a screen rendering "2 open
        orders" from `count` and "no subscriptions module is bound" from
        `reason` reads the same shape in both cases — which is what stops the
        honest answer being the one that needs a special case.

        `reason` is a code, never a sentence: it is rendered in the customer's
        language and it goes into audit metadata. The vocabulary is each
        blocker's own — `orders_in_flight`, `subscriptions_live`,
        `deliveries_upcoming`, `memberships_live`, `signatures_pending`,
        `credit_memos_unsettled`, and the `*_module_absent` family.
      required:
        - code
        - status
        - count
        - reason
      properties:
        code:
          type: string
          enum:
            - open_orders
            - active_subscriptions
            - organisation_memberships
            - pending_b2b_signatures
            - unsettled_credit_memos
            - wallet_balance
            - payment_methods
        status:
          $ref: '#/components/schemas/ClosureBlockerStatus'
        count:
          type: integer
        reason:
          type:
            - string
            - 'null'
    ClosureAcknowledgement:
      type: object
      description: |
        One shape for both scopes and every stage, because the screen behind it
        is one screen. A client that had to branch on the response *type* would
        be a client that renders the blocked case by accident.

        **The verdicts travel with the acknowledgement rather than behind a
        second call.** "Why can I not close my account" is the only question a
        refusal raises, and answering it in a separate request is how a screen
        ends up saying "you cannot close your account" with no explanation while
        the second call is in flight.

        `destination_masked` is server-authored from the contact point and never
        echoed from client input: the client is told where the code went, and is
        not in a position to be told anything it could have made up.
      required:
        - request_id
        - scope
        - status
        - blocked
        - blockers
        - verification_required
      properties:
        request_id:
          $ref: '#/components/schemas/Uuid'
        scope:
          $ref: '#/components/schemas/ClosureScope'
        status:
          $ref: '#/components/schemas/ClosureRequestStatus'
        blocked:
          type: boolean
          description: Whether anything found **stops** the closure. An `advisory` verdict does not set this.
        blockers:
          type: array
          items:
            $ref: '#/components/schemas/ClosureBlockerVerdict'
        verification_required:
          type: boolean
        destination_masked:
          type:
            - string
            - 'null'
        expires_in_seconds:
          type:
            - integer
            - 'null'
        scheduled_for:
          type:
            - string
            - 'null'
          format: date-time
    ClosureAcknowledgementEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - closure_request
          properties:
            closure_request:
              $ref: '#/components/schemas/ClosureAcknowledgement'
        meta:
          $ref: '#/components/schemas/Meta'
    LiveClosureRequestEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - closure_request
            - blockers
          properties:
            closure_request:
              description: Null when nothing is in flight. There is at most one, by index.
              oneOf:
                - $ref: '#/components/schemas/ClosureAcknowledgement'
                - type: 'null'
            blockers:
              type: array
              description: |
                Re-evaluated on every call and filled in **regardless** of
                whether a request exists, so a closure screen can render "here is
                what would stand in your way" before anybody asks. A cached
                verdict would miss a subscription started this morning.
              items:
                $ref: '#/components/schemas/ClosureBlockerVerdict'
        meta:
          $ref: '#/components/schemas/Meta'
    CancelledClosureRequestEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - closure_request
          properties:
            closure_request:
              type: object
              required:
                - request_id
                - scope
                - status
              properties:
                request_id:
                  $ref: '#/components/schemas/Uuid'
                scope:
                  $ref: '#/components/schemas/ClosureScope'
                status:
                  $ref: '#/components/schemas/ClosureRequestStatus'
                cancelled_at:
                  type:
                    - string
                    - 'null'
                  format: date-time
                cancelled_because:
                  type:
                    - string
                    - 'null'
        meta:
          $ref: '#/components/schemas/Meta'
    OpenClosureRequestRequest:
      type: object
      description: |
        **`scope` is required and has no default.** A default would have to be
        either `marketing_opt_out`, so a client with a bug silently downgrades an
        erasure somebody asked for, or `full`, so a client with a bug erases
        somebody who asked only to stop being emailed. Neither is a mistake this
        endpoint should be able to make on a caller's behalf.
      required:
        - reason_code
        - scope
      properties:
        reason_code:
          $ref: '#/components/schemas/ClosureReasonCode'
        reason_note:
          type:
            - string
            - 'null'
          maxLength: 2000
          description: |
            Optional prose, and the one free-text field in the journey. Never
            written into an audit row, and deleted at finalisation with
            everything else. Optional for **every** reason: insisting somebody
            explain themselves before they may leave is a dark pattern wearing a
            form label.
        scope:
          $ref: '#/components/schemas/ClosureScope'
        delivery_channel:
          description: |
            Where the passcode should go. SMS and WhatsApp have no provider in
            this deployment, so asking for one answers
            `422 otp.channel_unavailable` with the channels that would work — a
            better dead end than silently sending an email to somebody waiting
            on a text.
          oneOf:
            - $ref: '#/components/schemas/OtpDeliveryChannel'
            - type: 'null'
    VerifyClosureRequestRequest:
      type: object
      required:
        - code
      properties:
        code:
          type: string
          minLength: 4
          maxLength: 12
          description: |
            There is no `request_id` beside it. The request is in the path and
            the challenge is bound to it on the row, which is what stops a code
            obtained for one closure finalising another.
    OffboardingStatus:
      type: string
      description: |
        Where a wind-up has got to. `revoking` is separate from `completed` on
        purpose: revoking every member's access is the step that can partially
        fail, and a status collapsing it into the terminal state would make a
        half-revoked organisation look finished.
      enum:
        - requested
        - notice_served
        - settlement_pending
        - awaiting_signoff
        - signed_off
        - revoking
        - archiving
        - completed
        - cancelled
    OffboardingTrigger:
      type: string
      description: |
        Four different stories about the same act, and every one of them is what
        somebody will read a year later when the company asks why. Required, with
        no default: a default would put one of those stories on the record
        without anybody choosing it.
      enum:
        - contract_end
        - termination
        - non_renewal
        - client_request
    SettlementStatus:
      type: string
      description: |
        `cleared` means every check answered clear or stated why it could not
        run; `waived` means somebody with the second permission set an
        outstanding position aside and wrote down why. The two are never
        collapsed — a waiver recorded as a clearance erases the only difference
        a dispute turns on.
      enum:
        - pending
        - cleared
        - waived
    SettlementCheckOutcome:
      type: string
      description: |
        `not_applicable` carries a `reason` and is **not** a green tick: a check
        that cannot tell "we looked and found nothing" from "nobody was there to
        look" produces a settlement summary that reads as an all-clear when it is
        a gap.
      enum:
        - clear
        - outstanding
        - not_applicable
    SettlementCheck:
      type: object
      required:
        - check
        - outcome
      properties:
        check:
          type: string
          examples:
            - open_buyer_orders
        outcome:
          $ref: '#/components/schemas/SettlementCheckOutcome'
        reason:
          type:
            - string
            - 'null'
        detail:
          type:
            - string
            - 'null'
    Offboarding:
      type: object
      description: |
        A corporate wind-up in progress.

        **`notice_period_days` and `effective_on` are copied onto the row** at
        the moment notice is served, never read back through the agreement, so an
        amendment signed next week cannot shorten notice already served.

        **The signatory block travels in full** — name, title, the consent
        wording accepted, the document digest — because evidence that cannot be
        read is not evidence. The two session hashes do **not**: they exist for
        corroboration inside the platform, and serving them would put a stable
        pseudonymous identifier for a named person onto an API response.

        `allowed_transitions` travels so a wind-up screen renders buttons without
        reimplementing the state machine — the one that disagrees after the first
        change.
      required:
        - id
        - organisation_id
        - status
        - requested_at
        - settlement
        - signoff
        - revocation
        - archive
        - lock_version
        - allowed_transitions
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        b2b_agreement_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        status:
          $ref: '#/components/schemas/OffboardingStatus'
        trigger:
          oneOf:
            - $ref: '#/components/schemas/OffboardingTrigger'
            - type: 'null'
        reason:
          type:
            - string
            - 'null'
        reason_note:
          type:
            - string
            - 'null'
        requested_by:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        requested_at:
          type: string
          format: date-time
        notice_period_days:
          type:
            - integer
            - 'null'
        notice_served_at:
          type:
            - string
            - 'null'
          format: date-time
        effective_on:
          type:
            - string
            - 'null'
          format: date
        settlement:
          type: object
          required:
            - status
            - checks
          properties:
            status:
              $ref: '#/components/schemas/SettlementStatus'
            note:
              type:
                - string
                - 'null'
            checks:
              type: array
              items:
                $ref: '#/components/schemas/SettlementCheck'
            started_at:
              type:
                - string
                - 'null'
              format: date-time
            resolved_at:
              type:
                - string
                - 'null'
              format: date-time
            waived_by:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
            waiver_reason:
              type:
                - string
                - 'null'
        signoff:
          type: object
          properties:
            awaiting_since:
              type:
                - string
                - 'null'
              format: date-time
            signed_off_at:
              type:
                - string
                - 'null'
              format: date-time
            signed_off_by:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
            signatory_name:
              type:
                - string
                - 'null'
            signatory_title:
              type:
                - string
                - 'null'
            consent_statement:
              type:
                - string
                - 'null'
            document_sha256:
              type:
                - string
                - 'null'
        revocation:
          type: object
          properties:
            started_at:
              type:
                - string
                - 'null'
              format: date-time
            completed_at:
              type:
                - string
                - 'null'
              format: date-time
            memberships_revoked:
              type:
                - integer
                - 'null'
            tokens_deleted:
              type:
                - integer
                - 'null'
        archive:
          type: object
          properties:
            started_at:
              type:
                - string
                - 'null'
              format: date-time
            summary:
              type:
                - object
                - 'null'
              additionalProperties:
                type: integer
            legal_entity_retained:
              type:
                - boolean
                - 'null'
              description: Said out loud rather than left to be inferred from the summary's silence. Keeping the company record was a decision, not an omission.
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
        cancelled_at:
          type:
            - string
            - 'null'
          format: date-time
        cancelled_by:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        cancellation_reason:
          type:
            - string
            - 'null'
        lock_version:
          type: integer
        allowed_transitions:
          type: array
          items:
            $ref: '#/components/schemas/OffboardingStatus'
    OffboardingEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - offboarding
          properties:
            offboarding:
              $ref: '#/components/schemas/Offboarding'
        meta:
          $ref: '#/components/schemas/Meta'
    RecordExportStatus:
      type: string
      description: |
        `expired` is a **state rather than a comparison** against `expires_at`:
        expiry here is an act, the transition is what deletes the bytes, and the
        status is the record of it having happened. The row survives, because
        deleting it would erase the evidence that an export was ever made.
      enum:
        - requested
        - building
        - ready
        - delivered
        - expired
        - failed
    RecordExport:
      type: object
      description: |
        A company's own records, packaged.

        **No `disk`, no `path` and no permanent URL.** The object key is one half
        of a credential, and the only route to the bytes is this operation with
        `?purpose=`, which mints a fresh fifteen-minute signature and records who
        took a copy. `sha256` does travel — it is what a recipient checks the
        download against, and it names nothing.
      required:
        - id
        - organisation_id
        - status
        - format
        - requested_at
        - download_count
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        b2b_offboarding_id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        status:
          $ref: '#/components/schemas/RecordExportStatus'
        format:
          type: string
          examples:
            - zip
        requested_by:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        requested_at:
          type: string
          format: date-time
        started_at:
          type:
            - string
            - 'null'
          format: date-time
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
        byte_size:
          type:
            - integer
            - 'null'
        sha256:
          type:
            - string
            - 'null'
        row_counts:
          type:
            - object
            - 'null'
          additionalProperties:
            type: integer
        manifest:
          type:
            - object
            - 'null'
          additionalProperties: true
        expires_at:
          type:
            - string
            - 'null'
          format: date-time
        downloaded_at:
          type:
            - string
            - 'null'
          format: date-time
        download_count:
          type: integer
        purged_at:
          type:
            - string
            - 'null'
          format: date-time
        failure_reason:
          type:
            - string
            - 'null'
        download_url:
          type: string
          description: |
            Present **only** when `?purpose=` was sent. In the envelope and never
            a 302: a redirect would put an expiring credential into browser
            history, the referrer chain and every proxy log on the way to the
            bucket.
        download_url_expires_at:
          type: string
          format: date-time
          description: |
            Computed before the URL is minted, so it is always at or before the
            moment the signature actually stops working. A client that stops
            trusting the link a fraction early retries; one that trusts it too
            long gets an opaque 403 from object storage.
    RecordExportEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - export
          properties:
            export:
              $ref: '#/components/schemas/RecordExport'
        meta:
          $ref: '#/components/schemas/Meta'
    StartOffboardingRequest:
      type: object
      description: |
        **`organisation_id` is in the body rather than the path**, and the reason
        is what the prefix already means: `/platform/b2b` is reached with the
        *platform operator's* organisation selected, so a `{organisation}`
        segment would be a second organisation in the same request with
        completely different meaning. Here they cannot agree — the whole point is
        that an operator is acting on somebody else's company — so it goes in the
        body where it reads as a subject rather than as a scope.
      required:
        - organisation_id
        - trigger
      properties:
        organisation_id:
          $ref: '#/components/schemas/Uuid'
        trigger:
          $ref: '#/components/schemas/OffboardingTrigger'
        reason_note:
          type:
            - string
            - 'null'
          maxLength: 2000
        effective_on:
          type:
            - string
            - 'null'
          format: date
          description: |
            A negotiated end date, overriding the computed notice date — the
            parties agreed March 31st, and the alternative would be an operator
            editing the agreement to make the arithmetic come out. Never in the
            past: backdating notice is not a data-entry convenience, it is a claim
            that somebody was told earlier than they were.
    WaiveOffboardingSettlementRequest:
      type: object
      required:
        - reason
      properties:
        reason:
          type: string
          minLength: 10
          maxLength: 2000
          description: |
            Required prose. A waiver nobody explained is not a waiver, and "ok"
            is not something anybody can review a year later. The minimum does
            not make somebody thoughtful; it makes the empty gesture take as long
            as typing a real one. There is no `authorised_by` field — who is
            waiving is the authenticated caller, and a body that could name
            somebody else would be an audit row attributing a decision to a
            person who did not make it.
    SignOffOffboardingRequest:
      type: object
      description: |
        **`otp_challenge_id` names a challenge and is not proof.** The server
        demands that it exists, carries purpose `b2b_signatory`, has been
        *consumed*, and belongs to the person signing — and all four failures
        answer identically, so a caller holding somebody else's identifier learns
        only that it did not work. There is no `otp_verified` field and there
        never will be: a flag a caller can assert records its own claim rather
        than an observation.

        `consent_statement` is the exact wording shown on screen, echoed back
        rather than looked up server-side, because the evidence has to be what
        the person actually read.
      required:
        - signatory_name
        - signatory_title
        - consent_statement
        - otp_challenge_id
      properties:
        signatory_name:
          type: string
          maxLength: 160
        signatory_title:
          type: string
          maxLength: 120
        consent_statement:
          type: string
          maxLength: 2000
        document_sha256:
          type:
            - string
            - 'null'
          pattern: ^[0-9a-f]{64}$
        otp_challenge_id:
          $ref: '#/components/schemas/Uuid'
    CancelOffboardingRequest:
      type: object
      required:
        - reason
      properties:
        reason:
          type: string
          minLength: 10
          maxLength: 2000
          description: '"Why did we stop offboarding Acme" is a question somebody asks when Acme is still trading eighteen months later.'
    PlatformKitchenCatalogueCounts:
      type: object
      description: |
        What a kitchen has in its catalogue, in the two shapes an operator
        reads it in.

        **The type breakdown counts published rows only.** `meals`, `products`
        and `plans` answer "what is this kitchen selling", and a half-written
        draft is not an answer to that question. `published` and `draft` beside
        them are the **progress pair** and count everything, which is why the
        three type counts do not add up to `published` when a kitchen sells
        something outside those three types, and why `draft` is never included
        in any of them.
      required:
        - meals
        - products
        - plans
        - published
        - draft
      properties:
        meals:
          type: integer
          minimum: 0
          description: Published meals.
        products:
          type: integer
          minimum: 0
          description: Published products.
        plans:
          type: integer
          minimum: 0
          description: Published subscription plans.
        published:
          type: integer
          minimum: 0
          description: Every published catalogue item, whatever its type.
        draft:
          type: integer
          minimum: 0
          description: Every item still being written — draft, or sent back for review.
    PlatformKitchenOwner:
      type: object
      description: |
        An **active** membership holding the `organisation_owner` role.
        Ownership on this platform has always been a membership with the owner
        role assigned to it rather than a column or a flag, so this asks the
        same question the permission checker asks. Ended and suspended
        memberships are excluded: they are people who *used* to be owners, and
        counting them would report an owned kitchen nobody can get into.
      required:
        - membership_id
        - user_id
        - name
        - email
        - status
      properties:
        membership_id:
          $ref: '#/components/schemas/Uuid'
        user_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type:
            - string
            - 'null'
          description: From the person's profile. Null until they have one — an invited owner who has not yet completed sign-up has an address and no name.
        email:
          type: string
          format: email
        status:
          type: string
          description: The membership status. Always `active` in this list, and present rather than assumed so a client never has to infer it.
    PlatformKitchenBranch:
      type: object
      description: |
        One of the kitchen's locations, as the platform console lists them.
        Deliberately thin: opening hours, cut-offs and delivery zones are the
        kitchen's own operational surface, and an operator console that carried
        them would become the accidental canonical read for a workspace it does
        not own.
      required:
        - id
        - name
        - city
        - country_code
        - timezone
        - status
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        city:
          type:
            - string
            - 'null'
        country_code:
          type: string
          minLength: 2
          maxLength: 2
        timezone:
          type: string
          examples:
            - Asia/Beirut
        status:
          type: string
          enum:
            - active
            - closed
    PlatformKitchenSummary:
      type: object
      description: |
        The queue row — enough to scan a list of kitchens and decide which one
        to open.

        Two shapes rather than one with a flag, the rule this platform writes
        everywhere: a summary and a detail separated by a boolean argument is
        one careless call away from returning every owner's email address on a
        screen that only wanted names. So the owner *list* lives on the detail
        shape and only `owner_count` appears here.

        This is not the organisation shape a member reads about their own
        tenant. That one answers "which organisation am I signed into?" and its
        capability list is that member's view of themselves; this answers "what
        has the platform got here?" about somebody else's tenant, and the two
        are expected to diverge.
      required:
        - id
        - slug
        - name
        - status
        - country_code
        - default_currency_code
        - default_language_code
        - branch_count
        - active_branch_count
        - owner_count
        - catalogue
        - suspended_at
        - lock_version
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        slug:
          type: string
          description: Unique across the platform and immutable. Accepted in place of the identifier on every path in this family.
          examples:
            - verdant-kitchen
        name:
          type: string
          description: |
            The single name `organisations` stores. The Arabic name collected
            at creation goes where the platform already keeps bilingual
            organisation naming — the branch and the sales channel — because
            this table has one name column and inventing a second here would be
            a copy that drifts.
          examples:
            - Verdant Kitchen
        status:
          type: string
          enum:
            - active
            - suspended
            - pending
            - closed
        country_code:
          type: string
          minLength: 2
          maxLength: 2
        default_currency_code:
          type: string
          minLength: 3
          maxLength: 3
        default_language_code:
          type: string
          minLength: 2
          maxLength: 2
        branch_count:
          type: integer
          minimum: 0
        active_branch_count:
          type: integer
          minimum: 0
          description: |
            Counted separately rather than inferred, because the gap between
            the two is the thing worth seeing — a kitchen with four branches
            and none of them open is a support call waiting to happen.
        owner_count:
          type: integer
          minimum: 0
          description: |
            How many **active** owner memberships this kitchen has. Zero is a
            legitimate value and not an error: the last owner may be revoked
            deliberately, and a kitchen created by the console has none until
            somebody accepts an invitation.
        catalogue:
          $ref: '#/components/schemas/PlatformKitchenCatalogueCounts'
        suspended_at:
          type:
            - string
            - 'null'
          format: date-time
          description: When the platform withdrew this kitchen from trading. Null whenever it is not suspended — reactivation clears it.
        lock_version:
          type: integer
          minimum: 0
          description: |
            The optimistic-concurrency validator, served as the `ETag` on the
            single-kitchen read and required back as `If-Match` on suspend and
            reactivate. Present on the row as well as in the header so a list
            client can tell that two rows moved without re-reading either.
        created_at:
          type:
            - string
            - 'null'
          format: date-time
    PlatformKitchen:
      allOf:
        - $ref: '#/components/schemas/PlatformKitchenSummary'
        - type: object
          description: |
            The kitchen's own screen: everything the summary carries, plus the
            people, the places and — when it is suspended — the note and who
            wrote it.

            The `allOf` is a documentation convenience. The server builds the
            two shapes through two named methods rather than by adding to or
            subtracting from one, for the reason stated on the summary: a
            narrow shape produced by unsetting keys from a wide one is one
            careless refactor away from leaking.
          required:
            - suspension_reason
            - suspended_by
            - owners
            - branches
          properties:
            suspension_reason:
              type:
                - string
                - 'null'
              maxLength: 500
              description: |
                The operator's own words, or null. Prose rather than a code,
                and optional rather than required — a mandatory field would
                produce "n/a" and "see ticket", and a column full of those is
                worse than a column full of nulls because it looks like it has
                been filled in. Cleared on reactivation; the audit log keeps
                the history.
            suspended_by:
              oneOf:
                - $ref: '#/components/schemas/Uuid'
                - type: 'null'
              description: The platform user who suspended this kitchen. Cleared on reactivation, for the same reason the note is.
            owners:
              type: array
              description: The active owner memberships, oldest first. Personal data, which is why reading this endpoint is audited as a classified access.
              items:
                $ref: '#/components/schemas/PlatformKitchenOwner'
            branches:
              type: array
              description: Every branch, whatever its status, by name.
              items:
                $ref: '#/components/schemas/PlatformKitchenBranch'
    PlatformKitchenEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - kitchen
          properties:
            kitchen:
              $ref: '#/components/schemas/PlatformKitchen'
        meta:
          $ref: '#/components/schemas/Meta'
    PlatformKitchenSummaryEnvelope:
      type: object
      description: |
        What the lifecycle actions return. The summary rather than the detail,
        because suspending a kitchen is not a reason to re-serve every owner's
        email address; a console that needs the full row re-reads it.
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - kitchen
          properties:
            kitchen:
              $ref: '#/components/schemas/PlatformKitchenSummary'
        meta:
          $ref: '#/components/schemas/Meta'
    CreatePlatformKitchenRequest:
      type: object
      description: |
        **One request creates five things, in one transaction: the
        organisation, one active main branch, the `kitchen_production`
        capability, a `b2c_web` sales channel coded `web-shop`, and a `pos`
        sales channel coded `desk`.** All five or none.

        A kitchen that is only an organisation row is a kitchen whose workspace
        refuses on the first screen: the organisation context resolves a
        *branch* before the kitchen area will open, the capability is what the
        workspace reads to know what it is, and a published meal has to be
        available on a channel before a price can resolve or a marketplace
        listing can appear. Creating the organisation and leaving an operator
        to add the rest afterwards was rejected because it makes the console's
        success message a lie for the twenty minutes before somebody notices. A
        new kitchen works immediately; it simply has nothing in it yet.

        The fifth is the counter. The Order Desk sells through its own channel
        rather than through the web shop, so that a kitchen may put the catering
        trays across the counter without putting them on the site — and a
        kitchen provisioned without one can take a web order but not a walk-in,
        which is half a business. Both channels open empty: a kitchen created a
        moment ago has no articles and no tariffs, so there is nothing to
        assign to either.

        The wholesale channel is deliberately **not** created. A kitchen that
        has never traded with a company does not need a B2B channel sitting
        inactive in its list.

        **Both names are required.** `organisations` stores a single `name`, so
        the Arabic goes onto the sales channel, which carries both. Collecting
        it now is the point: the operator typing the English name knows the
        Arabic one, and a screen that asks three weeks later gets a
        transliteration.
      required:
        - name_en
        - name_ar
        - slug
        - country_code
        - default_currency_code
        - default_language_code
        - timezone
        - branch_name
      additionalProperties: false
      properties:
        name_en:
          type: string
          maxLength: 160
          description: The organisation's name.
          examples:
            - Verdant Kitchen
        name_ar:
          type: string
          maxLength: 160
          description: The Arabic name, stored on the branch and the sales channel rather than on the organisation, which has one name column.
        slug:
          type: string
          maxLength: 100
          pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
          description: |
            Lowercase words joined by single hyphens. Unique across the
            platform and immutable; a taken one is `422` with
            `details.fields.slug`, decided inside the insert rather than by a
            separate check that would race it.
          examples:
            - verdant-kitchen
        country_code:
          type: string
          minLength: 2
          maxLength: 2
          description: Must exist in the platform country table.
          examples:
            - LB
        default_currency_code:
          type: string
          minLength: 3
          maxLength: 3
          description: Must exist in the platform currency table.
          examples:
            - USD
        default_language_code:
          type: string
          minLength: 2
          maxLength: 2
          description: Must exist in the platform language table.
          examples:
            - en
        timezone:
          type: string
          maxLength: 64
          description: |
            A plain bounded string, deliberately not checked against the
            runtime's timezone list: that list changes with whatever tzdata the
            container happens to ship, and a validator that started refusing
            `Asia/Dubai` after a base-image bump would be one nobody could
            debug.
          examples:
            - Asia/Beirut
        branch_name:
          type: string
          maxLength: 160
          description: The name of the one active branch created alongside the organisation.
        city:
          type:
            - string
            - 'null'
          maxLength: 120
          description: The branch's city. The only optional field here.
    SuspendPlatformKitchenRequest:
      type: object
      description: |
        One optional field, and it is optional on purpose — see the operation
        for why a reason vocabulary would not survive contact with the things
        people actually need to write here.
      additionalProperties: false
      properties:
        reason:
          type:
            - string
            - 'null'
          maxLength: 500
          description: |
            Free prose, shown to nobody outside the platform. Blank and absent
            are the same thing: both store null, because a column that
            distinguishes "" from nothing is a distinction no reader can use.
    InvitePlatformKitchenOwnerRequest:
      type: object
      description: |
        **No `role_code`.** The role is always `organisation_owner` and is not
        a caller's choice — that is precisely what separates this from
        `POST /organisations/{organisation}/invitations`, which exists for a
        member of an organisation to invite anybody to any role inside it.
      required:
        - email
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
          maxLength: 160
          description: A live offer already outstanding to this address is superseded rather than refused.
        name:
          type:
            - string
            - 'null'
          maxLength: 160
          description: |
            Used **only** in the greeting line of the invitation email, and
            never stored. The person's real name arrives with their profile
            when they accept, and a name typed by an operator into an
            invitation form would be a second, worse copy of it.
        message:
          type:
            - string
            - 'null'
          maxLength: 1000
          description: A sentence that travels with the invitation. An invitation with no context is an email people assume is phishing, and rightly.
    PlatformKitchenOwnerInvitationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - invitation
            - mailed
          properties:
            invitation:
              $ref: '#/components/schemas/OrganisationInvitation'
            mailed:
              type: boolean
              description: |
                Whether the invitation email actually went out. Reported rather
                than assumed: a transport failure does not lose the invitation,
                but it does mean nobody has been told, and a console that
                showed an unqualified success would leave an operator waiting
                for a reply to a message that was never sent.
        meta:
          $ref: '#/components/schemas/Meta'
    PlatformKitchenOwnerRevocationEnvelope:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          type: object
          required:
            - membership
            - remaining_owners
          properties:
            membership:
              type: object
              description: The membership as it now stands — `ended`, and still there. Nothing was deleted.
              required:
                - id
                - user_id
                - status
              additionalProperties: false
              properties:
                id:
                  $ref: '#/components/schemas/Uuid'
                user_id:
                  $ref: '#/components/schemas/Uuid'
                status:
                  type: string
                  description: Always `ended` on a successful revocation.
                  examples:
                    - ended
            remaining_owners:
              type: integer
              minimum: 0
              description: |
                How many active owners this kitchen has left. **Zero is a
                legitimate outcome**, not a refusal: the last owner may be
                revoked because the platform acts deliberately, and this number
                exists so the console can warn rather than the API refusing.
        meta:
          $ref: '#/components/schemas/Meta'
